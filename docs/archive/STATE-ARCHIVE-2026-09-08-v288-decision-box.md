# STATE archive — the V288 rev 2 decision box as it stood 2026-09-07 → 2026-09-08 (superseded by the V289 box)

*A record, not an instruction.*

## ✈ THE DECISION, IN ONE PLACE  — updated 2026-09-07 (**V288 rev 2 BUILT: a SETPOINT PRE-FILTER code cave on the V282 base, 0 cal bytes, adversarial pass PASSED on rev 2; V287 driven and REJECTED by the operator — "still felt the grinding", back on V282**)

**ON THE CAR: V282** (the operator flashed V287 rev 2, still felt the grind, and returned to V282; he rejects V287's class outright: *"we're lowering the Kp value acting on angular acceleration, even though our PI on angular acceleration already feels well tuned"* — no build may reduce the feedback P/D terms or the V282 authority).

**BUILT, UNFLASHED, FLASH CANDIDATE: V288 rev 2 = V282 + ONE CODE CAVE, no calibration byte moved.**
`39990-TVA,A160-V288R2-V282BASE-SPFILT.K4.EINIT-KP.FLAT.Y0-CAVE.R24CMP.B6-SPSIGN.B5-MAP.LINEAR.TO6X.FEEDBACK46080.TORQUE.TAP-0x13000-0x100000.rwd`
rwd sha256 **43efd0c98446d331a5b529cbaf93f1173e61e9daa132903cf3467078a7207714** · image `_v288r2_…_plain_image.bin` sha256 **94cabdefd39a103ad10ec34b9c64b6ac94d6552b80bfe7cc192a8977cbbdbd8c** · script `analysis-2020accord/builds/v108_plus/build_v288_tva.py` (K is one constant; K=3 fallback re-cuts clean). Diff vs V282: **91 bytes, 5 regions** — hook 0x29D72 (`st.h r16,-0x6a32[gp]` → `jr 0xC4C00`), 0x14A cave exit 0xC4BD6 → 0xC4BDC, telemetry rung 0xC4BDC–FD (34 B), filter cave 0xC4C00–2F (48 B), CRC 0xC4FFC. 🛑 **V288 rev 1 (bc8a5b1a… / 862cb525…) is SUPERSEDED-DO-NOT-FLASH** (renamed on disk): it failed the adversarial pass twice. Page: https://claude.ai/code/artifact/fb9a34f0-9b1c-4850-b673-187c100ce6fd · prereg + verdicts: `docs/review/ADVERSARIAL-V288-PREREG-2026-09-07.md`.

### WHAT V288 DOES (all read from the built image)
The LKAS rate PID (FUN_00028ea6, 1 kHz) forms E = 32·sp − fb where sp is the assist-map output (±1032 on the live selector 7). V288 low-passes **sp only**, before the ×32: `y += (sp − y) >> 4` with a +1 fix so y reaches sp exactly (V850 `sar` floors; the naive form sticks up to 15 counts short). State = gp-0x6a32, the cell the displaced store already wrote and nothing reads (census by seven methods, GATE 1 PASS). Corner **10.3 Hz**, DC gain exactly 1, group delay **15 ms**, per-tick kick into the D term **÷16** on a capped command step (ΔE 3936 → 224; the D clamp binds at 640). **Engage init:** on the first tick after any hook-skipping tick the cave reads Honda's own sentinel (gp-0x6cf8 == 0x7FFFFFFF, written at 0x2A18C on all three skip routes and read by Honda at 0x29E5E to suppress the D term's stale history) and passes raw sp through, so tick 0 of every engage is byte-identical to V282. **A reference pre-filter is outside the loop:** nothing feeds y back, the rate loop's return ratio is unchanged byte for byte, GATE 2 is untouched by construction. Instrument: **0x14A byte 4 bit 5 = sign(y)** (replaces V282's |r24| ≥ |aggregator| comparator; bit 6 = |r24| ≥ |T| kept; bits 0–2 are STOCK Honda — rev 1 overwrote bit 0); sp_raw is reconstructable offline from the logged 0xE4 (memoryless chain), so bit-5 zero crossings vs sign(sp_raw) read the group delay directly, and bit-5 duty 0.000/1.000 over ≥ 20 s engaged = the rung did not execute.

### WHY THIS CLASS — the wire evidence (r39/r3a/r3c on V282; `rlog-tools/studies/grind/WIRE-0XE4-20HZ-2026-09-07.md`)
- The 0xE4 command **changes on 92 % of 100 Hz frames**; there is **no 20 Hz staircase and no 4-frame hold** [EVIDENCE]. The "stairstep held 4 frames" premise from outside is falsified.
- It is **slew-capped at 123 raw/frame by openpilot's own limit** (0.03 × 4096, verified in the fork's Honda carcontroller) — the census's "top-1 % step ≥ 122" WAS that cap. Capped frames are 7–60× enriched inside grind episodes, 3.9–11.5× in the 0.5 s BEFORE onset, in same-sign runs up to 854 ms, and **~54 % of D-clamp binds land on a capped frame** [EVIDENCE].
- The command's 20.03 Hz line is an **ECHO** (openpilot's unfiltered 100 Hz angle measurement re-transmits the wheel's ring), 9 % share of the PID sum's 20 Hz vs 63 % feedback — **open-loop shares** (the 1 kHz mirror feeds the logged rate back), NOT closed-loop bounds.
- **Grind #1 is a RUNG BELL, not a self-sustained oscillation**: 97.6 % of 127 episodes decay after their peak, rise AND decay faster than phase-randomised same-spectrum surrogates (r39 p < 1e-4), nothing rails, ζ_eff ≈ 0.026 at 20 Hz. ⇒ cutting the excitation shrinks the ring; the 8–13 % direct share is a FLOOR of the closed-loop benefit [EVIDENCE for the classification, BELIEF for the magnitude].
- V287 attacked the same excitation INSIDE the loop (clipping feedback D in loaded strata — its adversary-B FAIL) and the operator still felt the grind. **No build V38→V287 ever filtered the reference**; V50–V52C low-passed the torque SENSOR (null, cost to feel), V43 the observer lane, V40/V41 the output governor. V288 is genuinely new (`docs/research/LINEAGE-COMMAND-PATH-FILTER-CHECK-2026-09-07.md`).

### ADVERSARIAL PASS (four independent surfaces, FAIL criteria written first)
- **Rev 1 FAILED twice.** B: the three hook-skipping routes (0x29A5C/0x29A64 → 0x2A164, 0x29A70 → 0x2A0C6) zero every PID state cell but not the filter's, so y froze across disengagement and seeded re-engage (up to 11 ms railed in the wrong direction after an override). A: the telemetry rung wrote 0x14A b4.0, a stock Honda flag (frame builder 0x55B06 runs before the cave hook).
- **Rev 2: A PASS-with-conditions (instrument renamed, decoders must read b4.5 = sign(y)) · B PASS-with-conditions · C PASS · D PASS.** Independent rebuild reproduces the hash; CRC cell recomputed (f007c24a); a V850 interpreter executing the cave bytes matches the script's mirror over 107,363 cases; all displacements land; r10/r26/lp preserved; registers r6/r9 provably dead at the hook. **B's standing condition: openpilot's outer-loop phase margin has never been measured** — the 15 ms costs ≈ 20° at the 3.9 Hz crossover, 7.5 % of the 0.2 s SteerDelay it models; a stated cost, not a cleared one. Carry-forward: the Kp/Kd LERP is indexed by the RAW command, not sp_f (inert while Kp is flat 248).

### ✈ RISK BEFORE THE DRIVE, and how it will be read
Authority does not rise. Steady-state assist is unchanged (DC gain exactly 1). The one new cost is 15 ms of reference lag on openpilot's loop: watch for a **new slow wallow at 1–3 Hz on engaged straight-road driving**; if felt, back to V282 (or the K=3 re-cut: 7 ms, kick ÷8). Feedback-dominated strata (hands-on bar > 700, |ang| > 60, wheel > 25 deg/s) are NOT expected to move; a null there is not evidence against the lever. Reading: Q-liveness first (b4.5 duty and lag vs the offline mirror), then D-bind duty and the 18–22 Hz envelope on capped-frame onsets vs V282's r39/r3a/r3c; the operator scores the grind.

### ✈ NEXT — in order
1. **Operator decides on V288 rev 2.** Flash only the file named above; kill openpilot first.
2. openpilot-side, no firmware: the 20 Hz echo enters through the unfiltered angle measurement and the friction term's steep slope — a measurement low-pass or lower SteerFriction on the fork is a separate lever (`docs/research/STARPILOT-FORK-COMMAND-PIPELINE-2026-09-07.md`).
3. Golden model: **the LKAS rate PID stage is absent** (no E = 32·sp − fb, no Kp/Kd banks, no 0xC61B6); `lkas_setpoint_prefilter` is in (contract 88 symbols, hash unchanged) but has no caller. Add the PID stage; then the governor MIN-fold and the oscillation detector.
4. Update the rlog 0x14A decoders: b4.5 = sign(y) on V288 (was |r24| ≥ |aggregator|).
5. Open: outer-loop PM measurement; `0xC61C0/C2/C4` intent unattributed since V36; the 12 bytes at 0xC4FF0 (partly identified as CRC-chain fields).

### CORRECTIONS OF RECORD (2026-09-07)
1. "The 0xE4 command is a 100 Hz staircase … 32·Δsp is an impulse train" — half right: it is a continuously updating signal slew-capped at 123/frame; the impulse is the per-tick landing of each (often capped) frame step on one of ten ticks.
2. The kit's 1 kHz mirror is open-loop; every "share of T's 20 Hz" figure is a share, not a closed-loop prediction.
3. Adversary D's rev 1 "y decays to 0 while disengaged" is retracted: the hook is skipped once the engagement ramp reaches 0; during the ramp-down (0.10 s / 2.05 s) it still runs with sp = 0.
4. 0x14A byte 4 bits 0–2 are stock Honda (gp-0x679a/679b/6799); "bits 2–0 are written by nothing" (V288 rev 1 script) is false.
5. The operator is on his own fork, `openpilots/raayyymond-StarPilot/StarPilot` @ Dom (the 2026-09-02 "plain StarPilot" convention is superseded).
