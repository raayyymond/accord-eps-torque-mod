# MEMORY — PART 8

🛑 **Split off MEMORY-PART7 on 2026-08-29, at 183 KB against the 256 KB cap.** A file past
that cap loads with its TAIL SILENTLY TRUNCATED and no warning — which is how `STATE.md` hit
506 KB and the golden model hit 310 KB before anyone noticed. **The newest entries are here,**
so a truncation of PART7 can no longer hide them.

⚠ **This page is part of the mandatory read.** `CLAUDE.md` and `memory/MEMORY.md` both point
at it; PART7 holds the older half of the same series.

- 🛑⭐★★★★★ [**THE RATCHET IS A TORQUE-PATH EFFECT, NOT A MOTION OSCILLATION — it re-aims the whole chain**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — same slope-corrected excess across ALL channels, 1080 pooled engaged-creep windows, null ~3.9×: **`cs_tq` ratchet 13.5× / grind 5.1× · `cs_rate` ratchet 1.7× (BELOW NULL) / grind 7.3× · `sc_tq` 1.2× / 3.3×.** ✅ **The GRIND is a genuine motion oscillation — STRONGEST IN RATE** ⇒ the closed-loop model holds and **the notch is well aimed.** 🛑 **The RATCHET is NOT in the motion**: 13.5× in torque, **1.7× in rate.** The wheel is not oscillating at 8 Hz — the TORQUE is. That is a **friction/stiction** signature. ➕ **CONSEQUENCE 1: the detector watches `gp-0x6c2c`, an ACCELERATION EMA — no 8 Hz in rate ⇒ none in its derivative ⇒ the amplitude gate is not crossed either.** V191/V192/**V193** are inert for the ratchet on **both** frequency AND amplitude. ➕ **CONSEQUENCE 2: the FLYING build runs `0xC40D2` K1 = 1020 vs Honda's 102 — TEN TIMES the modelled Coulomb friction**, and Coulomb friction is precisely what makes torque ripple without motion. **V177's K1 revert, carried on V189–V194, is aimed straight at it.** ⚠ `0xC40BC` knee is 3000 vs Honda 600, never reverted (was 600 at V108) — shallower ramp, aligned with the low-friction requirement, but non-stock and unattributed. ⭐ **RE-ORDERS THE RECOMMENDATION TO V189**: notch (motion/grind) + inertia and K1 reverts (torque/ratchet), no sign bets, nothing that can change normal driving. Everything after V189 aims at the motion path, where the ratchet is not.
- 🛑★★★★★ [**Coulomb REFUTED, and “the ratchet is not in the motion” was OVER-CLAIMED — two of three claims died to their own controls**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **(1)** Coulomb must flip sign at rate zero-crossings; with a MATCHED control (dwelling at similarly low |rate|, no sign change): **RATCHET cross/dwell 3.73 [2.97,4.35] but the GRIND CONTROL 4.96 [3.84,5.92] — HIGHER** ⇒ crossings excite everything; **no Coulomb-specific preference. The friction explanation is NOT supported.** **(2)** the rate-scaling test is CONFOUNDED — ratchet/ctrl rises 63.7→211 peaking at 20–40 °/s **but the grind control does the same**, and ratchet/grind stays flat 6.2–8.5 ⇒ the rise is the NORMALISATION. **(3)** 🛑 **I OVER-CLAIMED:** the first coherence run gave **1.000 for everything INCLUDING the shuffled surrogate** (degenerate — one sub-window per segment). Redone properly (2048-sample episodes, 256-sample sub-windows): **cs_tq×cs_rate 0.888 @8 Hz / 0.842 @20 Hz vs shuffled floor 0.049/0.053** ⇒ **torque and motion ARE strongly coupled at 8 Hz.** The ratchet's motion is **SMALL, NOT ABSENT** — the rack is stiff at 8 Hz — consistent with the recorded Q 14–29 motor/rack resonance, and **not** a sensor artifact. ⇒ **`gp-0x6c2c` DOES contain 8 Hz, so the detector's amplitude gate is NOT provably uncrossable; V193 is not dead and V194's probe is still the decider.** ⊕ Unchanged: **the GRIND is a real motion oscillation, strongest in RATE (7.3×)** ⇒ the notch stays well aimed. ⊕ `d(cs_ang)/dt` gives 1.5×/2.6× — corroborates the small ratchet but degrades the grind (differentiating a quantised angle amplifies HF noise), so **`cs_rate` is the better motion instrument.**
- ✅★★★★★ [**V195 — the grind notch re-fitted on `cs_rate`, the channel where the grind actually lives**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V195.md) — V188 fitted the notch by minimax over **`cs_tq` (driver torque)**, but the cross-channel work showed the grind is a MOTION oscillation strongest in **`cs_rate`** (7.3× vs 5.1×) ⇒ **the fit had used the weaker instrument.** Re-fit on `cs_rate` over 67 routes (peaks p10 16.33 / med 20.12 / p90 22.15 Hz vs torque's 15.74 / 19.92 / 21.68): **V188/V189 19.40 Hz r 0.9300 → median 15.0×, p90 10.4×, −3.8°** vs **V195 19.75 Hz r 0.9000 → median 21.5×, p90 14.3×, −4.6°** ⇒ **1.43× more grind power removed for 0.8° more phase.** ⊕ **The substantive change is the POLE RADIUS, not the centre**: 0.9300→0.9000 widens the notch because the rate-channel peak spread is wider; the 0.35 Hz shift is minor. ✅ V195 = V189 + 4 float32 cells, 11 payload bytes, 30/30, DC 1.000003, max|H| 1.7177, added lag −0.27°@1 Hz / −0.77°@3 Hz. `a3ea8683df48c6b3…` ⇒ **REPLACES V189 as the recommendation** — same levers, same risk, better aim. ➕ **THE LESSON: fit a filter against the channel the symptom is STRONGEST in. A 1.43× improvement came from changing the measurement channel, not the firmware understanding.**
- ✅★★★★ [**V195's shoulder is CLEAR and its wider notch is GENTLER than V189's — the re-fit was not a trade**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a wider pole (0.9000 vs 0.9300) changes the lag profile, so V188's shoulder check does not transfer. On `cs_rate`: **zero frequencies have excess>2 AND |H|>0.5 AND lag<−30°** — the danger pattern needs all three, and for a notch lag and attenuation grow together; the worst points (16.2–17.6 Hz) have |H| already at 0.22–0.35 where lag peaks. ➕ **V195's lag is SMALLER than V189's at every shoulder frequency** (−37.1° vs −44.3° at 18.95 Hz) because a lower-Q notch transitions more gently ⇒ **V195 dominates V189 on BOTH axes: 1.43× more grind removed AND less shoulder lag.** Tool: `rlog-tools/score/notch_shoulder_check.py`.
- ✅★★★★★ [**V196 — the last frequency-selective RATCHET lever: halve the ENGAGED anti-damping inertia**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V196.md) — the biquad is spent on the grind, so the only other ω² lever is **`gp-0x6b26`** (built from the acceleration EMA ⇒ **67× stronger at 8.2 Hz than 1 Hz**). Dose ladder: **FLYING V122 engaged Y = (−29490,−17202,−16000) ≈ 3× Honda and it ratchets · V189–V195 = Honda (−9830,−5734,−1966) · V196 = HALF Honda (−4915,−2867,−983)**. ✅ **ENGAGED ONLY** — m24 (`0xD6A64`) and m26 (`0xD7A54`) are **distinct records**, so only `0xD7A5C..0xD7A61` moves and **manual driving is byte-identical**; that is the V74 pattern the TVCA4 memory endorses. ⚠ **It deliberately RE-CREATES an engaged/manual asymmetry that earlier work removed — the difference is DIRECTION** (the removed ones made engaged WORSE; this makes it BETTER). Recorded so nobody “fixes” it. ✅ **THE TRADE: less negative apparent inertia ⇒ the wheel feels closer to its true inertia AT HIGH FREQUENCY ONLY; ZERO at DC ⇒ no LKAS authority cost and no added steering weight.** Half-dose rather than zero because the trade is real. `f904e43a1f4ccb94…` ⚠ Sign rests on the ★★★★★ anti-damping reading + the dose ladder; if inverted the ratchet worsens ⇒ revert to V195. ➕ **The shelf now separates by symptom: V195 = grind only, no sign bets · V196 = grind + ratchet, one sign bet · V194 = the detector probe.**
- ✅★★★★★ [**“Peak command oscillation” needs NO separate lever — and the low 1 Hz coherence was an EXPOSURE artefact**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **a lead/lag test is unusable at 20 Hz with 100 Hz data** (one period = 5 samples ⇒ lag resolves only modulo half a period, openpilot's latency is 1–3 periods) — established BEFORE running it. Coherence `sc_tq × cs_rate`: **pooled 0.115 @1 Hz / 0.180 @20 Hz vs floor 0.049**, but **hands-OFF the 1 Hz figure rises to 0.338 (~7× floor)** ⇒ **the low pooled value was MIXED EXPOSURE, not an authority finding** (flagged as a question, resolved by stratifying). 20 Hz coupling is weak but real (0.181, 3.8× floor) and **unchanged by hands** ⇒ not driver-related. ➕ **The decisive fact is a PRIOR: the LKAS lane is a ~1–5 Hz low-pass ⇒ openpilot CANNOT COMMAND 20 Hz** ⇒ the command's 20 Hz content is **REACTING** to the grind ⇒ **fixing the grind fixes it; no separate lever.** ⚠ Only **31 hands-off / 1 hands-on** 20.5 s episodes exist corpus-wide — hands-on remains the blind spot. ⊕ **ALL THREE SYMPTOMS NOW MAPPED: grind → the notch · ratchet → inertia half-dose + K1 revert · command oscillation → fixed by the grind · authority → sequenced behind the grind result (0xC6CD0 6×→8×).** Tool: `rlog-tools/score/command_coupling_at_grind.py`.
- 🛑⭐★★★★★ [**The 8× LKAS gain was tried and abandoned THREE times, not once — and the lineage gap is now partially closed**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/BUILD-LINEAGE-PART5-V122-ONWARD-MEASURED.md) — backfilling V122–V196 from the IMAGES surfaced undocumented history: **`0xC6CD0` V101 4×→8× (grind returned) · V102 8×→6× · **V124 6×→8×** · **V137 back to 6×** · **V142 6×→8×** · **V147 back to 6×**.** ⇒ **8× was reached and backed away from THREE separate times.** The sequenced authority recommendation (*confirm the grind fix, then 6×→8×*) is therefore entering **territory that has already failed three times** — the sequence is still right (the notch is what breaks the gain/grind coupling, and that IS new) but **the prior on 8× is far worse than the V101 story alone suggests.** ⊕ Also: **`0xC40BC`** ramp knee **3000→3600 at V151, reverted at V152** — another undocumented try-and-back-off. ✅ **`docs/BUILD-LINEAGE-PART5-V122-ONWARD-MEASURED.md`** is **generated, not narrated** — every row a byte diff between images; **43 cells, 57 builds, 7.4 KB** — restoring the lineage's one critical function: **`grep <address>` now says which build moved a cell** (it silently returned nothing for V122–V196, which is how the 10× K1 and the 72 dead bytes stayed invisible). ⚠ It carries NO reasoning; **not every build has an image** (10 gaps), so a change across a gap means *“at or before this build”*; diff against the STOCK IMAGE for anything load-bearing.
- 🛑⭐★★★★★ [**The CLOSED-loop notch prediction is WEAKER than the open-loop one — my figures were overstated**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every notch estimate multiplied the spectrum by `|H|²`, treating the filter as feedforward. The grind is closed-loop, so the measured engaged/manual ratio identifies loop gain: `R = 1/|1−L|²`. On `cs_rate` 15–25 Hz: **engaged 3.2 · manual 0.3 · OPEN-loop 0.2 (16.9×) · CLOSED-loop 0.4 (7.7×) · engaged/manual 11.3×**. 🛑 **The open-loop estimate attenuates the DISTURBANCE FLOOR too — but that floor is road and plant, and the notch is in the ASSIST path, so it cannot remove it. The loop can only give back what it added.** ⇒ **the 21.5×/15.0×/14.3× figures for V188/V195/V196 are OPEN-LOOP and OVERSTATE the achievable reduction; the honest prediction is ~7.7× with 11.3× as the CEILING.** The design RANKING is unaffected (all scored the same way), so V195's re-fit still beats V188's. ⊕ The notch DOES fully break the loop at its centre: `g=0.0025` at 19.73 Hz drives `R` 27.8 → **1.00**, exactly manual; the ceiling bites away from centre (`g` 0.26–0.60 at 22–25 Hz ⇒ `R` 1.5–2.4). ⚠ **DISCREPANCY TO FLAG, NOT RECONCILE: this gives L ≈ 0.78–0.81, an amplified resonance, NOT the near-unity instability implied by the recorded “9,200× less power LKAS-off”.** Different channel/band/conditions — do not treat them as the same measurement; only a drive settles which is right. ⊕ `L` taken real positive (a power ratio cannot identify phase) ⇒ right order, not exact. Tool: `rlog-tools/score/closed_loop_notch_prediction.py`.
- ⭐★★★★★ [**The notch delivers MOST where the grind is WORST — and a single number was the wrong output all along**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — per route, the engaged/manual grind ratio (15–25 Hz, `cs_rate`, 30 routes) runs **p10 2.3× · p25 7.7× · p50 24.6× · p75 57.9× · p90 102.9× · max 397.1× (r9e)**. ✅ **That RECONCILES the discrepancy**: the recorded *“9,200× LKAS-off”* and my pooled *11.3×* are different points on a very wide distribution, **neither is “the” number.** ✅ **The notch's benefit TRACKS SEVERITY** because loop gain is highest where the grind is worst: **worst quartile 87.8× → notch gives 51.9×; best quartile 2.7× → 2.4×** (r9e 397→224, r96 168→102, r95 158→90). ⇒ **operator-facing: on bad drives expect ~50× less grind power, on mild ones ~2×** — the right shape for a fix. 🛑 **THE NUMBER WAS CORRECTED TWICE AND THE PROGRESSION IS THE LESSON: 21.5× (open-loop — ranks designs but attenuates the disturbance floor a notch cannot remove) → 7.7× (closed-loop but POOLED — right method, wrong aggregation; a median spectrum underweights the bad routes) → 2.4×..51.9× (closed-loop PER ROUTE). Record the RANGE, never a point estimate.**
- 🛑🛑★★★★★ [**RETRACTION: per-route severity via engaged/manual ratio is BROADBAND-CONTAMINATED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — partial correlation killed it: **corr(log ratchet, log grind) = +0.748**, survives controlling 0.5–3 Hz activity (+0.793) but **COLLAPSES to −0.177 controlling the 30–45 Hz control band**, because **corr(ratchet, ctrl)=+0.914 and corr(grind, ctrl)=+0.859** ⇒ the engaged/manual ratio is elevated **BROADBAND** on some routes; it measures LKAS-on-vs-off EXPOSURE, not band severity. ❌ **WITHDRAWN: the per-route severity distributions (grind “87.8× worst quartile”, ratchet “466×”), “the notch gives ~52× on bad drives”, AND the closed-loop L ≈ 0.78–0.81 / 7.7× estimate**, all of which rested on that ratio. ➕ **ROOT CAUSE: the kit's scorers use a SLOPE-CORRECTED EXCESS — band vs its own local background on the SAME windows — precisely because it is band-specific. I drifted onto a ratio and inherited an exposure confound. Use the excess.** ✅ **REDONE (67 routes, engaged only, null 3.9×): RATCHET p50 60.7 / p75 186.9 · GRIND p50 31.6 / p75 64.8 · 99 % above null for both** ⇒ and **corr(log excess, log excess) = +0.304, weakly related** ⇒ **the common-cause hypothesis is REFUTED; separate levers remain right** (which is what V196 does). ⊕ Per-route figures exceed the pooled 13.5×/7.3× because pooling a median spectrum attenuates each route's peak — **the per-route number is what a single drive shows.** 🛑 **The notch's on-car effect is now UNPREDICTED; the drive measures it.** What survives: the design ranking and the firmware facts.
- ✅⭐★★★★★ [**A SOUND notch prediction, and it is the drive's pre-registration**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the 21.5× open-loop score was wrong because it multiplied the WHOLE spectrum by `|H|²`, attenuating a broadband floor the assist path cannot touch; the closed-loop fix went out with the contaminated ratio. **The defensible model splits `P = B + X`** (B = smooth background, X = the excess the loop amplifies), passes **only X** through the filter, and reports **the same slope-corrected excess the scorer prints**. ✅ **cs_rate GRIND 15–25 Hz, 67 routes, null ~3.9×: measured p25/p50/p75 = 22.5/31.6/64.8 → after V195 = 1.5/2.2/4.1**; reduction p25 9.7×, **median 12.7×**, p75 23.4×. ⭐ **49 of 67 routes (73 %) fall BELOW the null ⇒ on ~3 drives in 4 the grind should read as “not real”.** ➕ It also **confirms V195's re-fit on a sound statistic**: 73 % vs V188's 64 % below null, 12.7× vs 10.3×. ⊕ **Replaces the withdrawn 21.5× / 7.7× / 52× figures, and cannot repeat the floor-attenuation error because it never touches B.** 🛑 **PRE-REGISTERED: below 3.9× ⇒ gone · 4–10× ⇒ working but incomplete (the p75–p90 tail) · above ~15× ⇒ the notch is NOT reaching the signal, check the arm `0xC649B` and the engagement gate before touching the design · peak moves to 24–28 Hz ⇒ displaced, re-centre.** Tool: `rlog-tools/score/notch_prediction_excess_only.py`.
- ⭐★★★★★ [**The FLYING build's inertia term is a SATURATED RELAY — the best ratchet mechanism yet, and it needs no sign assumption**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `gp-0x6b26 = clamp(((accel·L)>>6)·273>>18, ±511)`, so the clamp binds at `|accel| > 511/scale`: **FLYING L=−29490 ⇒ saturates at 1065 · Honda −29830 ⇒ 3195 · V196 half −4915 ⇒ 6389 · detector T = 12800.** 🛑 **The flying build saturates TWELVE TIMES below the detector threshold**, so above 1065 the term is **pinned at ±511 and flips sign with acceleration — a RELAY, not a proportional inertia**, which is exactly a ratchet-generating mechanism. ✅ **THE REVERTS' REAL EFFECT IS UN-SATURATION, NOT GAIN REDUCTION:** V196 keeps the term proportional across **6× more** of the acceleration range. ➕ **This rationale does NOT depend on the anti-damping sign flagged as BELIEF** — un-saturating a relay is directionally safe either way ⇒ **V196 is better supported than when it was built.** ⊕ Same failure shape as [[accord-v80-damper-relay-and-grind1-inert]] (*“the damper became a RELAY, worst grinding ever — restore the RAMP, don't merely lower k”*), different lane. ⚠ **THE TWO REMAINING BRANCHES ARE MUTUALLY EXCLUSIVE:** |accel| > 12800 ⇒ the detector fires but the inertia term saturated long before, so V196 does little; |accel| < 3195 ⇒ the half-dose works but the detector never fires, so V191/V192/V193 are inert. ⇒ **V194's probe on `gp-0x6c2c` decides which branch is live — that is now the strongest reason to fly it.** ➕ Range bound: the term is ≤5.0 % of the aggregator (511/10240), halving moves ≤2.5 % of FULL RANGE; its share of the 8 Hz CONTENT is larger and this bound cannot determine it.
- 🛑★★★★★ [**My RELAY claim overreached — the ratchet is a DRIVEN RESONANCE, and two signatures say so**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I proposed the saturated inertia term as the ratchet's mechanism, which contradicted the ★★★★★ *“ring-down ζ 0.017–0.036, LIMIT CYCLE EXCLUDED”*. **Tested on 1037 engaged-creep windows (f0 median 7.81 Hz): HARMONICS — prominence f0 19.45, 3f0 4.14, 5f0 2.95 vs 2f0 (even control) 3.70 and off-harmonic control 3.41 ⇒ 3f0/control = 1.21, NO odd-harmonic excess (a relay would show one); AMPLITUDE — log10 sd 1.025, p10–p90 spans 521× ⇒ BROAD, where a limit cycle's amplitude is loop-set and would span a few ×.** ⇒ **DRIVEN RESONANCE; the record is corroborated and my relay-as-oscillator claim is WITHDRAWN.** ✅ **What survives:** the saturation arithmetic is unchanged (flying saturates at |accel|>1065, V196 at 6389); a pinned sign-flipping term **injects broadband energy that EXCITES** the plant's resonance ⇒ **V196 reduces an EXCITER, not the oscillator — expect a PARTIAL effect.** ✅ **The SIGN-SAFETY argument survives intact:** reducing a saturated term's injected energy is directionally safe whichever way the sign runs. ➕ **Third hypothesis of mine killed by its own test this session** (after the Coulomb sign-flip and the common-cause correlation), each with the control designed before the result was known. **That is the process working.**
- ⭐★★★★★ [**The exciter list: only FOUR aggregator terms are LIVE, and V196 touches the SMALLEST**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the ratchet is a PLANT resonance, so firmware can only reduce its EXCITERS, which are the `FUN_0003aa2c` sum terms. By clamp: **`gp-0x6b86` 12288 (biquad out, LKAS command 1–5 Hz) · `gp-0x6b4c` 10240 · `gp-0x6ad4` 10240 ELIMINATED (V56) · `gp-0x6b62` 8192 DEAD ENGAGED · iVar21/iVar16 8192 unidentified · `gp-0x6bbe` 2048 LIVE viscous ω¹ · `gp-0x6bd0` 2048 DEAD in the micro regime · `gp-0x6ade` 1024 unidentified · `gp-0x6b26` 1024 LIVE inertia ω², further clamped to 511.** ⇒ **4 LIVE, 3 dead, 3 unidentified.** 🛑 **V196 halves the SMALLEST live exciter — 12× less authority than the biquad output.** ✅ Not automatically bad: the big terms carry the LKAS command (a 1–5 Hz low-pass) so their energy is BELOW the ratchet, while the inertia term is the only ω² one — **a small clamp on a high-frequency term can still dominate the 8 Hz sum, and constants cannot settle which.** ⭐ **`gp-0x6bbe` is the next thing to MEASURE**: LIVE, ω¹, **2× the clamp**, and its cals are **`0xC6370`=2560 (scales `gp-0x6c2e`, the 2nd accel EMA, into this path) · `0xC6372`=205 · `0xC615A`=512 — all identical on STOCK/V122/V196, so the VISCOUS PATH IS BYTE-STOCK, untouched by the whole post-V38 arc.** 🛑 **NO BUILD FOLLOWS DELIBERATELY:** three hypotheses of mine died to their own tests this session, and proposing a fourth speculative lever on a byte-stock path from a clamp width alone, with no measurement of its 8 Hz content, would repeat the pattern. **The honest next step is the probe, not a lever.**
- ✅★★★★★ [**V197 = V196 + the probe that says whether its ratchet lever is well aimed**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V196 halves the SMALLEST live exciter (`gp-0x6b26`, clamp 1024→511) while **`gp-0x6bbe` is live, ω¹, has 2× the clamp and is BYTE-STOCK across the whole arc**; constants cannot say which dominates the 8 Hz sum, **so measure rather than guess a fourth time.** `0x55DF2` 0x9540→0x9442, `0x55E10` sar 4→sar 3. **3 payload bytes vs V196, ALL TELEMETRY — not one control cell changes.** 40/40. `b70483e02b110b74…` ⊕ **Shift sized to the SOURCE again**: `gp-0x6bbe` is writer-clamped to ±2048, so **sar 3** keeps 8-count resolution, sign intact (pos raw 0–256, neg 768–1023, unambiguous to |x|≤4088); **sar 6 would throw away three bits** — V194 needed 6 only because `gp-0x6c2c` spans the full int16. 🛑 **CORRECTION found while verifying: V196 does NOT carry V190/V191/V192/V193 levers** — it descends V195←V189←V188←V185. **V196's complete set: notch, K1→Honda, alpha→Honda, w[3] halved, FactorC m27→stock, engaged inertia HALF dose.** The builder now asserts m26=half / m27=Honda separately, which is what caught it. Decoder: `rlog-tools/probe/decode_v197_viscous_term.py <tag> --v197`.
- ✅⭐★★★★★ [**The exciter map is COMPLETE — 6 LIVE, 0 unknown; my “4 LIVE” was WRONG and it MISSED THE RATE LANES**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the aggregator's own tail identifies two of the three unknowns: **`*(gp-0x6adc) = iVar21` and `*(gp-0x6ada) = iVar16`** ⇒ **they ARE the r26 and r24 RATE LANES**, LIVE, summed into the aggregator with **8192 clamps**. The decompiler's `iVarNN` names hid them. ✅ **`gp-0x6ade` has ONE site in the whole image — a READ at `0x3AA48` — and NO WRITER** (Ghidra 1 match, raw scan 1 site, both agree) ⇒ BSS-zeroed forever ⇒ **DEAD.** ⇒ **6 LIVE · 4 dead · 0 unidentified.** ⭐ **THE CONSEQUENCE: `gp-0x6ada`/`gp-0x6adc` carry 8192 clamps — 8× the inertia term V196 halves (1024, further clamped to 511) — and they are ω¹ so they carry 8 Hz content.** ➕ **They are also ALREADY-PROVEN grind levers, both carried on V196: V62's `sar`×2 on r24 (*“18–22 Hz down 8–42×, the kit's first measured fix”*) and V88's Lever B `0xC6446`=5244 (*“grinding FIXED on-car”*, operator-confirmed).** ⇒ **if the ratchet needs a bigger lever than the inertia term, THE RATE LANES ARE WHERE TO LOOK** — 8× authority, ω¹, and a measured on-car dose-response history, needing **no new hypothesis**. ⚠ **But not before V197's measurement** — their 8 Hz content is equally unmeasured, and three hypotheses died this session for exactly that reason.
- ✅⭐★★★★★ [**V198 supersedes V197 — probe the BIGGEST 8 Hz exciter, not the second-biggest**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the completed exciter map reordered the probe choice: **`gp-0x6ada` (r24 lane) 8192 clamp · `gp-0x6bbe` (viscous) 2048 · `gp-0x6b26` (inertia) 511 effective** ⇒ the rate lane carries **4× the viscous clamp and 8× the inertia term's**, making it the biggest competitor to what V196 halves. ⊕ The record already designates it a telemetry target (*“mirrored to RAM and nothing reads them ⇒ free zero-blast-radius telemetry”*), and unlike every other candidate the rate lanes have a **MEASURED on-car dose-response history** (V62 `sar`×2 *“18–22 Hz down 8–42×”*; V88 Lever B *“grinding FIXED on-car”*), so a bigger lever would land somewhere already characterised. ✅ **V198 = V196 + 3 telemetry bytes**, `0x55DF2` 0x9540→0x9526, `0x55E10` sar 4→sar 5, 40/40, `9fbbf90b0bed9cb3…` ⊕ **sar 5** for a ±8192 source: pos raw 0–256, neg 768–1023, resolution 32, unambiguous to 16352. **Three probes, three shifts (6, 3, 5) — the shift is a property of the SOURCE, never the channel.** ⊕ V197 renamed SUPERSEDED and its decoder deleted; **shelf is four: V194 · V195 · V196 · V198.**
- 🛑🛑★★★★★ [**Every notch build since V188 ADDS loop gain — poles at the notch angle is a trap the lineage NAMES**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — measured from the built images, `max|H|` over 0–500 Hz: **V122 (Honda) 1.0000 · V188/89/94 1.3533 (+35 %) · V195/96/98 1.7177 (+72 %) · V199 1.0000**. `BUILD-LINEAGE.md` V105 says it outright: *“fixing DC with poles at the notch angle forces max|H| to 1.098–1.608 … Fix: Honda's own poles-BELOW-zeros layout. Check max|H| over 0–500 Hz against stock's 1.0000 before shipping any biquad edit.”* 🛑 **It shipped because V195's own GATE 2 assertion was `mx <= 2.0`** — the gate existed and was set wrong. ⭐ **The bar is ABSOLUTE 1.0, not “vs Honda”**: `0xC649B` 0→1 alone is inert (*the real arm `gp-0x671a ≥ 5` was never true in 255,292 engaged frames*) ⇒ **Honda ships the biquad DORMANT** and the car ran `H ≡ 1` until V103 armed it ⇒ the honest reference is `H ≡ 1`. ⊕ That also **deflates V108's revert of V105** (*“+14.0 dB at 61.1 Hz”*) — it compared against a state the car never occupies. ✅ **V199 = same zeros (19.75 Hz, still a TRUE null, depth 0.00156), poles moved BELOW to 17.45 Hz, r 0.9675 ⇒ max|H| 1.000000**, gives back **4.85° of LKAS phase** (−2.95° vs V196's −7.80° at 5 Hz) for 10.1× instead of 16.8× attenuation. 521 of 1,485 (pole, radius) pairs pass the gate; the free-HF corner costs −46°, the free-phase corner gives 6.9×. `c86646ab…` 31/31, preflight 8/8. ✅ **V200 = V199 + the r24-lane probe**, `db0b613a…` 40/40. 🛑 **V194/V195/V196/V198 renamed SUPERSEDED-DO-NOT-FLASH-GATE2; the shelf is TWO.**
- 🛑🛑★★★★★ [**`gp-0x6b86` is the BASE POWER-ASSIST, not the LKAS command — and the notch has a BYPASS**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — decompiled `FUN_000352b4`; tp anchors check out exactly (`tp+0x749b`=`0xC649B` arm, `tp+0x74fa`=`0xC64FA` CEIL, `tp+0x70a8/ac/b0/b4` = the four coefficient cells). Chain: **`gp-0x4f60` (TORQUE SENSOR) → clamp ±8192 → 10-knot assist map → `gp-0x6b7a` → friction-hold limiter → `gp-0x6b82` → BIQUAD → clamp ±12.0 → ×1024 → + `gp-0x6b7e` → clamp ±0x3000 → `gp-0x6b86`**. 🛑 **My own exciter-map label “biquad output — LKAS command” was WRONG and is retracted**, and with it the claim that the notch gives back phase in peak command oscillation's currency. ✅ **The upside: the notch costs NOTHING in LKAS authority** — it is not in the command path, so no notch dose can reduce how hard openpilot steers. ✅ It is still right for the GRIND: motion→torque→sensor→map→biquad→aggregator→motor **is** the loop. 🛑🛑 **THE BYPASS: `gp-0x6b7e` is NOT a constant** — `iVar24 += (clamp(gp-0x6b7a−limited,±0x3000)*0x80 − iVar24)*K >> 11`, **K ∈ [2,204]** ⇒ alpha ≤ 0.0996 ⇒ **fc ≤ 16.7 Hz** ⇒ **it passes 64.6 % of a 19.75 Hz input straight past the notch** (`0.0996/0.15419`). ⇒ **every predicted notch attenuation is an UPPER BOUND**, and a null on V199 could not separate “aimed wrong” from “bypassed”. Gated by `bVar3` = the friction-hold limiter is cutting — **never measured.** ✅ **V201 = V199 + the 427 probe on `gp-0x6b7e`** (`354f9dfb…`, 40/40, `0x55DF2`→`0x9482`, sar 5). Its levers if it fires: the EMA rate `K` (LERP at `tp+0x7900`/`0x7906`) or the ±0x80 deadband — **existing code, never touched.**
- ✅⭐★★★★★ [**The notch is a POINT fix — re-centring buys nothing, PHASE buys attenuation**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **RETRACTION: the pedestal passes 7.9 %, not the 64.6 % I quoted** — that was the clamp ceiling `K=204`; the table at `0xC6906/08/0A/0C` is **FLAT at K = 20** (fc 1.56 Hz), and `0xC6382`=41 is gated on `gp-0x6b62`, duty 0.0000 / 75,227 engaged frames. **Real but small; not a threat to the fix.** 🛑 **A joint minimax over (zero, pole, radius) across 16.3–23.0 Hz improves worst-case leakage 1.1× and makes the MEDIAN worse (15.6×→5.0×)** ⇒ **a single biquad cannot cover a 6.7 Hz band; V199's centre stays.** ⇒ **V199 gives 15.6× at the median grind peak but 1.6× at p10 and 2.2× at 23 Hz — SCORE EVERY DRIVE STRATIFIED BY ITS OWN PEAK, never pooled.** ⭐ **What buys attenuation is PHASE, and V199 is ON the frontier at its budget**: 2°→4.3× · 3°→4.8× · 5°→5.8× · 8°→7.3× · 12°→9.7× · 20°→14.8× — **doubles per 8–10°**. ➕ That phase is spent in the **DRIVER-ASSIST loop, not the command path**, so the cost is FEEL, priced as **added group delay: V199 +1.30→+2.37 ms, V202 +3.80→+5.52 ms — ~3 ms, versus human thresholds of tens of ms.** ✅ **V202 = V199 with poles 17.45→15.25 Hz, r 0.9675→0.9600, zeros UNMOVED** ⇒ **strictly more attenuation at EVERY frequency 16–30 Hz** (20.12 Hz **15.6×→24.7×**; 23 Hz 2.2×→3.4×), `max|H|` 0.999998, 31/31, `2c5bc569…` ✅ **V203 = V202 + pedestal probe**, 40/40, `0da3b7b9…` ⚠ **The notch still does NOT make 8× affordable** — 3.4× at 23 Hz is not enough to re-open a lever abandoned three times. **Shelf: V199 (low-phase fallback) · V202 · V203.**
- ✅⭐★★★★★ [**`0xC6CD0` is the ONLY firmware authority lever — the enumeration is now CLOSED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `lane_max = (clip 15360 × gain) >> 15`, read from the images: **stock `0xC646C`=891 → 417 ct = 4.1 % of the aggregator clamp 10240 · V202 6× → 2505 = 24.5 % · 8× → 3341 = 32.6 % · 10× → 4176 = 40.8 %**. ⇒ **the aggregator has 4× unused headroom and nothing downstream binds at 6×**; the forward clamps `0xC61B2`/`0xC61B4`=3072 are inert **because 2505 < 3072**, which is the real mechanism behind “0 % of the effect” — **and why V101 needed 4096 for 8× (3341 > 3072).** ⊕ anchor reproduces: `(15360×891)>>15 = 417`. ✅ **The setpoint clip `0xC61BE` is CLOSED** — built as V108 E3 at 16384 and pulled: achieved rate still rising at all five speed bins where a bound clip would pin it flat. **Do not re-propose.** ✅ **The latent 18.52 Hz square-wave injector is SILENT on the current build** — `0xC64DE` is a **BYTE** (stock 17 = 29.41 Hz, ours 27 = **18.52 Hz, inside the grind band**), and its amplitude LERP is `n=4` @`0xC6734`, X @`0xC6736` = [0,31872,31936,32000], **Y @`0xC673E` = [0,0,0,0]** in stock AND V202 ⇒ **not the grind's source**. ⚠ It sits **24 bytes from `0xC674E`/`0xC6750` which this kit edits** — a guard is now in `closeout_verify_published.py`. ⭐ **PREDICTION [BELIEF]: the m^0.88 authority sublinearity vs m^1.74 vibration is CAUSED by the vibration** (an oscillating command self-cancels) ⇒ **cutting 23 Hz loop gain should raise the exponent toward 1.0**, and V202 cuts it 3.4× ⇒ **sequence: fix the grind → re-measure the exponent → then raise the gain.** 🛑 **GOTCHA: `stock_fw_dump/code.bin` reads `0xFFFF` at `0xC6CD0` because V57 CREATED that cell** — never use it as a stock reference for post-V57 migrated cals.
- 🛑🛑★★★★★ [**The friction lane is at 0.200× HONDA, not “reverted to Honda” — and the 8 Hz ratchet notch stays rejected**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `friction = clamp(rate*12/knee, ±1) * (|model|*K1/1024 + K0/1024)`. **V177 reverted K1 1020→102 and everyone (me included) calls it “K1 → Honda” — but the RAMP KNEE `0xC40BC` was never reverted (600→3000) and it multiplies the WHOLE expression**: `(600/3000)×(102/102) = **0.200×**`, with saturation moved from motor-rate 50 to 250. Above saturation it equals Honda; **the ratchet lives entirely in the low-rate 0.200× regime.** ⚖ It cuts both ways — Coulomb friction is *“exactly what makes torque ripple without motion”* (the ratchet's own 13.5× `cs_tq` / 1.7× `cs_rate` signature) so 0.200× means LESS ratchet and matches the operator's low-friction directive, **but the verified polarity is more friction = MORE assist**, so it is also an authority cut. 🛑 **Reverting the knee raises friction 5×, contradicting a standing operator instruction ⇒ NOT built, recorded for the operator to decide.** A close-out guard now prints the true multiplier per image. ✅ **The 8 Hz ratchet notch re-priced (29,348 candidates) now that the phase is known to be FEEL not command tracking: at a 12° budget it buys only 2.56× on 6–9 Hz AND forfeits the grind (16.3–23 Hz falls to 0.93×); at 40° it buys 6.96× for −39.8° and +15.8 ms.** V202 buys **7.3× on the grind for −7.8°**. ⇒ **ONE biquad, ONE zero pair — grind OR ratchet, never both. V184's rejection SURVIVES; the biquad stays on the grind.**
- 🛑🛑★★★★★ [**`0xC63AA`'s recorded sensitivity is 41× UNDERSTATED — and `gp-0x6b4e` is the last unknown in its dilution ratio**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the record has `d(iVar6)/d(0xC63AA) = −(1/16)·(gp-0x6b4c/1024)`. `FUN_00038148` is `scaled=(SUM·sgn·cal(0xC6468)=2639)>>10` · **`target=scaled*0x10`** · EMA · `resid=gp-0x6bfe−(model>>4)+gp-0x6bfa` ⇒ **the `*0x10` and `>>4` CANCEL** (16× oversampled for EMA precision, not a divide). Perturbing the mirror: **zeroing the weight moves the residual by 2.577 × `gp-0x6b4c`**, vs the recorded 0.0625 ⇒ **41.2×**. ⚠ **Cuts both ways — `gp-0x6b70` is clamped ±8192 and 2.577×4000 already exceeds it, so this is a lever to SIZE, not a free one.** ✅ **`gp-0x6b46` CLOSED: ≤512 by construction** (`FUN_00036682` tail clamps its driver to ±0x200, EMA cal `0xC63D2`) ⇒ a lag-compensator error, never near its ±1024 reject window. 🛑 **`gp-0x6b4e` is THE remaining unknown and it is BIG: `gp-0x3d8c` SATURATED to ±10240** (`0x2743E`–`0x2746A`, `movea 0x2800`/`cmovle`, lockstep twin `-0x4cd6`), same ceiling as `gp-0x6b4c`, reject window exactly ±10240 so it never drops out. **Dilution: 43–92 % if `gp-0x6b4e`≈0, 16–75 % if it runs ≈500** ⇒ one unmeasured number decides it. ✅ **V204 = V202 + the 427 probe on `gp-0x6b4e`**, `30e7da9f…`, 40/40, sar 5. **Small ⇒ strongest cal-only structural lever in the kit; comparable ⇒ STRIKE it rather than leave it parked since 2026-08-20.** ⊕ All six model weights are unity except `0xC63A6`=512 (V181).
- 🛑🛑★★★★★ [**The cached `probe`/`field` columns are a BOOLEAN RUNG BYTE, not a magnitude — a shape statistic on them looked like a finding**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — on r80/r81/r82 they take only **4–6 distinct values spaced 64 and 16** (`r80 {15,79,143,207}`, `r81 {23,71,87,135,199,215}`) ⇒ **bit positions.** I computed rail-mass 0.21–0.45 vs `cs_tq` controls 0.09–0.19 and it read as a result; **the distinct-value count killed it. Retracted.** ⭐ **The CONTROL did not catch this — looking at the data did**: `cs_tq` behaved perfectly, so the statistic was fine and the CHANNEL was wrong, which no control on another channel can detect. ✅ `rlog-tools/score/observer_relay_shape.py` now **REFUSES** a channel with <64 distinct levels and prints them. ⊕ **No magnitude channel for `gp-0x6b70` exists anywhere in the corpus** — V96–V99 taps packed it into the rung byte (V100's changelog repoints AWAY from `gp-0x6b70`, dating the earlier target). 🛑 **THE OPEN QUESTION: `FUN_00038148` ends in `gp-0x6b70 = sgn(resid)·LERP(|resid|)` clamped ±8192 — if that LERP saturates early the stage IS a relay, which is the record's own “command-proportional Coulomb relay” blamed for the ratchet.** Its knots are 2 hops from any cal (X from `gp-0x373c` staging, Y from an `ep` table), so read the OUTPUT. ✅ **V205 = V202 + 427 on `gp-0x6b70`, sar 6** (±8192 ⇒ raw 0–128/896–1023, res 64), `8cf10086…`, 40/40. **Three live probes, three shifts (5,5,6) — the shift is a property of the SOURCE.** 🛑 **V203 retired** (its bypass question shrank to 7.9 %). **Shelf: V202 · V204 · V205 · V199. Fly V205** — it aims at the ratchet, the symptom nothing in 60 builds has moved.
- 🛑⭐★★★★★ [**The relay curve is BUILT AT RUNTIME from steering angle — it cannot be read from the image**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00038148`'s LERP (X `gp-0x64b6..`, Y `gp-0x641c..`) is filled by `FUN_000389ec`, and both sources are **RAM staging, not flash**: `0x39508 movea -0x3714, gp, ep` ⇒ **ep = gp-0x3714**, X from `gp-0x373c`. The staging is **COMPUTED**: `ld.hu` of **`gp-0x6982`, `gp-0x6a10`, `gp-0x6a64`, `gp-0x6984`** → `cvtf.uws` → `mulf.s` with **`movhi 0x3a80` = 1/1024** (Q10→float) and **`0x3dcccccd` = 0.1f**, in a **TEN-iteration loop** (`cmp 0x9,r14` / `jr 0x39258`), one per knot. ⇒ **`gp-0x6a10` is ABSOLUTE STEERING ANGLE** ⇒ **the curve is re-derived every pass from steering angle** ⇒ **no static curve exists; V205's drive is REQUIRED.** ⭐ **The bigger finding: a LERP that reshapes with steering angle makes the stage's CHARACTER condition-dependent** — relay at one angle, smooth at another ⇒ **a single static answer never existed, and V205's endpoint must be STRATIFIED BY STEERING ANGLE**, not pooled. Fits a symptom the operator reports as intermittent. [reshaping = EVIDENCE; that it explains intermittency = BELIEF]. 🛑 **PROCESS: I hand-rolled a gp displacement scan and hit the recorded odd/even trap** — reported “0 hits” for `gp-0x3738` and 1 for `gp-0x373a` on cells the disassembly reads four times, because **`ld.hu -0x373a` encodes `hw2 = 0xC8C7`, not `0xC8C6`** (`disp | 1`). **`analysis-2020accord/verify/scan_gp_relative_no_whitelist.py` already handles this** and its census prints *“op 0x3F ld.hu ← MISSED by the old whitelist”*. **Use it; never hand-roll a displacement scan — “0 hits” reads as “dead cell”.**
- ✅⭐★★★★★ [**`gp-0x6b70` is a SOFT relay — answered FROM THE IMAGE — and `0xC63AE` is its private gain cal**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00038148`'s LERP copies `gp-0x373c`/`gp-0x3714` verbatim (`0x39548`/`0x39522`), which are **exactly `assist_map_mirror.py`'s `Xi`/`Yi`** (validated 200/200 vs V72's flown probe) ⇒ **`gp-0x6b70 = sgn(resid) × ASSIST_CURVE(|resid|)`: the observer re-uses the POWER-ASSIST CURVE.** 🛑 **TWO OF MY OWN CLAIMS CORRECTED:** it is NOT unreadable statically (the mirror had it all along), and it is **NOT angle-dependent** — **1 distinct curve across 8 angles at every speed**; angle enters via `boost`→`SCALE` into the DOWNSTREAM `Xsrc`/`Ysrc`, not these arrays. **It is SPEED-dependent** (6 curves / 6 speeds); mode 24 vs 26 differ by one knot 0.4 % at 640 and are identical at 2560. ⭐ **NOT a hard relay** (ceiling only at X=14490, outside range) **but a SOFT one: gain 2.67–3.77× near zero vs 0.256–0.516× mid-range, ratio 6.7–10.7×** — high small-signal gain around a zero crossing is the shape that sustains a small-amplitude limit cycle, and it matches the record's “command-proportional Coulomb relay”. ⭐⭐ **`0xC63AE` = 1024 scales the LERP INPUT (`0x38242`), EXACTLY ONE site, ZERO writers, VIRGIN** ⇒ halving it halves the soft relay's small-signal gain, **and it scales THIS STAGE ONLY** (the assist map is fed by the differently-transformed `Xsrc`/`Ysrc`) — which is likely why the ratchet resisted 60 builds: its shape is otherwise welded to the ROM assist records. ⚠ **`FUN_00038148` is NOT engagement-gated** (caller task 0, 1000 Hz) so it changes manual driving, **and its SIGN on delivered assist is UNESTABLISHED** (`gp-0x6b70`→`gp-0x6ad6` is a torque-tracking REFERENCE) ⇒ **NOT built; no sign bets.** ⭐ **V205 is now MORE valuable: its job is to size and SIGN the `0xC63AE` dose.**
- ✅⭐★★★★★ [**`0xC63AE`'s sign needed NO drive — the record's own nine-link trace already covered it; V206 built and priced**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `accord-friction-polarity-more-friction-is-more-assist.md` **step 4 IS this stage** (`gp-0x6b70 = clamp(sgn(res)·LERP(|res|), ±8192)`, `f' ≥ 0` ⇒ `d/d(MODEL) ≥ 0`), step 5 `gp-0x6ad6 += gp-0x6b70·w`, step 9 `delivered = gp-0x6752 × gp-0x6b94`, **measured `d(gp-0x6b94)/d(gp-0x6b70) = +0.2529/+0.2565`**. ⇒ lowering `0xC63AE` shrinks `|gp-0x6b70|`, and **V87 measured it NEGATIVE 67.19 % of engaged time** ⇒ **predominantly LESS assist, a heavier wheel.** 🛑 **`0xC64B0` IS NOT A WEIGHT — it reads 257 = `0x0101`, two enable BYTES.** `CLAUDE.md` cites `tp+0x74B0` as the off-by-0x1000 case that *“invented lane weights for what are 0/1 enable flags”*; **the trap recurred as a byte-pair read as a u16**, not a wrong address. ✅ **V206 = V202 + `0xC63AE` 1024→512**, `71bd8312…`, 34/34, **ONE payload byte**, cave byte-identical. **GATE 1 cleanest possible: 1 site (`0x38242`), 0 writers, byte-stock on every build**; scales THIS STAGE ONLY (the assist map uses the differently-transformed `Xsrc`/`Ysrc`). Small-signal gain **2.67→1.33 · 3.04→1.52 · 3.77→1.89 · 3.43→1.72**. ⚖ **PRICE STATED: ratchet gain halves, wheel gets heavier** ⇒ **NOT the recommended build; V205 is**, because it sizes the dose instead of guessing. Quarter dose is the follow-up. 🛑 **Byte-count trap recurred**: `1024 = 0x0400 → 512 = 0x0200` moves only the HIGH byte ⇒ **ONE** byte, not two. The builder now **derives** the expectation from the values.
- ✅⭐★★★★★ [**GATE 2 for a scaled NONLINEARITY needs a DESCRIBING FUNCTION — “halving the gain halves the loop gain” is FALSE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0xC63AE` scales the INPUT of a memoryless CONCAVE curve, so scaling down moves the operating point onto a STEEPER part and the two effects fight. **`N_g(A) = k·N_f(k·A)`, NOT `k·N_f(A)`.** Measured on the real curve: **ratio 0.486 / 0.472 / 0.619 / 0.794 / 0.771 / 0.658** at A = 25 / 200 / 800 / 3200 / 6400 / 12800 — **never a flat 0.500, worst 0.794**, so the dose buys **1.26–2.1×**, not a uniform 2×. ✅ **GATE 2 PASSES**: gain reduced at every amplitude and speed, and being memoryless it adds **zero phase** ⇒ the Nyquist locus contracts **radially with no rotation** ⇒ cannot create a −1 encirclement that did not exist ⇒ a stable loop stays stable. 🛑 **I published “halves the gain” in V206's builder — that is the SMALL-SIGNAL LIMIT only; corrected with the amplitude table.** ⭐ **UNASKED-FOR FINDING: N is NON-MONOTONIC, peaking at A ≈ 200–400 counts** (speed 2560: 3.64→3.75→3.55→1.95→0.61). A limit cycle sits where `N(A)·|G|=1`, so **a peak in N is a PREFERRED AMPLITUDE** ⇒ **if the ratchet is a limit cycle through this stage its amplitude should sit near 200–400 counts** — falsifiable, and V205 reads exactly this signal. V206 lowers the peak ~3.8→1.9. [describing function = EVIDENCE; the limit-cycle identification = BELIEF] ⊕ **First GATE 2 in the kit done with a describing function rather than a linear Bode sum** — a linear sum would have reported a flat 0.5 and missed that the dose is 1.6× weaker at mid amplitude.
- 🛑⭐★★★★★ [**The limit-cycle FREQUENCY test is vacuous; my amplitude prediction is in tension with the record; V206 re-justified as a CEILING raise**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **FREQUENCY: a Q 14–29 resonance sweeps 180° over `f0/Q`, so the −180° crossing is pinned within ±0.13–0.28 Hz of 7.79** ⇒ **any limit cycle through it locks to 7.6–8.0 Hz BY CONSTRUCTION**, inside the measured 6–9 Hz band ⇒ **zero discriminating power. Do not spend a drive endpoint on it.** ⚠ **AMPLITUDE: my describing-function peak is SPEED-INDEXED and predicts 460/438/453/619/1224 counts at ~5/10/20/40/80 km/h — a 2.8× rise — but the record calls the ratchet SPEED-INVARIANT with amplitude scaling on WHEEL RATE / command magnitude.** Opposite directions ⇒ **the limit-cycle-amplitude framing is WEAKENED** (not cleanly refuted — a compensating `|G|` could cancel — but the burden sits on that coincidence). ⭐⭐ **V206 IS RE-JUSTIFIED ON THE RECORD'S OWN TARGET**: `accord-ratchet-and-grind-are-command-gated-saturation` says *“a linear lever cannot fix a relay — find what clips, and either RAISE ITS CEILING or soften its corner”*. `0xC63AE` scales the LERP INPUT, so **halving it doubles the residual needed to clip: 14490 → 28980, at every speed** (resid is gated ±20000/term, so Honda's ceiling IS reachable and V206's is much less so) ⇒ **exactly a 2× ceiling raise, the “raise” branch.** ⊕ **This survives the speed-invariance objection because it is about CLIP DUTY, not loop gain.** ✅ **NEW ENDPOINT for V205 (which reads `gp-0x6b70` directly): clip duty at ±8192** — high ⇒ model confirmed and V206 aimed right; low-but-nonzero ⇒ partial, quarter dose quadruples the ceiling; **identically zero ⇒ the element never clips and V206 comes OFF the shelf.** A duty needs no scale calibration and cannot be averaged away.
- ✅⭐★★★★★ [**The saturation census converges on `gp-0x6b70` — the governor clip is MEASURED DEAD in a cache we already had**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the record says *“find what clips”*; I enumerated **every clamp from the LKAS command to the motor**, each ceiling read from the image and placed next to its own producer. **14 of 18 CANNOT CLIP** (ceiling ≥ producer, or already measured inert). ✅✅ **The biggest survivor is now dead too: `gp-0x4f64` is the governor ceiling, pinned at its cal max 4762 for 99.9 %+ of engaged time (a CONSTANT limit, vs the aggregator's own 10240 = 2.15× higher) — and V105's cave `b6` IS `|gp-0x6b94| ≥ |gp-0x4f64|`. V105 FLEW as route `a5` and that cache is ON DISK: **duty 0.000000 over 49,021 engaged frames**, with **b7 0.383 / b5 0.280 / b4 0.434 / b3 0.487 as positive controls** on the same byte ⇒ not a stuck field. ⊕ `gp-0x6ad6` likewise dead: V100 `b5` duty 0.000000, CI [0, 0.0186], `b4`=0.6057 same cell. ⭐ **⇒ `gp-0x6b70` is the ONLY clamp in the path that CAN clip and has NEVER been measured — exactly the cell V205 reads and V206 doses.** Independent corroboration by ELIMINATION, needing no drive: census from the image, duties from caches on disk. ⚠ One other stays open: **`gp-0x6b84` (resid mirror, ±0x3000)**, worth a rung if a future cave has a spare. 🛑 **Lesson: before designing a new probe, check whether a FLOWN build already carried the rung** — V105's b6 was cut for exactly this question and had been sitting unread.
- ✅★★★★ [**Swept all 23 cached routes for unread cave rungs — 72 informative readings, and the route registry STOPS at r77**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `verify/unread_rung_sweep.py` reads `0x14A` byte4 bits 7:3 on engaged frames for every cached route and prints each duty against the registry's evidence. ✅ **METHOD VALIDATED**: the lineage records V104's `b6` (`|r24|≥|r26|`) at *duty 1.0000, carried no information* — route `a4` reads **exactly 1.000000**, and route `a5` (same cave, `b6` repointed) reads **0.000000** ⇒ a positive AND a negative control on one bit across consecutive builds. ✅ **THREE ANSWERS READ FROM CACHES, NO DRIVE**: (1) **`r85` IS V100**, confirmed by three exact matches to the recorded *b5 0.000000 / b6 0.000000 / b4 0.6057*; (2) **`r9e` (V103) `b3` = 0.4675 VARIES ⇒ the lineage's own RUN-INVALIDATING identity gate PASSES**, never previously checked; (3) 🛑 **`r97` carried NO CAVE** — probe byte `7` in all **68,883 engaged frames**, which the registry itself calls *“stock STEER_SENSOR_STATUS with NO probe bits”* ⇒ one of the largest exposures in the corpus with zero instrument, and nothing said so. 🛑 **STRUCTURAL GAP: all 13 routes r77–ra6 (the whole V90–V106 arc) are “not in registry”**, so their rung meanings live only in lineage prose — **exactly why V105's `b6` went unread.** ⚠ Not extended, because the registry's `tail` field is the rlog hash and those are NOT in `_scratch/cache`; filling them would mean inventing identifiers. ⭐ **DEGENERATE ≠ NULL**: 43 of 115 readings are 0.000000/1.000000, which is **uninterpretable, not a null**, and this kit has confused the two repeatedly (V64's gate, V68's never-non-zero detector, V104's b6). The sweep labels them apart by construction.
- ✅⭐★★★★★ [**An UNREAD on-car dose-response for the ratchet lever, and the cave is byte-identical V105→V202 so the shelf gets it FREE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V102's cave defines **`b5 = |gp-0x6ae2| ≥ |gp-0x6b26|`** (modelled friction vs **INERTIA**) and V103 changes *only* `b3`, so **b5 is the SAME rung on V102–V106**: 0.2481 / 0.2384 / 0.2305 / 0.2798 / 0.1907 ⇒ **inertia exceeds friction ~75 % of engaged time — V196/V199/V202 halve the DOMINANT term.** ✅✅ **V105→V106 is a SINGLE-CELL pair on the inertia curve.** Episode bootstrap (9 and 7 episodes, 4000 resamples, length-weighted, per the episodes-not-windows rule): **b5 −0.0891 [−0.1328, −0.0200] EXCLUDES 0**, controls b7 −0.0511 and b4 −0.0184 both include 0 ⇒ **direction correct, effect 1.7× the largest control ⇒ `0xD7A5C` REACHES THE CAR with the expected sign**, never before shown for the shelf's ratchet lever. ⚠ 9/7 episodes, b7 moved 57 % as much ⇒ **corroborating, not proof.** 🛑 **TAG CORRECTION: V106 is named `X3.0` but the images show exactly ×2.000** (−14745/−8601/−2949 → −29490/−17202/−5898) — quoting the tag instead of the image is off by 1.5×. ⭐⭐ **THE CAVE AT `0xC4B34` IS BYTE-IDENTICAL v105 v106 v107 v108 v122 v202** (sha `d3bb75d8fce08211`; v103/v104 differ) ⇒ **V202/V205/V206 carry V105's exact rungs, so every shelf build already reports the ratchet lever for ZERO extra bytes.** ✅ **PRE-REGISTERED: doses read from images are V105 1.000×, V106 2.000×, V202 0.333× ⇒ b5 on V202 should read ≈0.42, range 0.31–0.49. b5 ≤ 0.28 ⇒ the halving is NOT reaching the car and the shelf's ratchet lever is INERT** — the most useful null available. ⚠ log-dose extrapolation 1.6 doublings outside the measured range, from one pair.
- 🛑🛑★★★★★ [**`gp-0x6b70` DOES NOT SATURATE — 1 frame in 72,916; V205's question answered from cache and V206's best argument REFUTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `BUILD-LINEAGE-CATCHUP` records V96/V97's probe: **CAN 427 ← `gp-0x6b70`, LSB 12.8 ct, `raw = (|x|×5)>>6`, clamp ±8192 → raw 640**, with **`b7` = `gp-0x6b70 < 0`** carrying the sign. V100 repoints 427 away, bounding the window to **V96–V99 = routes 7d/7e/7f/80/81/82, all cached** (427 is half-rate, so engagement is INTERPOLATED onto the 427 timebase). **Pooled 72,916 engaged frames, ONE at the clamp, duty 0.000014.** p50 141–1370, p95 1677–4990 vs 8192. 🛑 **V206's ceiling justification is DEAD** — I had argued a 2× ceiling raise *survives the speed-invariance objection because it is about clip duty*; **the ceiling is never reached.** Only its GAIN effect survives, which is the argument in tension with speed-invariance ⇒ **demoted, not withdrawn.** 🛑🛑 **The saturation census now has NO surviving clamp in the command→motor path** ⇒ if the ratchet is a command-gated saturation, **the element is not a CLAMP** — the remaining candidates are the aggregator's **zero-REJECT gates**, which drop a lane to 0 rather than clipping. ⭐ **SHELF RE-RANKED: `gp-0x6b4e` SATURATES at ±10240 with its reject window at exactly ±10240 and has NEVER been measured ⇒ V204 is now the probe to fly; V205 is demoted, its question answered here.** ✅ The measured range also sizes V206 honestly: at the real operating point (p50 ≈500, p95 ≈2500) the N ratio is **0.47–0.72 ⇒ a 1.4–2.1× gain reduction**, not 2×.
- 🛑🛑★★★★★ [**No zero-REJECT gate can EVER fire — combined with the clamps, the command-gated-saturation model has NO mechanism in the command→motor path**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the gate idiom is an **unsigned** compare `(x + W) < 2W+1`, which passes **exactly `|x| ≤ W`**. Against each producer's own bound: `gp-0x6b4e` 10240/10240 · `gp-0x6b4c` 10240/10240 · `gp-0x6b26` 1024/511 · `gp-0x6b46` 1024/512 · `gp-0x6bd0` 2048/1024 · `gp-0x6bbe` 2048/512 ⇒ **not one can fire.** `gp-0x6b4e` is EXACT — its writer saturates to ±10240 and the gate passes `|x| ≤ 10240`, so the saturated value passes **by one count**. ⭐ **REFRAME: these are FAULT GUARDS, not shaping nonlinearities.** Every window is sized at or above its own producer, so they exist to drop a CORRUPTED lane, not to shape the control law — **reading them as shaping elements, which the “find what clips” hunt invites, is a category error.** 🛑🛑 **COMBINED with the clamp census (every clamp structurally unable to clip or measured zero, incl. `gp-0x6b70` at 1/72,916): if the ratchet is a command-gated saturation, the element is NOT in the command→motor path.** What remains uncensused: the **delivery chain** (EME shaper, integrator, FOC/PWM) and the **plant**. [census = EVIDENCE; “the model is wrong” = BELIEF — it may be looking at the wrong stage.] ✅ **V204 SURVIVES and is the only saturation question left standing**: `gp-0x6b4e` is still SATURATED BY ITS WRITER at ±10240 (`0x27442..0x27454`) — the gate passing is irrelevant, the clipping happened upstream — and whether `gp-0x3d8c` drives it to the rail is UNMEASURED.
- ⭐⭐★★★★★ [**The DELIVERY CHAIN zero-rejects the MERGED COMMAND at ±8192 — the one gate the census cannot rule out**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — byte-confirmed at `0x431D0`–`0x431D8`: `addi 0x2000,r9,r6` · `addi -0x4001,r6,r0` · **`cmovc 0x0,r9,r11`** ⇒ **outside ±8192 the merged command is REPLACED BY ZERO, not clipped** — the most violent nonlinearity in the chain, and exactly the “command-gated saturation” shape the ratchet model needs. ✅ **Its producer is NOT structurally bounded below it.** Comp-add at `0x458B8`–`0x458CE`: `ld.h -0x6ace` (governor out) · `st.h r6,-0x6ad0` (the COMPENSATION) · **`add r6,r12` ⇒ `gp-0x6acc = gp-0x6ace + gp-0x6ad0`** · `sxh` (int16 WRAP, no clamp), lockstep twin `gp-0x4cc8`. **`gp-0x6ace` ≤ 4762** (governor pinned at its cal max 99.9 %+), **`gp-0x6ad0` UNKNOWN** — a LERP at `0x45892`–`0x458a2` sign-flipped on `gp-0x6abe`. ⇒ **fires iff compensation > ~3430 with the governor railed.** 🛑 **All six aggregator gates were structurally dead (producer ≤ window); THIS ONE IS NOT.** The golden model concedes it: envelope *“4762 + 2560 = 7322”*, **870 under the window**, and *“does not claim every combination is contained”*. 🛑 **NEITHER CELL HAS EVER BEEN MEASURED** — `gp-0x6acc` appears only as chain prose (*“the gp-0x6acc bridge”*), `gp-0x6ad0` nowhere. ⭐ **NEXT PROBE TARGET, OUTRANKS V204**: V204 asks whether a model lane clips; this asks whether **the merged command is being ZEROED**. **Tap `gp-0x6ad0`** — it is the unknown term and gives the margin directly. [gate/comp-add/bound = EVIDENCE; that it fires = BELIEF, unmeasured.]
- ✅⭐★★★★★ [**THE SATURATION CENSUS IS CLOSED — the delivery-chain gate CANNOT FIRE, and V207 is retired BEFORE flying**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — decompiling `FUN_000456a4` (instead of reading assembly upward, which `CLAUDE.md` warns against) gives it whole: the compensation is **0 unless `gp-0x6ac0` > LERP1(angle)** (a motor-rate deadband) and then **`min(((rate−LERP1)·cal(0xC6204)=3072)>>10, LERP2(angle))`** ⇒ **capped by max(LERP2 Y)**. Tables: **LERP1 X `0xC6832..6836`=[3800,4000,4150] Y `0xC6838..683C`=[5000,3037,1000]; LERP2 X `0xC67D2..67D6`=[3200,3800,4150] Y `0xC67D8..67DC`=[512,1024,2560]**. ⇒ **max compensation 2560 + governor ≤ cal(0xC6202)=4762 = 7322 vs the ±8192 zero-reject ⇒ MARGIN 870, THE GATE CANNOT FIRE.** ⊕ the alternate rescale is **disarmed** (`cal(0xC64BA)`=0, needs −0x17) **and an identity** (`0xC648E`=0, `0xC6134`=1000/1000). ✅ **Upgrades the golden model's own caveat** — it called 7322 a conservative envelope that *“does not claim every combination is contained”*; it IS contained, provably, because 2560 is `max(LERP2 Y)` behind a `min()`. 🛑🛑 **CENSUS COMPLETE: no clamp saturates and no gate fires anywhere from the LKAS command to the motor** ⇒ **the command-gated-saturation model has NO mechanism in the firmware command path** (it would have to be FOC/PWM-inner-loop or mechanical). ✅ **V207 RETIRED BEFORE FLYING — a drive saved by reading a three-knot table**; V204 returns to the top. ⭐ **The compensation itself, never named before: a motor-rate deadband SCHEDULED ON STEERING ANGLE** — arms more easily as angle rises (LERP1 falls 5000→1000) and permits more (LERP2 rises 512→2560).
- ✅★★★★ [**The method that retired V207 does NOT retire V204 — `gp-0x3d8c` is an UNCAPPED accumulator**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V207 died because its producer had an explicit `min(·, LERP2)` cap at 2560. `gp-0x6b4e`'s source `gp-0x3d8c` has none: `0x271de`–`0x27318` walks **four parallel slot arrays** (`gp-0x61b8`, `gp-0x61d0`, `gp-0x61e8`, `gp-0x6324`) with running min/max, accumulates over **~10 slots** (`cmp 0xa,r15` / `jr 0x271de`), and **stores with NO cap, NO clamp, NO min()**. ⇒ **the ±10240 saturation is applied DOWNSTREAM at the READER** (`0x27442`–`0x27454`), so a sum of ~10 signed halfwords has no structural ceiling below it and **the saturation is genuinely reachable on paper.** ⭐ **That is the dividing line between what analysis can settle and what needs a drive**: V207's bound was PROVABLE and it retired; V204's is NOT, so it must be MEASURED. ⚠ **Weak prior only**: the sibling sum `gp-0x6b4c` measures `|·| ≥ 4096` at duty 0.000000 over 17,614 engaged frames, but it is a different cell with a different slot mask (`0xC4124` = [0,0,5,0,5,5,0,0,0,5,0], four slots forced zero) ⇒ transfers as a prior, not a bound. [BELIEF] ✅ **NEW STRUCTURE: the mixer writes FIVE accumulators in one pass** — `gp-0x3d74`, `gp-0x3d88`, `gp-0x3d70`, `gp-0x3d98`, `gp-0x3d8c` at `0x27308`–`0x27318`; **only the last is traced anywhere in this kit, the other four have never been named.**
- ✅⭐★★★★★ [**The notch was 1 Hz LOW — re-centred 19.75 → 20.50 Hz on the MEASURED episode peaks, 1.66× for free**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V195 fitted 19.75 from a **per-route** median of 20.12; surveying **per engaged EPISODE** (20 routes, **125 episodes**, Welch on `cs_rate` 15–25 Hz) gives **p10 16.37 / median 20.70 / p90 23.05**. Scoring the ACTUAL peaks through each candidate under V202's own constraints (`max|H|≤1.0`, `|Δphase@5Hz|≤8°`): **V202 (19.75/15.25/0.9600) median 5.7×, p10 2.3× → V208 (20.50/15.50/0.9575) median 9.5×, p10 2.0×** ⇒ **1.66× at the median, same gate, same budget, and only −0.11° more lag at 3 Hz vs V202 — very nearly free.** ⚠ 20 routes here vs 67 in the original fit, so the 0.58 Hz gap could be sampling — **but 19.75 is below BOTH medians**, so the re-centre moves toward either. ✅ **V208** `e27b4fcc…` 31/31, depth 0.00000 at 20.50, poles still below zeros, cave byte-identical. **V209 = V208 + the `gp-0x6b4e` probe** `984dfe55…` 40/40, preflight 8/8. ✅⭐ **NEW: `rlog-tools/score/score_drive.py <tag>` — the kit had ~40 scoring tools and NOTHING saying which to run.** One command: exposure first, cave rungs with **degenerate flagged as uninterpretable not null**, the b3 validity gate, b6 vs measured-dead, **b5 vs its pre-registered range**, the 427 channel pointing at the matching decoder, and the grind peak **stratified by the drive's own episodes**. 🛑 **It refuses to apply the b5 prediction to a non-shelf route** — caught on its first run, where it would have announced a false “the lever is not reaching the car”. 🛑 **Shelf consolidated to FOUR: V209 (fly) · V208 · V206 · V199**; V202/V204/V205 retired. ⚠ **V206 still carries the 19.75 notch** — rebuild it on V208 before flying it.
- ✅⭐★★★★★ [**Nothing predicts the grind peak — the spread is BIMODAL, and V208 is near-optimal**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — per-episode (130 ep / 23 routes) the 15–25 Hz peak vs covariates: speed ρ +0.109 (p 0.22), |rate| −0.062 (0.48), |driver tq| −0.109 (0.22), **|LKAS cmd| ρ −0.351 p<0.0001**. 🛑 **RETRACTED same tick: within-route/total variance = 0.24 ⇒ episodes inside a route are NOT independent; at ROUTE level (n=20) ρ = −0.340, p = 0.14.** Pseudo-replication. ⊕ **The record names this trap one level DOWN (“episodes not windows”) but not one level UP (routes not episodes) — now recorded.** ⇒ **no covariate predicts the peak.** ✅⭐ **THE SPREAD IS TWO CLUSTERS, NOT A TAIL: 2 routes (r1e 15.62, r97 15.82) vs 18 routes at 19.53–23.05 (median 20.99), a 3.71 Hz GAP with nothing in between.** ⇒ the “p10 16.37 so one biquad cannot cover it” worry was two routes, not a continuum. ✅ **Re-scored on the cluster (112 episodes): V208 gives median 10.4×, p10 3.4×** (not the 2.0× the pooled figure implied), **and the best possible re-centre (20.75) buys only 1.11× ⇒ inside noise. DO NOT RE-CUT; V208 stands.** ⚠ What r1e/r97 are is NOT established [BELIEF] — r97 is also the route that carried NO CAVE, anomalous on an independent axis.
- ✅⭐★★★★★ [**A peak histogram is the WRONG statistic — power-weight it; one “outlier route” had no grind at all**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — comparing band POWERS not peak locations: **`r97` P(19.5–23) = 0.0, i.e. 0.01× the cluster median ⇒ its “peak at 15.82 Hz” is `argmax` on a FLAT spectrum**, an absence read as a frequency; **`r1e` is real** (P(15–17) = 3.2 outranking its own 21 Hz content) — **one route, not a cluster.** ⊕ **a PROMINENCE floor does not catch `r97`** (locally prominent, absolutely tiny) ⇒ **the criterion must be absolute ENERGY.** 🛑🛑 **An unweighted median over episode peaks gives an episode with 1 % of the grind the SAME VOTE as one with 20 % — my own statistic last tick.** Power-weighted: **median 21.48 (not 20.70), p10 19.53 (not 16.37)**; the top 10 episodes carry **53 %** of all corpus grind energy, peaking 21.5–23.0; and the corpus band spectrum peaks at **21.09 Hz**. ✅ **BUT on the right objective — TOTAL RESIDUAL BAND ENERGY — V208 (20.50) already gives 14.9× vs a best-possible 17.8× at 21.75: only 1.19×, inside sampling noise ⇒ V208 STANDS**, its wide skirt making the centre non-critical (the 19.75→20.50 move was worth 1.66× because it was a full notch-width off). ✅ **`score_drive.py` now reports the POWER-WEIGHTED peak as the figure to trust**, prints how many episodes carry half the drive's band energy, and warns when a drive has essentially no band energy — the `r97` failure mode, which would otherwise hand the operator a confident frequency for an absent symptom.
- ✅⭐★★★★★ [**The 8× LKAS gain is RE-OPENED — V208's notch changes the trade that killed it three times**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — authority was the one symptom with nothing on the shelf; `0xC6CD0` is the only firmware lever and was abandoned at V101/V124/V142 because **vibration ∼ m^1.74 vs authority ∼ m^0.88**. **That trade is set by the BASELINE.** Energy-weighted over the gain-driven 22–26 Hz band (130 episodes): **V208 attenuates 3.70× in amplitude**, a 6×→8× step grows vibration **1.65×** ⇒ **net 0.45× vs the car today, i.e. ~2.2× QUIETER, for 1.29× more authority.** V101 flew 8× at 1.65× the then-current level and drew a grinding complaint; this is 0.45×. 🛑 **TRAP I NEARLY PUBLISHED: 13.63× is the ENERGY ratio, 3.70× the AMPLITUDE ratio** — they differ by the square, and `m^1.74` was fitted to an amplitude `G`, so comparing 1.65 against 13.63 would have overstated the case **3.7×**. Per-bin amplitudes 6.1×…2.4× square to 37×…5.8×, harmonic-mean 13.6×, √13.6 = 3.69× = the flat amplitude average — they agree once labelled. **Compare like with like.** ✅ **V211 = V208 + `0xC6CD0` 5346→7128 + `0xC61B2`/`0xC61B4` 3072→4096**, `70b20558…`, 37/37, preflight 8/8, **4 payload bytes across 3 cells (DERIVED — 3072→4096 moves one byte each)**. Builder asserts the gates that killed earlier gain builds: `0xC674E`=5120 > clamp (the abort condition capping the lever below 10×), lane max 3341 < 4096, `0xC407E` still 511. 🛑 **STAGED — DO NOT FLY BEFORE V208/V209 CONFIRMS.** It rests on [BELIEF] that the notch attenuates a COMMAND-excited line: the notch is on the base-assist path but sits INSIDE the loop, so it lowers the gain that SUSTAINS the resonance whatever excites it. Reasoning, not data — if wrong, 8× lands at 1.65× and is audible immediately.
- 🛑🛑★★★★★ [A SECOND driver-torque gate exists (`0xCBAE4`/`0xCBBC4`) but is INERT in the operator's actual override case](reference/firmware/reference-accord-second-driver-torque-gate-cbae4-cbbc4.md) — a `cmovne` on `gp-0x6803==2` routes the real override case to flat-to-near-flat grab-rate curves instead; V277's `TAPER_PTRS` correctly omits it. Also resolves `FUN_0002a30e` as unreachable (no duplicate PID block).
- 🛑🛑★★★★★ [The PID-output 5 Hz lag's exact coefficients, and its DC-GAIN TRAP](reference/firmware/reference-accord-pid-output-5hz-lag-dc-gain-trap.md) — `y[n]=(992*y[n-1]+507*x[n])>>10`, fc≈5.05Hz at 1kHz (BELIEF on rate); DC gain = 507/32 = 15.84, NOT unity — moving the pole without holding `b/(1024-a)` constant silently changes forward gain too.
- 🛑🛑★★★★★ [`gp-0x4f60`'s producer chain has ZERO dynamic filtering — raw, uncompensated torque sensor](reference/firmware/reference-accord-gp4f60-no-producer-filter-raw-sensor.md) — corrects "phase correction" `FUN_0007f300`, which is a static zero-offset bias, not a filter; nothing compensates for the motor's own reaction torque anywhere in the chain.

- 🛑🛑⭐⭐⭐⭐⭐ [THE RESIDUAL IS ON THE **ANGLE** AXIS, NOT SPEED — the operator's under/over split is confounded (hard corner <20 mph = large angle; >20 mph = near centre, and he has NO data above 10 deg at 20+ m/s); the SR map's >=48 deg knots are ~9% too low; decomposition FLAT 0.006-0.012 / ANGLE ~0.09 / SPEED 0.042](accord/mechanism/accord-the-residual-is-on-the-angle-axis-not-speed.md) — arbitrated on wheel-speed yaw (CAN 0x1D0), which has no gyro/roll/Kalman in it
- 🛑⭐⭐⭐⭐⭐ [The car's TRUE lat-accel-per-torque is ≈3.3 (3.20/3.27/3.37 across three assumed LAFs spanning 1.9x) — and "LAF is inert" was a conditioning artefact of |setpoint|>0.5; unconditioned road gain is monotone 1.122→1.106→1.039](accord/calibration/accord-true-lat-accel-per-torque-is-3-3-and-laf-is-not-inert.md) — recommend SteerLatAccel 3.6
- 🛑🛑⭐⭐⭐⭐⭐ [GRIND #1 CAL-ONLY LEVERS ON V282 ARE EXHAUSTED: the output-lag pole is a WATERBED (peak already at ~26 Hz; 15 Hz takes GM to 0.72x, DO-NOT-FLASH), fb pole is gain-only, Kd closed, Kp costs authority, 0xC6446->2048 is a STUTTER lever (ring 0.98->0.48) not a grind lever, and the D clamp 0xC61B6 at 2560 FAILED adversary B (0.6x Kd cut in loaded/hands-on/fast strata re-arms the 7.3 Hz ring); 7680 is the largest ring-safe dose and a ~5 % partial mitigant (V287 rev 2, needs ~38 min engaged); NO cal exists on the setpoint path -- the 100 Hz staircase reaches D unfiltered; the real target is a setpoint-interpolation CODE edit](accord/mechanism/accord-grind1-cal-only-levers-on-v282-are-exhausted-the-lag-pole-is-a-waterbed-and-the-d-clamp-trades-the-ring.md)
- 🛑🛑⭐⭐⭐⭐⭐ [HONDA'S OSCILLATION DETECTOR (FUN_000428d4) IS LIVE, ungated, watches a 39 Hz-filtered rotor-rate derivative gp-0x6c2c, counts >10 Hz reversals past 40 % of full scale for >=375 ms, and MIN-folds a x0.600 cut into the governor's motor-demand scale (slot 2, by register -- the RAM mirror gp-0x6994 has no readers); not observed firing on any V282 episode; the golden model cannot represent it; 0xC40DC = 14 on V282 is NOT stock (22)](accord/firmware/accord-honda-oscillation-detector-is-live-and-cuts-motor-demand-x06-through-governor-slot-2.md)
- 🛑⭐⭐⭐⭐⭐ [THE D CLAMP IS 0xC61B6, NOT 0xC61BA (the integrator anti-windup -- both hold 10240, assert on the ADDRESS); 4 live ld.hu readers, symmetric, inclusive, memoryless (E_prev is unclamped); the 0xC61B8 = 102 deadband is GATED OFF ENGAGED, so the 'P-only deadband = 0xC61B8' stall attribution is WITHDRAWN; D and the PID sum are write-only, not on the wire; 0xC61B4/0xC61BE carry a latent ld.h/ld.hu mismatch -- never >= 32768](accord/firmware/accord-the-d-clamp-is-0xc61b6-and-the-102-deadband-is-gated-off-engaged.md)
- 🛑⭐⭐⭐⭐⭐ [A NONLINEAR LEVER MUST BE PRICED IN EVERY DRIVING STRATUM -- V287 rev 1 passed on creep/episode windows and failed where the car spends 20-28 % of engaged time; reuse adv_v287_b_units_strata.py's stratification; FAIL thresholds outside the baseline CI and route spread](feedback/process/feedback-a-nonlinear-lever-must-be-priced-in-every-driving-stratum-not-only-where-the-symptom-lives.md)
