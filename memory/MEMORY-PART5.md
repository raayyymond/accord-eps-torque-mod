# MEMORY — PART 5

🛑 **Continuation of `MEMORY-PART4.md`, split 2026-08-28 because PART4 approached the 256 KB
`Read` cap.** Same format, same conventions. **Read PART1–PART5 together**; the constellation
(`MEMORY_CONSTELLATION.md`) carries the chains between facts that these flat lists do not.

- ✅✅★★★★★ [**An endpoint that survives the noise floor — V133 IS scoreable**](../../../../Desktop/Projects/accord-eps-torque-mod/rlog-tools/score/score_v133_creep.py) — every BETWEEN-route endpoint died on route variance, but the symptom is **ENGAGED-ONLY**, so both arms come from **ONE drive**: an **ENGAGED-vs-MANUAL contrast at matched speed** cancels road, tyres, weather and speed profile. **r22 (V112) 7.10 [2.52,16.60] control 1.12 FLAT** · **r24 (V122) 3.88 [1.63,10.47] control 0.61 FLAT** ⇒ **band-specific**, and it **TRACKS THE OPERATOR** — V112→V122 **halved** the engaged excess exactly when he said *"better … rare moments"*. ⚠ **CIs OVERLAP** ⇒ the halving is **suggestive, not significant**; the endpoint resolves a drop to **≤1.6** (outside V122's 1.63 bound). 🛑 **DRIVE DESIGN**: needs **engaged creep AND manual creep over the same stretch at the same speed** — drive the low-speed loop twice. ✅ **PRE-REGISTERED**: ≤1.6 GONE · 1.6–3.0 partial · >3.0 unchanged; **MANDATORY control guard [0.5, 2.0]** — on **r1e and ra6 the control MOVES (7.87, 16.81)** so those contrasts are **global activity, void**, the same failure that killed the relay hypothesis. Shipped as `score_v133_creep.py` with `--validate` reproducing both rows.

- ✅★★★★★ [**The creep endpoint is PRECISE — and it demotes V135 before flight**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — scored every cached route (NW=128) on the within-drive engaged/manual creep endpoint. ✅ **V112's TWO routes give 4.66 and 4.82, agreeing to 3 %** against bootstrap CIs of [2.19,11.08] and [1.99,29.44] ⇒ **the CIs are conservative and real repeatability is ~3 %**, making **V133 vs V122's 3.38 genuinely discriminable** (⚠ n=2, suggestive not measured). 🛑 **The knee ladder is NOT monotone**: **600→4.40, 1800→4.74, 3000→3.38** ⇒ V111→V112 raised the knee and the endpoint got **worse** ⇒ **V135's premise is NOT supported here** (⚠ V122 moved three cells; V111/V112 differ in α2 too) ⇒ **V135 DOWNGRADED** to a duty-ladder step whose symptom effect is unconfirmed and mildly contradicted — still harmless and −17 % friction, **but not a grind fix**. ✅ **The control guard voids 6 of 13 routes**, including **every V104–V107 route** (controls 7.06–8.32) where engaged driving was merely more active — the same failure that killed the relay hypothesis, now caught automatically.

- ✅✅★★★★★ [**V136 built — α2 is a NEW lever with headroom, and it is SELECTIVE**](../../../../Desktop/Projects/accord-eps-torque-mod/analysis-2020accord/builds/v108_plus/build_v136_tva.py) — `0xC40DC` **α2 5→2**, ONE payload byte on a V133 base, **65/65**. ✅ **Mechanism predicts measurement**: |H| over 18–22 Hz falls **4.098→1.849 (2.22×)**; predicted α2 14→8 = 1.22× vs **measured 1.35×**, and same-α2 V111/V112 predicted 1.00× vs measured 1.08× = noise. ⊕ physics closes it — `gp-0x6b26 = −K·accel` is **APPARENT MASS**, less mass raises ζ ⇒ less resonant, **and lowering apparent steering mass is what the operator asked for** ⇒ both goals, same direction. 🛑 **α2 IS A SHARED LEVER**: `gp-0x6c2c` is this EMA's output with **8 gp-based consumers** — the inertia lane `0x36C1A`, the **reversal-detector cluster** `0x428FA/0x4292C/`0x42968` (drives gp-0x671a, itself 4-consumer), two writers, and **two FLOAT model consumers** `0x71378`/`0x7B1A2` (`mulf.s`, one beside `0xC523C`) — but **all were in force across V91/V111/V112/V122 at α2 22/14/14/8, a 2.75× swing, fault-free**; this rung is 2.50×. ✅ **QUANTIZATION GATE**: state is **32-bit**, output `>>9` ⇒ deadband **0.0625 OUTPUT LSB, SUB-LSB** ⇒ cannot stair-step (the one way low α2 could CAUSE the symptom). ✅ **SELECTIVITY GATE**: EMA has **unity DC gain for any α**, so on a 100–400 ms DRIVER reversal the detector loses only **1.03–1.23×** vs **2.22×** on the grind band; the real loss is at 10–30 ms (2.10×/1.68×), acceptable **only because V133 already de-fanged `0xC640A` −8192→−1966**. 🛑 **Supersedes V135's docstring rationale.** ⭐ **V133 STILL FLIES FIRST.**

- 🛑🛑🛑★★★★★ [**V134 RETRACTED — inert at creep; the base-assist damper family is CLOSED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — read the actual records: **FactorC X[0] = 2240/64 = 35.00 km/h**, **FactorE X[0] = 60/4.7121 = 12.73 °/s**, both clamped to Y[0] below X[0]. V134 raises **FactorC Y[0] 0→60** (`0xD77DA`, `0xD77EE`) = the SPEED gate only, but `ch0 = (FactorC × FactorE) >> 10` and **FactorE Y[0] = 0 below 12.73 °/s** ⇒ in the **micro regime (1–13 °/s)** the product is **FactorC × 0 = 0**. 🛑 **V134 reads 0 at creep AND at highway**; its edit bites only at **rate > 12.73 °/s AND speed < 35 km/h** = parking manoeuvres. **Mis-targeted, not mis-sized.** 🛑 **And the family is BACKWARDS**: FactorE is keyed on **RATE ALONE**, so every raise adds **MORE damping at HIGHWAY than at CREEP** (24v2 · 61v39 · 183v87) because FactorC is larger there ⇒ it adds apparent friction where it is least wanted, against the operator's standing instruction ⇒ **[EVIDENCE] family CLOSED for this symptom.** ⊕ independently re-derives the kit memory *"the base-assist damper CANNOT reach the micro regime"*, **which existed and was MISSED when V134 was designed.** ⭐ **RULE: a lever gated by a PRODUCT of two tables is only as open as its NARROWEST gate — evaluate the FULL product at the operator's actual operating point (8 km/h, 6 °/s) before proposing a table edit.** ✅ leaves **V136 (α2 5→2)** as the follow-up of choice: reduces mass, no friction cost. **V133 still flies first.**

- 🛑🛑🛑★★★★★ [**V133 REGRESSED ON-CAR — a SIX-variable build; V137 is the correction**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — operator 2026-08-28: *"massive, violent grinding after enabling LKAS which **continues after disengaging**… also **grind #2 while disengaged** doing a hard turn."* V133 was sold as a clean test of Lever A but moved **SIX cells vs V122**: clamp `0xC407E` **511→1023**, float twin 0.5→1.0, **both** Lever A arms, α2 8→5, `0xC640A` −8192→−1966, and **LKAS gain 5346→7128 (6×→8×)**. 🛑 **Grind #2 disengaged → Lever A's r24 arm** (`0x3AC20`): the gain is engaged-only so it cannot cause a disengaged symptom, and the memory `accord-v81-carries-neither-grind1-fix` says **"r24 half CAUSED grind #2"** — **the record existed and was not checked.** 🛑 **Violent grind #1 persisting after disengage → the clamp**: `gp-0x6b26 = −K·accel` is **APPARENT MASS**, doubling its ceiling **lowers ζ**, and `0xC407E` is **NOT mode-gated** — the builder's *"de-rails without changing linear damping"* was true only of the LINEAR region; **8× added 33 % excitation** into a ζ 0.017–0.036 mode against an explicit instruction. ✅ **The α2 mechanism is REINFORCED** — V133 was an accidental large experiment in the OPPOSITE direction on the same quantity and worsened the car, as predicted. ✅ **V137 = V122 + α2 8→5**, ONE payload byte, 48/48, every implicated cell asserted by name at its V122 value; sizing gate 1.60× ≤ the 1.75× largest α2 step ever flown. 🛑 **V133/V135/V136 all SUPERSEDED** — V135/V136 are V133-based and inherit clamp+gain+Lever A. ⭐ **RULE: diff every candidate against the last FLOWN image, never against its own build parent — the parent chain hides accumulated drift.**

- 🛑🛑★★★★★ [**CORRECTION: V133's regression is **LEVER A**, not the clamp**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the 427 probe on **V90 never approached its rail** (p99 **67**, max **199**, saturated **0.000000**) ⇒ peak |b26| ≈ **159–318** vs a **511** clamp ⇒ **the clamp was NEVER BINDING**, so `0xC407E` 511→1023 was almost certainly **INERT** (⚠ not proven on an 8× build). 🛑 **The prime suspect is Lever A**: `0xAA→0xA9` is **`sar 10`→`sar 9`** = **×2 on the arm**, applied to **BOTH** r24 and r26 **aggregator** arms ⇒ **+6 dB loop gain in a lane that is NOT LKAS-gated** ⇒ **it alone explains BOTH symptoms** (grinding **persisting after disengage**, and grind #2 **disengaged**), which the engaged-only 8× gain cannot. The 8× gain is an **amplifier** of symptom 1, not its cause. 🛑 **Why it looked safe**: the memory's ***"2× ≈ OPTIMUM"*** was measured on **V62's own 4×-gain base** ⇒ **[EVIDENCE] a lever's measured optimum does NOT travel across a base that changed the magnitude of what the lever multiplies.** ✅ **Recommendation unchanged** — V137 holds both arms stock, gain 6×, clamp 511, so it avoids all three suspects. 🛑 **Do NOT spend a build LOWERING the clamp** — it would be a null for the same reason the raise was. ⭐ **RULE: before attributing a regression to a cell, check whether the quantity that cell bounds ever REACHES it.**

- ✅✅★★★★★ [**V140 — a DEADBAND on a confirmed PUMP: the one lever that serves BOTH operator goals**](../../../../Desktop/Projects/accord-eps-torque-mod/analysis-2020accord/builds/v108_plus/build_v140_tva.py) — `FUN_0003aa2c` shows the **r24 pump lane already has a deadband**, cal **`0xC61F6`**, and **Honda ships it at 3 counts = 0.037 % of the ±8192 lane clamp** — a quantization floor, not a dead zone. V140 takes it **3 → 96** (1.17 %), ONE cal, 55/55. ⭐ **It is the only lever shape that does not trade the operator's two goals**: it kills the pump where the signal is SMALL (= grinding/ratcheting) and leaves LARGE commands untouched ⇒ **authority does not pay for it**. ✅ **SAFE**: the deadband **SUBTRACTS rather than clips** ⇒ continuous, `0→0→1→2` across the boundary ⇒ **no notchiness mechanism**; it reduces a **confirmed pump** (`gp-0x6752 = −1`, verified 3 ways incl. on-car, and its config table at `0x1000–0x15xx` is **below the `0x13000` floor every .rwd writes from**); large-signal cost is a 1.17 % offset. ⊕ **Each pump lane clamps to ±8192 of a ±10240 aggregator total ⇒ either lane alone can drive 80 % of the output**, and V133 demonstrated their potency on-car. ⚠ **The DOSE is BELIEF** — the lane amplitude during a grind has never been measured; **if NULL the next rung is 192, not a different lever**; if steering feels vague near centre, back to 48. 🛑 The **r26** lane has **NO deadband** — adding one needs an instruction edit, a separate decision.

- ✅✅★★★★★ [**Authority and "peak command oscillation" MEASURED — two of the three targets are ONE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ✅ **the command does NOT oscillate at peak**: `sc_tq` spectra on r24 give **15–22 Hz = 0.13 % of command power at PEAK vs 0.71 % at LOW**, and 3–8 Hz **0.93 % vs 5.10 %** ⇒ at peak the command is **CLEANER**, reproducing `reference-accord-lkas-lane-is-a-lowpass` ⇒ **[EVIDENCE] the felt peak oscillation is generated DOWNSTREAM in the EPS — it is the SAME problem as the grinding.** Do not build a separate lever for it. (⚠ n=25 peak windows, one route.) 🛑 **Authority is capped on OPENPILOT's side**: it sits at its own **±4096 request limit on 2–4 % of engaged frames on EVERY build** (V91 2.58 · V111 3.24 · V112 3.79 · V122 2.70) and the duty **does not fall as EPS gain rises** across 4×→6× ⇒ with openpilot edits forbidden, the **only** authority lever is EPS gain `0xC6CD0`. ⭐ **So authority and grinding are in tension through ONE cell**, and the operator's own conditional (*"8x only IF we dont get even more oscillation and grinding"*) **fixes the ORDER: fix grinding on the 6× base first (V141), THEN raise 0xC6CD0 to 8× with clamps `0xC61B2/4` 3072→4096 to match, then re-check.** 8× is **deferred, not abandoned** — a gain rise before the fix cannot satisfy him whatever it measures.

- 🛑🛑🛑★★★★★ [**A TRUE NOTCH FILTER EXISTS — the kit believed it did not**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_000352b4`, the only writer of `gp-0x6B86`, holds a **gated 2nd-order FLOAT section**: `w[n] = D·x − A·w[n-1] − B·w[n-2]`, `y[n] = w[n] + C·w[n-1] + w[n-2]` with **A=−1.53720 `0xC60A8` · B=0.63462 `0xC60AC` · C=−1.88080 `0xC60B0` · D=0.81731 `0xC60B4`**. ✅ **Numerator zeros EXACTLY on the unit circle at ±19.88° ⇒ a TRUE NOTCH, min |H| ≈ −74 dB**; poles |z|=0.7966 ⇒ stable; **|H| = 1.0000 at DC AND at Nyquist ⇒ transparent except at the notch** ⇒ **costs NO authority, mass or friction — the only lever in the kit with that shape.** All four coefficients are **cals ⇒ retunable with NO cave.** 🛑 **Falsifies the memory "no notch filter exists anywhere"**; `0xC60A8` is already `BQ_ADDR`, asserted frozen for ~90 builds — **the kit had the ADDRESS, never the FUNCTION.** 🛑 **BLOCKED ON THE TASK RATE**: notch f = 19.88/360×fs ⇒ 250→13.8 · 333→18.4 · 500→27.6 · 1000→55.2 Hz, and task 5 is **bounded ≥250 Hz but NEVER pinned** ⇒ **at ~333 Hz Honda's notch is ALREADY on the grind (lever = the GATE); at 1000 Hz it is at 55 Hz (lever = C → −1.984229 for 20 Hz)** — **opposite edits, so guessing is a coin flip.** ✅ **V143** (V122 + deadband + 427 tap→`gp-0x6B86`, 1 functional + 2 telemetry bytes, 64/64) resolves it: a −74 dB null is unmistakable at 49.9 Hz sampling — **250⇒13.8, 333⇒18.4 direct; 500⇒aliases 22.3; 1000⇒aliases 5.3** — and also shows whether the lane is live and the gate ever opens. ⚠ `0xC649B` enable is **0 stock, 1 in V122**; the other half needs `gp-0x671a ≥ 0xC64FA = 5`, and **`0xC64FA` also selects the `FUN_00036c12` Y branch and two aggregator branches ⇒ NOT a free lever.** ⚠ **tp off-by-0x1000 hit a SIXTH time** — `0xC70A8` returned denormals (1.35e-39); `tp+0x70A8` is **`0xC60A8`**. The denormals were the tell.

- 🛑★★★★ [**CORRECTION: `0xC64FA` ≠ `0xC64FD` — "widening the notch gate rails b26" is WRONG**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — reader census: **`0xC64FA` (notch gate + aggregator) has 18 readers** incl. `0x35A02`/`0x35BE6` inside `FUN_000352b4`; **`0xC64FD` (the b26 Y-branch in `FUN_00036c12`) has 2** — `0x36A1E`, `0x36C42`. **Disjoint** ⇒ lowering `0xC64FA` would **not** touch the b26 branch. ✅ The conclusion stands for a DIFFERENT reason: 18 readers incl. an **unexamined 10-reader cluster at `0x260BC`–`0x261A2`** ⇒ still not a free lever. ⭐ **RULE: two cals three bytes apart, both = 5, in the same subsystem, are still TWO CALS — run the reader census before asserting a shared consumer.**

- 🛑🛑★★★★★ [**`0xC64FA` fully characterised — "notch always on" CLOSED by a real mechanism**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 the **"18 readers" was the `ld.bu` disp|1 trap**: all 18 encode `hw2 = 0x74FB` but split by hw1 — **`0x85` family → `0xC64FA`** (`0x3AA78 ld.bu 0x74fa` = `8577fb74`), **`0xA5` family → `0xC64FB`** (`0x260BC ld.bu 0x74fb` = `a57ffb74`) ⇒ the *unexamined 10-reader cluster reads a DIFFERENT CAL*. ✅ **`0xC64FA` has EIGHT readers, all known**: the notch gate (`0x35A02`, `0x35BE6`), the aggregator (`0x3AA78`), and the reversal counter's own CEIL clamp (`0x429DA..0x42A08`). 🛑 **The closure**: setting it to 0 makes the notch condition `0 <= gp-0x671a` always true — but the aggregator reads the same cal, and **gate SHUT + `gp-0x6b5e != 0` forces the r26 lane to ZERO** while **gate OPEN computes it** ⇒ **opening the notch's gate ENABLES the r26 PUMP** (`gp-0x6752 = −1`), the family whose doubling caused V133's violent grinding ⇒ **[EVIDENCE] notch arming is STRUCTURALLY COUPLED to un-suppressing a pump.** ⊕ `0xC64FA = 1` is the same trade at a smaller dose. ⭐ **V145 is correct as built — leave the cal alone and MEASURE the gate; if it reads shut the fallback is V141, NOT widening.**

- 🛑🛑★★★★ [**RETRACTION: the r26/notch-gate coupling is UNRESOLVED, not [EVIDENCE]**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the closure "opening the notch gate enables the r26 pump" hinges on **`gp-0x6b5e != 0`**, which was **ASSUMED**. Its only writer `FUN_000361c8` computes `gp-0x6b5e = ±(gp-0x6752 × ((LERP(gp-0x6bda) × cal(0xC63C2)=1024) >> 10))` with **X = [−384,−128,128,294,384], Y = [0,4762,4762,717,0]**. 🛑 **Two errors, same class**: the closure never checked `gp-0x6b5e`; then my "refutation" put `gp-0x6bda = 0` in the **below-X[0]** branch (→Y[0]=0) when **0 lies MID-TABLE between Y[1] and Y[2] = 4762** ⇒ non-zero ⇒ the coupling would bite. ⊕ And the index is unknown anyway: the memory's *"gp-0x6bda gate = 0.0000"* is a **derived boolean, not the raw cell**. ✅ **Honest state: UNRESOLVED**; settled by probing **gp-0x6bda / gp-0x6b5e** on an engaged drive. ⭐ **V145 is unaffected** — it does not move `0xC64FA`, it MEASURES the gate; the coupling only matters if the gate reads shut. ⭐ **LESSON, paid for FOUR times this session** (V133's clamp · `0xC64FA`≠`0xC64FD` · 18-vs-8 readers · this): **mark [EVIDENCE] only when the number was READ from the image or logs — knowing WHERE a value comes from is not knowing WHAT it is.**

- ✅✅★★★★★ [**V146 — the notch RE-SIZED from MEASURED grind data; supersedes V144/V145**](../../../../Desktop/Projects/accord-eps-torque-mod/analysis-2020accord/builds/v108_plus/build_v146_tva.py) — measured the dominant 14–30 Hz `cs_rate` peak, **engaged, 1–24 km/h**, pooled over **12 routes / 1180 windows**: **p10 14.84 · p25 17.19 · p50 20.31 · p75 21.88 · p90 23.44 Hz**. ✅ **V144's CENTRE was right** (p50 20.31 vs 20.0) 🛑 **but its WIDTH was not** — r = 0.98 covers only **68.2 %** of those peaks ⇒ a third of the grind escaped. ✅ **V146 = 20.3 Hz, r = 0.96**: `A=−1.90440325 B=0.9216 C=−1.98375338 D=1.05848204` ⇒ **mean |H| at the measured peaks 0.347 vs 0.514 = 1.48× better, coverage 90 % vs 68 %**, for a 10 % lift at 500 Hz; DC still **1.000000**, 1 Hz 0.9994, 3 Hz 0.9941. 80/80. ⭐ **The no-boost gate was REPHRASED, not relaxed**: V144's *peak ≤ 1.05* would have **vetoed the better filter**, because in a unity-DC notch the peak is ALWAYS the **Nyquist shelf**, not a resonance — it now asserts peak ≤ 1.12 **AND peak frequency > 200 Hz**, catching the real danger (my first retune's **3.82 = +11.6 dB resonance just BELOW the notch**) that a magnitude bound alone would have let through. ⚠ the gate-arming belief (`gp-0x671a ≥ 5`) is unchanged and still unmeasured; re-sizing makes the notch worth more **if** the gate opens and changes nothing if it does not.

- ✅✅★★★★ [**V146 VALIDATED by simulating the firmware recursion itself**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ✅ **steady-state output == predicted |H|×12 to 4 dp at every frequency, for BOTH Honda's and V146's coefficients** ⇒ **the recursion transcribed from `FUN_000352b4` IS the designed filter** (previously an assumption). ✅ **Honda passes 10.504 of a 12 input at 20.3 Hz ⇒ as shipped its notch does NOTHING for an 18–22 Hz grind**; V146 passes **0.000**, with **4.35 @ 18 Hz / 3.31 @ 22 Hz** vs Honda's 10.84 / 10.22. ✅ **The alarming max|y| is PURELY the startup transient** — settling ≈ 1/((1−r)·fs) = **25 ms at r=0.96 vs 50 ms at r=0.98** ⇒ **the wider notch settles TWICE AS FAST**, an unanticipated argument FOR V146 if the gate chatters. ⚠ **Clipping of the ±12 output clamp is NOT introduced by the retune**: Honda's own coefficients clip on a full-scale step (12.507) and 5 Hz square (13.014); V146 reaches 13.812 / 15.592, and overshoot **barely moves with r** (0.94→15.89, 0.98→15.43) ⇒ inherent to the stage, a **bounded saturation**, transient-only, ~20 % worse than Honda.

- 🛑🛑🛑★★★★★ [**The notch gate almost certainly NEVER OPENS — V144/V145/V146 are INERT; fly V147**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V90 tapped `gp-0x6B26` DIRECTLY** (tap `0x94DA`, sar 3) and **r77 measured wire max 199** (vs 1023 saturation ⇒ **not clipped** ⇒ |b26| max **318** is a TRUE max). Back-solving `|c2c| = b26·61440/|Y|` with the mode records **X=(0,1280,5760) Y=(−9830,−5734,−1966)**: **|gp-0x6c2c| max ≈ 1,990 (creep) / 3,412 / 9,950 (smallest Y)** vs the detector threshold **cal(0xC620A) = 12,800** ⇒ **never reaches it under ANY attribution** ⇒ `gp-0x671a` never hits 5 ⇒ **the notch gate NEVER OPENS**. ⊕ **V122's α2 = 8 vs V90's 22 = MORE smoothing ⇒ peaks LOWER still ⇒ the conclusion STRENGTHENS on the newer base.** ⇒ **[BELIEF, well-founded] V144/V145/V146 are INERT** — the notch is real, correctly retuned and validated, it simply never runs. ✅ **V147 = V122 + pump deadband 3→96 + the BINARY gate probe** (`gp-0x6C24`, sar 3→1 or both values map to wire 0 and the probe is blind), **4 payload bytes, 1 functional, 67/67** — the deadband does **not** depend on the gate, so one drive gives a live lever **and** settles the family: **duty > 0 ⇒ back-solve wrong, fly V146; duty = 0 ⇒ retire V144/V145/V146.** ⭐ **METHOD: before designing around a threshold, back-solve whether the quantity ever REACHES it from data already flown** — four builds were designed around a gate whose input the cache had already measured.

- 🛑🛑★★★★★ [**Two direct cache measurements — the detector probe is CENSORED, and the notch lane is TINY**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a tap census found both cells already flown: **`gp-0x6C2C` by V107–V110 (r1b, r1e)** and **`gp-0x6B86` by V104–V106 (ra4–ra6)**. 🛑 **The detector-input read is CENSORED**: r1e engaged n=49,089, p50 **123**, but the 427 wire **saturates at 1023 = |c2c| 1637 on 3.2 % of engaged frames**, and the threshold **12800 is 7.8× above the clip point** ⇒ **it CANNOT settle whether the gate opens.** ⇒ **the "gate never opens" claim is downgraded from [EVIDENCE] to [BELIEF, well-founded]**, resting on the V90 back-solve, which is better only because it was **UNCLIPPED** (wire max 199 vs 1023). ⊕ **This is exactly why V147 probes `gp-0x6c24`: a BINARY mirror CANNOT clip.** ✅ **The notch lane IS live** — `gp-0x6B86` reads zero on only 11–31 % of engaged frames, refuting the "lane dead" branch in advance — 🛑 **but it carries p50 ≈ 6–19 counts of a ±12288 range, ~0.1 % of its own gate** ⇒ even a running notch would filter a nearly empty lane. ⇒ **the notch family is weak on TWO independent counts.** ⭐ **V147 is still the build to fly** — the measurements changed the CONFIDENCE, not the plan, and retired an [EVIDENCE] mark that had not been earned.

- ⭐⭐★★★★★ [**The probe census reframes the search — the kit has been tuning the SMALLEST lanes**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every flown lane on one scale, engaged p50 from cache: **`gp-0x6B94` AGGREGATOR OUTPUT 115–218** · **`gp-0x6BBE` viscous+pedestal 74** · `gp-0x6B4C` 0–26 · **`gp-0x6B86` notch lane 6–19** · **`gp-0x6B26` b26 inertia 2–5**. 🛑 **`gp-0x6BBE` is ~HALF the entire assist output at creep, while `gp-0x6B26` — the target of ~15 builds (clamp, α2, knee, K1) — carries 2–5 counts.** ⇒ **[EVIDENCE] the builds have gone to the smallest lanes.** ⭐ **RULE: rank lanes by MEASURED p50 before choosing what to tune** — `probe_census.py` makes it one command. 🛑 **Fifth value-assumption error, caught by reading**: I inferred the 74-count pedestal was the lane SATURATING at its clamp; the clamp records (`PTR_DAT_000c7970[mode]`, all four identical) are **X=[0,640,2560,5760,6400] Y=[512]×5 — FLAT at 512, never reached** (lane max 352) ⇒ **the pedestal is genuine and the clamp is NOT the lever.** ✅ `FUN_00034a72` exposes **`0xC6370` = 2560, the weight on `gp-0x6c2e` (the ACCELERATION twin)** and **`0xC6372` = 205, the torque-EMA α** ⇒ `0xC6370` is the same KIND of lever as α2 but on a lane with **15–35× more signal**. ⚠ **NOT a proposal yet** — `gp-0x6bbe` is an ASSIST lane, so cutting it makes steering HEAVIER, against the operator's first goal; the trade must be sized first. **V147 is still the flight build; this changes what comes AFTER.**

- ⭐⭐⭐★★★★★ [**WHICH LANE CARRIES THE GRIND — measured; two families close, a new target appears**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ranked every probed lane by **18–22 Hz share of its OWN engaged variance** (control 8–12 Hz; 30–40 aliases at 49.9 Hz). 🛑 **`gp-0x6B26` ratio 0.51 / 0.54 — BELOW its own control on both its routes ⇒ b26 is NOT the grind carrier**, and ~15 builds (clamp, α2, knee, K1; V126–V138 and most of this session) were aimed there — which explains that family's nulls. 🛑 **The notch lane reads 4.61 / 1.07 / 1.00 %, the LOWEST of any lane ⇒ THIRD independent reason V144/V145/V146 are retired** (with the shut gate and the p50 6–19 lane) — the notch is real and correct, just on the wrong lane behind a shut gate. ✅ **NEW TARGET: `gp-0x6B4C`, the 11-slot assist sum — 22.84 % and 22.50 % on BOTH its routes**, the only consistently elevated lane (ratios 1.34, 3.08). Its cal **`0xC4124` = [0,0,5,0,5,5,0,0,0,5,0]** is a **per-slot weight vector, 4 slots active (2,4,5,9)**, 5 readers all in its producer ⇒ **it can remove ONE contributor without touching the rest — exactly the lever shape this search wanted.** 🛑 **Not yet a proposal**: what the four slots SOURCE is unknown; next step is tracing the five readers. ⭐ **CORRECTED RULE: rank lanes by BAND POWER in the symptom band, not by p50** — p50 found `gp-0x6BBE`, which proved **torque-dominated 11:1** and neither the source nor a safe lever; band power found `gp-0x6B4C`, which p50 ranked near the bottom.

- ✅★★★★★ [**The `gp-0x6B4C` lever is IDENTIFIED — blocked only on WHICH slot**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00027b0a` walks an **11-slot TYPE DISPATCH**: `movea 0x5124,tp` (the cal) + `movea -0x62f8,gp` (the data array), 10 iterations, `cmp 7/6 -> r24 += slot ; r28 += 0x400`, with **r24 the SUM and r28 the WEIGHTS**. 🛑 **The cal bytes are TYPE CODES, not weights** — I first read them as weights; the kit memory is right that `[0,0,5,0,5,5,0,0,0,5,0]` = *"7 slots raw, 4 forced zero"* ⇒ **type 0 = CONTRIBUTES, type 5 = FORCED ZERO**, so the four I called active are the DISABLED ones and the **seven zeros (0,1,3,6,7,8,10) are the contributors**. ✅ **THE LEVER: `0xC4124[i] : 0 → 5` disables slot i** — removes **ONE contributor** from the top grind carrier, using Honda's own dispatch value, a **byte cal, no cave**; nothing else found this session can subtract a single source. 🛑 **BLOCKED on WHICH slot**: they are written through a **COMPUTED POINTER** (`ep = gp-0x62F8 + r12`), so a byte scan sees only the base (15 hits) and slots 1–9 show **0 accesses each**; spot-reading `0x25EA6` gives `add r1,ep` + `sst.h r0`, a **zeroing LOOP**, not a per-slot store ⇒ **needs real dataflow tracing of the 15 writers.** ⭐ **Deliberately NOT guessing a slot** — that is the error class committed five times this session. **V147 still flies; this is the best NEXT direction, blocked on bounded mechanical tracing rather than a missing idea.**

- ✅✅★★★★★ [**The 11 slots ARE identifiable — `FUN_00025c32` is a REGISTRATION function**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the earlier "unidentifiable" note looked at the WRITES, which all use a loop register through `ep = gp-0x62F8 + r12`. **The right place is the SIGNATURE**: `FUN_00025c32(byte *param_1)` does `iVar9 = min(*param_1, 10)` — **the SLOT INDEX comes from the CALLER** — then writes `gp-0x62e0/62f8/6274/633c/6230 + iVar9*2` from `param_1[2..10]` with per-field clamps, plus a per-slot LIVE STATE byte at `gp-0x61a0 + iVar9` (distinct from the `0xC4124` cal of the same shape). ✅ **TEN CALLERS for eleven slots**: `FUN_00023ad2 00023fe2 0002b422 0002c246 0002caa2 0002e52e 000339cc 0003405a 0003a8a8 0003aff4` ⇒ **a bounded enumeration: decompile each, read `param_1[0]`, and the slot map is complete.** ⊕ Two sit beside the aggregator `FUN_0003aa2c` and one beside the damper/`gp-0x6bbe` producers, so the map will name real subsystems. ⭐ **Then `0xC4124[i] : 0→5` removes ONE NAMED subsystem from the top grind carrier** — byte cal, no cave, the most precise lever this kit has had. 🛑 **Not built**: disabling an unidentified slot is the V133 pattern; and a *disable-all-seven* experiment was **rejected** because `r28` accumulates weights and four divide-class instructions exist in `FUN_00027b0a` (0x28168/2820C/2847C/285A0) — none divides by `r28` in a direct read, but that scan is not exhaustive and a divide-by-zero in an EPS control path is not worth an incomplete check. **V147 still flies.**

- ✅✅✅★★★★★ [**THE SLOT MAP IS COMPLETE — the `gp-0x6B4C` lever is now AIMABLE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ten `FUN_00025c32` call sites by xref; **`FUN_0003a8a8` decompiled IN FULL to validate the method** (`local_1c = 7;` then `FUN_00025c32(&local_1c)`) ⇒ **the last `mov imm,rN` before the struct setup is the slot index.** Map: **0←FUN_0002e52e · 1←FUN_0002b422 · 2←FUN_0003405a · 3←FUN_0002c246 · 4←FUN_00023ad2 · 5←FUN_00023fe2 · 6←FUN_0003aff4 · 7←FUN_0003a8a8 · 8←FUN_0002caa2 · 9←FUN_000339cc · 10 unused**. ⭐ **TEN callers → TEN DISTINCT slots, no collisions** — a wrong rule would give duplicates and gaps and gives neither. With `0xC4124 = [0,0,5,0,5,5,0,0,0,5,0]` (0 = contributes, 5 = forced zero): **contributors are slots 0, 1, 3, 6, 7, 8**; but **`FUN_0003a8a8` passes ALL-ZERO data fields** ⇒ **slot 7 contributes nothing numerically** ⇒ **five real candidates: 0, 1, 3, 6, 8**, each a NAMED function. ✅ `0xC4124[i] : 0→5` disables one, using Honda's own dispatch value, **byte cal, no cave** ⇒ **aimable at a named subsystem, the most precise lever this kit has had.** ⚠ **Not built**: `FUN_0003aff4` (6) and `FUN_0002caa2` (8) are unread, and removing an unknown assist component is the V133 pattern. **Next: decompile the five and pick the one whose payload is rate/acceleration-derived rather than torque-derived.** V147 still flies.

- 🛑🛑★★★★★ [**CORRECTION: the band-share ranking was inflated by the FLAT baseline**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — naming `gp-0x6B4C` "the highest-ranked grind carrier" at 22.8 % quoted a **raw share with no flat-spectrum control**, and **any 4 Hz window in 1–24 Hz holds 17.4 % by construction**. Re-ranked: **11-slot sum 1.44× / 1.38× flat**, default tap 1.42×, detector input 1.16×, **everything else < 1.0×** ⇒ **NO lane shows a strong 18–22 Hz peak**; on **r9e the CONTROL reads 1.03× flat**, i.e. featureless. ✅ **What survives**: `gp-0x6B4C` is the most CONSISTENT candidate (top on both routes; the default tap swings 1.42/0.53/0.08 and is unreliable), and the **b26 (0.43–0.56×) and notch (0.06–0.27×) closures are STRENGTHENED** — far below baseline. 🛑 **The slot discriminator also FAILED**: none of the five candidate callers reads a rate/accel cell, only speed ⇒ with slot 7's all-zero payload they read as **thin STATE REGISTRANTS**, matching `gp-0x6B4C`'s **p50 0–26** (r9e p50 = **0**) ⇒ a mostly-zero, occasionally-spiking signal is **BROADBAND**, which is exactly why its share sat near flat — **the two findings explain each other.** ⭐ **PROCESS: `feedback-run-the-control-before-the-measurement` already said this** — I ran the baseline AFTER publishing, and the ranking did not survive it (**sixth self-correction today**). **Compute the flat baseline in the same function as the share so they cannot be reported apart.** ⇒ **`gp-0x6B4C` is NOT a build candidate at this confidence; V147 still flies.**

- ✅✅★★★★★ [**First cross-build regression on the SYMPTOM — 19 routes; `knee=300` is catastrophic**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — scored all 19 cached routes with a known build on the within-drive ENGAGED/MANUAL 18–22 Hz `cs_rate` creep endpoint and regressed against the differing cals. **Spearman: knee −0.770 (p<0.001) · α2 +0.612 (p=0.005) · K1 −0.477 (p=0.039) · gain +0.326 (p=0.173, n.s.)**. ✅ **ROBUST: all NINE knee-300 routes are elevated (11→1521) while every knee ≥ 600 route sits 2.6–51.8** ⇒ that one cal explains the whole V101–V107 era — the strongest cross-build effect ever measured here (⊕ historical, not a live lever: V122 is at 3000). 🛑 **The three significant cals are COLLINEAR — ONE variable, not three**: the gain-holding invariant scales knee+K1 together (300/204, 600/204, 1800/612, 3000/1020) and α2 tracks knee across history ⇒ their separate p-values are not independent evidence. 🛑 **The ordering ABOVE 600 does NOT hold**: knee 600 median ~10.2 (but spans 2.60–51.81), 1800 ~8.9, 3000 = 8.18 (n=1) ⇒ **within-group variance swamps it; no evidence 600 beats 1800/3000** — an "optimum ≈ 600" reading would be a lucky-route artifact. ✅ **Gain NOT significant** across 4×/6×/8× ⇒ mildly supports V142 (8×) being safe once grinding is fixed (⚠ one 8× route). ⚠ **SELF-CAVEAT: this run gives V112 8.51/9.21 and V122 8.18 where the earlier creep run gave 4.74 and 3.38 — same data, different window parameters** ⇒ **the endpoint is sensitive to analysis choices; treat ORDERING as informative and absolute values as NOT comparable across analyses, and always quote the window parameters with any number from this table.**

- 🛑🛑🛑★★★★★ [**THE BETWEEN-BUILD ENDPOINT HAS A 20–36× NOISE FLOOR — every build comparison this session is RETRACTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the control never run: **routes with IDENTICAL control cals**. **gain 3564 / α2 22 / knee 600 / K1 204, n=6: 2.60 → 51.81 = 19.9×.** **gain 5346 / α2 22 / knee 300 / K1 204, n=6: 42.01 → 1520.81 = 36.2×.** ⇒ **no build comparison below ~36× carries information.** 🛑 **ALL of this session's comparisons fail**: *"knee is null"* 1.08× · *"α2 IS the lever"* 1.40× · *"3 % repeatability"* 1.03× · *"knee=300 catastrophic"* 29.35× — **including the knee-300 result called robust one section earlier**, whose group's OWN spread (36.2×) exceeds the difference attributed to it. 🛑 **RETRACTED**: the α2-vs-knee single-variable ladder (which drove V137/V138 sizing and the V135 demotion); the *"endpoint is precise, V112's routes agree to 3 %"* claim (n=2 of the SAME build is the floor's BEST case, and the 6-route identical-cal group spans 19.9×); and *knee=300*. ⊕ **the BUILDS are unaffected — only the ranking rationale is withdrawn.** ✅ **SURVIVES (non-statistical)**: the notch's existence/retune/validation, the 11-slot map, the pump polarity, the deadband location, the `0xC64FA`≠`0xC64FD` split, the instrument findings (probe clipping, flat baseline, `ld.bu` disp|1), and **V133's regression — from the OPERATOR'S EAR, not a scorer.** ⭐ **CONCLUSION: this kit has NO working between-build symptom endpoint; the operator's report is the only instrument that can tell builds apart** — exactly what *"score bands, let the OPERATOR score symptoms"* already said. 🛑 **A scorer comparing two builds must REFUSE below ~36×.** ⊕ `feedback-episodes-not-windows` warned of this class: *get a split-half null BEFORE quoting any ratio* — **the identical-cal groups ARE that null and were available from minute one.**

- ✅✅✅★★★★★ [**A WORKING between-build endpoint recovered — noise floor 1.8×, down from 36×**](../../../../Desktop/Projects/accord-eps-torque-mod/rlog-tools/score/score_creep_share.py) — the ratio endpoint died because it **explodes when the manual arm is small** (r9e: 1520.81 on 39 manual windows). Fixed by **(1) a SHARE not a ratio** — (18–22 Hz)/(1–45 Hz) engaged at creep, **bounded in [0,1], cannot explode** — and **(2) an n ≥ 90 window gate ≈ 2 minutes of engaged creep**, since the low-n routes (r81 45, r96 70, r9e 57) drove the residual spread. **Identical-cal floor: 7.22×→1.62× and 5.59×→1.79×** ⇒ **effects above ~1.8× are now detectable — a 20× resolution improvement.** ✅ **It settles the knee with a proper instrument**: knee 300 median **0.0887** vs knee 600 median **0.0866** — **IDENTICAL** ⇒ the knee has no measurable effect, **confirming** the earlier retraction rather than merely withdrawing it. 🛑 **Caveats**: the n≥90 gate is **post-hoc** (chosen after seeing the outliers; a minimum-exposure rule is defensible a priori but the number wants validation), and **only 8 routes qualify — V112 (52, 24) and V122 (45) do NOT**, so **the build on the car has never had enough engaged creep to be scored.** ⭐ **DRIVE REQUIREMENT: ≥ 2 MINUTES of ENGAGED CREEP (1–24 km/h, hands off, real steering activity)** or the drive is unscoreable. The scorer **refuses rather than guessing** — r24 returns NOT SCOREABLE, r77 (n=260) returns a verdict. Shipped with `--floor` to re-derive the floor on demand.

- 🛑🛑🛑★★★★★ [**NO CAL EVER FLOWN MOVES THE CREEP GRIND — the strategic result**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — with the working endpoint (floor **1.8×**), every tuned cal tested against it: **gain 4× vs 6× = 1.05× · α2 14 vs 22 = 1.02× · knee 300 vs 600 = 1.02× — ALL below floor**, while **V96's OWN two routes differ 1.49×**, most of the entire 2.42× range across all 8 scoreable routes. ⇒ **[EVIDENCE] across gain 4×/6×, α2 8–22, knee 300–3000, K1 204–1020, NO calibration this kit has ever flown has a detectable effect on the creep grind.** ⭐ **~30 builds of cal tuning moved cells now measured INERT** — which explains that whole run of nulls at a stroke. ⇒ **either the controlling cal has never been flown, or the grind is not cal-controllable** ⇒ **this is why V147 is right: `0xC61F6` is a cal NEVER FLOWN on a lane NEVER PROBED**, and the same argument elevates the `0xC4124` slot disable. ✅ **It also clears the gain**: 1.05× agrees with the 12-route null and the 19-route regression (p=0.173) ⇒ **the LKAS gain does not drive the creep grind, so V142 (8×) is not a grinding risk** — still gated only because the operator's instruction was conditional (⚠ no 8× route passes n≥90, so 8× is untested here). ⭐ **Families CLOSED by measurement: b26, the notch, the base-assist damper, `gp-0x6BBE`, and now EVERY flown cal. Still open: the two levers never flown. The only instrument that has ever resolved a build is the OPERATOR'S REPORT.**

- ✅✅★★★★★ [**19 VIRGIN assist-path cals + the `FUN_00038148` per-term weight map**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — since **no flown cal moves the grind**, the answer must be in a cal never flown. Census over **131 images** (948 cells ever touched) → **19 virgin assist-path cals**. ✅ **Weight map verified from the decompile**: `sum = Σ (lane × admission-gate × cal) >> 10` then **× gp-0x6752 (the −1 polarity), ×16, through a cal(0xC63AC)=102 IIR → gp-0x6b70**, with **`0xC63A0`→gp-0x6bd0 (damper, ±0x800, TOUCHED) · `0xC63A2`→gp-0x6bbe (±0x800) · `0xC63A4`→gp-0x6b46 (±0x400) · `0xC63A6`→gp-0x6b26 (±0x400) · `0xC63A8`→gp-0x6b4e (±0x2800) · `0xC63AA`→gp-0x6b4c (±0x2800)** — **all UNITY 1024 in stock and V122; only `0xC63A0` ever moved.** ⭐ **`0xC63AA` is the best-formed lever found all session**: a per-term weight on the **most consistent grind carrier**, unity baseline, virgin, cal-only, **continuously dosable** ⇒ **strictly better than disabling an unidentified slot inside that lane** — same target, no slot map needed, scales instead of zeroing. 🛑 **Honest check**: this loop outputs **`gp-0x6b70`, whose band share is 0.46–0.52× flat — BELOW baseline** ⇒ not itself a grind carrier, so the weight may not reach the symptom; and `gp-0x6b4c` **also enters the aggregator directly**, a path this weight does not touch. ⇒ **[BELIEF] well-formed; [UNKNOWN] whether it reaches the symptom — NOT a build yet. V147 still flies first** (equally virgin cal, and its lane feeds the aggregator directly).

- 🛑★★★★ [**`0xC6372` sized and REJECTED before building — and it narrows the field to ONE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the aggregator adds its ten lanes **BARE (admission gates only, no per-lane weights)** ⇒ **a lane can only be scaled at its SOURCE**. For `gp-0x6BBE` (largest lane, p50 74) all three source cals are virgin, and `0xC6372` (torque-EMA alpha, task 5, fs≈500) **sizes well**: α 205→77 gives **2.24× at 20 Hz for 9 % at 3 Hz**, and the lane is **11:1 torque-dominated** so this alpha shapes **~92 %** of its input. 🛑 **Still pointless**: `gp-0x6BBE`'s band share is **0.54× FLAT — BELOW baseline**, i.e. it carries LESS 18–22 Hz than a featureless spectrum ⇒ **attenuating 20 Hz in a lane that does not carry it achieves nothing; V148 would have been a null.** ⭐ **RULE: size the lever, THEN check the lane carries the band — in that order, because the sizing is the seductive part.** ⭐ **Field narrowed to ONE**: `gp-0x6B4C` (1.44× / 1.38× flat) is **the only lane consistently above flat** — the default tap swings 1.42/0.53/0.08, and `gp-0x6BBE` 0.54×, `gp-0x6B26` 0.43–0.56×, the notch lane 0.06–0.27× are all BELOW. Since the aggregator adds it bare, its **only** lever is **`0xC4124`, the slot vector** (`0xC63AA` scales it only into `gp-0x6b70`, itself below baseline ⇒ wrong output). ⚠ evidence base is **1.4× flat = weak**, and it still needs the five payload decompiles. **V147 still flies.**

- ✅✅★★★★★ [**`gp-0x6B4C` contributors narrowed to FOUR — method validated**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_0003a8a8`'s decompile fixes the struct: **[0] slot · [1] mode · [2][4][6][8] DATA · [10][12][14] CONSTANT 0x400 weights** ⇒ an earlier scan counted the weights as "computed fields" and wrongly called every caller a contributor. Reading only offsets 2/4/6/8: **slots 0/1/3/8 write offset 4; slot 6 writes offset 8; slot 7 writes NOTHING** — and **slot 7's all-zero reading MATCHES its full decompile, validating the method** before trusting it on unknowns. ✅ **The offset decides the array**: `FUN_00025c32` sends **offset 4 → `gp-0x62f8`, the array the 11-slot sum walks**, while offset 8 → `gp-0x633c`, a different array ⇒ **ONLY slots 0, 1, 3, 8 feed `gp-0x6B4C`; slots 6 and 7 do NOT.** Candidates: **slot 0 `FUN_0002e52e` · slot 1 `FUN_0002b422` · slot 3 `FUN_0002c246` · slot 8 `FUN_0002caa2`**, all in one cluster `0x2B4xx–0x2E5xx`. ⊕ seven unknown indices → four named functions, and `0xC4124[i] : 0→5` can now be aimed at any one. ⚠ **not a build**: what the four compute is unread and the lane's evidence is **1.4× flat = weak**. **V147 flies first.**

- 🛑🛑🛑★★★★★ [**`gp-0x6B4C` is a FAULT-ARBITRATION sum — last cal candidate CLOSED, and the lever was UNSAFE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_0002caa2` (slot 8) is a **LATCHING PLAUSIBILITY MONITOR**: it passes `gp-0x6b12` until it exceeds `cal(0xC61D2)`, then sets fault flag `gp-0x6788`, advances `gp-0x3cba` 1→2 and **latches its contribution to ZERO permanently**. ⇒ **`gp-0x6B4C` is NOT an assist sum — it is a sum of MONITORED contributions behind latching fault gates**, and `0xC4124` is that monitor's per-slot CONFIGURATION. ✅ **This explains every measurement at once**: p50 0–26 (contributions latch off) · max 1459–1664 (spikes before latching) · broadband spiky spectrum (latching is impulsive) · and the **"1.4× flat" share as an IMPULSIVENESS ARTIFACT, not 20 Hz content** ⇒ **[EVIDENCE] not the grind carrier; the last cal-path candidate is CLOSED.** 🛑 **And the lever was UNSAFE, not just weak**: `0xC4124[i] : 0→5` would have **disabled a plausibility monitor** on an EPS — the kit came within one decompile of proposing that on 1.4×-flat evidence. ⭐ **RULE: before disabling any per-slot/per-term configuration cell, read what the slot COMPUTES — a weight vector and a monitor configuration table look IDENTICAL from the cal side.** ⭐ **Cal space exhausted**: closed by measurement = b26, the notch, the base-assist damper, `gp-0x6BBE`, every flown cal, and now `gp-0x6B4C`; rejected before building = `0xC6372` (lane below flat) and `0xC4124` (a fault monitor); **still open = `0xC61F6`, the r24 pump deadband, which V147 tests** — virgin cell, lane feeding the aggregator DIRECTLY, confirmed pump polarity behind it. **The analysis has converged; the remaining information is on the car.**

- ✅✅★★★★★ [**V139 RE-ELEVATED — its evidence survived the retraction because it never used the endpoint**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the noise-floor retraction was applied to this session's comparisons but **not propagated to older claims of the same class**. 🛑 **V62's headline is a BETWEEN-BUILD ratio of 8×** (18–22 Hz 0.124 [0.036,0.387] vs V59; 42× at |rate| 16–32 °/s) and the measured between-route floor is **20–36×** ⇒ **the 8× sits INSIDE the floor of a closely related statistic.** ⚠ NOT invalidated — different statistic, and it carried a **passing 30–40 Hz control**, which is more than a bare ratio — but the caveat was never recorded, and the **42× is the more robust half**. 🛑 **A contradiction never stated plainly**: **V62 (sar 9, 4× gain) → "grinding is GONE"** vs **V133 (sar 9, 8× gain) → "massive, VIOLENT grinding"** — same two bytes, opposite outcomes, different base gain. ⊕ Gain-normalising V62 onto the 6× base computes to `0xC6446 ≈ 6989`, **NOT proposed** because it **RAISES a confirmed pump** — the V133 direction. ⭐ **Between an 8× between-build ratio and a recent unambiguous operator report, the operator's report wins**; the kit has now measured that its statistics cannot resolve builds while his ear demonstrably can. ✅ **V139 re-elevated to co-primary with V147**: its rationale is (1) `gp-0x6752 = −1` verified 3 ways ⇒ confirmed pump, (2) V133 doubled these bytes and the car got violent ⇒ **proven potent on-car**, (3) reducing a feedback magnitude is **safe by construction** — **none of which uses the retracted endpoint**, whereas I demoted it partly ON that endpoint and never undid it. ⊕ arguably **stronger than V147**, whose lever's potency is unknown (`gp-0x6ADA` never flown). ⚠ caveat unchanged: sar 9 worse than 10 does not prove 11 better; expected failure is number steering without grind improvement ⇒ revert to V122.

- 🛑🛑🛑★★★★★ [**CAPSTONE AUDIT: essentially EVERY historical between-build grind ratio is BELOW the noise floor**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — measured floor **20–36×**; sweeping the memory index: **V72 1.05× · V76 1.8× · V75/V74 2.9× · V62-vs-V59 8× · V107 10× — ALL BELOW**; **V69's ladder 23× and V62's 42× at |rate| 16–32 °/s are MARGINAL**, the latter the only one clearing 36×. ⇒ **[EVIDENCE] the kit's entire history of between-build grind comparisons is uninformative.** ⊕ several carried negative controls, which is more than a bare ratio and is why they are *marginal* not *dead* — but none clears the floor on magnitude. ✅ **WHAT SURVIVES: the OPERATOR'S REPORTS, because they are not ratios** — V62 *"grinding at 2-5 mph is GONE"*, V80 *"worst ever"*, V88 fixed, V122 *"better … rare moments"*, V133 *"massive, violent … continues after disengaging"*. ⇒ **every durable thing this kit knows about grinding came from his ear**, and every number attached to those reports is individually within route noise. ⭐ **The doctrine "score bands, let the OPERATOR score symptoms" was right all along and under-obeyed.** ⭐ **FOR THE NEXT DRIVE: (1) judge by ear — scorers are for MECHANISM questions (gate open? lane live? notch running?), NOT build ranking; (2) `score_creep_share.py` (floor ~1.8×) is the ONE exception and needs ≥2 min engaged creep, refusing below it; (3) do NOT re-derive conclusions from the historical ratios — any future quote of one must state it against the 20–36× floor.**

- ✅★★★★ [**V139's design VERIFIED optimal — and a flag on V88's "Lever B"**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a `sar` edit is locked to ÷2, so a finer cal-only variant would be preferable; **there is none.** All five multiplier cals are **BRANCH-GATED**: `0xC643E` r26=1536 and `0xC6440` r24=2048 need **`gp-0x671a ≥ CEIL`, the gate measured as effectively NEVER OPEN**; `0xC6444`/`0xC6446`/`0xC6442` need specific `gp-0x683c`/`gp-0x671d` states; and the **MAIN path is a RAM LERP table** (`gp-0x6e30/6e28`, `gp-0x6e40/6e38`) whose **flash source is unidentified**. ⇒ **the `sar` at `0x3AB76`/`0x3AC20` is applied AFTER the multiply on EVERY path ⇒ the ONLY universal scaler** ⇒ **V139 is correct as built.** 🛑 **FLAG: V88's "Lever B"** (`0xC6446`, 512→5244, recorded *"best in kit"*) applies **only when `gp-0x671d==0 AND gp-0x683c != 0`**, and **that gate's duty has NEVER been measured** ⇒ **[UNKNOWN] how often Lever B is in force** — the same failure mode that made the notch inert. Cheap to settle: both are byte cells, and a 427 tap gives a binary duty exactly as V147's gate probe does. ⊕ **this does NOT retract V88** — its fix was an OPERATOR report, which survives the noise-floor audit; it flags that the **mechanism credited for it is unverified.** ⭐ **RULE: before crediting a cal with an effect, check the DUTY of the branch that reads it** — three levers this session (the notch, `0xC643E`/`0xC6440`, and possibly Lever B) sat behind gates whose duty nobody had measured.

- 🛑🛑🛑★★★★★ [**THE ASSIST CALIBRATION IS NOT FIXED — it MIGRATES as fault counters accumulate**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — sweeping `gp-0x6700`–`gp-0x6728` for cells read in the assist chain finds a **FAMILY of lockstep-shadowed LATCHING counters**: **`gp-0x671A`** gates **three** cal selections (aggregator `0x3AA70`, b26 Y-branch `0x36C1E`, notch gate `0x35BEA`) · **`gp-0x671B`** gates float behaviour at count > 1 (`0x39854`) · **`gp-0x671C`** is written with a shadow at `0x3A346` · **`gp-0x671D`** selects the r24 multiplier with a **5.12× step** (V149) · **`gp-0x671F/20` and `gp-0x6725`–`0x6728` (35–40 accesses each) UNEXAMINED**. ⊕ the adjacent-pair pattern is the `ld.bu` disp|1 ambiguity. 🛑 **`FUN_00041d56` gave the full pattern**: increment on a threshold crossing, **never decrement**, saturate at 255, lockstep-shadowed, raise a DTC at a count limit, reset only on a clear path — **and it is not one counter, it is an ARCHITECTURE.** ⇒ **[EVIDENCE] the effective assist calibration depends on how many fault thresholds have been crossed so far IN THE DRIVE**; at least four counters select different cal values as they advance ⇒ **"which build is on the car" is NOT the full state** — two runs of identical firmware can run materially different configurations, moving in one direction only. ⭐ **A concrete mechanical explanation for the 20–36× between-route noise floor and for grinding that "comes and goes" — it is the firmware reconfiguring itself mid-drive, not road noise.** ⭐ **Consequences**: (1) **V149 gains weight** — it makes one lane's calibration **drive-invariant**, not merely smaller; (2) every future build must ask **"does this cal sit behind a counter?"** (three levers this session did); (3) **a drive's early minutes are not the same firmware as its late minutes**, so any pooled endpoint averages across configurations — and the share endpoint's n≥90 gate makes that worse, not better.

- 🛑🛑★★★★ [**CORRECTION: "the fault counters never reset within a drive" is NOT established**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — that claim came from a **single zero-store without checking its callers**. `FUN_0003e936` (`0x3EAA6`–`0x3EAC4`) is a **CLEAR ROUTINE** that walks the counters, compares each against its lockstep shadow and `st.b r0` to both — and it has **NINE callers**; `FUN_0003bcb2`, which holds the `gp-0x671d` zero-store at `0x3BD2A`, has **SEVEN of its own**, all in a dense `0x3Cxxx`–`0x3Exxx` fault-management web ⇒ **NOT an init-only path; the counters CAN be cleared during operation.** 🛑 **[RETRACTED] "never reset within a drive"**, and **[RETRACTED] that this is the established explanation for the 20–36× noise floor** — it stays a plausible HYPOTHESIS with an unknown time course. ✅ **SURVIVES, verified at sites**: the counters exist, are lockstep-shadowed and **gate cal selections**; `gp-0x671A` gates three (aggregator / b26 Y-branch / notch gate); **`gp-0x671D` selects the r24 multiplier with a 5.12× step** (`0x3AB98`); it **increments on a threshold crossing and saturates at 255** (`FUN_00041d56`). **WHEN it resets is UNRESOLVED.** ⭐ **This makes V149 STRONGER**: if the counter latches once, V149 removes a one-time step; if it can be cleared and re-armed, the multiplier **toggles 5.12× repeatedly through a drive** — worse, and a better candidate for a symptom that "comes and goes" ⇒ **V149 removes the step under EITHER time course** and its rationale does not depend on the retracted claim. ⭐ **V148 already probes `gp-0x671d`** (via the even `gp-0x671E`, high byte) ⇒ one drive shows whether it **TOGGLES**, settling the time course directly instead of by tracing seven callers — **V148 and V149 are complementary: one removes the step, the other measures it.**

- 🛑🛑🛑★★★★★ [**ROOT-CAUSE CANDIDATE: a 5.12× GAIN SWITCH toggling at TASK RATE inside a CONFIRMED PUMP**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — tracing both paths to their task roots: **INCREMENT `FUN_00041d56` ← `FUN_0002214a` = TASK 1, the CONFIRMED 1 kHz task**; **CLEAR `FUN_0003bcb2` ← … ← `FUN_00022ca0` = TASK 5, ~500 Hz**; **SELECT at `0x3AB98`**, `gp-0x671d == 0 ? cal(0xC6446) : cal(0xC6442)`. ⇒ **`gp-0x671d` is incremented at 1 kHz and cleared at ~500 Hz ⇒ NOT a drive-long latch but a flag that can TOGGLE AT TASK RATE**, selecting between two r24 multipliers **5.12× apart** in a lane whose polarity (`gp-0x6752 = −1`, verified 3 ways incl. on-car) makes it a **CONFIRMED PUMP**. ⭐ **A 5.12× gain switching at hundreds of Hz inside a positive-feedback lane is a SWITCHING NONLINEARITY — a textbook mechanism for a rough, grinding, ratcheting feel**, and it explains the intermittency, since the toggle rate depends on how often `|x| ≥ cal(0xC61FA)=5530` is crossed, which depends on the road. ✅ **The history lines up**: **STOCK 512↔1024 = a 2.00× switch (the mechanism is HONDA's), V88 5244↔1024 = 5.12× — V88 made an EXISTING switch 2.6× WORSE**, V122 carries it now, and **V149 1024↔1024 = NO SWITCH**. ⇒ **V149 ELIMINATES it rather than reducing it**, regardless of toggle rate, in 2 cal-only bytes, safe by construction ⇒ **the strongest-motivated build of the session.** ⚠ **[BELIEF] that this is what he HEARS** — mechanically apt but untied to the symptom, and this kit has shown repeatedly that a compelling mechanism is not a cause; it also partially reverts V88, and **any value other than 1024 RESTORES the switch.** ⭐ **V148 measures the toggle directly** ⇒ V149 removes the switch, V148 proves it was switching.

- ⭐⭐★★★★★ [**The switching is EVERYWHERE — but `gp-0x671d`'s is the ONLY one a cal edit can remove**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `gp-0x671a` has the same pattern: updated in `FUN_000428d4` ← `FUN_0002214a` = **TASK 1, 1 kHz**, and it is **`min(revcount, CEIL)`, a clamped copy recomputed EVERY TICK**, so it moves **both ways**, not a one-way latch. It gates **three** cal selections — the **notch gate** (`0x35BEA`, so the filter **arms/disarms at up to 1 kHz**), the **b26 Y-branch** (`0x36C1E`, LERP vs `cal(0xC640A)=−8192`), and the **aggregator r26 branch** (`0x3AA70`, LERP vs `0xC643E`=1536 / `0xC6444`=512). ⇒ **switching nonlinearities are a PATTERN in this firmware, not one anomaly.** ⭐ **But only one is removable**: `gp-0x671d`'s switch is **constant vs constant** (5244 vs 1024) while every `gp-0x671a` switch has a **speed-varying LERP** on one side — and V149's trick is to make both branches return the SAME value, which **cannot be done against a LERP** (matching at one speed unmatches every other). ⇒ **[EVIDENCE] `gp-0x671d`'s 5.12× switch is the ONLY switching nonlinearity a cal edit can eliminate** ⇒ **V149 is not just the best available build, it is the only such removal the firmware's structure permits without a code edit.** ⚠ **If V149 is NULL**, either switching is not the cause or the culprit is a `gp-0x671a` switch, which **no cal can remove** — leaving only an in-place instruction edit on the branch, the class that has bricked this ECU three times ⇒ **V149 is also the cheapest possible test of the entire hypothesis class, and a null retires switching-as-cause for every lever a cal can reach.**

- ✅✅✅★★★★★ [**The SWITCHING CENSUS is COMPLETE — exactly TWO are cal-removable, both built**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every counter-gated selection in the assist chain read at its site: **`gp-0x671a`** gates the notch **enable** (not a pair), the **b26 Y-branch** and the **r26 multiplier** (both **LERP vs const** ⇒ not removable), and the **r26 SUPPRESSION flag** (**cal=1 vs cal=0, BOTH CONST ⇒ REMOVABLE → V150**); **`gp-0x671d`** gates the **r24 multiplier** (**5244 vs 1024, BOTH CONST ⇒ REMOVABLE → V149**); `gp-0x671b` is a float branch, not a cal pair. 🛑 **Two corrections to my own sweep**: **`gp-0x671f` is a state index driving a `jr` JUMP TABLE** (`addi -0xa1` → `bnc` → `jr 0x377b2`), not a selector; and **`gp-0x6700/03/04` are 32-bit FLOATS** — `0x38AEA` is `ld.w -0x6704` + `trncf.sw` — **the sweep classified them as counters purely by displacement.** ⭐ **RESULT: exactly TWO switching nonlinearities are cal-reachable, and both are built** — **V149** (`0xC6446` 5244→1024, removes the 5.12× r24 switch, 2 bytes, 52/52) and **V150** (`0xC6136` 0→1, removes the r26 suppression switch in the pump-SUPPRESSING direction, 1 byte, 51/51). Everything else needs an **in-place instruction edit** — the class that bricked V24/V27/V48B — and is not proposed without a confirmed reason. ⊕ **V149 and V150 are independent and must NOT be stacked before either flies**; each builder asserts the other's cell is held. ⊕ **If both are null, switching-as-cause is retired for every cal-reachable lever.** ⭐ **The first hypothesis class this session to be enumerated EXHAUSTIVELY rather than sampled.**

- 🛑★★★ [**Flattening the LERP to remove the remaining switches — considered and REJECTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the three `gp-0x671a` switches are unremovable only because one side is a **speed-varying LERP**; flattening the table to the other branch's constant would remove them. **Reachability**: `gp-0x6E28/30/38/40` have **2–3 accesses each and NOT ONE gp-relative STORE** ⇒ they are a **`.data` section bulk-copied from flash at startup**, so the source would have to be found via the startup copy table — doable, a real trace. 🛑 **REJECTED ON MERIT, NOT DIFFICULTY**: flattening a speed-scheduled multiplier does not merely remove the switch, it **removes SPEED SCHEDULING ENTIRELY** — the r24/r26 lanes would carry the same gain at 2 km/h and 120 km/h ⇒ **the cure is far more invasive than the disease**, and this kit's record (V133) is that broad changes to these lanes produce large, hard-to-attribute regressions. ⇒ **[DECISION] not pursued; the switching class is CLOSED at its two cal-removable members (V149, V150).** ⭐ **Recorded so a future session does not re-derive the idea and mistake "reachable" for "advisable".** ✅ **Handoff verified**: V139/V148/V149/V150 all rebuild **bit-identically** (49/69/52/51 assertions), both repos clean, every capped file under 256 KB.

- 🛑★★★★★ [**The COULOMB RELAY is ~99 % OUT OF SATURATION on what is flying — largely RETIRED; V151 built anyway**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the switching census covered **counter**-gated switches and never the **sign**-gated class, so I followed the Coulomb relay (`friction = K1·min(|model|,knee)/knee` in `FUN_0003b8f6`, knee `0xC40BC`, K1 `0xC40D2`). **THE GAP**: V135 made the knee 3000→3600 edit but sits on the **regressed V133 base**, and every V122-based build since (**V137–V150**) holds the knee at 3000 ⇒ **it had never been built on a flyable base** → **V151** (2 B, 53/53, CRC 50/50, single-variable vs V122; rwd `eb98eb65…`). 🛑 **THE RETIREMENT**: the measured duty ladder is a **survival function**, decaying **×0.206 per 600 counts** — 600→0.7439, 1200→0.4810, 1800→0.2353, 2400→0.0484, **3000→~0.010 (V122, ON THE CAR)**, 3600→0.0000 ⇒ **V122 already runs the relay at ~1 % duty and the operator STILL hears grinding** ⇒ **a nonlinearity active ~1 % of the time cannot cause a continuous symptom** ⇒ **the relay is retired as the cause of the REMAINING grinding** (it was genuinely the live lever across V108→V122, duty 0.74→0.01 — **the ladder is spent**). ⭐ **Corrects my own framing**: V151's real effect is the **17 % friction cut** (slope 0.340000→0.283333, monotone, K1 held — gain-holding needs K1=1224 vs a **1023 ceiling**), which by the verified polarity means **slightly LESS assist** ⇒ **V151 is MARGINAL and does NOT displace V149.** Flight order: **V149 → V139 → V150 → V148 → V151**, never stacked.

- 🛑🛑★★★★★ [**The “Coulomb relay” is NOT friction and NOT a relay — it is `|model| × sat(ANGLE)`**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **[EVIDENCE, `decompile_function 0x0003b8f6`]** the kit described `0xC40BC` for ~40 builds as the knee in `friction = K1·min(|model|,knee)/knee`; **that formula is wrong.** Actual: `iVar20 = polarity·gp-0x6abc·12` (**an ANGLE**), `fVar13 = clamp(iVar20/knee, ±1)`, `term = (|model|·K1/1024 + OFFSET/1024)·fVar13` ⇒ **BILINEAR `|model| × sat(angle)`**, then an EMA and a ±10 clamp. ⭐ **PROOF `gp-0x6abc` is an angle**: its own first difference is `(iVar20-prev)·0.5·17.453293`, and **17.453293 = 1000·π/180** = deg→rad at the confirmed 1 kHz tick. 🛑 **OVERTURNS**: the knee normalises **ANGLE not |model|**; **K1 and knee are INDEPENDENT axes**, so “slope = K1/knee” was never meaningful; it saturates at **|gp-0x6abc| ≥ knee/12 = 250 counts**, not at |model| ≥ knee; and it is **CONTINUOUS through zero ⇒ a SOFT saturation, NOT a relay** — Coulomb friction switches on **velocity sign**, this switches on nothing. ⊕ Corroborated by a `sar 31` sign-extract census over `[0x30000,0x40000)`: **6 hits, NONE in the aggregator `0x3AA2C` or the model `0x3B8F6`** — a relay needs a sign switch and there is none. ✅ **SURVIVES**: the ×0.8333 figure, because in the unsaturated regime the term is exactly ∝1/knee (uniform 17 % cut) and in the saturated regime it is unchanged ⇒ monotone. ⇒ but the term is **∝ steering angle** and creep runs **small angles**, so **V151 reduces something already small in the symptom's own regime — it stays MARGINAL, ranked 5th.** See also [[accord-friction-polarity-more-assist]], [[accord-v89-built-plant-model-friction]].

- ⭐⭐★★★★★ [**`0xC40D0` — the best remaining structural lever, GATE 2 OPEN**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the EMA on the `|model|×sat(angle)` term, `α = cal/4096` in the 1 kHz task. **WELL-AIMED**: cal=408 ⇒ α=0.09961, **fc 16.70 Hz, |H(7.8 Hz)| = 0.906** ⇒ **the bilinear path is 91 % OPEN at the ratchet frequency**; and an **EMA has DC gain EXACTLY 1**, so lowering the pole cuts only the FAST component and leaves steady-state friction untouched ⇒ **no assist cost** (unlike V151, which cuts DC too). **VIRGIN — 408 in ALL 151 images.** 🛑 **NOT BUILT**: the lineage's warning that `0xC63AC`'s α *“matches `0xC40D0` to the last bit”* is **confirmed arithmetically — 102/1024 = 408/4096 = 0.099609375, and 408 = 4×102 EXACTLY** ⇒ the match is **by construction**. They sit on **opposite arms of ONE observer residual** (V98: `iVar6 = MODEL + REQUEST − ACTUAL`; `0xC40D0` shapes MODEL, `0xC63AC` shapes ACTUAL) ⇒ **Honda matches their time constants so the phases cancel in the difference; moving one alone injects a phase error at 7.8 Hz.** **[UNRESOLVED] the SIGN** — attenuating a *subtracted* term can remove or EXPOSE 7.8 Hz depending on phase. ⚠ The twin `0xC63AC` **flew as V97 → UNINTERPRETABLE**, Bode-filed **“Predicted WORSE”**. ⭐ **TO CLOSE**: (1) measure the residual's MODEL-vs-ACTUAL phase at 7.8 Hz via a re-cut V98 comparator, **or** (2) move BOTH cells together preserving `408 = 4×102` — **the SAFE form**, moving only the shared corner — which needs the operator's call since V99 restored `0xC63AC` to Honda stock deliberately. **[DECISION] unclosed GATE 2 is not a build.**

- ✅✅✅★★★★★ [**V152 / V153 — the observer corner moved on BOTH arms at once, GATE 2 CLOSED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the objection to `0xC40D0` was that moving it alone breaks Honda's byte-exact arm-match; **moving both cells together removes the objection.** **V152**: `0xC40D0` 408→204 **and** `0xC63AC` 102→51 (**204 = 4×51 EXACT**), corner 16.70→8.13 Hz, |H(7.8)| 0.906→0.722 = **1.26× less**; rwd `2a5ceef7…`. **V153**: 408→104 **and** 102→26 (**104 = 4×26 EXACT**), corner 16.70→4.09 Hz, |H(7.8)| →0.465 = **1.95× less**; rwd `c25fc1d6…`. Both **3 payload bytes, 54/54, CRC 50/50**. ⭐ **WHY STRONGEST**: (1) **GATE 2 CLOSED not argued** — α identical on both arms ⇒ **no RELATIVE phase** in the residual, only the **shared** corner moves ⇒ a **pure added low-pass** ⇒ **HF loop gain DOWN**, stabilising for a Q 14–29 resonance; (2) **NO AUTHORITY COST** — an EMA has **DC gain EXACTLY 1**, so steady-state friction and assist are **unchanged** (V151 scales DC too); (3) **CERTAIN TO ACT** — the EMA runs every 1 kHz tick, unlike **V149 which is INERT if `gp-0x671d` never increments**; (4) aimed where the path measured **91 % open**; (5) **the direction the kit's own Bode sum favours** — `0xC63AC` was *“Predicted WORSE”* for being **RAISED**, so lowering drops `|L|` below 0.875. 🛑 `diff_vs_flown` says **MULTI-VARIABLE** — **expected**: two cells, **ONE logical lever**, and they *must* move together. **Do NOT reduce either build to one cell.** ⚠ `0xC63AC`=102 is **Honda stock** (V99 restored it); below stock is new territory. ⚠ **[BELIEF]** the 1.26×/1.95× is audible. ⊕ **Same lever, two doses — fly ONE.** Order: **V153 → V152 → V149 → V139 → V150 → V148 → V151.**

- 🛑🛑★★★★★ [**The sign-gated relay in `FUN_00038148` — found, then REFUTED by flown data**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the census covered **counter** gates, never **sign** gates. Sweeping them found a real signum multiply in the PID-reference path: `gp-0x6b70 = sign(residual) × LERP(|residual|)`, clamped to ±cal(`0xC6200`)=8192 ⇒ **odd-symmetric, continuous ONLY IF `values[0]`=0**; otherwise it jumps `2·values[0]` at every zero crossing. The table is **RAM (`gp-0x641c`/`gp-0x64b8`, 10 entries), 0 gp-relative stores** ⇒ `.data`, written register-indirect, **unreadable from the image**. ⭐ **CLOSED BY MEASUREMENT**: 427 = **0x1AB**, and V96–V99 repointed its `MOTOR_TORQUE` to `gp-0x6b70` — cached. **`ab_mt` reaches 0** (r7e 1048 frames = 5.9 % of engaged) and populates 1,2,3,4,5,6 with **NO forbidden band**, over **1,741 zero crossings**; replicated on r7f (15,779) and r82. ⭐ **THE BOUND IS SHIFT-INDEPENDENT**: for any packer `sar k`, `mt=0` ⇺ `|gp-0x6b70| < 2^k`, so jump `< 2^(k+1)` against an observed max `216·2^k` ⇒ **jump/range < 2/216 = 0.93 %, k CANCELS** ⇒ **[EVIDENCE] not a relay in any meaningful sense.** 🛑 **Do not re-propose `gp-0x641c`, `0xC63AE` or the signum branch** — and `0xC63AE` is backwards anyway: shrinking it drives `uVar7→0` so `sVar8→values[0]` always, turning a continuous law **INTO** a pure relay. ✅ **Both sign-gated candidates are now closed** (this one, and the `|model|×sat(angle)` soft saturation) ⇒ **V152/V153 remains the strongest candidate, and is strengthened**: with no relay in the path, the ratchet is a lightly-damped resonance excited through a loop **91 % transparent at 7.8 Hz** — exactly what those builds attack.

- ✅★★★★ [**V152/V153's premise checked — the observer loop DOES carry 6–9 Hz, modestly**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — control run first, same signal and route, LKAS-off arm. **`gp-0x6b70` 6–9 Hz share: engaged 8.7 % (r7e) and 10.2 % (r7f) vs manual 6.6 % / 7.4 %**, against a **6.8 % flat-spectrum baseline** ⇒ engaging shifts the spectrum toward the band by **1.32× / 1.38×, replicated on two routes** ⇒ **[EVIDENCE] the loop V152/V153 attack demonstrably passes the symptom band.** ⚠ **[NOT PROVEN]** that reducing transmission fixes the symptom — the effect is **modest (1.3–1.4×)** and **confounded by operating point** (engaged = creep, manual not speed-matched), so it is **supporting, not decisive**; and the **ABSOLUTE** 6–9 Hz power is **higher** manual (503 vs 183) because manual steering moves more ⇒ **only the SHARE comparison is meaningful.** 🛑 **KIT-WIDE SAMPLING FACT: `0x1AB` (427) transmits at 49.8 Hz, NOT 100 Hz** (measured from `ab_t1ab`, both routes) ⇒ **Nyquist 24.9 Hz** ⇒ 6–9 Hz is safe but **any 18–22 / 18–28 Hz claim derived from `0x1AB` sits near the fold** and can take aliased content from above 24.9 Hz. **Check the source ID's rate before quoting a high-band number** — `0x14A` is the 100 Hz builder, `0x1AB` is not. See [[accord-averaged-spectrum-needs-matched-speed-distributions]].

- 🛑🛑★★★★★ [**The plant model has NO damping term — `0xC646E`/`0xC40D6` CLOSED as inert; + the reader-count METHOD FIX**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the rate term looked ideal (**exactly zero DC gain, ∝ω ⇒ HF-selective by construction**) but **is not there**: `rate_term = fVar19[rad/s] · 1428 · 2^-24 = fVar19·8.51e-05`, so at the ratchet (7.8 Hz, 0.1°) it is **7.28e-06 = 0.0001 % of the ±10 clamp**, while friction in the same sum clamps at 10.00 ⇒ **4–6 ORDERS below**. **No dose revives it**: at the u16 ceiling 65535 it reaches **0.0033 %**. ⇒ **`0xC646E` STRUCTURALLY INERT, `0xC40D6` inert by consequence — both CLOSED at any dose**, both virgin across 153 images. ⭐ **Structural fact: Honda's observer models FRICTION but NOT DAMPING**, so real viscous damping is un-modelled and lands in the residual. ✅ **METHOD FIX — applies to EVERY tp-relative reader count**: my first scan said **20 readers**; most were **ASCII** (`0x746E` = `"nt"`, so `"halt"`/`"synt"` matched as disp16). **A tp-relative load must have `hw1 & 0x1F == 5` (tp = r5)** + Format-VII opcode ≥ 0x38. With the filter: `0xC646E`→1 @`0x3BB92`, `0xC646C`→5, and **`0xC63AC`→1 @`0x38202`, reproducing the lineage's independently-recorded “sole reader `ld.hu 0x73ac,tp,r13` @`0x38202`” to the address.** 🛑 **Ghidra CANNOT answer this** — `get_xrefs_to` returns *“No references found”* because tp is a runtime register ⇒ **for tp-relative cals the filtered Python scan is the ONLY instrument.** ⭐ **One HF-selective candidate remains: `0xC63A6`**, the weight on the **INERTIA lane `gp-0x6b26`** (`−K·α`, acceleration ⇒ **zero at DC, ∝ω²**), virgin 1024/153 — **NOT proposed: I derived the direction twice and got OPPOSITE answers**, since it turns on how `polarity = −1` composes with *“residual ↓ ⇒ more assist”*. **An unresolved sign is not a build.** To close it, one probe carrying `sign(gp-0x6b26)` on the same frame as the flown `sign(gp-0x6b70)` rung settles it.

- ✅✅✅★★★★★ [**V154/V155 — the INERTIA lane's weight, the CLEAN version of the α2 lever**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0xC63A6` **1024→512 (V154)** / **1024→256 (V155)**, **1 payload byte each, 58/58, CRC 50/50, single-variable vs V122**; rwd `6fe3eceb…` / `0d138c83…`. ⭐ **THE SIGN AMBIGUITY WAS A CATEGORY ERROR**: the earlier pass asked about **direction of assist** (sign-dependent) when the question is **amplitude of an oscillation** (sign-independent). `polarity = −1` sets the **PHASE** of the oscillating component, not its amplitude, and `gp-0x6b70 = sign(iVar6)·LERP(|iVar6|)` is **ODD and MONOTONE** ⇒ smaller input amplitude gives smaller output amplitude **regardless of sign** ⇒ **[EVIDENCE] reducing this weight unambiguously reduces the 7.8 Hz amplitude at the PID reference.** ⭐ **HF-SELECTIVE BY CONSTRUCTION**: `gp-0x6b26` is `−K·α`, an **ACCELERATION** ⇒ **exactly zero at DC, ∝ω²** ⇒ **61× more influence at 7.8 Hz than at 1 Hz steering**, and **nothing in steady state**. ⭐ **THE LEVER FOUR α2 BUILDS WANTED**: the α2 ladder scaled this **same lane** and was **nearly INERT at 20 Hz** — |H| fell 1.77× but phase rotated 56.3°→16.0° and the delivered component stayed **flat** — because **α2 is a POLE (magnitude AND phase, which cancelled)**. **`0xC63A6` is a PURE GAIN ⇒ magnitude only, no phase, NOTHING CANCELS.** Virgin 1024/153. 🛑 **NEW: a THIRD switching class found in passing — ADMISSION GATES.** Each lane is **ZEROED, not clamped**, outside its bound (`gp-0x6b26` at ±1024, `gp-0x6b4c`/`4e` ±10240, `gp-0x6b46` ±1024, `gp-0x6bd0`/`gp-0x6bbe` ±2048), and **the bounds are hard-coded IMMEDIATES (`0x400`/`0x801`), not cals** ⇒ **code edits, not cal edits; duty NEVER measured** — the cheap next probe. ⚠ **[BELIEF]** the 2×/4× cut is audible: worst-case share of the sum is **3.8 %**, HF share higher but **unmeasured**. ⊕ **One lever, two doses — fly ONE.**

- 🛑★★★★ [**The admission-gate class — HALF CLOSED from the record, and V154/V155 DEMOTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I called measuring the lane gates *“the cheap next probe”*; **three of six were already measured.** **`gp-0x6b26`**: p50 5.5 / p90 39.1 / p99 114.3 / **max 319.1** vs a **±1024** gate ⇒ **never trips, 3.2× margin**, and it is **unreachable BY CONSTRUCTION** since an upstream `±cal(0xC407E)` = **±511** clamp binds first (clamp duty 0.000000). **`gp-0x6b4c`**: V101's b6 gave `|x| ≥ 4096` duty **0.000000** vs a ±10240 gate ⇒ never trips. **`gp-0x6bbe`**: p50 73.6 vs ±2048 ⇒ never trips. Unmeasured: `gp-0x6b4e`, `gp-0x6b46`, `gp-0x6bd0`. ⇒ **[EVIDENCE] no measured gate has EVER tripped ⇒ admission gates are not the ratchet's source on any instrumented lane.** 🛑🛑 **AND IT DEMOTES MY OWN BUILD**: the same data **sizes** the lane — **max 319 against `gp-0x6b4c` < 4096 ⇒ ≤ ~8 % of the sum at its own maximum**, p50 5.5 ⇒ a few tenths of a percent typically ⇒ **halving it moves `sum6` by a few percent at most.** The ω² argument holds but starts from a **small base**, and the 7.8 Hz share is **unmeasured** ⇒ **[CORRECTION] V154/V155 drop BELOW V152 — I ranked them on mechanism quality without sizing the lane first.** ✅ They stay **SAFE**: `FUN_00036c12` holds an **int32 WRAPAROUND** (×0x111, high half discarded, **unclamped, UPSTREAM of `0xC407E`**) binding at ≈**1.6005×** present level with a **full-scale SIGN INVERSION** — V154/V155 **reduce** the lane, moving away; anything **raising** `gp-0x6b26` moves toward it. ⭐ **`gp-0x6b4c`/`gp-0x6b4e` are DISJOINT PARTITION SUMS of the same 11-slot request array `gp-0x62c8[]`** ⇒ they carry the assist request and its **DC** ⇒ **not selective levers** ⇒ **the inertia lane is the ONLY frequency-selective lane in the observer sum.** ⇒ **Order: V153 → V152 → V149 → V139 → V155 → V154 → V150 → V148 → V151.**

- 🛑🛑★★★★★ [**RECONCILIATION — I was searching a space the kit already CLOSED; `gp-0x6b70`-as-carrier RETRACTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — [[accord-ratchet-is-a-lightly-damped-resonance]] is PROMOTED ★★★★★ and says outright *“the firmware search on it is CLOSED”*, by a **SHAPE argument**: every gain-bearing element is a **flat Q10 scalar** (would lift the 26–31/32–38 Hz controls, which went **down** 0.61–0.76) or a **differentiator** (wrong direction); a 6–9 Hz band-limited lift needs a **biquad and none exists**; and **“a 2-pole EMA has DC gain exactly 1 so no EMA can be the amplifier.”** 🛑🛑 **[RETRACTED] my “V152/V153 premise supported” claim**: I compared `gp-0x6b70`'s 6–9 Hz engagement contrast (**1.32–1.38×**) to a **flat baseline** — wrong comparator. The **symptom's** own contrast is **11.7–13.4×**, and a Q 14–29 mode cannot turn 1.3× of excitation into 12× of output ⇒ **`gp-0x6b70` is NOT the carrier.** Passing a band is not carrying a symptom. ⭐ **BUT the closure explicitly does NOT exclude a LOOP POLE** (the caveat carried in `MEMORY.md` itself) — the shape argument excludes **amplifiers**, not **poles**, and a pole changes the **phase** with which the loop re-excites a mode that rings ~Q≈20 cycles (≈2.5 s) per kick. **`0xC63AC` was V97, *“the arc's FIRST loop-POLE lever”*, and V152/V153 move that same pole AND its byte-exact twin `0xC40D0`, matched, DOWNWARD** ⇒ **they sit in the ONE slot the kit's own closure leaves open** — a better justification than the one I gave them. ⚠ V97 moved it **alone and upward** → uninterpretable; this is **both, matched, downward.** ⇒ **V153 stays first for the LOOP-POLE reason, not the transmission reason.** 🛑 **Honest frame: the mode is MOTOR/RACK/TYRE side and no channel on this bus observes it — firmware cannot remove a mechanical resonance, only change what excites it and the loop phase. No remaining build can “eliminate” a mechanical mode.**

- 🛑🛑★★★★★ [**Unmatched engagement contrasts are operating-point ARTEFACTS — including mine**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — searched every cached channel for the carrier signature (high 6–9 Hz contrast, flat controls). **RAW/UNMATCHED**: `tq` 0.13/0.10, `rate_f` 0.12/0.10, `probe` 0.57/0.83 ⇒ engagement appears to **SUPPRESS** the band **8–10×**, and no channel on either route exceeds **2.0×** ⇒ **[EVIDENCE] an unmatched engaged/manual ratio measures the DRIVING, not the firmware.** ✅ **MATCHED** on (speed bin × |rate| RMS bin), 5.1 s pure-arm windows: **r7e** `tq` 0.86 / `rate_f` 0.76 / `probe` 0.66 vs controls 1.04–1.11; **r7f** `tq` **1.31** / `rate_f` 1.08 / `probe` **1.35** vs 0.87–1.31 ⇒ **SAME BUILD, SAME DRIVER, OPPOSITE SIGNS**, contrast 0.66–1.35, **no replication in direction** ⇒ underpowered (6–7 strata), cannot refute the corpus result but **proves the unmatched figures are artefacts.** ⚠ **CAVEAT ON A PROMOTED MEMORY**: [[accord-ratchet-is-a-lightly-damped-resonance]] cites **11.7–13.4×** with **no n, no CI, no matching stated**, while [[accord-engagement-amplifies-6-9hz]] gives **2.8×** from **235 blocks with a CI** ⇒ **treat 11.7–13.4× as unmatched and non-comparable**; that memory's ring-down Q, limit-cycle exclusion and not-rim-side results each passed their own control and **stand untouched**. 🛑🛑 **VOIDS THE REASONING OF MY OWN RETRACTION last turn** — I compared `gp-0x6b70`'s **1.32–1.38×** against **11.7–13.4×**, **both unmatched** ⇒ **[CORRECTED] `gp-0x6b70` is UNPROVEN as carrier, NOT refuted**; the **loop-pole** justification for V152/V153 never rested on the contrast and is unaffected. ⭐ **THE BINDING CONSTRAINT IS A DRIVE**: no cached route has matched engaged+manual exposure at the same speed and activity. **Protocol: one build, ALTERNATE arms every ~60 s — engaged hands-off vs manual at the SAME speed and gentle activity, steady 5–15 km/h, ≥8 alternations, ≥2 min engaged total.** This closes the carrier question for **every lever at once** and is worth more than another build.

- 🛑🛑🛑★★★★★ [**MATCHED, engagement adds ~12 % at 6–9 Hz — NOT 2.8×; the BUS CANNOT SEE the symptom**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — pooled over **every cached route with both arms** (27 qualify, **24 yield matched strata**): 5.1 s pure-arm windows stratified on (speed bin × |rate| RMS bin), per-route median, **bootstrap over ROUTES** (4,000 draws). **`tq` 1.12 [1.01, 1.27] · `cs_tq` 1.13 [1.00, 1.28] · `rate_f` 1.09 [0.95, 1.21] · `probe` 1.04 [0.99, 1.18]**, with **CONTROLS NULL** (26–31 Hz 0.96–1.06, 32–38 Hz 0.94–1.02, every CI spanning 1) ⇒ **the null controls validate the matching.** 🛑 **OVERTURNS**: the **2.8×** of [[accord-engagement-amplifies-6-9hz]] and the **11.7–13.4×** of [[accord-ratchet-is-a-lightly-damped-resonance]] **do NOT survive matching** — both compared arms differing in operating point, and the artefact is huge (**unmatched the same channels read 0.10–0.13×**, engagement appearing to SUPPRESS the band 8–10×) ⇒ **an unmatched engaged/manual ratio is uninterpretable in EITHER direction.** ⭐⭐ **RECONCILES WITH THE PHYSICS**: that same memory says the mode is **“on the motor/rack/tyre side, which no channel on this bus observes”** ⇒ **a ~12 % residue is exactly what an unobservable mode leaks onto observable channels.** 🛑🛑 **CONSEQUENCE: CAN band statistics CANNOT ARBITRATE BUILDS.** If the entire engagement-conditional effect on the bus is 12 %, removing half the engaged contribution moves a bus statistic ~6 %, against a measured between-route floor of **19.9× / 36.2×** for identical cals ⇒ **this is the quantitative reason every between-build ratio here has been uninformative.** ✅ **UNTOUCHED**: ring-down **Q 14–29 / ζ 0.017–0.036**, the limit-cycle exclusion, not-rim-side — none is a contrast ratio — and the **loop-pole** case for V152/V153. ⭐ **The operator's ear is the instrument, now as a MEASURED conclusion, not a resignation — fly one build, judge by ear; do not ask for a score the bus cannot carry.**

- ✅✅✅★★★★★ [**A CALIBRATED BOUND — engagement adds ≤ ~2 % of RMS as a 7.8 Hz line; NO CAN statistic can rank these builds**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — band power was the wrong statistic (the record's claim is about a **LINE**, and at **Q≈20** the linewidth **7.8/20 ≈ 0.39 Hz** is **diluted ~8×** by a 3 Hz band). Redone on **line prominence**, 20 s windows, 0.098 Hz resolution: **`tq` 1.17 [0.86, 1.27] · `cs_tq` 1.01 · `rate_f` 1.08 · `probe` 0.89**, controls null ⇒ **still null ⇒ the dilution hypothesis is REFUTED.** ⭐⭐ **POSITIVE CONTROL PASSES** — injecting a noise-driven **Q=20 line at 7.8 Hz** into real manual `tq` windows (baseline prominence **9.82**): **2 % of RMS → 1.35× · 5 % → 1.96× · 10 % → 3.90× · 20 % → 6.66×** ⇒ the instrument resolves a line at **2 % of RMS**, and the measured contrast's **CI upper bound 1.27 sits BELOW the 2 % response of 1.35** ⇒ **[EVIDENCE, positive control passing] engagement adds AT MOST ~2 % of signal RMS as a 7.8 Hz line on the column** — the kit's **first calibrated bound** rather than an inference from a ratio. 🛑🛑 **SETTLES SCORING WITH A NUMBER**: removing HALF the engaged line moves the column ≤ **1 % of RMS** ≈ a **1.2×** prominence change, inside route-to-route spread ⇒ **NO CAN-derived statistic — band share, prominence, matched, pooled over 24 routes — can rank these builds. Closed quantitatively.** ⊕ **Corollary**: probes stay valuable for **mechanism** questions (does a gate fire, does a counter toggle — binary and large) and are **useless for ratchet AMPLITUDE questions.** ⚠ **`accord-ratchet-is-a-lightly-damped-resonance`'s “0 of 97 fully-manual windows carry a line” does NOT replicate** — manual windows here show median prominence **9.82** vs a white-noise expectation **~3.4** ⇒ **not line-free**; flagged **NOT overturned** (its exact line test is unknown to me), but its companion inference **“engagement supplies the resonance, it does not amplify an existing tone” should be treated as UNCONFIRMED.** ✅ Untouched: ring-down Q, limit-cycle exclusion, not-rim-side, and the loop-pole case for V152/V153.

- 🛑🛑🛑★★★★★ [**CORRECTION — AUDIO is a working instrument, and there are TWO symptoms**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — my *“no statistic can rank these builds”* was **overstated**: the bound holds for **CAN-derived statistics at 6–9 Hz** only. **The operator's reported low-speed modes are ABOVE EVERY CAN NYQUIST** (angle 50 Hz, 427/0x1AB **24.9 Hz** measured this session) ⇒ **audio is the only instrument**, and the kit built one. ✅ **ELEVEN-ROUTE LADDER, fixed 90–110 Hz + THREE adjacent control bands**, engaged-minus-manual, matched speed, <10 mph: **STOCK is the ONLY route that FAILS its null** (−0.30 dB, p=0.890) while **9 of 10 gain-modified builds clear at p<0.001**, and the level **ladders with gain: 1× −0.04 | 4× 0.84 | 6× 1.13 | 8× 2.24 dB** ⇒ **the engaged acoustic excess IS ours.** ⚠ controls rise equally ⇒ residual ≤0 on 6/10 ⇒ **BROADBAND, not a mode.** ⭐⭐ **ANSWERS THE OPERATOR'S 8× CONDITIONAL BY MEASUREMENT**: 6× = 1.13 dB, **8× = 2.24 dB ≈ DOUBLE the grinding** ⇒ **8× FAILS his own stated condition — do not propose it**; and the lineage's **vibration ∝ m^1.74 vs authority ∝ m^0.88** means gain buys authority **sub**-linearly and grinding **super**-linearly. 🛑 The converse stays barred by [[accord-4x-lkas-gain-is-the-frozen-variable]] — **do NOT lower it either.** 🛑🛑 **TWO SYMPTOMS, and this session chased only one**: **A** = the ~7.8 Hz ratchet (CAN-visible barely, mechanical, motor/rack, engagement adds ≤~2 % of RMS) vs **B** = the low-speed **GRINDING** (audio-only, above CAN Nyquist, **BROADBAND**, ∝gain^1.74, **stock does NOT fire and we do**). **V149–V155 all target A**; **B is the one the operator calls grinding, has a WORKING instrument and a MEASURED dose-response, and is under-served by the queue.** ✅ **THE NEXT DRIVE MUST CAPTURE AUDIO** — tooling exists (`rlog-tools/decode/extract_audio*.py`, `studies/acoustic/audio_matched.py`).

- 🛑🛑★★★★★ [**Ghidra's `code.bin` is the STOCK image — every decompile this session was of stock**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a Python-vs-Ghidra disagreement at `0x2A1EE` (Ghidra: `ld.h 0x746c,tp` = `0xC646C`; V122 bytes `25 3f d0 7c` = `0xC6CD0`) resolved as **both right, different images**: Ghidra holds `ghidra_project/code.bin`, the **STOCK dump**, and **V57 is the build that moved that reader from `0xC646C` to `0xC6CD0`** — exactly as [[reference-accord-c646c-shared-gain-not-lkas-only]] records. ✅ **The `reg1 == tp` scan filter is vindicated** (it also reproduced the lineage's “sole reader @`0x38202`” for `0xC63AC`). ✅ **CHECKED, not assumed — which decompiles survive**: `FUN_0003b8f6` (plant model) and `FUN_00038148` (ACTUAL arm) are **byte-IDENTICAL stock vs V122** ⇒ the `|model|×sat(angle)` correction and the signum-relay refutation **both stand**; `FUN_0003aa2c` differs by 1 byte (Lever B) and `FUN_000352b4` by 4. 🛑 **STANDING RULE for the decompile skill: Ghidra holds STOCK — before trusting a decompile for a BUILD, diff that function's byte extent stock-vs-target in Python.** ⚠ **AND A RECORDED CLAIM LOOKS WRONG**: [[reference-accord-lkas-only-rate-limiter-c6194]] says `0xC6194` is *“DEAD calibration — output ×0”*, but `FUN_00026c80` (region byte-identical stock vs V122) uses it as a **live ±3 slew step** on `gp-0x3d6c`, feeding `gp-0x6b4a`/`gp-0x6b4c` — the **dominant** observer lanes. The ×0 is **`0xC6196` = 0**, a *different* cell, and only in the `gp-0x6a62 > 0x7d00` branch. 🛑 **[UNRESOLVED — do NOT build on it]: `FUN_00026c80`'s CALL RATE is unknown**; at 1 kHz a ±3/tick limit is already ~3.4 s full-scale (too smooth to be symptom B's source), at a slow task rate it is a real lever. **Resolve the rate first.** ⊕ Forward path confirmed as **`(command × gain × polarity) >> 15`, clamped, stored — with NO SMOOTHING anywhere**, and since openpilot's `STEER_DELTA` is not rescaled, **each 1-count step becomes 6 counts at 6×.**

- ⭐⭐★★★★★ [**A SIGN-AGREEMENT GATE sits directly on the LKAS command path, UPSTREAM of the gain**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — disassembled at `0x2A1C0`, region **structurally identical to V122** (only the 2 gain-cal bytes differ): `ld.h -0x6b30,gp,r13` (**the PREVIOUS stored output**) → `mul r13,r6,r0` (**prev × current**) → `cmp r0,r6` / `bgt` → else **`mov 0x0,r9` FORCES THE COMMAND TO ZERO** ⇒ **[EVIDENCE] the LKAS command is zeroed whenever its sign disagrees with the previous output's sign** — a step discontinuity, i.e. a **broadband generator**. ⭐ **The gate is UPSTREAM of the gain multiply** (`0x2a1e2` precedes `0x2a1ee`) ⇒ **the discontinuity amplitude scales with gain** ⇒ broadband ∝ gain, matching symptom B's measured ladder (1× −0.04 · 4× 0.84 · 6× 1.13 · 8× 2.24 dB), and it is **engagement-conditional by construction** (no command when disengaged), matching *stock does not fire, we do*. 🛑 **[UNRESOLVED — DO NOT BUILD]: it READS as though it could LATCH** (if `prev`=0 the product is 0, failing the strict `>0` test, forcing 0 again). LKAS demonstrably works, so either the **second store at `0x2A900`** resets it, or `r14`/`r6` differ from this 48-byte window's implication, or an entry branch bypasses the leg. **Read all of `FUN_00028ea6` first** — I formed this from an assembly window, the kit's most expensive mistake class: **the instruction sequence is EVIDENCE, the behavioural reading is BELIEF.** ⚠ **No cal on the gate** — `mul`+`cmp`+`bgt`, hard-coded; only the deadband `0xC61B8`=102 gates entry and it is already filed ELIMINATED ⇒ **softening it needs an in-place instruction edit, the class that bricked V24/V27/V48B — NOT proposed.** ✅ **`0xC6194` CLOSED as a symptom-B lever**: `FUN_00026c80`'s only caller is `FUN_0002214a` = **TASK 1, 1 kHz** ⇒ 3 counts/tick against a ±2048/±3072 state = **~2 s full-scale slew** ⇒ already smooth. ⊕ This also **softens my flag from last turn**: the memory's claim is *no live **LKAS-specific** slew limit*, and this one is on the **assist-arbitration sum** ⇒ **its claim stands**; only *“output ×0”* mismatches.

- 🛑🛑★★★★★ [**The sign-agreement gate is DORMANT when engaged — lead CLOSED, two self-corrections**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the full `FUN_00028ea6` shows the deadband + sign test **nested inside an enable gate**: `if ((cal(0xC64A3)==1) && (gp-0x6806==0))`. 🛑 **SELF-CORRECTION 1**: I said *“no cal on the gate”* — **wrong**, `0xC64A3` is a byte enable; but it is **1 in stock and ALL 155 images**, so not a free lever. ⭐ **SELF-CORRECTION 2 — the gate is DORMANT WHEN ENGAGED, proved behaviourally**: the block stores `iVar23` back to `gp-0x6b30`, so once zeroed, `iVar34×0 = 0 < 1` re-fires ⇒ a **self-holding zero** ⇒ **if it were active while engaged the FIRST zero-crossing would latch LKAS at zero permanently**, which demonstrably never happens ⇒ **[EVIDENCE, behavioural] `gp-0x6806 ≠ 0` when steering ⇒ deadband AND sign test are both INACTIVE engaged ⇒ NOT symptom B's source. LEAD CLOSED.** ⊕ independently re-confirms [[reference-accord-pregain-deadband-c61b8]] — the 102-count deadband sits in this same dormant block, which is **why** it was filed ELIMINATED. 🛑 **IMPLICATION**: the engaged forward path is now traced end to end with **NO switching nonlinearity active** — `command → [gate: dormant] → ×gain → ×polarity → >>15 → clamp (INERT) → gp-0x6b30` ⇒ the gain-laddered broadband excess does **not** come from a command-path discontinuity ⇒ **[BELIEF] what remains is the motor/inverter driven harder** (current ripple + commutation rising with amplitude, superlinear acoustics giving m^1.74) ⇒ **SYMPTOM B MAY BE IRREDUCIBLE IN FIRMWARE**, reachable only by the LKAS gain, which is frozen both ways. **The disproof would be a broadband source that is engagement-conditional and NOT proportional to command amplitude — none exists in the forward path.**

- 🛑🛑🛑★★★★★ [**SYMPTOM B is UNREACHABLE by calibration — the drive stage has no cals**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — measured **calibration density** in the FOC/PWM stage vs the control stage on V122: **PI current regulator + SVPWM 0.25 cals/KB** (1 cal in 4 KB) · TSG20 PWM emitter 2.00 · `gp-0x6b98` writers 5.00, against **ACTUAL arm 10.50 · plant model 12.25 · LKAS forward path 10.00** ⇒ **[EVIDENCE] the motor drive stage is 40–49× LESS CALIBRATABLE**; Honda left the current loop and PWM generation essentially without calibration operands (gains are immediates or RAM). The whole PI/SVPWM region is **byte-identical stock vs V122**, and the golden model already records **[OPEN] the PWM carrier frequency**. The **sole exception is `0xC4936` = 250** (1 reader @`0x6c486`), %s — the only cal operand in the entire PI/SVPWM computation, role unidentified. ✅ **SYMPTOM B CLOSED END TO END**: (1) the engaged forward path has **no active switching nonlinearity** (deadband+sign gate DORMANT, clamp INERT); (2) `cal(0xC6194)`=3 runs in TASK 1 at 1 kHz ⇒ ~2 s full-scale ⇒ already smooth; (3) the drive stage has **no cals** ⇒ **the gain-laddered excess (1× −0.04 | 4× 0.84 | 6× 1.13 | 8× 2.24 dB) originates in NO cal-reachable element** ⇒ **[BELIEF] it is the motor/inverter driven harder** (ripple + commutation ∝ amplitude, superlinear acoustics → m^1.74) ⇒ **IRREDUCIBLE BY CALIBRATION**; only the **LKAS gain** touches it and it is frozen **both** ways (8× doubles the excess and fails the operator's own condition; lowering is barred by [[accord-4x-lkas-gain-is-the-frozen-variable]]). ⭐ **FALSIFIABLE**: overturn it with (1) a broadband source that is engagement-conditional but **not** ∝ command amplitude — none in the now fully-traced forward path; (2) `0xC4936` proving to be a current-loop gain or carrier divisor; (3) an in-place FOC instruction edit — **the bricking class, on the one region with no calibration surface. Not proposed.** 🛑 **TWO-SYMPTOM POSITION: A** = mechanical resonance ⇒ firmware changes excitation/phase only ⇒ **V153 is the best remaining lever**; **B** = not fixable by calibration. **Neither is “100 % eliminable” by a cal build — but substantial reduction of A is available and UNTESTED on-car.**

- 🛑🛑🛑★★★★★ [**`0xC4936` is a PWM HARDWARE-TIMING cal — DO NOT TOUCH IT**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the **only** calibration operand in the entire FOC PI/SVPWM region (0.25 cals/KB) and the last open symptom-B candidate. Its single reader `0x6C486` is inside **`FUN_0006c446`, a PERIPHERAL-INITIALISATION routine** writing the motor timer/PWM block (region byte-identical stock vs V122): `_DAT_ffffcc58 = 0x1388` (period-like 5000), **`_DAT_ffffcc5c = cal(0xC4936)×2 + 0x50 = 580`**, `_DAT_ffffccb0/b4/b8 = 0x1428` (**three identical ⇒ 3-phase compare registers**), plus `_DAT_ff809220/24/28` and `_DAT_ff81c084/88` in peripheral space ⇒ **[EVIDENCE] it is NOT a control-law gain, it is PWM/timer HARDWARE CONFIGURATION** written once at init. 🛑🛑🛑 **HARD STOP**: a `2×cal + offset` field in a 3-phase PWM timer block is most plausibly **DEAD TIME or a phase offset**, and **shortening inverter dead time causes SHOOT-THROUGH, destroying the power stage** — **strictly worse than bricking**, which this kit has survived three times (V24/V27/V48B) precisely because a bricked ECU is reflashable and a destroyed inverter is not. **[DECISION] never change `0xC4936`** — virgin at 250 across all 155 images, and it stays that way. ⭐ **Recorded prominently because the trap is attractive**: a future scan will surface *“a VIRGIN cal, single reader, in the FOC region, untouched by 155 builds”* and read it as opportunity — **it is the opposite**; Honda left the drive stage uncalibratable on purpose. ✅ **SYMPTOM B ANALYSIS COMPLETE**: forward path has no active switching nonlinearity, `0xC6194` is already smooth at 1 kHz, the drive stage has no calibration surface, and its sole exception must not be touched ⇒ **not reachable by any calibration edit this kit may safely make**; the falsifier is down to **one** item — a broadband source that is engagement-conditional but **NOT** ∝ command amplitude, and the fully-traced forward path contains none. ⭐ **BYPRODUCT**: the golden model's **[OPEN] PWM carrier frequency** is **narrowed** — its register block is `FUN_0006c446` (`_DAT_ffffcc58`=5000 + compares `0x1428`); absolute Hz still needs the clock tree (PCLK 40 MHz would give 8 kHz edge-aligned / 4 kHz centre-aligned — **arithmetic, NOT verified**).

- ✅✅✅★★★★★ [**V156 — the DAMPER REACHES THE MICRO REGIME for the first time**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a **ζ 0.017–0.036** resonance sits where there is **no added damping at all**, because `ch0 = (FactorC(speed) × FactorE(rate)) >> 10` is a **PRODUCT OF TWO DEAD ZONES** and below `X[0]` a LERP returns `Y[0]`: **FactorC Y[0]=0 ⇒ zero below 35 km/h**, **FactorE Y[0]=0** with `X[0]=60` ↔ the recorded **12.73 °/s** dead zone (~0.212 °/s per count) ⇒ **the micro regime (1–13 °/s) sits ENTIRELY below X[0], exactly where Y[0] applies** ⇒ measured **zero on 100 % of the micro regime**. ⭐ **NEITHER FACTOR ALONE OPENS IT, and the kit tried both separately**: **V134** raised FactorC Y[0]→60 and measured **INERT** (its own header says FactorE Y[0]=0 leaves the product zero), and the **FactorE X[0] 60→12 variant was WITHDRAWN** as *“structurally vacuous at creep”* ⇒ **both Y[0] must move together and that build never existed.** ✅ **V156**: `0xD77DA`/`0xD77EE` FactorC Y[0] 0→60 **and** `0xD7816`/`0xD782A` FactorE Y[0] 0→539 on engaged modes 26/27 — **6 payload bytes, 60/60, CRC 50/50**, rwd `bc070cba…`. ⊕ **Addresses resolved TWO WAYS AGREEING**: the record block at stride `0x14` is **three FactorC then three FactorE records** (modes 25/26/27), giving FactorE m26 Y[0]=`0xD7816`, and that **matches the lineage's independent “FactorE m27 = `0xD7822`” anchor**. ⊕ **RULE 7 mode-proof**: `ENGAGED_MODES=(26,27)`, `MANUAL_MODES=(24,25)`, read from the builder. 🛑 **DOSE SIZED BY V80's CATASTROPHE** (FactorC FLAT 566 passed the ceiling, made the damper **BANG-BANG**, worst grinding in the kit's history): creep product **(60×539)>>10 = 31 = 6.1 % of the 512 ceiling**; FactorC 60 is V134's safety-checked value and FactorE 539 is FactorE's **own Y[2]**. ✅ **RESPECTS THE OPERATOR'S CONSTRAINT** — damping is added **only below 12.73 °/s**; above it FactorE is **byte-unchanged**, so **max angular velocity and acceleration are untouched** and the cost lands entirely inside the symptom's own regime. ⚠ **[BELIEF] 31 counts may be too small to feel** — no dose-response exists because no build ever had a live damper here. ⊕ **`0xC63A0` (the damper WEIGHT) HELD at 1024** — exonerated of V74's fault (that was `0xC407E`) and it **multiplies** what this admits ⇒ **the natural second dose.**

- ✅✅★★★★ [**V157 — the 4× dose of V156; and `MEMORY-PART4` split at 199.5 KB**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V156's creep product is **31 = 6.1 % of the bang-bang ceiling**, possibly too small to feel, so **V157 is the same lever, same four cells, 4× the dose**: FactorC `Y[0]` → **each mode's OWN `Y[1]`** (234 m26 / 233 m27) and FactorE `Y[0]` → its own `Y[2]` (539) ⇒ **product 123 = 24.0 %, a 4.2× margin to 512**; **6 payload bytes, 62/62, CRC 50/50**, rwd `65021b6d…`. 🛑 **The V80 distinction**: V80's catastrophe was FactorC **FLAT 566 across ALL FOUR knots**; V157 flattens **only the FIRST segment** and leaves **`Y[1..3]` byte-identical**, so **the high-speed ramp is untouched** — asserted in the builder. ⊕ **FLY ONE OF V156/V157**; **V157 is the better first flight** given the operator wants best results rather than a ladder. ✅ **HOUSEKEEPING**: `MEMORY-PART4.md` split at 199.5 KB → **PART4 100.7 KB / 88 entries + PART5 99.3 KB / 59 entries**, entry sets equal, **zero lost**; **`CLAUDE.md` repointed “PAGINATED IN FOUR” → “IN FIVE”** naming `MEMORY-PART5.md`.

- ✅✅★★★★★ [**CONSOLIDATED HANDOFF — eleven verified unflown builds, two symptoms, one place to act from**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/handoffs/2026-08/HANDOFF-2026-08-28-eleven-builds-two-symptoms.md) — **the analysis side is exhausted; every remaining question needs a drive.** All eleven rebuilt 2026-08-28 **bit-identically, all assertions passing** (V157 62/62 · V156 60/60 · V153/V152 54/54 · V149 52/52 · V139 49/49 · V155/V154 58/58 · V150 51/51 · V148 69/69 · V151 53/53), all on **base V122**, all `.rwd` present on disk. 🛑 **Carries the TWO-SYMPTOM split** (A = mechanical resonance, firmware reaches excitation/phase only; B = broadband, above every CAN Nyquist, **closed as unreachable by calibration**) ⇒ **all eleven target A**. ⭐ **V157 first** — damping is the textbook fix for an underdamped mode and it is the first build with a live damper in the micro regime; adds damping **only below 12.73 °/s** so max angular velocity/acceleration are untouched; **24 % of the 512 ceiling**, which itself must never be raised (it is the `0xC407E` fault interlock). ⊕ **PAIR RULE**: V157/V156, V153/V152, V155/V154 are one lever at two doses — **fly one of each**. ⊕ **STANDING ANSWERS not to re-derive**: 8× gain is a **measured NO** (6× 1.13 dB → 8× 2.24 dB); `0xC4936` never; the 512 ceiling never; **Ghidra's `code.bin` is STOCK**; tp-relative counts need the **`reg1 == tp`** filter. ⊕ **Judge by EAR and capture AUDIO** — justified by the matched contrast **1.12× [1.01, 1.27]** with a **passing positive control** ⇒ engagement adds **≤ ~2 % of RMS** ⇒ **no CAN statistic can rank these builds.**

- ⭐⭐⭐★★★★★ [**The LKAS AUTHORITY limiter is located — `0xC61BC`/`0xC61BE`, VIRGIN on all 157 builds**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — in `FUN_00028ea6`, **upstream of the 6× gain**: `uVar33 = (iVar31 × LERP) >> 8` then a **symmetric ±clamp to `cal(0xC61BC)` = 15360** (7 readers; `0xC61BE` is its twin, 8 readers). 🛑🛑 **THE KIT HAS BEEN MOVING THE WRONG CLAMPS**: `0xC61BC`/`0xC61BE` read **{15360: 157} — VIRGIN across every build**, while `0xC61B2`/`0xC61B4` have been set to **five different values** (3072/4096/2048/1024/512) across 157 images — and the record itself files that pair as **“INERT, 0 % of the effect”** precisely *because* the **“setpoint is LERP-clipped to 15360 upstream of the gain”** ⇒ **[EVIDENCE] the moved cells are the known-inert ones and the named upstream limit was never touched.** ⚠ **[UNRESOLVED] whether ±15360 BINDS** — it clamps a *product*, and *“81.5 % of rail”* is consistent with either a saturating setpoint or a LERP whose own max is 15360 (making the clamp redundant); **no probe has measured its duty**, and raising an inert clamp does nothing. 🛑🛑 **NOT BUILT — this is a SAFETY decision, not an engineering one: raising an authority clamp INCREASES the maximum torque LKAS can apply against the driver**, categorically unlike every other queued lever, which only ever *reduce*. **Needs the operator's explicit direction, and a probe first.** ✅ **SAFE NEXT ARTIFACT**: a cave rung reading **`|uVar33| ≥ cal(0xC61BC)`** — zero cal bytes, cave only, an instrument not a fix (V148/V98/V100 class) ⇒ **duty 0.0000 closes the cell; duty > 0 makes the dose meaningful and lets the operator decide knowingly.** ⊕ **The first genuinely NEW question in several turns, and it serves the operator's SECOND stated complaint rather than the first.**

- ✅★★★★ [**The authority clamp now has a concrete threshold — but the static shortcut FAILED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — tried to settle *“does ±15360 bind?”* **without** spending a cave edit (the bricking class). ✅ **ESTABLISHED**: the setpoint LERP is reached via a **pointer table at `0xCB994`** (`X` at `+2..+10`, `Y` at `+0xC..+0x14`); all six records decode to **Ymax 696–717** ⇒ with `uVar33 = (iVar31 × LERP) >> 8` clamped to 15360, **the clamp binds ⇺ `iVar31 ≥ 15360×256/717 = 5482`** — a single concrete threshold, and a **cheaper probe than instrumenting the clamp** (one comparison against a constant). 🛑 **DID NOT CLOSE STATICALLY**: `iVar31` is **not one semantic variable** — the decompiler reuses it across **16 assignments**; the one dominating the clamp is `iVar31 = ±uVar25` then `iVar31 = iVar31*0x20 - uVar35`, so bounding it needs `uVar25` **and** `uVar35` ⇒ **[UNRESOLVED] and NOT guessed** — a wrong static bound is exactly the kit's most expensive error class (a mis-read that reads as fact and propagates). ✅ **The probe remains the instrument**, but is now **simpler**: one rung `iVar31 ≥ 5482` ⇒ **duty 0.0000 closes `0xC61BC` exactly as `0xC61B2`/`B4` closed; duty > 0 makes the dose real.** 🛑 **Still not a fix, and still needs the operator's call before any dose** — raising an authority clamp **increases the max torque LKAS can apply against the driver**, unlike every other queued lever.

- 🛑🛑🛑★★★★★ [**The LKAS command RAILS at ±4096 — “peak command oscillation” is AUTHORITY SATURATION**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — first measured answer to the operator's THIRD complaint, from **cached data, no drive, no build**. `co_tqcan` (the command **as sent on the bus**), engaged frames, **114 routes / 1.37 M frames**: **overall rail duty 0.783 %**, **32 of 114 routes (28 %) EVER rail**, worst **8.633 % (r85s15)**, and the **LONGEST CONTINUOUS RAIL is 399 frames ≈ 4 SECONDS** (r9e), with `|max|` **exactly 4096** on every railing route. ⇒ **±4096 = 2^12, a 13-bit signed field — the CAN signal's own PROTOCOL MAXIMUM** ⇒ **[EVIDENCE] the command is pinned at the largest value the wire can carry, up to 4 s at a time** ⇒ **that IS peak command oscillation, and it is authority saturation.** 🛑🛑 **THE THREE COMPLAINTS SHARE ONE KNOB**: the field cannot carry more than ±4096, it is already railed, so more authority needs more **torque per count** = the **gain `0xC6CD0`** — but that is the grinding knob (**measured 6× = 1.13 dB, 8× = 2.24 dB**, vibration ∝ m^1.74 vs authority ∝ m^0.88) ⇒ **authority and grinding are in DIRECT tension through one cell**, which is why authority has been stuck the whole arc. **No openpilot fix exists — forbidden, and the field cannot represent a larger number anyway.** ⭐⭐ **THE ONE ESCAPE**: if `0xC61BC`/`0xC61BE` = ±15360 **binds**, raising it gives **more torque per command count ⇒ authority WITHOUT touching the gain ⇒ without the grinding penalty** — the only path off the tension this firmware offers, settled in one drive by the **`iVar31 ≥ 5482`** rung. ⇒ **the probe's value has gone UP: it is now the sole test of whether the authority/grinding tension can be broken at all.** ⚠ binding still **UNRESOLVED** and not statically guessable (`iVar31` is a 16-assignment temporary); 🛑 raising it stays a **safety decision for the operator**.

- ⚠★★★ [**The railed command is SUSTAINED ONE-SIDED saturation, not a rail-to-rail limit cycle**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — follow-up to the ±4096 finding. **Only 4 of 114 routes ever swing rail-to-rail**, yielding **12 intervals total** with a **4.7× spread** in implied frequency (r78 0.401 Hz · r85 0.086 · r96/r96s11 0.272) ⇒ **[NOT CLAIMED] a limit-cycle frequency** — 12 intervals across four disagreeing routes is not a measurement, and quoting “0.27 Hz” would be the kind of number this kit has had to retract. ✅ **WHAT IT DOES ESTABLISH**: the command **rails on ONE side and STAYS there**, up to **399 frames ≈ 4 s** continuous, rather than alternating ⇒ **“peak command oscillation” is SUSTAINED ONE-SIDED AUTHORITY SATURATION, not a controller limit cycle** ⇒ strengthens the authority diagnosis (openpilot holds the field's maximum because the plant under-delivers per count) and ⇒ **no oscillation-damping lever applies — there is no cycle to damp**; the fix is torque-per-count, i.e. the **gain (frozen)** or **`0xC61BC` (binding unknown)**.

- ⚠★★ [**The railed-command natural experiment is UNDERPOWERED — do not re-run**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a rail episode freezes the command at ±4096, so it should test whether the ratchet is command-driven. Ran it: **only 3 routes qualify with 3–9 railed windows each**, ratios spanning **1.73 / 2.21 / 19.05** ⇒ **[NOT CLAIMED]**. 🛑 **And the statistic is COMPOSITIONAL** — band *share* is normalised to 1–45 Hz, so 6–9 Hz rising **forces** the 26–31 Hz “control” down (0.31/0.32/0.04) arithmetically, so it is **not independent evidence** — exactly the trap [[feedback-run-the-control-before-the-measurement]] names. ⊕ Directionally all three exceed 1 with the command frozen, consistent with the ratchet **not** being command-driven — but that is **already settled by V87** (7.8 Hz line prominence **12.9 in the COLUMN vs 4.0 = chance in the COMMAND**), so the experiment **adds nothing.** ⇒ rail episodes are **0.78 % of engaged frames** on **28 % of routes**; closing it would need a drive deliberately sustaining saturation, to re-confirm a settled point. **Low value — recorded so it is not attempted again.**

- 🛑🛑🛑★★★★★ [**ALL THREE complaints are CREEP phenomena — and speed-scheduling the gain is DEAD**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — tried to **make** a lever: schedule the gain by speed, high where authority saturates and low where grinding lives, which would break the tension outright. **It requires them to live at different speeds. They do not.** Rail duty by speed, 1.6 M engaged frames pooled: **0–2 km/h 0.389 % · 2–8 km/h 6.367 % (THE PEAK) · 8–16 km/h 0.875 % · 16–25 km/h 0.169 % · 25–40 km/h 0.009 %** ⇒ **CREEP (0–8) 3.540 % vs HIGHWAY (≥16) 0.101 % = 35×** ⇒ **[EVIDENCE] authority saturation is a CREEP phenomenon** ⇒ **🛑 speed-scheduling the gain is DEAD — there is no band where authority is needed and grinding is absent; lever class closed before a build was spent on it.** ⭐ **BUT IT UNIFIES THE THREE COMPLAINTS**: peak command oscillation (rails 6.4 % at 2–8 km/h), LKAS authority (saturated in that same band), and grinding (symptom A's micro regime 1–13 °/s and symptom B's <10 mph acoustic excess) are **all the SAME OPERATING POINT — engaged creep, ~2–8 km/h.** Treated as three problems for the whole arc; they are **three observations of one regime**, and any real fix must act **at creep**. ✅ **SHARPENS THE FLIGHT ORDER**: **V157/V156 act ONLY at creep** (FactorC opens below 35 km/h **and** FactorE below 12.73 °/s ⇒ the damper is non-zero exactly in the 2–8 km/h band), while **V153/V152, V149, V150, V139, V155/V154 are not speed-gated** and spend most of their effect outside it ⇒ **V157 is the ONLY queued build confined to the band where all three symptoms live — now an argument from MEASUREMENT, not mechanism.**

- 🛑🛑★★★★★ [**The creep-authority chain is CLOSED — lockout already pulled, next conjunct not a cal**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — all three complaints live at **2–8 km/h**, so I followed the **low-speed steer lockout** to the end. **LINK 1**: `0xC62EA` is **0 on V122** (across 157 images **{0: 108, 320: 49}**) ⇒ **the lockout has been OFF for most of the arc**, so the 6.4 % railing at 2–8 km/h is **not** caused by it — **lever spent, do not re-propose.** **LINK 2**: that activates the memory's own pre-registered suspect ([[accord-low-speed-lockout-window-c62ea]]: *“if a lowered `0xC62EA` doesn't work, `gp-0x69aa` is the next suspect”*). Read at its site (region **byte-identical stock vs V122**): `ld.hu -0x69aa,gp,r14` · **`ori 0x8000,r0,r9`** · `cmp r9,r14` · `bh` → failure ⇒ **the 0x8000 threshold is a HARD-CODED IMMEDIATE, NOT `cal(0xC63F2)`** (which is read at a different site, `0x28ECE`) ⇒ **[EVIDENCE] the governor-derate conjunct is NOT reachable by calibration — the pre-registered suspect is closed as a cal lever.** ⚠ **CORRECTION 1**: the memory records `gp-0x69aa == 0x8000`; the instruction is `bh` (unsigned HIGHER) to the failure path ⇒ **the passing condition is `<= 0x8000`, not `==`**. ⚠ **CORRECTION 2**: I first wrote `tp+0x73F2` as `0xC73F2` — **the SIXTH recurrence of the off-by-0x1000 trap** `CLAUDE.md` lists five of — caught by **anchoring against the memory's stated 32768** (wrong address read 14, right one 32768). ✅ **WHAT REMAINS**: of the AND-chain gating control-active at creep, `0xC62EA` is spent, `gp-0x69aa` is an immediate, `gp-0x67fe` is a state, and `gp-0x69ae` plus a 5-channel validity test are unexamined ⇒ **the ONLY cal-reachable candidate left is `0xC61BC`**, exactly what the **`iVar31 ≥ 5482`** probe settles ⇒ **the probe is now the last cal-reachable question in the whole creep-authority chain.**

- ✅✅✅★★★★★ [**Control is FULLY ACTIVE where the command rails — the creep-authority chain is CLOSED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the memory's *“0 % control-active below 2 mph”* was measured with `0xC62EA` = 320; it is **0 on current builds**, so I redid it: **227 routes, 1.88 M engaged frames**, `STEER_STATUS` = **{0: 1,901,564 | 3: 14,538 | 4: 92}**. **ST=3 duty by speed: 0–2 km/h 5.868 % · 2–8 km/h 0.000 % · 8–16 0.000 % · 16–25 0.000 % · 25–40 0.000 %** ⇒ **[EVIDENCE] STEER_STATUS=3 is EXACTLY ZERO at 2–8 km/h**, the band where the command rails 6.4 % ⇒ **the lockout removal WORKED and the old “0 % below 2 mph / 88 % at 3–4 mph” figures are OBSOLETE for every build since.** ⭐⭐ **WHAT IT SETTLES**: lockout already 0 ⇒ not gating; ST=3 zero at 2–8 km/h ⇒ **control FULLY ACTIVE**; the command rails anyway ⇒ **the demand genuinely exceeds the 13-bit field** ⇒ **[EVIDENCE] the creep authority shortfall is NOT a dropout, lockout or gating failure** ⇒ **the ONLY remaining explanation is TORQUE PER COUNT** — the gain `0xC6CD0` (frozen both ways) or the virgin ±15360 clamp `0xC61BC` (binding unknown). **Every other hypothesis for creep authority loss is eliminated by measurement.** ⊕ **ST=3 survives only at 0–2 km/h** (standstill, governed by the `gp-0x68b3` bypass) — expected, not a complaint regime. ⊕ **ST=4 occurs 92 times in 1.88 M frames (0.005 %)** ⇒ **V42's state-4 governor ratchet fix is holding across the whole corpus**, independently confirming [[reference-accord-state4-governor-ratchet]] and that [[accord-v42-ratchet-fix-lost-since-v53]] is restored. ⇒ **the `0xC61BC` probe is the last standing question about creep authority.**

- 🛑★★★★ [**“Grinding continues after disengaging” — NO band-specific persistence on the bus**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the operator's V133 report claimed the grinding **outlasts disengagement**, which is structural (the command is gone ⇒ the mechanism would need **memory**) and had never been tested. Tested on **139 creep-ish disengage events across 76 routes**, aligning on the engaged→manual edge and tracking the 6–9 Hz Hilbert envelope of `tq` against its own engaged baseline. ⚠ **UNCONTROLLED IT LOOKED REAL**: 1.293 / 1.212 / 1.225 / 1.119 over the first 5 s ⇒ *“~25 % elevated for 5 s”*. ✅ **THE CONTROLS KILL IT**: 6–9 Hz **1.377 [0.918, 1.588]** (CI includes 1), 26–31 Hz **1.051 [0.948, 1.288]**, 32–38 Hz **1.117 [1.025, 1.249]**, and the band-specific **RATIO 6–9/26–31 = 0.911 [0.795, 1.132]** ⇒ **[EVIDENCE, with controls] NO band-specific persistence** — the elevation is in the control bands too, so it is **the driver taking over**, not ratchet memory. ⭐ **THE DURABLE POINT**: the same 139 events read as *“persists 25 %”* or *“no effect”* depending **only** on whether the control band is computed — [[feedback-run-the-control-before-the-measurement]] on a fresh question, and the uncontrolled number was **plausible, specific, and pointed at a mechanism that does not exist**. 🛑 **DOES NOT refute the operator**: either (a) the persistence is not a 6–9 Hz column phenomenon, or (b) **it is real but NOT OBSERVABLE ON THIS BUS** — and **(b) fits everything else** (motor/rack-side mode, engagement adds ≤~2 % of RMS) ⇒ **the ear remains the only instrument; the null is as much about the instrument as the symptom.**

- ⚠★★★ [**Audio envelope demodulation for the ratchet — NEW method, no signal, imperfect control**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the kit uses audio only for symptom B's **band power**, but **a ratchet is an impulse train**, so it would show as **AM of the audio ENVELOPE at 7.8 Hz**, never tried. ✅ **The instrument already exists**: caches store per-band envelopes at **62.5 Hz** (`wide` x 10 with `wide_lab`; the 100 Hz `a20_100...` format is only on r81/r82) => Nyquist 31.25 Hz, control moved to **20–28 Hz**; **5 routes carry creep-engaged audio** (r9e 12,952 · r96 9,803 · r97 5,495 · r85 5,238 · ra4 4,312). ⚠ **RESULT: every 6–9 Hz CI spans 1** — 15–21 0.971 · **28–40 1.309 [0.978, 1.627]** · 40–60 1.110 · 60–100 1.134 · 100–300 1.020 · 300–1000 0.969 · 1k–3k 1.031 · 3k–8k 1.030 => **no detectable ratchet AM.** 🛑 **BUT the 60–100 Hz CONTROL reads 0.804 [0.626, 0.923], EXCLUDING 1** — windows were matched on **creep speed only, not steering activity** => **[NOT A CLEAN NULL]**, underpowered at 5 routes with an imperfect control, and **must not be cited as evidence the ratchet is acoustically silent.** ⭐ **KEEP THE METHOD** — envelope AM demodulation is a legitimate, previously-unused instrument for an impulse-train symptom, and the most suggestive cell is the **28–40 Hz band at 1.309**, where a mechanical ratchet's carrier would plausibly sit. => **to close it: more routes with audio AND matched creep engaged/manual exposure, stratified on activity as well as speed — only 5 of ~230 routes have usable creep-engaged audio** => **capture audio on every future drive.**

- ✅✅★★★★★ [**The audio AM null is now CLEAN — and that is the THIRD artefact killed by a control**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — last turn's envelope-AM test had a **failing 60–100 Hz control** (0.804) because windows were matched on **creep speed only**. Re-ran with **(speed bin x |rate| RMS bin)** stratification: **ALL EIGHT control bands now span 1**, and **every 6–9 Hz AM estimate collapses to ~1** — 15–21 1.020 · **28–40 0.958 (was 1.309)** · 40–60 1.048 · 60–100 0.869 · 100–300 1.013 · 300–1000 1.004 · 1k–3k 1.038 · 3k–8k 1.058 ⇒ **[EVIDENCE, clean controls] NO engagement-conditional 6–9 Hz AM in the audio, in any band**; the previously “suggestive” 28–40 Hz cell was **pure matching artefact.** ⚠ 60–100 Hz excludes 1 *below* (0.869) but with 8 bands tested that is expected by chance — **not claimed.** ⇒ **both available instruments now return clean nulls on symptom A** (CAN bound ≤ ~2 % of RMS; audio AM ~1.0), consistent with the mode being **motor/rack-side and unobservable**. 🛑🛑 **THE PATTERN — three times in one session the control killed the effect**: engagement contrast **2.8× → 1.12 [1.01, 1.27]** (matched); post-disengage persistence **1.29× → ratio 0.911** (control band added); audio AM **1.309 → 0.958** (activity-matched). **Each was plausible, specific, and pointed at a real mechanism; none survived its control.** ⭐ **RULE: on this kit an uncontrolled engaged-vs-manual ratio is worth nothing** — the arms differ systematically in operating point (engaged = creep and steady, manual = faster and more active) and **that difference is larger than every effect measured this session**. **Compute the control first, or do not compute the number.** See [[feedback-run-the-control-before-the-measurement]].

- ✅✅★★★★ [**The ratchet is NOT episodic in band share — the tail looks like the median**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a flaw in my own method: every matched analysis took the **MEDIAN**, and the kit's characterisation says the ratchet hit **44 of 46 windows** on one route, so an **episodic** symptom would be washed out. Re-ran the matched contrast at the **TAIL**, 23 routes, stratified on (speed x |rate| RMS): **median 1.067 [0.966,1.226] · p75 1.073 · p90 1.160 [0.993,1.256] · p95 1.100 [1.039,1.243] · p99 1.110**, controls 0.987 / 1.082 / 1.022 / 1.065 / 1.024 ⇒ **[EVIDENCE] the tail is indistinguishable from the median** (1.10 vs 1.07) ⇒ **the engagement effect is a UNIFORM ~10 % elevation, NOT episodic concentration.** The kit's “44/46 windows” figure was about detecting a **LINE**, a different statistic from band share — **no conflict.** ✅ **It also FIRMS THE EFFECT UP**: at **p95 the 6-9 Hz CI EXCLUDES 1** while its control spans 1, converging with the independent median estimate **1.12 [1.01, 1.27]** ⇒ **engagement adds ~10 % to the 6-9 Hz band, robust across central AND tail statistics, controls clean at every percentile.** ⊕ **Closes the last live objection to the ≤ ~2 % of RMS line bound** — the natural challenge was *“an episodic symptom would be diluted”*; **it would not be, because the tail behaves like the centre.**

- ✅✅✅★★★★★ [**FIVE independent metrics converge on ~1.1x — the measurement side is EXHAUSTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the last objection was that **band power is the wrong perceptual quantity** (grinding = roughness = **modulation DEPTH**, and 7.8 Hz is in the fluctuation-strength range; depth normalises to mean level, share to the envelope spectrum). Tested: **depth 6-9 Hz 1.161 [1.049, 1.284]** (excludes 1) vs **depth 26-31 CONTROL 1.086 [0.912, 1.292]** (clean) vs **share 6-9 1.106 [0.998, 1.273]** => depth is marginally better but its **band-specific advantage over its own control is only 1.07x** => **[EVIDENCE] the perceptual-metric hypothesis does NOT explain the gap.** ⭐⭐ **CONVERGENCE**: CAN band share median **1.12 [1.01,1.27]** · CAN band share p95 **1.10 [1.04,1.24]** · CAN line prominence **1.17** (=> **<= ~2 % of RMS**, positive control PASSED) · CAN modulation depth **1.16 [1.05,1.28]** · AUDIO envelope AM **~1.00 across all 8 bands** — **five statistics, two instruments, all between 1.0 and 1.2 with clean controls.** 🛑 **[CONCLUDED] the measurement side is EXHAUSTED** — not *“we haven't found the right statistic”*: **five were tried, including the perceptually correct one, and they agree.** => the only consistent reading is the structural one — **the mode is motor/rack/tyre side and no channel this vehicle exposes observes it**; a symptom reading ~1.1x on every instrument while unmistakable to the driver is **an observability limit, not a statistics problem.** => **the ear is the instrument, now supported by five converging negatives rather than resignation. No further metric should be attempted on cached data.**

- ✅✅✅★★★★★ [**The damper is a FOUR-factor cascade, not two — and V157 is VALIDATED by it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the kit models the base damper as `ch0 = FactorC(speed) x FactorE(rate) >> 10`; its actual writer **`FUN_00034350`** computes `((((clamp(gp-0x698a,1024) * L1 >>10) * L2 >>10) * L3 >>10) * L4 >>10)` with **L1 = LERP [0xC9CCC] on torque · L2 = LERP[0xC9E9C] on `gp-0x6a5e` SPEED · L3 = LERP[0xC9DB4] on `gp-0x6a10` ANGLE · L4 = LERP[0xC9F84] on `gp-0x6ac0` MOTOR RATE**, then a **sign flip from `gp-0x6abe`** and a **ceiling LERP[`PTR_DAT_000c77a0`] on `gp-0x6ac2`**. ✅ **THE CHECK**: **L1 and L3 are FLAT UNITY [1024,1024,1024,1024] at every knot** ⇒ they can never zero the product; **only L2 [0,234,429,908] and L4 [0,140,539,927] have Y[0]=0** ⇒ **V157 opens EXACTLY the two that gate**, and the full four-factor arithmetic `((((1024*1024)>>10)*234>>10)*1024>>10)*539>>10 = 123` **reproduces the builder's asserted dose exactly** ⇒ **V157 is correctly and completely targeted**; the two-factor model gave the right answer **by luck**, because the omitted factors are unity. ⚠ **CORRECTIONS**: “FactorC” is **L2 (speed, via `0xC9E9C`)** and “FactorE” is **L4 (motor rate, via `0xC9F84`)** — two of four cascaded LERPs, not a bare pair; record layout is **`X[0]` at base+2, `Y[0]` at base+10** (L3 uses +0xC/+0x14), and **I first compared pointer-table BASES against V157's X[0] addresses and got “NOT FOUND” on all four — a 2-byte off-by-one that would have condemned a correct build**, caught by re-deriving the layout from the decompile. ⭐ **ADDS TO V157'S CASE**: the two factors it opens are the **only** gating ones, so **nothing else in the cascade can silently zero it** — the failure mode that made V134 inert **cannot recur**; and the branch conditions (**L4: `gp-0x6ac0 < 0x32C9` and `|gp-0x6abe| <= 13000`; L2: `gp-0x6a5e <= 0x7D00` and `gp-0x67f4 == 1`**) are all satisfied at creep.

- ✅★★★★ [**The damper ceiling read from its own records — V157's 24 % confirmed, 3.7x headroom left**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V134 cited the ceiling as `Y[512,1024]` without reading it per mode. Read: **m25/m26/m27 all X=[300,800] on `gp-0x6ac2`, Y=[512,1024]**, fallback **cal(`0xC6158`) = 512** when `gp-0x6ac2 >= 0x32C9` ⇒ **at creep the operative ceiling is 512** ⇒ **[EVIDENCE] V157's product of 123 is 24.0 % of it**, confirming the builder from the records rather than a citation, with **max safe product at 90 % = 460, i.e. 3.7x headroom.** ⭐ **PRE-COMPUTED NEXT DOSE** (so a good result is not followed by guesswork): **L2_Y0 → own Y[2] (429/426) and L4_Y0 → own Y[2] (539) ⇒ product 225 = 44 %, margin 2.3x** — both still the tables' **own knots**. 🛑 **NOT BUILT deliberately**: two doses of this lever are queued and **neither has flown**; a third artifact adds nothing a recipe does not. ✅ **THE DAMPER IS NOW FULLY CHARACTERISED** — product, the four LERPs (**L1/L3 unity, L2 [0,234,429,908], L4 [0,140,539,927]**), **sign from `gp-0x6abe`**, ceiling LERP + fallback, and both gate conditions (**L2: `gp-0x6a5e <= 0x7D00` and `gp-0x67f4 == 1`; L4: `gp-0x6ac0 < 0x32C9` and `|gp-0x6abe| <= 13000`**) — **all read from the image, and the only two zero knots are the two V157 opens.**

- ✅✅✅★★★★★ [**V158 — the GOLDEN MODEL's own damper prescription; it SUPERSEDES V156/V157**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **PROCESS FAILURE CORRECTED**: V156/V157 were designed from BUILD-LINEAGE and V134's docstring (a **two-factor** damper model); the **golden model** already had the **five-factor** structure **and a measured prescription**, so last turn's “four-factor discovery” was a **re-derivation**. 🛑 **V157 IS WRONG**: it sets FactorE `Y[0]=539` against `Y[1]=140` ⇒ **Y=[539,140,539,927] NON-MONOTONE**, which **flattens the rate factor across the symptom's own range — V72's flatten-to-relay error**, which the golden model explicitly warns against. ✅ **V158 follows the model**: `FactorC Y[0] := own Y[2]` (429/426) + `FactorE X[0] 60→12` + `FactorE Y[1] := own Y[2]` (539) ⇒ **Y=[0,539,539,927] MONOTONE**, `Y[0]` stays 0 and the dead zone is opened by **moving the AXIS**. **Priced at the model's MEASURED operating point `gp-0x6ac0`=99** (on FactorE's first rising segment, not flat at Y[0]): FactorE=120, FactorC=429 ⇒ **product 50, inside the model's own requirement ~43 [30,60]**, and **9.8 % of the 512 creep ceiling (10.2× margin)** ⇒ **V157's 123 is 2.4× the requirement, V156's 31 is below it — only V158 is inside.** **10 payload bytes, 67/67, CRC 50/50**, rwd `511c4a71…`. ⚠ **My own gate caught a subtlety**: `FactorC Y[0]:=Y[2]` is **also non-monotone** ([429,234,429,908], a dip at 35–60 km/h) — **accepted deliberately and asserted as such**, because FactorC is a **speed schedule** not the damping law, and the monotone alternative (234) gives **product 28, below the requirement**. 🛑 **LESSON: for any damper or lane work read `eps_chain_lanes.py` FIRST** — it had the structure, the measured operating point, the priced requirement and the shape rule all along.

- ⭐⭐⭐★★★★★ [**The AUTHORITY complaint has a specific mechanism — the operator drives on the collapse curve's KNEE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — auditing the queue against the golden model (the V158 lesson) surfaced an answer **already in the record**: the **LKAS authority COLLAPSE CURVE** (`0xE547C`/`0xE5404`/`0xE52FC`/`0xE5284`, mode 7) takes authority **254 -> 0 across raw driver torque 2240 -> 2560**, is **VIRGIN on all 90 images**, and **the operator's measured median override torque is 2235 — ONE COUNT below the first knot** => **[EVIDENCE] he drives on the knee**, so any small increase in his input collapses authority toward 0. ⊕ **A SECOND, DISTINCT mechanism from the +-4096 command rail**: the rail is the command hitting its **13-bit protocol max** (6.4 % of frames at 2-8 km/h); the curve is authority being **cut by the firmware** above driver torque 2240. 🛑 **THE RULE THAT BLOCKS THE FIX**: the golden model requires any change to be **MONOTONE-NON-INCREASING — never more authority than stock at any torque** — so **moving the knee up violates it**, and the rule exists because **raising the curve makes the car fight the driver harder during override.** **[DECISION] NOT proposed — like `0xC61BC`, an operator decision about how hard the car may resist him.** ⚠ **It will not help grinding**: filed as **“NOT a 6-9 Hz lever — refuted five ways; it drives the ~0.5-1 Hz SURGE.”** ☑ **RE-CONFIRMED `0xC64B8` IS DEAD** — at mode 7 both arms deliver 0 everywhere the branch could fire (all four records clamp to `Y[last]=0` above `X[last]`=80/112, below the gate's 113) => **stock and V37 are bit-identical on this car; do not re-propose it.** ✅ **All three complaints now have named mechanisms**, and two are blocked **by deliberate safety constraints rather than ignorance.**

- 🛑🛑★★★★★ [**V154/V155 SUPERSEDED — `0xC63A6` is a STRUCK cell; and the eight Path-2 coefficients are READ**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `eps_chain_lanes.py`: **“NO WEIGHT MAY BE MOVED”** because `gp-0x6b70` is a **PID reference that gets SUBTRACTED**, and **“Path 2 IS A REAL CLOSED LOOP... GATE 2 CANNOT BE CERTIFIED. 0xC63A6 was struck on exactly this.”** ⊕ my amplitude justification was **right open-loop** (the model: the two `sign(iVar6)` factors **square to +1 and cancel**) but **I reasoned open-loop about a CLOSED loop** — what GATE 2 exists to catch => **V154/V155 superseded.** ✅ **THE EIGHT COEFFICIENTS, byte-read for the first time**: `0xC404C` **0.0** · `0xC4050` **0.0** · `0xC40BC` 3000 · `0xC40D0` 408 · `0xC40D2` 1020 · `0xC40D4` 573 · `0xC40D6` 246 · `0xC40D8` 3686 => **two are EXACTLY 0.0 in stock and V122, and they multiply the HISTORY terms**, so `fVar14 = 0.0*gp-0xc9c8 + 10.0*fVar19 + fVar15*0.0 = 10.0*fVar19` => **that three-tap structure is a PURE GAIN of 10.0, both memory taps DEAD.** GATE 2 is **not closed** (the RAM LERP slope is still unextracted) but **the loop gain has two fewer live terms than assumed.** ⭐ **AUDIT PATTERN**: V156/V157 mis-shaped, V154/V155 on a struck cell, **V158 correct** — **three recommendations built from BUILD-LINEAGE instead of the golden model, two of them wrong; the cost of skipping required reading was four superseded artifacts.**

- 🛑🛑🛑★★★★★ [**V149 SUPERSEDED — it REMOVES Lever B, the kit's only measured grinding fix**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **my premise was wrong**: V149 was built to remove a *“5.12x switch driven by the fault counter `gp-0x671d` at task rate”*, but `0x3AA96` is **`fb` on V122**, i.e. `ld.bu -0x6806[gp]` — **the gate is LKAS CONTROL ACTIVE, not a counter**, so it toggles on engage/disengage and **there is no task-rate switching nonlinearity to remove.** 🛑🛑 **WORSE: 5244 IS THE FIX.** The golden model: *“THE FIX: V67 = V66 + the grind #1 fix GATED ON LKAS. `0x3AA96 c5->fb` + `0xC6446 512->5244`… the firmware already HAS a conditional-gain arm and it is merely wired to a dead cell… gate TRUE (LKAS on) -> flat 5244 = **2.00x the LERP at grind #1's operating point**”*, and **V88 = “Lever B restored” is the build that FLEW with “grinding FIXED”** ([[accord-v88-flew-grinding-fixed-command-intact]]; [[accord-v81-carries-neither-grind1-fix]] calls it **“best in kit”**) ⇒ **[EVIDENCE] Lever B is ACTIVE on the flying build and V149 collapses it ⇒ V149 REMOVES THE ONLY CHANGE THIS KIT HAS EVER MEASURED AS FIXING GRINDING. SUPERSEDED — never fly it.** ⚠ **V152/V153 carry the SAME open GATE 2**: `tp+0x50d0` = `0xC40D0` is **one of the eight uncertifiable Path-2 loop-gain coefficients**. Their argument is **better** than V154/V155's (a pure low-pass lowers HF loop gain, vs an unresolved sign) but **a low-pass also adds phase lag and margin cannot be checked without the RAM LERP slope** ⇒ **NOT superseded, but DEMOTED and flagged GATE-2-OPEN; do not fly ahead of V158.** 🛑 **FIVE builds superseded this session (V149/V154/V155/V156/V157), all one root cause: designed from BUILD-LINEAGE and my own decompiles instead of the GOLDEN MODEL**, which already held the structure, the prescription, the strikes and the fix.

- 🛑🛑★★★★★ [**V139 SUPERSEDED — and the `sar` pair is CLOSED at 6x gain, BOTH directions**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0x3AB76` and `0x3AC20` are **imm5 = 10 (stock) on V122**, so V62's sar pair — *“the kit's ONLY measured grind-#1 fix”*, 18-22 Hz down 8-42x — **is absent from the flying build**, and the obvious move is to restore it. **It is not the right move.** 🛑 **BOTH DIRECTIONS ARE WRONG ON A 6x BASE**: **sar 9** (double) is V62's fix **but was measured on a 4x-GAIN base**, and **V133 flew sar 9 at 6x => “massive, violent grinding”**; **sar 11** (halve) is V139, and memory records **“r24 HALF CAUSED grind #2”**. ⊕ **V139's OWN builder holds the decisive argument** I should have followed instead of promoting it: *“scaling V62's 4x optimum to V122's 6x base lands BETWEEN sar 9 and sar 10, which argues Honda sar 10 is already about right for a 6x base and sar 11 OVERSHOOTS.”* ⇒ **stock sar 10 is ~optimal at 6x; the sar pair is CLOSED as a lever. V139 superseded, and do NOT restore Lever A either.** ⭐ **GENERAL LESSON: the sar optimum is COUPLED TO THE GAIN** — V62's result came at **4x**, the car runs **6x**, and **a measured optimum does not transfer across a gain change** ⇒ **[RULE] before restoring ANY historical fix, check the gain base it was measured on**; this applies to every pre-V101 result, since `0xC6CD0` moved 3564->5346 at V101. 🛑 **SIX builds superseded this session** (V139/V149/V154/V155/V156/V157), all from **designing off BUILD-LINEAGE instead of the golden model** — **V158 is the one built from the model, and the one that survives.**

- ✅★★★★★ [**The audit is COMPLETE — V150 is inert hands-off, and the queue is final**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V150's structure is **right** (the model confirms the zero-force gate is **`gp-0x6b5e != 0` AND `assist_state_671a < cal 0xC64FA`**, and V150 makes the state term always true so the gate reduces to `gp-0x6b5e != 0`) — **but the model also says the gate does not fire where the symptom is**: *“Hands-off the margin sits ~24x above the kill threshold => THE GATE LEAVES r26 LIVE in ordinary driving and most strongly live in hands-off creep — exactly where the grinds and the ratchet occur.”* ⇒ hands-off `gp-0x6b5e == 0`, so V150's reduced condition is **already satisfied** ⇒ **[EVIDENCE] V150 changes behaviour ONLY under driver torque ⇒ INERT in hands-off creep**, able to touch **grind #2** (tq_avg 1600–2700) only ⇒ **NOT superseded but DEMOTED; do not call it a ratchet lever.** ✅ **FINAL QUEUE**: **FLY V158** (damper, the model's own measured prescription, GATE 2 closed) · V148 probe · V150 grind-#2 only · V151 marginal · V152/V153 GATE-2-OPEN · **SUPERSEDED V139/V149/V154/V155/V156/V157**. 🛑 **SESSION VERDICT: 12 builds made, 6 superseded (two would have REMOVED measured fixes — V149 removes Lever B which flew with “grinding FIXED”, V139 is the direction recorded as CAUSING grind #2), 4 demoted or inert, and 1 survives as a fix — V158, the only one designed FROM the golden model.** ⭐ **The audit was worth more than the builds it deleted. Read `eps_chain_*.py` FIRST.**

- ⭐⭐⭐★★★★★ [**The PARAMETRIC PUMP is located — lane gain A's knee straddles the operating point**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ⚠ first, a **correction: V158's GATE 2 is NOT fully closed.** The model warns that **all three PID lane gains are LERPs on `gp-0x6ac0`, the SAME rectified rate that indexes FactorE**, so *“size any FactorE edit against this, not just dose”* — **I priced V158 by dose alone and called its gate closed; that was overstated.** ⭐ **SIZING IT PRODUCED A FINDING**: `0xC671E` (lane gain A) has **X=[96, 104, 608, 704], Y=[704, 832, 832, 832]** — its **first segment spans 96→104 and the measured operating point `gp-0x6ac0` = 99 sits DEAD CENTRE**, an **18 % gain swing over 8 counts, slope 6.74/count**; lanes B and C are **flat** there. Since `gp-0x6ac0` is **RECTIFIED**, it **sweeps at 2f = 15.6 Hz** across that window during a 7.8 Hz oscillation ⇒ **[EVIDENCE] the PID's lane gain A is PARAMETRICALLY MODULATED ~18 % at 2f at the symptom's own operating point, and it is STRUCTURAL — present on STOCK.** ⊕ a named source at last for [[accord-v59-parametric-pump-marginal]]. ✅ **THE LEVER: flatten the knee DOWNWARD** — `0xC671E` **Y[1] := Y[0]** ⇒ Y=[704,704,832,832], so the 96–104 segment is flat and the 2f sweep sees **no gain change**; downward **lowers** PID gain rather than raising it, the ramp moves to 104–608 (a **6x wider, gentler** span), and **monotonicity is preserved** — the rule V157 broke. ⚠ **[BELIEF]** that removing an 18 % modulation is audible; ⚠ **[UNVERIFIED] readers, mode-indexing and record layout** — the 4-knot pattern fits lane A but gives implausible X for B and C (`[256,256,0,8]`, `[717,0,0,5]`), so **the layout is probably not uniform. RULE 7 is NOT satisfied — verify before building.**

- 🛑🛑★★★★★ [**RETRACTED — the “parametric pump at lane gain A” was a table-layout artefact**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I reported an 18 % parametric gain modulation at 2f from lane gain A's knee straddling the operating point, **flagged RULE 7 unsatisfied, and said verify before building. Verified — it is WRONG.** The raw halfwords are `... 0, 8, | 64,65,67,73,80,88,96,104 | 608,704,704,832,832,832,832,832 | 0,4 ...` ⇒ **`0xC671E` = 96 sits in the MIDDLE of one ascending run of 8, not at a record base**, so my 4-knot reading **straddled the X/Y boundary** and the “steep knee” follows from that mis-split alone. ⊕ **The tell was in my own output and I noted it without heeding it**: the same layout gave lanes B and C the impossible axes `[256,256,0,8]` and `[717,0,0,5]` — **a layout that produces nonsense on two of three records is not a layout.** ⚠ **The plausible reading REVERSES it**: as count=8 then X[8]=[64…104] then Y[8]=[608…832], the operating point 99 falls between X[6]=96 and X[7]=104 where **Y = 832 on both sides — FLAT**, so no modulation there at all (**also unverified — stated as the alternative, not a finding**). 🛑 **And it cannot be verified by scanning**: the `reg1==tp` scan finds **NO readers** for `0xC671E`/`0xC670A`/`0xC66DE` ⇒ **[UNRESOLVED] layout, readers and mode-indexing of all three lane-gain tables; all three of last turn's readings are VOID**, including “lanes B and C are flat”. ✅ **SURVIVES**: the golden model's own warning that the three lane gains share `gp-0x6ac0` with FactorE and *“size any FactorE edit against this, not just dose”* — **so V158's shared-axis GATE 2 remains OPEN**, because I could not read the tables it refers to. ⭐ **Cost: nothing on the car** — the verification caught it **before** a build, unlike V156/V157/V149/V139 where I built first.

- ✅✅✅★★★★★ [**UN-RETRACTED and BUILT — V159 removes an 18 % PARAMETRIC modulation of K_p at 2f**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **I over-corrected: the retraction was itself WRONG.** Decompiling `FUN_0003a382` gives **X[0]=tp+0x7b1e, X[last]=tp+0x7b24, Y[0]=tp+0x7b26, Y[last]=tp+0x7b2c** ⇒ **X is 4 halfwords at `0xC671E`, Y at `0xC6726`** — exactly the layout I first assumed. **What went wrong: lanes B/C read non-ascending X, and I let that anomaly override DIRECT INSTRUCTION EVIDENCE for lane A.** ⭐ **MECHANISM**: K_p is a LERP on **`gp-0x6ac0`, the RECTIFIED motor rate**; the model's **measured** operating point **99 [94,113]** lies **inside segment 0 (X 96→104)** where Y rises **704→832 = 18.2 % across 8 counts**, and a rectified index **sweeps at 2f = 15.6 Hz** during a 7.8 Hz ratchet ⇒ **[EVIDENCE] the PID's proportional gain is parametrically modulated ~18 % at 2f at the symptom's own operating point, STRUCTURALLY, on STOCK** — a **named candidate source** for [[accord-v59-parametric-pump-marginal]]. ✅ **V159: `0xC6728` 832→704** ⇒ Y=[704,704,832,832], **2 payload bytes, 54/54, CRC 50/50**, rwd `7c51b28b…`; swing **18.2 %→0.0 %**, **downward** (no new clamp reachable), **monotone** (the ramp moves to X 104–608, a **63× wider** span), **RULE 7 satisfied BY STRUCTURE** (bare `tp` displacements, **no index register** ⇒ flat table, all modes), and **VIRGIN** (832 on all 158 images). ⚠ **[BELIEF]** audibility; ⚠ **[OPEN]** lanes B/C's non-ascending X; ⚠ **V159 does NOT close V158's shared-axis GATE 2** — it addresses the **PID side**, not the FactorE side ⇒ **independent builds, do not stack.**

- 🛑🛑🛑★★★★★ [**V159 SUPERSEDED — an OFF-BY-0x400 address error, and the lane gains are FLAT**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **`tp+0x7B1E` = `0xC6B1E`, but V159 edited `0xC6728` = `tp+0x7728`, an UNRELATED cal**: I confused `0x7B1E` with `0x771E`, an **off-by-0x400**, same family as the off-by-0x1000 trap recorded six times. 🛑 **THE REAL TABLES KILL THE FINDING**: **K_p `0xC6B1E` X=[0,300,2000,4000] Y=[256,256,225,153]** · **K_i `0xC6B0A` Y=[98,98,98,98]** · **K_d `0xC6ADE` Y=[2048]x4** ⇒ the operating point **99 [94,113] lies in segment 0 of all three**, where K_p is **256→256 FLAT** and K_i/K_d are flat everywhere ⇒ **[EVIDENCE] ALL THREE PID LANE GAINS ARE FLAT AT THE OPERATING POINT — there is NO parametric modulation there.** The whole “parametric pump at lane gain A” line is **VOID**, and the hypothesis is now **CLOSED on the correct tables.** 🛑🛑 **THE PROCESS FAILURE IS WORSE THAN THE ARITHMETIC**: lanes B/C read **non-ascending X** — the **correct symptom of a wrong BASE** — and I read it twice and misdiagnosed it twice, first blaming the LAYOUT (retracting for the wrong reason), then **“verifying” against the decompile by RE-DERIVING THE SAME WRONG ADDRESS** and un-retracting. ⭐ **RULE: when a neighbouring record of the same family decodes as nonsense, suspect the BASE ADDRESS before the LAYOUT** — a wrong base garbles the whole family, and **lane A “working” while B/C were garbage was itself the tell.** ⭐ **RULE: re-deriving an address the same way is NOT verification** — **add the offset to `tp` explicitly and print both**, which is what finally caught it. ✅ **Nothing reached the car**, and ✅ **V158's shared-axis GATE 2 is substantially ADDRESSED**: the model demanded FactorE edits be sized against the PID schedule on the same axis, and **that schedule is FLAT at 99 counts**, so the coupling does not bite ⇒ **V158 is the lead build again.**

- ✅✅★★★★ [**V158 address-verified independently — and why it was immune to V159's error**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — after the off-by-0x400 that killed V159, the lead build's addresses were re-derived by **walking the pointer tables** (`0xC9E9C` -> records `0xD77BC/D0/E4`; `0xC9F84` -> `0xD77F8/80C/820`). **All six edits match their re-derived roles exactly** — `0xD77DA`/`0xD77EE` FactorC Y[0] 0->429/426, `0xD780E`/`0xD7822` FactorE X[0] 60->12, `0xD7818`/`0xD782C` FactorE Y[1] 140->539 — with an image diff of **14 bytes = 10 payload + 4 CRC, ZERO unattributed.** ⭐ **THE STRUCTURAL REASON IT WAS IMMUNE**: V159's addresses were **computed as `tp + offset`** (one wrong digit = a wrong cell, silently), while **V158's are READ as ABSOLUTE POINTERS out of the image** and then walked by a known layout => **no offset was ever added, so the off-by-0x400/0x1000 class cannot occur**, and a bad pointer would fall outside the image and be caught by the range filter. ⭐ **RULE: PREFER POINTER-DERIVED ADDRESSES OVER OFFSET-DERIVED ONES** — walk the in-image pointer array where one exists, because it is **self-validating**; reserve `tp+offset` for scalars with no pointer, and then **add it to `tp` explicitly and print both.** ⊕ This retrospectively explains why the **damper** work survived the audit and the **lane-gain** work did not: **the damper's records are pointer-reachable, the PID's lane gains are not.** ✅ **V158's standing: addresses verified · dose 50 inside the model's own [30,60] · monotone · 9.8 % of ceiling (10.2x margin) · RULE 7 read not assumed · shared-axis coupling shown not to bite** => **every gate the model raised is now addressed.**

- ✅✅★★★★★ [**The damper is FIVE tables, the record header is the KNOT COUNT, and V158 verifies from its own bytes**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00034350` decompiled 2026-08-28 carries **L1 `0xC9CCC` · L2 `0xC9E9C` · L3 `0xC9DB4` · L4 `0xC9F84` AND the OUTPUT CEILING `0xC77A0`** (2-knot, X[0]@+2 Y[0]@+6). ⭐ **THE RECORD'S FIRST HALFWORD IS THE KNOT COUNT** (2/4/4/5/4) — a **self-validating invariant**: any correct read must have `hdr == len(X)` with X strictly ascending, and **it would have caught V159's off-by-0x400 instantly.** Put it in every build script. ✅ The ceiling's **512** floor is now READ FROM THE IMAGE, not prose: `gp-0x6ac2` is a sign-gated kickback detector (0 in same-sign driving) so the LERP clamps flat to Y[0]=512, and the `>=0x32c9` bypass hits `0xC6158`=512 — **both paths agree.** ✅ **V158 re-verified by exact integer arithmetic on its shipped image**: dose **27 / 50 / 109** at rate **60 / 99 / 200** ct (stock **0** at every creep case) ⇒ **GENUINELY RATE-PROPORTIONAL, NOT A RELAY** — the substantive GATE 2 test — and **dose 50 at the measured operating point** is both the model's design target and the value V74 flew with 67.4 % engaged-creep liveness / 0 frames at the ceiling. Build-time rule `(FactorC×FactorE[3])>>10 ≤ 512`: **m26 388, m27 385, PASS**.

- ⚠★★★★ [**V158's FactorC arm is NOT monotone — and that is CORRECT; name the AXIS when applying a shape law**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V158 leaves FactorC `Y=[429,234,429,908]` (m26), which **dips between 35 and 60 km/h**. I certified it "MONOTONE"; **the wording was wrong, the build is right.** The golden model prescribes `FactorC Y[0] := Y[2]` **explicitly**, and separately records that `Y[0] := Y[1]` is *“the largest monotone lift of Y[0] alone”* ⇒ it knew it was exceeding the monotone limit. ⭐ **The shape law exists to stop FactorE being flattened across the RATE axis into a bang-bang relay** (V72's error — a limit-cycle generator at a lightly-damped resonance). **FactorC is SPEED-indexed and “costs NO rate-proportionality”**; vehicle speed varies over seconds and **cannot pump a 7.8 Hz ratchet.** ⇒ **“Monotone” is load-bearing on a FAST axis and cosmetic on a SLOW one** — asserting it unqualified nearly cost the lead build a false supersede. [[accord-v158-damper-golden-model-shape]]

- ⛔★★★ [**`0xCC914` is a DEAD table — and `movea` immediates cannot be scanned for table bases**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the pointer-table family is now CLOSED: **35 of 37 are loaded by exactly one `mov imm32` literal**, each located; **`0xCC214` and `0xCC914` have NO reference anywhere in the image** (no literal, no `movhi`+`movea` pair). `0xCC914`'s **double dead zone** (`Y=[0,0,512,2560]`, zero below ~40 km/h) looked like the perfect creep lever and **is not one — nothing reads it.** ⚠ **The “single reference `movea 0xC914` at `0x3938E`” was a FALSE POSITIVE**: the instruction is `movea 0xC914, r4, r30` with **r1 = r4 = gp**, i.e. **`gp - 0x36EC`**, a RAM cell all over `FUN_000389ec` — not a table base. **A raw byte scan for an immediate cannot distinguish a BASE REGISTER from a `movhi` partner**, and sign-extension makes `0xC914` read as `-0x36EC`. One `decompile_function` call settled it. [[feedback-decompile-first-then-assembly]]
