# Memory Index — Honda EPS Firmware Project

- [Run as a pure orchestrator; verify at the end](feedback-orchestrator-mode-delegate-verify-at-end.md) — operator wants the lead to delegate all digging to subagents and only open Ghidra/fine-details to finalize the picture or resolve a subagent dispute.
- [Damper variant ROW vs INDEX trap](reference-accord-damper-variant-row-vs-index-trap.md) — 3-stage indirection (EEPROM ID → ROW via 16-key match → INDEX via a second table → ptr array); this car's PN → row 2 → index 10 → the SAME tables V44/V47 edited, so their nulls likely are real.
- [Double-check subagent conclusions; never relay unverified](feedback-verify-subagent-conclusions.md) — in orchestrator mode, run a minimal independent confirmation of any decision-bearing subagent claim (tight script / paper decode / math check), even in the safe direction. Operator emphasized twice (2026-07-23/24); extends the older [[feedback_verify_subagent_claims]].
- [Iron rule: full-frame logging on every live CAN op](feedback_full_frame_logging.md) — log every TX + relevant RX (not summaries); working transport = Panda(disable_checks=True)+set_safety_mode(3) ELM327 bus 1 (ALLOUTPUT/set_obd don't reach the EPS)
- [Joey works style: rigorous validation before vehicle flash](feedback_rigorous_validation.md) — full byte diff > spot diff; ghidra before moving on; don't declare premature victory
- [Lightweight Python/PowerShell over token-heavy Ghidra MCP](feedback_lightweight_inspection_over_ghidra.md) — for byte/endianness/entropy/signature questions, script the raw .bin; reserve Ghidra for decompiler/xrefs
- [Build new RE tooling when the target needs it (operator-authorized)](feedback_build_re_tools_when_needed.md) — write new Ghidra loaders/processor specs/analyzers/disassemblers when kit tooling doesn't fit the target; don't stop at "tool unsupported"
- [Joey's framing: 3-node balance + process-not-degrees](feedback_3node_and_process_framing.md) — model+firmware+controller are ONE coupled system (don't single-node the hug); optimize good-driving-ness not proxy metric degrees (Goodhart)
- [Stress-test relayed LLM advice against the bytes before implementing](feedback_stress_test_llm_advice.md) — external AI = good idea generator, poor oracle for car-bricking specifics; build-then-adversarially-verify catches confident-wrong claims
- [★ Accord UDS RAM telemetry WORKING (V31U, DID 0x4801)](reference_accord_uds_did_read_surface_a160.md) — flashed 2026-07-10, live-validated. Req **0x18DA30F1**/resp 0x18DAF130 (NOT …80F1; A160 %hdr=30). RDBI table TRUE base **0xB77FC** (NOT 0xB7800), stride 0x14 [did/len/gate/session/group/handler_ptr]. LIVE per-DID dispatch = handler_ptr@entry+0x10 called w/ ctx ptr (FUN_000209ea, per-tick from w_steer_control_task); groupID jump-table 0xB7568 is orthogonal (masked scalar). DID 0x4801=idx1@0xB7810. Working patch = handler_ptr@**0xB7820**→cave 0xC4E00 + declared_len@0xB7812→10 (BOTH; old build off-by-one wired cave to 0x4800). Cave reads voter-MAX/AVG/|coltq|/angle (0xFEDF159E/15A2/3098/133C) as 4× LE u16. On-car: coltq+angle live, voters=0 (need LKAS). Next: log into rlogs via sunnypilot fork — `docs/HANDOFF-2026-07-10-v31u-uds-telemetry-working.md`.

- [Ground decompilation in the V850 CMSIS SVD](feedback_svd_grounding.md) — 2026-07-07: prefer the datasheet-authored SVD (analysis-2020accord/svd_for_ghidra/UPD70F3508_V850E2Px4.svd, chip µPD70F3508/V850E2) over bare peripheral addresses; cite register/field NAMES (e.g. 0xFF481000 = FCN0M0DAT0B; TX/RX dir = FCN0M{n}STRB.SSOW bit7). Settled the FCN0 0xFF489000-sub-block vs "channel B" dispute in the CAN-TX swarm. Companion to feedback_rigorous_validation.
- [.rwd output dir + one-script-per-version convention](feedback_rwd_output_dir.md) — build scripts write to ../accord-firmware/flashing-2020accord/rwd/ (external artifact root), not inside the repository; one build_vN_tva.py per lever family (V11=ceiling-raise, V12=gain)
- [Joey's lived driving experience overrides analyst recs](feedback_operator_lived_experience_overrides_analyst_recs.md) — when operator reports behavior, trust over abstract dwell-time concerns
- [Use short branch names for comma installer links](feedback_short_installer_branch_names.md) — he hand-types installer.comma.ai/<acct>/<branch> on the tiny comma 4 screen; alias to ~5-7 char branches (latlon/latonly)
- [Operator's real red-panda hardware topology](reference_operator_flash_hardware_topology.md) — red panda → comma Bosch harness → laptop (NOT OBD-II direct as docs say); reaches bus 1 where 0x221 lives
- [.rwd builders output beside the script, not D:](feedback_rwd_output_beside_script.md) — OUT_PATH relative to script dir; hardcoded D: path was machine-specific and diverged from reality
- [V850 TVA SA algorithm — universal Honda formula with Group C constants](reference_v850_sa_algorithm_tva.md) — verified at 0x00AAB4 via V850E2M datasheet; .rwd ! header is family-marker NOT SA secret on V850
- [Honda EPS SA-key secret is per-MCU-family not per-chassis](reference_honda_eps_sa_secret_per_mcu_family.md) — SH-2A = Group B, V850 = Group C; iHDS .rwd ! header is family marker
- [rizin/Ghidra V850E2 decode bugs (sld.hu disp + divq dst==src)](reference_rizin_ghidra_v850_quirks.md) — two compounding tooling bugs; suspect tools first when V850 analysis looks weird
- [Three senses of "rebuilt" — disambiguate round-trip vs canonical vs production](feedback_three_senses_of_rebuilt.md) — Joey caught conflation after stock TVA recon; recurring framing trap
- [Don't kill long agents early — SendMessage to refine, not TaskStop](feedback_dont_kill_long_agents_early.md) — Wave 2 agent landed answer right as I killed it; refine focus instead
- [Tight agent briefs beat broad — single-goal converges, 7-step gets lost](feedback_tight_agent_briefs.md) — validated multiple times; sequential single-goal chains work, comprehensive single agents flounder
- [2020 Accord SA-key chain solved 2026-05-23 — branch state](project_2020accord_sa_key_solved_2026_05_23.md) — algorithm verified, v2 .rwd built, flasher shipped; rayy's dry-run is the next concrete step
- [Accord data.bin — CORRECTED 2026-05-25: 32KB real flash @0x02000000, stored DOUBLED with 4-byte tag word per 4-byte data word; ⚠ tp(app)=0xBF000 NOT 0xF8000](reference_accord_databin_tp_base.md) — CORRECTED 2026-05-26 eve: bootloader sets tp=0xF8000 @0x9152 but the APPLICATION re-sets tp=0xBF000 @0x140ce; all tp+offset cal is at 0xBF000+off (PROGRAMMED 0xBF000-0xC6FFF), no absent partition. Real: gain 0xC646C=891, clamps 0xC61B2/B4=512. (data.bin: 32KB real flash @0x02000000, doubled-with-tags, de-tag via strip_data_tags.py)
- [Accord LKAS steering-torque path: CAN 0xE4 → mb54 → … → TSG20 PWM](reference_accord_lkas_torque_path.md) — both ends Ghidra-verified; CAN→control routing + exact torque setpoint are the 2 open gaps; flash/CRC layer ruled out
- [Accord demand aggregator: 10 producers → distributor FUN_00025c32 → mixer FUN_00026c80 → 0xFEDF1502 → shaper → serializer](reference_accord_demand_aggregator_pipeline.md) — steering is slot 1 of 10; full forward spine verified; SHARPENED GAP 2: principal cmd routes to a CSIG0 serial frame, no st to gp-0x2bf0/-0x2be0 q-ref → on-chip FOC handoff NOT proven
- [Accord arbitration limit is a FAMILY of curves + the x=4096 bottleneck chain](reference_accord_arbitration_limit_family.md) — FUN_00028ea6 clamps setpoint to ±LERP(curve[mode]); 8 mode-indexed ptr arrays 0xCB844.. → tables @0xE4xxx (cb844[0]=const 15360); waterfall x*-4→±0x4000(exact)→arb~15360→±0x2800→shaper±0x2000=8192; cb844 hard limit is mode/gear-INVARIANT but c9a88 varies by gear. ⚠ 2026-05-26: this waterfall MISSED the real LKAS binder — the arb OUTPUT GAIN tp+0x746c=0xC646C=891 (tp=0xBF000) yields only ~±418 at full cmd, capping LKAS below the ±0x2800/±0x2000 walls. 2x = gain 891→1782 + clamps 0xC61B2/B4 512→1024
- [⚠ CORRECTION OF RECORD: gp-0x4f60 is SENSOR-B DRIVER COLUMN TORQUE, not angular velocity / not vehicle speed](reference_accord_gp4f60_is_sensor_b_column_torque.md) — proven by CAN-399 packer FUN_00055c42 (STEER_TORQUE_SENSOR = -(gp-0x4f60 × 125/128)). Falsifies the Era-18/segmentD "[STRONG] angular velocity" label and the assist-lane "speed gate" reading. **Consequence: the gentle-EME debounce watches DRIVER hand torque, not the LKAS command** (gp-0x682f = min(|gp-0x4f60|>>5,255), r15 loaded @0x28f26, never rewritten through the store @0x29068) → the LKAS setpoint CANNOT provoke the gentle EME; V37 still works, only the "why" changes. Same correction was made 2026-07-07 and failed to propagate — this is now the single node of record
- [Accord LKAS setpoint ±15360 clamp = a DEGENERATE (flat) LERP; raising it is safe but needs a new CRC block](reference_accord_setpoint_limit_15360_lerp.md) — all 28 records across 5 banks have a FLAT 15360 Y row, so the axis (gp-0x6a5e = driver torque, a latent tuning surface shipped flattened) is irrelevant; 0xCB844 has exactly ONE code reader (0x28FCE); A160 → gp-0x674e=1 → live record 0xE41A8, Y row 0xE41BC..0xE41CC (9 halfwords). openpilot Accord torqueBP=4096 → setpoint 16384 > 15360, so top 6.25% of range is clipped; raise → **+6.71% top-end at EVERY build tier**. EME-safe (no float 15360.0 image-wide; lockstep casts the same int gp-0x6acc @0x4467a; gentle-EME channel is driver-fed). ✅ **BUILD BLOCKER RESOLVED + SHIPPED IN V38 (2026-07-18)**: added `(0xE4000,0xE4FFC)`+`(0xE5000,0xE5FFC)` to `TOUCHED_BLOCKS` — first build in this kit to touch a bootloader block outside the compact `0xC6xxx` cal block; the 49-block chain survives because the linked-list page fields at `block_start-8/-6` live in the **preceding** block, so only the trailers move. V38 patches **all 8 selector-reachable records** (sel {0,1,3,4,6,7,8,9}) rather than only the live sel-1 `0xE41A8`, making the raise immune to the unclosable HW-ID slot-provenance caveat
- [Accord base assist joins at the AGGREGATOR; V39 targets its direct Sensor-B torque-rate lane](reference_accord_base_assist_lane_architecture.md) — FUN_0003aa2c sums boost + 5 named siblings + inline r26/r24 + FUN_00036682 before the shared governor. r24 uses the 0xD30-produced, 0xC30-consumed four-sample Sensor-B derivative, can reach +/-8192, and V39 suppresses both signs at the exact V9 full-scale equivalent (`|lane|>=417`) with low driver torque; r26 stays live
- [Accord assist-curve family (gp+0x63fd) + CONFIDENT NEGATIVE: Sport mode is NOT in this EPS](reference_accord_assist_curve_family_sport_mode.md) — 34-entry array @0xCA154, two shapes (rising top-end 1238 / falling top-end ~440); our A160 runs **index 10** (falling). All 3 writers of gp+0x63fd traced: boot table / internal fault-failover / PasCom UDS — **none CAN-fed**; 21 decoded CAN IDs contain no drive-mode signal; and our row's 4 reachable columns {10,10,11,11} differ by ~1% so the table forecloses it anyway. ⚠ slot 0 (blank/no-match HW-ID) selects the RISING family = ~2.8x more assist — matters before swapping/re-IDing an EPS
- [★★ V31→V38 scaling audit corrected for assist-inclusive aggregate](reference_accord_v31_to_v38_scaling_audit.md) — V31's matched boost floor fixed the observed 2x soft EME and V38 is fault-free on-car, but the old 3584/4608 "max command" proof omitted assist lanes before the governor. Conservative gp-0x6acc envelope is 7322, so 4096/5120 does not statically dominate every combination; treat unknown lane values as replay inputs in the canonical model
- [★ FUN_00043e44 watchdog fault SM: 7 weighted flags, threshold 128, ~10-cycle debounce → DTC setter](reference_accord_watchdog_fault_sm_fun43e44.md) — 7 plausibility flags weights 1/2/4/8/16/32/64 (max sum **127**) vs threshold **128.0** — unreachable in one cycle BY DESIGN; the trip comes from a debounce SM (gp-0x3540/gp-0x3550, integrate 0.001/cyc to 0.01 ≈ **10 cycles ≈ 0.1s**, decay 0.0005) that adds **1024.0** → 1151 > 128. Trip → `FUN_000462e6(0x3f1b)` → **`FUN_00016de6(0x1D, 0x3f1b, 1, 1)`**. **RESOLVES a memory contradiction — `corridor_lockstep` RIGHT, `override_snap` WRONG (retracted in place)**: TWO monitors — M1 `FUN_00042af8`/`gp-0x3564` (int +10/cyc thr100) → `FUN_00016de6(0x1c,…)`, M2 `FUN_00043e44`/`gp-0x3550` (float +0.001/cyc thr0.01) → `FUN_00016de6(0x1d,…)`. **`gp-0x3564` traces to MOTOR-OFF** (`FUN_0001611e` bits0x41 → `FUN_00018738` → gp-0x685c=1 → gp-0x3ef8=1 → `FUN_00019f7c` → gp-0x67fa=8 → `FUN_00045608(3,0,0x8000,0x8000)` → gp-0x3ee8=1, **power-cycle to recover**) = the V25/V26 brick path. ⚠ **An agent-memory "Monitor 2 PERMANENTLY GATED OFF" verdict was an off-by-0x1000 misread**: gate is `tp+0x74a4` = **0xC64A4 = 0x00 = ENABLED** (trip REACHABLE), not 0xC74A4=0xEA. **These monitors are hard-shutdown capable, NOT advisory.** Whether index 0x1d is hard-fault-eligible (bits 0x41) is UNVERIFIED. ✅ **Closes the V38 setpoint-slew [OPEN]**: `FUN_00043e44` never reads the setpoint or the gain — it consumes gp-0x6acc as an INPUT and **every tolerance compares its own float re-derivation vs the int shaper's stored result**, so a clamp raise translates BOTH sides; plus the per-cycle slew is bounded upstream on gp-0x6ace by cals 0xC6206=512/0xC6208=205, independent of the clamp. ⚠ **POLARITY TRAP**: the ±8.0 compare on gp-0x6acc is a **sanitize-to-zero (out-of-range→0)**, NOT a gate excluding the command — an earlier draft had it backwards; check against the ±25.0 gp-0x4f60 reference @0x43eda. ⚠ Also: cal **0xC64DE is NOT a command ramp step** (all 18 reads in the arbitration/STEER_STATUS region) — the V18 "EME ramp" label is unsupported
- [Accord ECU-ID variant table @0xCD000 (16 × 0x24): layout, columns, and the HW-ID provenance caveat](reference_accord_ecu_id_variant_table.md) — 5-byte ASCII key at record **+0x00** (NOT +0x22); selector columns +0x12 (gp+0x63fd assist), +0x16/17/19 (gp-0x674c/f/d), +0x1A (gp-0x674e setpoint mode). A160 = slot 2 "TVAA1". ⚠ the selector is a **FETCHED BYTE, not the slot index** (mulhi 0x24 @0x4271e → ld.bu @0x42724 → st.b @0x4272a) — assuming otherwise turned a 9-halfword edit into a 144-halfword estimate. ⚠ the matching HW-ID at gp-0x6408..640C is **not in code.bin** (UDS service-0x84 written at manufacture) → slot identity closable only by a live read
- [Accord LKAS torque has a HARD ±0x3FFF (~2x) static-edit ceiling — 3x is a code rewrite](reference_accord_lkas_window_ceiling.md) — gate FUN_00042ac6 AND shaper FUN_00042af8 input-check (0x43ae8, TORQUE_MOD_V0.md MISSED it) both use +0x2800/-0x5001; widening needs 2nd imm=-(2W+1), W=0x4000 overflows imm16 → max ±0x3FFF; out-of-window → 0x7FFF sentinel → shaper cmovc 0x0 ZEROES it (V10A kill mode), NOT saturate; mixer LKAS lane pinned = 0x27442 block; runtime limiter *(gp-0x4f64)=0xFEDF309C may still bind; built V11A (~2x) not 3x. CORRECTS the "~3.99x value-edit" claim below. **⚠⚠ 2026-05-26 eve SUPERSEDED: the real binder is the arb OUTPUT GAIN tp+0x746c=0xC646C=891 — and tp(app)=0xBF000 NOT 0xF8000, so cal is PROGRAMMED, NO absent partition. Full-cmd output ≈±418 (below clamps 0xC61B4/0xC61B2=512). V11/V13's "absent-partition / gain=-1 / source the .rwd" framing is RETRACTED (wrong tp base). 2x = gain 0xC646C 891→1782 + clamps 0xC61B2/B4 512→1024 (flashable). See top of body.**
- [Accord torque-mod V0 → V33 BUILT (gentle-EME torque disengage DISABLED)](project_accord_torque_mod_v0.md) — ✅ 2026-07-02 (LATEST) V33 = build_v33_tva.py = V31 + disable the GENTLE-EME torque disengage (a DISTINCT mechanism from the soft-EME/corridor lineage: engage-SM FUN_00040d58 cuts LKAS when sensor-A voter gp-0x6a62 ≥ cal 0xC6312, stock 320): raise 0xC6312 320→65535 (u16 datatype max; ld.hu/unsigned + gp-0x6a62 clamp 32000 → never fires; invalid-sensor sentinel gp-0x6a62==0xffff kept). Cal-only, decider code byte-identical to stock, 49/49 CRC, V33-vs-V32 delta = only the 2-byte threshold + block CRC, UNFLASHED. Handoff docs/HANDOFF-2026-07-02-v33.md; radare2 v850.gnu-verified (r2 DEFAULT v850 plugin mis-decodes — use v850.gnu): exactly 3 readers @0x40db8/dd0/df4 (NOT 0x40dae/dc6/dea = gp-0x6a62 value reads), 0 writers, no twin. (prior) ✅ 2026-06-03 V31 BUILT (build_v31_tva.py = V30 + matched FLAT BOOST FLOOR 4096/4.0 int 0xC6768/6A/6C + float 0xC65C4/C8/CC) — fixes V30's residual soft EME on a hard sustained HANDS-OFF turn (V30 FLASHED, drove well, but the corridor it widened is the driver-override arm gated off hands-off; boost is the authority-gated arm that's ON at authority≈0 → floors the bound where the corridor can't → self-stable; [[reference-accord-soft-eme-bound-arm-gating]]); 49/49 CRC, 0 code edits, UNFLASHED, handoff docs/HANDOFF-2026-06-03-v31.md. (prior) V30 BUILT (corridor ×4=4096/float4.0, holds LKAS≤1024 + COMP ceiling 2560 → 3584<4096; ⚠ trade: integrator gp-0x3570 never winds up → corridor/SM soft-EME cutback inert, holds 2× under override; hard-EME lockstep intact; comp magnitude uncertain → right-size via gp-0x6ac0) + V29 (corridor ×2=2048, holds 2× LKAS only) — both build_vNN_tva.py, cal-only, 49/49 CRC, byte-verified 0 code edits, UNFLASHED. Handoff docs/HANDOFF-2026-06-03-v30.md. Re-tracing both monitors on STOCK code.bin myself overturned the V27/V28 models: the int wall is a THREE-WAY MAX max(dir_corridor[0x774e], velocity/cmd-envelope IIR[gp-0x3574, NOT driver torque], boost[0x7760=angular-rate]) × polarity; both monitors compare FLOAT twins gp-0x6db0/gp-0x6db8 ×1024 vs INT walls gp-0x6af6/gp-0x6b00 ±5 LSB. ⚠ Fixed two address errors: FLOAT corridor mirror Y = 0xC6598/9C/AC/B0 (NOT 0xC6590/0xC65A4 = X breakpoints, the V28-handoff's wrong V29 proposal); 0xC6664 = ENVELOPE LERP_B (V26's rest-fault) NOT corridor. V29 = V18 GAIN/clamps/ramp + INT corridor ×2 (0xC674E/50/5A/5C) + FLOAT corridor mirror ×2 (0xC6598/9C/AC/B0) + PN; NO trampoline/tolerance/0xC6664. Holds where V25/26/27 faulted: doubles ONLY the matched corridor arm on both sides → IIR/boost arms + stock ±5/1024 residual UNTOUCHED. Conf ~85%; road-test arbiter; if soft EME is the plausibility dropout not corridor overflow, corridor widen won't fix it. Live model [[reference-accord-corridor-lockstep]]; handoff docs/HANDOFF-2026-06-03-v29.md. (prior) V25 CLEAN = V18 GAIN + DIRECTION CORRIDOR ×2 (tp+0x7748/0x7754 Y ±1024→±2048); the shl/envelope thread DROPPED (IIR envelope watchdog-only; [[reference-accord-corridor-vs-envelope]]). 19 bytes/12 runs, 49/49 CRC. (prior) V0 plan Ghidra-verified 2026-05-25 → V11A (~2x LKAS ceiling-raise) BUILT + 49/49 CRC + clean byte-diff, UNFLASHED. CORRECTS the V0 doc: hard ceiling is ~2.0x (±0x3FFF), NOT 3.99x; shaper is a dual range-check that ZEROES overshoot (doc missed 0x43ae8); mixer LKAS lane = 0x27442 block; 2.5x/3x not value-edits. Runtime limiter 0xFEDF309C may still bind. **⚠⚠ 2026-05-26 eve: tp(app)=0xBF000 fix supersedes the whole absent-partition arc. Real high-end binder = arb output GAIN tp+0x746c=0xC646C=891 (PROGRAMMED). V11/V12/V13 missed it (edited erased 0xFF46C + downstream code clamps). V14 = gain 891→1782 + clamps 0xC61B2/B4 512→1024, three flashable .rwd cal edits.** ✅✅✅ 2026-05-26 RESOLVED: V14 FLASHED + ROAD-TESTED — IT WORKS (~2x LKAS torque at the wheel). First Accord torque-mod that delivers; closes the V11/V12/V13 saga. Confirms arb GAIN tp+0x746c=0xC646C 891→1782 + clamps tp+0x71b2/b4=0xC61B2/B4 512→1024 were the binder; LKAS is REQUEST-LIMITED (~418 stock→~835 V14) far below the 4762 governor, so the doubling reaches the motor uncut (Case A) — V15/governor 0xC6202 edit NOT needed for 2x. See [[reference-accord-lkas-delivery-and-governor]]
- [★ Vibration levers: FALSIFIED (cal lane-cuts) vs UNTESTED (gp-0x4f60 filter)](reference-accord-vibration-levers-falsified-vs-untested.md) — r24/r26/0xC644A(V43)/0xC6450(V46)/damping = validly falsified cal-only cuts; the gp-0x4f60 SIGNAL-FILTER is UNTESTED (V48B bricked catastrophically before testing efficacy; V50 never flashed) → filtering gp-0x4f60 late+narrow is the leading OPEN hypothesis. Four-frame telemetry finds the carrier lane first. ⚠ foc-backward-map mis-ranked 0xC6450 as "new" — it's V46-falsified.
- [★ Accord CAN-TX architecture + how to add a NEW TX ID](reference-accord-can-tx-architecture-new-id.md) — single FCN0; 18 slots via 0xB7208(route)/0xB721C(ID=entry>>18)/cadence 0xB7C9C; ALL broadcast IDs ride mailbox 6; FREE mailbox pool 7-32 (recommend 16); new ID = dedicated free mailbox + small cave. ★ Gateway verdict (lead-verified): the visible/absent split is a DOWNSTREAM gateway per-ID whitelist (the 4 absent IDs are actively fired at real rates, incl. 0x19F at 62.5 Hz = same as visible 399, yet dropped) → a new ID won't reach the comma's built-in panda; new-ID telemetry needs a red-panda tap directly on the EPS bus. Resolves the old can330-tx-rate + 2026-07-07 car-facing questions.
- [★ Accord CAN broadcast visibility = EXTERNAL gateway (single FCN0)](reference_accord_can_single_fcn0_external_gateway.md) — 2026-07-08 PROVEN: one controller FCN0/mailbox-6 for ALL frames (FCN1 never init'd, zero xrefs @0xFF4A0000); 0x660 & 399 leave on the SAME wire (routing tbl 0xB7208 all=6; emitter FUN_0001d68e; slot→ID 0xB721C) → the 399/427/0x14A-visible vs 0x660/0x19F/0x32E/0x64D-absent split is 100% the car gateway, NOT the EPS. On-car proof: TIER1's 0x660→100Hz rearm still ABSENT (scan analysis-2020accord/can-scans/2026-07-08-*). No EPS-side broadcast edit can be made comma-visible; whitelisted frames are full-DLC+checksum (no piggyback). Telemetry must ride UDS → [[reference-accord-uds-did-read-surface-a160]]
- [★ Accord UDS-over-CAN DID read surface — RAM telemetry insertion point](reference_accord_uds_did_read_surface_a160.md) — 2026-07-08: app UDS stack (req 0x18DA80F1 / resp 0x18DAF180, the flasher's) is comma-visible / crosses the gateway. Dispatcher FUN_0002075c; SID 0x22 RDBI → fixed descr idx9 (FUN_00021036, 1-5 DIDs) → FUN_00020f0a → RDBI DID table @0xB7800 (stride 0x14: +0x0C handler ptr, +0x10 u16 DID, +0x12 u16 len; +0x00=0xDF/+0x04=0x0F = default session/no-SA). DID 0xF181=idx25 (part string the flasher reads — DON'T touch). SID 0x23 ABSENT → PATCH required: repurpose/add a 0xB7800 DID → cave 0xC4E00 handler reusing RAM-read primitive FUN_0001CA0A (no SA); ABI = set *(u16)(ctx+0xc)=data_len+2; FUN_000211ba(); FUN_0002114e(abs_ptr,n) append; FUN_0002073a(). UDStelem .rwd repurposes DID 0x4801 (idx0). ⚠ Ghidra: 2 programs open → target code.bin. Spec docs/SPEC-uds-can-ram-telemetry-a160.md; bench tools/bench_uds_telem_read.py
- [★ Accord delivery + exact A160 motor-rate governor and split shaper inputs](reference_accord_lkas_delivery_and_governor.md) — gp-0x6acc drives the Q15 integrator; gp-0x6afe+r20 drives final gp-0x6b98 through the second governor. gp-0x4f64=MIN(4762,A160 Q13 table,budget); table=4607 at z=1318, while assist-inclusive gp-0x6acc can conservatively reach 7322. V39 leaves it unchanged
- [Accord sub-3mph LKAS cutoff is OPENPILOT-side, NOT a firmware pipeline gate](reference_accord_sub3mph_lkas_openpilot_gate.md) — 2026-07-17: a firmware trace found NO speed threshold in CAN-decode→arbitration→decider→engage-SM→mixer→shaper. 3 mph = openpilot STEER_GLOBAL_MIN_SPEED (3*MPH_TO_MS) AND Accord minSteerSpeed (3*MPH_TO_MS, values.py) → below max(those) controls runs latActive=False → STEER_REQUEST=0 (openpilot stops commanding). EPS also reports STEER_STATUS=LOW_SPEED_LOCKOUT near standstill (opendbc: "All Honda EPS cut off slightly above standstill"). OPEN: firmware LOW_SPEED_LOCKOUT producer NOT in the command pipeline (wheel-speed decoder unlocated; KFC_WHEEL_SPEED @0xB9BA4 dead-ends at a DTC table). RULE: don't re-hunt the LKAS firmware chain for a low-speed gate. See docs/HANDOFF-2026-07-17-lkas-model-firmware-verification.md
- [★ Accord corridor lockstep — wall=3-way max(corridor,IIR,boost); V30 FLASHED→V31 boost-floor BUILT](reference_accord_corridor_lockstep.md) — ★★★★ 2026-06-03: V30 (corridor ×4) FLASHED→drove well but residual soft EME on a hard sustained HANDS-OFF turn → V31 = V30 + matched flat BOOST FLOOR 4096 (the float twin's 3-way max INCLUDES boost, so matched int↔float is lockstep-clean); full per-arm gating model [[reference-accord-soft-eme-bound-arm-gating]]. (prior) ★★★ RE-TRACED myself (shaper FUN_00042af8 decompile + watchdog FUN_00043e44 disasm). V30 = corridor ×4 (4096/4.0) holds LKAS+COMP (handoff docs/HANDOFF-2026-06-03-v30.md); V29 = ×2 (2048) holds 2× LKAS. Arms: IIR gp-0x3574 = velocity/cmd-envelope (NOT driver torque), cap 0x3000=12288; boost = steering ANGULAR RATE gp-0x6ac2, out 0-2048. the int wall is a THREE-WAY MAX `gp-0x6af6 = max(dir_corridor[cal 0x774e, velocity-flat ±1024], velocity/cmd-envelope IIR[gp-0x3574 sar-8, NOT driver torque; input vel-vs-cmd contested], boost[cal 0x7760 X700/800/1100 Y0/1536/2048]) × polarity`. BOTH monitors compare FLOAT twins gp-0x6db0/gp-0x6db8 ×1024 vs INT walls gp-0x6af6/gp-0x6b00, ±5 LSB → ≥128.0 → 0x3f1b DTC 0xF00049. ⚠ TWO prior ADDRESS errors fixed: (a) the FLOAT corridor mirror Y = **0xC6598/0xC659C(dir1,+1.0)/0xC65AC/0xC65B0(dir2,−1.0)**, NOT 0xC6590/0xC65A4 (those are X velocity/torque BREAKPOINTS — the V28-handoff's V29 proposal named them = V26-class wrong-table brick); (b) 0xC6664 = ENVELOPE LERP_B (feeds gp-0x6da8 + envelope monitor gp-0x6c84, V26's rest-fault), NOT the corridor. Traced r11=LERP(0xC6590)/r7=LERP(0xC65A4) feed ONLY the float twin's corridor arm (gated off at rest, not stored elsewhere). **V29 = build_v29_tva.py BUILT (cal-only):** V18 GAIN/clamps/ramp (real 2×, GAIN 0xC646C monitor-INDEPENDENT, flashed+validated) + INT corridor ×2 (0xC674E/50/5A/5C) + FLOAT corridor mirror ×2 (0xC6598/9C/AC/B0) + PN; NO trampoline/tolerance/0xC6664. Holds where V25/26/27 faulted: doubles ONLY the matched corridor arm on BOTH sides → IIR/boost arms + the stock ±5/1024 residual UNTOUCHED (V27 doubled the whole twin → 2× residual → fault). 49/49 CRC, 27B/16 runs, 0 executable code edits (byte-diffed vs stock + re-decoded .rwd). Conf ~85%; road-test arbiter; if the soft EME is the driver-override PLAUSIBILITY dropout not corridor overflow, the corridor widen won't fix it. Output …/39990-TVA,A160-V29-…rwd + _v29_plain_image.bin; handoff docs/HANDOFF-2026-06-03-v29.md. (prior, FALSIFIED) the V28 model claimed wall=max(driver-torque,corridor) & proposed doubling float 0xC6590/0xC65A4 — both wrong per the 3-way-max retrace; V28 (trampoline + tolerance widen) DO NOT FLASH.
- [★ Accord soft-EME bound = gated 3-way max/min; corridor=driver-override arm (off hands-off); V31=boost floor](reference_accord_soft_eme_bound_arm_gating.md) — 2026-06-03: V30 FLASHED→drove well but residual soft EME on ONE hard SUSTAINED HANDS-OFF turn. Walked FUN_00042af8 myself: integrator gp-0x3570 winds up on (command − bound), bound = MAX/MIN(corridor, IIR gp-0x3574, boost). EACH ARM GATED: corridor (cal 0xC674E, the driver-override arm) off when |gp-0x6bf0 driver-assist|≤9216 (cal 0xC6156, hands-off) AND when authority r13≠0; boost (cal 0xC6760/float 0xC65B8, |angular rate|) latched 0 by SM gp-0x3562 when authority>0xC641E=16384 for ~20cyc; IIR decays when column velocity≈0. **r13 = gp-0x6966 = (|gp-0x3570>>15|×1092[0xC61DA])>>10 = AUTHORITY** (gates corridor@0x43114 AND the boost SM). Hands-off held turn → all 3 collapse → 2× cmd winds up → SM2/SM3 cut = the V30 EME (V30 widened the corridor, gated off there). V31 = build_v31_tva.py = V30 + matched flat BOOST FLOOR 4096 (int 0xC6768/6A/6C, float 0xC65C4/C8/CC=4.0, ÷1024, lockstep-safe — float twin includes boost); boost is authority-gated so it's ON at authority≈0 → bound≥4096>cmd3584 → no wind-up → self-stable fixpoint → boost-SM never fires. 49/49 CRC, 0 code edits, UNFLASHED. Resolves IIR identity=column velocity. handoff docs/HANDOFF-2026-06-03-v31.md
- [★ Accord EME: corridor vs envelope — 3-lever model ⚠CORRECTED by corridor-lockstep (V26)](reference_accord_corridor_vs_envelope.md) — 2026-06-02: THREE distinct 2× levers — GAIN tp+0x746c (real torque), shl IIR envelope gp-0x3574→gp-0x6af6 (WATCHDOG-ONLY: gates neither delivered torque nor EME; delivered gp-0x6b98=clamp(min(lanes,governor gp-0x4f64),±0x2000)), DIRECTION CORRIDOR tp+0x7748/0x7754 (soft-EME integrator ref). Two faults: SOFT EME (no DTC) = command exits corridor → integrator gp-0x3570 → SM2/SM3; HARD fault DTC 0xF00049 = consistency-monitor desync SELF-INFLICTED by the shl (V18 never hard-faulted). V25 = GAIN + corridor ×2; shl/B thread dropped. MODE 0 (tp+0x74c8=0) ≠ variant mode gp-0x674e=1.
- [⚠ Accord 2× EME — driver-override / torque-sensor plausibility dropout on sharp low-speed turns](reference_accord_driver_override_plausibility_eme.md) — SAFETY: V14/V15 2× build, op-engaged, driver adds hand torque → LKAS snaps to zero, ~10s jerky recovery. Mechanism Ghidra-verified: 2× arb gain (0xC646C, after the integrator) amplifies a pre-existing driver-override inhibition. Driver torque enters via column torque sensor (5 ADC ch gp-0x6a44/40/3c/38/46) → voter FUN_00041eec → fused gp-0x6a5e(0xFEDF15A2) + flag gp-0x67f4(0xFEDF180C); ENABLE byte 0xFEDF195C∈{2,3}. DO NOT widen the plausibility threshold (real sensor fault detector). **✅ CORRECTED 2026-05-27 (4-analyst Ghidra review + road test): the V16 slew-fix is REJECTED — slew 0xC61D6=0 FREEZES a dormant 2D shaping lane (NOT a disabled damper), 0→14 ACTIVATES an uncalibrated map; V17 deadband-only is INERT (slew=0 pins gp-0x356c at 0); the real cut node is the override SM gp-0x6960, NOT the deadband. V18 = 2× + ramp-only (0xC64DE 17→27, cal-only, NO trampoline) is FLASHED + drives well. Lever semantics: [[reference-accord-eme-lever-semantics]].**
- [★ Accord pointer-base audit — every tp/gp register write; app tp=0xBF000 via THREE instrs](reference_accord_pointer_base_audit.md) — program-wide (185K instr): app builds tp via movhi 0xb @0x140ce + movea 0x7000 + **add r1(0x8000) @0x140d6** → 0xBF000 (the missed 3rd instr = the address-error source); bootloader tp=0xF8000; gp=0xFEDF8000 both. Confirms every 0xC6xxx cal addr. Resolve tp at the app site, not movhi/movea pair.
- [★ Accord V18 (2× + ramp-only) FLASHED + ROAD-VALIDATED — V16/V17 dismantled](project_accord_torque_mod_v0.md) — V18 = V15B 2× (gain 0xC646C=1782, clamps 0xC61B2/B4=1024) + ramp 0xC64DE 17→27 ONLY (deadband stock, slew stock); calibration-only, decode-verified 15-byte diff, NO code patch / NO trampoline; drives well (operator). A 4-analyst review (../assessment/) REJECTED V16 (slew activates a dormant 2D map) and found V17 deadband-only INERT. Full lever semantics: [[reference-accord-eme-lever-semantics]]. ⊕ 2026-05-30 V19 = V18 + SM2/SM3 override-gate proportional rescale (0xC6422 16384→32768 + 0xC61DC 30720→61440) for HIGH-END 2× that survives hard override without the EME snap; BUILT, 49/49 CRC, UNFLASHED.
- [Accord EME lever semantics (slew/deadband/ramp/override-SM), disasm-verified](reference_accord_eme_lever_semantics.md) — slew 0xC61D6 = step of a limiter on dormant state gp-0x356c (freeze≠passthrough; 0→14 activates 2D map 0xC6770×0xC69E8); deadband 0xC6424 gates ONLY that limiter (inert at slew=0); real cut = override SM gp-0x6960; ramp 0xC64DE = re-engage ceiling (17→27 LENGTHENS, targets recovery not snap); NO output rate-limiter as cal (would need a trampoline code patch — scoped, never built)
- [Accord V22 — float-monitor 2× via code cave + gp-0x3578 negative-envelope 2×](reference_accord_v22_float_monitor_2x_cave.md) — V22 = V21 + (1) gp-0x3578 shl 0x8→0x9 @0x42F16 (negative-envelope twin of V21's gp-0x3574) + (2)/(3) float watchdog FUN_00043e44 upper_raw(r10)/lower_raw(r12) ×2 via minimal redirect @0x44230 (jr 0xC4FC0) into a 20-byte code cave in free 0xFF padding @0xC4FC0; needed because the float monitor cross-checks the integer shaper's doubled outputs (gp-0x6af6/6b00/6b0a/6b98) and would else throw CAN fault 0x3f1b. FIRST Accord build with a code cave. BUILT, 49/49 CRC, Ghidra-verified on built image, UNFLASHED. Verified jr/mulf.s/movhi V850 encodings inside.
- [★ Accord EME snap = THREE redundant authority-gate SMs (FUN_00042af8), arming gate 0xC6422=16384=50% Q15](reference_accord_override_snap_state_machines.md) — VERIFIED 2026-05-29 (3 tracer subagents, instruction-level): SM1/2/3 (nodes gp-0x6960/6962/6787) OR-linked by 3-way MIN @0x439c0; demand=blend×min>>15 @0x43a3a; node=0 bypasses slew → INSTANT snap, recovery ramps via sp+0x38 (~10s). Arms when demand > tp+0x7422=0xC6422=16384 (stock LKAS ~15360 sits just below → EME is 2×-ONLY). Fight = cmd OPPOSES gp-0x6af8 (=gated gp-0x4f60). SM is LOCAL (zero external readers) + sets NO DTC, but broadcasts trip to another core via CSIG msg 0x2a every cycle. Dual-path int/float monitor (gp-0x3564, fault 0x3f1b) is REPORT-ONLY. High-end 2× ⟺ EME (same lever); safe 2× = setpoint-gain under the gate. Non-verified companions + open traces: analysis-2020accord/EME_OVERRIDE_SM_NONVERIFIED.md. **⊕ 2026-05-30 Trace A COMPLETE: SM1 arm 0xC61DE=2048 + SM3 arm 0xC61DC=30720 PINNED; ALL 3 SMs COMMAND-driven (integrator gp-0x3570), NOT velocity (a swarm agent's velocity-seed claim was a misread, corrected by direct read); gp-0x4f60=angular velocity [STRONG]; V19 high-end-2× BUILT = 0xC6422→0x8000 + 0xC61DC→0xF000, 49/49 CRC, UNFLASHED → [[project-accord-torque-mod-v0]].**
- [TVA bootloader integrity = CRC-32 linked-list walk (0xFF01)](reference_tva_bootloader_crc_scheme.md) — solved 0xFCB8212C = crc32 over [0xFD000,0xFFFFC); 49 blocks; the pre-flash gate (V9b passed it + flashed OK); verify_bootloader_crc.py
- [TVA flash cipher operand order — CONFIRMED](reference_tva_cipher_operand_order.md) — on-ECU decryptor FUN_0xB35E = ((c^0xBF)^0x10)-0x9E [xor,xor,sub], proven by V9b flash; v3-v8's ((c-0xBF)+0x10)^0x9E → garbage → 0x72; round-trip self-check can't catch a wrong cipher
- [2020 Accord V9 cipher fix — SOLVED 2026-05-24](project_2020accord_v9_cipher_fix_2026_05_24.md) — V9b flashed SUCCESSFULLY; cipher+window+CRC recipe confirmed; V9a retired; recipe in analysis-2020accord/HOW_TO_BUILD_ACCORD_TVA_RWD.md
- [Accord gp base = 0xFEDF8000](reference_accord_gp_base_fedf8000.md) — master key: abs = 0xFEDF8000 - gp_offset; status bitfield 0xFEDF1288, LKAS staging 0xFEDF68CC; cross-checked twice; gp-relative still not xref-able. ⚠ companion tp(app)=0xBF000 (NOT 0xF8000); tp+off cal at 0xBF000+off is programmed
- [Dream — CAN topology method as cross-platform methodology](dream_can_topology_method_generalization.md) — vfn's opendbc + mailbox + RAM-trace method generalizes to Accord/CR-V/Pilot/Clarity; durable artifact is the methodology, not just C120's specialist-7 application
- [Accord LERP envelope gated by DRIVER ASSIST — inactive during hands-off LKAS](reference_accord_lerp_envelope_gating.md) — T1/T2 both zero when |driver_assist gp-0x6bf0| < 9216; ±1024 plateau only active on driver override; explains zero stock 1x EMEs; left/right velocity asymmetry OPEN
- [★ Honda Bosch radar + longitudinal architecture (openpilot side)](reference_honda_bosch_radar_longitudinal_arch.md) — Civic Bosch + Accord are Bosch-WITH-radar (NOT radarless); OP never reads stock radar (radarUnavailable=True all Bosch); OP-long gated behind alpha_long (default OFF=stock ACC); enabling OP-long disables stock radar via UDS 0x18DAB0F1 ⇒ DISABLES AEB. Bus0=radar side (addon radar joins here), bus1=powertrain (ACC_CONTROL accel out). Firmware-level AEB-coexistence = OPEN (Ghidra 36802), not proven impossible. For the addon-radar spec.

## 2026-05-30 session — lateral PID tuning / fleet data / auto-tune surrogate

- [Talk straight — no hedge-armor](feedback_direct_no_hedging.md) — Joey wants direct, peer-level communication; drop caveat-stacking; he's comfortable with uncertainty, reads hedging as friction
- [Verify load-bearing sub-agent claims — don't relay](feedback_verify_subagent_claims.md) — re-derive before car-affecting/durable acts; sub-agents were confidently WRONG twice this session (false "route-07 degenerate"; wrong-branch gas-ceiling inversion), caught by direct reads

## 2026-06-02/03 session — UDS access surface / SA cipher families / engineering sessions / read-only telemetry (omega horde-of-hordes)

Detail of record lives in `.claude/agent-memory/firmware-codepath-tracer/reference_*` (the tracer's own index). These are the load-bearing facts:


## 2026-06-03 session — EPS observability stub / RDX recovery fork / radar dump posture (multi-horde)


## 2026-06-04 session — radar SIGNAL PRESERVATION + EPS stub v8 QA + no-flash diag read-primitive hunt (3 hordes)

Detail of record: `docs/SESSION-SYNTHESIS-2026-06-04.md`. Trust the bytes/live probe over the narrative (operator: prior radar memories are a false-start chain).

- Corrections of record: radar chip = file+**0x60000** (not +0x30000) [⚠ REVERSED 2026-06-05: canonical is +**0x30000** — the +0x60000 was a Ghidra import-base artifact; see the 2026-06-05 section]; radar **DSP island = e200z7 PowerPC** (app core still ARM Cortex-M+Helium/MVE); M_CAN base **0x44010000 REFUTED** (BE index 0x0144; real bases 0x4005xxxx); packer hook-site 0x10607C **UNCONFIRMED** (MVE wall — needs llvm-objdump --mve or live vtable read).

## 2026-06-05 session — radar recovery + 0x211FF000 stack autopsy + re-patch kit

A fine-range packer-hook patch broke comms (wrote the CPU stack); the stock revert did NOT take (NRC uncaptured). Recoverable, not bricked. Detail: `docs/HANDOFF-2026-06-05-radar-recovery.md`. ⚠ Addressing CORRECTED: chip = file + **0x30000** is canonical (the 2026-06-04 +0x60000 was a Ghidra import-base artifact).

## 2026-07-21 session — V46 (lever A) falsified, V47 dampers-only, whole-system 21 Hz audit

- [project_v46_falsified_v47_dampers_only](project_v46_falsified_v47_dampers_only.md) — V46 (Stage A carrier low-pass) FLASHED, no effect → lever A falsified; V47 = ratchet + dampers-only (Factor C + Factor E aggressive), BUILT + verified, UNFLASHED. Handoff `docs/HANDOFF-2026-07-21-v46-v47-vibration.md`.
- [reference-accord-damper-two-deadzones-factorC-factorE](reference-accord-damper-two-deadzones-factorC-factorE.md) — the damper is a 5-factor product with TWO hands-off deadzones (Factor C driver-torque AND Factor E motor-rate); V44 opened only C → failed. Output clamp dynamic ±512..1024, not ±2048.
- [reference-accord-no-vehicle-speed-input-5mph-is-plant](reference-accord-no-vehicle-speed-input-5mph-is-plant.md) — NO vehicle-speed input anywhere in the command/base-assist path; only motor-rate adaptation. "5 mph" = openpilot minEnableSpeed=3 + plant physics, not a firmware gate.
- [reference-accord-dualpinion-arch-one-torsion-sensor](reference-accord-dualpinion-arch-one-torsion-sensor.md) — dual-pinion EPS (motor off-axis on a 2nd rack pinion); Sensor A/B = MAIN/SUB channels of ONE torsion-bar sensor (DTC C1420); 21 Hz = rack-coupled driveline mode; resolver for motor angle; OP minEnableSpeed=3.
- [reference-accord-governor-g1-total-command-not-thermal](reference-accord-governor-g1-total-command-not-thermal.md) — G1 (FUN_0004503c) clamps the TOTAL command (not LKAS-only); "energy budget" is NOT a thermal integrator (structurally unreachable) → relabel motor-rate-adaptive total-command ceiling; doesn't bind at resonance amplitude.
- [reference-accord-damping-clamp-dtc1d-trap](reference-accord-damping-clamp-dtc1d-trap.md) — damping clamp bound (int 0xD209C/0xD20A8) has a float mirror (0xC6554/58/5C/60) with a NO-DEBOUNCE DTC-0x1d hard shutdown; never edit int clamp without a bit-exact float edit. V47 leaves it stock.

## 2026-07-21 LATE session — loop-gain characterization, collocation keystone, V48A (failed), V48B notch

★ Master reference for the vibration is now `docs/VIBRATION-DOSSIER.md`. Handoff: `docs/HANDOFF-2026-07-21-v48-vibration-loopgain-notch.md`.

- [reference-accord-collocation-motor-rate-damper-dead](reference-accord-collocation-motor-rate-damper-dead.md) — ★ KEYSTONE: the 21 Hz vibration is a two-inertia torsional mode; the firmware's MOTOR-RESOLVER-RATE damper is NON-COLLOCATED with the wheel-side mode → cannot damp it at any gain (V44/V47 nulls are that theorem). The hand cures it because a grip is collocated at the antinode. STOP tuning the motor-rate damper. Loop-gain model: bare-plant Q≈1.7, 4× drives ~0° anti-damping → Q=13.6, |L|=0.875, 1.16 dB margin, onset ~3×. `analysis-2020accord/eps_loop_gain_model.py`.
- [project-v48-loopgain-v48a-failed-notch-next](project-v48-loopgain-v48a-failed-notch-next.md) — V48A (cal-only: mute type-8 `0xC4120` + `FUN_0003a382` `uVar27`→256) BUILT+VERIFIED+FLASHED → did NOT fix the vibration ⇒ anti-damping is DISTRIBUTED ⇒ the 21.4 Hz notch V48B is the remaining lever. **V48B is now BUILT** (see below). Route B ("4× via setpoint") is hygiene-only (ΔL=0).
- [reference-accord-v48b-notch-cave-build](reference-accord-v48b-notch-cave-build.md) — ⚠ **V48B WAS FLASHED → BRICKED (see the CATASTROPHIC entry in the next section).** 138-byte/41-instr DF-I Q12 biquad code cave @`0xC4B34` (coeffs `b0=4045 b1=-7949 b2=3977 a1=-7949 a2=3926`, unity at DC) filters a copy of `gp-0x4f60`→new RAM `gp-0x1500`; `jr` trampoline @`0x7FEAC` (return `0x7FEB0`, re-exec cmp/mov last for flags); 7 live carrier repoints `gp-0x4f60`→`gp-0x1500` (`FUN_0002c478/000352b4/0003a382/0003b49a/0003b66a`); 2 dormant reads (`0x34392/0x34ace`) left raw. RAM x1/x2/y2=`gp-0x14FC/FA/F8`. 50/50 CRC + RWD round-trip + every edit re-disassembled in Ghidra. Adversarial review: all monitor-asymmetry (V27) items CLOSED SAFE incl. the DTC-0x1c/0x1d matched int/float lockstep. `build_v48b_tva.py`+`v48b_cave_asm.py`+`eps_v48b_cave_model.py`; handoff `docs/HANDOFF-2026-07-21-v48b-notch-build.md`. ⚠ CODE CAVE (kit's only bricked class V24/V27) — flash only on explicit operator instruction.
- [reference-accord-gp4f60-notch-filter-feasibility-v48b](../../.claude/agent-memory/firmware-codepath-tracer/reference_accord_gp4f60_notch_filter_feasibility_v48b.md) — (agent memory) gp-0x4f60 is shadow-lockstep (fault 0x17) + feeds 2 no-debounce hard-shutdown monitors + 2 CAN → notch must be a FILTERED COPY (new RAM), not source-filter; repoint the carrier reads; cave 0xC4B34.

## 2026-07-21 — 🛑 V48B FLASHED → CATASTROPHIC (root-caused; two mandatory cave gates added)

- [reference-accord-v48b-flashed-catastrophic-ram-collision](reference-accord-v48b-flashed-catastrophic-ram-collision.md) — 🛑 V48B notch cave FLASHED → full-authority wheel oscillation on startup, parked, NO LKAS (recovered by reflash; no hardware damage). Kit's THIRD cave brick (V24/V27/V48B). Root cause: (1) RAM COLLISION — biquad x2 cell `gp-0x14FA` high byte `0xFEDF6B07` aliases a live monitor/DTC status bitfield (readers `FUN_00051fbc`/`FUN_00053f32` case 8), b2≈0.97 near-unity → external write injects full-scale → clamp ±25600 → motor slams; cave also stomps the monitor byte 1000×/s; writer unlocated but aliasing confirmed; `gp-0x14FA` in poison region `gp-0x1401..0x1502`, safe alt `gp-0x14E0`. (2) lightly-damped resonator (r=0.979, Q≈3.2) dropped into the always-on base-assist loop (no LKAS/speed gate) with NO closed-loop stability check — only `|N(21.4)|` inserted into the LKAS loop-gain model. (3) clock rate EXONERATED (undivided 1 kHz, correct). Handoff `docs/HANDOFF-2026-07-21-v48b-flashed-catastrophic.md`.
- [feedback-cave-two-gates-ram-ownership-and-closed-loop](feedback-cave-two-gates-ram-ownership-and-closed-loop.md) — ★ HARD RULE (apply without being asked): before ANY code cave / filter / dynamics change is flash-ready it MUST pass GATE 1 (RAM ownership — every byte's full footprint proven free incl. writers + register-indirect/6-byte-disp; `gp-0x1401..0x1502` poison) AND GATE 2 (closed-loop stability — magnitude+phase/Nyquist of EVERY loop the signal is in, esp. the always-on base-assist loop, element inserted; not a single-frequency magnitude). Byte/CRC/disassembly/open-loop-DSP verification is necessary but NOT sufficient — V48B passed all of them and bricked.

## 2026-07-22 session — 21 Hz re-audit: comma is a PASSENGER, measurement corrected, V49B StageC-flip is the lead

- [feedback-default-maximal-thoroughness](feedback-default-maximal-thoroughness.md) — ★ standing operator instruction: DEFAULT to maximal thoroughness without checkpointing for permission on analysis/prep — run every thread, close every gate, verify subagent work byte-level, build the ready UNFLASHED candidate, keep records current. The flash/CAN/UDS gates are the ONLY hard stop and are NOT relaxed by this.
- [reference-accord-v49-stagec-flip-collocated-damper](reference-accord-v49-stagec-flip-collocated-damper.md) — ★★ the 21 Hz vibration is a FIRMWARE/PLANT loop: the openpilot bus command (0xE4) STRIPS 21.5 Hz via its slew-saturated rate limiter (52% of steps at ±123 cap) → comma is a PASSENGER, no Kp/LPF fix helps. Measurement corrected (broad shelf not Q=13.6; 21.5-vs-78.6 aliasing UNRESOLVED). Topology COMPLETE (gp-0x6ade dead, gp-0x6b62 new+slow). **V49 = ratchet + collocated damper via FUN_0003a382 StageC sign-flip (subr→sub @0x3a836) + band-limit (pole 0xC644A 1024→64); GATE-2 required the band-limit (bare flip creates HF anti-damping).** BUILT+VERIFIED, UNFLASHED — 🛑 GATED on polarity gp-0x6752 (0xFEDF18AE) = +1 (brick if −1; EEPROM-resident → needs live read). Models: `eps_v49_a382_stagec_flip_model.py` + `reanalyze_b9_vibration.py`.

## 2026-07-22 session (later) — FOC hypothesis closed; V50 = EMA low-pass cave, BUILT

- [feedback-account-for-prior-iterations-before-new-build](feedback-account-for-prior-iterations-before-new-build.md) — ★ standing operator instruction: before any new RWD version, internalize EVERY prior iteration (what was flashed + falsified) and don't re-propose a lever already falsified on-car. Pairs with maximal-thoroughness + the two cave gates.
- [🛑 gp-0x1500 is a LIVE I/O-mailbox slot — the 0xb7260 array](reference-accord-b7260-io-mailbox-array.md) — the "poison region gp-0x1401..0x1502" is a subset of a 40-slot×8B I/O registry at 0xb7260 (0xFEDF6AE0..0xFEDF6C18); gp-0x1500 (slot 5) + gp-0x14E0 (slot 9) are LIVE, written via table-dispatched pointers. Proven on-car by the V50P probe (gp-0x1500 nonzero 99.47% of the drive) ⇒ **V50 is NO-FLASH**. Static clearance failed 3/8 addresses (gp-0x1500/0x14E0/0x1700) → probe ANY candidate cell live. Replacement candidates: gp-0x1300+gp-0x1100 (roomy corridor).
- [reference-accord-v50-lowpass-ema-cave](reference-accord-v50-lowpass-ema-cave.md) — 🛑 **NO-FLASH — GATE-1 failed on-car (gp-0x1500 has a live writer; see the 0xb7260 mailbox memory above).** ★★ **V50 = V38 + ratchet + a FIRST-ORDER EMA LOW-PASS (fc~12 Hz, alpha=74/1024) on gp-0x4f60 (code cave), keeps 4x. BUILT + VERIFIED (50/50 CRC, RWD round-trip, all encoders Ghidra-verified), UNFLASHED.** Chosen over V48B's notch: the mode is SPEED-DEPENDENT (~21.7 Hz low-speed → ~8-12 Hz highway; fresh manual-drive data) + alias-unresolved → a low-pass covers the whole band and rolls off 78.6 Hz harder (−16 vs −6 dB). Single 16-bit cell gp-0x1500 (V48B-flash-proven) removes V48B's 4-cell RAM-collision failure mode; deadband is a FEATURE (fully suppresses small ripple, over-attenuates moderate). Polarity-INDEPENDENT. **FOC current-loop hypothesis RULED OUT** (no isolable PI; model-based coeffs in the risky 0xC5000 block; the 8 kHz loop is the actuator, not the source). GATE 2 closed (`eps_v50_gate2_lowpass.py`); GATE 1 = gp-0x1500 best-evidenced but 2 pre-flash gates OPEN (built-image Ghidra re-disasm + the 0xbb640/0xb7260 table-walker register-indirect check). Also: the route the operator gave was NOT a V49P drive → polarity gate still unresolved (moot for V50). Build: `build_v50_tva.py` + `v50_cave_asm.py`.


## 2026-07-24 session (later) — V51P GATE-1 PASS (both cells clean); V52 built-but-incomplete; gp-0x4f60 = 19-carrier surface

- [reference-accord-v51p-gate1-both-cells-clean](reference-accord-v51p-gate1-both-cells-clean.md) — V51P flashed+driven (rlog 7): BOTH gp-0x1300 and gp-0x1100 read 0/24000 (beacon 100%, 2 independent decoders, stock null distinguishable) → definitive live-probe RAM clearance that gp-0x1500 failed. V52 uses gp-0x1300. Clears the CELL, not the filter's completeness.
- [reference-accord-gp4f60-carrier-surface](reference-accord-gp4f60-carrier-surface.md) — byte-scan: gp-0x4f60 has 64 raw readers (not ~12); ~19 command-path CARRIERS across the 1kHz control task AND the ~100Hz assist task; V52-as-built repoints only 10 (misses 9, incl. 3 self-filtering + 2 mode-gated) → INCOMPLETE + fragile + GATE-2-invalidating. NO monitor hazards (M1/M2/gate all compare raw vs literal constants → risk is feel, not brick). ⇒ empirical case for FFT-narrow (FOURFRAME) over the broad source-filter.

## 2026-07-24 session (third) — V52C: ALL 19 carriers filtered; every pre-flash gate PASSED

- [reference-accord-v52c-complete-broad-lowpass](reference-accord-v52c-complete-broad-lowpass.md) — **V52C BUILT + ALL GATES PASSED, UNFLASHED** (SHA `af01c8bd…`, 132B/24 runs, 50/50 CRC). ALL 19 gp-0x4f60 command-path carriers repointed to gp-0x1300. **Operator directive + governing principle: a MIXED raw/filtered population is ITSELF the hazard — that is how V27 bricked (ASYMMETRY, not magnitude).** GATE-1 closed FIVE ways (method reproduces V50's + V48B's failures as controls); GATE-2 edge 4.66×→**21.19×**, monotonic; no monitor asymmetry (3 shadow pairs all input-invariant); key-on transient can only SHRINK (RAM clears on the flash's power cycle). ⚠ gp-0x4f60 is SHADOWED (gp-0x4486 → fault 0x17 → hard motor-off) — filter a COPY, never the source.
- ⚠ **[reference-accord-gp4f60-carrier-surface](reference-accord-gp4f60-carrier-surface.md) is now PARTLY SUPERSEDED** — its "broad and fragile ⇒ don't filter at the source" verdict is REVERSED, its "3 self-filtering lanes" claim was WRONG on all three (`0x3B908`'s biquad is DEGENERATE in stock cal → ~366 Hz passthrough; `0x36846` isn't a filter at all), and its 64-reader count was an undercount. Header rewritten in place.
- [accord-dtc-0x18-hard-eligible-cadence-watchdog](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-dtc-0x18-hard-eligible-cadence-watchdog.md) *(user-store)* — **DTC 0x18 IS hard-fault eligible** (record 0xB7FDC = 0x3D01, same as monitors 0x1C/0x1D → motor-off); it is the per-task cadence/overrun watchdog, so ANY cave on the 1 kHz path has a timing budget (V52C's = ~0.06% of the tick). An agent called it benign off a 2-byte-slipped record address.
- [accord-gp4f60-two-encodings-enumeration-trap](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-gp4f60-two-encodings-enumeration-trap.md) *(user-store)* — gp-relative accesses exist in TWO encodings; "69 definitive" was wrong, true total **76**. Byte scan misses the 6-byte form and over-matches it **7.6×** (`hw2=0xff61` is shared across ~13 nearby displacements); `search_instructions` misses anything outside a function boundary (61 vs true 64, 4th occurrence). Register field is `(hw1>>11)&0x1F`.
- [feedback-stale-ghidra-import-defeats-hash-check](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/feedback-stale-ghidra-import-defeats-hash-check.md) *(user-store)* — an OPEN Ghidra program can hold an earlier revision of a file whose on-disk SHA verifies correctly, returning RAW bytes at edited sites. Re-import fresh + spot-check one edited site against a Python byte read before trusting any pre-flash re-disasm gate.
- ★ [accord-low-speed-lockout-window-c62ea](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-low-speed-lockout-window-c62ea.md) *(user-store)* — **THE LOW-SPEED STEER LOCKOUT IS CAL `0xC62EA`=320 (~5 km/h)**, a two-sided window (hi `0xC62E8`=12800 ≈200 km/h) vs voted speed `gp-0x6a5e`; failing it writes `STEER_STATUS=3`, which via an **intra-function** `cmp 0x2` @`0x29382` blocks `STEER_CONTROL_ACTIVE` (`gp-0x6806`) and the authority ramp (`gp-0x69b0`). Cal-only, one reader each, **no float mirror**, `0xC6000` block. Proven on-car by 3 independent decoders (~305k frames): 0% control-active <2 mph, ≥99% >4 mph, **zero** co-occurrence with status 3. **openpilot is NOT the obstacle** (`CP.minSteerSpeed=0.0`; the 3 mph in `values.py:163` is `HondaCarDocs` metadata). Second gate `0xC62EE` is a **CAN-commanded** permissive (trigger = `0x17C` byte 5 bits 7/5) — leave stock, **never raise**.
- ★ [accord-v850-scan-traps-formatv-and-storezero](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-v850-scan-traps-formatv-and-storezero.md) *(user-store)* — **seven byte-scan/method traps, each of which produced a confident WRONG answer this session.** Headline: **Format-V `jr`/`jarl` aliases `ld.bu`** (same opcode field `0x3C`, separated only by `hw2` bit 0) → **44% false positives, 10,105 of 22,850**; filtering `reg2==0` drops every store-of-zero; `ld.bu`/`ld.hu` use `hw2=(disp|1)`; never require "compare **then** boolean store"; `(lo,hi)` pairs are confounded by `[count][X][Y]` LERP records; an **external-readers sweep cannot prove report-only**; and **"Ghidra defined no function" ≠ dead** (use the task table at `0xBB928`, stride `0x30`, entry `+0x08`, self-validating on `0x2214A`/`0x22CA0`). Includes a 4-point validation harness for any new scanner.
- ⚠ [accord-fun45608-authority-slots-not-motoroff](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-fun45608-authority-slots-not-motoroff.md) *(user-store)* — **`FUN_00045608` is a generic AUTHORITY-SLOT SETTER, not "motor off"**: 3 parallel 7-slot arrays (`gp-0x652c`/`0x64fc`/`0x6514`) consumed by the G1 governor as a **running MIN** applied as a Q15 scale on the total command. `(3,0,0x8000,0x8000)` = slot-3 target 0 with instant slew — effect right, label wrong. Also: **the governor DOES read vehicle speed** (`gp-0x6a64` vs cal `0xC6316`=640 ≈10 km/h, below which the slew limiter is BYPASSED) ⇒ **falsifies "no vehicle-speed input anywhere in the command path"**; and its slot loop covers **slots 0-5, not 0-3**.
- ★★ [reference-accord-vibration-requires-lkas-engaged](reference-accord-vibration-requires-lkas-engaged.md) — **the 21.09 Hz Q≈14 resonance EXISTS ONLY WHILE OPENPILOT IS COMMANDING.** Matched test (hands-OFF, moving `vEgo>0.3`, same Nfft/speed gate, route 13): **9,200× less 21 Hz power with LKAS off**, and the disengaged pool carries **6× MORE** low-frequency energy — so it is NOT an excitation artifact. ⇒ **closed-loop LKAS instability, NOT the "command-independent base-assist limit cycle"** CLAUDE.md describes ⇒ **an openpilot-side 21 Hz notch/rolloff is a ZERO-BRICK-RISK experiment to run before any further `.rwd`.** Three method traps recorded: never mix hands-on/off (mixed data peaks at a spurious **7.42 Hz**, RETRACTED); the "steeringPressed is circular" objection is **testable and FALSE** (driver torque 2166 vs 328); and the raw `latOFF & handsOFF` cell is a **PARKED car** (median vEgo 0.00) — gate on `vEgo>0.3`. Mode spans **<1.5 → ~15 m/s** across 3 datasets; only >15 m/s becomes a broad low-Q 11-12.5 Hz shelf ⇒ "3-8 m/s worst regime" is **refined, not special**.
- ★ [reference-accord-can-tx-100hz-base-tick-and-gateway-evidence](reference-accord-can-tx-100hz-base-tick-and-gateway-evidence.md) — **CORRECTION: the CAN-TX base tick is 100 Hz, NOT 62.5 Hz** (derived 3 ways from cadence × measured wire rate; CAN 399 fitted at exactly 100.000 Hz) ⇒ FOURFRAME transmits at 100 Hz. **Gateway whitelist now confirmed on 8 controls, not 1**: of 11 broadcast slots only `0x14A`/`0x18F`/`0x1AB` reach the comma; slot 8 (`0x19F`) is configured **identically** to slot 9 (`0x18F`) yet never appears ⇒ **FOURFRAME's silence says NOTHING about whether the cave fired.** ⚠ the planned red-panda confirmation **may not discriminate** — the red panda taps the SAME comma Bosch harness. **Better channel: `0x18F` byte5 (constant 0x00) + `0x14A` byte4 (constant 0x07) = a free 16-bit signal at 100 Hz on gateway-crossing frames**; `0x1AB` is a POOR carrier (DLC 3, bytes 0-1 a live saturated signal). Tables read with pure Python — no Ghidra.
- ★★ [reference-accord-fourframe-strb-ssam-defect](reference-accord-fourframe-strb-ssam-defect.md) — **FOURFRAME NEVER TRANSMITTED, and it was OUR firmware.** The cave wrote `STRB = 0x80` to mailboxes 16-19; bit0 is `SSAM` and the V850E2 manual is explicit (`0 = Message buffer not used`), with §20.8.1 making SSAM=1 a hard precondition for joining the TX priority search. Stock `FUN_0001cf30` writes **0x01** to all seven TX mailboxes and **0x00** to the unused pool — `0x80` left ours configured exactly like the unused pool. Root cause: `build_vcantx_test_tva.py:54` mislabelled bit7 `SSOW` as "TX direction"; **the VCANTX seed carries the same defect.** Fix = `203e8000`→`203e0100` at 4 sites, 8 bytes, identical encoding. ⇒ **The gateway-whitelist theory is WEAKENED**: `0x19F` is gated at its own request site (`0x5559E`) while `0x18F` is not, so it was never a clean control.
- ★ [reference-accord-c646c-shared-gain-not-lkas-only](reference-accord-c646c-shared-gain-not-lkas-only.md) — **`0xC646C` is NOT "the LKAS authority gain"** — it is the firmware's single shared Q15 sensor-to-command scale with **exactly 6 readers** across three subsystems (enumerated twice, both encodings, no float mirror, neither monitor among them). Two of them (`0x36686`, `0x3684a`) apply it to the **raw torsion bar `gp-0x4f60`** on a feedback path verified end-to-end into `gp-0x6b94` → governor → motor. So 4× LKAS authority silently quadrupled two feedback lanes. **Probably NOT the 21 Hz driver** (that lane is IIR'd at fc≈2.2 Hz = −19.7 dB, clamped to 5% of aggregator range) and **the saturation hypothesis is empirically DEAD** (0 of 10,178 active-LKAS frames reach the clamp). Minimal fix designed + verified: one 2-byte retarget of `0x2a1ee` to a confirmed-free cal word at `0xC6CD0`.
- ★★ [reference-accord-vibration-needs-applied-torque](reference-accord-vibration-needs-applied-torque.md) — **the vibration needs the EPS to ACTUALLY APPLY LKAS torque, not merely to be commanded.** Three-way split on route 13: openpilot off → nothing; commanding *harder* but EPS in lockout → nothing (1.33× = noise); commanding **and applying** → **14,750×** more 21 Hz power. **Also: the 21 Hz IS in the openpilot command** — the "not in the output" premise was FALSE and came from reading `0xE4` on src=2/src=128, which are **identically zero**; only src=129 carries data. Coherence 0.685 @21 Hz vs 0.171 @1-3 Hz; **no OP-side low-pass at 21 Hz** (−2.70 dB) ⇒ **an OP notch is UNTESTED, not a no-op.** Retracts the "latency makes 21 Hz impossible" argument as bad control theory. Aliasing 21 vs 78.91 Hz still OPEN (IMU tie at the noise floor; leans 21.09 on implied Q).
- 🛑 [accord-check-build-lineage-before-proposing-lever](accord-check-build-lineage-before-proposing-lever.md) — **two agents in one session proposed `0xC6450` 1024→32 as "never flashed". It is V46 — flashed, null.** A third nearly repeated it with `0xC644A` (V43, null). **Grep `build_v*_tva.py` and check `docs/BUILD-LINEAGE.md` before naming ANY cal address, and state its on-car result.** Structural plausibility is not evidence of novelty. This is why CLAUDE.md was restructured into an index — the fact was present but not findable.
- ★ [project-v53-fourframe2-plus-minsteerspeed0](project-v53-fourframe2-plus-minsteerspeed0.md) — **V53 = FOURFRAME2 byte-for-byte + `0xC62EA` 320→0 (min steer speed → 0). BUILT 2026-07-27, UNFLASHED**, exactly 6 bytes off FOURFRAME2. Chosen 0 (not the old "suggest 64") because stock **already** unlocks true standstill via `gp-0x68b3` — 0 removes the 1–319-count discontinuity instead of moving it. Sole-reader + LERP-masquerade + SNA checks all re-run in Python at build time. `build_v53_tva.py` **imports** the cave from the FOURFRAME2 builder — zero transcription surface, reuse that pattern. No V42 ratchet fix (`0x454FE` stock, asserted).
- ★★ [reference-accord-v53-flashed-steer-to-zero-confirmed-telemetry-null](reference-accord-v53-flashed-steer-to-zero-confirmed-telemetry-null.md) — **V53 FLASHED 2026-07-27 and is the image on the car.** ✅ Steer-to-zero **CONFIRMED from raw CAN 399**: `STEER_STATUS=0` in 5,995/5,995 frames (ST=3 never fires) + **226 frames of `STEER_CONTROL_ACTIVE=1` below 5 km/h**, a cell identically EMPTY on V38 — the golden model's first successful advance prediction of an on-car state-machine change. 🛑 But the four-frame telemetry `0x6A0`-`0x6A3` was **still absent (0 frames / 301,824)**, and that null is **UNINTERPRETABLE**: six IDs the stock firmware genuinely broadcasts (`0x19F`,`0x32E`,`0x64D`,`0x660`,`0x722`,`0x723`) are equally absent, while non-DBC IDs (`0x669`,`0x750`,`0x674`) *are* logged — so "openpilot didn't know the ID" is excluded. **Never build another new-mailbox telemetry channel.** What V53 did to the vibration is UNANALYSED.
- ★ [reference-accord-piggyback-channel-audit-dbc-panda](reference-accord-piggyback-channel-audit-dbc-panda.md) — full carrier audit of the 3 whitelisted EPS frames. **`0x14A` byte4 bits 7:3 = 5 free bits @100 Hz** is the only usable channel (4 successful flashes, proven to cross, openpilot-invisible, hook at `0x55C0E` before the checksum). ⚠ **`0x18F` byte5 bits 5:4 are LIVE in firmware** (`gp-0x6880&3`, packer `0x55CAE`) — a DBC-only read wrongly calls them free; it also has no located pre-checksum hook. 🛑 **opendbc VERIFIES Honda checksums** — a bad one drops `can_valid` = a disengage. Panda's Honda RX list (`0x1A6/0x296/0x158/0x17C/0x326/0x1BE`) gates none of them.
- 🛑 [feedback-telemetry-must-reserve-a-did-not-fire-value](feedback-telemetry-must-reserve-a-did-not-fire-value.md) — **any telemetry encoding must reserve a wire value a live probe can never emit.** Caught in V54: the unbiased encoding made a *dead* probe decode as "authority 0-127, lane at FULL bound — V55 = mute", a confident fabricated answer from a channel that never ran. Bias the live range to start at 1. Also: prefer an encoder that changes a *register field* of a verified instruction over one introducing a **new opcode value**.
- ★ [project-v54-authority-probe-built](project-v54-authority-probe-built.md) — **V54 BUILT 2026-07-27, UNFLASHED — the one to flash.** V38 + `0xC62EA` 320→0 + a 44-byte read-only cave packing `wire = min((gp-0x6966>>7)+1, 31)` into `0x14A` byte4 bits 7:3 @100 Hz. It is the instrument that unblocks the `0xC6AF0` direction: wire 1-25 ⇒ lane at full bound (mute it), wire 30-31 ⇒ already clamped to zero (hypothesis dies), mixed ⇒ the knee crossing is the trigger. **wire 0 ⇒ the cave did not fire, drive is VOID.** All gates + GhidraMCP re-disassembly of the written image passed.
- [reference-accord-v54-flashed-authority-is-zero-by-design](reference-accord-v54-flashed-authority-is-zero-by-design.md) — **V54 FLASHED; the probe FIRED** (byte4 `0x07`→`0x0F`, one bit, 5,989/5,989) — the kit's **first working firmware telemetry channel**. `gp-0x6966` is the soft-EME wind-up magnitude, **not** a speed-scheduled authority, and is ~0 **BY DESIGN** on V31+ (boost floor 4096, raised to 5120 at V38, makes wind-up unreachable). ⇒ `0xC6AF0` selects unity in 100% of normal operation, the residual lane runs at FULL bound, **mute is the only meaningful edit** — but **GATE 2 (damping sign) is still open**. Do NOT re-drive faster to move it.
- [reference-accord-vibration-creep-speedshift-and-saturation](reference-accord-vibration-creep-speedshift-and-saturation.md) — the mode **moves with speed** (20.12 Hz @1.0 m/s → 21.68 Hz @4.0 m/s, 8 bins) so **no fixed-frequency notch can track it**; it **reproduces at 3.4 mph creep** (771× engaged/disengaged) in the cell V53 unlocked; and **saturation suppresses it 141× speed-controlled** — though stiction/backlash predicts that equally. All eight falsified levers share one assumption: that the ~20 Hz is *commanded*.
- [reference-accord-v55-dual-probe-built](reference-accord-v55-dual-probe-built.md) — **V55 BUILT, UNFLASHED, all gates + Ghidra re-disasm passed.** Dual report-only probe: bit7 = damper variant ≥10, bits 6:3 = 4-bit `gp-0x6b98` (final merged command). A **partition**, not a ninth lever. Carries three reusable encoding facts (`gp-0x6b98` is SIGNED; `ld.bu` puts disp bit0 in the opcode; `st.w` carries `|1` like `ld.w`) and the trap that **a V54 rlog decodes as a plausible V55 reading**.
- [feedback-no-openpilot-side-modifications](feedback-no-openpilot-side-modifications.md) — standing operator instruction 2026-07-28. **Retires the long-running "openpilot 21 Hz notch" recommendation.** openpilot stays in scope as a *measurement instrument* only; the fix must be firmware-side.
- 🛑 [feedback-explain-with-python-mirroring-decompiled-arithmetic](feedback-explain-with-python-mirroring-decompiled-arithmetic.md) — **standing operator instruction 2026-07-28: explain firmware with simple Python that mirrors the decompiled arithmetic EXACTLY** (integer `>>`, real Q-format, real branch conditions, each line annotated with its instruction address, constants byte-read little-endian). Prose-only explanations hid the `0xC63D2`=14 error, the off-by-`0x1000` slips, and the "mute `0xC6AF0`" shorthand. dB/Hz comes *after* the code, never instead of it.
- ★ [accord-lerp-tables-count-word-first](accord-lerp-tables-count-word-first.md) — **LERP tables start with a POINT-COUNT word; the base address is NOT the first X.** For `0xC6AF0`: count@`0xC6AF0`, X@`0xC6AF2..AFA`, **Y[0]@`0xC6AFC`, Y[1]@`0xC6AFE`**. Proved by the firmware's own `addi 0xc,r15,r13` (&Y[0]) and `addi 0x2,r15,ep` (&X[0]) at `0x3a63a`/`0x3a63e`, plus three sibling tables. **"Mute `0xC6AF0`" writes `0xC6AFC`/`0xC6AFE`** — writing the base clobbers the count.
- ★★ [reference-accord-v55-flashed-oscillation-is-internal](reference-accord-v55-flashed-oscillation-is-internal.md) — **V55 FLASHED, route `1c`: the ~21 Hz IS in `gp-0x6b98` and the loop is INSIDE the EPS.** Vibration 877× on the torsion bar / 996× on the independent angle-rate channel @20.90 Hz; command peaks in the SAME bin (coherence 0.93; route `1b` is a clean null control). **openpilot is 8.7× too small even with the LKAS low-pass deleted, and while RAILED its 21 Hz is exactly 0 yet the command still carries 105.8 counts.** Sensor→command transfer is **FLAT 0.19→0.22 from 1 Hz to 21 Hz** ⇒ the carrier is unfiltered. Damper bit7=1 ⇒ V44/V47 hit the LIVE tables.
- ★★ [reference-accord-gp6ad4-lane-and-c6af0-output-gate](reference-accord-gp6ad4-lane-and-c6af0-output-gate.md) — `FUN_0003a382` has **three PARALLEL branches** (Stage A and C poles are exact 1024/1024 identities); the `0xC6AF0` LERP is a **Q15 multiplier on the ceiling that clamps the FINAL combined value**, so muting is a **complete, branch-agnostic kill** — which is why V43/V46/V48A (one branch each) were null without exonerating the lane. ⚠ `gp-0x6ad4` is **NOT LKAS-gated** (`gp-0x67fe` = FOC substate, live when disengaged) ⇒ muting changes manual feel. Monitor risk CLOSED (1 writer/1 reader, no lockstep); damping sign OPEN.
- ★ [accord-c6372-c636e-untested-wideband-assist-emas](accord-c6372-c636e-untested-wideband-assist-emas.md) — both = 205/1024 → **only −1.29 dB at 21 Hz**, the only lanes besides `gp-0x6ad4` not band-limited below the mode. **Never flashed** — V44 pins them as "the rejected candidate B". 🛑 GATE 2 severe: `gp-0x6bbe` is base power steering, and adding 60-73° of assist-loop lag is the **V48B brick class**. Candidate #2, needs its own GATE 2 pass.
- ★ [project-v56-c6af0-mute-built](project-v56-c6af0-mute-built.md) — **V56 BUILT, UNFLASHED** = V55's probe + the mute, exactly 6 bytes off V55 (only 2 cal bytes move — `32768`=`00 80` LE). Builder is a POST-PROCESSOR over V55's SHA-verified image, zero transcription. ⚠ `V53.assert_stock_cals()` rightly refused it; **do not weaken that shared guard** — V56 runs it unmodified on the pre-edit source instead. Reversible experiment, not a known-good fix.
- ★★ [reference-accord-v56-flashed-mute-is-null-and-costs-damping](reference-accord-v56-flashed-mute-is-null-and-costs-damping.md) — **V56 FLASHED (route `24`, first ROAD drive with a probe): the `0xC6AF0` mute BOUGHT NOTHING ⇒ REVERT TO V55.** Speed-matched creep **786×** engaged/disengaged (V55 877×; replicated 1,878×/5,524×) and the command's in-band content did **not** drop ⇒ outcome (iii): **`gp-0x6ad4`/`FUN_0003a382` ELIMINATED as the driver of the 20-25 Hz mode, all three branches at once.** ★★ **The few-Hz resonance is WHEEL ORDER 1, not V56's doing** — `f = 0.489·v − 0.186`, r=+0.997, through the origin, implied circumference **2.088 m** ⇒ **tyre imbalance; get a road-force check.** A separate FIXED ~7-8 Hz resonance exists on every build and the wheel-order line sweeps up through it at 15-20 m/s. ⚠ "V56 removed damping" is **not supported** by the creep data (V56's 1-10 Hz power is 0.17× V55's; envelope Q not lowest) but **not closable** — no prior build has road-speed data. 🛑 A 50% partial restore is **not** a candidate: 0% and 100% already agree. 🛑 **Stop calling the mode "21 Hz"** — `f = 0.177·v + 20.48`, reaching **25.0 Hz** at 21 m/s, Q=160-228; and **steering angle shifts it ±2 Hz**, which confounds every cross-route comparison.
- ★ [reference-accord-fun3a382-is-a-real-pid](reference-accord-fun3a382-is-a-real-pid.md) — **supersedes the "three parallel lag stages" framing.** It is a genuine discrete **PID**: Stage A = P (gain 153-256/1024, motor-rate scheduled), Stage B = I (98/1024, anti-windup), Stage C = **D, a raw backward difference at gain 2048/1024 = 2.0, UNATTENUATED**. The two `1024/1024` cals are each stage's *own extra* smoothing EMA collapsing to identity — so V43/V46 were re-introducing a defeated pole, not filtering the lane. Frequency table 3/5/8/21 Hz (P-dominated at few-Hz, D rivals P by 21 Hz, +41.8° lead). **Sign PROVEN: plain `add`, all 9 aggregator lanes, polarity `gp-0x6752` boot-fixed to +1, no inversion.** ⚠ contradicts the old "−3.3° to −5.4°" phase memory.
- 🛑 [reference-accord-probe-underranges-to-a-one-bit-comparator](reference-accord-probe-underranges-to-a-one-bit-comparator.md) — **the `gp-0x6b98` probe UNDER-ranges to ~1.5 bits (we guarded against railing; the opposite happened).** 99.2% of engaged+hands-off frames in **two adjacent levels**; command inside ±512 while one LSB is 512. **An amplitude comparator but a usable SPECTRAL probe.** ⚠ **"120.5 counts at 21 Hz" is a REAL detection — keep it, restate it** (encoder gain 1.006; false-positive floor 10-18 counts ⇒ 7-12× above floor; but it is a **bin-RMS not an amplitude**, and **38× vs 66× depends on which estimator openpilot's 31.7 was — unresolved**). ⚠ **"flat H1" is UNCONFIRMED, not refuted** — quantisation is exonerated *by construction* (a memoryless nonlinearity cannot flatten a pole; coherence bias is DOWNWARD so 0.93 is a lower bound); the problem is dof: at ±19.6% a 16.8 Hz pole and flat are indistinguishable. 🛑 **The `0xC646C` elimination STANDS on its structural leg** (0 matches in 468 instructions). Also: **"9 independent segments" is wrong — K=3, significance 0.776**; re-scale to **`SHIFT=7/OFFSET=8`**, and 🛑 **`SHIFT=6` needs `OFFSET=9` or it collides with the `field==0` sentinel**; and the **5th harmonic of the mode folds into the few-Hz band**.
- ⚠ [accord-takeover-beep-is-openpilot-device-load](accord-takeover-beep-is-openpilot-device-load.md) — the intermittent "TAKE CONTROL IMMEDIATELY" is `commIssue`+`selfdrivedLagging` **softDisable that self-recovers** (once in 15:43, 1.04 s, `latActive` never dropped). **EPS/CAN is a clean null**: ST=0 throughout, `saturated` False 94,241/94,241, panda counters flat zero. Cause = **5-6 of 8 cores chronically near saturation** on a StarPilot fork; no single culprit process. Carries 4 rlog tooling traps: **`alertSound` is unpopulated route-wide**, `onroadEvents` DOES exist, **anchor the clock on each segment's first `carState`** (a bootstrap duplicate biases it +1.34 s), and `0x33D` byte4 is counter+checksum.
- 🛑★★ [reference-accord-gp6a5e-is-speed-reclassifies-v44-v47](reference-accord-gp6a5e-is-speed-reclassifies-v44-v47.md) — **`gp-0x6a5e`/`-0x6a62`/`-0x6a64` are VOTED VEHICLE SPEED, settled** (voter `FUN_00041eec`, validity window −6400..32000, closest-to-previous fallback; zero overlap with `gp-0x4f60`'s cluster). ⇒ **retire the "Sensor A" label** — it was never a torque sensor. 🛑 **The damper Factor C LERP indexes on it**, pointer chase byte-verified (`0xC9E9C+10*4` → `0x000D27BC`), X=(2240,3840,5120,8960) ≈ **35/60/80/140 km/h** ⇒ **`Y[0]=0` means "below ~35 km/h", NOT "hands-off"** ⇒ **V44 and V47 were aimed at a mechanism that does not exist.** Their on-car results stand; their stated rationale is withdrawn. The "2240 counts driver torque" story is almost certainly a **collision** with the override curve's unrelated torque breakpoint of the same number. Also: invalid speed ⇒ factor defaults to **UNITY**, not zero.
- ★★ [reference-accord-driver-override-curve-kills-lkas-authority](reference-accord-driver-override-curve-kills-lkas-authority.md) — the operator's *"significant driver torque in a direction kills the grinding"* is a **mapped authority cutoff** at `FUN_00028ea6` `0x29a74`: curve indexed by `gp-0x682f = |gp-0x4f60|>>5` with direction from `sign(gp-0x4f60)`, byte-read X=[70,72,78,80] Y=[254,234,12,0] ⇒ **LKAS authority collapses 254→0 between raw torque 2240 and 3584** (≈±2190..±3500 in DBC units). ⚠ The `0xC64B8` threshold branch is **DEAD** since V37 set it to `0xFF`. 🛑 It explains the **KILL, not the CREATION** — hands-off the curve is flat at 254. ⚠ It also means a raw-band-power driver-torque sweep **looks like it contradicts the operator**; you must use a **prominence** estimator.
- 🛑★★ [reference-accord-deadband-signgate-eliminated-on-car](reference-accord-deadband-signgate-eliminated-on-car.md) — **`gp-0x6806` IS ON THE BUS: CAN `0x18F` byte4 bit3 = `STEER_CONTROL_ACTIVE`** (packer `FUN_00055c42`, matches opendbc `BO_ 399` exactly). Measured route 24: **==1 in 96.26%, TWO transitions in 180 s, max toggle 0.1 Hz** ⇒ the `0xC61B8` deadband + sign relay is **bypassed in steady engaged driving and CANNOT be a 20-25 Hz mechanism.** Kills the relay/limit-cycle hypothesis (which fit the *entire* symptom profile — a warning about explanations that fit too well). ⚠ **BUT the bus carries PARITY, not the gate's test** — the packer does `andi 0x1` (`0x55c7e`) while the gate tests exact equality (`cmp r0,r12 ; bne` @`0x2a1ba`); four of eight live writers store a REGISTER, so a 0↔2 toggle at 22 Hz would be invisible. **V57's probe bit6 is the exact test and closes it** (`rlog-tools/decode_v57_deadband.py`). ⚠ It **IS** on the forward path (`r1`→`r16`→`st.h -0x6b3c @0x2a2ea`); the `-0x6b38` store at `0x2a23c` is a **diagnostic copy** and a subagent wrongly stopped there. ⚠ `0xC61B8`=102 was **never rescaled** while `0xC61B2/B4` went ×4 — a real defect, but on the **engage ramp** only. 🛑 **Split every `FUN_00028ea6` scan at `0x2a30d`**: `gp-0x6806` has 16 raw writers = **8 live + 8 dead-echo**; `gp-0x6b30` has 4 refs = 2 live + 2 dead.
- ★★ [accord-angle-rate-lane-gp6bbe-top-candidate](accord-angle-rate-lane-gp6bbe-top-candidate.md) — **TOP surviving vibration candidate, UNBUILT.** `FUN_00034a72` (boost, summand `gp-0x6bbe`) reads the **steering ANGLE RATE `gp-0x6a56` UNFILTERED** (`0x34AB8`/`0x34E8E`, byte-verified by the lead after two subagents disagreed) as one side of an FSM-result subtraction, scaled by two **speed-indexed** LERPs. That signal is what the EPS **transmits** as `STEER_ANGLE_RATE` (0x14A[2:3] and the 10× finer 0x18F[2:3]) ⇒ **opendbc ground truth**, and it carries the mode at **996×** vs torque's 877×. ⇒ the rate path **bypasses the torque EMA**, so the −1.29/−14.91 dB figures and the unresolved `FUN_00022ca0` task rate **matter far less than they appeared**. 🛑🛑 **GATE 2 ANSWERED *AGAINST* CUTTING IT — THE LEVER INVERTS.** The torque EMA is a **multiplicative amplitude scale**, not an additive branch, and the core term is `rate_error = baseline − angle_rate` (`sub r6,r28` @`0x34e96`) ⇒ **`gp-0x6bbe` ≈ −(gain)·angle_rate = viscous DAMPING.** **Cutting it would REMOVE damping and likely make the grinding WORSE — the V56 error one build later.** The interesting direction is **RAISING** the gain: cleanest lever **`K1` @`0xD200C`=43** (1 hit image-wide). ⚠ **INFERRED, not simulated** — rests on `baseline` being slow at 22 Hz; **certify by simulation before building.** Also: **speedLERP2 `0xD20C0` is FLAT** (fixed ±512 clamp), speedLERP1 is a hump peaking at 40 km/h, not a monotonic speed rise. **First candidate ever in the angle-rate domain** — all ~50 falsified levers were torque-domain.
- ★ [v57-decouple-built](v57-decouple-built.md) — **V57 BUILT, UNFLASHED** = V55 + the `0xC646C` decoupling: `0x2A1F0` disp `0x746C`→`0x7CD0` so the **forward** reader gets a private word at `0xC6CD0` = 3564 while the **four feedback readers revert to stock 891**. 14 bytes off V55, 50/50 CRC, RWD round-trip gated, verified against the built image. ✅ **no float mirror** (fresh 32-bit scan of `[0x7440,0x74A0)` → 0 hits) ⇒ no V27 desync risk. 🛑 **Correctness fix — expected NULL for the grinding** (≤0.28 dB at 22 Hz; only 1 of the **11** aggregator summands reads the cal, at −46..−58 dB). ⚠ Manual feel **will** change. ⚠ Reader #6 is **not** a second additive path — it modulates #5's hysteresis dead-band **width**. **Flash V55 FIRST** to undo V56 cleanly.

## 2026-07-29 session (documentation hygiene) — golden model distilled, no firmware change

- 🛑 [feedback-golden-model-distilled-keep-terse](feedback-golden-model-distilled-keep-terse.md) — **`eps_lkas_chain_model.py` cut 4,709→2,200 lines (347→145 KB), comments/docstrings only.** Operator instruction: comments ballooned into dated changelogs + multi-hundred-line findings essays embedded in function docstrings (e.g. `vibration_hands_off_analysis` alone carried ~430 lines). Distilled to comments ≤1 sentence, docstrings ≤1 paragraph, address/Q-format/confidence-tag ties kept. **Verified two ways, not just claimed:** an AST-equality diff (docstrings stripped) proves zero code changed; `python eps_lkas_chain_model.py` produces byte-identical stdout before/after. No new firmware finding — this is a documentation-quality pass. Two content fixes made in passing: a stale "4-of-16 phase-gated" claim (already contradicted by the module docstring's own EXECUTION MODEL correction) was removed rather than perpetuated; two densest functions (`vibration_hands_off_analysis`, `motor_torque_governor`) were left at ~20-28 lines rather than a strict one-liner since they carry the two most load-bearing CONFIRMED root-cause chains in the file.

## 2026-07-30 session — V57 driven; the two symptoms separate; V58 probe built

- ★★ [accord-ratchet-and-grinding-are-two-symptoms](accord-ratchet-and-grinding-are-two-symptoms.md) — 🛑 **The ~7.4 Hz mode is the RATCHETING; the 20-25 Hz mode is the GRINDING.** Operator, authoritative. Everything written before 2026-07-30 conflates them, and a mid-session claim that "the kit has been optimising the 5.3% component for 50 builds" is **WITHDRAWN**. The ratchet rises **8.42×** with command rail duty while the grinding falls to 0.74×; it is **not** the V42 state-4 ratchet (`ST==4` fires 0/37,922). Angle excitation is correlation only, via return-to-centre.
- 🛑 [accord-telemetry-conventions-that-produced-wrong-answers](accord-telemetry-conventions-that-produced-wrong-answers.md) — three rlog conventions that each flipped a headline: `cruiseState.enabled` is long+lat (reads **0.00%** on parking-lot routes; use `carControl.latActive`), raw `|tq|<=200` hands-off **discards the frames carrying 8.79× the oscillation**, and mean Welch power hides bursts (**median 0.419 vs p99 0.891**). Fourth, ✅ **SOLVED 2026-07-30** by route `2b` seg 13 (60 s moving-but-disengaged at 0.5–4.8 m/s) — see [[accord-v58-drive-grinding-engagement-gated-creep-only]]; the old 877×/786×/14,750× stay retired. Fifth and sixth, added the same day: use a **strict 18–26 Hz band** (a wider one catches the ratchet's 2nd harmonic and fakes a frequency slope) and **prominence, not envelope amplitude**, to separate a mode from broadband.
- 🛑★★ [accord-gp6a56-is-motor-rate-not-an-angle-sensor](accord-gp6a56-is-motor-rate-not-an-angle-sensor.md) — `STEER_ANGLE_RATE` is **synthesized from the MOTOR resolver rate** (`FUN_0003f776`, fixed Q15 scale of `gp-0x6abe`), not independently sensed. Kills "the only control-path signal with an external anchor", and since `baseline` is also `gp-0x6abe`-derived, `gp-0x6bbe`'s damping sign is **unresolved** after four static flip-flops.
- ⚠ SUPERSEDED [accord-v58-boostlane-probe-built](accord-v58-boostlane-probe-built.md) — **V58 has since FLASHED and flown clean**; see [[accord-v58-drive-grinding-engagement-gated-creep-only]]. Its bit5 result ELIMINATED the `0xD20C0` ceiling, but **bit6 returned nothing** and this file's "method pre-validated" claim did not transfer — see [[accord-sign-probe-needs-zero-crossings]].
- 🛑 [accord-c61d6-slew-is-rejected-not-fresh](accord-c61d6-slew-is-rejected-not-fresh.md) — `0xC61D6` 0→14 is **REJECTED** ("highest-risk lever; last/never" — it activates an uncalibrated 2D map onto the live command), not untested. Re-proposed as fresh on 2026-07-30 because it appears nowhere in `BUILD-LINEAGE.md` and an agent-memory still recommended it. Both fixed. **`0xC64DE`=27 (V18) is carried forward correctly; there is no output rate limiter available as a cal.**
- ★★ [accord-v58-drive-grinding-engagement-gated-creep-only](accord-v58-drive-grinding-engagement-gated-creep-only.md) — **V58 FLASHED, flight-clean** (route `2b`, 83,959 frames, `ST==4` = 0). ★★ **The collinearity confound is BROKEN** — seg 13 gives 60 s moving-but-disengaged at creep speed ⇒ **13.4×** speed-matched, **184×** on occupancy, and the resonance is **ABSENT disengaged** (prominence 122.7× vs 3.6×). Three record corrections: the **frequency law does not reproduce** (fixed ~20.9 Hz; `a = 0.177` rejected at 3.2–7.1σ), the grinding is **CREEP-ONLY** not road-speed, and **~21 Hz IS in openpilot's command** (direction unresolved).
- 🛑★★ [accord-gp6ba6-is-the-boost-amplitude-index](accord-gp6ba6-is-the-boost-amplitude-index.md) — **`gp-0x6ba6 == |gp-0x6b9a|`** and it — not `gp-0x6b9a` — indexes both boost amplitude LERPs; **`0xD28DC` hangs off `0xca4f4`, not `0xca23c`**. `build_v58_tva.py` was wrong on both counts. ⇒ the index is V58's 20.93 Hz signal **rectified**, sweeping the curve at **~2× the mode frequency on the BASE ASSIST path**. Depth unmeasured ⇒ V59. Also kills a subagent's "2-pole biquad" claim (it is the identity 3-tap FIR).
- 🛑 [accord-sign-probe-needs-zero-crossings](accord-sign-probe-needs-zero-crossings.md) — **a 1-bit SIGN probe carries phase only for a signal that crosses zero at that frequency.** V58's headline bit6 returned nothing because `gp-0x6bbe` is DC-dominated (0–1.1 transitions/s vs the ~44/s a 22 Hz flip needs). ⚠ **Pooling runs manufactures coherence** out of splice steps. Probe **magnitude**, not sign. Phase resolution is also capped at ±77° by the one-sample mailbox skew.
- ★ [accord-v59-boostindex-depth-probe-built](accord-v59-boostindex-depth-probe-built.md) — **V59 BUILT, UNFLASHED**: thermometer on `gp-0x6ba6` at 512/1024/2048 + the `0xFFFF` fault-sentinel bit. **19 bytes off V58, CAL CRC unchanged.** No new encoder, no new condition code. Measures the one thing V58 could not — **depth**, i.e. how much gain swing the modulation actually delivers (`<512 ⇒ ≤1.12×` … `≥5120 ⇒ 2.00×`).
- ★★ [The grinding is an UNCOMPENSATED loop through the torque sensor](reference_accord_loop_through_torque_sensor_uncompensated.md) — command→bar transfer peaks at 21.09 Hz (global max, 15.6–25.7×); no motor-command feedforward exists anywhere; damping is zero below 35 km/h. Displaces the parametric-pump story.
- ⚠ [`search_instructions` OVER-counts too — address-literal collisions](reference_accord_search_instructions_overcounts_address_literals.md) — 21 hits reported vs 2 real for `gp-0x6b70` (`jarl 0x0006b700`). Both error directions now documented.
