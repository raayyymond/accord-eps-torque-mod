- 🛑🛑★★★★★ [The 8 Hz mode is the LOOP, not the plant](accord-the-8hz-mode-is-the-loop-not-the-plant.md) — loop IDENTIFIED: `|kG|`=0.630 [0.512,1.001], gain margin **1.2-1.6x**, plant `Z0` a near-lossless spring ⇒ **100% of `Re(Z)`=-3761 is LOOP-GENERATED**. ⇒ a resonant pole is always the wrong sign; the matched fix is a **differentiator**. **THE 4:1 CANCELLATION LAW: size every 6-9 Hz lever against the SUM (0.053), never a lane.** Gain ceiling is **stability, not clamps** — already hit at 8x.
- 🛑🛑★★★★★ [`ld.bu` displacement's LOW BIT is in `hw1` bit 5, not `hw2`](accord-ldbu-displacement-lowbit-in-hw1.md) — the kit's `"ld.bu -> hw2==enc|1"` rule is **WRONG** and has been **conflating ADJACENT CELLS**. `0xC64FA` has **8** readers, not 18; the other 10 read **`0xC64FB`**, an uncharacterised cal. **Suspect every prior byte-cell reader/writer count.** Adjudicate with `disassemble_bytes`, never with displacement bytes.
- 🛑🛑★★★★★ [`gp-0x6752` is −1, NOT +1 — three memory files were wrong](accord-gp6752-is-negative-one.md) — second writer `FUN_00048a40`, config record `0x14C0` beats `0x1180`, last-write-wins. **Verified 3 ways** (orchestrator + independent agent + V98 on-car duty 0.0000/5 routes). ⇒ **D pumps, P/I damp, net PID damps**; **r24/r26 PUMPS −431…−1294 ct**. ⚠ **V49 was gated "brick if −1" and never flashed — correctly.** 🛑 The GATE2-vs-canonical convention mismatch caused THREE sign reversals in one session.
- 🛑🛑★★★★★ [`f0` — the `Re(Z)` crossover — IS THE ENDPOINT](accord-f0-crossover-is-the-endpoint.md) — anti-damping is a REGION ≤16 Hz→`f0` on **every** arm incl. stock. **21.90/23.61/24.90 Hz at 1×/4×/6×, disjoint CIs, `f0 ≈ 21.3 + 0.60·m`.** **Needs NO symptomatic drive** (margin is STANDING, only excitation is intermittent). ~100 s hands-off 30–85 km/h; **>85 km/h contributes nothing.** 🛑 **But `f0` also tracks COMMAND AMPLITUDE at fixed gain (−0.99 Hz) and pooled the gain term goes n.s.** — report median `|0x0E4|` alongside `f0` or the readout is uninterpretable.
- 🛑🛑★★★★★ [The 6–9 Hz anti-damping is HONDA'S — the first instrumented stock baseline](accord-the-antidamping-is-hondas.md) — stock `Re(Z)` **−1297/−1709/−1507** at every speed; we multiply 2.4–3.0× below 86 km/h and not at all above. **But at 22–26 Hz stock is DAMPED (+247/+496) and we REVERSE the sign (−134/−99, disjoint)** — the only band where we flip sign not size. Stock's whole HF spectrum sits at **3–15 counts**; **exactly two excesses, 7–8 and 23–25 Hz.** 🛑 **Hands-on is a mandatory THIRD matching axis** — it halved this session's own band ratios.
- 🛑★★★★ [Handoffs must carry ALL findings and ALL open items](../../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/feedback-handoffs-must-carry-all-findings-and-open-items.md) — standing operator instruction 2026-08-20: document every finding **including the negative ones** so they are never repeated, every open item **with what would close it**, and every retraction — under the **256 KB** cap. Collect from subagents *before* stopping them.
- 🛑🛑★★★★★ [THE 8× GAIN IS THE CARRIER — `0xC6CD0` is the measured cause of the ~23 Hz vibration](accord-the-8x-gain-is-the-carrier.md) — de-confounded 2×2 vs route `71`, **shape units** against a **measured 1.45× placebo floor**: **gain G = 2.7–3.9× · Lever B 0.84–1.30× (inside the floor) · `0xCBE74` k = 0.86–0.90 (inert)**. The peak **MOVED 20.3 → 23.0 Hz** across three 4× routes ⇒ **a POLE moved**. The line is **in the firmware's own demand** (`gp-0x6b94` 21–24 Hz shape 1.71×), with the **aggregator's sign reversing 25–37 /s where V100 reverses 0.7–3.2** (re-weighted 3.06× ⇒ not quantisation). 🛑 **NO clamp binds** (`b6` 0.000000, all four controls pass; 81.5 % of rail since V14). 🛑 **NOT a limit cycle** (σ inside a surrogate null, kurtosis 3.85). **Vibration m^1.74, authority m^0.88** ⇒ V102 = 6× (`0xC6CD0` 5346), **the operator's own choice from the curve**. ⭐ Build **ABORTS at 10×** — structural cap. **Nine levers killed on evidence, none on the road.**
- 🛑🛑★★★★ [`band_envelope` is RECTIFIED, not analytic — ~20 callers affected](accord-band-envelope-is-rectified-not-analytic.md) — `_r31_common.py` **and** `_r2b_common.py` build one-sided `H=2X` then `irfft` ⇒ `abs()` is `2A·|cos ωt|`. **Ratios survive; every envelope-SHAPE result — growth σ, decay τ, ring-down ζ/Q, p50 "amplitude" — is WRONG**, including historical Q/ζ figures. `_r31_common`'s docstring claiming *"this is the AMPLITUDE A"* is false (median = RMS = 1.414A). ✅ Use `rlog-tools/r95_lib.py::band_envelope`. **REPORTED, NOT FIXED.**
- 🛑🛑★★★★ [`gp-0x6b4c` is NOT the LKAS command — it is an 11-SLOT ASSIST SUM](accord-gp6b4c-is-an-11-slot-assist-sum.md) — `gp-0x3d88 = Σ_{i=0..10}(0xC4118[i]≠0 ? gp-0x62f8[i] : 0)`, `0xC4124` = `[0,0,5,0,5,5,0,0,0,5,0]` ⇒ slots {0,1,3,6,7,8,10} raw passthrough, {2,4,5,9} forced zero; **algebraically flat** at that stage. **Explains why its sign agrees with openpilot's command at CHANCE (52.80 % vs 54.36 %) while flipping 8.2/s vs 0.31/s.** Much inherited "the LKAS lane" reasoning rests on the wrong label. 🛑 **Which slot is LKAS's is UNTRACED.**
- 🛑🛑★★★★★ [V100 FLEW (route `0x85`) — **THE REFERENCE-CLAMP HYPOTHESIS IS DEAD**](accord-c6200-clamps-the-pid-reference.md) — `d(b5)`=`d(b6)`=**0.000000** over **249.2 s engaged / 6 episodes (~4× the best ever)**, all 8 rate bins, CI [0, 0.0186], `b4`=0.6057 **on the same cell**. Rungs **proven correctly coded from the image** (cond `0xE`=GE, no guard, shared store with the controls) and **`mov` flag-transparency closed EMPIRICALLY** via V98's byte-identical idiom firing at 0.4235. 🛑 Budget was a **GATE-3 error: 2.09×, not ~12×** (windows ≠ own writer clamps); **3 of 8 terms ≡ 0**. `0.2565` stands **unconditioned**. `0x39ff6` chased = **motor-phase fault threshold** ⇒ DO-NOT-EDIT stands with a reason; and the cell is **self-cancelling** as a global edit.
- 🛑🛑★★★★★ [**THE RATE LANE IS CLOSED AT AN OPTIMUM — Lever B off every shortlist, BOTH directions**](accord-v88-flew-grinding-fixed-command-intact.md) — at `a=0` **V88 and V71c are ARITHMETICALLY IDENTICAL (1.707)** yet on-car they are the corpus **extremes** ⇒ **`a` is materially non-zero, the r26 arm is LOAD-BEARING, proved from images with no drive.** ⇒ **Account A ("more derivative feedback ⇒ more damping") is REFUTED** — the higher dose (V71c) was **worst in the corpus**. **Both flanks now measured** (V61 below = worse; V71c above = worse) ⇒ *"2× ≈ OPTIMUM"* has both sides. **Retires the kit's self-declared leading open question.** ✅ `0xC6444`'s falsification verified in the safe direction (V71c had the gate ARMED).
- 🛑🛑★★★★ [**`0xC4118` IS A HARD NEVER-ARM — arming it silently DELETES LKAS STEERING**](accord-verify-a-lerp-axis-before-designing-to-it.md) — the partition byte does **DOUBLE DUTY**: zeroing it to "arm" `0xC6194`'s dormant slew limiter drives `gp-0x3d88`→0 ⇒ `gp-0x6b4c`→0, **while openpilot believes it is steering**. Cal-only, 11 bytes, looks safe. ⚠ And `0xC6194`'s recorded kill reason (*"output ×0"*) was **MISATTRIBUTED — it belongs to `0xC6196`** (`0xC6194`=3, `0xC6196`=0) ⇒ a live cell carried a false death certificate for ~40 builds.
- 🛑★★★★ [**Verify a LERP's AXIS — and a GATE's ENABLE — from its WRITER, not a label**](accord-verify-a-lerp-axis-before-designing-to-it.md) — **three axis misidentifications on record**, the third a repeat of the second by a session that never found the correction. `gp-0x69aa` is a **Q15 governor derate pinned at unity**, not speed ⇒ 6 of 8 Y cells inert by operating point. ⭐ Same class: **`gp-0x6806` is the ENGAGEMENT flag** (== `latActive` on 150,302/150,327), **not** the low-speed lockout — that reading was a **speed correlation on a creep-dominated corpus**, broken by route `0x85`. **V88's attribution is SAFE.**
- 🛑★★★★ [**GATE 3 must ask for a DROPOUT, not only a clamp — and an implausible null is a bug report**](accord-verify-a-lerp-axis-before-designing-to-it.md) — this firmware uses **latching zero-output dropouts** as an idiom in ≥2 places (`gp-0x6b30` sign-latch; aggregator `0x3acc4 cmovc` **DROPS** a lane past ±10240). **A dropout is invisible to every no-clip rule the kit runs.** ⊕ **Five scan-blindness classes in one session**, every one caught by a **decompile** disagreeing with a scan: `jarl` Format-V mask · `movea`+runtime index · a byte written by a **wider** store · wrong `st.b` opcode (**20 writers read as ZERO**) · `hw2=disp|1` applied to `st.b`. **Corrected rule: `st.b`/`ld.b` → `hw2 == enc` EXACTLY; `ld.bu` → `enc|1`; halfword/word → either.**
- ⚠★★★ [`0xC6200`'s ORIGINAL entry — superseded by the flight above](accord-c6200-clamps-the-pid-reference.md) — kept for the structural map (`0x3a7a2` clamp, P/I/D all from one `err`, term 0 ≡ 0 via `0xC616C`=0). 🛑 **Its "TERM 0 RAILS IT AT 32 %" heading and its ~12× budget are BOTH superseded** — see the corrected entry.
- ★★★★★ [The rack IS variable-ratio, MEASURED: 16.9:1 centre → 11.1:1 lock, SYMMETRIC, and `0xC6B64` is ADEQUATE](accord-measured-rack-ratio-and-two-instrument-traps.md) — 47 routes/427 min, 4 disjoint estimators. swing **1.176 [1.147,1.201]** vs firmware **1.206×** ⇒ agrees to 0.01–0.07 at every knot 0–120°; **the 1.67–1.82× schematic estimate is REFUTED** and so is *"the plant model is blind to rack position"* (`0x3ba12` reads angle). ~20 % uncompensated **only >120°, all below 5 m/s**. **≥2 % asymmetry EXCLUDED by injection.** θ₀ **−4.25°**. 🛑🛑 Two traps: **`carState.yawRate` ≡ 0** (0/512,895) and **`vEgo` invalid at angle** (+7.9 %, faked a flat plateau and passed its own control).
- 🛑🛑★★★★★ [**V97 FLEW (route `0x80`) — THE LEVER WAS LIVE; THE NULL WAS OURS**](accord-v97-flew-lever-live-null-was-ours.md) — operator: ***"I did not feel any difference in grinding or stuttering… so I stopped the drive."*** ✅ **Both his hypotheses REFUTED**: the address is right 3 ways (census 1r/0w, Ghidra∖Python **EMPTY**), and `FUN_00038148`'s guard is **BYTE-IDENTICAL to the assist-mixer's** ⇒ **a shut gate = NO POWER ASSIST AT ALL**; `sign(gp-0x374c)` **toggled 181× in 109 s**. 🛑 The null is on the **EXPERIMENT**: no instrument (regressor **34× over-range**, `M≡0` on 10,749/10,749), **1** engaged episode vs the **25** the direction rests on, and **DC gain 1.000 ⇒ no amplitude statistic could EVER see it**, none pre-registered. ⇒ **UNINTERPRETABLE, NOT FALSIFIED. DO NOT RE-DOSE.**
- 🛑🛑★★★★★ [**ONE OBSERVER RESIDUAL, TWO ARMS — V89 and V97 pushed on OPPOSITE sides**](accord-observer-residual-two-arms-v89-v97.md) — `iVar6 = gp-0x6bfe (MODEL) + gp-0x6bfa (REQUEST) − (gp-0x374c>>4) (ACTUAL)`, coefficients **exactly ±1**, both arms scaled by the same `0xC6468`=2639. **V89's K1 measured FLAT; V97's pole felt like nothing — ONE unmeasured quantity explains both.** 🛑🛑 **The "≤9 % share" bound is RETRACTED** — bounding one arm against the other's *admitted range* is invalid for a difference of correlated estimates. **Share UNRESOLVED, not small.** ⊕ `|iVar6| ≤ ~6,900` at creep, **~130 half the time** ⇒ hints at strong **CANCELLATION**. 🛑 **`f′` swings 1.000×, not ≥10×** — `gp-0x6982/84` have **ZERO writers** and boot to 1024; and §A6b's *"transfer cannot be read from the image"* is **FALSE**.
- 🛑🛑★★★★★ [**THE PROBE DESIGN LAW — compare, don't quantise**](accord-probe-design-law-compare-dont-quantise.md) — from all 45 probe builds: **every probe that DECIDED something was a sign bit + a magnitude channel, or a designed CONTROL; every UNINTERPRETABLE null was a SINGLE THRESHOLD RUNG on a quantity with no measured distribution and no positive control.** ⭐ **A comparator rung is immune to UNDER- and OVER-RANGE by construction** — no LSB, no ceiling, no assumed distribution; **its duty is the answer.** 🛑 The kit usually **knew at cut time** (V86: *"THE PROBE CANNOT SCORE `0xC40D4` IN FORCE"*; **V80 flew after its own docstring said it couldn't discriminate itself from V79**). ⊕ **17.2 s suffices for an ORDERING endpoint** (`SE=√(p(1−p)/n_eff)`, ~3σ at τ=1 s) — the operator **stops the moment he feels the symptom**, so endpoints needing matched episodes are **UNBUILDABLE**.
- ★★★★ [V97 = `0xC63AC` 102→150 — ONE BYTE, the arc's first LOOP-POLE lever](accord-v97-is-a-loop-pole-and-the-direction-is-measured.md) — built on the **V96** base, **131/131**, rwd `78c674a8…`. DC gain is **1.000000 at any A ⇒ a POLE, not a GAIN**, which is why it escapes the sign problem that killed all six lane weights. **Direction MEASURED by two independent on-car instruments agreeing to <7°:** `|Q| = 1.233` both routes (coh 0.974/0.978) ⇒ **`|Q|>1` excludes inversion at ANY phase**; and `arg(V)−arg(B′) = −178.1°`. 🛑 Cost **+2–13 % at 21 Hz** where V62/V88's grinding fix lives — **operator chose A=150 with the trade stated**. 🛑 **NOT a return-speed fix.**
- 🛑🛑★★★★★ [The crux is the RETURN-TO-CENTRE TRAJECTORY — and SEVEN levers died against it](accord-the-return-to-centre-crux-and-what-died-for-it.md) — operator: *"it should be as smooth as manual AND faster"*, *"wiggles/spikes on a raised flat section"*, *"effectively a steer angle rate limit"*. 🛑 **HANDS ARE OFF during the return** ⇒ the kit's `|tq|>1200` mask selects the WIND phase and **excludes the return by construction**. Ring **3.5–6.5×** robust; wiggle 1.94–2.76× marginal; 🛑 **slowness NOT established** (`p_placebo` 0.459, flips with the detector, CI fold 3.27× vs a 2.7× effect). Command is **DC for 52–70 % of the return and rings anyway ⇒ SENSOR-FED**. Dead: pre-declared V97 · the return lane (**a rack END-STOP cushion**) · `0xC520C` · `0xC6194` · AUTH · Ki · `0xC63A6`/`0xC63A4`.
- 🛑🛑★★★★★ [V96 FLEW as `7e`/`7f` while the record said V94 — and it cost work](accord-v96-flew-as-7e-7f-and-the-record-said-v94.md) — identity **byte7 b6 = 1 on 100.0000 % of 164,096 frames**, both drives fault-free. **Seventh** stale-flash-status instance; it sent an analyst to *"fly V96, S2 answers it"* when V96 had flown. ⇒ **mechanical close-out gate: `grep -n "ON THE CAR\|UNFLASHED\|never flashed" docs/STATE.md docs/BUILD-LINEAGE.md`.** 🛑 **V96's regressor was 34× over-range (M pinned at 0) ⇒ S1 AND S2 VOID**; next LSB **128–256**. Primary channel was healthy, which is what later made `|Q|` measurable with **no new flight**.
- ★★★★★ [The RAM LERP is FLASH-DERIVED and `f′ ≥ 0` is ENFORCED IN CODE](accord-ram-lerp-is-flash-derived-and-fprime-is-nonneg.md) — 🛑 `STATE.md` §A6b's *"the transfer cannot be read from the image"* is **FALSE**; closes a blocker that stopped **three sessions**. Three ungated monotone sites ⇒ holds for **any cal, any mode, any build**; flash data **14/14 strictly increasing**. ⊕ *"The 8 float coefficients"* never existed — 3 floats (two **hard ZERO** ⇒ the FIR is an **identity**) + 6 u16. 🛑 **No cal inside `FUN_0003b8f6` is a good bet for the ring** — it shapes the `A` branch; V86 flew that and got a **real null on a live lane**.
- 🛑★★★★★ [Two instruments must be allowed to DISAGREE — 4 tool-zeros + 1 inverted sign in one session](feedback-two-instruments-must-be-allowed-to-disagree.md) — `scipy.signal.csd(x,y)` = `arg(Y)−arg(X)`; an agent labelled every cross-spectrum backwards and recommended the **wrong direction**, caught only by a **replicated ~90°** disagreement. ⭐ **NEW TRAP: `ep`-relative short-format aliasing** — 15 hits, 14 base setups, **ZERO actual accesses**; *worse than a zero*. ⊕ A **filtered** zero is not a fact · `np.gradient(ang,t)` dies on **1,940 duplicate timestamps** · a LERP's quoted address is often its **`Y[0]`** (`0xC67BE` vs `0xC67C8`).
- ★★★★ [V96 BUILT — an instrument, explicitly NOT a fix; and **V95 is a VACATED number**](accord-v96-built-instrument-not-a-fix.md) — V92 base, **107 B, ZERO calibration**, 112-B cave **inside V92's proven 116-B footprint (no growth)**; V94's cut reverted **by construction**; Lever B asserted **as a pair**. Puts **`gp-0x6b70`** (427, LSB 12.8 ct) against **`gp-0x374c>>4`** (Honda's own shift `@0x38236`, saturating at 12288) to measure the LERP slope that **blocks every `FUN_00038148` weight**. **Two slopes pre-registered, never merged** — S1 lag-0/1 = open-loop `f′` (its SIGN decides), S2 = closed-loop. 🛑 **S1 CI spanning zero = "NOT RESOLVED", not "zero".** ⚠ Two self-declared weaknesses: V92 separation is **BELIEF**, and the freeze exclusion is a **wire-side heuristic** — the `gp-0x67fa` gate's boolean is **never stored**, so a cave cannot read it, and the cheap superset **would silently read "live" while frozen**. 🛑 **V95 = 3 artefacts in 2 hours ⇒ burned; DEAD hashes `ad8643c1…`/`3a791446…`.**
- ★★★★★ [The LKAS authority curve is VIRGIN on all 90 images — and the operator drives on its KNEE](accord-authority-curve-is-virgin-and-the-override-sits-on-its-knee.md) — mode-7 records `0xE547C`/`0xE5404` (primary) + `0xE52FC`/`0xE5284` (blend) take authority **254 → 0 across raw 2240–2560**, and the **measured median override torque is 2235 = ONE COUNT below the first knot.** ☠ **`0xC64B8` is DEAD** — structurally a hard authority kill, but at mode 7 **both arms deliver 0** wherever it could fire, so stock ≡ V37 bit-identical; do not re-propose it. 🛑 The curve targets the **~0.5–1 Hz SURGE, NOT 6–9 Hz** (already refuted there five ways). 🛑🛑 **Honda collapses authority when the driver pushes — the ONLY defensible change is MONOTONE-NON-INCREASING.** ⚠ `gp-0x674e` = 7 is inferred, never read on-car; **modes 28–39 have `Y[last]`=51, not 0.** V95 carries a rung.
- 🛑🛑★★★★★ [The six `FUN_00038148` weights are blocked by an unresolved **SIGN**, not by magnitude](accord-fun38148-weights-have-an-unresolved-sign.md) — **`gp-0x6b70` is a PID REFERENCE that gets SUBTRACTED, not an aggregator addend**, so a weight change's sign depends on `sign(iVar6)` and the **local slope of a RAM-resident LERP** — neither known. Struck **`0xC63A6`** (virgin on all 85 images, single reader `ld.hu 0x73a6,tp,r15 @0x381ca`, zero writers) despite it being a second independent multiplier on the one lane whose direction IS measured. 🛑 **`get_xrefs_to` returned a FALSE ZERO** — the tp-relative blind spot. ⚠ Open contradiction: the inversion model says `0xC63A0` 1024→2048 flips damping→inverted, but that value **flew four times and measured INERT**. ⇒ **a lever whose sign is unresolved is a coin flip; this is how V94 reached the car.**
- 🛑🛑★★★★★ [V94 FLEW AND WAS ABORTED — the lane is a DAMPER and the build removed 6/6ths of it](accord-v94-flew-and-the-lane-is-a-damper.md) — route `7d`, **fault-free**, **still on the car**: *"vibrated the entire car, I decided it was not safe to drive."* Motor accel **3–7× up above 9 Hz**; 18–31 Hz coherence the **highest in the corpus**. `gp-0x6b26` measured at **+137°/+139° vs WHEEL rate** ⇒ **+518/+565 counts of POSITIVE `Re(Z)`** ⇒ **the first measured `d(symptom)/dK` this lever has ever had, and it says UP.** The code byte (`0x55E10` `sar 3`→`sar 1`) is **EXONERATED** — the regression is the CALIBRATION. 🛑 But UP has been tried 13× and `K·α` **cannot measure `K`**. Revert candidate V92 on the shelf, **unflashed**.
- 🛑🛑★★★★★ [THE SYMPTOM REGIME IS ENGAGED + HANDS-ON + **OVERRIDE** — and `steeringPressed` excludes it by construction](reference-accord-steeringpressed-mask-excludes-the-symptom-regime.md) — the mask is `|STEER_TORQUE_SENSOR| > 1200`, a threshold on the **numerator of `Re(Z)`**, and **override is `steeringPressed == True` by definition** ⇒ **every `Re(Z)` number the kit ever produced came from a mask that excludes the condition the operator produces the symptom in.** Scored right (band power, grip matched on BOTH arms): **OVR/MAN-ON 1.43–2.90×, median ~2.2×, 10/10 routes / 9 builds.** ⇒ *"literally every bad symptom is LKAS engaged only"* **CONFIRMED**; ~55 % engagement-attributable; **an LKAS-gated lever is back on the table.** 🛑 Only **SEVEN** override runs corpus-wide reach 5.12 s.
- 🛑★★★★ [Two named 6–9 Hz mechanisms DEAD in the override regime — and a NEW ~0.5–1 Hz surge](accord-override-surge-and-two-dead-mechanisms.md) — authority-collapse-curve refuted **five ways with perfect exposure** (crossings 0.47–1.69 Hz; 88–95 % of authority energy in 0.5–3 Hz; **correlation INVERTS against its own control**, OVR −0.194/−0.255 vs MAN-ON +0.400/+0.495); sign-guard relay dead (request duty **1.0000**, and openpilot winds **UP 6.7–15×** when overridden). ★ **NEW:** authority pinned at exactly zero **17.5–40.5 %** of override time while openpilot winds up 10× ⇒ a real surge — **at ~0.5–1 Hz, NOT 6–9.** 🛑 No symptom attached to it yet; **operator has not been asked.**
- 🛑★★★ [CAN 427 is RECTIFIED — aliasing runs on `2f`, and the law is NOT `f mod 25`](reference-accord-427-is-rectified-and-folds-26to31-into-2to12hz.md) — `|2f − 50·round(2f/50)|`; 26/29/31 Hz fold to **2/8/12 Hz** ⇒ the band a 427 **magnitude** probe exposes is **2–12 Hz, not 19–24**, and it **cannot** separate a genuine 2–12 Hz line from a 26–31 Hz image. Carry **signed** values (or a spare-bit channel) when the distinction matters.
- 🛑🛑★★★★★ [V94 RCA — "reducing a gain" is NOT a safety class, and 133/133 green can mean a wrong premise](feedback-reducing-a-gain-is-not-a-safety-class.md) — `build_v94_tva.py:117` answered GATE 2 with *"a scalar adds ZERO phase and strictly REDUCES that lane's loop gain"*. True, irrelevant: **reducing loop gain on a DAMPER is what destabilises it.** Five compounding failures — phase never computed (10 min of desk arithmetic), the motivating null was a **closed-loop product invariance artifact** (`K·α` cannot measure `K`), direction derived and never measured (7 variants, all UP, zero DOWN), GATE 2 satisfied by assertion, and the check suite **encoded the premise as a PASS**. The last line of defence was the operator's hands.
- 🛑🛑★★★★★ [STANDING-RECORD CORRECTION: the PID's phase figure was measured at 21Hz and does NOT transfer to 6-9Hz](reference-accord-fun3a382-pid-phase-6to9hz-standing-correction.md) — recomputed from `FUN_0003a382`'s exact P/I/D combining arithmetic: net phase lag **−11° to −27°** at 6-9Hz vs the recorded **−3.3° to −5.4°** at 21Hz, P:(I+D) collapsing from ~8-10:1 to **~2-5:1** — P and D each carry an implicit ×32 from their own unity-alpha EMA that the integrator's plain accumulator never gets. **Any 6-9Hz ratchet-band argument that reused the 21Hz figure needs re-checking, not assumed to still hold.**
- 🛑🛑 [RETRACTED: Task 5 (`FUN_00022ca0`, assist-shaping) is NOT confirmed 100Hz](reference-accord-task5-100hz-syscall8-rate-divider.md) — `gp-0x6bbe`'s own flown data (route 79 freq/step/phase) contradicts 100Hz and points close to 1kHz; the `syscall8`/TCB-index derivation's "wake" functions turned out to be general kernel primitives, not confirmed task-dispatch. Task 1=1kHz (`control-task-tick-confirmed-1khz`) unaffected. **Do not use the −5.8/−7.5/−8.6dB attenuation figures this produced.** Task 5's true rate is OPEN.
- 🛑🛑 [STRUCK: `FUN_0002a93a` and `0xC6AF0`/`gp-0x6966` — both engagement-gate candidates that looked live and are not](reference-accord-two-engagement-gate-candidates-struck.md) — dead code (0 callers/xrefs since V37) and permanently-wide-open clamp (`gp-0x6966`≡0 every V31+ build, `v54-flashed-authority-measured`) respectively; the record already held both answers before either was proposed.
- 🛑🛑★★★★★ [`gp-0x6b26` IS AN **INERTIA** TERM, NOT A DAMPER — the dose family's premise is wrong](accord-gp6b26-is-inertia-not-damping.md) — `FUN_00041464` pinned in ASSEMBLY: `0x41602 sub r7,r9` is a **first difference of the filtered EPS-motor rate** ⇒ `gp-0x6c2c` is **ACCELERATION**, so `gp-0x6b26 = −K·α` and Path 1's unweighted `add` gives `(J+K)·α = T_driver` — **apparent inertia RISES by K and the term DISSIPATES NOTHING.** It cannot damp anything, so it could never have fixed an anti-damping problem. 🛑 `build_v91_tva.py`'s *"genuinely DISSIPATIVE, it opposes motor rate"* is **WRONG** and every "damping-comp" label inherits it. ⚠ The decompile alone CANNOT settle this — Ghidra folds the reset path into `uVar8 = uVar16`, making the difference look identically zero. ⊕ **13 builds touched this LERP family and every one RAISED it or restored stock; DOWN has never been tried** ⇒ V93/V94. ⊕ The telemetry attempt returned MIXED/UNRESOLVED with a **5× skew swing** — a two-message signed reconstruction is only valid for a LARGE, sign-STABLE lane (it worked for `gp-0x6bbe` at p50 76.8 ct, not for this at p50 4.8).
- 🛑★★★★ [SEARCH THE KIT'S OWN RECORD BEFORE NAMING A CAUSE FOR A NULL](feedback-search-the-kit-before-naming-a-cause.md) — named *"the car reads mode 24 in both states"* as the leading cause of the V91/V92 null, wrote it into a memory, the lineage, a published artifact **and a whole build proposal** — then **one grep refuted it**: V73 probed the exact index byte `gp+0x63fd` over 104,061 frames and watched it change **8 manual → 10 engaged**, 99.09 % lag-matched. **RULE 7 was cited IN SUPPORT of the wrong answer**, which is the tell — a rule used as decoration, not applied. ⇒ **grep `memory/` and `docs/` for the cells and the mechanism BEFORE proposing a cause; ask whether the kit has ALREADY MEASURED the thing you are assuming.** Enumerating the alternatives instead killed the mode hypothesis *and* both fallback branches, leaving the honest answer: **the null is UNEXPLAINED.**
- 🛑🛑★★★★★ [The ×1.5 dose measured **INERT** — mode record REFUTED as the cause; now a candidate **T10**](accord-cbe74-dose-measured-inert-wrong-mode-record.md) — V91 (r78) and V92 (r79) both flew it, both **fault-free**. Engaged ratio **0.988 / 1.230 / 0.941 / 0.964**, every CI containing 1.00 against a pre-registered **1.50**; **MANUAL control HOLDS at 1.009**; duty **0.167 / 0.161 / 0.165** vs a needed **0.204**. 🛑 **ADDRESS CORRECTION 2026-08-12 (verified from the images): the dosed bytes are `0xD7A5C`/`0xD7A6C`; `0xCBE74` is a POINTER PAIR, byte-identical stock→V97.** ⇒ **V96's "REVERT.CBE74" reverted to V91/V92's ×1.5 DOSE, not to stock — V97 carries it.** 🛑 **The "wrong ENGAGED mode record" explanation is REFUTED** — V73 probed the same index byte `gp+0x63fd` over **104,061 frames** and it changes on engagement edges. 🛑 **Evidence is ONE identity-proven leg (V92/r79), not two** — V91 is telemetry-identical to V90. 🛑🛑 **2026-08-12: this is a candidate T10 — "the instrument is invariant to the lever"** (`y = K·α`, α is what K damps ⇒ product invariant to K in a stable loop). **V94 cut the same cell 6× and the operator ABORTED ⇒ the cell DEMONSTRABLY reaches the car.** ⇒ **not a dead lever, an unmeasurable one. Do not file it FALSIFIED.**
- ★★★★★ [`Re(Z) < 0` REPLICATED ON THREE DRIVES — and it PEAKS in the operator's unfixed regime](accord-rez-antidamping-replicated-three-drives.md) — 6–9 Hz **−3375 / −3176 / −3073** on r77/r78/r79 (coh² 0.71–0.77, shuffled ≈0.000), **within ±5 % across three independent drives**; the **sign flip to damped at ~24–26 Hz is on all three** ⇒ grind #2's band is not anti-damped. ★ **NEW: windows classified by their OWN median |rate| put the anti-damping STRONGEST in the MICRO 1–13 °/s regime — −3480 at 6–9 Hz (coh² 0.804) and −4890 at 9–12 (coh² 0.815)**, exactly the regime the operator reports as still unfixed. 🛑 An instrument/symptom ALIGNMENT, not a cause. 🛑 Ratchet 13–50 °/s = **4 windows, NOT SCOREABLE**; the hands-off coast is **1.8 s / 0.0 s** ⇒ **§C's plant-vs-firmware experiment is still unrun.**
- ★★★★★ [`gp-0x6bbe` IDENTIFIED — VISCOUS + a big engagement-conditional DC pedestal](accord-gp6bbe-is-viscous-plus-dc-pedestal.md) — first measurement ever. Flat gain **≈90 ct/(rad/s)** with phase crossing zero at ~5–6 Hz ⇒ **viscous / rate-derived**, not inertial, not stiffness. 🛑 **REFUTES the structural flag that justified the bit** ("same-signed as the raw torque sensor ⇒ REINFORCING"): measured phase vs column torque is **+140°…+164°** — opposite-signed — and is **fully predicted by `boost ∝ rate` alone**, so no torque-derived component is evidenced. ★ **The DC pedestal is the story: `|gp-0x6bbe|` p50 = 73.6 ct FLAT across 0–6 °/s, and `P(<0)` = 0.8865 engaged vs 0.4990 manual** ⇒ engaging pins an aggregator lane to a near-constant sign and offset = ***"the ratchet is just on a DC LKAS command"*** in an instrument. ⚠ Signed reconstruction has a real **inter-message skew** (28–56° at 7.79 Hz), controlled by a −2…+2 shift sweep (moves <1.5 %).
- 🛑🛑★★★★ [The RETURN-CENTRE lane and the DETENT are **DEAD ENGAGED** — and one rung is INDICTED](accord-return-centre-and-detent-dead-engaged.md) — `gp-0x6b62 ≠ 0` and the `gp-0x6bda` outer LERP gate both read **0.0000 over 75,227 engaged frames**; `b4 ≡ b5` **exactly** on all 87,317 frames (gate shut ⇒ `gp-0x6b64 ≡ 0` ⇒ lane ≡ 0) ⇒ **a flat −1024 bias, not a relay. Do not propose a detent/dwell lever.** 🛑 **But `byte7 b6` (dwell snap) is a DEAD rung**: `(gate=0,snap=0)` on **99.898 %** in **3 runs, longest 855 s, 0 % adjacent to a falling edge** — the pre-registered SUSTAINED-RUN condition that **indicts the rung map**. Read it as a null on the GATE (the V64 class), never as "the detent never snaps". ⊕ byte4's b4/b5/b6 are NOT indicted — they fire and pass `(b6,b5)=(1,0)` = 0.
- ★★★★ [The THREE-ROUTE same-firmware PLACEBO FLOOR](accord-three-route-placebo-floor.md) — because the V91/V92 edit is inert, **r77/r78/r79 are three drives of the SAME functional car** — the kit's largest placebo set. Column-torque band spread (max/min), MICRO regime: 6–9 Hz **1.37×** · 18–22 **1.31×** · **26–31 Hz 1.99×** · 32–38 control 1.54×. Static regime is tighter (18–22 **1.09×**). ⇒ **any future band claim must beat its own row; no grind-#2 claim below 2× is supportable.** ⚠ MICRO rests on 36 windows for r78/r79 — an upper bound on precision, not a settled floor.
- 🛑🛑★★★★ [Name superseded hashes DEAD — omitting them is NOT enough](feedback-name-superseded-hashes-dead-not-merely-omitted.md) — **a VERIFIED artefact for a SUPERSEDED design is MORE dangerous than an unverified one: everything about it looks correct, INCLUDING ITS ASSERTION LOG, and a hash reported in a transcript OUTLIVES the artefact it names.** The instance: a real V92 cut, **182/182 assertions, from-disk verified**, carried the OLD rung map (`b4 = sign(gp-0x6abc)`, 110 B cave, no 427 `sar` fix), was superseded before flight and **never flew** — but its hashes stayed live in the transcript with a full passing log, and **the only tell was a `6ABC` token buried in the old filename.** ⇒ **On a re-cut, name the dead hashes DEAD in the record; stripping them from the docs is necessary and NOT sufficient — omission is invisible, a DEAD marker is not.** ⊕ The `SUPERSEDED-DO-NOT-FLASH-…` rename fixes the FILESYSTEM and does nothing for the TRANSCRIPT; both need doing. ⊕ Caught by the BUILDER, pushing back on the orchestrator's own filename which had called them a "dry run" — they were a real cut, and that distinction IS the hazard.
- 🛑🛑★★★★★ [The anti-damping is NOT the PID](accord-anti-damping-is-not-the-pid.md) — at 6–9 Hz P **−0.145** / I **−0.053** / D **+0.077** ⇒ **NET −0.121 = DAMPING**, while measured `Re(Z)` = **−3375 ct·s/rad = ANTI-DAMPING** at coherence 0.769 vs a shuffled 0.001 (221 windows / 884.5 s, 37× V89's exposure). ⇒ **it is another aggregator lane, or the PLANT — every remaining firmware candidate has to answer this.** ⊕ `Re(Z)` **flips sign at ~26 Hz** (anti-damped 2–26, damped 26–35) ⇒ **grind #2's band is not anti-damped at all.** 🛑 **The decisive experiment is the MANUAL HANDS-OFF COAST and the whole corpus has 2 windows / 21.4 s of it** — ~15–20 min of driving, and it yields 12–16 clean ring-down edges free. 🛑 Two opposite sign conventions are in play; confusing them inverts the result.
- 🛑🛑★★★★★ [Six levers CLOSED — five on ARITHMETIC, not on a null](accord-six-levers-closed-on-arithmetic.md) — **`0xCBE74`** (int32 wraparound at **1.6005×** ⇒ **×1.5 is 94 % of the lever's ENTIRE range**, and delivered damping is **5–69× below the resolvability floor**) · **`0xC63A6`** (micro-regime p50 **7.1 ct** = 0.22 % of the clamp, against a **PRE-REGISTERED ≲32 ct** line) · **the `Kd` cut** (D pumps only 2–12 Hz and **DAMPS 16–35**; costs **−0.217 at 18–22** and **−0.336 at 26–31** to buy +0.077) · **K1** (collinear with `|model|` above 1 °/s; discriminating cell **0.63 %** of engaged frames) · **term 0 / mixer lane 2** (severed by `0xC616C` = 0) · **`0xC520C`** as the return explanation (**misses by 8.3×**). 🛑 An arithmetic kill cannot be re-opened by more exposure or a bigger dose.
- 🛑🛑★★★★ [`0xC616C` = 0 — NEVER RAISE](accord-c616c-never-raise-driver-torque-relay.md) — virgin on every build and it **looks like a free untried lever**. It feeds `r14 = sign(driver torque) × cal` into `gp-0x6b76` → mixer lane 2 → `gp-0x6b4a` → **term 0 → the driver-feel reference `gp-0x6ad6`**. **Raising it is a Coulomb relay on DRIVER-TORQUE SIGN — the V80 class, arguably worse than `0xC4080`**, because driver-torque sign reverses on every micro-correction. ⊕ Its zero is also what makes **mixer lane 2 structurally inert today**, so a phase characterisation of that lane is moot. Three other gains in the same cascade are independently 0.0 ⇒ shipped-disabled (BELIEF, not traced).
- 🛑★★★★ [The rate-channel rule, and its SCOPE](accord-rate-channel-rule-and-its-scope.md) — **PHASE/IMPEDANCE ⇒ `rate_f` (`0x18F`)**: `tq` and `rate_f` share a frame so the skew **cancels exactly**; using `rate_c` **inverts the sign at 18–22 and 26–31 Hz at identical coherence** (−0.336 DAMPING becomes +0.184 PUMPING) — **the opposite build decision from the same data.** 🛑 **ABSOLUTE MAGNITUDE ⇒ `rate_c` (`0x14A`)**: measured slopes vs differentiated angle are `rate_f` **0.743–0.767 (reads ~24 % LOW)** vs `rate_c` **0.952–0.963**. ⇒ **STATE WHICH CHANNEL YOU USED, EVERY TIME.**
- 🛑★★★★ [A same-firmware PLACEBO PAIR is mandatory for any cross-build claim](accord-placebo-pair-is-mandatory-for-cross-build-claims.md) — route 77 ÷ route 75 is **BYTE-IDENTICAL FIRMWARE** and returned `e_6-9` **1.288 [1.017, 1.661]** with the **32–38 Hz band contrast ALSO excluding 1.00**; r77 ÷ r76 gave `e_18-22` **1.333 [1.001, 2.307]**. ⇒ **the band contrast does NOT rescue a thin cut.** Honest floors: **±16–22 % contrasted, ±33 % raw.** ⊕ A no-hypothesis placebo BAND (`e_10-16`) is the cheap tell for a stratum artefact. ⊕ The cheapest real fix is not statistical — **fly the next build on the SAME ROUTE.**
- 🛑★★★★ [`gp-0x6afe` ≡ `gp-0x6b4e` ≡ 0, ALWAYS, on every build](accord-gp6afe-gp6b4e-are-always-zero.md) — its source array `gp-0x62c8[0..10]` is written to literal **zero** or not written at all (**role 7 never appears in `0xC4124` on any build**), and the `.data` boot initialiser is **22 bytes, all zero** ⇒ the shaper's `iVar45 = gp-0x6afe + uVar34` reduces to `iVar45 = uVar34` ⇒ **there is NO second, independent LKAS injection at the final stage.** **CORRECTS the starred `accord-aggregator-reaches-motor-via-gp6acc-bridge` ("CAN/arbitration term") and `reference-accord-shaper-fun42af8` ("feed-forward addend"), and the team-lead's own brief.** ⚠ Its unity weight `0xC63A8` = 1024 is still correct — a unity weight on a zero lane is still zero.
- 🛑🛑★★★★★ [TERM 0 (`gp-0x6b4a`) IS STRUCTURALLY ≡ 0 — CORRECTED 2026-08-13](accord-gp6b4a-is-a-second-direct-lkas-term.md) — it is unweighted/negated with **no cal on its path** (instruction-verified) and its ±25600 gate equals its own clamp, so *structurally* it could rail `gp-0x6ad6` alone — **but it carries no signal.** Crux = the `FUN_00025c32` struct map: **+2→`gp-0x62e0`→term 0, +4→`gp-0x62f8`**; **9 of 10 registrants write a hard `r0` into +2**, and the 10th (slot 2) carries `gp-0x6b76` ∈ {0, 0x7FFF} because clamp cal `0xC616C`=0 annihilates it and 0x7FFF fails its own gate. ⇒ **`gp-0x6ad6` is entirely the OTHER SEVEN terms; term 0 is NOT what rails the ±8192 `0xC6200` clamp.** ⊕ The **4× LKAS gain reaches `gp-0x6b4c`, NOT term 0**, and is UNSATURATED end to end (its clamps tracked it; next fixed clamp 5.0× above) ⇒ it IS delivering 4×. ⚠ CORRECTS the `6b4c`/`6b4e` array map; reproduces `accord-c616c-never-raise-driver-torque-relay` independently.
- ★★★★ [V90 flew — PROBE-ONLY, so it is the CONTROL CONDITION](accord-v90-flew-probe-only-control-condition.md) — route `77`, **17.91 engaged min / 86.41 %**, fault-free, **identity PASS `b4 == 0` on 124,362/124,362 frames** (impossible on V86B–V89). **Byte-identical to V89 in every cal cell**, so the operator's *"grind #1 still exists · micro-ratcheting still exists · grind #2 on highway-speed curves or lane changes"* is a control, **not a failed fix**. Deliverables: **`gp-0x6b26`'s full distribution with clamp duty EXACTLY 0.000000 in every stratum** (engaged p50 5.5 / p99 114.3 / max 319.1 vs ±511 ⇒ **not a relay today**) and **the observer gate `gp-0x6c00` measured for the first time — it NEVER fails, 0 of 124,362 frames.** 🛑 The clipping ladder (1.60×/2.75×/4.45×) is **NOT a dose budget.**
- 🛑🛑★★★★★ [The `0xCBE74` friction row has ZERO clean flights on a LIVE column](reference-accord-cbe74-friction-row-zero-clean-flights.md) — byte-verified by dereferencing `0xCBE74 + mode*4`: **V73 wrote mode 10 only (DISENGAGED) ⇒ its clean flight tests NOTHING**; ×1.5 on modes 24/26 flew **twice — V74 and V75 — and BOTH hard-faulted**; V76's flown artefact had it reverted. 🛑 **Inverts the standing attribution: V73 carried `0xC407E` = 850 and flew clean** ⇒ the row moves **exonerated → OPEN SUSPECT** (64 differing V73→V74 runs ⇒ cannot be pinned). ⚠ At MODE level it is 1-for-1, not 2-for-2: V74 faulted in MANUAL where m24 was byte-Honda; V75 faulted ENGAGED where m26 was ×1.5. ⚠ **Two artefacts share the V76 number — the BASE column is the discriminator, a glob is not a check.**
- 🛑★★★★ [AN ADDRESS IS NOT A MODE](reference-accord-an-address-is-not-a-mode.md) — three instances in one session (V69's mode-10 `gain_B`; "mode 24 dosed" read off a stock table; `0xD6A5C` named as mode 24 when it is **mode 23**). Byte-verified friction mode map m10/23/24/25/26/27 → `0xD2A44`/`0xD6A54`/`0xD6A64`/`0xD7A44`/`0xD7A54`/`0xD7A64`. 🛑 **Y is at record base + 8**; writing it at `base+2` lands in the X array, compared **UNSIGNED** (`−29490` → `36046`) ⇒ flat `Y[0]` at every speed = **a silent, plausible 5× highway increase that byte-verifies and drives.** Rule: dereference `0xCBE74 + mode*4` and print the mode beside every address in a spec.
- 🛑★★★★ [A COUNT is not a PHYSICAL FACT](feedback-a-count-is-not-a-physical-fact.md) — four instances: r76's index "drift" (bookkeeping, not timing — its payload age is the cleanest of three routes); the `gp-0x6752` writer census (a disp16 scan is blind to the 6-byte form ⇒ a lower bound, not a count); V86's rung map applied to V89 (**V86B swapped b5/b6 and V87–V89 inherit V86B**); and "payload age vs the newest `0x18F`" = 0.000 ms, which **assumes its own conclusion**. Rule: name the physical quantity, measure THAT, and ask what the metric assumes.
- 🛑★★★★ [Neither Ghidra nor Python alone can ENUMERATE](feedback-neither-ghidra-nor-python-alone-is-complete.md) — `search_instructions` returned **73 `ld.h` hits with `truncated:false` against 3 more real sites** in unanalysed regions; a naive Python scan misses the 6-byte form (`disp=(sext16(hw2)<<7)|((hw1>>4)&0x7F)`); and 🛑 **operand-text search cannot see register-indirect writes at all** — `clr1 0x1,0x6aec[r18]` behind `movhi -0x121,r0,r18` is a live writer of `gp-0x1514` whose text never contains `1514`. ⇒ **"0 writers" from a text search is not a null**, and GATE 1 rests on exactly that null. Both tools, set-differenced, every disagreement adjudicated.
- 🛑★★★★ [Three signal identities re-asserted wrongly in one session](reference-accord-three-signal-identity-recurrences.md) — `gp-0x6a5e` is **voted VEHICLE SPEED** (settled 2026-07-29; the earlier version already cost V44/V47) · `gp-0x4f50` is a **RATE** — the wrap-correction on a modular difference proves it, and `0x8000` on the stored angle is an **INIT/INVALID sentinel** · `gp-0x6afe` **≡** `gp-0x6b4e` (the mixer's tail-call `FUN_00042ac6` writes it ⇒ one probe rung, not two). 🛑 **All three were caught by a teammate cross-checking an INHERITED claim, not by the agent that made it, and two were weeks old** ⇒ an inherited signal label is a **BELIEF** until re-derived from the producer's arithmetic.
- ✅★★★★ [POLARITY VERIFIED: more modelled friction = MORE assist](accord-friction-polarity-more-friction-is-more-assist.md) — nine links, Ghidra-traced: friction subtracted from the model → residual ↓ → `gp-0x6b70` more negative → `gp-0x6ad6` (**a torque-tracking REFERENCE, not a motor torque**) ↓ → `error = measured driver torque − reference` ↑ → PID ↑ → aggregator (`mov`, `add`×8, **no negation**) ↑ ⇒ **LIGHTER wheel; V89 does NOT fight LKAS.** Coulomb confirmed 5 ways (`gp-0x6abc`←`gp-0x4f50` = motor rate; companion cal `0xC646E` is the kit's own "INERTIA gain"). 🛑 **V56 muted this lane and got a null — but scored on 15–26 Hz, NOT 6–9 Hz, and route 24 is not on disk.** Band-scoped, carried as general.
- ★★★★★ [V89 BUILT — the first build to touch the PLANT MODEL](accord-v89-built-the-plant-model-friction.md) — `0xC40D2` 102→204: K1, the `|model|`-proportional **modelled Coulomb friction** in `FUN_0003b8f6`, 2.000× (**1 reader / 0 writers, virgin on all 88 builds**), + a cave probe on `gp-0x6ae2` = friction×1024. The chain is a **DISTURBANCE OBSERVER** (`residual = MODEL − ACTUAL`) ⇒ under-modelled friction is chased = stick-slip. **8 bytes, zero unattributed, 50/50 CRC.** 🛑 `0xC4080` (NEVER-RAISE relay hazard) untouched. 🛑 Direction measured, **K1≡gate is BELIEF**. ⚠ may feel notchier on-centre.
- ★★★★★ [Engagement amplifies 6-9 Hz 2.8× via a COMMAND-PROPORTIONAL COULOMB RELAY](accord-engagement-amplifies-6-9hz-via-coulomb-relay.md) — full corpus, 30 routes / 284 min / 235 blocks. **`eng` band contrast +0.413 [+0.146, +0.667]** ⇒ engaging multiplies the ratchet band **2.8×**, 1.5× more than a control band, **and it does NOT grow with wheel rate** (+0.022 [−0.070, +0.116] ⇒ **REFUTES the earlier +0.144**). 🛑 **⇒ nothing argues for limiting the LKAS command's angle rate — the target is a GAIN.** 🛑🛑 **De-relaying it made things WORSE: `0xC40BC` 600 → 2.89× vs 6000 → 6.58× (contrast +0.682 [+0.213, +1.166]) ⇒ the standing "FREEZE at 6000" is CONTRADICTED; the car is at 600 and that is better.** ★ Column FRICTION damps this mode — grip −0.655 vs control −0.266, disjoint. 🛑 Loader bugs: per-segment caches were skipped; `_{tag}_*_plain_image.bin` misses `_v67_plain_image.bin`.
- ★★★★★ [The ratchet scales with WHEEL RATE — the operator's own axis](accord-ratchet-scales-with-wheel-rate.md) — engaged/manual 6–9 Hz **1.16× at 2 °/s → 3.94× [2.19, 6.70] at 100 °/s**; corr(log rate, log speed) = **−0.640** so D5's creep→highway AMPLITUDE decay is partly a rate effect (the FREQUENCY result stands). ★★ **The mode is strongly damped by DRIVER GRIP: −0.720 [−0.918, −0.500] vs control −0.216, CIs DISJOINT.** 🛑 Band-specificity of the rate term is **BELIEF** (contrast +0.144 [−0.004, +0.267]). 🛑 Two traps: per-band order vetoes build different window sets; `_cache_r66`/`r66x` are the SAME route and double-counting alone flipped the headline CI.
- 🛑🛑 [REFUTED — the damper's "cannot reach micro regime" claim](accord-base-assist-damper-cannot-reach-the-micro-regime.md) — V75/V76/V80 had BOTH FactorC+FactorE dead zones open AND FLEW (routes `5e`/`65`/`66`); only route-73/V88's Honda-stock zero result still stands. Open again, not closed.
- 🛑 [The Lever-B × rate discriminator is UNDERPOWERED](accord-leverb-rate-discriminator-underpowered.md) — contrast −0.101 [−0.381, +0.298], **CI half-width 2.4× the effect** ⇒ **r24's engaged arm is NOT exonerated**. Closing it needs ~4× the episode blocks: matched ENGAGED *and* MANUAL exposure at matched WHEEL RATE. **The binding constraint on V89 is EXPOSURE, not analysis.**
# Memory Index — Honda EPS Firmware Project

- ★★★★★ [The ~7.79 Hz RATCHETING is a LINEAR LOOP OSCILLATION](accord-ratchet-is-a-linear-loop-oscillation.md) — measured **NOT a relay** (odd/even comb 0.858 [0.739, 1.000] vs a control firing at **1.204 on 15% injection**; PLV z ≤ 1.05; no 3rd harmonic ⇒ **<15% can be relay-generated**) and **NOT a plant resonance** (the wheel-on-bar mode is **12.8 Hz, ABOVE it**). ⇒ **a linear loop mode set by accumulated estimator lag** — fits speed-invariance (+0.074 Hz/m/s vs order-2's +0.961), engaged-only, in the bar but not the command. 🛑 **⇒ the lever class is PHASE/LAG, new since V38.** ⊕ NEW: a discrete engaged-only **~20.90 Hz** creep line on V85/V84, absent on V81 — **that** is the 18–22 Hz elevation, not a floor shift. ⚠ micro vs macro could **not** be separated.
- ★★★★ [V85 flew route 6e: the lever DELIVERED, the bands are a NULL](accord-v85-flew-lever-delivered-bands-are-null.md) — fault-free and the **cleanest flight in the lineage** (`STEER_STATUS` = {0: 43,641}). `0xC40BC` cut relay saturation **33.3% → 4.6% engaged (7.21×)**, both duty predictions hit. 🛑 **FREEZE at 6000 — and the "N is flat at 6000" reason was WRONG**: the ring rides on a bias 5–10× its amplitude ⇒ the **biased** DF is the instrument (pinning 0.0000 / 0.043 after 20.3×). 🛑 **Every band is a null** [0.63, 1.50]; **operator: ratcheting STILL UNFIXED.** 🛑 **EXPOSURE-LIMITED — 22.4 s ≥80 km/h ⇒ no highway/26–31 Hz verdict.** 🛑 `probe_build=['V80']` in `r6e.npz` is a **stale heuristic, WRONG**.
- ★★★★ [The plant-model → residual → assist-aggregator chain, end to end](accord-plant-model-residual-aggregator-chain.md) — `FUN_0003b8f6` → `FUN_0003bc20` → `FUN_00038148` (`gp-0x6b70`) → `FUN_00037fe6` (`gp-0x6ad6`) → PID → motor, with dual-encoding censuses. `0xC64AD..B3` are **0/1 ENABLE FLAGS, not gains** (`0xC64B0` gates `gp-0x6b70`); `0xC6200` is **NOT governor-shared** but **3 of its 15 readers are unidentified**. 🛑🛑 **THREE flatten-into-a-relay hazards: `0xC4080` NEVER RAISE · `0xC63AE` never →0 · `0xC6200` never < Y[0].** 🛑 `gp-0x67fa` ≡ **{11} alone** ⇒ **`0x454FE` is MEASURED INERT**; `gp-0x671a` **ruled out**. ⚠ the RAM LERP's `Y[0]` is `ep`-relative and **UNRESOLVED**.
- 🛑 [Eight levers killed on evidence, 2026-08-09](accord-levers-killed-2026-08-09.md) — `gain_A` **ENGAGED-INERT** (`0x3AB5E` overwrites it with 512; twice-failed pre-registered test) · **Lever A DO-NOT-RESTORE** (ungated ⇒ V62/V65 manual verbatim; ⚠ its int16-overflow leg **WITHDRAWN**) · the 13-point LERP **DEAD** (`gp-0x6a10` is **absolute steering angle**, 88.6% in the flat segment) · **FactorD structurally inert** (FactorC's `X[0]`=34.97 km/h, `Y[0]`=0, ×3 confirmations) · `0xC63A0`→2048 **INERT** (`ch₀`=0 on 98.8% of engaged frames) · `0xC61F6`→0 **DO NOT** (deadband is the relay's dual) · `0xC61D6` **already rejected**. 🛑 **⇒ THIS FIRMWARE HAS NO FREQUENCY-SELECTIVE LEVER.**
- ★★★★ [V86 BUILT, UNFLASHED — the kit's first PHASE/LAG lever](accord-v86-built-the-frequency-lever.md) — one cell `0xC40D4` **573 → 286** (command-branch EMA, α 0.1399 → 0.0698). Pre-registered as a **FREQUENCY ratio `f(V86)/f(V85) ∈ [0.797, 0.875]`** = 3.3 FFT bins; **FALSIFIED if it stays at 7.79 Hz**; **AMBIGUOUS is a declared outcome, not a null**. 🛑 **An EMA CANNOT limit max angle rate — `|H(0)| = 1` exactly for every α** (verified to 1.000000000000). Mode-proof (573 unique in `[0xC4000,0xC4200)`, no repeating stride). Probe `b7⇒b6`/`b5⇒b6` are exact **duals** of V85's ⇒ identity is free. ⚠ **V86B ON HOLD** — settled by a zero-byte creep protocol, not a build.
- 🛑 [A falsifier only fires if it COULD have fired](feedback-a-falsifier-only-fires-if-it-could-have-fired.md) — V84's `b7`/`b6` tested `|r24| ≥ 1024` on a lane capped at `|r1| = 201` ⇒ 0.0 in **both** arms, misread as "out of force"; and V85's damper abort "passed" on **22.4 s** of exposure. **RULE 5 applied to the INSTRUMENT** — the kit has now made the error at both ends.
- 🛑 [A count-only record census is blind to a write into an already-non-stock record](feedback-record-census-must-assert-every-record.md) — assert **every** record byte-identical to the **BASE** unless declared. It is **340 pointer slots (10 arrays × 34 modes), not 58**; **34 non-stock records**, including modes **32/33**.
- 🛑 [Smoke-test GhidraMCP at agent spawn — `check_tools` lies](feedback-smoke-test-ghidra-tools-at-agent-spawn.md) — registration is scoped to the session at spawn time; three tracers were **silently blind all session** while `check_tools` said "callable". **First call must be `get_current_program_info`.** A blind tracer's output is shaped exactly like a genuine null.
- 🛑 [A right answer for the wrong reason is a coincidence](feedback-right-answer-wrong-reason-is-a-coincidence.md) — V85's *"N is flat at 6000"* reached the right cell and the **wrong action** (revert, not freeze). Confirmation of the outcome is not confirmation of the model; record the corrected mechanism in place even when the recommendation is unchanged.
- ★★★★★ [V84 flew as route 6d and FIXED the highway ring](accord-v84-flew-and-fixed-the-highway-ring.md) — 26–31 Hz burst duty **25.1% → 2.54% on 3.4× the exposure**, the kit's second measured fix. 🛑 **RETRACTS V83a's "damper-dose model of the ring is FALSIFIED"** — V83a never removed the damper (mode 27 kept it) and had 19.2 s of highway. ⊕ **V84 is byte-identical to V67/V68 at every grind-relevant cell** ⇒ the rate lane is maxed out at V67's level. S2/S3/S4 all FAIL.
- ★★★★★ [FUN_0003b8f6 is a Coulomb relay proportional to the COMMAND](accord-fun3b8f6-coulomb-relay-proportional-to-command.md) — 1 kHz, undocumented until 2026-08-09. `ratio` saturates at 50 counts against a 13000 gate ⇒ pinned across **99.62%** of its range; **relay index 7.87 vs V80's 3.27**. Lever `0xC40BC`, **1 reader / 0 writers, virgin on all 84 builds**. 🛑 V56's full-lane mute bounds it: **do not expect 18–22 Hz to move.**
- ★★★★★ [The loop does NOT close through openpilot](accord-loop-does-not-close-through-openpilot.md) — the bar **leads** the command by **18.5 ms**; three methods agree with a working 8× positive control. **91–98% of bar band power is INCOHERENT with the command** ⇒ command-side filtering has nothing to remove. 🛑 Also: the *"`0x18F` is one frame stale"* rule is a **cache-extractor artefact, not a bus property**.
- ★★★★★ [The firmware ADDS torque to the bar, engaged, at low speed](accord-firmware-adds-torque-to-bar-engaged-at-low-speed.md) — at matched `θ̈` and matched speed, engaged/manual = **2.77× (6–9 Hz)**, **1.66× (17–23 Hz)**, control **1.04**. Purely inertial coupling would give 1.00. ⚠ The control FAILS above 8 m/s, so only the 2–8 m/s row is clean. ⊕ Wheel-on-bar mode is **12.8 Hz — between the two symptoms** ⇒ a single-gain `θ̈` feedforward arrives at S1 **inverted**.
- ★★★★★ [LKAS authority has been FROZEN since V38](accord-lkas-authority-frozen-since-v38.md) — gain 3564 / setpoint 16384 / clamps 2048 **byte-identical on all 53 images V38→V84**, ~30 flights. The excitation variable has never been an arm. ⊕ But **command buys DUTY (5–45×), not AMPLITUDE (≤2.3×)** — the standing rule survives. 🛑 `0x454FE` **cannot execute** (state 4 fires 0/123,277) and V42 was never single-lever.
- [Run as a pure orchestrator; verify at the end](feedback-orchestrator-mode-delegate-verify-at-end.md) — operator wants the lead to delegate all digging to subagents and only open Ghidra/fine-details to finalize the picture or resolve a subagent dispute.
- [Damper variant ROW vs INDEX trap](reference-accord-damper-variant-row-vs-index-trap.md) — 3-stage indirection (EEPROM ID → ROW via 16-key match → INDEX via a second table → ptr array); this car's PN → row 2 → index 10 → the SAME tables V44/V47 edited, so their nulls likely are real.
- [Double-check subagent conclusions; never relay unverified](feedback-verify-subagent-conclusions.md) — in orchestrator mode, run a minimal independent confirmation of any decision-bearing subagent claim (tight script / paper decode / math check), even in the safe direction. Operator emphasized twice (2026-07-23/24); extends the older [[feedback_verify_subagent_claims]].
- [Iron rule: full-frame logging on every live CAN op](feedback_full_frame_logging.md) — log every TX + relevant RX (not summaries); working transport = Panda(disable_checks=True)+set_safety_mode(3) ELM327 bus 1 (ALLOUTPUT/set_obd don't reach the EPS)
- [Joey works style: rigorous validation before vehicle flash](feedback_rigorous_validation.md) — full byte diff > spot diff; ghidra before moving on; don't declare premature victory
- [Lightweight Python/PowerShell over token-heavy Ghidra MCP](feedback_lightweight_inspection_over_ghidra.md) — for byte/endianness/entropy/signature questions, script the raw .bin; reserve Ghidra for decompiler/xrefs
- [Build new RE tooling when the target needs it (operator-authorized)](feedback_build_re_tools_when_needed.md) — write new Ghidra loaders/processor specs/analyzers/disassemblers when kit tooling doesn't fit the target; don't stop at "tool unsupported"
- [Joey's framing: 3-node balance + process-not-degrees](feedback_3node_and_process_framing.md) — model+firmware+controller are ONE coupled system (don't single-node the hug); optimize good-driving-ness not proxy metric degrees (Goodhart)
- [Stress-test relayed LLM advice against the bytes before implementing](feedback_stress_test_llm_advice.md) — external AI = good idea generator, poor oracle for car-bricking specifics; build-then-adversarially-verify catches confident-wrong claims
- [★ Accord UDS RAM telemetry WORKING (V31U, DID 0x4801)](reference_accord_uds_did_read_surface_a160.md) — flashed 2026-07-10, live-validated. Req **0x18DA30F1**/resp 0x18DAF130 (NOT …80F1; A160 %hdr=30). RDBI table TRUE base **0xB77FC** (NOT 0xB7800), stride 0x14 [did/len/gate/session/group/handler_ptr]. LIVE per-DID dispatch = handler_ptr@entry+0x10 called w/ ctx ptr (FUN_000209ea, per-tick from w_steer_control_task); groupID jump-table 0xB7568 is orthogonal (masked scalar). DID 0x4801=idx1@0xB7810. Working patch = handler_ptr@**0xB7820**→cave 0xC4E00 + declared_len@0xB7812→10 (BOTH; old build off-by-one wired cave to 0x4800). Cave reads voter-MAX/AVG/|coltq|/angle (0xFEDF159E/15A2/3098/133C) as 4× LE u16. On-car: coltq+angle live, voters=0 (need LKAS). Next: log into rlogs via sunnypilot fork — `docs/HANDOFF-2026-07-10-v31u-uds-telemetry-working.md`.

- [Ground decompilation in the V850 CMSIS SVD](feedback_svd_grounding.md) — 2026-07-07: prefer the datasheet-authored SVD (analysis-2020accord/svd_for_ghidra/UPD70F3508_V850E2Px4.svd, chip µPD70F3508/V850E2) over bare peripheral addresses; cite register/field NAMES (e.g. 0xFF481000 = FCN0M0DAT0B; TX/RX dir = FCN0M{n}STRB.SSOW bit7). Settled the FCN0 0xFF489000-sub-block vs "channel B" dispute in the CAN-TX swarm. Companion to feedback_rigorous_validation.
- [.rwd output dir + one-script-per-version convention](feedback_rwd_output_dir.md) — build scripts write to ../accord-firmware/flashing-2020accord/rwd/ (external artifact root), not inside the repository; one build_vN_tva.py per lever family (V11=ceiling-raise, V12=gain)
- [Joey's lived driving experience overrides analyst recs](feedback_operator_lived_experience_overrides_analyst_recs.md) — when operator reports behavior, trust over abstract dwell-time concerns
- [Use short branch names for comma installer links](feedback_short_installer_branch_names.md) — he hand-types installer.comma.ai/<acct>/<branch> on the tiny comma 4 screen; alias to ~5-7 char branches (latlon/latonly)
- [Operator's real red-panda hardware topology](reference_operator_flash_hardware_topology.md) — red panda → comma Bosch harness → laptop (NOT OBD-II direct as docs say); reaches bus 1 where 0x221 lives
- [.rwd builders output beside the script, not D:](feedback_rwd_output_beside_script.md) — OUT_PATH relative to script dir; hardcoded D: path was machine-specific and diverged from reality
- [V850 TVA SA algorithm — universal Honda formula with Group C constants](reference_v850_sa_algorithm_tva.md) — verified at 0x00AAB4 via V850E2M datasheet; .rwd ! header is family-marker NOT SA secret on V850
- [Honda EPS SA-key secret is per-MCU-family not per-chassis](reference_honda_eps_sa_secret_per_mcu_family.md) — SH-2A = Group B, V850 = Group C; iHDS .rwd ! header is family marker
- [rizin/Ghidra V850E2 decode bugs (sld.hu disp + divq dst==src)](reference_rizin_ghidra_v850_quirks.md) — two compounding tooling bugs; suspect tools first when V850 analysis looks weird
- [Three senses of "rebuilt" — disambiguate round-trip vs canonical vs production](feedback_three_senses_of_rebuilt.md) — Joey caught conflation after stock TVA recon; recurring framing trap
- [Don't kill long agents early — SendMessage to refine, not TaskStop](feedback_dont_kill_long_agents_early.md) — Wave 2 agent landed answer right as I killed it; refine focus instead
- [Tight agent briefs beat broad — single-goal converges, 7-step gets lost](feedback_tight_agent_briefs.md) — validated multiple times; sequential single-goal chains work, comprehensive single agents flounder
- [2020 Accord SA-key chain solved 2026-05-23 — branch state](project_2020accord_sa_key_solved_2026_05_23.md) — algorithm verified, v2 .rwd built, flasher shipped; rayy's dry-run is the next concrete step
- [Accord data.bin — CORRECTED 2026-05-25: 32KB real flash @0x02000000, stored DOUBLED with 4-byte tag word per 4-byte data word; ⚠ tp(app)=0xBF000 NOT 0xF8000](reference_accord_databin_tp_base.md) — CORRECTED 2026-05-26 eve: bootloader sets tp=0xF8000 @0x9152 but the APPLICATION re-sets tp=0xBF000 @0x140ce; all tp+offset cal is at 0xBF000+off (PROGRAMMED 0xBF000-0xC6FFF), no absent partition. Real: gain 0xC646C=891, clamps 0xC61B2/B4=512. (data.bin: 32KB real flash @0x02000000, doubled-with-tags, de-tag via strip_data_tags.py)
- [Accord LKAS steering-torque path: CAN 0xE4 → mb54 → … → TSG20 PWM](reference_accord_lkas_torque_path.md) — both ends Ghidra-verified; CAN→control routing + exact torque setpoint are the 2 open gaps; flash/CRC layer ruled out
- [Accord demand aggregator: 10 producers → distributor FUN_00025c32 → mixer FUN_00026c80 → 0xFEDF1502 → shaper → serializer](reference_accord_demand_aggregator_pipeline.md) — steering is slot 1 of 10; full forward spine verified; SHARPENED GAP 2: principal cmd routes to a CSIG0 serial frame, no st to gp-0x2bf0/-0x2be0 q-ref → on-chip FOC handoff NOT proven
- [Accord arbitration limit is a FAMILY of curves + the x=4096 bottleneck chain](reference_accord_arbitration_limit_family.md) — FUN_00028ea6 clamps setpoint to ±LERP(curve[mode]); 8 mode-indexed ptr arrays 0xCB844.. → tables @0xE4xxx (cb844[0]=const 15360); waterfall x*-4→±0x4000(exact)→arb~15360→±0x2800→shaper±0x2000=8192; cb844 hard limit is mode/gear-INVARIANT but c9a88 varies by gear. ⚠ 2026-05-26: this waterfall MISSED the real LKAS binder — the arb OUTPUT GAIN tp+0x746c=0xC646C=891 (tp=0xBF000) yields only ~±418 at full cmd, capping LKAS below the ±0x2800/±0x2000 walls. 2x = gain 891→1782 + clamps 0xC61B2/B4 512→1024
- [⚠ CORRECTION OF RECORD: gp-0x4f60 is SENSOR-B DRIVER COLUMN TORQUE, not angular velocity / not vehicle speed](reference_accord_gp4f60_is_sensor_b_column_torque.md) — proven by CAN-399 packer FUN_00055c42 (STEER_TORQUE_SENSOR = -(gp-0x4f60 × 125/128)). Falsifies the Era-18/segmentD "[STRONG] angular velocity" label and the assist-lane "speed gate" reading. **Consequence: the gentle-EME debounce watches DRIVER hand torque, not the LKAS command** (gp-0x682f = min(|gp-0x4f60|>>5,255), r15 loaded @0x28f26, never rewritten through the store @0x29068) → the LKAS setpoint CANNOT provoke the gentle EME; V37 still works, only the "why" changes. Same correction was made 2026-07-07 and failed to propagate — this is now the single node of record
- [Accord LKAS setpoint ±15360 clamp = a DEGENERATE (flat) LERP; raising it is safe but needs a new CRC block](reference_accord_setpoint_limit_15360_lerp.md) — all 28 records across 5 banks have a FLAT 15360 Y row, so the axis (gp-0x6a5e = **vehicle speed**, corrected 2026-08-06, was mislabeled driver torque — a latent tuning surface shipped flattened) is irrelevant; 0xCB844 has exactly ONE code reader (0x28FCE); A160 (row 11 TVCA4) → gp-0x674e=**7** (corrected 2026-08-06, was wrongly assumed 1 from a stale row-2/TVAA1 premise) → live record **0xE51A8**, Y row 0xE51BC..0xE51CC (9 halfwords). openpilot Accord torqueBP=4096 → setpoint 16384 > 15360, so top 6.25% of range is clipped; raise → **+6.71% top-end at EVERY build tier**. EME-safe (no float 15360.0 image-wide; lockstep casts the same int gp-0x6acc @0x4467a; gentle-EME channel is driver-fed). ✅ **BUILD BLOCKER RESOLVED + SHIPPED IN V38 (2026-07-18)**: added `(0xE4000,0xE4FFC)`+`(0xE5000,0xE5FFC)` to `TOUCHED_BLOCKS` — first build in this kit to touch a bootloader block outside the compact `0xC6xxx` cal block; the 49-block chain survives because the linked-list page fields at `block_start-8/-6` live in the **preceding** block, so only the trailers move. V38 patches **all 8 selector-reachable records** (sel {0,1,3,4,6,7,8,9}) including the true live sel-7 `0xE51A8`, so the raise stands regardless of the slot-provenance caveat
- [Accord base assist joins at the AGGREGATOR; V39 targets its direct Sensor-B torque-rate lane](reference_accord_base_assist_lane_architecture.md) — FUN_0003aa2c sums boost + 5 named siblings + inline r26/r24 + FUN_00036682 before the shared governor. r24 uses the 0xD30-produced, 0xC30-consumed four-sample Sensor-B derivative, can reach +/-8192, and V39 suppresses both signs at the exact V9 full-scale equivalent (`|lane|>=417`) with low driver torque; r26 stays live
- [Accord assist-curve family (gp+0x63fd) + CONFIDENT NEGATIVE: Sport mode is NOT in this EPS](reference_accord_assist_curve_family_sport_mode.md) — 34-entry array @0xCA154, two shapes (rising top-end 1238 / falling top-end ~440); our A160 runs **index 10** (falling). All 3 writers of gp+0x63fd traced: boot table / internal fault-failover / PasCom UDS — **none CAN-fed**; 21 decoded CAN IDs contain no drive-mode signal; and our row's 4 reachable columns {10,10,11,11} differ by ~1% so the table forecloses it anyway. ⚠ slot 0 (blank/no-match HW-ID) selects the RISING family = ~2.8x more assist — matters before swapping/re-IDing an EPS
- [★★ V31→V38 scaling audit corrected for assist-inclusive aggregate](reference_accord_v31_to_v38_scaling_audit.md) — V31's matched boost floor fixed the observed 2x soft EME and V38 is fault-free on-car, but the old 3584/4608 "max command" proof omitted assist lanes before the governor. Conservative gp-0x6acc envelope is 7322, so 4096/5120 does not statically dominate every combination; treat unknown lane values as replay inputs in the canonical model
- [★ FUN_00043e44 watchdog fault SM: 7 weighted flags, threshold 128, ~10-cycle debounce → DTC setter](reference_accord_watchdog_fault_sm_fun43e44.md) — 7 plausibility flags weights 1/2/4/8/16/32/64 (max sum **127**) vs threshold **128.0** — unreachable in one cycle BY DESIGN; the trip comes from a debounce SM (gp-0x3540/gp-0x3550, integrate 0.001/cyc to 0.01 ≈ **10 cycles ≈ 0.1s**, decay 0.0005) that adds **1024.0** → 1151 > 128. Trip → `FUN_000462e6(0x3f1b)` → **`FUN_00016de6(0x1D, 0x3f1b, 1, 1)`**. **RESOLVES a memory contradiction — `corridor_lockstep` RIGHT, `override_snap` WRONG (retracted in place)**: TWO monitors — M1 `FUN_00042af8`/`gp-0x3564` (int +10/cyc thr100) → `FUN_00016de6(0x1c,…)`, M2 `FUN_00043e44`/`gp-0x3550` (float +0.001/cyc thr0.01) → `FUN_00016de6(0x1d,…)`. **`gp-0x3564` traces to MOTOR-OFF** (`FUN_0001611e` bits0x41 → `FUN_00018738` → gp-0x685c=1 → gp-0x3ef8=1 → `FUN_00019f7c` → gp-0x67fa=8 → `FUN_00045608(3,0,0x8000,0x8000)` → gp-0x3ee8=1, **power-cycle to recover**) = the V25/V26 brick path. ⚠ **An agent-memory "Monitor 2 PERMANENTLY GATED OFF" verdict was an off-by-0x1000 misread**: gate is `tp+0x74a4` = **0xC64A4 = 0x00 = ENABLED** (trip REACHABLE), not 0xC74A4=0xEA. **These monitors are hard-shutdown capable, NOT advisory.** Whether index 0x1d is hard-fault-eligible (bits 0x41) is UNVERIFIED. ✅ **Closes the V38 setpoint-slew [OPEN]**: `FUN_00043e44` never reads the setpoint or the gain — it consumes gp-0x6acc as an INPUT and **every tolerance compares its own float re-derivation vs the int shaper's stored result**, so a clamp raise translates BOTH sides; plus the per-cycle slew is bounded upstream on gp-0x6ace by cals 0xC6206=512/0xC6208=205, independent of the clamp. ⚠ **POLARITY TRAP**: the ±8.0 compare on gp-0x6acc is a **sanitize-to-zero (out-of-range→0)**, NOT a gate excluding the command — an earlier draft had it backwards; check against the ±25.0 gp-0x4f60 reference @0x43eda. ⚠ Also: cal **0xC64DE is NOT a command ramp step** (all 18 reads in the arbitration/STEER_STATUS region) — the V18 "EME ramp" label is unsupported
- [Accord ECU-ID variant table @0xCD000 (16 × 0x24): layout, columns, and the HW-ID provenance caveat](reference_accord_ecu_id_variant_table.md) — 5-byte ASCII key at record **+0x00** (NOT +0x22); selector columns +0x12 (gp+0x63fd assist), +0x16/17/19 (gp-0x674c/f/d), +0x1A (gp-0x674e setpoint mode). A160 = slot 2 "TVAA1". ⚠ the selector is a **FETCHED BYTE, not the slot index** (mulhi 0x24 @0x4271e → ld.bu @0x42724 → st.b @0x4272a) — assuming otherwise turned a 9-halfword edit into a 144-halfword estimate. ⚠ the matching HW-ID at gp-0x6408..640C is **not in code.bin** (UDS service-0x84 written at manufacture) → slot identity closable only by a live read
- [Accord LKAS torque has a HARD ±0x3FFF (~2x) static-edit ceiling — 3x is a code rewrite](reference_accord_lkas_window_ceiling.md) — gate FUN_00042ac6 AND shaper FUN_00042af8 input-check (0x43ae8, TORQUE_MOD_V0.md MISSED it) both use +0x2800/-0x5001; widening needs 2nd imm=-(2W+1), W=0x4000 overflows imm16 → max ±0x3FFF; out-of-window → 0x7FFF sentinel → shaper cmovc 0x0 ZEROES it (V10A kill mode), NOT saturate; mixer LKAS lane pinned = 0x27442 block; runtime limiter *(gp-0x4f64)=0xFEDF309C may still bind; built V11A (~2x) not 3x. CORRECTS the "~3.99x value-edit" claim below. **⚠⚠ 2026-05-26 eve SUPERSEDED: the real binder is the arb OUTPUT GAIN tp+0x746c=0xC646C=891 — and tp(app)=0xBF000 NOT 0xF8000, so cal is PROGRAMMED, NO absent partition. Full-cmd output ≈±418 (below clamps 0xC61B4/0xC61B2=512). V11/V13's "absent-partition / gain=-1 / source the .rwd" framing is RETRACTED (wrong tp base). 2x = gain 0xC646C 891→1782 + clamps 0xC61B2/B4 512→1024 (flashable). See top of body.**
- [Accord torque-mod V0 → V33 BUILT (gentle-EME torque disengage DISABLED)](project_accord_torque_mod_v0.md) — ✅ 2026-07-02 (LATEST) V33 = build_v33_tva.py = V31 + disable the GENTLE-EME torque disengage (a DISTINCT mechanism from the soft-EME/corridor lineage: engage-SM FUN_00040d58 cuts LKAS when sensor-A voter gp-0x6a62 ≥ cal 0xC6312, stock 320): raise 0xC6312 320→65535 (u16 datatype max; ld.hu/unsigned + gp-0x6a62 clamp 32000 → never fires; invalid-sensor sentinel gp-0x6a62==0xffff kept). Cal-only, decider code byte-identical to stock, 49/49 CRC, V33-vs-V32 delta = only the 2-byte threshold + block CRC, UNFLASHED. Handoff docs/HANDOFF-2026-07-02-v33.md; radare2 v850.gnu-verified (r2 DEFAULT v850 plugin mis-decodes — use v850.gnu): exactly 3 readers @0x40db8/dd0/df4 (NOT 0x40dae/dc6/dea = gp-0x6a62 value reads), 0 writers, no twin. (prior) ✅ 2026-06-03 V31 BUILT (build_v31_tva.py = V30 + matched FLAT BOOST FLOOR 4096/4.0 int 0xC6768/6A/6C + float 0xC65C4/C8/CC) — fixes V30's residual soft EME on a hard sustained HANDS-OFF turn (V30 FLASHED, drove well, but the corridor it widened is the driver-override arm gated off hands-off; boost is the authority-gated arm that's ON at authority≈0 → floors the bound where the corridor can't → self-stable; [[reference-accord-soft-eme-bound-arm-gating]]); 49/49 CRC, 0 code edits, UNFLASHED, handoff docs/HANDOFF-2026-06-03-v31.md. (prior) V30 BUILT (corridor ×4=4096/float4.0, holds LKAS≤1024 + COMP ceiling 2560 → 3584<4096; ⚠ trade: integrator gp-0x3570 never winds up → corridor/SM soft-EME cutback inert, holds 2× under override; hard-EME lockstep intact; comp magnitude uncertain → right-size via gp-0x6ac0) + V29 (corridor ×2=2048, holds 2× LKAS only) — both build_vNN_tva.py, cal-only, 49/49 CRC, byte-verified 0 code edits, UNFLASHED. Handoff docs/HANDOFF-2026-06-03-v30.md. Re-tracing both monitors on STOCK code.bin myself overturned the V27/V28 models: the int wall is a THREE-WAY MAX max(dir_corridor[0x774e], velocity/cmd-envelope IIR[gp-0x3574, NOT driver torque], boost[0x7760=angular-rate]) × polarity; both monitors compare FLOAT twins gp-0x6db0/gp-0x6db8 ×1024 vs INT walls gp-0x6af6/gp-0x6b00 ±5 LSB. ⚠ Fixed two address errors: FLOAT corridor mirror Y = 0xC6598/9C/AC/B0 (NOT 0xC6590/0xC65A4 = X breakpoints, the V28-handoff's wrong V29 proposal); 0xC6664 = ENVELOPE LERP_B (V26's rest-fault) NOT corridor. V29 = V18 GAIN/clamps/ramp + INT corridor ×2 (0xC674E/50/5A/5C) + FLOAT corridor mirror ×2 (0xC6598/9C/AC/B0) + PN; NO trampoline/tolerance/0xC6664. Holds where V25/26/27 faulted: doubles ONLY the matched corridor arm on both sides → IIR/boost arms + stock ±5/1024 residual UNTOUCHED. Conf ~85%; road-test arbiter; if soft EME is the plausibility dropout not corridor overflow, corridor widen won't fix it. Live model [[reference-accord-corridor-lockstep]]; handoff docs/HANDOFF-2026-06-03-v29.md. (prior) V25 CLEAN = V18 GAIN + DIRECTION CORRIDOR ×2 (tp+0x7748/0x7754 Y ±1024→±2048); the shl/envelope thread DROPPED (IIR envelope watchdog-only; [[reference-accord-corridor-vs-envelope]]). 19 bytes/12 runs, 49/49 CRC. (prior) V0 plan Ghidra-verified 2026-05-25 → V11A (~2x LKAS ceiling-raise) BUILT + 49/49 CRC + clean byte-diff, UNFLASHED. CORRECTS the V0 doc: hard ceiling is ~2.0x (±0x3FFF), NOT 3.99x; shaper is a dual range-check that ZEROES overshoot (doc missed 0x43ae8); mixer LKAS lane = 0x27442 block; 2.5x/3x not value-edits. Runtime limiter 0xFEDF309C may still bind. **⚠⚠ 2026-05-26 eve: tp(app)=0xBF000 fix supersedes the whole absent-partition arc. Real high-end binder = arb output GAIN tp+0x746c=0xC646C=891 (PROGRAMMED). V11/V12/V13 missed it (edited erased 0xFF46C + downstream code clamps). V14 = gain 891→1782 + clamps 0xC61B2/B4 512→1024, three flashable .rwd cal edits.** ✅✅✅ 2026-05-26 RESOLVED: V14 FLASHED + ROAD-TESTED — IT WORKS (~2x LKAS torque at the wheel). First Accord torque-mod that delivers; closes the V11/V12/V13 saga. Confirms arb GAIN tp+0x746c=0xC646C 891→1782 + clamps tp+0x71b2/b4=0xC61B2/B4 512→1024 were the binder; LKAS is REQUEST-LIMITED (~418 stock→~835 V14) far below the 4762 governor, so the doubling reaches the motor uncut (Case A) — V15/governor 0xC6202 edit NOT needed for 2x. See [[reference-accord-lkas-delivery-and-governor]]
- [★ Vibration levers: FALSIFIED (cal lane-cuts) vs UNTESTED (gp-0x4f60 filter)](reference-accord-vibration-levers-falsified-vs-untested.md) — 🛑 **CORRECTED 2026-08-06: "damping (V44/V47)" is STRUCK from the falsified list** — V44/V47 wrote modes 10/11 on a modes-24/26 car ⇒ never in force, **UNINTERPRETABLE not falsified**; the FactorC/FactorE approach was **not actually tested until V74**. r24/r26/0xC644A(V43)/0xC6450(V46) = validly falsified cal-only cuts; the gp-0x4f60 SIGNAL-FILTER is UNTESTED (V48B bricked catastrophically before testing efficacy; V50 never flashed) → filtering gp-0x4f60 late+narrow is the leading OPEN hypothesis. Four-frame telemetry finds the carrier lane first. ⚠ foc-backward-map mis-ranked 0xC6450 as "new" — it's V46-falsified.
- [★ Accord CAN-TX architecture + how to add a NEW TX ID](reference-accord-can-tx-architecture-new-id.md) — single FCN0; 18 slots via 0xB7208(route)/0xB721C(ID=entry>>18)/cadence 0xB7C9C; ALL broadcast IDs ride mailbox 6; FREE mailbox pool 7-32 (recommend 16); new ID = dedicated free mailbox + small cave. ★ Gateway verdict (lead-verified): the visible/absent split is a DOWNSTREAM gateway per-ID whitelist (the 4 absent IDs are actively fired at real rates, incl. 0x19F at 62.5 Hz = same as visible 399, yet dropped) → a new ID won't reach the comma's built-in panda; new-ID telemetry needs a red-panda tap directly on the EPS bus. Resolves the old can330-tx-rate + 2026-07-07 car-facing questions.
- [★ Accord CAN broadcast visibility = EXTERNAL gateway (single FCN0)](reference_accord_can_single_fcn0_external_gateway.md) — 2026-07-08 PROVEN: one controller FCN0/mailbox-6 for ALL frames (FCN1 never init'd, zero xrefs @0xFF4A0000); 0x660 & 399 leave on the SAME wire (routing tbl 0xB7208 all=6; emitter FUN_0001d68e; slot→ID 0xB721C) → the 399/427/0x14A-visible vs 0x660/0x19F/0x32E/0x64D-absent split is 100% the car gateway, NOT the EPS. On-car proof: TIER1's 0x660→100Hz rearm still ABSENT (scan analysis-2020accord/can-scans/2026-07-08-*). No EPS-side broadcast edit can be made comma-visible; whitelisted frames are full-DLC+checksum (no piggyback). Telemetry must ride UDS → [[reference-accord-uds-did-read-surface-a160]]
- [★ Accord UDS-over-CAN DID read surface — RAM telemetry insertion point](reference_accord_uds_did_read_surface_a160.md) — 2026-07-08: app UDS stack (req 0x18DA80F1 / resp 0x18DAF180, the flasher's) is comma-visible / crosses the gateway. Dispatcher FUN_0002075c; SID 0x22 RDBI → fixed descr idx9 (FUN_00021036, 1-5 DIDs) → FUN_00020f0a → RDBI DID table @0xB7800 (stride 0x14: +0x0C handler ptr, +0x10 u16 DID, +0x12 u16 len; +0x00=0xDF/+0x04=0x0F = default session/no-SA). DID 0xF181=idx25 (part string the flasher reads — DON'T touch). SID 0x23 ABSENT → PATCH required: repurpose/add a 0xB7800 DID → cave 0xC4E00 handler reusing RAM-read primitive FUN_0001CA0A (no SA); ABI = set *(u16)(ctx+0xc)=data_len+2; FUN_000211ba(); FUN_0002114e(abs_ptr,n) append; FUN_0002073a(). UDStelem .rwd repurposes DID 0x4801 (idx0). ⚠ Ghidra: 2 programs open → target code.bin. Spec docs/SPEC-uds-can-ram-telemetry-a160.md; bench tools/bench_uds_telem_read.py
- [★ Accord delivery + exact A160 motor-rate governor and split shaper inputs](reference_accord_lkas_delivery_and_governor.md) — gp-0x6acc drives the Q15 integrator; gp-0x6afe+r20 drives final gp-0x6b98 through the second governor. gp-0x4f64=MIN(4762,A160 Q13 table,budget); table=4607 at z=1318, while assist-inclusive gp-0x6acc can conservatively reach 7322. V39 leaves it unchanged
- [Accord sub-3mph LKAS cutoff is OPENPILOT-side, NOT a firmware pipeline gate](reference_accord_sub3mph_lkas_openpilot_gate.md) — 2026-07-17: a firmware trace found NO speed threshold in CAN-decode→arbitration→decider→engage-SM→mixer→shaper. 3 mph = openpilot STEER_GLOBAL_MIN_SPEED (3*MPH_TO_MS) AND Accord minSteerSpeed (3*MPH_TO_MS, values.py) → below max(those) controls runs latActive=False → STEER_REQUEST=0 (openpilot stops commanding). EPS also reports STEER_STATUS=LOW_SPEED_LOCKOUT near standstill (opendbc: "All Honda EPS cut off slightly above standstill"). OPEN: firmware LOW_SPEED_LOCKOUT producer NOT in the command pipeline (wheel-speed decoder unlocated; KFC_WHEEL_SPEED @0xB9BA4 dead-ends at a DTC table). RULE: don't re-hunt the LKAS firmware chain for a low-speed gate. See docs/HANDOFF-2026-07-17-lkas-model-firmware-verification.md
- [★ Accord corridor lockstep — wall=3-way max(corridor,IIR,boost); V30 FLASHED→V31 boost-floor BUILT](reference_accord_corridor_lockstep.md) — ★★★★ 2026-06-03: V30 (corridor ×4) FLASHED→drove well but residual soft EME on a hard sustained HANDS-OFF turn → V31 = V30 + matched flat BOOST FLOOR 4096 (the float twin's 3-way max INCLUDES boost, so matched int↔float is lockstep-clean); full per-arm gating model [[reference-accord-soft-eme-bound-arm-gating]]. (prior) ★★★ RE-TRACED myself (shaper FUN_00042af8 decompile + watchdog FUN_00043e44 disasm). V30 = corridor ×4 (4096/4.0) holds LKAS+COMP (handoff docs/HANDOFF-2026-06-03-v30.md); V29 = ×2 (2048) holds 2× LKAS. Arms: IIR gp-0x3574 = velocity/cmd-envelope (NOT driver torque), cap 0x3000=12288; boost = steering ANGULAR RATE gp-0x6ac2, out 0-2048. the int wall is a THREE-WAY MAX `gp-0x6af6 = max(dir_corridor[cal 0x774e, velocity-flat ±1024], velocity/cmd-envelope IIR[gp-0x3574 sar-8, NOT driver torque; input vel-vs-cmd contested], boost[cal 0x7760 X700/800/1100 Y0/1536/2048]) × polarity`. BOTH monitors compare FLOAT twins gp-0x6db0/gp-0x6db8 ×1024 vs INT walls gp-0x6af6/gp-0x6b00, ±5 LSB → ≥128.0 → 0x3f1b DTC 0xF00049. ⚠ TWO prior ADDRESS errors fixed: (a) the FLOAT corridor mirror Y = **0xC6598/0xC659C(dir1,+1.0)/0xC65AC/0xC65B0(dir2,−1.0)**, NOT 0xC6590/0xC65A4 (those are X velocity/torque BREAKPOINTS — the V28-handoff's V29 proposal named them = V26-class wrong-table brick); (b) 0xC6664 = ENVELOPE LERP_B (feeds gp-0x6da8 + envelope monitor gp-0x6c84, V26's rest-fault), NOT the corridor. Traced r11=LERP(0xC6590)/r7=LERP(0xC65A4) feed ONLY the float twin's corridor arm (gated off at rest, not stored elsewhere). **V29 = build_v29_tva.py BUILT (cal-only):** V18 GAIN/clamps/ramp (real 2×, GAIN 0xC646C monitor-INDEPENDENT, flashed+validated) + INT corridor ×2 (0xC674E/50/5A/5C) + FLOAT corridor mirror ×2 (0xC6598/9C/AC/B0) + PN; NO trampoline/tolerance/0xC6664. Holds where V25/26/27 faulted: doubles ONLY the matched corridor arm on BOTH sides → IIR/boost arms + the stock ±5/1024 residual UNTOUCHED (V27 doubled the whole twin → 2× residual → fault). 49/49 CRC, 27B/16 runs, 0 executable code edits (byte-diffed vs stock + re-decoded .rwd). Conf ~85%; road-test arbiter; if the soft EME is the driver-override PLAUSIBILITY dropout not corridor overflow, the corridor widen won't fix it. Output …/39990-TVA,A160-V29-…rwd + _v29_plain_image.bin; handoff docs/HANDOFF-2026-06-03-v29.md. (prior, FALSIFIED) the V28 model claimed wall=max(driver-torque,corridor) & proposed doubling float 0xC6590/0xC65A4 — both wrong per the 3-way-max retrace; V28 (trampoline + tolerance widen) DO NOT FLASH.
- [★ Accord soft-EME bound = gated 3-way max/min; corridor=driver-override arm (off hands-off); V31=boost floor](reference_accord_soft_eme_bound_arm_gating.md) — 🛑 **THREE CORRECTIONS 2026-08-06:** (a) **the soft-EME / SM1-2-3 cut CANNOT LATCH** — [EVIDENCE, fresh decompile] the authority-node recovery branch is a single fixed-step rise **with no bypass condition**, whichever SM cut; **it self-clears**, so it cannot produce V74's or V75's latched fault; (b) **`gp-0x3570` is a PURE UNATTENUATED INTEGRATOR** (adds the entire `(cmd − bound)` every 1 kHz cycle), **not** a ¼-per-cycle tracker ⇒ a sustained 100-count excess arms SM2 in **153 ms**, and every dwell argument built on the tracker reading is void; (c) the **COMP ceiling 2560 is at `0xC67DC`, not `0xC67D8`** (=Y[0]=512) — 3-point LERP X `0xC67D2/D4/D6` = 3200/3800/4150, Y `0xC67D8/DA/DC` = 512/1024/2560. ⊕ Measured near-inert on-car (V54: authority ≤119 / 5,989 frames vs a 3,073 knee), and **boost-floor margin erosion is REFUTED as the V75 cause** (margin +215 clamp-sum / +481 realized, never crosses zero). (prior) 2026-06-03: V30 FLASHED→drove well but residual soft EME on ONE hard SUSTAINED HANDS-OFF turn. Walked FUN_00042af8 myself: integrator gp-0x3570 winds up on (command − bound), bound = MAX/MIN(corridor, IIR gp-0x3574, boost). EACH ARM GATED: corridor (cal 0xC674E, the driver-override arm) off when |gp-0x6bf0 driver-assist|≤9216 (cal 0xC6156, hands-off) AND when authority r13≠0; boost (cal 0xC6760/float 0xC65B8, |angular rate|) latched 0 by SM gp-0x3562 when authority>0xC641E=16384 for ~20cyc; IIR decays when column velocity≈0. **r13 = gp-0x6966 = (|gp-0x3570>>15|×1092[0xC61DA])>>10 = AUTHORITY** (gates corridor@0x43114 AND the boost SM). Hands-off held turn → all 3 collapse → 2× cmd winds up → SM2/SM3 cut = the V30 EME (V30 widened the corridor, gated off there). V31 = build_v31_tva.py = V30 + matched flat BOOST FLOOR 4096 (int 0xC6768/6A/6C, float 0xC65C4/C8/CC=4.0, ÷1024, lockstep-safe — float twin includes boost); boost is authority-gated so it's ON at authority≈0 → bound≥4096>cmd3584 → no wind-up → self-stable fixpoint → boost-SM never fires. 49/49 CRC, 0 code edits, UNFLASHED. Resolves IIR identity=column velocity. handoff docs/HANDOFF-2026-06-03-v31.md
- [★ Accord EME: corridor vs envelope — 3-lever model ⚠CORRECTED by corridor-lockstep (V26)](reference_accord_corridor_vs_envelope.md) — 2026-06-02: THREE distinct 2× levers — GAIN tp+0x746c (real torque), shl IIR envelope gp-0x3574→gp-0x6af6 (WATCHDOG-ONLY: gates neither delivered torque nor EME; delivered gp-0x6b98=clamp(min(lanes,governor gp-0x4f64),±0x2000)), DIRECTION CORRIDOR tp+0x7748/0x7754 (soft-EME integrator ref). Two faults: SOFT EME (no DTC) = command exits corridor → integrator gp-0x3570 → SM2/SM3; HARD fault DTC 0xF00049 = consistency-monitor desync SELF-INFLICTED by the shl (V18 never hard-faulted). V25 = GAIN + corridor ×2; shl/B thread dropped. MODE 0 (tp+0x74c8=0) ≠ variant mode gp-0x674e=1.
- [⚠ Accord 2× EME — driver-override / torque-sensor plausibility dropout on sharp low-speed turns](reference_accord_driver_override_plausibility_eme.md) — SAFETY: V14/V15 2× build, op-engaged, driver adds hand torque → LKAS snaps to zero, ~10s jerky recovery. Mechanism Ghidra-verified: 2× arb gain (0xC646C, after the integrator) amplifies a pre-existing driver-override inhibition. Driver torque enters via column torque sensor (5 ADC ch gp-0x6a44/40/3c/38/46) → voter FUN_00041eec → fused gp-0x6a5e(0xFEDF15A2) + flag gp-0x67f4(0xFEDF180C); ENABLE byte 0xFEDF195C∈{2,3}. DO NOT widen the plausibility threshold (real sensor fault detector). **✅ CORRECTED 2026-05-27 (4-analyst Ghidra review + road test): the V16 slew-fix is REJECTED — slew 0xC61D6=0 FREEZES a dormant 2D shaping lane (NOT a disabled damper), 0→14 ACTIVATES an uncalibrated map; V17 deadband-only is INERT (slew=0 pins gp-0x356c at 0); the real cut node is the override SM gp-0x6960, NOT the deadband. V18 = 2× + ramp-only (0xC64DE 17→27, cal-only, NO trampoline) is FLASHED + drives well. Lever semantics: [[reference-accord-eme-lever-semantics]].**
- [★ Accord pointer-base audit — every tp/gp register write; app tp=0xBF000 via THREE instrs](reference_accord_pointer_base_audit.md) — program-wide (185K instr): app builds tp via movhi 0xb @0x140ce + movea 0x7000 + **add r1(0x8000) @0x140d6** → 0xBF000 (the missed 3rd instr = the address-error source); bootloader tp=0xF8000; gp=0xFEDF8000 both. Confirms every 0xC6xxx cal addr. Resolve tp at the app site, not movhi/movea pair.
- [★ Accord V18 (2× + ramp-only) FLASHED + ROAD-VALIDATED — V16/V17 dismantled](project_accord_torque_mod_v0.md) — V18 = V15B 2× (gain 0xC646C=1782, clamps 0xC61B2/B4=1024) + ramp 0xC64DE 17→27 ONLY (deadband stock, slew stock); calibration-only, decode-verified 15-byte diff, NO code patch / NO trampoline; drives well (operator). A 4-analyst review (../assessment/) REJECTED V16 (slew activates a dormant 2D map) and found V17 deadband-only INERT. Full lever semantics: [[reference-accord-eme-lever-semantics]]. ⊕ 2026-05-30 V19 = V18 + SM2/SM3 override-gate proportional rescale (0xC6422 16384→32768 + 0xC61DC 30720→61440) for HIGH-END 2× that survives hard override without the EME snap; BUILT, 49/49 CRC, UNFLASHED.
- [Accord EME lever semantics (slew/deadband/ramp/override-SM), disasm-verified](reference_accord_eme_lever_semantics.md) — slew 0xC61D6 = step of a limiter on dormant state gp-0x356c (freeze≠passthrough; 0→14 activates 2D map 0xC6770×0xC69E8); deadband 0xC6424 gates ONLY that limiter (inert at slew=0); real cut = override SM gp-0x6960; ramp 0xC64DE = re-engage ceiling (17→27 LENGTHENS, targets recovery not snap); NO output rate-limiter as cal (would need a trampoline code patch — scoped, never built)
- [Accord V22 — float-monitor 2× via code cave + gp-0x3578 negative-envelope 2×](reference_accord_v22_float_monitor_2x_cave.md) — V22 = V21 + (1) gp-0x3578 shl 0x8→0x9 @0x42F16 (negative-envelope twin of V21's gp-0x3574) + (2)/(3) float watchdog FUN_00043e44 upper_raw(r10)/lower_raw(r12) ×2 via minimal redirect @0x44230 (jr 0xC4FC0) into a 20-byte code cave in free 0xFF padding @0xC4FC0; needed because the float monitor cross-checks the integer shaper's doubled outputs (gp-0x6af6/6b00/6b0a/6b98) and would else throw CAN fault 0x3f1b. FIRST Accord build with a code cave. BUILT, 49/49 CRC, Ghidra-verified on built image, UNFLASHED. Verified jr/mulf.s/movhi V850 encodings inside.
- [★ Accord EME snap = THREE redundant authority-gate SMs (FUN_00042af8), arming gate 0xC6422=16384=50% Q15](reference_accord_override_snap_state_machines.md) — 🛑 **CORRECTED 2026-08-06: THE CUT CANNOT LATCH** — the authority-node recovery branch is a single fixed-step rise **with no bypass condition** regardless of which SM cut ⇒ **it self-clears**; a *latched* loss of assist needs the DTC-eligibility chain, not these SMs. Also **`gp-0x3570` is a pure unattenuated integrator, not a ¼-per-cycle tracker** (100-count excess ⇒ SM2 in 153 ms), and the mechanism is **near-inert on-car** (V54 authority ≤119/5,989 vs a 3,073 knee). (prior) VERIFIED 2026-05-29 (3 tracer subagents, instruction-level): SM1/2/3 (nodes gp-0x6960/6962/6787) OR-linked by 3-way MIN @0x439c0; demand=blend×min>>15 @0x43a3a; node=0 bypasses slew → INSTANT snap, recovery ramps via sp+0x38 (~10s). Arms when demand > tp+0x7422=0xC6422=16384 (stock LKAS ~15360 sits just below → EME is 2×-ONLY). Fight = cmd OPPOSES gp-0x6af8 (=gated gp-0x4f60). SM is LOCAL (zero external readers) + sets NO DTC, but broadcasts trip to another core via CSIG msg 0x2a every cycle. Dual-path int/float monitor (gp-0x3564, fault 0x3f1b) is REPORT-ONLY. High-end 2× ⟺ EME (same lever); safe 2× = setpoint-gain under the gate. Non-verified companions + open traces: analysis-2020accord/EME_OVERRIDE_SM_NONVERIFIED.md. **⊕ 2026-05-30 Trace A COMPLETE: SM1 arm 0xC61DE=2048 + SM3 arm 0xC61DC=30720 PINNED; ALL 3 SMs COMMAND-driven (integrator gp-0x3570), NOT velocity (a swarm agent's velocity-seed claim was a misread, corrected by direct read); gp-0x4f60=angular velocity [STRONG]; V19 high-end-2× BUILT = 0xC6422→0x8000 + 0xC61DC→0xF000, 49/49 CRC, UNFLASHED → [[project-accord-torque-mod-v0]].**
- [TVA bootloader integrity = CRC-32 linked-list walk (0xFF01)](reference_tva_bootloader_crc_scheme.md) — solved 0xFCB8212C = crc32 over [0xFD000,0xFFFFC); 49 blocks; the pre-flash gate (V9b passed it + flashed OK); verify_bootloader_crc.py
- [TVA flash cipher operand order — CONFIRMED](reference_tva_cipher_operand_order.md) — on-ECU decryptor FUN_0xB35E = ((c^0xBF)^0x10)-0x9E [xor,xor,sub], proven by V9b flash; v3-v8's ((c-0xBF)+0x10)^0x9E → garbage → 0x72; round-trip self-check can't catch a wrong cipher
- [2020 Accord V9 cipher fix — SOLVED 2026-05-24](project_2020accord_v9_cipher_fix_2026_05_24.md) — V9b flashed SUCCESSFULLY; cipher+window+CRC recipe confirmed; V9a retired; recipe in analysis-2020accord/HOW_TO_BUILD_ACCORD_TVA_RWD.md
- [Accord gp base = 0xFEDF8000](reference_accord_gp_base_fedf8000.md) — master key: abs = 0xFEDF8000 - gp_offset; status bitfield 0xFEDF1288, LKAS staging 0xFEDF68CC; cross-checked twice; gp-relative still not xref-able. ⚠ companion tp(app)=0xBF000 (NOT 0xF8000); tp+off cal at 0xBF000+off is programmed
- [Dream — CAN topology method as cross-platform methodology](dream_can_topology_method_generalization.md) — vfn's opendbc + mailbox + RAM-trace method generalizes to Accord/CR-V/Pilot/Clarity; durable artifact is the methodology, not just C120's specialist-7 application
- [Accord LERP envelope gated by DRIVER ASSIST — inactive during hands-off LKAS](reference_accord_lerp_envelope_gating.md) — T1/T2 both zero when |driver_assist gp-0x6bf0| < 9216; ±1024 plateau only active on driver override; explains zero stock 1x EMEs; left/right velocity asymmetry OPEN
- [★ Honda Bosch radar + longitudinal architecture (openpilot side)](reference_honda_bosch_radar_longitudinal_arch.md) — Civic Bosch + Accord are Bosch-WITH-radar (NOT radarless); OP never reads stock radar (radarUnavailable=True all Bosch); OP-long gated behind alpha_long (default OFF=stock ACC); enabling OP-long disables stock radar via UDS 0x18DAB0F1 ⇒ DISABLES AEB. Bus0=radar side (addon radar joins here), bus1=powertrain (ACC_CONTROL accel out). Firmware-level AEB-coexistence = OPEN (Ghidra 36802), not proven impossible. For the addon-radar spec.

## 2026-05-30 session — lateral PID tuning / fleet data / auto-tune surrogate

- [Talk straight — no hedge-armor](feedback_direct_no_hedging.md) — Joey wants direct, peer-level communication; drop caveat-stacking; he's comfortable with uncertainty, reads hedging as friction
- [Verify load-bearing sub-agent claims — don't relay](feedback_verify_subagent_claims.md) — re-derive before car-affecting/durable acts; sub-agents were confidently WRONG twice this session (false "route-07 degenerate"; wrong-branch gas-ceiling inversion), caught by direct reads

## 2026-06-02/03 session — UDS access surface / SA cipher families / engineering sessions / read-only telemetry (omega horde-of-hordes)

Detail of record lives in `.claude/agent-memory/firmware-codepath-tracer/reference_*` (the tracer's own index). These are the load-bearing facts:


## 2026-06-03 session — EPS observability stub / RDX recovery fork / radar dump posture (multi-horde)


## 2026-06-04 session — radar SIGNAL PRESERVATION + EPS stub v8 QA + no-flash diag read-primitive hunt (3 hordes)

Detail of record: `docs/SESSION-SYNTHESIS-2026-06-04.md`. Trust the bytes/live probe over the narrative (operator: prior radar memories are a false-start chain).

- Corrections of record: radar chip = file+**0x60000** (not +0x30000) [⚠ REVERSED 2026-06-05: canonical is +**0x30000** — the +0x60000 was a Ghidra import-base artifact; see the 2026-06-05 section]; radar **DSP island = e200z7 PowerPC** (app core still ARM Cortex-M+Helium/MVE); M_CAN base **0x44010000 REFUTED** (BE index 0x0144; real bases 0x4005xxxx); packer hook-site 0x10607C **UNCONFIRMED** (MVE wall — needs llvm-objdump --mve or live vtable read).

## 2026-06-05 session — radar recovery + 0x211FF000 stack autopsy + re-patch kit

A fine-range packer-hook patch broke comms (wrote the CPU stack); the stock revert did NOT take (NRC uncaptured). Recoverable, not bricked. Detail: `docs/HANDOFF-2026-06-05-radar-recovery.md`. ⚠ Addressing CORRECTED: chip = file + **0x30000** is canonical (the 2026-06-04 +0x60000 was a Ghidra import-base artifact).

## 2026-07-21 session — V46 (lever A) falsified, V47 dampers-only, whole-system 21 Hz audit

- [project_v46_falsified_v47_dampers_only](project_v46_falsified_v47_dampers_only.md) — V46 (Stage A carrier low-pass) FLASHED, no effect → lever A falsified (that half stands, `0xC6450` is mode-proof); V47 = ratchet + dampers-only (Factor C + Factor E aggressive), BUILT + verified, UNFLASHED. 🛑 **BANNER 2026-08-06: the DAMPER half is VOID under RULE 7** — modes 10/11 on a modes-24/26 car ⇒ never in force. Handoff `docs/HANDOFF-2026-07-21-v46-v47-vibration.md`.
- [reference-accord-damper-two-deadzones-factorC-factorE](reference-accord-damper-two-deadzones-factorC-factorE.md) — the damper is a 5-factor product with TWO hands-off deadzones (Factor C driver-torque AND Factor E motor-rate); V44 opened only C → failed. Output clamp dynamic ±512..1024, not ±2048.
- [reference-accord-no-vehicle-speed-input-5mph-is-plant](reference-accord-no-vehicle-speed-input-5mph-is-plant.md) — NO vehicle-speed input anywhere in the command/base-assist path; only motor-rate adaptation. "5 mph" = openpilot minEnableSpeed=3 + plant physics, not a firmware gate.
- [reference-accord-dualpinion-arch-one-torsion-sensor](reference-accord-dualpinion-arch-one-torsion-sensor.md) — dual-pinion EPS (motor off-axis on a 2nd rack pinion); Sensor A/B = MAIN/SUB channels of ONE torsion-bar sensor (DTC C1420); 21 Hz = rack-coupled driveline mode; resolver for motor angle; OP minEnableSpeed=3.
- [reference-accord-governor-g1-total-command-not-thermal](reference-accord-governor-g1-total-command-not-thermal.md) — G1 (FUN_0004503c) clamps the TOTAL command (not LKAS-only); "energy budget" is NOT a thermal integrator (structurally unreachable) → relabel motor-rate-adaptive total-command ceiling; doesn't bind at resonance amplitude.
- [reference-accord-damping-clamp-dtc1d-trap](reference-accord-damping-clamp-dtc1d-trap.md) — damping clamp bound (int 0xD209C/0xD20A8) has a float mirror (0xC6554/58/5C/60) with a NO-DEBOUNCE DTC-0x1d hard shutdown; never edit int clamp without a bit-exact float edit. V47 leaves it stock.

## 2026-07-21 LATE session — loop-gain characterization, collocation keystone, V48A (failed), V48B notch

★ Master reference for the vibration is now `docs/VIBRATION-DOSSIER.md`. Handoff: `docs/HANDOFF-2026-07-21-v48-vibration-loopgain-notch.md`.

- [reference-accord-collocation-motor-rate-damper-dead](reference-accord-collocation-motor-rate-damper-dead.md) — ★ the 21 Hz vibration is a two-inertia torsional mode; the firmware's MOTOR-RESOLVER-RATE damper is NON-COLLOCATED with the wheel-side mode. The hand cures it because a grip is collocated at the antinode. 🛑 **CORRECTED 2026-08-06 — the EMPIRICAL leg is GONE and the conclusion is CONTRADICTED for grind #1.** ~~"V44/V47 nulls are that theorem confirmed on-car; STOP tuning the motor-rate damper"~~ — V44/V47 were **mode-inert** (modes 10/11 on a 24/26 car), so their nulls prove nothing; **V74 actually delivered damping and 18–22 Hz measured `d ln(y)/dk` = −0.599 [−0.856, −0.348], CI EXCLUDES ZERO**. The "wrong sensor at any gain" reading survives only for the **6–9 Hz micro-ratchet** (flat). Loop-gain model: bare-plant Q≈1.7, 4× drives ~0° anti-damping → Q=13.6, |L|=0.875, 1.16 dB margin, onset ~3×. `analysis-2020accord/eps_loop_gain_model.py`.
- [project-v48-loopgain-v48a-failed-notch-next](project-v48-loopgain-v48a-failed-notch-next.md) — V48A (cal-only: mute type-8 `0xC4120` + `FUN_0003a382` `uVar27`→256) BUILT+VERIFIED+FLASHED → did NOT fix the vibration ⇒ anti-damping is DISTRIBUTED ⇒ the 21.4 Hz notch V48B is the remaining lever. **V48B is now BUILT** (see below). Route B ("4× via setpoint") is hygiene-only (ΔL=0).
- [reference-accord-v48b-notch-cave-build](reference-accord-v48b-notch-cave-build.md) — ⚠ **V48B WAS FLASHED → BRICKED (see the CATASTROPHIC entry in the next section).** 138-byte/41-instr DF-I Q12 biquad code cave @`0xC4B34` (coeffs `b0=4045 b1=-7949 b2=3977 a1=-7949 a2=3926`, unity at DC) filters a copy of `gp-0x4f60`→new RAM `gp-0x1500`; `jr` trampoline @`0x7FEAC` (return `0x7FEB0`, re-exec cmp/mov last for flags); 7 live carrier repoints `gp-0x4f60`→`gp-0x1500` (`FUN_0002c478/000352b4/0003a382/0003b49a/0003b66a`); 2 dormant reads (`0x34392/0x34ace`) left raw. RAM x1/x2/y2=`gp-0x14FC/FA/F8`. 50/50 CRC + RWD round-trip + every edit re-disassembled in Ghidra. Adversarial review: all monitor-asymmetry (V27) items CLOSED SAFE incl. the DTC-0x1c/0x1d matched int/float lockstep. `build_v48b_tva.py`+`v48b_cave_asm.py`+`eps_v48b_cave_model.py`; handoff `docs/HANDOFF-2026-07-21-v48b-notch-build.md`. ⚠ CODE CAVE (kit's only bricked class V24/V27) — flash only on explicit operator instruction.
- [reference-accord-gp4f60-notch-filter-feasibility-v48b](../../.claude/agent-memory/firmware-codepath-tracer/reference_accord_gp4f60_notch_filter_feasibility_v48b.md) — (agent memory) gp-0x4f60 is shadow-lockstep (fault 0x17) + feeds 2 no-debounce hard-shutdown monitors + 2 CAN → notch must be a FILTERED COPY (new RAM), not source-filter; repoint the carrier reads; cave 0xC4B34.

## 2026-07-21 — 🛑 V48B FLASHED → CATASTROPHIC (root-caused; two mandatory cave gates added)

- [reference-accord-v48b-flashed-catastrophic-ram-collision](reference-accord-v48b-flashed-catastrophic-ram-collision.md) — 🛑 V48B notch cave FLASHED → full-authority wheel oscillation on startup, parked, NO LKAS (recovered by reflash; no hardware damage). Kit's THIRD cave brick (V24/V27/V48B). Root cause: (1) RAM COLLISION — biquad x2 cell `gp-0x14FA` high byte `0xFEDF6B07` aliases a live monitor/DTC status bitfield (readers `FUN_00051fbc`/`FUN_00053f32` case 8), b2≈0.97 near-unity → external write injects full-scale → clamp ±25600 → motor slams; cave also stomps the monitor byte 1000×/s; writer unlocated but aliasing confirmed; `gp-0x14FA` in poison region `gp-0x1401..0x1502`, safe alt `gp-0x14E0`. (2) lightly-damped resonator (r=0.979, Q≈3.2) dropped into the always-on base-assist loop (no LKAS/speed gate) with NO closed-loop stability check — only `|N(21.4)|` inserted into the LKAS loop-gain model. (3) clock rate EXONERATED (undivided 1 kHz, correct). Handoff `docs/HANDOFF-2026-07-21-v48b-flashed-catastrophic.md`.
- [feedback-cave-two-gates-ram-ownership-and-closed-loop](feedback-cave-two-gates-ram-ownership-and-closed-loop.md) — ★ HARD RULE (apply without being asked): before ANY code cave / filter / dynamics change is flash-ready it MUST pass GATE 1 (RAM ownership — every byte's full footprint proven free incl. writers + register-indirect/6-byte-disp; `gp-0x1401..0x1502` poison) AND GATE 2 (closed-loop stability — magnitude+phase/Nyquist of EVERY loop the signal is in, esp. the always-on base-assist loop, element inserted; not a single-frequency magnitude). Byte/CRC/disassembly/open-loop-DSP verification is necessary but NOT sufficient — V48B passed all of them and bricked.

## 2026-07-22 session — 21 Hz re-audit: comma is a PASSENGER, measurement corrected, V49B StageC-flip is the lead

- [feedback-default-maximal-thoroughness](feedback-default-maximal-thoroughness.md) — ★ standing operator instruction: DEFAULT to maximal thoroughness without checkpointing for permission on analysis/prep — run every thread, close every gate, verify subagent work byte-level, build the ready UNFLASHED candidate, keep records current. The flash/CAN/UDS gates are the ONLY hard stop and are NOT relaxed by this.
- [reference-accord-v49-stagec-flip-collocated-damper](reference-accord-v49-stagec-flip-collocated-damper.md) — ★★ the 21 Hz vibration is a FIRMWARE/PLANT loop: the openpilot bus command (0xE4) STRIPS 21.5 Hz via its slew-saturated rate limiter (52% of steps at ±123 cap) → comma is a PASSENGER, no Kp/LPF fix helps. Measurement corrected (broad shelf not Q=13.6; 21.5-vs-78.6 aliasing UNRESOLVED). Topology COMPLETE (gp-0x6ade dead, gp-0x6b62 new+slow). **V49 = ratchet + collocated damper via FUN_0003a382 StageC sign-flip (subr→sub @0x3a836) + band-limit (pole 0xC644A 1024→64); GATE-2 required the band-limit (bare flip creates HF anti-damping).** BUILT+VERIFIED, UNFLASHED — 🛑 GATED on polarity gp-0x6752 (0xFEDF18AE) = +1 (brick if −1; EEPROM-resident → needs live read). Models: `eps_v49_a382_stagec_flip_model.py` + `reanalyze_b9_vibration.py`.

## 2026-07-22 session (later) — FOC hypothesis closed; V50 = EMA low-pass cave, BUILT

- [feedback-account-for-prior-iterations-before-new-build](feedback-account-for-prior-iterations-before-new-build.md) — ★ standing operator instruction: before any new RWD version, internalize EVERY prior iteration (what was flashed + falsified) and don't re-propose a lever already falsified on-car. Pairs with maximal-thoroughness + the two cave gates.
- [🛑 gp-0x1500 is a LIVE I/O-mailbox slot — the 0xb7260 array](reference-accord-b7260-io-mailbox-array.md) — the "poison region gp-0x1401..0x1502" is a subset of a 40-slot×8B I/O registry at 0xb7260 (0xFEDF6AE0..0xFEDF6C18); gp-0x1500 (slot 5) + gp-0x14E0 (slot 9) are LIVE, written via table-dispatched pointers. Proven on-car by the V50P probe (gp-0x1500 nonzero 99.47% of the drive) ⇒ **V50 is NO-FLASH**. Static clearance failed 3/8 addresses (gp-0x1500/0x14E0/0x1700) → probe ANY candidate cell live. Replacement candidates: gp-0x1300+gp-0x1100 (roomy corridor).
- [reference-accord-v50-lowpass-ema-cave](reference-accord-v50-lowpass-ema-cave.md) — 🛑 **NO-FLASH — GATE-1 failed on-car (gp-0x1500 has a live writer; see the 0xb7260 mailbox memory above).** ★★ **V50 = V38 + ratchet + a FIRST-ORDER EMA LOW-PASS (fc~12 Hz, alpha=74/1024) on gp-0x4f60 (code cave), keeps 4x. BUILT + VERIFIED (50/50 CRC, RWD round-trip, all encoders Ghidra-verified), UNFLASHED.** Chosen over V48B's notch: the mode is SPEED-DEPENDENT (~21.7 Hz low-speed → ~8-12 Hz highway; fresh manual-drive data) + alias-unresolved → a low-pass covers the whole band and rolls off 78.6 Hz harder (−16 vs −6 dB). Single 16-bit cell gp-0x1500 (V48B-flash-proven) removes V48B's 4-cell RAM-collision failure mode; deadband is a FEATURE (fully suppresses small ripple, over-attenuates moderate). Polarity-INDEPENDENT. **FOC current-loop hypothesis RULED OUT** (no isolable PI; model-based coeffs in the risky 0xC5000 block; the 8 kHz loop is the actuator, not the source). GATE 2 closed (`eps_v50_gate2_lowpass.py`); GATE 1 = gp-0x1500 best-evidenced but 2 pre-flash gates OPEN (built-image Ghidra re-disasm + the 0xbb640/0xb7260 table-walker register-indirect check). Also: the route the operator gave was NOT a V49P drive → polarity gate still unresolved (moot for V50). Build: `build_v50_tva.py` + `v50_cave_asm.py`.


## 2026-07-24 session (later) — V51P GATE-1 PASS (both cells clean); V52 built-but-incomplete; gp-0x4f60 = 19-carrier surface

- [reference-accord-v51p-gate1-both-cells-clean](reference-accord-v51p-gate1-both-cells-clean.md) — V51P flashed+driven (rlog 7): BOTH gp-0x1300 and gp-0x1100 read 0/24000 (beacon 100%, 2 independent decoders, stock null distinguishable) → definitive live-probe RAM clearance that gp-0x1500 failed. V52 uses gp-0x1300. Clears the CELL, not the filter's completeness.
- [reference-accord-gp4f60-carrier-surface](reference-accord-gp4f60-carrier-surface.md) — byte-scan: gp-0x4f60 has 64 raw readers (not ~12); ~19 command-path CARRIERS across the 1kHz control task AND the ~100Hz assist task; V52-as-built repoints only 10 (misses 9, incl. 3 self-filtering + 2 mode-gated) → INCOMPLETE + fragile + GATE-2-invalidating. NO monitor hazards (M1/M2/gate all compare raw vs literal constants → risk is feel, not brick). ⇒ empirical case for FFT-narrow (FOURFRAME) over the broad source-filter.

## 2026-07-24 session (third) — V52C: ALL 19 carriers filtered; every pre-flash gate PASSED

- [reference-accord-v52c-complete-broad-lowpass](reference-accord-v52c-complete-broad-lowpass.md) — **V52C BUILT + ALL GATES PASSED, UNFLASHED** (SHA `af01c8bd…`, 132B/24 runs, 50/50 CRC). ALL 19 gp-0x4f60 command-path carriers repointed to gp-0x1300. **Operator directive + governing principle: a MIXED raw/filtered population is ITSELF the hazard — that is how V27 bricked (ASYMMETRY, not magnitude).** GATE-1 closed FIVE ways (method reproduces V50's + V48B's failures as controls); GATE-2 edge 4.66×→**21.19×**, monotonic; no monitor asymmetry (3 shadow pairs all input-invariant); key-on transient can only SHRINK (RAM clears on the flash's power cycle). ⚠ gp-0x4f60 is SHADOWED (gp-0x4486 → fault 0x17 → hard motor-off) — filter a COPY, never the source.
- ⚠ **[reference-accord-gp4f60-carrier-surface](reference-accord-gp4f60-carrier-surface.md) is now PARTLY SUPERSEDED** — its "broad and fragile ⇒ don't filter at the source" verdict is REVERSED, its "3 self-filtering lanes" claim was WRONG on all three (`0x3B908`'s biquad is DEGENERATE in stock cal → ~366 Hz passthrough; `0x36846` isn't a filter at all), and its 64-reader count was an undercount. Header rewritten in place.
- [accord-dtc-0x18-hard-eligible-cadence-watchdog](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-dtc-0x18-hard-eligible-cadence-watchdog.md) *(user-store)* — 🛑 **CORRECTED 2026-08-06 — WRONG AS WRITTEN.** ~~DTC 0x18 is the per-task cadence/overrun watchdog, so ANY cave on the 1 kHz path has a timing budget.~~ `0x18` is a **BOOT-TIME RESET-CAUSE REPORT, not a live per-task deadline monitor**: `FUN_00014b3e` (the 4-task liveness checker) **never calls the DTC latch chain** — it snapshots the faulting PC, writes an NVM cause-code and reboots; the real `FUN_00016de6(0x18,…)` lives in the sibling `FUN_00014ba0`, gated on **reading that NVM cause-code back**, reached only from **state 1** of the dispatcher (a boot pass) ⇒ **it cannot be tripped by a running task.** The record value (0xB7FDC = 0x3D01) and the 2-byte-slip warning still stand. ⊕ **The V75 probe cave is EXONERATED**: 45→68 B costs **+17 cycles ≈ 212 ns** (V74 18 cy → V75 35 cy at 80 MHz) — **4–5 orders of magnitude below any window.**
- [accord-gp4f60-two-encodings-enumeration-trap](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-gp4f60-two-encodings-enumeration-trap.md) *(user-store)* — gp-relative accesses exist in TWO encodings; "69 definitive" was wrong, true total **76**. Byte scan misses the 6-byte form and over-matches it **7.6×** (`hw2=0xff61` is shared across ~13 nearby displacements); `search_instructions` misses anything outside a function boundary (61 vs true 64, 4th occurrence). Register field is `(hw1>>11)&0x1F`.
- [feedback-stale-ghidra-import-defeats-hash-check](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/feedback-stale-ghidra-import-defeats-hash-check.md) *(user-store)* — an OPEN Ghidra program can hold an earlier revision of a file whose on-disk SHA verifies correctly, returning RAW bytes at edited sites. Re-import fresh + spot-check one edited site against a Python byte read before trusting any pre-flash re-disasm gate.
- 🛑 [Ghidra same-name import collision — `program:` by NAME is ambiguous](accord-ghidra-same-name-import-collision.md) — re-importing under the artifact's own filename is **necessary but NOT sufficient**: a same-named import already in the project yields **two programs distinguishable only by PATH**, and the name now *looks* correct. Pin by path, then prove freshness with `read_memory` vs an independent Python byte read. Found closing V75's GATE 1, 2026-08-06.
- ★ [accord-low-speed-lockout-window-c62ea](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-low-speed-lockout-window-c62ea.md) *(user-store)* — **THE LOW-SPEED STEER LOCKOUT IS CAL `0xC62EA`=320 (~5 km/h)**, a two-sided window (hi `0xC62E8`=12800 ≈200 km/h) vs voted speed `gp-0x6a5e`; failing it writes `STEER_STATUS=3`, which via an **intra-function** `cmp 0x2` @`0x29382` blocks `STEER_CONTROL_ACTIVE` (`gp-0x6806`) and the authority ramp (`gp-0x69b0`). Cal-only, one reader each, **no float mirror**, `0xC6000` block. Proven on-car by 3 independent decoders (~305k frames): 0% control-active <2 mph, ≥99% >4 mph, **zero** co-occurrence with status 3. **openpilot is NOT the obstacle** (`CP.minSteerSpeed=0.0`; the 3 mph in `values.py:163` is `HondaCarDocs` metadata). Second gate `0xC62EE` is a **CAN-commanded** permissive (trigger = `0x17C` byte 5 bits 7/5) — leave stock, **never raise**.
- ★ [accord-v850-scan-traps-formatv-and-storezero](accord-v850-scan-traps-formatv-and-storezero.md) — 🛑 **CORRECTED 2026-08-13 (later still): this pointer used to point into the auto-memory user-store; the file exists in repo `memory/` too and is now the one this pointer targets (the two had begun to diverge — see [[feedback-two-memory-stores-have-diverged]]).** **Eight byte-scan/method traps, each of which produced a confident WRONG answer.** Headline: **Format-V `jr`/`jarl` aliases `ld.bu`** (same opcode field `0x3C`, separated only by `hw2` bit 0) → **44% false positives, 10,105 of 22,850**; filtering `reg2==0` drops every store-of-zero; `ld.bu`/`ld.hu` use `hw2=(disp|1)`; never require "compare **then** boolean store"; `(lo,hi)` pairs are confounded by `[count][X][Y]` LERP records; an **external-readers sweep cannot prove report-only**; **"Ghidra defined no function" ≠ dead** (use the task table at `0xBB928`, stride `0x30`, entry `+0x08`, self-validating on `0x2214A`/`0x22CA0`); and **NEW: `search_instructions` returned a TOOL ZERO for `tp`'s own initialiser** — Ghidra never analysed that region; a raw Python scan found `gp` and `tp` set by the same idiom, four instructions apart, from the same `r1`. Includes a 4-point validation harness for any new scanner.
- ⚠ [accord-fun45608-authority-slots-not-motoroff](../../../.claude/projects/C--Users-dudei-Desktop-Projects-accord-eps-torque-mod/memory/accord-fun45608-authority-slots-not-motoroff.md) *(user-store)* — **`FUN_00045608` is a generic AUTHORITY-SLOT SETTER, not "motor off"**: 3 parallel 7-slot arrays (`gp-0x652c`/`0x64fc`/`0x6514`) consumed by the G1 governor as a **running MIN** applied as a Q15 scale on the total command. `(3,0,0x8000,0x8000)` = slot-3 target 0 with instant slew — effect right, label wrong. Also: **the governor DOES read vehicle speed** (`gp-0x6a64` vs cal `0xC6316`=640 ≈10 km/h, below which the slew limiter is BYPASSED) ⇒ **falsifies "no vehicle-speed input anywhere in the command path"**; and its slot loop covers **slots 0-5, not 0-3**.
- ★★ [reference-accord-vibration-requires-lkas-engaged](reference-accord-vibration-requires-lkas-engaged.md) — **the 21.09 Hz Q≈14 resonance EXISTS ONLY WHILE OPENPILOT IS COMMANDING.** Matched test (hands-OFF, moving `vEgo>0.3`, same Nfft/speed gate, route 13): **9,200× less 21 Hz power with LKAS off**, and the disengaged pool carries **6× MORE** low-frequency energy — so it is NOT an excitation artifact. ⇒ **closed-loop LKAS instability, NOT the "command-independent base-assist limit cycle"** CLAUDE.md describes ⇒ **an openpilot-side 21 Hz notch/rolloff is a ZERO-BRICK-RISK experiment to run before any further `.rwd`.** Three method traps recorded: never mix hands-on/off (mixed data peaks at a spurious **7.42 Hz**, RETRACTED); the "steeringPressed is circular" objection is **testable and FALSE** (driver torque 2166 vs 328); and the raw `latOFF & handsOFF` cell is a **PARKED car** (median vEgo 0.00) — gate on `vEgo>0.3`. Mode spans **<1.5 → ~15 m/s** across 3 datasets; only >15 m/s becomes a broad low-Q 11-12.5 Hz shelf ⇒ "3-8 m/s worst regime" is **refined, not special**.
- ★ [reference-accord-can-tx-100hz-base-tick-and-gateway-evidence](reference-accord-can-tx-100hz-base-tick-and-gateway-evidence.md) — **CORRECTION: the CAN-TX base tick is 100 Hz, NOT 62.5 Hz** (derived 3 ways from cadence × measured wire rate; CAN 399 fitted at exactly 100.000 Hz) ⇒ FOURFRAME transmits at 100 Hz. **Gateway whitelist now confirmed on 8 controls, not 1**: of 11 broadcast slots only `0x14A`/`0x18F`/`0x1AB` reach the comma; slot 8 (`0x19F`) is configured **identically** to slot 9 (`0x18F`) yet never appears ⇒ **FOURFRAME's silence says NOTHING about whether the cave fired.** ⚠ the planned red-panda confirmation **may not discriminate** — the red panda taps the SAME comma Bosch harness. **Better channel: `0x18F` byte5 (constant 0x00) + `0x14A` byte4 (constant 0x07) = a free 16-bit signal at 100 Hz on gateway-crossing frames**; `0x1AB` is a POOR carrier (DLC 3, bytes 0-1 a live saturated signal). Tables read with pure Python — no Ghidra.
- ★★ [reference-accord-fourframe-strb-ssam-defect](reference-accord-fourframe-strb-ssam-defect.md) — **FOURFRAME NEVER TRANSMITTED, and it was OUR firmware.** The cave wrote `STRB = 0x80` to mailboxes 16-19; bit0 is `SSAM` and the V850E2 manual is explicit (`0 = Message buffer not used`), with §20.8.1 making SSAM=1 a hard precondition for joining the TX priority search. Stock `FUN_0001cf30` writes **0x01** to all seven TX mailboxes and **0x00** to the unused pool — `0x80` left ours configured exactly like the unused pool. Root cause: `build_vcantx_test_tva.py:54` mislabelled bit7 `SSOW` as "TX direction"; **the VCANTX seed carries the same defect.** Fix = `203e8000`→`203e0100` at 4 sites, 8 bytes, identical encoding. ⇒ **The gateway-whitelist theory is WEAKENED**: `0x19F` is gated at its own request site (`0x5559E`) while `0x18F` is not, so it was never a clean control.
- ★ [reference-accord-c646c-shared-gain-not-lkas-only](reference-accord-c646c-shared-gain-not-lkas-only.md) — **`0xC646C` is NOT "the LKAS authority gain"** — it is the firmware's single shared Q15 sensor-to-command scale with **exactly 6 readers** across three subsystems (enumerated twice, both encodings, no float mirror, neither monitor among them). Two of them (`0x36686`, `0x3684a`) apply it to the **raw torsion bar `gp-0x4f60`** on a feedback path verified end-to-end into `gp-0x6b94` → governor → motor. So 4× LKAS authority silently quadrupled two feedback lanes. **Probably NOT the 21 Hz driver** (that lane is IIR'd at fc≈2.2 Hz = −19.7 dB, clamped to 5% of aggregator range) and **the saturation hypothesis is empirically DEAD** (0 of 10,178 active-LKAS frames reach the clamp). Minimal fix designed + verified: one 2-byte retarget of `0x2a1ee` to a confirmed-free cal word at `0xC6CD0`.
- ★★ [reference-accord-vibration-needs-applied-torque](reference-accord-vibration-needs-applied-torque.md) — **the vibration needs the EPS to ACTUALLY APPLY LKAS torque, not merely to be commanded.** Three-way split on route 13: openpilot off → nothing; commanding *harder* but EPS in lockout → nothing (1.33× = noise); commanding **and applying** → **14,750×** more 21 Hz power. **Also: the 21 Hz IS in the openpilot command** — the "not in the output" premise was FALSE and came from reading `0xE4` on src=2/src=128, which are **identically zero**; only src=129 carries data. Coherence 0.685 @21 Hz vs 0.171 @1-3 Hz; **no OP-side low-pass at 21 Hz** (−2.70 dB) ⇒ **an OP notch is UNTESTED, not a no-op.** Retracts the "latency makes 21 Hz impossible" argument as bad control theory. Aliasing 21 vs 78.91 Hz still OPEN (IMU tie at the noise floor; leans 21.09 on implied Q).
- 🛑 [accord-check-build-lineage-before-proposing-lever](accord-check-build-lineage-before-proposing-lever.md) — **two agents in one session proposed `0xC6450` 1024→32 as "never flashed". It is V46 — flashed, null.** A third nearly repeated it with `0xC644A` (V43, null). **Grep `build_v*_tva.py` and check `docs/BUILD-LINEAGE.md` before naming ANY cal address, and state its on-car result.** Structural plausibility is not evidence of novelty. This is why CLAUDE.md was restructured into an index — the fact was present but not findable.
- ★ [project-v53-fourframe2-plus-minsteerspeed0](project-v53-fourframe2-plus-minsteerspeed0.md) — **V53 = FOURFRAME2 byte-for-byte + `0xC62EA` 320→0 (min steer speed → 0). BUILT 2026-07-27, UNFLASHED**, exactly 6 bytes off FOURFRAME2. Chosen 0 (not the old "suggest 64") because stock **already** unlocks true standstill via `gp-0x68b3` — 0 removes the 1–319-count discontinuity instead of moving it. Sole-reader + LERP-masquerade + SNA checks all re-run in Python at build time. `build_v53_tva.py` **imports** the cave from the FOURFRAME2 builder — zero transcription surface, reuse that pattern. No V42 ratchet fix (`0x454FE` stock, asserted).
- ★★ [reference-accord-v53-flashed-steer-to-zero-confirmed-telemetry-null](reference-accord-v53-flashed-steer-to-zero-confirmed-telemetry-null.md) — **V53 FLASHED 2026-07-27 and is the image on the car.** ✅ Steer-to-zero **CONFIRMED from raw CAN 399**: `STEER_STATUS=0` in 5,995/5,995 frames (ST=3 never fires) + **226 frames of `STEER_CONTROL_ACTIVE=1` below 5 km/h**, a cell identically EMPTY on V38 — the golden model's first successful advance prediction of an on-car state-machine change. 🛑 But the four-frame telemetry `0x6A0`-`0x6A3` was **still absent (0 frames / 301,824)**, and that null is **UNINTERPRETABLE**: six IDs the stock firmware genuinely broadcasts (`0x19F`,`0x32E`,`0x64D`,`0x660`,`0x722`,`0x723`) are equally absent, while non-DBC IDs (`0x669`,`0x750`,`0x674`) *are* logged — so "openpilot didn't know the ID" is excluded. **Never build another new-mailbox telemetry channel.** What V53 did to the vibration is UNANALYSED.
- ★ [reference-accord-piggyback-channel-audit-dbc-panda](reference-accord-piggyback-channel-audit-dbc-panda.md) — full carrier audit of the 3 whitelisted EPS frames. **`0x14A` byte4 bits 7:3 = 5 free bits @100 Hz** is the only usable channel (4 successful flashes, proven to cross, openpilot-invisible, hook at `0x55C0E` before the checksum). ⚠ **`0x18F` byte5 bits 5:4 are LIVE in firmware** (`gp-0x6880&3`, packer `0x55CAE`) — a DBC-only read wrongly calls them free; it also has no located pre-checksum hook. 🛑 **opendbc VERIFIES Honda checksums** — a bad one drops `can_valid` = a disengage. Panda's Honda RX list (`0x1A6/0x296/0x158/0x17C/0x326/0x1BE`) gates none of them.
- 🛑 [feedback-telemetry-must-reserve-a-did-not-fire-value](feedback-telemetry-must-reserve-a-did-not-fire-value.md) — **any telemetry encoding must reserve a wire value a live probe can never emit.** Caught in V54: the unbiased encoding made a *dead* probe decode as "authority 0-127, lane at FULL bound — V55 = mute", a confident fabricated answer from a channel that never ran. Bias the live range to start at 1. Also: prefer an encoder that changes a *register field* of a verified instruction over one introducing a **new opcode value**.
- ★ [project-v54-authority-probe-built](project-v54-authority-probe-built.md) — **V54 BUILT 2026-07-27, UNFLASHED — the one to flash.** V38 + `0xC62EA` 320→0 + a 44-byte read-only cave packing `wire = min((gp-0x6966>>7)+1, 31)` into `0x14A` byte4 bits 7:3 @100 Hz. It is the instrument that unblocks the `0xC6AF0` direction: wire 1-25 ⇒ lane at full bound (mute it), wire 30-31 ⇒ already clamped to zero (hypothesis dies), mixed ⇒ the knee crossing is the trigger. **wire 0 ⇒ the cave did not fire, drive is VOID.** All gates + GhidraMCP re-disassembly of the written image passed.
- [reference-accord-v54-flashed-authority-is-zero-by-design](reference-accord-v54-flashed-authority-is-zero-by-design.md) — **V54 FLASHED; the probe FIRED** (byte4 `0x07`→`0x0F`, one bit, 5,989/5,989) — the kit's **first working firmware telemetry channel**. `gp-0x6966` is the soft-EME wind-up magnitude, **not** a speed-scheduled authority, and is ~0 **BY DESIGN** on V31+ (boost floor 4096, raised to 5120 at V38, makes wind-up unreachable). ⇒ `0xC6AF0` selects unity in 100% of normal operation, the residual lane runs at FULL bound, **mute is the only meaningful edit** — but **GATE 2 (damping sign) is still open**. Do NOT re-drive faster to move it.
- [reference-accord-vibration-creep-speedshift-and-saturation](reference-accord-vibration-creep-speedshift-and-saturation.md) — the mode **moves with speed** (20.12 Hz @1.0 m/s → 21.68 Hz @4.0 m/s, 8 bins) so **no fixed-frequency notch can track it**; it **reproduces at 3.4 mph creep** (771× engaged/disengaged) in the cell V53 unlocked; and **saturation suppresses it 141× speed-controlled** — though stiction/backlash predicts that equally. All eight falsified levers share one assumption: that the ~20 Hz is *commanded*.
- [reference-accord-v55-dual-probe-built](reference-accord-v55-dual-probe-built.md) — **V55 BUILT, UNFLASHED, all gates + Ghidra re-disasm passed.** Dual report-only probe: bit7 = damper variant ≥10, bits 6:3 = 4-bit `gp-0x6b98` (final merged command). A **partition**, not a ninth lever. Carries three reusable encoding facts (`gp-0x6b98` is SIGNED; `ld.bu` puts disp bit0 in the opcode; `st.w` carries `|1` like `ld.w`) and the trap that **a V54 rlog decodes as a plausible V55 reading**.
- [feedback-no-openpilot-side-modifications](feedback-no-openpilot-side-modifications.md) — standing operator instruction 2026-07-28. **Retires the long-running "openpilot 21 Hz notch" recommendation.** openpilot stays in scope as a *measurement instrument* only; the fix must be firmware-side.
- 🛑 [feedback-explain-with-python-mirroring-decompiled-arithmetic](feedback-explain-with-python-mirroring-decompiled-arithmetic.md) — **standing operator instruction 2026-07-28: explain firmware with simple Python that mirrors the decompiled arithmetic EXACTLY** (integer `>>`, real Q-format, real branch conditions, each line annotated with its instruction address, constants byte-read little-endian). Prose-only explanations hid the `0xC63D2`=14 error, the off-by-`0x1000` slips, and the "mute `0xC6AF0`" shorthand. dB/Hz comes *after* the code, never instead of it.
- ★ [accord-lerp-tables-count-word-first](accord-lerp-tables-count-word-first.md) — **LERP tables start with a POINT-COUNT word; the base address is NOT the first X.** For `0xC6AF0`: count@`0xC6AF0`, X@`0xC6AF2..AFA`, **Y[0]@`0xC6AFC`, Y[1]@`0xC6AFE`**. Proved by the firmware's own `addi 0xc,r15,r13` (&Y[0]) and `addi 0x2,r15,ep` (&X[0]) at `0x3a63a`/`0x3a63e`, plus three sibling tables. **"Mute `0xC6AF0`" writes `0xC6AFC`/`0xC6AFE`** — writing the base clobbers the count.
- ★★ [reference-accord-v55-flashed-oscillation-is-internal](reference-accord-v55-flashed-oscillation-is-internal.md) — **V55 FLASHED, route `1c`: the ~21 Hz IS in `gp-0x6b98` and the loop is INSIDE the EPS.** Vibration 877× on the torsion bar / 996× on the independent angle-rate channel @20.90 Hz; command peaks in the SAME bin (coherence 0.93; route `1b` is a clean null control). **openpilot is 8.7× too small even with the LKAS low-pass deleted, and while RAILED its 21 Hz is exactly 0 yet the command still carries 105.8 counts.** Sensor→command transfer is **FLAT 0.19→0.22 from 1 Hz to 21 Hz** ⇒ the carrier is unfiltered. Damper bit7=1 ⇒ V44/V47 hit the LIVE tables.
- ★★ [reference-accord-gp6ad4-lane-and-c6af0-output-gate](reference-accord-gp6ad4-lane-and-c6af0-output-gate.md) — `FUN_0003a382` has **three PARALLEL branches** (Stage A and C poles are exact 1024/1024 identities); the `0xC6AF0` LERP is a **Q15 multiplier on the ceiling that clamps the FINAL combined value**, so muting is a **complete, branch-agnostic kill** — which is why V43/V46/V48A (one branch each) were null without exonerating the lane. ⚠ `gp-0x6ad4` is **NOT LKAS-gated** (`gp-0x67fe` = FOC substate, live when disengaged) ⇒ muting changes manual feel. Monitor risk CLOSED (1 writer/1 reader, no lockstep); damping sign OPEN.
- ★ [accord-c6372-c636e-untested-wideband-assist-emas](accord-c6372-c636e-untested-wideband-assist-emas.md) — both = 205/1024 → **only −1.29 dB at 21 Hz**, the only lanes besides `gp-0x6ad4` not band-limited below the mode. **Never flashed** — V44 pins them as "the rejected candidate B". 🛑 GATE 2 severe: `gp-0x6bbe` is base power steering, and adding 60-73° of assist-loop lag is the **V48B brick class**. Candidate #2, needs its own GATE 2 pass.
- ★ [project-v56-c6af0-mute-built](project-v56-c6af0-mute-built.md) — **V56 BUILT, UNFLASHED** = V55's probe + the mute, exactly 6 bytes off V55 (only 2 cal bytes move — `32768`=`00 80` LE). Builder is a POST-PROCESSOR over V55's SHA-verified image, zero transcription. ⚠ `V53.assert_stock_cals()` rightly refused it; **do not weaken that shared guard** — V56 runs it unmodified on the pre-edit source instead. Reversible experiment, not a known-good fix.
- ★★ [reference-accord-v56-flashed-mute-is-null-and-costs-damping](reference-accord-v56-flashed-mute-is-null-and-costs-damping.md) — **V56 FLASHED (route `24`, first ROAD drive with a probe): the `0xC6AF0` mute BOUGHT NOTHING ⇒ REVERT TO V55.** Speed-matched creep **786×** engaged/disengaged (V55 877×; replicated 1,878×/5,524×) and the command's in-band content did **not** drop ⇒ outcome (iii): **`gp-0x6ad4`/`FUN_0003a382` ELIMINATED as the driver of the 20-25 Hz mode, all three branches at once.** ★★ **The few-Hz resonance is WHEEL ORDER 1, not V56's doing** — `f = 0.489·v − 0.186`, r=+0.997, through the origin, implied circumference **2.088 m** ⇒ **tyre imbalance; get a road-force check.** A separate FIXED ~7-8 Hz resonance exists on every build and the wheel-order line sweeps up through it at 15-20 m/s. ⚠ "V56 removed damping" is **not supported** by the creep data (V56's 1-10 Hz power is 0.17× V55's; envelope Q not lowest) but **not closable** — no prior build has road-speed data. 🛑 A 50% partial restore is **not** a candidate: 0% and 100% already agree. 🛑 **Stop calling the mode "21 Hz"** — `f = 0.177·v + 20.48`, reaching **25.0 Hz** at 21 m/s, Q=160-228; and **steering angle shifts it ±2 Hz**, which confounds every cross-route comparison.
- ★ [reference-accord-fun3a382-is-a-real-pid](reference-accord-fun3a382-is-a-real-pid.md) — **supersedes the "three parallel lag stages" framing.** It is a genuine discrete **PID**: Stage A = P (gain 153-256/1024, motor-rate scheduled), Stage B = I (98/1024, anti-windup), Stage C = **D, a raw backward difference at gain 2048/1024 = 2.0, UNATTENUATED**. The two `1024/1024` cals are each stage's *own extra* smoothing EMA collapsing to identity — so V43/V46 were re-introducing a defeated pole, not filtering the lane. Frequency table 3/5/8/21 Hz (P-dominated at few-Hz, D rivals P by 21 Hz, +41.8° lead). **Sign PROVEN: plain `add`, all 9 aggregator lanes, polarity `gp-0x6752` boot-fixed to +1, no inversion.** ⚠ contradicts the old "−3.3° to −5.4°" phase memory.
- 🛑 [reference-accord-probe-underranges-to-a-one-bit-comparator](reference-accord-probe-underranges-to-a-one-bit-comparator.md) — **the `gp-0x6b98` probe UNDER-ranges to ~1.5 bits (we guarded against railing; the opposite happened).** 99.2% of engaged+hands-off frames in **two adjacent levels**; command inside ±512 while one LSB is 512. **An amplitude comparator but a usable SPECTRAL probe.** ⚠ **"120.5 counts at 21 Hz" is a REAL detection — keep it, restate it** (encoder gain 1.006; false-positive floor 10-18 counts ⇒ 7-12× above floor; but it is a **bin-RMS not an amplitude**, and **38× vs 66× depends on which estimator openpilot's 31.7 was — unresolved**). ⚠ **"flat H1" is UNCONFIRMED, not refuted** — quantisation is exonerated *by construction* (a memoryless nonlinearity cannot flatten a pole; coherence bias is DOWNWARD so 0.93 is a lower bound); the problem is dof: at ±19.6% a 16.8 Hz pole and flat are indistinguishable. 🛑 **The `0xC646C` elimination STANDS on its structural leg** (0 matches in 468 instructions). Also: **"9 independent segments" is wrong — K=3, significance 0.776**; re-scale to **`SHIFT=7/OFFSET=8`**, and 🛑 **`SHIFT=6` needs `OFFSET=9` or it collides with the `field==0` sentinel**; and the **5th harmonic of the mode folds into the few-Hz band**.
- ⚠ [accord-takeover-beep-is-openpilot-device-load](accord-takeover-beep-is-openpilot-device-load.md) — the intermittent "TAKE CONTROL IMMEDIATELY" is `commIssue`+`selfdrivedLagging` **softDisable that self-recovers** (once in 15:43, 1.04 s, `latActive` never dropped). **EPS/CAN is a clean null**: ST=0 throughout, `saturated` False 94,241/94,241, panda counters flat zero. Cause = **5-6 of 8 cores chronically near saturation** on a StarPilot fork; no single culprit process. Carries 4 rlog tooling traps: **`alertSound` is unpopulated route-wide**, `onroadEvents` DOES exist, **anchor the clock on each segment's first `carState`** (a bootstrap duplicate biases it +1.34 s), and `0x33D` byte4 is counter+checksum.
- 🛑★★ [reference-accord-gp6a5e-is-speed-reclassifies-v44-v47](reference-accord-gp6a5e-is-speed-reclassifies-v44-v47.md) — **`gp-0x6a5e`/`-0x6a62`/`-0x6a64` are VOTED VEHICLE SPEED, settled** (voter `FUN_00041eec`, validity window −6400..32000, closest-to-previous fallback; zero overlap with `gp-0x4f60`'s cluster). ⇒ **retire the "Sensor A" label** — it was never a torque sensor. 🛑 **The damper Factor C LERP indexes on it**, pointer chase byte-verified (`0xC9E9C+10*4` → `0x000D27BC`), X=(2240,3840,5120,8960) ≈ **35/60/80/140 km/h** ⇒ **`Y[0]=0` means "below ~35 km/h", NOT "hands-off"** ⇒ **V44 and V47 were aimed at a mechanism that does not exist.** Their on-car results stand; their stated rationale is withdrawn. The "2240 counts driver torque" story is almost certainly a **collision** with the override curve's unrelated torque breakpoint of the same number. Also: invalid speed ⇒ factor defaults to **UNITY**, not zero.
- ★★ [reference-accord-driver-override-curve-kills-lkas-authority](reference-accord-driver-override-curve-kills-lkas-authority.md) — the operator's *"significant driver torque in a direction kills the grinding"* is a **mapped authority cutoff** at `FUN_00028ea6` `0x29a74`: curve indexed by `gp-0x682f = |gp-0x4f60|>>5` with direction from `sign(gp-0x4f60)`, byte-read X=[70,72,78,80] Y=[254,234,12,0] ⇒ **LKAS authority collapses 254→0 between raw torque 2240 and 3584** (≈±2190..±3500 in DBC units). ⚠ The `0xC64B8` threshold branch is **DEAD** since V37 set it to `0xFF`. 🛑 It explains the **KILL, not the CREATION** — hands-off the curve is flat at 254. ⚠ It also means a raw-band-power driver-torque sweep **looks like it contradicts the operator**; you must use a **prominence** estimator.
- 🛑★★ [reference-accord-deadband-signgate-eliminated-on-car](reference-accord-deadband-signgate-eliminated-on-car.md) — **`gp-0x6806` IS ON THE BUS: CAN `0x18F` byte4 bit3 = `STEER_CONTROL_ACTIVE`** (packer `FUN_00055c42`, matches opendbc `BO_ 399` exactly). Measured route 24: **==1 in 96.26%, TWO transitions in 180 s, max toggle 0.1 Hz** ⇒ the `0xC61B8` deadband + sign relay is **bypassed in steady engaged driving and CANNOT be a 20-25 Hz mechanism.** Kills the relay/limit-cycle hypothesis (which fit the *entire* symptom profile — a warning about explanations that fit too well). ⚠ **BUT the bus carries PARITY, not the gate's test** — the packer does `andi 0x1` (`0x55c7e`) while the gate tests exact equality (`cmp r0,r12 ; bne` @`0x2a1ba`); four of eight live writers store a REGISTER, so a 0↔2 toggle at 22 Hz would be invisible. **V57's probe bit6 is the exact test and closes it** (`rlog-tools/decode_v57_deadband.py`). ⚠ It **IS** on the forward path (`r1`→`r16`→`st.h -0x6b3c @0x2a2ea`); the `-0x6b38` store at `0x2a23c` is a **diagnostic copy** and a subagent wrongly stopped there. ⚠ `0xC61B8`=102 was **never rescaled** while `0xC61B2/B4` went ×4 — a real defect, but on the **engage ramp** only. 🛑 **Split every `FUN_00028ea6` scan at `0x2a30d`**: `gp-0x6806` has 16 raw writers = **8 live + 8 dead-echo**; `gp-0x6b30` has 4 refs = 2 live + 2 dead.
- ★★ [accord-angle-rate-lane-gp6bbe-top-candidate](accord-angle-rate-lane-gp6bbe-top-candidate.md) — **TOP surviving vibration candidate, UNBUILT.** `FUN_00034a72` (boost, summand `gp-0x6bbe`) reads the **steering ANGLE RATE `gp-0x6a56` UNFILTERED** (`0x34AB8`/`0x34E8E`, byte-verified by the lead after two subagents disagreed) as one side of an FSM-result subtraction, scaled by two **speed-indexed** LERPs. That signal is what the EPS **transmits** as `STEER_ANGLE_RATE` (0x14A[2:3] and the 10× finer 0x18F[2:3]) ⇒ **opendbc ground truth**, and it carries the mode at **996×** vs torque's 877×. ⇒ the rate path **bypasses the torque EMA**, so the −1.29/−14.91 dB figures and the unresolved `FUN_00022ca0` task rate **matter far less than they appeared**. 🛑🛑 **GATE 2 ANSWERED *AGAINST* CUTTING IT — THE LEVER INVERTS.** The torque EMA is a **multiplicative amplitude scale**, not an additive branch, and the core term is `rate_error = baseline − angle_rate` (`sub r6,r28` @`0x34e96`) ⇒ **`gp-0x6bbe` ≈ −(gain)·angle_rate = viscous DAMPING.** **Cutting it would REMOVE damping and likely make the grinding WORSE — the V56 error one build later.** The interesting direction is **RAISING** the gain: cleanest lever **`K1` @`0xD200C`=43** (1 hit image-wide). ⚠ **INFERRED, not simulated** — rests on `baseline` being slow at 22 Hz; **certify by simulation before building.** Also: **speedLERP2 `0xD20C0` is FLAT** (fixed ±512 clamp), speedLERP1 is a hump peaking at 40 km/h, not a monotonic speed rise. **First candidate ever in the angle-rate domain** — all ~50 falsified levers were torque-domain.
- ★ [v57-decouple-built](v57-decouple-built.md) — **V57 BUILT, UNFLASHED** = V55 + the `0xC646C` decoupling: `0x2A1F0` disp `0x746C`→`0x7CD0` so the **forward** reader gets a private word at `0xC6CD0` = 3564 while the **four feedback readers revert to stock 891**. 14 bytes off V55, 50/50 CRC, RWD round-trip gated, verified against the built image. ✅ **no float mirror** (fresh 32-bit scan of `[0x7440,0x74A0)` → 0 hits) ⇒ no V27 desync risk. 🛑 **Correctness fix — expected NULL for the grinding** (≤0.28 dB at 22 Hz; only 1 of the **11** aggregator summands reads the cal, at −46..−58 dB). ⚠ Manual feel **will** change. ⚠ Reader #6 is **not** a second additive path — it modulates #5's hysteresis dead-band **width**. **Flash V55 FIRST** to undo V56 cleanly.

## 2026-07-29 session (documentation hygiene) — golden model distilled, no firmware change

- 🛑 [feedback-golden-model-distilled-keep-terse](feedback-golden-model-distilled-keep-terse.md) — **`eps_lkas_chain_model.py` cut 4,709→2,200 lines (347→145 KB), comments/docstrings only.** Operator instruction: comments ballooned into dated changelogs + multi-hundred-line findings essays embedded in function docstrings (e.g. `vibration_hands_off_analysis` alone carried ~430 lines). Distilled to comments ≤1 sentence, docstrings ≤1 paragraph, address/Q-format/confidence-tag ties kept. **Verified two ways, not just claimed:** an AST-equality diff (docstrings stripped) proves zero code changed; `python eps_lkas_chain_model.py` produces byte-identical stdout before/after. No new firmware finding — this is a documentation-quality pass. Two content fixes made in passing: a stale "4-of-16 phase-gated" claim (already contradicted by the module docstring's own EXECUTION MODEL correction) was removed rather than perpetuated; two densest functions (`vibration_hands_off_analysis`, `motor_torque_governor`) were left at ~20-28 lines rather than a strict one-liner since they carry the two most load-bearing CONFIRMED root-cause chains in the file.

## 2026-07-30 session — V57 driven; the two symptoms separate; V58 probe built

- ★★ [accord-ratchet-and-grinding-are-two-symptoms](accord-ratchet-and-grinding-are-two-symptoms.md) — 🛑 **The ~7.4 Hz mode is the RATCHETING; the 20-25 Hz mode is the GRINDING.** Operator, authoritative. Everything written before 2026-07-30 conflates them, and a mid-session claim that "the kit has been optimising the 5.3% component for 50 builds" is **WITHDRAWN**. The ratchet rises **8.42×** with command rail duty while the grinding falls to 0.74×; it is **not** the V42 state-4 ratchet (`ST==4` fires 0/37,922). Angle excitation is correlation only, via return-to-centre.
- 🛑 [accord-telemetry-conventions-that-produced-wrong-answers](accord-telemetry-conventions-that-produced-wrong-answers.md) — three rlog conventions that each flipped a headline: `cruiseState.enabled` is long+lat (reads **0.00%** on parking-lot routes; use `carControl.latActive`), raw `|tq|<=200` hands-off **discards the frames carrying 8.79× the oscillation**, and mean Welch power hides bursts (**median 0.419 vs p99 0.891**). Fourth, ✅ **SOLVED 2026-07-30** by route `2b` seg 13 (60 s moving-but-disengaged at 0.5–4.8 m/s) — see [[accord-v58-drive-grinding-engagement-gated-creep-only]]; the old 877×/786×/14,750× stay retired. Fifth and sixth, added the same day: use a **strict 18–26 Hz band** (a wider one catches the ratchet's 2nd harmonic and fakes a frequency slope) and **prominence, not envelope amplitude**, to separate a mode from broadband.
- 🛑★★ [accord-gp6a56-is-motor-rate-not-an-angle-sensor](accord-gp6a56-is-motor-rate-not-an-angle-sensor.md) — `STEER_ANGLE_RATE` is **synthesized from the MOTOR resolver rate** (`FUN_0003f776`, fixed Q15 scale of `gp-0x6abe`), not independently sensed. Kills "the only control-path signal with an external anchor", and since `baseline` is also `gp-0x6abe`-derived, `gp-0x6bbe`'s damping sign is **unresolved** after four static flip-flops.
- ⚠ SUPERSEDED [accord-v58-boostlane-probe-built](accord-v58-boostlane-probe-built.md) — **V58 has since FLASHED and flown clean**; see [[accord-v58-drive-grinding-engagement-gated-creep-only]]. Its bit5 result ELIMINATED the `0xD20C0` ceiling, but **bit6 returned nothing** and this file's "method pre-validated" claim did not transfer — see [[accord-sign-probe-needs-zero-crossings]].
- 🛑 [accord-c61d6-slew-is-rejected-not-fresh](accord-c61d6-slew-is-rejected-not-fresh.md) — `0xC61D6` 0→14 is **REJECTED** ("highest-risk lever; last/never" — it activates an uncalibrated 2D map onto the live command), not untested. Re-proposed as fresh on 2026-07-30 because it appears nowhere in `BUILD-LINEAGE.md` and an agent-memory still recommended it. Both fixed. **`0xC64DE`=27 (V18) is carried forward correctly; there is no output rate limiter available as a cal.**
- ★★ [accord-v58-drive-grinding-engagement-gated-creep-only](accord-v58-drive-grinding-engagement-gated-creep-only.md) — **V58 FLASHED, flight-clean** (route `2b`, 83,959 frames, `ST==4` = 0). ★★ **The collinearity confound is BROKEN** — seg 13 gives 60 s moving-but-disengaged at creep speed ⇒ **13.4×** speed-matched, **184×** on occupancy, and the resonance is **ABSENT disengaged** (prominence 122.7× vs 3.6×). Three record corrections: the **frequency law does not reproduce** (fixed ~20.9 Hz; `a = 0.177` rejected at 3.2–7.1σ), the grinding is **CREEP-ONLY** not road-speed, and **~21 Hz IS in openpilot's command** (direction unresolved).
- 🛑★★ [accord-gp6ba6-is-the-boost-amplitude-index](accord-gp6ba6-is-the-boost-amplitude-index.md) — **`gp-0x6ba6 == |gp-0x6b9a|`** and it — not `gp-0x6b9a` — indexes both boost amplitude LERPs; **`0xD28DC` hangs off `0xca4f4`, not `0xca23c`**. `build_v58_tva.py` was wrong on both counts. ⇒ the index is V58's 20.93 Hz signal **rectified**, sweeping the curve at **~2× the mode frequency on the BASE ASSIST path**. Depth unmeasured ⇒ V59. Also kills a subagent's "2-pole biquad" claim (it is the identity 3-tap FIR).
- 🛑 [accord-sign-probe-needs-zero-crossings](accord-sign-probe-needs-zero-crossings.md) — **a 1-bit SIGN probe carries phase only for a signal that crosses zero at that frequency.** V58's headline bit6 returned nothing because `gp-0x6bbe` is DC-dominated (0–1.1 transitions/s vs the ~44/s a 22 Hz flip needs). ⚠ **Pooling runs manufactures coherence** out of splice steps. Probe **magnitude**, not sign. Phase resolution is also capped at ±77° by the one-sample mailbox skew.
- ★ [accord-v59-boostindex-depth-probe-built](accord-v59-boostindex-depth-probe-built.md) — **V59 BUILT, UNFLASHED**: thermometer on `gp-0x6ba6` at 512/1024/2048 + the `0xFFFF` fault-sentinel bit. **19 bytes off V58, CAL CRC unchanged.** No new encoder, no new condition code. Measures the one thing V58 could not — **depth**, i.e. how much gain swing the modulation actually delivers (`<512 ⇒ ≤1.12×` … `≥5120 ⇒ 2.00×`).
- ★★ [The grinding is an UNCOMPENSATED loop through the torque sensor](reference_accord_loop_through_torque_sensor_uncompensated.md) — command→bar transfer peaks at 21.09 Hz (global max, 15.6–25.7×); no motor-command feedforward exists anywhere; damping is zero below 35 km/h. Displaces the parametric-pump story.
- ⚠ [`search_instructions` OVER-counts too — address-literal collisions](reference_accord_search_instructions_overcounts_address_literals.md) — 21 hits reported vs 2 real for `gp-0x6b70` (`jarl 0x0006b700`). Both error directions now documented.

## 2026-07-31 session — V60 null closes the pump; the V52C "halving" was never a measurement

- 🛑🛑 [A caveat can mutate into a positive result](accord-a-caveat-can-mutate-into-a-result.md) — "V52C halved the mode" was the FILTER'S OWN TRANSFER FUNCTION (-6.1 dB = 0.496x) quoted inside a caveat about why its NULL was weak, then restated as the kit's biggest positive result. No V52C rlog exists. Inverse of the withdrawn-rationale trap.
- ★★ [V60 FLASHED: null -> the parametric pump is CLOSED](accord-v60-null-closes-parametric-pump.md) — the discriminator returned the predicted null; V58/V59/V60's whole arc closes. gp-0x6ba6's index drives ONLY the boost/damping amplitude LERPs (readers byte-verified), so 0xC63BA is pre-falsified by the same null.
- ★★ [The torque RATE gp-0x4f62 is a second feedback path V52C could not reach](accord-torque-rate-lane-v52c-structurally-blind.md) — 9 sites, 3 consumers, 2 of them gate-only; the ONE live magnitude path is r24/r26 via 0x3AA9C, ~264x gain at 21 Hz, clamped +/-5120. Its shadow gp-0x4488 never leaves the producer. r24 (V39, CONDITIONAL) and r26 (V42) were each killed alone -- never together.
- ★★ [Task 5 is 100 Hz — the damper cannot damp 21 Hz](accord-task5-is-100hz-damper-cannot-damp-21hz.md) — rate divider FUN_00014be4, mod-100 on the 1 kHz tick; idx*0x30+0xBB920 reproduces all 7 TCB entries. Boost+damping run at 100 Hz ⇒ 37.6-75.2° ZOH lag at 20.9 Hz ⇒ a structural reason V44/V47 were null, independent of the FactorC speed axis. Also invalidates V59's eps table (bracketed 1 kHz/500 Hz).
- ★★★ [V61 FLASHED → WORSE: the rate lane is the DAMPER, not the amplifier](accord-rate-lane-is-the-damper-not-the-amplifier.md) — the kit's first SIGNED on-car result. r24/r26 = +Kd·d(T_bar)/dt added IN PHASE with assist (polarity is one shared load, combine chain is ten `add`s, no `sub`) ⇒ viscous damping across the torsion bar; at Kd=0 the mode has no damping term at all. Grinding worsened engaged AND appeared in manual/reverse. A derivative is DC-neutral, so "V61 removed assist" is ruled out. 🛑 Falsifies the golden model's "r26 = excitation-to-amplifier" framing; V39/V42/V61 all tested the lane DOWNWARD and bracket the wrong side of the optimum.
- ★★ [V62 BUILT: double the rate lane via two `sar` immediates](accord-v62-doubles-the-rate-lane.md) — 0x3AC20/0x3AB76 `sar 0xa`→`sar 0x9`, 6 bytes off V59, CAL CRC unchanged. Chosen over the gain cals because the arm is a priority chain that can't be pinned statically, r24's default arm is MODE-INDEXED via gp+0x63fd, and gp-0x683c is a dead gate. 🛑 0xD6AEC is mode 22, NOT a redundancy twin — the V27-desync reading was wrong. 🛑 0xC6C42 lockstep objection RETRACTED (single reader, broadcast in sync); D is a real V63 PHASE lever, 4→2 halves the lead's lag 15.1°→7.6°.
- 🛑 [A strict band presence-tests, it cannot LOCATE a mode that moved](accord-rate-lane-is-the-damper-not-the-amplifier.md#methodology-trap) — V61's first pass pre-restricted to 18-26 Hz and the argmax pinned to the band edge (18.04 Hz, sd 0.00) because the mode had shifted BELOW it. Locate over 12-30 Hz, then interpret. The mode moved 21.18 -> 18.25 Hz, which a pure gain change cannot do — that frequency shift is the structural proof the lane is a PHASE (lead) term.
- ★★★ [gp-0x671a is an OSCILLATION DETECTOR (hard-reversal counter)](accord-state671a-is-an-oscillation-detector.md) — FUN_000428d4 resets the count to 0 EVERY TICK while neutral and only leaves neutral if |gp-0x6c2c| > 12800; a reversal increments it, 50 quiet ticks clear it. So `state>=5` = "an oscillation is happening", NOT "smooth steering". Both rate lanes already branch on it ⇒ 0xC6440/0xC643E are OSCILLATION-ONLY gain arms. 🛑 Polarity was DISPUTED by two subagents and resolved by the orchestrator in Ghidra — the wrong reading was plausible and would have inverted the build. ⚠ No LKAS-active signal exists in this path at all (gp-0x6806 has 7 readers, none of them FUN_0003aa2c/FUN_000428d4).
- ★★ [V63 BUILT: oscillation-gated rate damping, 2 cal bytes](accord-v63-oscillation-gated-rate-damping.md) — 0xC6440 2048→4096, 0xC643E 1536→3072; MAIN CRC unchanged. Supersedes V62: same damping increase, zero manual-feel cost, smaller edit. 3072 is already gain_A's stock max so there is no new arithmetic risk. 🛑 A null is AMBIGUOUS (the detector may never trip) — resolve by then flying V62, which cannot miss. Fly V63 FIRST.
- 🛑 [Describe builds FUNCTIONALLY and against STOCK, not as a per-build delta](feedback-describe-builds-functionally-vs-stock.md) — standing operator preference 2026-07-31. One plain-language bullet per capability (4x LKAS gain, EME fixes, steer-to-zero, dampers, telemetry), cumulative vs stock, plus what is notably ABSENT. 🛑 Derive it from the IMAGE via `analysis-2020accord/diff_build_vs_stock.py <build>`, which fails loudly on any unattributed byte — a doc-based answer would have missed that 0xC646C is back at stock and that V42's 0x454FE is not in this line.
- ★★★ [gp-0x671a is a ONE-WAY LATCH with a 5 s hold — V63/V64's decoupling is narrower than first claimed](accord-state671a-is-an-oscillation-detector.md) — FUN_000428d4's output stage re-pins the counter to CEIL once reached; the only way down is 5000 consecutive ticks with driver torque ≥ 640 (cal 0xC62DE) AND no reversals, and torque dips below 640 on every direction change. ⇒ "V62, but only after an oscillation has happened", NOT "only while oscillating". 🛑 The "zero manual-feel cost by construction" claim is WITHDRAWN. ✅ But the latch is PROTECTIVE — a per-tick-gated gain would modulate AT the mode frequency, i.e. a parametric pump.
- ★★ [V64 BUILT: V63 + the probe repointed at the detector](accord-v63-oscillation-gated-rate-damping.md) — operator's proposal. The old probe measured gp-0x6ba6, the pump V60 falsified, so a V63 null would have been uninterpretable. 0x14A byte4: bit6 = state>=5, bit5 = state!=0, bit4 = FSM left neutral, bit3 = r24 override. Actionable in EVERY failure mode (bit4 clear ⇒ lower T 0xC620A; bit4 set + bit6 clear ⇒ lower CEIL 0xC64FA). 68/68 cave bytes, zero budget left. ⚠ V850 ld.bu carries disp bit0 in hw1 bit5, not hw2 — a naive decode reports false mismatches.
- ★★★ [V64 FLASHED: the null is on the GATE, not the hypothesis](accord-state671a-is-an-oscillation-detector.md) — probe read a constant 0x87 across all 14,980 frames: gp-0x671a/gp-0x67df/gp-0x671d ZERO on every frame through 1,158 steering-rate reversals, so |gp-0x6c2c| never crossed T=12800 and 0xC6440/0xC643E were never in force. Spectra confirm V64 ≡ V59 (2-3 m/s bin: 20.98 vs 20.99 Hz, env99 1811 vs 1804). 🛑 Do NOT cite V64 as evidence against raising the rate lane. ⇒ V62 promoted to the recommended next flash. ⚠ Even if armed it delivers r24 x1.78 / r26 x1.00 — Honda's oscillation arms are gain REDUCTIONS (defaults 2305 and 3072 vs arms 2048 and 1536).
- ★★ [gp-0x6c2c is a MOTOR-RATE DERIVATIVE, not torque](accord-gp6c2c-is-the-detector-input.md) — the detector's only input; FUN_00041464 @0x4184E differences a smoothed gp-0x4f50, x32, twice EMA'd, >>9. Tripping T=12800 needs ~1683 counts at 21.3 Hz (frequency-domain cross-check: 1685), inside the ±13000 validity ceiling ⇒ the drive was ~1.7-2x short, NOT structurally blind. 🛑 Three bus-torque-derived numbers are VOID (T≈2048-2560 band, the 3.29x LSB bound, "per-tick ⇒ dead") — the bus bounds amplitude but not scale, and this firmware rescales by 8.000x between two copies of one signal.
- 🛑 [gp-0x671a has FOUR external consumers — T is not a free lever](accord-state671a-is-an-oscillation-detector.md) — FUN_0003a382 uses it as a CONTINUOUS LERP index into the live P/I/D lane gp-0x6ad4, FUN_00036c12's friction-comp sums into the SAME aggregator, plus FUN_000352b4/FUN_00035b20. Lowering T changes FIVE things at once. gp-0x67df by contrast is clean (2 hits, both internal). 🛑 0xC64FA (CEIL) is a BYTE cal = 5 — a halfword read gives 517 and is wrong.
- 🛑 [Probe the gate and the input, not just the output](feedback-probe-the-gate-not-just-the-output.md) — V64 removed V63's ambiguity and created a new one a layer up (did T never trip, or did the detector never run?), closable only by a post-hoc trace. Every gated mechanism is enable → input → threshold → state → effect; a probe on the state collapses everything upstream into one null. Ask "if every bit reads zero, how many stories explain that?"
- ★★★ [V62 FLASHED: THE GRINDING IS FIXED](accord-v62-flashed-grinding-is-fixed.md) — route 37, 86,278 frames. 18-22 Hz creep **0.124 [0.036, 0.387]** vs V59 (8x), **0.024 [0.016, 0.234] at |rate| 16-32 deg/s (42x)**, with a 30-40 Hz negative control at ~1.0 ⇒ band-specific. Transient rates 0.793/0.486/0.338 at >200/>500/>1000 — monotonically cleaner; lowest p90/p99/>1000-rate of ANY build. **The kit's first measured fix.** 🛑 The reported "new grinding" is NOT an established regression: the 43 excursions >2000 are ONE 0.92 s burst (n=1), V62's burst-rate CI **[0.00004, 0.00793] sits inside V59's [0, 0.00986]**, and an exposure-matched test gives **p = 0.51**. Instant #2 is an ordinary burst V59 produces 3x MORE often. ⇒ **No new build — fly V62 again and count bursts.** 🛑 **TWO CORRECTIONS 2026-08-04:** (a) the companion *"r26 is structurally inert ⇒ `0x3AB76` was a no-op"* claim is **REVERSED** — V62 doubled **both** lanes; the measured results stand, the single-lane attribution does not. (b) **V62's dose is near the OPTIMUM, not on a ramp** — V69's 4× brought grind #1 back (median `e_18-22` engaged creep 2501/879/**168**/109/746 at 0×/1×/2×/2×-gated/4×) ⇒ **do not read 8–42× as "more would be better."**
- 🛑 [The "r26 is INERT" claim SPLITS — one leg reversed, one downgraded](accord-r26-is-structurally-inert.md) — **filename kept so old links resolve. NOT a flat reversal** (that would be the mirror image of the original error). **LEG 1 — THE GATE: REVERSED [EVIDENCE].** `r26 == 0 ⟺ gp-0x6b5e != 0`, `gp-0x6b5e = ((LERP(gp-0x6bda) × 0xC63C2) >> 10) × polarity` (producer `FUN_000361c8` @`0x36256`/`0x36264`), trapezoid `0xC66CC` X = [−384,−128,128,294,384] Y = [0,4762,4762,717,0] ⇒ killed **only** at `|gp-0x6bda| ≥ 384` — and `gp-0x6bda` is a **margin to a peak-hold envelope of driver assist torque `gp-0x6bf0`** (`FUN_00036022` @`0x36068`–`0x3608C`; envelope half-width never < 9390), **≈ 9262 = 24× the threshold hands-off**. ⇒ **the gate does NOT kill r26 in ordinary driving**, least of all hands-off at creep; the kill window is a **~512-count sliver at the driver-override end** (cf. `0xC6156` = 9216). **LEG 2 — THE MAGNITUDE: STILL BELIEF, unresolved either way.** `0xC6564` really is 40 zero bytes with no writer found for the RAM adjustment (10 of 18 cells) ⇒ `stage1 ≈ 0` **IF** that cal base feeds `gp-0x69a4` — **but that link was NEVER VERIFIED**, and `gp-0x69a4`'s real producer is a **live runtime 10-segment LERP at `0x355C6` in `FUN_000352b4`** (1 writer / 3 readers: `0x355A4`, `0x3575A`, `0x3AB3A` = the aggregator). ⇒ **"r24 carries the entire lane" rests on LEG 2 alone, and the V42/V61/V62 re-attribution is CONTINGENT on it.** ★ **The one indirect argument that LEG 2 holds**: at `a ≈ 1` V67/V68's gate (gain_A 3072 → 512, a 6.00× cut) would put their engaged **total at ~0.94× stock** — essentially on stock — **yet V67/V68 measured the best grind #1 result in the kit (109 vs stock's 879)** ⇒ the empirical record argues **`a` is small**. ✅ **DIRECTLY MEASURABLE**: r24/r26 share **one** polarity load (`ld.b -0x6752[gp],r14` @`0x3AB78`, reused @`0x3AB7E`/`0x3AC3E`) so they **always carry the same sign** ⇒ `sign(gp-0x6adc)` vs `sign(gp-0x6ada)` is a matched pair — **bit4 pinned at 1 while bit3 toggles ⇒ r26 is zero; bit4 tracking bit3 ⇒ r26 is live.** V70 flies exactly that pair. ⚠ **`0xC6444` raising is genuinely UNTESTED** (V42 tested it **downward**, 512 → 0, falsified — the same "downward ≠ upward" distinction V61→V62 turned on); 1 reader / 0 writers, no float mirror, CRC block #48, ceiling ≤ 6553. 🛑 **A CANDIDATE, NOT A RECOMMENDATION — V70 does not take it**, because `a` is unmeasured and V67/V68's control path is the measured best. ✅ V62/V65's `sar` route is the **only** edit here that is dose-exact independent of `a`.
- ★★ [The ratchet is a SATURATED RESONANCE, not friction](accord-ratchet-is-a-saturated-resonance.md) — LKAS-gated at **Fisher OR 115.5, p = 1.09e-08** on episodes pooled across 5 builds (75.3% vs 2.1% presence); **waveform SYMMETRIC on every build** (skew(dx/dt) −0.16…+0.06 vs a −3.27 sawtooth calibration) ⇒ damping/loop gain, not stick-slip. Fixed in hertz (CV 0.211 vs order 0.829). ❌ Damper `gp-0x6bd0` **inert at both operating points** (f5 = 0 below 12.7 deg/s; ratchet median 2.0 deg/s) — a THIRD reason V44/V47 were null. ❌ Relay, friction-comp, and the motor-rate LERP as a discriminator all eliminated. ✅ Open: gain_B's X **breakpoints** are calibration and the modes DO separate on rate (9.4 vs 73.0 counts).
- 🛑 [Bootstrap over EPISODES, and measure the noise floor first](feedback-episodes-not-windows-and-the-noise-floor.md) — windows within a run are correlated; a window bootstrap shrinks CIs by ~√28 and manufactures significance. Split-half-by-episode null gave a **2.2-2.8x resolution floor**; judge ratios against that, not 1.0. Retracted three of the orchestrator's own claims in one session, incl. a Simpson's-paradox f0 "shift" and an effort-gating that was really **motor rate** (partial ρ(effort|rate) NEGATIVE, ρ(rate|effort) +0.57…+0.70). ★ Prefer `|d(tq)|` for cross-route work (null 0.87-0.92 vs 0.63-1.23 for band power).
- 🛑 [0x14A byte4 = 0x87 means the OPPOSITE thing on V62 vs V64](accord-v62-flashed-grinding-is-fixed.md) — V59's thermometer (which V62 carries) reads it as "boost index ≥ 2048", the DEEPEST reading; V64's detector reads it as "unarmed", the V64 null. It is 9.24% of route 37 and the V64 handoff primes everyone to see a null. Always confirm which probe the flown build carries before decoding byte4.
- 🛑 [Byte scans confirm instructions that DO NOT EXIST — 5 false positives in one afternoon](accord-formatv-aliasing-five-false-positives.md) — building V65's cave, a raw LE scan "found" `cmp -0x2,rN` at four addresses and a `bgt +6` at a fifth; **all five were aliases inside `mov`/`jarl`/`dispose`**. On a short 2-byte pattern the hit rate was 5 of 6 WRONG. Pin every new encoder to a **boundary-confirmed** Ghidra instance, or by **three-way field decomposition** if none exists — and assert the near-miss (`sar` vs `shr`, `ld.h` vs `ld.hu`, `ld.h` 0x39 vs `st.h` 0x3B). ⚠ Different failure mode from `search_instructions`, which under/over-counts *analysed* instructions; a byte scan **hallucinates** them. ⊕ **Second, distinct trap in the same file:** `hw1` bit 5 is an `ld.bu`/`ld.hu` displacement's **bit 0**, but on `ld.h` (op 0x39) it is **just the opcode's low bit** — same position, opposite meaning. Encoding `-0x67ac` with it set reads **`gp-0x67ab`**, a real adjacent cell: a plausible wrong answer, not a crash. Always reconstruct `(hw2 & 0xFFFE) | (hw1 bit 5)` **as a live gate**.
- ★★ [V65 BUILT, UNFLASHED — the saturation ladder](accord-v62-flashed-grinding-is-fixed.md) — V62 + cave payload only; **CAL CRC and 0xD2000 byte-identical to V62**, 56 bytes off V62 (52 cave + MAIN CRC). A 4-level symmetric ladder on the aggregator output `gp-0x6b94` vs its own ±10240 clip: `0xE7` +RAIL / `0xA7` +HALF / `0x87` NEUTRAL / `0x97` −HALF / `0x9F` −RAIL, via one `ld.h` + one `sar 0xc` + 4 rungs, 62/68 bytes. Three decoder-checked invariants (bit6⇒bit5, bit3⇒bit4, **never both sides** — also the discriminator vs V59/V64 byte4). Headline = bit6↔bit3 alternation rate. 🛑 Draft 1 spent 2 bits on `gp-0x67ac`, which is **structurally pinned at 0** (cal `0xC4124` = (0,0,5,0,5,5,0,0,0,5,0), nothing in {2,3,4}) — the V64 mistake forming again; rebuilt. Image `f12171a8…`; RWD `dabc9241…`.
- 🛑 [The V62 "new grinding" is ONE phenomenon with ≥5 instances, and the RATCHET is loudest HANDS-OFF](accord-v62-flashed-grinding-is-fixed.md) — **operator corrected both, data confirmed both.** Ranking engaged windows by 30-49 Hz env99: 10:12:14-15 (3829/3794/3477), **10:23:21-24 (1225/946)**, 10:15:59 (1219), 10:14:18 (886) — all engaged, all under real driver effort, **1.9-17.7 m/s**. The "two different phenomena" split was an artifact of a `|d(tq)|>2000` threshold only the largest crossed; a sibling agent had already found them the same class at 3.7× severity. **Rank by a continuous statistic before classifying.** ⊕ RATCHET 6-12 Hz env99 at creep: V62 **hands-off 2665 (n=25) vs hands-on 392 (n=14) = 6.8×**; V59 1265 vs 506. **The driver's hands DAMP it** — use the hands-off arm as primary; a driver-load gate selects AGAINST it.
- ★★★★ [GRIND #2 IS V62's OWN FIX SEEN AT HIGH FREQUENCY](accord-grind2-is-a-45hz-mode-under-driver-load.md) — corner-conditioned tail maxima, Kd=1x vs Kd=2x, 219 blocks: **1-4 Hz 1.01 · 10-16 Hz 0.80 · 18-22 Hz 0.35 · 24-28 Hz 2.66 · 30-40 Hz 2.98 · 40-49 Hz 11.71 (p=0.0003)** — a **monotone response with a crossover at 22-24 Hz**, driver band flat as a control. **One knob cut grind #1 by 2.9x and raised grind #2 by 11.7x.** Cause: `gp-0x4f62` is a **4-sample finite difference**, so its gain RISES with frequency (1.93x at 41.6 vs 20.9 Hz) and V62's *flat* x2 is not frequency-selective. Grind #2 = **~44.9 Hz, Q≈37**, NOT a harmonic (slope 0.173 [-0.92,1.59] vs 2.0), **NOT LKAS-gated** (p99 1.33x vs grind #1's 6.63x), lives at creep under `tq_avg` 1600-2700. ✅ **Confirmed on the comma IMU** — 20-50x baseline during bursts, ρ 0.23-0.55, p≪1e-70 ⇒ a real whole-car vibration on a sensor independent of the EPS. 🛑 Frequency is **aliased and unresolved** (CAN ~100.5 Hz grid; IMU ~101 Hz is only 0.5 Hz away so it does NOT break the degeneracy). 🛑 **A filter cannot fix it** — differentiator +20 dB/dec vs one pole −20 dB/dec is FLAT above the corner; two poles cost −92° at 20.9 Hz and destroy the fix. Separation must come from an operating condition: **driver torque separates them >8x**, steering rate only ~2x.
- ★★ [The dead gp-0x683c gate is a free conditional-gain arm](accord-gp683c-dead-gate-is-a-free-lkas-arm.md) — **ONE access image-wide** (`ld.bu -0x683c[gp],r15` @`0x3AA94`) and **ZERO writers**, raw-scanned in both encodings; its only consumer is `setfne lp` @`0x3AAA8`, and `lp` selects cal `0xC6446` for r24 and `0xC6444` for r26 (**one reader each**). ⇒ repointing that single load is a **ONE-BYTE edit** (`0x3AA96`, even displacements only) that makes the rate-lane gain conditional on any gp byte cell — the only conditional-gain surface here needing **no code cave**. 🛑 Chatter is the kill criterion (a gain switching at the mode frequency is a parametric pump); 🛑 `gp-0x671d` OUTRANKS the arm and would mask it.
- ★ [r24's gain_B uses FOUR pointer arrays, not four consecutive records](accord-r24-gain-b-four-pointer-arrays.md) — `0xCBF5C`/`0xCC044`/`0xCC12C`/`0xCC214` at `mode*4`; mode 10 ⇒ `0xD2A74`/`0xD2AB0`/`0xD2AEC`/`0xD2B28`. Reading them **consecutively** from `0xD2AEC` picks up mode 11's interleaved rows and **understates the rolloff by 2x**. At creep the gain is **3072** (not the oft-quoted 2305, which is the 50 km/h record), flat to motor rate 400, falling to 1536 at 3000. gain_A (r26) is **not** mode-indexed. ⚠ `build_v62_tva.py`'s tripwire watches only 2 of the 4 records.
- 🛑 [Report the MEAN and the TAIL together](feedback-mean-and-tail-must-be-reported-together.md) — the same data gave **0.913 [0.791, 1.026]** (a confident null) on matched-cell means and **27/219 vs 1/91 blocks** (a confident positive) on the extreme tail. The matched q99 threshold was **317** against burst amplitudes of **3000-4000** — the mean test was structurally blind. Print tail thresholds next to event amplitudes; mark **provoked** routes (a route driven to demonstrate a symptom cannot evidence its rate); report exposure **in seconds** in the conditioning corner.
- ★★★ [The aggregator NEVER rails — the loop is LINEAR there](accord-aggregator-never-rails-loop-is-linear.md) — V65's ladder on `gp-0x6b94`, **120,049 frames**: liveness 100%, zero invariant violations, **+RAIL 0 / −RAIL 0**, only **54** frames past ±4096, `bit6↔bit3` alternation **0.0000/s in every arm** (no rail frame exists, so no flip sequence exists). The sum never comes within 20% of its ±10240 clip ⇒ **no describing-function/saturation reasoning is needed anywhere in this chain, and a lane-gain change propagates faithfully** — which is why V62's flat ×2 produced its band table. ★ All 54 non-neutral frames sit **inside grind #2 bursts** at 36–106× the median 30-49 Hz envelope. 🛑 **DO NOT apply V65's pre-committed "all four quiet ⇒ NOT another lane gain" clause to grind #2** — that branch tested the RATCHET as a rail-to-rail limit cycle, and grind #2's attribution rests on an on-car **dose-response on exactly a lane gain**. An intervention outranks an inference from a different hypothesis. What it DOES close: the ratchet cannot be amplitude-saturated *at the aggregator*, and the `0xD2AEC` breakpoint lever loses its clipping rationale. ⚠ 100 Hz sampling a ~43 Hz burst is stroboscopic — quote the route-wide ±8192 null, never the 54 as a rate. ✅ V65 flight-clean, **ST==4 = 0** both routes (recounted off raw CAN) ⇒ zero-EME streak extends.
- ★★★ [gp-0x6806 IS the LKAS gate, validated on-car](accord-gp6806-is-the-lkas-gate-validated-on-car.md) — V57's probe measured it in July and nobody correlated it. **99.90–99.94% agreement with `carControl.latActive`** over **37,914 frames** on two routes at very different duty (21.7%/49.9%), and **0.03–0.05 transitions/s**. ✅ It IS "LKAS is applying"; ✅ it does NOT drop out during steady engaged holding (the one ambiguity static analysis could not close — the ramp-FSM "settled" phases simply do not occur mid-drive); ✅ the parametric-pump kill criterion passes by **three orders of magnitude**. ⇒ V67's gate is closed BEFORE the flash. 🛑 The lesson is where to look: the answer sat in `_cache_r28`/`_cache_r29` since July while every session treated it as an open Ghidra question. **Check whether a past probe already flew on a signal before tracing it.**
- ★★★★ [V67 FLEW: both grinds fixed, and the highway symptom is NOT the rate lane](accord-v67-flew-both-grinds-fixed.md) — route `47`, 150,327 frames. Probe live: byte4 = {0x87,0xC7}, **bit6 == latActive 99.983%**, bit5/bit4 = **0 in every frame** ⇒ clean binary arm. **Grind #1 fixed with a WITHIN-ROUTE gate A/B**: engaged arm **0.524 [0.337, 0.804]** vs Kd=1, disengaged arm **1.055 [0.669, 1.354]** ⇒ suppression in ONE arm only (also the first evidence ever separating V66 from V67). **Creep grind #2 ELIMINATED**: 0 bursts vs Kd=2's 24, max 84 vs 1831 — 🛑 manual arm solid (P(0)=0.020), **engaged arm UNRESOLVED (22 s, P(0)=0.35)**. 🛑🛑 **The 2.44×-at-highway prediction was REFUTED**: three-dose highway null — 40–49 Hz **0.970 [0.787, 1.154]** and **0.938 [0.764, 1.184]** vs null [0.73, 1.37], manoeuvre-conditioned 0.999 / 0.884, and the **corpus-max highway envelope (851.5) is on the STOCK Kd=1.00 lane**. ✅ Positive control: 18–22 Hz IS suppressed at highway on Kd=2 (**0.509 [0.39, 0.92]**). ⚠ 10–16 Hz is **wheel order 1**, not a dose effect. 🛑 The ratchet lead is **CLOSED negative** (f0 9.13 Hz, moves to 11.13 in cruise, 15 Hz lock absent). ✅✅ **The microphone — no frequency ceiling, validated 4.14× on grind #2 — also sees nothing** (1.067×; the stock-Kd route r2b gives 1.071×), bounding the highway event at **≤~9% of grind #2's absolute acoustic amplitude**. Flight-clean, `ST==4` 0/150,327. ⇒ **KEEP V67; no control change supported.**
- ★★★ [r24's gain is a two-axis SPEED × RATE surface — and the units were got WRONG once](accord-r24-gain-is-a-speed-rate-surface.md) — cross axis `0xC6010` = 0/10/50/100 km/h, inner axis `gp-0x6ac0`. 🛑🛑 **RETRACTED the same night: “bus = 8 × deg/s”, “the rate axis is dead” and “V67's arm delivers 1.94×” were ALL WRONG.** Settled two ways: regressing `rate_c` on the differentiated ANGLE channel gives slope **0.95–1.00, r ≥ 0.985** ⇒ the bus field IS deg/s; and at 4.7121 counts/deg-s the breakpoints are **85/297/637 deg/s**, which real driving reaches (max **521 deg/s** over 407,617 frames), whereas the wrong scale puts them at 679/2377/5093 where Honda's rolloff could never engage. ⇒ **V67's build note was CORRECT (LERP 2622 ⇒ exactly 2.00×)**, and the rate axis IS usable: grind #1 ~603, creep grind #2 ~1206 (both on the `[400,1400]` rolloff), highway ~141–198 (X1 = `0x0190` exactly, and Y0 == Y1 in every curve **except** mode-10's 50 km/h record `0xD2AEC` (2305 vs 2304 — a +1 rounding artifact, 0.04%, behaviourally nil, but an exact equality test breaks on it)). **The error was composing two unverified structural relations into a scale instead of measuring it.** ★ **Design A** — one halfword, `0xD2ABC` 2561→7051 — gives grind #1 **2.00×**, creep grind #2 **1.22×**, highway **1.00×**; blast radius clean two ways, no float mirror, CRC block #41 only. Not recommended while V67 already has both at zero.
- 🛑🛑 [Every VIBRATION instrument is blind above ~50 Hz — but the MICROPHONE is not](accord-both-instruments-blind-above-50hz.md) — CAN **100.000 Hz exactly** (Nyquist **50.00**), comma IMU **101.02 Hz** (Nyquist **50.51**, settled by a lattice fit 77 µs vs 2889 µs and a synthetic fold test where **7/7 tones fold per 101.02**; the earlier 99.9–100.5 came from the dt *mean*, which ~1% dropped samples inflate). ★ Headroom is the **wrong quantity** for the alias: the discriminant is a **1.021 Hz** apparent-peak difference and the measured sem is **0.856** where ≪0.34 is needed ⇒ needs a log at a different IMU ODR (208/416 Hz). ★★ **The MICROPHONE has no ~50 Hz ceiling** (`soundPressure` — ⚠ **corrected 2026-08-03**: one RMS over 1600 samples of 16 kHz PCM ⇒ **0–8000 Hz analysed**, level at 10.000 Hz; this line previously said "audio at 16–48 kHz", which makes the channel look more capable than it is and causes its null to be **over-weighted** — the **26.4 dB** bandwidth-penalty argument depends on the band being 0–8 kHz) and a **working positive control**: creep grind #2 reads **4.14×** un-weighted p95 and **+9.7 dB(A)** burst-vs-quiet. ★★ It is also no longer the only above-50 Hz instrument — `gp-0x671a`'s input is a band-pass **peaking at ~61 Hz**, see [[accord-state671a-is-an-oscillation-detector]]. 🛑 A-weighting is the trap (−30 dB at 50 Hz) — use the **un-weighted** channel; *un-weighted up with A-weighted flat* IS the low-frequency signature.
- 🛑 [Check the data exists before concluding it doesn't](feedback-check-the-data-exists-before-concluding-it-doesnt.md) — two sessions asserted "no Kd=1 highway baseline exists"; route `2b` had **227 s** of it, and finding it refuted a confident, arithmetically-correct prediction. A scoping claim inherited from a previous session is **not evidence** — re-derive it. Same failure as V57's probe sitting unread for a month. **When your own arithmetic makes a tidy story with the operator's report, look HARDER for the disconfirming dataset.**

- 🛑 [No speed/torque byte exists to gate on, and the >50 Hz probe is dead at the cave site](accord-no-speed-byte-exists-in-this-firmware.md) — two search passes: every candidate is multi-valued, inline-only, standstill-only, dead (`0xC62EA`=0 since V53), or answers the same “LKAS applying” question V67 already found insufficient. Architectural reason: this firmware's idiom for speed is **“always LERP, never threshold-and-latch”**. 🛑 **Do NOT repoint the mask arm `gp-0x671d`** — a rising-edge counter driving **DTC 0x5e**, read by **8 functions** incl. 4 reads in the motor-off dispatcher `FUN_0003d4a2`; unlike the dead `gp-0x683c` it is a LIVE fault response. 🛑🛑 The hook `0x55C0E` runs at **100 Hz (task 5)**, NOT 1 kHz, and **no stock writer ever clears bits 7:3** of `gp-0x1514` (8 accesses, all masked RMW) ⇒ a sticky HF latch could never clear and the cave cannot see 1 kHz content at all. ⚠ **`gp-0x67ac` OPEN**: when it is 1, r24/r26 drop out of the aggregate entirely, regardless of gain arm.
- 🛑 [A hardcoded SEGS_2B list hid the only Kd=1 highway baseline for three sessions](accord-segs2b-bug-hid-the-kd1-highway-baseline.md) — `_r31_common.SEGS_2B` **and** `_r37_ratchet_lib.ROUTES` both enumerate route `2b` as `[0,1,2,11,12,13]`, but it has **14 segments and 3–10 are the highway leg** = **227 s of Kd=1.00 highway**, the only stock-rate-lane highway exposure in the corpus. That absence is what let a confident wrong prediction stand unrefuted. **A hardcoded subset in a helper module is invisible to every analysis that imports it** — the code runs and the numbers look reasonable. Count exposure from the caches, not through a route table: `r47_orchestrator_checks.py exposure`.
- ⚠ [Two measurement-instrument corrections that move published frequencies](accord-segs2b-bug-hid-the-kd1-highway-baseline.md) — **`fs_of()` is biased +0.5–1.4% route-dependently**; the true `0x14A` rate is **100.000 Hz on every route**, so grind #2's long-quoted "44.9 Hz" is **44.6 Hz** and the between-route frequency spread was the instrument, not the car. And **`0x18F`'s opendbc factor 0.1 is wrong — it should be 0.125** (`rate_f/rate_c` measured 0.756–0.799 ≈ 0.80 on four routes). Analyses using `rate_c` alone are unaffected.
- 🛑 **SUPERSEDED 2026-08-03 by [there is NO line at all in 30–49.5 Hz at highway](accord-highway-30-49hz-has-no-line.md)** — the order-3 half of the line below is an **estimator tautology**: `order = f0·CIRC/v` returns ≈3.00 whenever a band-limited argmax sits near the centre of 30–49.5 Hz at the corpus's median ~28 m/s, *whatever the spectrum contains*. Replaced by: peak of the **averaged** periodogram reads prominence **1.23–3.83** across 30–49.5 Hz on every route, build and channel, against the kit's **>4** criterion — no line, so not order 3 either. ⚠ **The 10–16 Hz order-1 half STANDS** (prominence up to **79**, order 1.00–1.02 in every speed bin), and so does the general warning. Original claim, kept visible:
- 🛑 [At highway, 40–49 Hz is WHEEL ORDER 3 and 10–16 Hz is ORDER 1](accord-v67-flew-both-grinds-fixed.md) — measured per-window order p50 **2.994** and **1.995** (n>600); at 30.8 m/s order 3 = **44.3 Hz, one bin from grind #2**. ⇒ **anyone peak-finding 40–49 Hz on a highway log will "find grind #2" and it will be a tyre.** The bursts themselves are NOT the order (on/off-order power ratio 6.94 quiet → **0.82 in bursts**). Order 1 explains the only band that reads outside the null in the three-dose table (10–16 Hz at 1.55/1.50) — tyre balance between drives months apart, **not** a dose effect. Second time this kit has nearly published a wheel order as a firmware effect.
- 🛑 [Ground EVERY session in the golden model AND the post-V38 arc](feedback_ground_sessions_in_golden_model_and_post_v38_arc.md) — operator instruction 2026-08-03; `eps_lkas_chain_model.py` end-to-end **plus** the whole V38→present record read as one arc, not the latest handoff. Prime every subagent with both. Direction matters: V39/V42/V61 all pushed the rate lane DOWN, the gradient pointed UP.

- 🛑🛑 [There is NO spectral line at all in 30–49.5 Hz at highway — order 3 RETIRED](accord-highway-30-49hz-has-no-line.md) — the recorded *"40–49 Hz is wheel order 3, per-window order p50 **2.994**"* is an **arithmetic tautology**: `order = f0·CIRC/v` returns ≈3.00 whenever a band-limited argmax sits near the centre of 30–49.5 Hz at ~28 m/s. Replaced by the **averaged**-periodogram peak: prominence **1.32–3.83** (bar), **1.23–2.13** (`ay`), **1.26–1.76** (`gz`) against the kit's **>4** criterion — every route, every build, every channel. ✅ The instrument WOULD see a line: the 8–30 Hz positive control returns wheel order 1 at **prominence 15–79**, free-order fit **1.07**, slope **+0.4836 [+0.4806, +0.4863]** vs order 1's 0.4808. Event `f0` vs speed **+0.1139 [−0.0539, +0.3072]** excludes orders 1/2/3/4; on/off-order power **7.24 quiet → 1.97 at event peaks**. ⚠ **10–16 Hz order 1 still stands.**

---
🛑 **THIS INDEX IS PAGINATED — READ `MEMORY-PART2.md` AS WELL.**

`MEMORY.md` reached **287,321 bytes** on 2026-08-12, past the **256 KB hard cap** stated in `CLAUDE.md`.
Beyond the `Read` limit the file loads **TRUNCATED and the tail becomes silently invisible** — the same
failure that hit `docs/STATE.md` on 2026-08-09, where 47 sections were unreadable and nobody was told.

The remaining pointers live in **`MEMORY-PART2.md`**, split verbatim at a bullet boundary with
**nothing removed, reworded, reclassified or marked superseded**. It is **not an archive** — it is the
second half of this index, and every pointer in it is live. Verified on split: all 312 distinct links
preserved, and the two files rejoin byte-for-byte identical to the original.

⚠ **ROOT CAUSE, for whoever prunes this properly:** the hooks average **829 bytes** across 405 lines.
`CLAUDE.md` asks for `- [Title](file.md) — hook`, **one line per memory**. Many entries carry several
sentences of content that belongs in the memory file itself, not in the index.
**Compressing hooks is the fix — NOT deleting pointers, and NOT guessing which entries are superseded.**
A "FALSIFIED"/"DEAD"/"REFUTED" marker usually means the entry is load-bearing, not disposable: four
candidate levers were killed by exactly those entries on 2026-08-12 alone.
---
- 🛑🛑★★★★★ [The `steeringPressed` mask EXCLUDES the symptom regime](reference-accord-steeringpressed-mask-excludes-the-symptom-regime.md) — it is `|STEER_TORQUE_SENSOR| > 1200`, **a threshold on the NUMERATOR of `Re(Z)`** (agrees 99.28-99.96 %, free-fit T = 1200 exactly). It contaminates `Re(Z)` below 6 Hz (**2-4 Hz reverses sign**) — but the bigger problem is that **the operator produces the symptom by OVERRIDING, and override is `steeringPressed == True` by definition**, so every `Re(Z)` number the kit has ever produced excludes the symptom regime. 🛑 In HIS regime (hands-on, band power, grip matched out) the engagement contrast at 6-9 Hz is **1.43-2.90×, 10/10 routes** — **his report is confirmed.**

- 🛑 [Exposure law: contiguous blocks, not total engaged seconds](feedback-exposure-law-contiguous-blocks-not-total-seconds.md) -- V89/V97/V99 primary endpoints all died to fragmentation; one ~60s continuous episode beats scattered total exposure.
- [Cabin microphone instrument + jitter trap](reference-accord-cabin-microphone-instrument-and-jitter-trap.md) -- rawAudioData is LIVE (env-speed rho 0.55) but FAILS its 6-9Hz coupling control; grid-snapping logMonoTime fakes an 11Hz line.
- 🛑🛑★★★★★ [`0xC6200`=8192 clamps the PID's REFERENCE `gp-0x6ad6` @`0x3a7a2` ⇒ `|ref|≥8192` zeroes P,I,D at once](accord-c6200-clamps-the-pid-reference.md) -- **conditions `0.2565`**; term 0 rails it at 32% of its own clamp; 4 roles + 1 unchased reader ⇒ **DO NOT EDIT the cell**; both duties UNMEASURED = V100's content.
- 🛑🛑 [Aggregator is UNWEIGHTED (11-term sum, zero multiplies); 427 rectification costs 4.9x, a REGRESSION at V98/V99](reference-accord-aggregator-unweighted-and-427-rectification-trap.md) -- phi = Path2/total needs no model; free anchor RMS(6b70)/RMS(column) = 1.13+/-0.09 (CORRECTED, was 1.18 on the rectified channel, ~8x too tight); scorer scripts v98/v99 feed the rectified lane into two live outputs.
- 🛑🛑 [PROMOTED from auto-memory — the 4x LKAS gain is the frozen variable](accord-4x-lkas-gain-is-the-frozen-variable.md) -- exactly 4.000x every build since V38, migrated cells at V57 (invisible to bisection); structurally decoupled from the ~8Hz mode's loop. NEVER lower it. Was already [[linked]] from 4 files despite not existing in repo until now.
- [PROMOTED — the "8.69Hz line V56 introduced" is wheel order 1](accord-869hz-line-is-wheel-order-not-v56.md) -- tracks speed on V57 too, so it was never a firmware artifact.
- ⭐⭐ [PROMOTED — f' compression explains BOTH V89's and V97's nulls](accord-fprime-compression-explains-v89-and-v97.md) -- the observer lane desensitises 6.3x exactly when the driver pushes (f'=2.174 hands-off, 0.346 hands-on); one mechanism, the kit's first dose-in-counts against a measured perceptual floor. Central to the V98/V99 session; had NO repo memory file until this promotion.
- ★★★★★ [PROMOTED — the ~8Hz ratchet is a lightly-damped RESONANCE, Q 14-29](accord-ratchet-is-a-lightly-damped-resonance.md) -- motor/rack side, not a loop, not a limit cycle, not rim-side; the firmware search on it is CLOSED.
- 🛑 [PROMOTED — raw14 off-by-one in EVERY cache](accord-raw14-offbyone-in-every-cache.md) -- row arrays (t,probe) are ONE SAMPLE SHORTER than raw arrays; pairing t with raw14_b4 reads every cave byte ~10ms early = 28° at 7.79Hz.
- 🛑🛑★★★★★ [PROMOTED — OPERATOR-CONFIRMED steering sign convention](accord-steering-sign-convention-confirmed.md) -- negative driver torque AND negative angle = a RIGHT turn; +LKAS and +driver torque push OPPOSITE ways. Safety-critical, was in NO repo file until this promotion.
- [PROMOTED — V57 confirms the wheel-order tyre line](accord-v57-confirms-wheel-order-tyre-line.md) -- circumference 2.073-2.080m, firmware-independent.
- ★★★★★ [PROMOTED — V87 flew (route 71): the probe fired, gp-0x6b98 is BROADBAND](accord-v87-flew-the-probe-fired-and-6b98-is-broadband.md) -- the ratcheting is a plant mode driven by broadband command content, not a commanded tone.
- ★★★★★ [PROMOTED — V88 FLEW (route 73): grinding FIXED, LKAS command UNTOUCHED](accord-v88-flew-grinding-fixed-command-intact.md) -- 15-22Hz delivered command halved while 0.5-3Hz LKAS command untouched; first fix shown not to cost steering authority. Central finding, had NO repo file until this promotion.
- ★★★★ [PROMOTED — V88 built: Lever B restored + the sign fix](accord-v88-lever-b-restored.md) -- V87 + Lever B restored + the probe's rectification hole closed; 0xC6444 is FALSIFIED (flew as V71c), not untested.
- ★★★★★ [PROMOTED — V98 FLEW (route 0x81): the comparator ranked the observer arms](accord-v98-comparator-ranked-the-observer-arms.md) -- REQUEST is minor, MODEL and ACTUAL are COMPARABLE, "arms are wildly unequal" refuted. Central V98 finding, had NO repo file until this promotion.
- 🛑 [PROMOTED — standing instruction: ground every session in the golden model + post-V38 arc](feedback-ground-every-session-in-golden-model-and-post-v38-arc.md) -- every subagent must be primed with both; also in CLAUDE.md but had no memory backing.
- 🛑 [PROMOTED — run the control BEFORE the measurement](feedback-run-the-control-before-the-measurement.md) -- four claims died to controls in one session, three retracted by their own authors; calibrate against the RIGHT null before reporting a number. Was dangling-linked twice this session before this promotion.
- [PROMOTED — 0xC520C cap table: axis provenance SETTLED](reference-accord-c520c-cap-table-axis-provenance.md) -- index IS clamp(gp-0x6ac0,0,10000) = motor resolver/FOC electrical-angle rate; 0xC5224 is a redundant mirror, not a composed stage.
- [PROMOTED — 0xC6194 is DEAD calibration](reference-accord-lkas-only-rate-limiter-c6194.md) -- output x0 via cal 0xC63CC=0; no live LKAS-specific slew limit exists, the lane reaches the aggregator unfiltered.
- 🛑🛑 [The two memory stores have DIVERGED — repo memory/ vs auto-memory](feedback-two-memory-stores-have-diverged.md) -- 24-file gap measured 2026-08-13; 15 promoted (incl. this session's own central f'-compression and V98-comparator findings, which had NO repo backing), 8 skipped as duplicates, 1 skipped as a REFUTED claim (promoting would have regressed the record). Cost real work 3x in one session — read before promoting anything cross-store.
