# MEMORY.md — PART 2 (continuation, NOT an archive)

Split verbatim from `MEMORY.md` on 2026-08-12 because it exceeded the **256 KB `Read` cap** in
`CLAUDE.md` and was loading truncated — the tail of the index had become **silently invisible**.

🛑 **Every pointer below is LIVE.** Nothing was removed, reworded, reclassified or marked superseded;
the split is at an arbitrary bullet boundary chosen only to get each half under the cap. Verified: the
two files rejoin **byte-for-byte identical** to the original, and all **312** distinct links survive.

**Read this file together with `MEMORY.md` — neither half is complete on its own.**

🛑 **A THIRD part now exists — [`MEMORY-PART3.md`](MEMORY-PART3.md)**, split 2026-08-21: the DATED
SESSION SECTIONS, `## 2026-05-30` → `## 2026-07-31`. Same rules — verbatim, every pointer LIVE, not an
archive. **Read all three parts.**

---

- ★★★ [The highway 40–49 Hz null survives an EVENT-RATE re-test — and here is the power](accord-highway-event-rate-null-with-power.md) — the pooled-median null was challenged as blind to rare bursts, so the machinery that found creep grind #2 was pointed at highway. **Same null**: event-rate ratios **0.855 [0.432, 1.702]** and **1.152 [0.496, 2.690]** vs a split-half null of **[0.36, 2.50]**, **min detectable 1.61×** at 80 % power; monotone-rising in **no** band, and at 40–49 Hz the max-dose build has the *lowest* rate. ✅ Positive control: grind #1's own event rate falls monotonically with dose (**0.565 [0.329, 0.984]** → **0.319 [0.130, 0.661]**). ⇒ two independent statistics, one answer — the earlier null was **not** a statistic-choice artefact. 🛑 **0.0 s of LKAS-off driving above 12 m/s exists** (verified via `cc_lat` **and** `0x18F` b4 bit3) ⇒ "only when engaged" is **untestable by construction**; `cruiseState` would fake a 123–187 s arm. Bound: a fixed-pitch mode **above ~50 Hz is invisible to both channels**.
- ★★ [The highway "events" are the smooth loading effect's top tail, not a threshold](accord-highway-events-are-the-loading-tail.md) — the operator reported threshold-like; **it is not.** P(event) rises through **every** decile of steering rate from the 5th up (0.7 → 6.4 → 22.1 → **61.2 %**, ρ +0.420) with no zero-then-jump, and envelope-vs-`rate_pk` ρ **+0.654 / +0.705 / +0.721** reproduces the recorded +0.64…+0.93. Trigger is a **~1.5 s steering-rate transient** (median |rate| 1.0 → 5.0 → 6.5 → **18.0** → 5.0 deg/s) at constant speed. ✅ **Hands-off confirmed 19/20**, **rail duty 0.00 on every event**, duration 0.22–0.87 s. 🛑 **No closed-loop signature** — command→bar coherence **0.169** in event windows vs **0.166** background, against grind #1's 0.917 at 21.09 Hz. ⚠ A 2-component fit "prefers bimodal" (ΔBIC 174) — don't read it; the high component is **42 % of windows at 2.4×**.
- 🛑 [A median-of-per-window-argmax MANUFACTURES a line at band centre](feedback-average-periodograms-before-peak-finding.md) — it reported a fixed **~42 Hz mode** on four IMU axes across 22→35 m/s and beat "wheel order 3" by **ΔBIC 249–460**; it matched the operator's reported fixed pitch; **it was an artefact and was withdrawn.** With no line present the argmax scatters and its median lands at band centre (39.75 Hz for a 30–49.5 band). **Fix: average the periodograms FIRST, then find the peak** — the mode vanished, prominence 1.23–3.83. Same class: the `order = f0·k/v` tautology. ⇒ **never test with a ratio built from a band-limited estimate divided by the variable under test — use a slope with a CI or a binned table**, and always run a positive control through the identical pipeline. Also killed here: the **CVT/engine-order** alternative for a fixed pitch (rpm 1330–2400, corr(rpm,v) only **+0.270**, required slope **−0.0333 Hz/rpm** vs measured **−0.00071 [−0.00251, +0.00084]**). ⚠ `argsort(argsort(x))` is not a tie-aware rank — it gave ρ **+0.393** against a monotonically *falling* decile table.
- 🛑 [Route 47 owns 79.8 % of the corpus above 28 m/s — dose is confounded 1:1 with route there](accord-route47-owns-the-fast-highway-exposure.md) — 623.8 s of 781.2 s; **Kd=1 holds 39.2 s (5.0 %)**, Kd=2 118.3 s. So any >28 m/s dose claim is a **one-route-per-arm** comparison across different road, tyres, weather and date, and a within-route split-half null cannot see that. It bit once already: an IMU 41–44 Hz ratio of **1.484 [1.295, 1.617]** looked decisive until the pre-declared **33–36 Hz negative control moved with it (1.185)**. Only between-route control at constant dose: `r37` vs `r3b` = **0.958 [0.836, 1.112]**, one degree of freedom. ⚠ Route `4a` is V67 but has **zero highway** (max 13.92 m/s). ⇒ **~4 min of engaged >28 m/s on a non-V67 build** closes this and the power gap.

- ★★★ [The mic's TWO WEIGHTINGS are a two-point FILTER BANK — grind #2's sound is NOT all at 40–49 Hz](accord-mic-two-weightings-are-a-filter-bank.md) — A-weighting is **−32.41 dB at 44.6 Hz** but **−19.14 dB at 100 Hz**, so `soundPressure` ÷ `soundPressureWeighted` reports *where energy sits* although neither channel can name a frequency. On 10 demonstrated creep bursts the **A-weighted channel rose MORE** (6.51× vs 4.59×) ⇒ excess mean A-weight **4.28× [2.28, 9.86]** that of a pure 44.6 Hz excess ⇒ **effective centroid 63.5 Hz [54.2, 79.6]** — the whole interval **above the 50 Hz ceiling**, and on `gp-0x6c2c`'s band-pass peak (61 Hz). First data ever to place grind #2 energy above the ceiling; independent of the firmware arithmetic that set V68's threshold. Robust to statistic (5.64/3.07/3.31); mic fires on 8 of 10 and tracks bar magnitude. 🛑 **MEAN-WEIGHT inversion — it proves the excess is not all sub-50 Hz, it does NOT locate the energy**; harmonics at 89/134/178 Hz are BELIEF. ⚠ Tyre scrub not excluded (partial ρ **+0.507 [+0.297, +0.634]**, n=2956, survives |rate|/effort/speed control). ⚠ A reviewed centroid of "95.5 Hz [66.8, 170.5]" **does not reproduce** — that maps to 18.29×, not 4.28×.
- ★★ [Grind #1 is a TORSIONAL COLUMN MODE; grind #2 reaches the chassis](accord-grind1-is-torsional-grind2-reaches-the-chassis.md) — two independent estimators on the same events. **Level:** grind #2 clears its null on **every** IMU axis (bar 77.1 / `ay` 58.8 / `gz` 36.2 / `gy` 21.3 / `ax` 20.1 / `az` 19.2 / `gx` 11.4, mic 4.59); grind #1 clears on **NONE** over 48 events (1.45–2.10, all inside null) while the bar reads 12.87. **Coherence** (scale-free): grind #2 **0.82–0.88 every axis** vs 0.30–0.61 control; grind #1 **no contrast anywhere**. ⇒ that is **why the IMU never showed grind #1's reduction** — it was never coupled to it, at any frequency, independently of the 50 Hz ceiling. Axes identified from data (`ax` vertical, `az` ρ −0.839 vs dv/dt, `gx` ρ +0.975 vs v·steer = yaw, `gy` pitch / `gz` roll). BELIEF: lateral ≫ roll > pitch ≈ vertical ≫ yaw reads as a **lateral rack/subframe force with a roll couple**, not wheel-hop, not yaw.
- 🛑 [The mic read 1.061 on grind #1 — inside its null — on a large REAL oscillation](accord-mic-negative-carries-almost-nothing.md) — the cleanest demonstration of its blind spot in the corpus: the bar reads **12.87× [9.0, 14.9]** on the same 48 events. **Rule: a mic POSITIVE is informative** (its creep grind #2 control **replicates at 4.59× [2.95, 8.31]** vs the 4.14× on record, different estimator *and* different control design, and via the A/un-weighted contrast a positive carries real spectral information); **a mic NEGATIVE on a TACTILE event carries almost nothing.** Never read "the mic saw nothing" as "there was no vibration" — it means "nothing this instrument can hear."
- 🛑 [Three timing constants: the CAN rate, the 115 ms mic delay, and the ±50 ms lead/lag floor](accord-three-timing-instrument-constants.md) — **(1) `1/median(dt)` is the WRONG CAN rate**: frames are timestamped **per log packet**, so on r47 12 % of `dt` exceed 15 ms and p10 is exactly 0, and `median(dt)` reads **100.76 Hz** on a grid that is **100.000 Hz to 2e-5**. Use the mean rate + an index lattice; timestamps wander **7.5–10.3 ms** = the CAN alignment uncertainty. **(2) The microphone pipeline delay is 115 ms**, MEASURED against road impacts (35 segments, peak ρ 0.512); `micd.py` alone predicts 75 — the extra **~40 ms is audio-capture buffering**. Subtract it from any sound↔CAN alignment. **(3) Accel and gyro carry SEPARATE hardware-timestamp offsets** (`a_off_sd` 1.47–2.19 ms): bar→IMU lags read +45/+80 ms on bursts but **+90/+40 ms on road excitation with no grind — same magnitudes, order swapped** ⇒ **±50 ms is the empirical floor and NO physical ordering is resolvable** (true transit 0.2–3 ms, 30–500× below the finest step). Use **coherence**, not lag, to test causation here.
- ⚠ [The highway acoustic budget: a quantified BOUND, because the test cannot bear the weight](accord-highway-acoustic-budget-bound.md) — κ = **0.0091 [0.0059, 0.0159]** (log–log slope 0.72 [0.39, 0.96], R² 0.83) calibrated on creep bursts predicts a highway acoustic excess of **8.5–12.7 %** against a measured mic floor of **19.3–109.9 %** ⇒ the prediction sits **2–9× UNDER the instrument** on every route. **A highway acoustic null is therefore uninformative**, and the honest output is the bound: unexplained acoustic energy up to **2–9×** the sub-50 Hz-implied amount would be invisible. Three assumptions, each failing in a known direction (radiation efficiency ~f² vs a 9.9× louder ambient — partly cancelling, **neither measured**; a torsional highway mode would be invisible to the IMU even at creep; the floor is a steady-tone floor, a <5 Hz modulated event is ~5× easier). ⚠ **The joint detector is MIC-LIMITED** — the mic is the minimum channel in **97 %** of burst blocks ⇒ **"joint" buys SPECIFICITY, not sensitivity**; its reach is the *weakest* channel's. Highway counts 1/0/1/4, exact Poisson intervals **all overlap**.
- ⚠ [The highway tri-channel coincidence is REAL but DOSE-INDEPENDENT — it does NOT revive the rate lane](accord-highway-trichannel-coincidence-is-dose-independent.md) — top-decile 40–49 Hz highway blocks vs a circular-shift null: the chassis clears on **4/4** routes (`ay` 1.347 / 1.866 / 1.372 / 1.437, `gz` 1.348–1.608) and the mic on **2/4**. So the joint view *does* see something the single views underweighted. **But it is not monotone in dose** and the **stock Kd = 1.00 lane is not the lowest** ⇒ it is the already-characterised **manoeuvre-loading tail**, a property of car and driver, not firmware. The rate lane has now returned null at highway under **three** independent statistics — pooled level, event rate, and tri-channel coincidence — with both positive controls firing. A fourth channel did not change the answer, only the characterisation.
- 🛑 [A re-cut under the same build number DESTROYS its predecessor's plain image](accord-recut-overwrites-the-previous-plain-image.md) — **OPEN HAZARD; the fix is recommended, NOT applied.** Two V70 cuts 19 min apart both wrote `_v70_plain_image.bin`, so the second silently **overwrote** the first's snapshot while the first's `.rwd` stayed **flashable** ⇒ **an artefact existed that NO gate could check** (`verify_v70_image.py` asserts the *current* topology so it **fails on the old build by construction**; `diff_build_vs_stock.py` has no image to read). ⚠ Its bytes are documented only because they were read **inside the 19-minute window** — luck, not process. ✅ The flash risk was closed by renaming it `SUPERSEDED-DO-NOT-FLASH-…` (`9d44efc`); 🛑 **the verifiability hazard is not.** **FIX (not done): write `_v<NN><tag>_plain_image.bin`, or refuse to overwrite a snapshot whose SHA differs.** ⚠ The old image **cannot be trivially regenerated** (its builder config is gone); recovery by decoding the `.rwd` **was not attempted**. ★ Companion: **`bit6 ⇒ bit3` is build-CLASS identity, never FILE identity** — re-cuts share a byte-identical cave, so **the filename is the only pre-drive discriminator.**
- ★★★ [V70's 4-bit SIGN probe — the durable part of V70](accord-v70-built-sign-probe.md) — 🛑 **no SHAs/build status in that memory on purpose: V70 has been RE-CUT and its hashes, filename and control path change with each cut** (they live in `docs/STATE.md`'s "V70 — BEING RE-CUT"); **the probe does not change.** ★★ 68 of 68 cave bytes, zero spare: bit6 `gp-0x6ada >= +512` · **bit5 `gp-0x67fa == 10` (THE STATE GATE)** · bit4 `gp-0x6adc >= 0` (r26 SIGN) · bit3 `gp-0x6ada >= 0` (r24 SIGN). ★ **Build-CLASS identity from the value set alone — a first**: bit3 is guaranteed non-constant ⇒ invariant **bit6 ⇒ bit3**, only 12 of 16 payloads reachable ⇒ **V53/V54/V65–V69 excluded absolutely**, incl. the build on the car; ⚠ residual **V55/V57/V58/V64** filename-only. 🛑 **It cannot separate two V70 cuts from each other — build-CLASS identity is not FILE identity.** 🛑 bit4 is the SIGN not a `+512` threshold (cave 2 B short; a `>=+512` null was the *predicted* outcome) ⇒ **r26 LIVENESS, not the quantitative `a`.** ★ bit3 is amplitude-independent ⇒ **bit6 = ratchet SIZE, bit3 = PRESENCE.** 📋 Pre-registered: **bit5 reads LOW.** 🛑 **A SUPERSEDED FIRST V70 `.rwd` (`…LKASGATED-V68CONTROLPATH…`) IS STILL IN THE FLASH DIRECTORY** with the OPPOSITE control path and a **byte-identical cave** ⇒ the probe cannot separate them on-car; **rename it `SUPERSEDED-DO-NOT-FLASH-…`.** ⚠ It is also unverifiable now — both cuts write `_v70_plain_image.bin`, so the newer one **overwrote** the older image. ⚠ **Lesson: a fixed `_vNN_plain_image.bin` silently destroys the previous cut of the same version.**
- ★★★ [The ratchet, first real characterisation](accord-ratchet-characterised-on-route-4f.md) — **46 windows / 118 s at ≥1200 counts p-p, peak 6,065**; seg 1 a **continuous 20 s** event, seg 0 one more — **the operator's "mostly segments 0 and 1" confirmed exactly, called before the data.** Median **7.79 Hz** (zero-crossing, FFT-free) / 7.56 Hz (spectral), matching the recorded 7.56 ± 0.36. Order veto: slope **+0.0358 Hz per m/s** vs order 1's 0.482 ⇒ speed-invariant; rpm 773–1724 with f0 static ⇒ not an engine order. ★ **NEW: the ~7.5 Hz line is in the BAR and the ANGLE-RATE but NOT in openpilot's COMMAND** (`e4tq` 6–9 Hz prominence 2.7 vs a threshold of 10, holding at rail duty ≤2%) ⇒ **the loop closes inside the EPS + plant; openpilot is not the oscillator.** **44/46 engaged, 0 manual**, Fisher **p = 4.6e-5**, matched **6.65 [2.45, 12.85]**. ⚠ **Widen "creep only"** — one 4,843-count episode at **12.7 m/s**. ⚠ **Q is NOT measurable at NFFT 256** (cap ~13.3) ⇒ the recorded Q ≈ 36 is neither confirmed nor refuted. ⚠ **The "flat-topped/saturated" premise is NOT what `4f` shows** — crest **2.07–2.45** vs 1.414 for a sine, no flat-topping on any filter ⇒ BELIEF-level re-framing. r24 dose ladder 0×→4×: **NULL and under-powered** (negative control itself 0.38–2.47). ⚠ **Route `4a` cannot speak to the ratchet** — hands-off cell is 13.0 s with zero episodes.
- ★★★★ [`gp-0x67fa` state-gates the assist chain — and state 10 SPLITS IT IN HALF](accord-gp67fa-state-gate-on-assist-chain.md) — **[EVIDENCE, instruction level, `FUN_0002214a` `0x2214a`–`0x22a84`]**, index a plain `1 << (gp-0x67fa & 0xf)`, no off-by-one. **The guard wraps the `jarl` in the CALLER**, and each callee has exactly one call site ⇒ in a masked-out state the function is **never invoked at all** — no stack frame, 0% of body. **THREE masks:** `0x221d6` **`0x830` = {4,5,11}** → `FUN_00036388` + **`FUN_000428d4` (the OSC DETECTOR)**; `0x22518` **`0x930` = {4,5,8,11}** → `FUN_00028ea6`/`FUN_0002b422`/`FUN_0002b57a` (**ARBITRATION = `gp-0x6806`'s producer**); `0x2269a` **`0xc30` = {4,5,10,11}** → `FUN_0003a382` + **`FUN_0003aa2c` (THE AGGREGATOR)**. ⇒ **in state 10 the aggregator and the residual lane RUN while the detector, return-to-centre AND arbitration DO NOT — assist is delivered from a stale `gp-0x6806`.** ★ **State 10 is REACHABLE in normal operation**: written twice in `FUN_00019970` (the state-4 handler) — `0x199CC` (diagnostic, `tp+0x74d0 == 0xa`) and **`0x19A72` (NORMAL)**, gated on bit 15 of `gp-0x6d78` with bit 16 → state 11 taking priority; writer set over 33 `st.b` sites (Ghidra and a raw byte scan agree exactly) = {1,3,4,5,6,7,8,9,10,11}. **[OPEN] what bit 15 means** — that decides how *often*, not whether. 🛑 **This is a LIVE ALTERNATIVE for the five-build detector null** ("never CALLED" has the identical signature to "ran and found nothing"). ⚠ **BUT V67's OWN PROBE ARGUES AGAINST IT, and must be quoted alongside**: state 10 is absent from `0x930` too, so `gp-0x6806` would go **stale** there — yet V67 measured `gp-0x6806` == `latActive` in **150,302/150,327 = 99.983%**, all 25 disagreements single-frame edges. **A stale flag cannot track transitions that closely** ⇒ **the ECU is predominantly NOT in state 10 while engaged and the nulls are probably GENUINE** [BELIEF, indirect]. ✅ **V70's bit5 rung (`gp-0x67fa == 10`) settles it, non-vacuous both ways**: ≈ 0 ⇒ the nulls are genuine and five builds vindicated; materially non-zero ⇒ the nulls were on the gate. ⚠ **The detector has a SECOND entry gate, still OPEN** — `FUN_00046ea6(5)` on bit 5 of `gp-0x18d0`/`gp-0x18d4`; the record's earlier closure established only that that **function** has one caller, **not** that the **bit** is clear in operation. 🛑 **Bus `STEER_STATUS` is NOT `gp-0x67fa`** (`4f` reads ST=0 on 47,990/47,990 while steering, and state 0 is in no mask) — any reasoning that equated them is invalid. ⚠ **Provenance:** decompiled against stock `code.bin` with the 33 writer sites byte-identical in `_v68_plain_image.bin`; the **dispatcher itself was not decompiled from a V68/V69 image** — BELIEF by adjacency, not EVIDENCE. ⚠ `get_xrefs_to` returned a false "No references found" here, and a `jarl` Format-V scanner mask bug (bits 15:11 are **reg2**; `disp = ((hw1 & 0x3F) << 16) | hw2` sign-extended from 22 bits) gave zero hits for functions Ghidra had just given callers for.
- ★★ [The ~28 Hz lane-change transient is DOSE-INDEPENDENT](accord-lane-change-transient-is-dose-independent.md) — V69's stated purpose **failed**: the transient **survived and is LARGER in p-p on V69** (2,599 / 4,094 counts vs V68's 1,468), and **it runs at full amplitude on the STOCK rate lane** (V58/r2b at 1.000× → 2,389 counts p-p @27.59 Hz; **V59/r2c at 1.000× carries the corpus's largest, 3,283 @27.07 Hz**). Non-monotone — V62 at 2.000× is *quieter* than V58 at 1.000×. Pooled speed-matched **2.000×/1.000× = 1.176 [0.641, 2.320]** (inside null), **2.403×/1.000× = 2.897 [1.271, 11.439]** (does not clear), route-level Theil-Sen slope on dose **+5.736 [−25.432, +34.934]** — **0 inside**. ★ **Excitation, not gain, is the live candidate**: within dose = 1.000× exactly, ALC vs driver-commanded = **2.389 [1.453, 4.898]** (null [0.44, 2.26], does not clear, one manual route), and holding excitation fixed collapsed the 2.403× contrast **2.849 → 2.013** with the CI crossing 1 — *"an excitation contrast wearing a dose label"*, the same class as the withdrawn 28 Hz "mode". ⇒ 🛑 **V70 must NOT chase the rate lane for this symptom.**
- 🛑 [Size a probe rung against the LANE's own reachable output, not a downstream gate width](feedback-size-probe-rungs-against-lane-reachable-output.md) — V69 spent **all three rungs** and got nothing, and the failure was **arithmetic**. **bit4 (`gp-0x6ad4` ≥ +4096) was STRUCTURALLY VACUOUS — it could never have fired on any build, any drive**: `±0x2800` is the **ERR *input* clamp**, while the output is clamped to **±CEILING = MIN of three LERPs**, the binding one `0xC67C2`/`0xC67C8` indexed on **voted vehicle speed**, **max 1024**, starting at **ZERO** — at the ratchet episodes' speeds CEILING was **164–341**, so the test sat **12–25× above the lane's entire reachable range**. ★ Also explains **why V56's mute of this lane changed nothing**. **bit5 was insensitive not vacuous** (reachable max 5786 ⇒ 4096 = 71% of range, saw only the top 29%); **bit6 had no exposure** (replay predicts ~1 hit, observed 0, p ≈ 0.37 — not the V64 gate failure, but not a positive control either). ⇒ **compute the producing lane's reachable range at the operating point and state it next to the threshold**; budget a probe like a cave — **enable + raw input + a rung whose range you have computed**. Companion to [[feedback-probe-the-gate-not-just-the-output]]: that one is *which signal*, this one is *what value*.
- ★★★ [V69 BUILT then FLASHED, ×4 + a ratchet probe](accord-v69-built-speed-shaped-rate-lane.md) — reverts V67/V68's **flat** LKAS-gated arm (whose delivered multiplier *rises* with speed and peaks at highway, **2.4383×**) and scales the low-speed end of Honda's own gain surface instead: **4.000× to 10 km/h → EXACTLY 1.000× at and above 50 km/h**, both candidate axis scales, **no hump**, highway byte-identical to stock (12,221-point sweep — ≥ 50 km/h reads only rec2/rec3, which the edit does not touch). 🛑🛑 **What ×4 costs, and it was the operator's explicit call:** (1) **the flown bracket is BROKEN** — at 2.000× GATE 2's magnitude leg *interpolated* between stock (1.00×) and V62/V65 (2.00×, flight-clean); 4.000× **extrapolates to twice the largest dose ever driven** (phase untouched, the lane is linear, V65 measured the aggregator never railing over 120,049 frames, grind #1's dose-response monotone through 2.00×); (2) **saturation CROSSES the record** — peak gain 12288 rails r24 at |dtorque| **683** vs the repo max **839** (margin **0.81×**, *it can rail*; at ×2 it could not), and every |dtorque| here is a **LOWER BOUND**; (3) manual creep is 4.000× on the pessimistic scale, manual highway still stock. ⚠ **P1/P2/P6 were sized for ×2 and are NOT re-derived** — the dose-response is measured only to 2.00×, so quoting their intervals at ×4 invents precision; read them as directions. Image `48bb4192…`, RWD `e62fcbba…`.
- ★★★ [V69's probe is re-aimed at the RATCHET, not the grinds](accord-v69-ratchet-probe.md) — **bit6 `gp-0x6ada` ≥ +4096** (r24's lane OUTPUT after its ±0x2000 saturating clip — **0 readers / 1 writer image-wide**, so duty is a rail-proximity meter that prices the ×4 dose on-car) · **bit5 `gp-0x6b62`** (return-to-centre, ±0x2000 ZERO gate — **the operator's own hypothesis, never probed in 69 builds**) · **bit4 `gp-0x6ad4`** (the **unfiltered** residual lane, ~~±0x2800 ZERO gate~~ 🛑 **STRUCTURALLY VACUOUS — see below**, gain LERP-indexed by `gp-0x671a` ⇒ it **closes a loop from Honda's own detector back into assist**). 🛑🛑 **POST-FLIGHT 2026-08-04: ALL THREE RUNGS FAILED** — bit4 could never have fired on any build/any drive (the ±0x2800 is the ERR *input* clamp; the output CEILING at the ratchet's speeds was **164–341** vs a 4096 test), bit5 was **insensitive** (4096 = 71% of a 5786 reachable max), bit6 had **no exposure** (~1 predicted hit, 0 observed, p ≈ 0.37). See [[feedback-size-probe-rungs-against-lane-reachable-output]]. ✅ `decode_v69_ratchet.py` fixed in place (128-sample floor replacing a 256 that made its own null a tautology) — **do not regress it.** **Two reasons for the original aim:** the grind detector is **exhausted** (`gp-0x67df` has **never** been non-zero in this kit — 0/53,991 V68, 0/186,321 V67 ⇒ an uninterpretable null), and ★ **the ratchet is the one symptom a 100 Hz channel can RESOLVE** (~7.4–7.6 Hz ⇒ ~13.5 samples/cycle, so each bit's **own time series** carries the line; at 21 and 43 Hz it never could). Target class: the ratchet is **symmetric + amplitude-saturated** ⇒ a hard nonlinearity in the loop, and V65 killed only the aggregator **SUM** — each lane's **own** ZERO gate / clip upstream of it has **never been measured**. **Three rungs is arithmetic**: 44 free cave bytes / 14 per signed-halfword rung. 🛑 **Residuals:** rungs are **one-sided**; **bits 5/4 have no positive control** (read bit6 first — the V64 lesson); V69-vs-V66/V67 is **not** structural, though V68 (the build on the car) is excluded absolutely. Decoder `rlog-tools/decode_v69_ratchet.py`, linked mechanically — the build **fails** on a stale `CAVE_HEX` or a retired rung still described as live.
- ★★ [Both inline torque-rate lanes are mirrored to RAM and NOTHING reads them](accord-aggregator-lane-mirrors-6ada-6adc.md) — `FUN_0003aa2c` stores **r24 → `gp-0x6ada`** (@`0x3AD5A`) and **r26 → `gp-0x6adc`** (@`0x3AD4E`) **after** their ±0x2000 saturating clips, each **0 readers / 1 writer** image-wide (raw-byte census, two decoders). ⇒ **free, blast-radius-zero telemetry taps on exactly the quantity every rate-lane build scales**, and the strongest GATE-1 statement in the chain: nothing consumes them, so a probe cannot perturb anything even in principle. 🛑 ~~⚠ `gp-0x6adc` reads ~0 (r26 is structurally inert, `0xC6564` = 40 zero bytes) — probing it would be a rung spent on a known constant.~~ **REVERSED 2026-08-04 — r26 is LIVE, so `gp-0x6adc` should carry real signal, most strongly in hands-off creep. It is a legitimate rung, not a known constant.** 🛑🛑 **ONE-BIT TRAP, concrete here:** `ld.h` is opcode **0x39** and `st.h` is **0x3B**, and `gp-0x6ada`'s *only* real instance **IS** the store (`64c72695` @`0x3AD5A`), carrying **the same displacement halfword** an `ld.h` probe must emit (`24372695`) — one bit turns the read into a **write into a 1 kHz aggregator lane**. Assert the opcode field **by value**, in builder *and* verifier.

- 🛑🛑 [BOTH confirmed fixes had fallen OFF the car](accord-both-confirmed-fixes-were-off-the-car.md) — byte-read across all 60 built images 2026-08-04: **`0x454FE`** (V42's ratchet kill, recorded *"CONFIRMED ROOT CAUSE, carry forward"*) is carried by **V42–V52C only, stock in V53→V70** — a **silent rebase loss**, nobody decided it; **`0x3AB76`+`0x3AC20`** (V62's ×2 on BOTH lanes, the kit's only measured grind-#1 fix) is carried by **V62 and V65 only** — removed as **V66's confirmatory control** and never restored. ⇒ **from V66 to V70 the car carried NEITHER**, across ten builds. ⚠ `0x454FE`'s later retirement rested on *"`STEER_STATUS==4` fires 0/37,922"*, **VOIDED** when bus ST was shown not to be `gp-0x67fa`. ⇒ **RULE 3** (byte-check the current image before citing any confirmed result) and *"write the restore into the next build's spec"*. ⏳ both restored in V71.
- ★★★★ [THE DOSE AXIS THIS KIT HAS USED SINCE V62 IS THE WRONG LANE](accord-r24-r26-two-selectors-one-gate.md) — 🛑 **there IS a clean single-variable r24 series and it says r24 is NEAR-INERT**: stock→V70→V69 holds r26 at ×1 and steps r24 **×1→×2→×4 = 879 / 729 / 746, all three CIs overlapping** [EVIDENCE, medians recomputed from `_grind2_lib.wrecs`]. **Every build that FIXED grind #1 changed r26** (V62 ×2, V67/V68 ÷6.00); **every build that changed only r24 did not.** Structure: `r26→gain_A` (`0x3AB5E` `0xC6444`=512 ▸ `0xC643E` ▸ own LERP 3072) vs `r24→gain_B` (`0x3ABFE` `0xC6442`=1024, the `gp-0x671d` mask, **outranks all** ▸ `0xC6446` ▸ `0xC6440`=2048 ▸ the mode-10 surface) — **one shared gate**, so V67/V68's one-byte repoint **raises r24 AND cuts r26 6.00× at once**; net `(5244+512a)/(3072+3072a)` ⇒ **1.707× at a=0, PARITY at 0.848, BELOW stock above.** **r26 LIVE**: `gp-0x6adc` strictly negative **1,644/18,010** ⇒ [r26-inert](accord-r26-is-structurally-inert.md) LEG 2 **REFUTED**. ★ Byte facts: gain_A's records `0xC6A68/7C/90/A4` **byte-identical across all 11 images** (so ÷6.00 is exact, engaged-only); separate RAM `gp-0x6e30/6e28` vs `gp-0x6e40/6e38`, two halves of `FUN_0003ad74`; **gain_A is NOT mode-indexed** ⇒ V69/V70's surface edit could never reach r26; **no `gp-0x671d` arm on the r26 side.** ⚠⚠ **CARRY UNEXPLAINED: r26 ×2 AND ÷6 BOTH helped, ÷6 more** — both monotone stories refuted by the same two rows; **the leading open question.** ✅ V62's `sar` is the only dose-exact encoding ⇒ **`0x3AB76` is V71's load-bearing half; `0x3AC20` is parity only.** 🛑🛑 **`0xC6444` is STRUCK — NULL BY CONSTRUCTION** (read only at `0x3AB5E`, only when `lp != 0`; `gp-0x683c` has **0 writers**, so on every gateless build the load never executes).
- ★★★★ [V70 FLEW — grind #1 is BACK at the STOCK level](accord-v70-flew-grind1-back-at-stock.md) — route `50--50f2e00e8f`, 181.6 s, 18,010 frames, **flight-clean**; ⚠ **small route** (engaged 72.4 s, **engaged creep 28.9 s**, highway 7.9 s, **zero manual highway**; **seg 0 is PARKED**). Median `e_18-22` engaged creep **729.1** — **CONSISTENT** with stock (P=0.635) and V69 (P=0.495), **EXCLUDED** from V62/V65 and V67/V68 (**P=0.0000**). ★★ **CI OVERLAP IS NOT A TEST** — the subsample-at-matched-exposure test excludes V62's level at **P<5e-5** where the CI comparison called it undecided ⇒ **"V70 is not at V62's level" IS established; where it sits between stock and V62 is NOT.** 🛑 **AND GRIND #1 IS BLIND TO r24 GAIN** — log-log slope **−0.144 [−0.991, +0.347]**, pairwise P=0.667/0.610/0.426 ⇒ **it cannot be an in-force check for r24 on ANY future build** (structural, not power). **Grind #2: 0 bursts but "gone" NOT established** (P(0)=0.34/0.56/**0.98**, power 66/44/**2**%); V67 already cleared engaged creep ⇒ **replication, not credit.** 🛑 **"Stiffer" undetected bus-side, its saturation mechanism REFUTED** (hard clamp; V69 **0.0000%** at the rail). 🛑 The **rateKey/peak-velocity** hypothesis is **dead on scale B, alive only at the ~90th-pct worst instant on scale A** — its founding `A_rk=1927` was the **whole-drive** top decile, not a burst measurement (burst windows: 18–22 Hz swing **p50 140 / p90 327**; monotone window needs ≳1400, reached by **9.20%** scale A / **0.00%** scale B).
- ★★★ [The ratchet's Q is MEASURED — Q ≈ 40 at f0 = 7.793 Hz](accord-ratchet-q-measured-40.md) — one **12.81 s** provoked episode; ★ **the invariance test is what makes it real**: **39.0 at window cap 54, 40.0 at cap 111** (window-limited would have doubled) ⇒ ζ ≈ 0.0125, ~3× more lightly damped than the 21 Hz mode. ✅ **It CONFIRMS the record's Q ≈ 36** — the only thing superseded is *"Q is not measurable at NFFT 256"*, the claim it could not be measured, not the value. ✅ **Measured on the RIGHT data**: the episode reconciles with the transition trace (2×2,452 = 4,904 ≈ 4,894; speed span = the post-engagement window, **not** the operator's cranking) ⇒ **not contaminated**. ⚠ **ONE episode**, and f0 drift would **deflate** it ⇒ **40 is a LOWER BOUND.** ⚠ **Re-opens V47's FactorC+FactorE *"marginally quieter at 5 mph"*, never evaluated against the RATCHET** — damping is **exactly zero below ~35 km/h** while the ratchet lives at 4.9–8.0 km/h.
- 🛑 [Read the GAIN IN FORCE, not a lane OUTPUT](feedback-probe-the-gain-in-force-not-a-lane-output.md) — **four probes in a row** returned an uninterpretable zero by reading a lane output (V64/V67/V68's `gp-0x67df`, then **V70's bit6 0/18,010**). ★ V70's is the informative one because it is **NOT vacuous**: the replay predicts **311 hits on route 50's own data**, **stock predicts 52**, observed **0**, and `|dtorque|` off a 100 Hz grid is a **lower** bound ⇒ **delivered gain < ~1574 Q10, below stock's 3072**, and **`0xC6442`=1024 is the only arm predicting exactly 0.** ✅ the identification was verified and is **not** at fault. ⇒ **GATE 4: spend a bit on the SELECTOR/MASK before the lane's output** — V71's `bit6 = gp-0x671d != 0` is the first rung built to it. Companion to [size-probe-rungs](feedback-size-probe-rungs-against-lane-reachable-output.md) (which threshold) — this one says **which cell**.
- 🛑 [The state-4 CADENCE is REFUTED — state 4 is STICKY, then leaves permanently](accord-state4-cadence-refuted-state-is-sticky.md) — [EVIDENCE, instruction level] **`gp-0x68ad` can NEVER be set in the field** (both SET paths need permanently-zero flags: `gp-0x437c`, and newly-closed `gp-0x679d` whose sole writer reads `gp-0x67ba` — **one access image-wide, ZERO writers**) ⇒ `FUN_00019970` returns early ⇒ **4→5 never fires; state 5 is DEAD CODE on the road**. **`gp-0x6d78` bit 15 is a one-way OR-only latch** (15 sites, 1 writer, **no clear anywhere**) ⇒ **4→10 is a one-shot drift; 10→4 can never fire after.** With V70's bit5 at 0.0000%, **the reachable set is {4, 11}.** ⚠ **Tension to carry:** the substitution is **asymmetric** but the ratchet measures **symmetric** (skew −0.16…+0.06, crest 2.07–2.45) ⇒ **evidence against it shaping the CURRENT ratchet** — which is why V71 justifies `0x454FE` as *a confirmed fix lost by accident*, nothing more. 🛑 **[OPEN]** what sets `gp-0x6d78` bits 15/16 mid-drive (21 untraced callers).
- 🛑 [The aggregator is ELIMINATED — all EIGHT zero-type range gates are VACUOUS](accord-aggregator-zero-gates-all-vacuous.md) — [EVIDENCE, every ceiling byte-read] each gate is capped by its **own producer's** ceiling at or inside its window, on every drive and build: boost 512/2048 · damping **exactly 0 at creep** (FactorC `0xD27BC` Y[0]=0, multiplicative, ≈35 km/h onset) · friction 511/1024 · magnitude ±0x3000 **== window exactly** · LKAS ±0x2800 **== window exactly** · `gp-0x6ade` **0 writers** · resonance 1024 (**164–341** at the ratchet's speeds)/2800 · return-centre 5786/8192. ⇒ **no reachable hard nonlinearity in the aggregator stage**, joining the aggregator SUM (V65, 120,049 frames) ⇒ **the relay/limit-cycle framing for the aggregator is REFUTED — do not re-propose it.** ★ `FUN_00036388`'s counters give ~20–40 ms or ~1 s periods ⇒ **it INHERITS the ratchet, it does not GENERATE it.**
- ★★★★ [The ratchet is engagement-REQUIRED — and NO BUILD HAS EVER MOVED IT](accord-ratchet-is-engagement-required.md) — [EVIDENCE] grip confound removed (**both arms hands-off**, `|lowpass(tq,3Hz)| ≤ 300`, creep < 4 m/s), pooled over four routes/builds: **73/88 = 83.0% engaged vs 0/118 = 0.0% manual, Fisher p = 3.8e-41** — **zero hits in 118 manual hands-off creep windows / 302 s**. 🛑🛑 **The rate is BUILD-INDEPENDENT (80/81/79/94% on V70/V69/V62/V59) ⇒ no build in this kit has ever moved the ratchet.** ⚠ **Supersedes "engagement-conditional, 44/46".** ★ Converse: **a hand on the wheel SUPPRESSES it while engaged** (V59 94%→14%, p=3.5e-4; V69 81%→37%, p=4.5e-3). ★★ **Transition trace at constant speed:** `t=33.9` (`lat` 0.06, eff 320) → **134 counts** vs `t=34.6` (`lat` 0.31, eff 441) → **1,179** — **8.8× in 0.7 s with speed FALLING** (1.75→1.60 m/s); death: eff 1548→2129 over 0.6 s collapses the band **910→273**. ✅ **The 6,502-vs-591 discrepancy is SETTLED** — 6,502 is **raw broadband**, the operator cranking at `lat`=0.00 where 6–9 Hz reads **190**; **the ratchet proper runs mono 135.2→146.7, NOT from 123.69.** 🛑 **Correction to the operator's framing, respectfully:** his hard **manual** provocation produced **no** ratchet (6–9 Hz p-p 422–797, prom 1–6); the manoeuvres **set up** the condition and **the ratchet fires when LKAS engages and he lets go** — **both parts of his account are correct, the causal order is the other way round.** ★★ ⇒ `0x454FE` is a **genuinely untested** lever for the ratchet (absent from all four measurements), and *engagement-required + hands-off + Q≈40 + damping exactly zero below ~35 km/h* ⇒ **at creep the driver's hand is the only damping in the system.**
- 🛑 [Decompile FIRST, then read the assembly](feedback-decompile-first-then-assembly.md) — standing operator instruction 2026-08-04. Structure from `decompile_function`; only then disassemble to pin an exact instruction/encoding. **Assembly CONFIRMS a claim you already framed; it does not FORM it.**
- ★★★★★ [The TWO-LANE rule — why fixing one grind fed the other](accord-two-lane-rule-grind2.md) — 🛑🛑 **AMENDED 2026-08-06: the SHAPE survives, the NUMBERS do not.** ~~"r24 ≳3.4x AND r26 ≳1.5x"~~ — **V62/V65's DELIVERED r24 is 2.000x, not 3.414x** (`sar` is a flat doubling of BOTH lanes, not the arm) ⇒ **never quote a numeric r24 threshold.** What survives: bursts have only ever been seen with **BOTH** lanes elevated, and each single-lane arm is clean (V71B r26 2.0x alone; V72/V73/V74 r26 0.25x with r24 stock). 🛑 **"What governs grind #2 is V62's `sar`" is REFUTED** — V71C carries NEITHER `sar` byte and produced a spectrally identical 44.31 Hz event (p99 1741.9 = 12.2x any non-bursting build; 3 of 13 corpus events in 5.28% of exposure, P(≥3)=0.028). 🛑 **The r26 cut's NECESSITY is NOT established** — V67/V68's "none" is 11.5 s at P(0)=0.80 and was the operator's HEDGE. ★ grind #2 follows the GATE not the hands (ungated V62/V65 burst in BOTH arms equally; gated V71C only engaged). 🛑 grind #1 and grind #2 are NOT the same mode — see the harmonic retraction.
- ★★★★★ [The grind-#1 fix and grind #2 are PERFECTLY COLLINEAR — never separated](accord-grind1-fix-and-grind2-are-collinear.md) — split-half null [0.663, 1.502] computed FIRST; the builds that measurably moved grind #1 are **exactly {V62, V65, V67, V68, V71C}**. Three show grind #2; the other two hold **11.5 s** and **0.0 s** of engaged creep-CORNER exposure — the cell where **18 of 21 creep bursts live** — and **0.0 s** of engaged high-rate creep. ⇒ **every build with adequate grind-#2 exposure failed to move grind #1.** All 13 merged events sit in the 29.4% of exposure held by the two "both lanes up" groups, **p = 1.2e-7**. 🛑 **Do not propose a rate-lane grind-#1 fix as if the trade were solved.** ✅ Costs no bytes to break: **~90 s of deliberate ENGAGED hard cornering at creep.**
- 🛑🛑 [Never log a HEDGE as a null — a well-powered subjective report outranks an under-powered measurement](feedback-never-log-a-hedge-as-a-null.md) — the operator's V67 words were *"mostly gone… might still be there somewhat… more so LKAS-engaged at low-speed, I am not sure. Might just be dampened"*; the table recorded **"none"**, and a later session cited that "none" to justify a build. The measurement behind it is **11.5 s, P(0)=0.80, 19% power**; his exposure was days. **How to apply:** keep the hedge or write UNMEASURED, never a null · every zero carries its exposure and P(0) in the same cell · match exposure on the PROVOKING covariate, not wall-clock · when he contradicts the record, check the record's power before defending it · **quote retractions whole** (`STATE.md:453` lost *"and V71C"* and flipped its meaning).
- ⚠ [V76 is UN-DERISKED, not de-risked — do not rename the artifact](accord-v76-rate-lane-restore-is-underisked.md) — built/unflashed; its **ENGAGED rate lane is byte-identical to V67/V68** ⇒ delivered (3.414, 0.250), and the r24 dose is **rate-dependent 1.707x→3.414x** (quoting "≈1.71x" understates it 2x where the bursts live). ✅ mode-proof; ✅ masking risk already closed (V67 read `gp-0x671d` 0/150,327). 🛑 grind-#2 risk **NOT ESTABLISHED**: 11.5 s engaged creep-corner (P(0)=0.607, 39% power), **0.0 s** high-rate creep. The powered r26-cut evidence is at r24 = 1.000x and does not transfer. ✅ **~90 s of engaged hard cornering at creep settles it in one drive.**
- 🛑🛑 [A ratio of two lines is NOT a harmonic test — shuffle the pairing](feedback-a-ratio-is-not-a-tracking-test.md) — the orchestrator published "grind #2 IS grind #1's 2nd harmonic, f_hi/f_lo = 2.003 [1.997, 2.008]" as the session headline and **retracted it the same session**. Two INDEPENDENT lines with those marginals give median ratio 2.048 [2.012, 2.072]. Harmonicity needs a **SLOPE** of 2.0, not a ratio; shuffling the pairing **reproduced** the observed ratio. Slopes contain 0 and exclude 2.0 on four routes.
- 🛑🛑 [`0x454FE`'s test was VACUOUS — state 4 never occurs while driving](accord-0x454fe-test-was-vacuous-state4-never-occurs.md) — `gp-0x67fa == 4` reads **0/123,277** and **8/92,826** (all eight in PARK). V71B/V71C carried the fix but it was **never in force** ⇒ a null by construction, NOT a falsification (the orchestrator published the wrong version mid-session). ★ What survives is stronger: it never runs on stock either ⇒ **structurally eliminated** for the 7.79 Hz ratchet. ⚠⚠ [OPEN] V42's confirmed hard-turn fix could not have acted either.
- ★★★★★ [V72's damping levers were NEVER IN FORCE — the damper is MODE-TABLE SELECTED](accord-damper-is-mode-table-selected.md) — `FUN_00034350` picks all five factors via `mode = *(byte)(gp+0x63fd)`, **13 variants**; V72 edited **10/11 only**; in those modes `|gp-0x6bd0| = 389` **unconditionally** so bit4 would fire 100% — it fired **0/87,940**. ⇒ inert **by table selection**, not a bad probe/seed/factor (all three eliminated first). ★ `39990-TVA-A160` "reads as" row 2 `TVAA1` — **an assumption in BUILD-LINEAGE, never a measurement**, and V44+ patched 10/11 because of it. 🛑 **V44/V47/V72's damping nulls are UNINTERPRETABLE, not falsified.** ⚠ Live mode OPEN: 4/5 and 12 consistent, 0-3 marginal, 10/11 excluded.
- ★★★★ [Grind #1 is a LIMIT CYCLE](accord-grind1-is-a-limit-cycle.md) — duty spans **64x** across builds while in-burst amplitude spans **1.24x** against a 5.62x dose ladder; bimodal on arms that carry it, unimodal on arms that suppress it. **Successful builds stop it STARTING, never shrink it.** Excess-over-control floors at 2.21 and **reaches 1.0 on no build**. ⊕ No value of `a` (0→32.0, summed or differential) orders the ladder ⇒ **not a scalar-gain phenomenon**. 🛑🛑 **RETRACTED 2026-08-06:** ~~V72 delivered V67/V68's bit-identical creep gain and scored stock (614) ⇒ the rate lane is exhausted.~~ **At ≤10 km/h V72 delivers r24 = 1.000× and V67/V68 deliver 1.707–2.048×** — they match on r26 only. ★ **RULE 7 in disguise:** both carry the literal **5244**, V72 in the **mode-10 `gain_B` surface (inert)** and V67/V68 in the **`0xC6446` gated arm (live)** — *same number, same lane, opposite delivery.* ⇒ **compare DELIVERED gains, never cal values**; **"the rate lane is exhausted" is WITHDRAWN** (V72 grind #1 ratio 0.953 = unmoved, V67 0.430 / V68 0.229 = moved — equal gain could not do that). The limit-cycle finding itself is unaffected.
- ★★★★ [TWO ratchets — MICRO **is** the 7.79 Hz line](accord-two-ratchets-micro-is-the-779hz-line.md) — operator-confirmed. **7.7 Hz is below the ~20 Hz hearing threshold**, which is why it is felt and not heard. **NOT grind #1 at lower amplitude** (six tests, incl. a tracking slope with a working positive control, a shuffle control, and *opposite-signed* position dependence). **V72 did NOT fix it — attenuation 1.0, three instruments.** ⚠ MACRO is **unmeasured and unattributed**: both purpose-built instruments **fail their own positive control**, so the null is uninterpretable in BOTH directions ⇒ V73 **adds only**.
- 🛑 [Compare SHAPES across pipelines, never single-bin LEVELS](feedback-cross-pipeline-compare-shape-not-level.md) — two independent implementations of the same metric differed **1.16–2.40x per bin** while agreeing on shape at corr **0.856**; comparing one cell nearly filed a contradiction that did not exist. Quote a bin's CI before comparing it to another agent's. ⊕ Sensor zero confirmed **-4.4 deg**, twice derived.
- ★★★★★ [THE CAR IS `TVCA4` — modes 24 (manual) / 26 (engaged), and the mode TOGGLES with engagement](reference-accord-car-is-tvca4-mode-24-26.md) — forced from the MANUAL arm (raw 8 is in no row of `0xCD000`); the engaged reading of 10 alone never would have. **Every “mode 10/11” statement before 2026-08-05 is wrong.** ★ Engaged (e014/e015) and disengaged (e012/e013) column sets are **disjoint**, 13 and 13 — so dosing the engaged columns of all 16 rows leaves manual byte-stock. 🛑 The r24 dose ladder never existed.
- 🛑🛑 [RULE 7 — a lever is MODE-PROOF, or it is a BET](feedback-rule7-mode-proof-or-a-bet.md) — **every measured fix in this kit came from a mode-proof lever; every mode-indexed lever was INERT** (V44, V47, V72 B/C, both V73 levers, the whole r24 dose). Write every mode or probe the selector. ⊕ Corollary (a): several symptoms were **created by this kit’s own earlier fixes** — grind #2 is V62’s `sar`. ⊕ Corollary (b): **“FALSIFIED” must name the SYMPTOM.**
- ★★★★ [There are TWO dead zones — speed AND rate](reference-accord-two-dead-zones-speed-and-rate.md) — `|gp-0x6bd0| = (FactorC × FactorE) >> 10` (B and D flat 1024, seed pinned 1024). FactorC `X[0]` = 2240 = **35 km/h** (speed); FactorE `X[0]` = **60** (rate); **both rows start at Y[0] = 0**. 🛑 Scaling a record by k is **structurally vacuous** (k × 0 = 0) — only lifting `Y[0]` delivers. Raising FactorC costs no rate-proportionality; flattening FactorE makes a **relay**. ★★ Measured `gp-0x6ac0` in-burst = **99** [94,113] ⇒ **no FactorC rung alone reaches** the ~43 [30,60] requirement (6 / 14); **opening BOTH dead zones gives ~50** — the V74 lever (`FactorE X[0]: 60 → **12**`, the top of the reviewed 6-12 band), and it works by opening the RATE dead zone rather than raising a gain. ⚠ The column→motor rate conversion is biased LOW through a torsional mode, so err low.
- ★★★★ [V42’s confirmed fix was the r26 KILL, not `0x454FE`](reference-accord-v42-fix-was-the-r26-kill.md) — V42-vs-V41 byte diff: `gain_A` all four records → `[0,0,0,0]`, `0xC643E`→0, `0xC6444`→0. Closes a two-session [OPEN]; ch.2 sat in the ledger marked FALSIFIED **against the vibration**. ⚠ The wider “ratchet tracks r26” pattern is a **leading hypothesis, not a result** (n=2, operator reports, and the measured micro ratchet has never moved).
- ★★★ [The 7.8 Hz micro ratchet — Q ≈ 14, a ring-down, load-dependent f0, no trigger](reference-accord-78hz-mode-characterisation.md) — **supersedes Q ≈ 40** (a window artefact). The mod-100 scheduler reaches only {1000,500,200,100,10} Hz ⇒ **no 7.8 Hz firmware divider exists**. 🛑 Friction-lane gain **corrected**: 3.08× (+9.8 dB) at 7.79 Hz and 7.496× at 20.9 Hz — any ladder built on the stale − 14.5 dB figure is void.
- 🛑 [Verify with Ghidra decompilation AND byte reads — BOTH](feedback-verify-with-ghidra-and-bytes-both.md) — standing operator instruction 2026-08-05; a byte diff proves the bytes, a decompile proves the structure, neither alone proves the lever. Re-import the BUILT image; reconcile both explicitly.
- ★★★★★ [V74 FLEW: THE DAMPER IS IN FORCE — the kit's first positive control on `gp-0x6bd0`](accord-v74-flew-damper-is-in-force.md) — route `5d`, 101,118 frames. `bit7 = (gp-0x6bd0 != 0)` fires on **23,603 frames (23.342%)**: ENGAGED **39.927%**, **engaged creep 67.443% vs manual creep 0.292% = 230.7×**. V72's probe on the SAME cell read **0/87,940**. ★ Negative control is perfect — all 943 manual bit7 frames are within 5 s of a disengagement, **0 of 40,398 beyond** ⇒ the engaged-column-only design is confirmed and manual stays byte-stock; it also re-measures V73's 2.08 s fall lag on a different cell. ★ Positive control: disengaged frames past both stock breakpoints read **157 / 100.000%** (the *"183 / 99.45%"* version used the superseded 10.0 rate scale). Dose at the ratchet's own 99 counts = **exactly 50**, the design target; stock **0** at every creep speed; 0 frames at the ceiling. 🛑 The torsion-bar↔aggregator unit gap is NOT closed. ★★ Build identity settled by a byte read the decoder does not do: **V67–V73 all open their cave `203e1000 movea 0x10` ⇒ constant bit7**, and 5d reads bit7 = 0 on 76.658% ⇒ cannot be any of them. ⚠ D2 was UNPOWERED, not a pass.
- ★★ [V74's flight: gate CLEAR, result UNDERPOWERED, both symptoms active](accord-v74-flight-underpowered-both-symptoms-active.md) — `5×f0` prominence **2.227** vs the 3.0 abort ⇒ **the relay-chatter risk is retired for this dose**. Success metrics all favourable, **none clearing CI** (duty 0.797 [0.544, 1.045], duration 0.934, envp99 0.835; MDE 2.0–2.9× on **9 episodes**) ⇒ 🛑 **an underpowered FAVOURABLE, not a null.** Both symptoms persist — 6-9 Hz **3.27×**, 18-22 Hz **2.72×** over the 24-28 Hz control. ⇒ first interpretable damper result in the kit: the dose is too small, or the damper is the wrong lever. **Next flight's binding need is engaged-creep EPISODE COUNT** (5d gave only 78.0 s of it). ⚠ Band ratios are sibling-agent work, not independently replicated.
- ★★★ [`gp-0x6ac2` is a SIGN-GATED BACK-DRIVE DETECTOR, not a rate](reference-accord-gp6ac2-is-a-backdrive-detector.md) — [EVIDENCE, decompile of `FUN_00041464`] `gp-0x6ac2 = |motor rate| >> 10` **iff `sign(motor rate) ≠ sign(gp-0x6b98)`, else 0** — i.e. the rack moving *against* the command. ★★ **⇒ the damper ceiling `0xC77A0` (all 26 modes `X=[300,800] Y=[512,1024]`) is PINNED at its 512 FLOOR in ordinary driving** and lifts only on genuine kickback ⇒ **size every damper lever against 512, never 1024.** ⚠ The validity bypass at `0x41852` (`|gp-0x6b98|` outside ±0x2000) writes a **`0xFFFF` sentinel**, it does **not** hold. 🛑 [OPEN] whether the ceiling's reader is `ld.h` or `ld.hu` decides whether that sentinel floors the clamp (−1) or rails it (65535) — one bit, opposite answers. Corrects the "steering angular rate" label in `reference_accord_corridor_lockstep`.
- ★★ [`FactorE X[1]` is the FREE dose lever; `FactorE Y` and `FactorC` are at their caps](reference-accord-factore-x1-is-the-free-dose-lever.md) — moving the knee **LEFT** raises dose at the symptom's operating point with the **surface maximum unchanged**, so it cannot breach the 512 ceiling: `X[1]` 400→**200** = **2.08×** at r=99 (50→104), engaged-creep mean 52.2→79.8. By contrast **`FactorE Y` has only 1.318× of headroom** ((429×Y[3])>>10 ≤ 512) and `FactorC` has **zero** (Y[0]→908 gives 821 > 512). 🛑 The effective dead zone is **r ≥ 15**, not `X[0] = 12` — the `>>10` truncation, not the breakpoint ⇒ lowering `X[0]` further buys nothing. ⚠ [BELIEF] the cost of a steeper ramp is **phase (GATE 2)**, unsettleable from a log; `Y[0] = 0` is preserved either way so there is still no chatter mechanism.
- 🛑 [The `4.7121` rate scale STANDS — do not revise it from the on-car fit](reference-accord-rate-scale-4p7121-stands.md) — `gp-0x6ac0` counts = |column deg/s| × **4.7121**. Treating the raw 0x18F int16 as `gp-0x6ac0` implies **10.0** and is **2.12× too high** — it put V74's `X[0]=12` at "1.2 deg/s" instead of **2.55**, and inflated a control cell to "183 / 99.45%" from its true 157 / 100%. On-car arbitration via V74's bit7: **4.7121 beats 10.0 in 8 of 9 episodes** (engaged agreement 84.74% vs 82.07%). ⚠ The fit *peaks* near 5.8 [5.12, 8.27] only because the estimator substitutes **column** for **motor** rate and its residual runs one way ⇒ **upper-biased. [EVIDENCE] 10.0 disfavoured; [BELIEF] 4.7121 correct.** Every column-rate-derived dose is a LOWER BOUND.
- ★★★ [openpilot has TWO hard rails, and they are the felt "throttle"](reference-accord-openpilot-two-hard-rails.md) — amplitude **`STEER_MAX` = 4096**, matched **exactly** to the firmware intake `FUN_00052676 = clamp(req × −4, ±0x4000)` since **4096 × 4 = 0x4000** ⇒ 🛑 **ZERO upstream headroom; raising `STEER_MAX` alone buys NOTHING.** Plus a slew cap at **123 counts/frame = 0.03 × STEER_MAX**, zero frames exceeding, dominant at highway speed — which **confirms exactly** the modelled term at `eps_lkas_chain_model.py:2238`. **16.07% of engaged time against one rail or the other.** ⚠ Sibling-agent measurement, not independently replicated; the `4096 × 4` identity is arithmetic and does check out. Not a licence to edit openpilot.
- 🛑 [Evaluate a no-clip rule on the OBSERVED envelope — and SAY which envelope](feedback-evaluate-clip-rules-on-the-observed-envelope.md) — the rectangular grid's worst corner assumes **849 deg/s**; route 5d's max was **330** (p99 188.7), **zero frames** above 2000 counts. **Three analysts reached three conclusions from identical arithmetic** purely by policing different envelopes. **How to apply:** name the envelope in the same sentence as the clip fraction, quote p99/p99.9 not the max, keep the grid as the safety floor and the envelope as the design target — and prefer a structural argument ("the peak is unchanged"), which needs no envelope at all.
- 🛑 [A CONSTANT probe field is NOT a void cave — only a constant ZERO is](feedback-constant-field-is-not-a-void-cave.md) — **this misled the orchestrator directly on 2026-08-06** and nearly produced a "void drive" headline on the kit's first successful damper build. `bits 6:3 == 0` is void *only because* 0 is unreachable in `gp-0x67fa`'s value set; **constant 5 is a MEASUREMENT** (5 on 101,117 frames; the one state-4 frame is the last of the route, in PARK). ✅ `decode_v74_probe.identify()`'s **code was always right** (`if states == {0}`); the defect is the prose — 🛑 **fix the `STATE_SHADOW_DISP` comment and `docs/STATE.md`, both of which drop the `== 0`.** State the probed cell's value set alongside any liveness claim.
- 🛑🛑 [An ANCHORED harmonic search presupposes its own answer — retire Falsifier B/C, don't re-size](reference-accord-falsifier-b-anchored-search-presupposes-answer.md) — V74's `5×f0` abort gate read ambiguous (K-free method hit the corpus MAXIMUM, CI crossing 3.0) because the search is anchored ±4 bins to that build's OWN predicted `5×f0` — it cannot tell "peak moves with f0" from "peak is fixed and 5×f0 happened to land near it." V74's true peak sits **43 NFFT-2048 bins** outside the anchored window's reach (this kit's own pre-existing 42.19 Hz = 2×21.09 Hz grind-#1 harmonic). **Two independent un-anchored checks confirm it's the fixed line, not a relay**: cross-build tracking regression (peak vs 2×grind-1 r=0.759 p=0.007; vs 5×f0 r=0.144 p=0.673, replicated by a sibling's per-window Theil-Sen test with a working positive control) and odd-harmonic completeness (V74's `3×f0` = rank 5/11, unremarkable — the series is incomplete). ⚠ `3×f0` isn't automatically clean either (confounds V62/V71B against THEIR OWN grind-1 fundamental) — check the gap to the nearest known fixed line before trusting any single anchored reading. Reference instrument: `analysis-2020accord/r5d_tracking_test.py`.
- ★★★★★ [V74/V75 turned the damper into a SAMPLED-DATA RELAY](reference-accord-v74-v75-damper-is-a-sampled-relay.md) — `FactorE Y[1]:=Y[2]` makes magnitude **constant** across `X[1]→X[2]` while the sign follows `gp-0x6abe` ⇒ **bang-bang relay**; stock `[0,140,539,927]` has **no plateau**. V74 band 85–531 °/s, **V75 42–531** (amp 225→297). 🛑 This is **exactly V72's error**, which `STATE.md` claimed the design avoided — `Y[0]=0` governs only *below* `X[1]`. ★ The relay is a **100 Hz sampling artifact**: at 21 Hz the zero crossing takes 2.6 ms = ¼ sample ⇒ the held output steps `+297→−297` in one tick **whatever the table shape**; at 7.79 Hz it takes 27–55 ms and IS resolved ⇒ **the two symptoms want different fixes.** ★ The damper is **inside a loop at 2× weight** — `0xC63A0` 1024→**2048 since V72** (MODE-PROOF, always live); 🛑 `BUILD-LINEAGE.md:259` still says this chain is "all unity and stock, no hidden loop gain" — **FALSE**, and RULE 7 wrongly files Lever C as mode-inert. Replay: V75 crosses the boundary **8.1× more often** (282 vs 35 entries, engaged 0–35 km/h), median dwell **one tick** vs 210 ms; direction p=0.039, magnitude not establishable at n=9.
- 🛑🛑 [V75's hard fault — six mechanisms refuted, one survives](reference-accord-v75-fault-refutation-ledger.md) — latched total loss of power steering at a stoplight launch; **V74 flew 1,011 s clean**. Delta is ONLY `FactorC Y[0]` 429→566, `FactorE X[1]` 400→200, cave 45→68 B. **Of 17 monitor constants, no build ever moved a fault threshold** ⇒ the fault is in the SIGNAL. Dead: surface arithmetic · `FUN_000347b8` (215-count margin, FactorE has no headroom past `X[3]`) · int/float lockstep (**no float mirror of FactorC/E exists**) · governor slew-step (`FUN_0004595a` tolerates lag) · `FUN_00045a20` (`gp-0x6bd0` **cancels** in its subtraction) · duty/dwell (**V74 sits 210 consecutive 1 kHz cycles = 21× the trip requirement, 35 times, and never faulted**). ⇒ **only a TRANSITION-triggered trip survives** — the one regime differing 8.1×, not 1.32×. ⚠ **RESTATED 2026-08-06:** ~~"route 5d has ZERO engaged stoplight stops ⇒ it structurally could not contain the faulting regime"~~ is true only for *engaged while STOPPED* (0.0 s); route 5d contains **5–6 engaged stoplight LAUNCHES** (two independent counts), so **V74 flew the faulting regime and did NOT fault** — which **strengthens** the V74/V75 contrast rather than voiding it. 🛑🛑 But the ledger's anchor has since collapsed: **V74 hard-faulted in MANUAL** ([[accord-v74-hard-faulted-in-manual-over-a-bump]]) ⇒ every *"V74 did X and never faulted"* refutation is weakened and `k*` is VOID. 🛑 The closing *"decisive measurement is a DTC read"* is **WITHDRAWN** ([[accord-dtc-read-is-structurally-blind-here]]). ⚠ n=1; GATE 2 was never run.
- 🛑🛑 [Monitor 2 mechanics + the `tp+0x74a4 = 0xC64A4` trap (**made THREE TIMES** — third occurrence 2026-08-06)](reference-accord-monitor2-corridor-and-the-c64a4-trap.md) — **RULE: never byte-check a tp-relative address you were handed; recompute `tp+disp` and show the addition.** ⊕ **Re-verified 2026-08-06: `0xC64A4 = 0x00` on STOCK, V74 AND V75 ⇒ Monitor 2 is ARMED in every build**; the wrong `0xC74A4` reads `0xEA`. 🛑 And its **magnitude sizing is REFUTED** — the faulting V75 launch was the MILDEST of four, so the "maximum-demand, minimum-speed corner" story is dead; only the edge-vs-dwell *structure* survives. Reading `0xC74A4` (=0xEA) instead of `0xC64A4` (=0x00) declared Monitor 2 "permanently gated off"; it is **ARMED in every build**, and the orchestrator relayed the error by verifying the wrong address. Mechanics: ±5/1024 on `gp-0x6b98`, charge **+0.001**/leak **−0.0005** ⇒ **break-even duty exactly 1/3**, trip at 0.01 ⇒ **10 consecutive 1 kHz cycles = 10 ms → DTC 0x1c/0x1d → `0xF00049`, latched**. ★ **Sign alternation does NOT cancel** (bilateral `|diff|` flags); **no minimum-duration gate**. ★ The corridor compares `gp-0x6b04` (PRE-clamp) vs `gp-0x6b98` (POST) ⇒ **equal unless a clamp BINDS** — the physical reason a transition trips it and a dwell cannot. ✅ `gp-0x6dac` CLOSED (driver-torque sensor voting, 1 writer/1 reader, two methods) — not the path.
- ★★★★ [LKAS "friction" is VISCOUS DAMPING, not a rate limit — and V74 made it engaged-only](reference-accord-lkas-friction-is-viscous-not-a-rate-limit.md) — the operator was right; "it's an openpilot safety feature" was wrong. 🛑 **No live hard rate limiter exists anywhere** (`0xC61D6`=0 disabled, `0xC6194` dead, 45-site `gp-0x6b98` sweep finds only magnitude clamps). Three viscous terms oppose column rate; the always-there baseline is **`gp-0x6b26` (`FUN_00036c12`), VIRGIN — never edited, strongest at 0 km/h**. 🛑 **No term is indexed by commanded torque — `FactorB` is flat unity in modes 24 AND 26**; opposition is mediated through MOTION, never demand. ⊕ On stock, modes 24/26 carry **byte-identical** friction and damping ⇒ **the LKAS-vs-manual asymmetry now on the car is OURS**: mode-26 friction ×1.5 since V74 (manual byte-stock), plus `0xC407E` 511→850 since V73 which is **not** mode-indexed and hits manual too. Levers ranked; friction ×1.5 was already in force on V74 which did NOT fix the grind ⇒ reverting it is the clean single-variable feel test.

## 2026-08-06 session — V74 hard-faulted in MANUAL; V75's fault pinned to one frame; V77 built

- ★★★★★ [V74 HARD-FAULTED in MANUAL, over a bump](accord-v74-hard-faulted-in-manual-over-a-bump.md) — latched total loss of power steering, **LKAS DISENGAGED**; lamp on continuously, still on after engine restart, out after ~30 s of driving (classic DTC maturation: fault set with `warningIndicatorRequested` → assist latched off → power cycle restores assist but not the lamp → monitor re-runs clean → indicator dropped). 🛑 **[EVIDENCE, orchestrator-verified TWICE] the FactorC/FactorE edits were NOT in force** — disengaged = **mode 24**, and all five mode-24 damper records are **byte-identical to stock on V74 AND V75** (FactorC `0xD67E4` `X=[2240,3840,5120,8960] Y=[0,234,429,908]`, FactorE `0xD6820` `X=[60,400,2500,4000] Y=[0,140,539,927]`, FactorB `0xD6760`, FactorD `0xD67A4`, ceiling `0xD60B4`); independently **0 of the 54 non-CRC V73→V74 diff runs** land inside any mode-24 record. 🛑🛑 ⇒ **`k* ∈ (0.580, 1.580]` is VOID and "V74 flew 1,011 s clean" is no longer a safety anchor** — every gain-margin argument in the V75 analysis rested on it. **No build in the current lineage has demonstrated safety.** ★ Live in manual and non-stock: `0xC63A0`=2048 (V72), `0xC407E`=850 (V73), `0xC61B2/B4`=2048 (V38), boost floor 5120/5.0 (V38), `0xC62EA`=0 — **none is new at V74** (V72/V73 carried the same manual config with no manual fault) ⇒ **n=1**, and the trigger may simply be the first sufficient bump.
- ★★★★★ [V75's fault is PINNED TO ONE FRAME](accord-v75-fault-pinned-to-the-frame.md) — route `5e` (`75604b0a432fdc89_0000005e--857d0bd164`), 7 segments, 401.2 s. Fault at **t = 284.7947 s, segment 4**, one 100 Hz frame, everything latching together: bus `STEER_STATUS` (`gp-0x6807`) 0→**7**; `STEER_CONTROL_ACTIVE` 1→0; `gp-0x6880` 0→1 (0 for the whole prior drive); **`0x1AB` byte0 bit2, the firmware's own DTC-active flag, 0→1**; `0x14A` STEER_ANGLE/ANGLE_RATE/WHEEL_ANGLE **all → `0x7FFF`**; `STEER_SENSOR_STATUS` 7→4. openpilot reacted +5 ms; dash-lamp frames on `0x450`/`0x440` at +0.12/+0.28/+0.52 s. ★★ **The faulting launch was the MILDEST of four engaged stoplight launches** — #2 sat on openpilot's ±4096 rail **76%** of its window and drove the damper to a higher bracket without faulting; #4 had **0.00% rail contact** and the lowest driver torque ⇒ 🛑 **magnitude and rail contact are NOT the discriminator; every magnitude-based mechanism is dead.** ★★ The damper never approached its ceiling — the `≥448` probe bit fired **0 / 39,961 frames**. ★★ 300 ms pre-fault: a **20.0 Hz oscillation** — 1,368 ct p-p driver torque, 93 ct p-p angle rate, dominant line 20.0 Hz on both, **absent from openpilot's command (3.3 Hz)** — the kit's own ~21 Hz plant mode; the damper thermometer stepped up one bracket 20 ms before, then froze for 116 s. ★ Post-fault the control task is **ALIVE** (probe cave still executing, 2,165 `gp-0x6ac2` transitions) and CAN is alive (0x14A/0x18F both exactly 100.0 Hz) with MOTOR_TORQUE frozen ⇒ **motor-off latch, not a task death or a reset**; driver effort median rose **12×**.
- ★★★★ [Descriptor **bit13** is the fault fingerprint](accord-descriptor-bit13-is-the-fault-fingerprint.md) — [EVIDENCE, orchestrator-verified in Ghidra] `FUN_00040a50` forces the `0x14A` angle fields to `0x7FFF` when `FUN_00040906(1)==0xff && FUN_00046ea6(0xd)!=0`. `FUN_00046ea6(N)` = **bit N of the OR-aggregate `(gp-0x18d0 | gp-0x18d4)`**, which ORs the first word of the fault-descriptor record at **`tp-0x72bc = 0xBF000-0x72BC = 0xB7D44`, stride `0x1c`, fault_id 0–125**; `0xd` = **bit13**. ⇒ **the angle sentinel and `STEER_SENSOR_STATUS` 7→4 are CONSEQUENCES of a bit13 fault, not a cause — no re-pointing to the angle domain is warranted.** And bit13 is a **fingerprint**: it **RULES OUT** fid 4 (`0x00001C01`, init self-test), fid 80 = `0xC41668` (`0x00000C00`, ADC timeout), fid 72 = `0xD48394` (`0x00000C20`); it **RULES IN** the category-`0x2D`/`0x3D` ids, including **fid 28 (Monitor 1)** and **fid 29 (Monitor 2 / `FUN_00045a20`)**, both `0x00003D01`. ⚠ **fault_id numbering trap** — the direct map at `tp-0x5AB8 = 0xB9548` (stride 4, DTC = raw bytes[2],[1],[0]) gives `0xC41668`=**80**, `0xD48394`=**72**, `0x540011`=**16**, one lower than several agent reports claimed.
- 🛑🛑 [The DTC read is STRUCTURALLY BLIND here](accord-dtc-read-is-structurally-blind-here.md) — **`0xF00049` is a catch-all shared by ~42 distinct fault_ids**; its status byte cannot identify which monitor fired even in principle. Worse, a multi-member group's UDS status is **not an OR across members**: the display picks a winner from a **live RAM** fault-log array (pointer `tp-0x7fcc`) by priority, falling back to the group's first ROM member (**fid 4**, a power-on self-test) — and **the RAM log is cleared by the power cycle** ⇒ **a fresh `0x1c`/`0x1d` trip is invisible in a `19 02` read taken after a restart.** ★ Provenance [EVIDENCE, from raw ISO-TP]: `flashing-2020accord/dtc_script_output_stock_eps_preV21new.txt` shows `5902ce 540011 40 c41668 08 d48394 40 f00049 48 f00055 40` on **stock firmware before this kit's first flash**; today's read differs in exactly two bits (`0xC41668` gained pendingDTC, `0xD48394` gained confirmedDTC) and **`0xF00049` is byte-identical to stock**. `dtc_script_output_2.txt` shows `f00049 = 0x0E` when it genuinely fires, so the store IS responsive. 🛑 **`0x23` ReadMemoryByAddress is not implemented on this ECU** (NRC `0x11` in all three captures, three firmware eras). ⇒ **retires "the decisive outstanding measurement is a DTC read."**
- ★★★★ [Path 2 is a CLOSED firmware loop, and `0xC63A0` weights it](accord-path2-is-a-closed-firmware-loop-and-c63a0-weights-it.md) — `gp-0x6bd0` reaches the motor by TWO routes. **Path 1** = `FUN_0003aa2c`, the aggregator, **unity weight, zero phase** — this is what delivers the damping. **Path 2** = `FUN_00038148` stage 1: **six gated terms, plain ADD, no subtraction**, each `(x * gate * w) >> 10`, then × polarity `gp-0x6752` × `tp+0x7468 = 0xC6468 = 2639` >>10, then a 1 kHz IIR `state += ((16*target − state) * (tp+0x73ac = 0xC63AC = 102)) >> 10` (**corner 16.70 Hz**) → stage 2 → `gp-0x6b70` → `FUN_00037fe6` → `gp-0x6ad6` → `FUN_0003a382` (PID) → back into the aggregator → `gp-0x6b98`. ★★ **`gp-0x6b98` RE-ENTERS Path 2 one sample later via `FUN_0003b8f6`** ⇒ **Path 2 is a closed feedback loop inside the FIRMWARE, not through the plant**; `FUN_0003b8f6` runs at `0x2240e`, before the governor at `0x229ce`, in the same 1 kHz tick — a clean one-sample delay, not an algebraic loop. 🛑 **`0xC63A0` does not touch the `gp-0x6b98` re-entry term at all, and that term may dominate. OPEN, highest-value next trace.** ★★ **`0xC63A0` = `tp+0x73a0`, u16 Q10, is the ODD ONE OUT of six sibling weights** (`0xC63A0`..`0xC63AA`, all stock **1024**): it is **the only one any build has ever moved** — V72 set it to **2048** and no build reverted it until V77. **Mode-proof bare `tp` scalar ⇒ live in MANUAL and ENGAGED.** 1 reader (`0x381AC`), 0 writers, **no monitor, no float mirror** (two-method null). Reverting = **−6.02 dB, zero phase**, zero cost to Path 1. ⚠ Its gate is a **ZEROING** gate not a clamp: `|gp-0x6bd0| > 2048` ⇒ the whole term × 0 — always open at the observed ~448, but a cliff.
- ⚠🛑 [The damper FIXES the grind and is FLAT on the ratchet — **GRIND HALF RETRACTED 2026-08-07**, read `accord-grind1-is-inert-to-the-damper-dose.md` first](accord-damper-fixes-the-grind-but-is-flat-on-the-ratchet.md) — dose-response over four builds differing only in the damper cells (V72 k=0, V73 k=0, V74 k=0.5799, V75 k=1.5798), episode-bootstrapped: **18–22 Hz (grind #1) `d ln(y)/dk` = −0.599 [−0.856, −0.348] = −5.20 dB per unit k — CI EXCLUDES ZERO**; **6–9 Hz (micro-ratchet) −0.089 [−0.350, +0.163] — CI INCLUDES ZERO, FLAT.** V75/V74 grind **0.349 [0.192, 0.784]** speed-matched, **0.378 [0.201, 0.806]** speed×rate-matched; **limit-cycle duty 0.034, LOWEST of 13 builds**, ratio **0.067 [0.000, 0.283]**; negative controls flat (24–28 Hz 1.071, 40–49 Hz 0.830, 1–4 Hz 0.640). Micro-ratchet: five of six statistics point down, **none clears its own null**; absolute 6–9 Hz envelope V73 210.1 → V74 209.4 → V75 205.0. 🛑 **k required to fix the ratchet = 4.2–13.5, against 1.5798 which hard-faulted** ⇒ **the damper alone cannot eliminate the micro-ratchet; it needs a different lever.** ★ **V75 is the first build where the two bands DECOUPLED** — paired ratio `(6-9)/(18-22)` = 1.18 (V72) → 1.38 (V73) → 1.40 (V74) → **2.75 [2.09, 3.72]** (V75). ⚠ **V75's dose increase is NOT established on-car at episode level** (engaged-creep bit7 duty ratio 1.347, CI [0.052, 1.833] vs a split-half null [0.676, 1.413]); the pooled "67.44% → 82.85%" is a **window** statistic. ⚠ Also corrects **"V75r keeps ~99% of V75's grind benefit"** → **48%** (66 vs 137 counts at the in-burst rate of 99), predicted to give back **1.63× [1.33, 2.01]**, landing inside the perceptual bracket.
- ★ [V77 BUILT — the `0xC63A0` loop-gain revert](accord-v77-built-c63a0-revert.md) — **V77 = V74 base + `0xC63A0` 2048→1024, single variable.** rwd `39990-TVA,A160-V77-V74BASE-C63A0.1024-loopgain-revert-0x13000-0x100000.rwd` sha `fd8db4e2ed140035782a55b2e6808bcf87a0ea85692cbe547960a13de1cfc8c5`; image `_v77_C63A0.1024_v74base_plain_image.bin` sha `a0f7c09c038931cabc419ccf79d4bb9819e647e88c0fb817ebc23cd44d102782`. **V74→V77 diff = 2 runs / 5 bytes: `0xC63A1` `08`→`04` + `0xC6FFC` CRC. Nothing else.** ⚠ **It is ONE CELL, not two bytes** — `2048 = 00 08` LE, `1024 = 00 04` LE, low byte `0x00` in both ⇒ ***count cells, not bytes.*** 50/50 CRC pass, `.rwd` round-trips byte-for-byte, cave and all mode-24/26 records byte-identical to V74, mode-24 also identical to stock. **V77B** = the same single revert on the **V75** base — `…-V77B-V75BASE-C63A0.1024-NOT-RECOMMENDED-UNFLASHED-…rwd` sha `f2c2dc0ba4f5e01bbd95925b8e42c1323a1b6b99bf658b795aa25cb2fa539dd7`, image sha `acbc218751af827d5ddc696e24d6ae44f11ef06dc04e11a3b383d366b4d4fc10` — 🛑 **BUILT, UNFLASHED, NOT RECOMMENDED**, it carries V75's engaged config, which hard-faulted. 🛑 **Neither is clearance to fly. V77 is a hypothesis test, not a known-good.**
- ★★★ [The FactorC dip is OURS, and `FactorE X[0]` 12→0 is NOT a no-op](accord-factorc-dip-is-ours-and-factore-x0-is-not-a-noop.md) — FactorC's X axis is **vehicle speed at 64 counts/km-h**: `[2240,3840,5120,8960]` = `[35,60,80,140] km/h`. **Stock mode-26 FactorC `Y = [0,234,429,908]` — strictly monotone, NO dip.** V74 set `Y[0]:=Y[2]=429` (dip **−195**), V75 raised it to 566 (dip **−332**) ⇒ **the dip is ours**; it is a descending ramp 35→60 km/h and all builds are identical above 60. 🛑 **Monotone at `C_Y0=566` is ARITHMETICALLY IMPOSSIBLE** under the no-clip guard — it needs `Y[2]≥Y[1]≥566`, but at rate ≥4000 (`E=927`) any `C>566` gives `(C·927)>>10 > 512`, the ceiling floor; first offending speed **80.5 km/h**. **Reachable alternative: half-fill `[566,429,429,908]`** — dip −332→**−137**, damping at 60 km/h / 21 °/s **56→104 counts**, `k` unchanged, passes every guard. ★ **`FactorE X[0]` 12→0 is NOT a no-op**: below `X[0]` the evaluator clamps to `Y[0]`(=0), but moving the breakpoint left raises E across all of `[0,200]` — **196 of 4001 integer rate points change**; at the measured in-burst rate (99 ct = 21 °/s) dose goes **137→147 (+7%)** while `k` goes **1.5798→1.4850 (−0.53 dB)** — right direction on BOTH axes, but far too small to be a fault fix. It violates guard `E_X0_MIN_SAFE=12`, whose own stated rationale (a steep ramp near zero) **argues the wrong way** — `X[0]→0` REDUCES the slope 2.867→2.695/count. ⚠ `M = (C_Y0·E_Y1)>>10` is **capped at 297** (`C_Y0 ≤ 566`, `E_Y1` frozen) ⇒ `k = M/(X1−X0)`, and the best reachable at full plateau is **k = 0.7425** — still **+2.15 dB over V74**.
- 🛑 [Verify the crux yourself — it caught FOUR errors in one session](feedback-verify-the-crux-yourself-it-caught-four-errors.md) — 2026-08-06: the orchestrator's own byte/Ghidra spot-checks caught **four** decision-bearing subagent or record errors in one session: the `tp+0x74a4` off-by-`0x1000` (**third occurrence**); a reversed DTC-map byte order plus an out-of-range descriptor scan; a fault_id off-by-one; and a `0x1AB` flag described as a narrow 3-group test when **bit10 alone covers 75 fault_ids (40 of the 43 EPS-disabling ones)**, making it a broad fault-class indicator. ⇒ **the standing "verify the crux yourself" rule paid for itself four times in one session. Keep doing it, and verify in the SAFE direction too.**
- ★★★★★ [V74's fault: the damper WAS in force — the mode LAGS disengagement](accord-v74-fault-damper-WAS-in-force-mode-lag.md) — route `61`, fault pinned to **t=732.3872 s**, `gp-0x67fa` **5→8** + DTC flag + all three `0x14A` angle fields → `0x7FFF` + SENSOR_STATUS 7→4, **one 100 Hz transmission**, one transition in 760 s, never exits; `0x14A` holds 99.97 Hz for 28.3 s ⇒ **motor-off latch, not a reset.** 🛑🛑 **REFUTES "the FactorC/E edits were not in force" and un-voids `k*`**: `bit7`=1 at the fault (560 ms continuous) at **33.29 km/h**, *below* stock mode-24 FactorC `X[0]`=2240 ct=35 km/h where the evaluator **hard-clamps to `Y[0]`=0** (`0x3451e`→`0x34522`) and the chain is **purely multiplicative** (four `mulu`+`shr 0xa`, `0x34684`–`0x3469c`, **zero `add`/`or`**) ⇒ mode 24 gives an arithmetic **0**. Disengage was only **2.509 s** earlier ⇒ still on the **engaged column**. ★ Negative control replicated on TWO routes: manual `bit7` fires only inside a ~4 s tail, **0 of 9,286** (r61) and **0 of 39,794** (r5d) beyond it = **49,080 true-manual frames of zero**. ⊕ The "disagreeing" 0.00% episode had **never been engaged** (tsd=∞). ⚠ Don't use the 5 km/h manual bucket table (n=3 episodes). ⚠ **OPEN:** the ROM mechanism for the multi-second hold — mode cell `gp+0x63fd`, writer `FUN_00042746`; only debounce found is `0xC624E`=**40 ms**, not 2.5 s; candidate is the `gp-0x6733==−1` sentinel (`FUN_000527da`, callers null). 🛑 **DURABLE RULE: "the operator was in manual, therefore the engaged-column edits were not in force" is UNSOUND** — bus disengagement ≠ mode 24 active.
- ★★★★★ [Both hard faults fired at the route-MAX angle-rate slew](accord-both-faults-fired-at-max-angle-rate-slew.md) — **V74** \|tq\| 3,676 (99.999 pct) · **\|d(rate)/dt\| 5,400/s = route MAX, n=1**; **V75** \|tq\| 922 (**86.3 pct**) · **\|d(rate)/dt\| 6,900/s = route MAX, n=1**. **Magnitude does NOT unify them; slew does** ⇒ dissolves V75's "mildest of four launches" paradox. V74's bump was **ordinary** — IMU (vertical axis = **`ax`**) −1.494 m/s², **#84 of 388, 78.6th pct**, route max 2.94× bigger; V74 survived **8** earlier damper-live episodes >3,000 ct. Rate 20–78 ct = FactorE's **ramp**, not the flat band ⇒ **the bang-bang relay is ELIMINATED here.** ★★ **ROM converges independently: FOUR trip surfaces for fid 28/29** (A: `FUN_00034350`/`FUN_000347b8` on **`gp-0x6bd0` itself** ±5/1024; B: `FUN_000456a4`/`FUN_00045a20` on `gp-0x6acc` vs `gp-0x6ace`), **none computes a derivative — all per-cycle STATIC int-vs-float consistency checks**, and the DTC dwell is a **no-op** (threshold field reads `0x0000` for both fids, `0xB8054`/`0xB8070`, both `0x3D01`) ⇒ **trips on the FIRST qualifying call**; only real debounce is ~0.1 s inside each monitor (`gp-0x3564`/`gp-0x3550`). ⇒ **The lever this implies has NEVER been proposed: limit `gp-0x6bd0`'s SLEW, not its magnitude.** 🛑 SENTINEL TRAP: a derivative window touching the fault frame imports the `0x7FFF` spike and inflates `|d(rate)/dt|` **~300×**.
- 🛑🛑🛑 [V77 is a NULL EXPERIMENT — all THREE monitor surfaces are blind to `0xC63A0`](accord-v77-cannot-reach-the-monitors.md) — [EVIDENCE, orchestrator-verified] `FUN_000347b8` reads **`gp-0x6bd0` directly** (`* 0.0009765625`, tested vs `0x3ba00000` = 5/1024, reports `0x417a`); in `FUN_00038148` `gp-0x6bd0` appears **exactly once** as a read-only summand weighted by `tp+0x73a0`=`0xC63A0` behind a `|x|≤2048` zeroing gate, and **the function's only store is `gp-0x6b70` — no write to `gp-0x6bd0` exists.** ⇒ **reverting `0xC63A0` changes not one bit either monitor sees**; if V77 flies and faults, that carries **NO information.** ★ **But `0xC63A0` is NOT inert** — a subagent's "gate-only / 0.00 dB / not a PID" claim was **FALSE**: `gp-0x6ad6` appears **three** times in `FUN_0003a382`, incl. `uVar19 = *(short*)(gp-0x6ad6)`, a **data** read feeding `iVar30 = gp-0x4f60 − clamp(uVar19)` → the error → **P/I/D lanes** (states `gp-0x367c`/`gp-0x3688`/`gp-0x3684`) → `gp-0x6ad4`. **It IS a gain-scheduled PID; both things are true — `0xC63A0` moves delivered torque AND cannot move what the monitors see.** ⚠ Its effect was always **confounded with damper liveness** (V72/V73 had 2048 but a structurally zero damper). ⭐ **Surface C — the ORIGINAL Monitor 1/2 pair (`gp-0x3564`/`gp-0x3550`) — does NOT compare `gp-0x6bd0` at all**; it compares **`gp-0x6b98`, the merged command**, vs a float envelope `gp-0x6dbc`/`fVar23` at ±5/1024 (flag **32.0** = "torque arm") → fid 29 via `FUN_000462e6(0x3f1b,…)`, and one cycle later fid 28 via `FUN_0004613e`@`0x43D42`. **IF `0xC63A0` reaches `gp-0x6b98`, V77 has a real mechanism.** ✅ **RESOLVED — IT DOES NOT.** That premise cited "Path 2 closes through `gp-0x6b98`" — **wrong direction** (`FUN_0003b8f6` READS it back INTO Path 2 ⇒ input, not output). Forward trace: **`gp-0x6afe` has exactly ONE writer**, `FUN_00042ac6`@`0x42ad6`, a 6-line clamp/store [orchestrator-verified]; sole caller `FUN_00026c80`@`0x277f6` passing `sVar38`, accumulated entirely from local stack buffers — **989 instructions scanned, ZERO hits** for `6ad4`/`6b94`/`6ad6`/`6b70`; same for `uVar34` over `FUN_00042af8`’s 1,769. ⊕ `sVar38` also feeds **`gp-0x6b4e`**, a *sibling* input of `FUN_00038148` ⇒ **parallel, not series.** ⇒ **ALL THREE SURFACES BLIND; V77 cannot prevent a Monitor-1/2 trip.** ⚠ The tracer needed THREE attempts (NO→YES→NO) — **a subagent reversing itself deserves MORE scrutiny, not less.**
- 🛑 [A displacement grep misses a reused Ghidra variable binding](feedback-displacement-grep-misses-reused-ghidra-variable.md) — grepping a decompile for `gp-0xNNNN` finds only the **load**; every downstream use appears as `uVarN`. Cost this kit a wrong decision-bearing answer: a tracer grepped `-0x6ad6`, found only the entry-gate hits, and concluded "gate-only ⇒ 0.00 dB ⇒ not a PID" — **reversing its own correct earlier report.** The third occurrence bound to `uVar19`, a slot **already used** earlier for `LERP(gp-0x6a5e)`. ⇒ **To establish a cell is *only* a gate — a null result — READ THE BODY AND FOLLOW THE BINDING, never grep the displacement.** ⚠ The orchestrator had **already propagated the error into the golden model** before checking: **check before you write it down.** ⊕ A subagent **reversing its own earlier finding** deserves MORE scrutiny, not less.
- ★★★★★ [THE HARD FAULT: the friction lane crosses a flat 512-count ceiling that V73 unlocked](accord-friction-lane-ceiling-is-the-hard-fault.md) — **✅ CONFIRMED AND CLOSED 2026-08-07** (sole writer `0x36CF0`, `0xC407E` 0w/3r all in one function, 511 ⇒ untrippable BY CONSTRUCTION; the ×1.5 friction came from **V73 not V74**; `0xC63A0` **exonerated**) — **`FUN_00036d74`, called UNCONDITIONALLY from the 1 kHz task `FUN_0002214a`@`0x2290a`, tests `|gp-0x6b26|/1024 > cal(0xC4004)`** where `0xC4004` = float **0.5 = 512 raw counts** (bytes `0000003f`, byte-identical in every image) → `FUN_000462e6(0x39bc,…)` → `FUN_00016de6(0x1d)` → **hard fault**. 🛑 **Flat, symmetric, unconditional — NO race, no timing escape**, unlike the damper's Surface A. ★★ `gp-0x6b26` is clamped to ±`0xC407E`: **511 in stock/V38/V72 — exactly ONE COUNT under the ceiling, an INTERLOCK** ⇒ a clamped signal cannot trip its own check. **V73 raised it to 850 = 338 counts OVER**, removing the interlock unknowingly; **V74's ×1.5 mode-26 friction table (`0xD7A54`)** then dropped the `gp-0x6c2c` needed to cross from ~6258 to ~4180. **V74 and V75 BOTH hard-faulted.** ★ Fits everything the damper never could: **mode-proof** (explains V74's disengaged fault — the only candidate that can), single-frame latch (threshold-0 dwell), and the exact build history (faults are new at V74). ⚠ Index is `gp-0x6a5e` (**vehicle speed**); multiplier is `gp-0x6c2c` (**filtered motor ACCELERATION** — corrected 2026-08-08, not a rate ⇒ the lane is DC-blind); **bar torque is absent from the arithmetic** and there is **no bare `sign()` flip** — a monotonic rise in `|gp-0x6c2c|` trips it as well as a reversal. ✅ **FIX = `0xC407E` → 511, one cell, mode-proof, loosens no monitor — and a V38 base gets it FREE.** 🛑 **`0xC407E` is now a DO-NOT-RAISE cell.** ⚠ OPEN: `gp-0x6c2c`'s physical scale undetermined ⇒ mechanism is EVIDENCE, "it caused both faults" is a strong BELIEF.
- ⚠★★★★ [V76 FLEW CLEAN: grind #1 is DOSE-LIMITED (**"DOSE-LIMITED" RETRACTED 2026-08-07**), the micro-ratchet is DOSE-INDEPENDENT (**stands, and it DOES move at k=4.16**)](accord-grind1-dose-limited-ratchet-dose-independent.md) — route `65`, 636.30 s / 63,477 frames, 0–96.7 km/h, engaged 70.87%, **zero DTC transitions**; identity settled **four independent ways**. ★★★★ The split, fit over V72/V73 (k=0), V74 (0.5799), V75 (1.5798), V76 (1.3866), creep, speed-stratified, **episode**-bootstrapped — and V76 sits BETWEEN V74 and V75, so the monotone model made a falsifiable **point** prediction: **grind #1 slope −0.614 [−0.810, −0.416]**, observed 1.577 vs predicted 1.613 (**held to 0.19 dB**) ⇒ **DOSE-LIMITED**; **ratchet slope −0.094 [−0.291, +0.098]**, observed 3.877 vs predicted 3.906 ⇒ **DOSE-INDEPENDENT — more damper will NOT fix it; it needs a non-dose lever.** 🛑 **V76's grind-#2 prediction was FALSIFIED**: predicted 0.57× vs V75 at 42 °/s, **measured 1.394 [1.017, 1.768]** — opposite direction ⇒ discount the arithmetic surface's ability to predict *delivered* grind #2 (the `k` axis itself was validated by the same drive). ★ **The friction-margin null is REAL, not unarmed**: bit7 (`|gp-0x6b26|>448`) **0/63,477** with the positive control at 99.93% *in the same frames* ⇒ weakens but does NOT refute [[accord-friction-lane-ceiling-is-the-hard-fault]]. ★ **Mode lag measured directly: median 994.9 ms [830.0, 1575.0]**, n=6 — **2.5× shorter than the ~2.5 s on record.** ⚠ Every non-zero-k point is still **n=1 route**.
- ★★★★★ [`FUN_00034350` is the damper evaluator — pointer arrays, an ignored count field, and a HARD OUTPUT CLAMP](accord-damper-evaluator-fun34350-ceiling-clamp.md) — [EVIDENCE, orchestrator-decompiled] sole caller `FUN_00022ca0`. Records come from a **pointer array per factor** (`B 0xC9CCC · C 0xC9E9C · D 0xC9DB4 · E 0xC9F84 · ceiling 0xC77A0 · friction 0xCBE74`, `u32 @ arr+mode*4`), **34 distinct records over 34 modes, zero sharing.** Layout `base+0` u16 n · `base+2` n×i16 X · `base+2+2n` n×i16 Y · `base+2+4n` u16 term = `4+4n`; 🛑 **X starts at base+2, NOT base+4** (reading at +4 silently yields `[X1,X2,X3,Y0]` — the orchestrator hit this). Shipped counts **2/3/4/5**. 🛑🛑 **THE COUNT FIELD IS NEVER READ**: the lookup IS a real `while (X[i]<=idx) i++` loop, but `n` is pinned by three hardcoded immediates (B/C/E `rec+10 / rec+8 / rec+0x10`; D `rec+0xc / rec+10 / rec+0x14`; ceiling `rec+6 / rec+4 / rec+8`) ⇒ **more points = a CODE edit to the always-on damper = the V24/V27/V48B bricking class**, while relocating a same-size record is **cal-only**. ★★ **THE OUTPUT IS HARD-CLAMPED**: `gp-0x6bd0 = clamp(product, ±ceiling_LERP(gp-0x6ac2))`, ceiling n=2 `X=[300,800] Y=[512,1024]`, fallback `*(u16*)0xC6158`=512 ⇒ **|gp-0x6bd0| ≤ 1024, and 512 at low ceiling index — the real limit on dose.** Product is 4× `mulu`+`>>10`, **zero add/or**; **sign from `gp-0x6abe`, NOT the index `gp-0x6ac0`**. `gp-0x6bd0` is **lockstep-shadowed at `gp-0x4cf2`**. 🛑 **`gp-0x67f4 != 1` turns FactorC's whole speed shaping to unity and has NEVER been probed.** ⊕ **FactorB (n=4) and FactorD (n=5) are flat 1024 — inert unity in BOTH modes**; FactorD's axis `gp-0x6a10` is a **free, 5-point, UNTESTED** lane on the same chain.
- 🛑🛑 [The ReLU / bigger-table plan INVERTS — the ceiling clamp is the obstacle, not the point count](accord-relu-plan-inverts-at-the-ceiling.md) — a ReLU is **2 DOF**; a 4-point table has 8 numbers and spends 3 on collinearity, and both clamps are **independent of n** ⇒ **more points buy EXACTLY ZERO for a pure ReLU** (constructive n=4 witness: dose 206, true-ReLU both factors, `E_Y[0]`=0, add-only over 406,500 points). 🛑 **The constraint that breaks is PARAMETER-FREE**: a ReLU FactorC is speed-proportional, so `dose(v,99)/dose(515,99) = v/515` *whatever you pick* — pinning 206 at 5 mph forces **3,593 counts at 140 km/h = 7.02× the 512 ceiling**, railing above **3.2 °/s at 140 km/h, 7.0 at 60, 21 at 20**. ★★ **A railed damper with its sign from a different cell than its index IS the Coulomb relay — you'd forbid it at `E_Y[0]` and re-create it at the ceiling.** Flat FactorC at the same dose rails no earlier than **563 °/s (176.7× more linear range)**. ⚠ **"Which factor isn't a ReLU" has TWO readings indicting OPPOSITE tables** — literal `max(0,k(x−x0))` indicts FactorC (566 floor); the operator's own recorded gloss in `v76_surface.py` (*"FLAT — no taper down, like a ReLU"* = a floor clamp) indicts FactorE (slopes 2.521/0.100/0.259). **Check how the kit recorded the operator using a shape word before designing to it.** 📋 **RULE: ask which FOURTH segment they need; if they can't name it, n=4 is enough.** ⊕ Relocation IS available (cal-only, one u32; **`0xD7BB8`–`0xD7FEF` = 1,080 B virgin `0xFF`, same page, same CRC block `0xD7FFC`**, confirmed unreferenced) and **V74's "pointer arrays must stay byte-stock" was a SELF-IMPOSED GUARD, not a firmware requirement.** ⊕ **New Ghidra trap: `get_xrefs_to(0xD780C)` returns "No references found"** though the pointer exists at `0xC9FEC` — **don't trust xref completeness on pointer-array slots.**
- ★★★ [V78 BUILT, UNFLASHED: the operator's 150% dose in ONE byte](accord-grind1-dose-limited-ratchet-dose-independent.md) — `39990-TVA,A160-V78-V76BASE-EY1.449-dose206-probe-6bd0-63fd-67fa-…rwd` sha `305234c37f797d0476b89ac793b414d6b0d5ba7cbbadf665d6e64778fe091afb` · image `_v78_v76base_ey1_449_dose206_plain_image.bin` sha `c8d8e5e1c606dd920ccec8d41ea6398c73dbe473f58912092770e700ffd50ab1`, base = **V76, the build that flew**. 🛑 **V78 not V77** — V77/V77B are consumed numbers with artifacts on disk. **The edit is FactorE m26 `Y[1]` 300→449 = ONE byte at `0xD7818`** (`300=0x012C`, `449=0x01C1` share their high byte — the *count CELLS not bytes* trap, hit again). **dose(515 ct, 99 ct) 137→206 = 1.5036×, k 1.3866→2.0840.** ★ **The lift DECAYS with rate** — ×1.50 at 21 °/s, ×1.46 at 42, ×1.40 at 85, ×1.20 at 255, **×1.00 above 530** — and max product stays `(566*927)>>10 = 512` ⇒ **no clipping V76 didn't already have.** Diff V76→V78 = **51 B / 7 runs**: 42 B cave (**inside the proven 68-B extent, NOT grown**), **1 B table**, 8 B the two CRC trailers. CRC **50/50 PASS**; readback re-derives the record through its pointer array and **re-EMULATES the cave**; re-run reproduces **bit-for-bit**. Probe: **bit7 `|gp-0x6bd0|>=448`** (448 < 512 ≤ every reachable ceiling ⇒ **a zero PROVES no clip and no Coulomb relay at the rail**) · **bit6 `>=192`** (the **V76→V78 dose discriminator** — same threshold needs 598 ct on V76 vs 93 ct on V78, **6.4×**, and 93 sits just under `R_OP`=99) · bit4 mode · **bit3 `gp-0x67fa==5` positive control**. 🛑🛑 **The orchestrator specified bit6 as `>=512` and the BUILDER REFUSED, correctly** — `>=512` is *implied by* `>=448`, so it would be a bit spent refining a null (the V64/V68/V69 failure mode). **A subagent overruling the orchestrator's spec on sizing grounds is the direction this kit less often sees; it was right.** 🛑 **UNFLASHED, NOT CLEARED** — k_tw 35–80 km/h **2.086 = 4.64× V74, 1.50× V76**, the **first point above V75's creep loop gain, and V75 hard-faulted**; V76 has ONE flight; **and the ratchet is not expected to move.**
- ★★★★ [V80 BUILT: dose ×2 with a FLAT FactorC, and V42's macro-ratchet fix restored](accord-v80-flew-the-damper-is-a-relay.md) — 🛑 **IT WAS FLOWN 2026-08-07 AND IT IS THE WORST GRINDING THE CAR HAS EVER MADE** — see the flight node for the result; ⊕ its `0xC63A0` operator directive quoted below is **refuted on evidence** (`accord-c63a0-exonerated-of-the-hard-faults.md`); ⊕ this pointer previously aimed at the ReLU node, which never carried V80's build record. `39990-TVA,A160-V80-V79BASE-flatC566-ratchet454FE-dose412-…rwd` sha `3ea81bd734e6845393d09099754eccb7a0b5682ce147d65d26511c29d37e230d` · image `_v80_v79base_flatC566_ratchet454FE_dose412_plain_image.bin` sha `2606d557da9c3a09de6f2b63bd74308e8d6023c3d423e64d9a3b90a3e66211e7`. **Three edits:** `0x454FE 0xBA→0xB5` (V42's state-4 governor fix — `bne 0x455C4`→`br`, cond nibble `0xA`→`0x5`; **it was byte-stock on V76/V78/V79, lost in the V38 rebase, and PRESENT on V42 and V75**) · **FactorC m26 `Y[3]` 908→566 ⇒ flat `[566,566,566,566]`** · FactorE `Y=[0,897,912,927]`. **dose(r=99) = 412 at EVERY speed 5→140 km/h**, 3.007× V75, `k` = **4.1597** (highest ever built; **FORCED** — `E_X0`=0 ⇒ `dose(r)=k·r`, so doubling dose doubles loop gain). 🛑 **`0xC63A0` = 1024 UNTOUCHED — operator directive: *"Do not double 0xC63A0, that is what was causing hard faults."*** ⇒ **`0xC63A0` is a DO-NOT-RAISE cell; `0x454FE 0xB5` is a MUST-CARRY cell.** ★★ **Why FactorC was flattened: V79 RAILED** — clips **38.9%** of the RULE-8 envelope at ceiling floor 512 (stock/V78 = 0.00%), first clip 85 km/h/25 °/s and 140 km/h/16 °/s; **a railed damper is a Coulomb relay** (sign `gp-0x6abe` vs index `gp-0x6ac0`, RULE 12(b)). Flat C makes the supremum `(566*927)>>10` = 512 = the ceiling floor exactly ⇒ **0.00% clip at ceiling 512 AND 1024**, and it also erases **V75's FactorC DIP** (234 @60 km/h — V75's damper collapsed to 56 there). ⚠ **Add-only is envelope-scoped**: worst post-clamp drop vs stock is 0 inside RULE-8 at both ceilings but **310 on the whole gated domain at ceiling 1024** — *always state the envelope*. ⚠ The probe **cannot discriminate V80 from V79 below 80 km/h**; both rungs speed-invariant (bit6 47 ct, bit7 108 ct) and **a non-zero bit7 is EXPECTED**. 🛑 NOT CLEARED TO FLY: GATE 2 unsatisfied, forward path unfound, micro-ratchet is dose-independent so only grind #1 + the MACRO ratchet are targeted.
- 🛑🛑 [The damper's FORWARD PATH TO THE MOTOR IS NOT FOUND — six methods](accord-damper-evaluator-fun34350-ceiling-clamp.md) — `gp-0x6bd0` reaches the aggregator **twice** (bare, and via `0xC63A0`→`FUN_00038148`→`gp-0x6b70`→`FUN_00037fe6`→`gp-0x6ad6`→**PID `FUN_0003a382`**→`gp-0x6ad4`), both converging at **`gp-0x6b94`** — so `0xC63A0` scales **one of two additive terms**, through a PID ⇒ **frequency-dependent, no scalar ratio**. Downstream: `gp-0x6b94`→`FUN_0004503c`→`gp-0x6ace`→**`FUN_000456a4` (an ADDER, not a monitor)**→`gp-0x6acc`→`gp-0x6b08` — then **excluded** at `FUN_00042af8`'s mux by `cVar8 = *(char*)(tp+0x74c9)` = `0xC64C9`, confirmed via `cmp r0,r18` + **`cmove r28,r11,r20`** (mnemonic-encoded condition; NOT the `ba05`/`b205` class). **The live FOC commit is `FUN_00071272`** (1 kHz → `FUN_0006bcb2` → `FUN_00070634`), ending `FUN_0006129a(computed_buffer, mode)` → duty `TS0CMPU/V/W` `0xFFFFCCB0` — and its **154 gp-cells cluster in `gp-0x37c..gp-0x530`, a different RAM region entirely, with ZERO references to any of the eight tracked cells.** ⇒ **an architectural boundary** between torque-command computation and FOC execution, joined by an unidentified handoff. 🛑🛑 **`0xC64C9` = 0 on stock, V42, V62, V74, V75, V76 — a CONSTANT CANNOT GATE A VARIABLE**, and V62's rate-lane ×2 (computed in that same aggregator) produced the kit's measured **8× grind fix**. ⇒ **the road outranks the trace; a failed search is a fact about the search.** 📋 **RULE: before reporting any structural claim about this chain, ask "does it survive V62?"** ⭐ Best remaining hypothesis: the damper injects into the **current/FOC loop downstream of `gp-0x6b98`**, which would explain six failed forward traces, `gp-0x6b98` recurring as a *quiescence gate* not a data input, V62's band-limited effect, and the motor-rate-indexed ceiling. Next lead: the lone `gp-0x6b98` reference at **`0x70BFC`**, inside the FOC cluster.
- 🛑 [Register spawned agents as TASKS or the roll-call rule is unexecutable](feedback-decompile-first-then-assembly.md) — 2026-08-07: ten agents spawned, **`TaskList` empty all session**, so `CLAUDE.md`'s mandatory roll-call could not be run; the orchestrator guessed liveness from file mtimes and declared "taking it over" on a **live** builder's script (operator caught it). ⇒ **`TaskCreate` + `owner` at spawn time**, always. 📋 Second, from two builders silently dropping a requested one-byte edit and one then reporting it frozen: **a builder must assert every requested edit is PRESENT IN THE EMITTED IMAGE before reporting hashes** — that single gate catches both failures. ⊕ Also: **scope every add-only/no-clip claim to a stated envelope** — "worst drop 0" was true inside RULE-8 and false (310) on the whole gated domain.
- ★★★★ [`FUN_0003a382` is a TORQUE-TRACKING PID, and its gains ride FactorE's own axis](accord-fun3a382-is-a-torque-tracking-pid.md) — [EVIDENCE, orchestrator-decompiled] `feedback = clamp(gp-0x6ad6, ±tp+0x7200)`; **`err = clamp(gp-0x4f60 − feedback, ±0x2800)`** with the setpoint = **driver torsion-bar torque** ⇒ Path 2 arrives as the **FEEDBACK** term, not feedforward. Three lanes: **filtered-P** `gp-0x367c` (α=`tp+0x7450`) · **integrator** `gp-0x3688` **clamped into an authority-derived window (anti-windup)** · **dirty derivative** `gp-0x3684`→`gp-0x3680` (α=`tp+0x744a`). Out `= ((D+I+P)>>5) × LERP(gp-0x671a) × polarity(gp-0x6752)`, clamped to ±authority (soft-start `gp-0x3678`, up `tp+0x744e`/down `tp+0x744c`, × `LERP(gp-0x6966)`) → **`gp-0x6ad4`**. Hard gate zeroes it unless `|gp-0x6ad6|≤0x6400 && |gp-0x4f60|≤0x6400 && gp-0x6ac0<0x32c9`. 🛑🛑 **ALL THREE LANE GAINS ARE LERPs ON `gp-0x6ac0` — THE SAME RECTIFIED MOTOR RATE THAT INDEXES FactorE** (`tp+0x7b1e`/`tp+0x7b0a`/`tp+0x7ade`) ⇒ a FactorE slope edit and the PID's gain schedule **move on one axis, not independently**; that is the mechanism behind the recorded **+41.8°..+55.0° lead at 21 Hz, |D|≈|P|** and it meets the **2f parametric pump** because the index is rectified. **Size any FactorE edit against this, not just dose.** ✅ **RESOLVED 2026-08-07 — THE NET SIGN IS DISSIPATIVE AND PATH 2 IS NON-INVERTING** (was [OPEN] for four sessions): **`gp-0x6abe` is the SIGNED TWIN of `gp-0x6ac0`** — both filtered from `gp-0x4f50` in `FUN_00041464`, split at `0x41b56` into signed (`gp-0x6abe`) and rectified (`gp-0x6ac0`) ⇒ `sign(gp-0x6bd0) = −sign(motor rate)` exactly (`0x3469E`–`0x346A2`). The Stage-2 subtraction in `FUN_00038148` and the PID's own `err = setpoint − feedback` **cancel**, and the two `polarity(gp-0x6752)` multiplies cancel **whatever the value**: `(−P)(+1)(−1)(+P) = P² = +1`; `FUN_00037fe6` is a genuine unity adder (all 7 weights `tp+0x74ad..0x74b3` = 1) ⇒ **Path 1 and Path 2 REINFORCE at `FUN_0003aa2c`; dissipative by construction at `gp-0x6b94`.** [EVIDENCE] ⚠ A sign result at `gp-0x6b94`, **not** at the motor — that hop is still missing (`gp-0x6ace` = the governor-clamped form, readers only the hard-shutdown monitors; `gp-0x6afe`/`gp-0x6b08` **RULED OUT** as bridges) ⇒ **a missing link, not a discovered inversion.** 🛑 And a correct sign does not make a surface safe — V80 is dissipative *and* a Coulomb relay. ★★ **`0xC63A0`'s blast radius is a STRICT SINGLE-FILE CHAIN** — `0xC63A0`(1 reader `0x381AC`) → `gp-0x6b70`(1 reader) → `gp-0x6ad6`(**2 refs, BOTH the PID**) → `gp-0x6ad4`(1 reader, the aggregator); `gp-0x374c` internal-only; **not even UDS reads it.** `FUN_00037fe6` is a **unity-gain adder on stock** (byte weights `0xC64AD..0xC64B3` all **1**, `tp+0x7aba` LERP flat 1024). 🛑 It scales **ONE SUMMAND**, not a signal (6- and 7-input sums). ⚠ **3 raw hits were FALSE POSITIVES** — `0x767a8`/`0x767b2` land **inside 4-byte `mulf.s`** and `0xBCC52`/`0xBDF92` are data: **validate every byte-scan hit on an instruction boundary.** ⊕ **Why STOCK damps nothing at creep**: FactorC `Y[0]`=0 below 2240 ct = **35.00 km/h**, and the chain is 4× `mulu`+`>>10` with **zero add/or** ⇒ zero at EVERY rate, no rescue; the knee is a clamp not a fade (35.0→0, 35.1→1), and stock `FactorE(99)` = **16 of 927**.

## 2026-08-07 session — V80 flew (worst grinding ever), the damper is a RELAY, and V81 was built

- ★★★★★ [V80 FLEW — and the damper surface is a COULOMB RELAY](accord-v80-flew-the-damper-is-a-relay.md) — route `66`, **901.71 s / 89,997 frames**, engaged **33.62%**, 0–112.8 km/h. **Operator: the WORST grinding the car has ever produced** — ~90% of engaged time, low *and* high speed, noticeable vehicle instability. 🛑 **IT DID NOT FAULT** (`0x1AB` DTC-active **0 transitions, 0.000% duty**, 0 sentinels) ⇒ **a STABILITY failure, not a fault-class failure.** ★★★★ **ROOT CAUSE, from the shipped images:** dose is a **constant 495 counts — 3.4% variation over a 34× rate range — at 97% of the 512 ceiling, above only ~25 °/s, at EVERY speed** (V75 plateaus at 297 and only above 54 °/s); with `sign(gp-0x6bd0) = −sign(motor rate)` that **is** a Coulomb relay. 🛑🛑 **WHY EVERY GATE WAS BLIND: they all test `product > ceiling`, and V80's supremum `(566*927)>>10 = 512` IS the ceiling ⇒ it clips 0.00% and passes.** The relay was not removed by the flat-FactorC edit — it **moved to FactorE's own knee, 17 counts under the rail** (slope drops ~1200× at `X[1]`=119). ***"DOES NOT CLIP" AND "IS NOT A RELAY" ARE DIFFERENT STATEMENTS AND ONLY THE FIRST WAS EVER CHECKED.*** Describing function `N(50)/N(500)`: **V75 1.45× · V80 3.27×**; `k` = **V74 0.5799 · V76 1.3866 · V75 1.5798 · V80 4.1597**. ★★ **MEASURED by both builds' own probes** — `|gp-0x6bd0| ≥ 448` engaged: **V75 0.000%** of 28,317 frames (never above 128 counts at all over 40 km/h; L4 census 0.000%) vs **V80 19.4%** overall / 32.7% above 15 m/s / **71% through the worst event**. ★ On the road: a **broadband HF FLOOR LIFT**, not a new peak — 30–49 Hz **2.09× [1.46, 2.70]** with a **pre-declared 32–38 Hz negative control failing identically (2.035)**, IMU vertical 1.07 [0.92, 1.33] ⇒ **not a rougher road**; FFT-free confirmation, engaged windows with a `|step| > 800` reversal: **V75 0.0% · V74 0.5% · V76 0.6% · V80 23.3%.** ★★ Plus **a sustained ~27.4 Hz limit cycle no other build produces** (26–31 Hz envelope >1000 ct in **32/215** windows vs 0/413, 0/328, 0/133): worst event **~30 s unbroken at 99–104 km/h, 27.56 Hz at ×92**, bar **6,830 ct p-p**, Q ≈ 140, crest **1.838**, damper ≥448 duty **71%**, no fault and no lockout; **NOT wheel order 2** (`df/dv` = −0.131 [−0.231, −0.016] vs the +0.961 order 2 demands), frequency pinned across a 20× speed range. ⚠ It is **the kit's own ~28 Hz line amplified ~2.7×**, not a new mode. ⚠ Command caveat: `0x0E4` correlates **+0.93 at lag 0** but the bar/command ratio is **15.8×** — [BELIEF] an echo; settling it needs a **phase-resolved coherence**.
- 🛑🛑 [RETRACTION — GRIND #1 IS INERT TO THE DAMPER DOSE](accord-grind1-is-inert-to-the-damper-dose.md) — four builds re-scored on **ONE** instrument (`rlog-tools/compare_v75_v76_v80_grind.py`, NFFT 256, p99 analytic envelope, ~10.2 s blocks nested in engagement runs). Ratio to V76 on **18–22 Hz**: V74 1.166 [0.98,1.41] · V75 0.735 [0.50,1.22] · **V80 0.835 [0.64,1.07]** against a **split-half null of [0.63, 1.60]** ⇒ **EVERY grind-#1 point sits inside its own noise floor across `k` = 0.58 → 4.16.** ⇒ **V80 did NOT "overshoot an optimum" — grind #1 never responded to `k` at all**, and V75-vs-V76's difference is a **creep-EXPOSURE** artefact (V76's creep windows carry **3.4×** V75's steering effort). 🛑 This **withdraws** the −0.599 and −0.614 slopes in two existing memories (both annotated in place). ★ **What survives, and is what to build on:** `k ∈ [1.39, 1.58]` **buys most of the ratchet benefit at ZERO HF cost**, and something switches on between **1.58 and 4.16** that costs **2× broadband HF plus a limit cycle** — **where in that gap is UNMEASURED.** ★ The **micro-ratchet is the band that DOES move** and is best at V80's dose (**0.418 [0.33, 0.61]**), consistent with the older "needs `k` = 4.2–13.5"; 40–49 Hz **2.017 [1.32,2.83]** and 30–49 Hz **2.091 [1.46,2.70]** both move together with the neutral control ⇒ a floor lift, not grind #2. 🛑 **DO NOT READ V80's CREEP NUMBERS** — median sustained effort **173 ct** / **1.3 °/s** vs V74/V76/V75's 685/588/1113 ct and 33/33/48 °/s, **ZERO matched cells**; the earlier "V80 is 3–30× quieter at creep" is **RETRACTED**. The **10–40 and 40–80 km/h strata are well matched and carry the load.**
- 🛑 [`0xC63A0` is EXONERATED of the hard faults — the operator directive's premise is refuted](accord-c63a0-exonerated-of-the-hard-faults.md) — the standing directive *"do not double `0xC63A0`, that is what was causing hard faults"* (quoted verbatim in `build_v80_tva.py`) rests on a **false premise**. [EVIDENCE] `0xC63A0` = `tp+0x73A0` has **exactly ONE reader** (`ld.hu` @`0x381AC`), **0 writers, 0 disp23 hits**; its reader `FUN_00038148` writes **exactly two cells** — `gp-0x374c` and `gp-0x6b70` — and **NEVER** `gp-0x6b26`, `gp-0x6c2c` or `gp-0x6a5e`; `gp-0x6c2c`'s two writers are both in `FUN_00041464` (`0x4184E`, `0x41AC2`). ⇒ **NO firmware data path from `0xC63A0` to the faulting monitor.** A *physical* path (aggregator → motor → plant → motor rate → `gp-0x6c2c`) exists and is **irrelevant, because the `0xC407E` clamp acts BEFORE the store.** ★ This is the **fourth** monitor surface shown blind to `0xC63A0` — it closes the "a fourth surface is not formally excluded" caveat in [accord-v77-cannot-reach-the-monitors](accord-v77-cannot-reach-the-monitors.md). 🛑 **Keep the two questions apart**: it **DOES** move delivered torque (Path 2 weight into a gain-scheduled PID) and **CANNOT** move what any monitor compares ⇒ raising it is a **GATE 2 loop-gain decision, not a fault-safety one.** ⚠ Its history could never separate them anyway (V72/V73 carried 2048 with a structurally **zero** damper). ⊕ `build_v80_tva.assert_c63a0_block`'s rationale comment is **known-wrong** and should be corrected. 📋 **RULE: a directive that names a CAUSE is a claim, and claims get verified.**
- ★★★★★ [THE FAULT MECHANISM IS `0xC407E`, CONFIRMED AND CLOSED](accord-friction-lane-ceiling-is-the-hard-fault.md) — 2026-08-07 additions [EVIDENCE, orchestrator's own Ghidra + raw LE scan]: **sole writer of `gp-0x6b26` is `st.h r6,-0x6b26[gp]` @`0x36CF0`** — exactly ONE writer image-wide (disp16, the 6-byte disp23 form, LE32 literals and `movhi`/`movea` pairs all scanned: **0 hits on all three alternatives**), and the stored value is **already clamped** to ±`0xC407E` (arms `0x36CCC`–`0x36CE2`); **`0xC407E` itself is 0w/3r, all `ld.h` SIGNED, all three inside `FUN_00036c12`** ⇒ its entire blast radius is one lane's clamp magnitude. The monitor's `gp-0x67fa ∈ {4,5,11}` gate is the **same** gate that wraps the producer's call ⇒ **unconditional relative to the producer.** **Margins: stock/V38/V76/V78/V79/V80/V81 = 511 ⇒ +1, UNTRIPPABLE BY CONSTRUCTION; V73/V74/V75 = 850 ⇒ −338, TRIPPABLE.** 🛑 **CORRECTION: the ×1.5 friction table came from V73, NOT V74** (stock/V70/V71c/V72 carry Honda's row; V73/V74/V75 carry ×1.5, and V73 raised `0xC407E` too) ⇒ V73 carried **both** legs and simply never met a big enough event. ★ **V75's fault was NOT the damper**: the damper was identically **zero for 4.98 of the last 5 s** and reached only level 2 **19 ms** before the trip; the car launched 0 → 7.6 km/h, column rate reversed sign twice in the final 150 ms, and **peak jerk hit 7,154 °/s² = 4.3× that route's own p99.9 (1,664) and the route maximum** — exactly what this mechanism predicts. ⚠ Unchanged: *"`0xC407E`=850 caused BOTH faults"* is still **[BELIEF]** — the DTC number was never confirmed on-car.
- 🛑 [The V38 rebase silently reverted THREE levers](accord-v38-rebase-silently-reverted-three-levers.md) — V76 was cut from **V38**, which predates V57, and nothing in V76→V78→V79→V80 re-applies it. `0x2A1F0` reader disp `0x7CD0` (decoupled `0xC6CD0`) on V62/V68/**V74/V75** → `0x746C` (**shared `0xC646C` = 3564**) on **V76/V78/V79/V80**; `0xC646C` stock 891 → **3564 (4×)**; `0xC62EA` low-speed lockout 0 → **320 restored**; `0xC63A0` 2048 → 1024; `0x454FE` `0xB5` → `0xBA` (V80 restored `0xB5`). 🛑 **V80 vs V75 was NEVER a single-variable damper comparison.** ★ **`0xC646C`'s full reader map re-enumerated three independent ways** (Ghidra + a fresh raw LE scan of both encodings + fresh decompiles: **6 readers, 0 stores, 0 disp23, 0 LE32-pointer hits**; every site `(x*cal)>>0xf`; 3564 = 4×891 exactly): #1 `0x2a1ee` forward (the one V57 decoupled) · #2 `0x2a904` **DEAD** · #3 `0x2b656` **RECLASSIFIED — NO TORQUE PATH** (output `gp-0x6af0` reaches only a private 2-function mode-flag debounce, `gp-0x677d` having exactly 2 static refs image-wide, plus a UDS packer with 0 static callers) · #4 `0x2c488` output `gp-0x6b10` **3 refs, all `st.h`, ZERO loads = proven dead** · #5 `0x36686` **the ONLY one reaching the motor** · #6 `0x3684a` modulates #5's hysteresis. 🛑 **Reader #5 CANNOT drive a 27 Hz mode — a BANDWIDTH argument**: its IIR `alpha = tp+0x73d2 = 6` ⇒ corner **≈0.93 Hz, ≈−26.6 dB at 21 Hz** (this settles the prior **"6 vs 14" in favour of 6**). ⚠ Its `±0x200` pre-filter clamp drops from ~18,829 to **~4,707** counts at 4× and was **never screened against a V76-lineage log** until now: route 66 `|bar|` engaged max **3,849**, `≥4707` fired **0/89,997** ⇒ **it did not bind — but the margin is only 22%** and the CAN scale is not proven identical ⇒ "did not fire", not "cannot fire". ⇒ **a real uncosted headroom regression, NOT the 27 Hz driver**; V81 removes it for free. ✅ `0xC6CD0` = `0xFFFF` on V76/V78/V80 is **provably inert** (0 instructions read `tp+0x7cd0`).
- ★★★ [V81 BUILT, VERIFIED, **UNFLASHED** — the flown V75 with Honda's friction lane back](accord-v81-built-c407e511-friction-stock.md) — **CAL-ONLY, NO CAVE CHANGE, 126 bytes.** Base = `_v75_CY0.566-EX1.200_magprobe_plain_image.bin` sha `e16ba4093205772e3a1bfb48f8790ade5c12f0e042b6608e51a48faaf1edf61c`, **the cut that FLEW route 5e**. Image `_v81_C407E.511-FRICTION.STOCK_plain_image.bin` sha **`4ddbd0e2fca5c37873f4c1b633e88a81d4d62a3b45743ce2c13e1c7403bfd65b`** · rwd `39990-TVA,A160-V81-V75BASE-C407E.511-FRICTION.STOCK-magprobe-6bd0-thermo-6ac2-0x13000-0x100000.rwd` sha **`fc4d4f74956c76dbda340e17ecf4c3ecbe3f86bbc47418cbc3b3185c52aea109`** (986,042 B). **EDIT 1** `0xC407E` 850→511 (`5203`→`ff01`) restores Honda's interlock; **EDIT 2** the ×1.5 friction table → **stock at all 14 sites** (`67c667de7bf4`→`9ad99ae952f8`). 🛑 **`0xD2A4C` is mode 10, a DISENGAGED-column record V74's derivation never wrote** — V81's edit there is a revert to stock, so that column can only become more stock. **Verification: 25 differing runs / 126 bytes (15 functional + 10 CRC), 0 unexpected functional runs, 14/14 friction sites; value-anchored — restoring exactly those 126 bytes reproduces the flown V75 BIT-FOR-BIT; all 34 friction records byte-stock; CRC 50/50 PASS; exactly 1 flashable V81 `.rwd` and 1 plain image on disk.** `k` = **1.5798 identical to V75**; `0xC63A0` 2048 · `0xC62EA` 0 · `0x454FE` `0xB5` · `0xC646C` 891 stock · cave and hook byte-identical. **GATE 1 PASS (vacuous — cal-only). GATE 2 PASS empirically**: no loop changes at all, `k` is a **frequency-independent scalar** so loop gain equals V75's at every frequency with **no plant model needed**, and the one nonlinearity that moves is a **saturation bound moved DOWN** (V81-A clamps ~10% MORE often than V75, at stock's own threshold, and at 511 clamping **cannot fault**). ★ **Decisive empirical bound: on V76 — Honda's friction row with `0xC407E`=511, exactly V81's configuration — `|gp-0x6b26| > 448` fired 0/63,477 frames** with the positive control at 99.926% ⇒ the lane is **measured to be unexercised.** ⚠ **It removes drag the operator may be used to** — a feel change as well as a safety change; tell the operator. **Variant B** (keep ×1.5) is implemented but **NOT cut**; the probe **could not discriminate** it (×1.5 pins at a stock-equivalent raw ≥ 340.7 vs a rung at 448 — 76%, inside the comparator's first cell) ⇒ rungs at **320/352/416** would settle it for ~30 cave bytes.
- 🛑 [Six tooling traps from the V76/V80 routes](feedback-v76-v80-tooling-traps.md) — (1) **`rlog-tools/decode_v76_probe.py` is the WRONG decoder for route 65** and will give a confident wrong answer: it documents the **superseded V74-base** V76 whose **bit7 is `gp-0x6bd0 != 0` (the damper, not the friction lane)**; the build that flew route 65 is `V76-V38BASE-RELU-C566-damper-frictionCLAMP511-probe-6b26-63fd` and its extractor is `analysis-2020accord/v76flight_extract.py` → `_cache_r65_records.pkl` (**not** `_cache_r65/`). (2) **Two `_v76*plain_image.bin` on disk and a `Glob` returns the WRONG one first** — `_v76_gate_fb_arm5244_gateprobe_plain_image.bin` is the abandoned V74-base candidate (still carrying the V57 decouple); the V78/V80 ancestor is `_v76_v38base_relu_damper_plain_image.bin`. The `.rwd` was renamed `SUPERSEDED-…`; **the stale plain-image snapshot still reads as current.** (3) **`build_v75_tva.py`'s default lever set does NOT produce the flown V75** — you must pass `ACCORD_V75_LEVERS=CY0,EX1`; the default (`CY0` only) writes never-flown `…CY0.566…` artefacts. **The flown V75 is the `EX1.200` cut, dose 137, `k`=1.5798.** (4) **Route `5d` — V74's clean symptom-measurement flight — has NO raw rlogs anywhere in the repo**; only `_cache_r5d/*.npz` + `.pkl` survive, so every downstream V74 conclusion in `STATE.md` runs against a cache. (5) **V80's probe cannot distinguish V80 from V78/V79** (byte-identical cave); identity rests on the `.rwd` filename plus the absolute exclusion of V76-V38BASE (13,183 frames set bit6 with bit5 clear). (6) The default `python` (anaconda base) has a **broken numpy DLL** — prepend `C:\Users\dudei\anaconda3\Library\bin` to `PATH` or use `C:/Users/dudei/anaconda3/envs/bin_decompile/python.exe`.
- ★★★★★ [STOCK ships mode 24 ≡ mode 26 — the engaged-only damper is OURS](accord-stock-mode24-equals-mode26-damper-is-ours.md) — byte-identical across **all six factor families** in Honda's firmware, and `FactorC Y[0]==0` in **all 13** distinct stock records. Armed at **V74**, never before. Sign is `−sign(motor rate)` ⇒ it fights the **driver** too, even turning WITH the command ⇒ explains "heavier when engaged". FactorC axis = voted speed @**64 ct/km/h** (`X` = 35/60/80/140 km/h); FactorE = motor rate @4.7121 ct per °/s. 🛑 Honda's surface is **viscous** (`N(50)/N(500)` = 0.00×, stabilising); ours is a **relay at every speed** (1.45×) from FactorE's `Y=[0,539,539,927]` **plateau** — **and it never saturates**, so the V80-era no-clip/`≥448` tests cannot see it. Mode 26 = engaged confirmed twice: V73's `gp+0x63fd` probe **and** a fresh `FUN_00042746` decompile (`gp-0x67f6`, `gp-0x6806` edge, debounce `gp-0x4f68` vs `tp+0x7180`). ★ Closes `gp-0x67f4`: a **vehicle-speed voter plausibility flag**, NOT an engagement gate.
- ★★★★★ [V81 carries NEITHER measured grind-#1 fix](accord-v81-carries-neither-grind1-fix.md) — byte-stock at **both**. Lever A = V62's `sar`×2 (`0x3AB76` r26 + `0x3AC20` r24; 0.39 [0.32,0.48], but the **r24 half CAUSED grind #2**) — only V62/V65/V71a. Lever B = V67/V68's LKAS-gated r24 arm (`0x3AA96` `C5`→`FB` + `0xC6446` 512→5244; **0.40 [0.27,0.58] best in kit AND creep grind #2 → 0 bursts**) — only V67/V68/V71c. ⇒ route 67's grinding is an **absence of any fix, not a regression**. **Third** silent loss of a confirmed fix (after `0x454FE`). 🛑 Lever B is NOT the highway answer — **V67/V68 had it and the highway grind was still present.** V81↔V67/V68 delta is **five cells**; 4× LKAS (`0xC6CD0`=3564, disp `0x7CD0`) is intact on both.
- ★★★★ [CAN gateway is a WHITELIST — 20 free bits in the 3 frames that cross](accord-can-tx-gateway-whitelist-and-20-free-bits.md) — V53's FOURFRAME2 was **mechanically correct and still got zero frames**, as did stock IDs `0x19F`/`0x660`/`0x32E` ⇒ **a new ID can never reach openpilot; close that branch.** Only `0x14A`/`0x18F`/`0x1AB` cross. **16 clean bits (20 with a mask edit) vs the 5 in use.** Checksum `FUN_00057b24` is called last ⇒ **auto-covers** spare-bit writes. Hooks: 330 `0x55C0E` **taken**; **399 `0x55D50` and 427 `0x55EFA` are byte-stock on every build ever made.** 1,144 cave bytes free at `0xC4B78`. ⚠ Corrects the "0x18F byte5 is fully free" claim — bits[5:4] are a **live signal**.
- 🛑🛑 [`0x18F`'s payload is ONE CAN FRAME (≈10 ms) stale vs `0x14A`'s](accord-0x18f-payload-one-frame-stale.md) — **corrects every `command → bar` phase this kit has ever computed**, by 99° at 27.5 Hz. Derived from a first-principles invariant: the transmitted rate is the derivative of the transmitted angle, so it must lead by +90°, and a lead-error constant in **ms** (not degrees) is a pure timebase offset — `rate_c`/`0x14A` **−2.2 ms** vs `rate_f`/`0x18F` **−12.1 ms**, difference = one frame. ⚠ `rate_f`'s stored scale is 0.8× the truth (use ×1.25); `cs_tq`/`cs_rate` are unusable above ~10 Hz (corr 0.60 at 27.5 Hz); 1.7% of CAN rows share a `logMonoTime` and silently NaN any `np.gradient` over `t`.
- ★★★★ [Aliasing IS resolvable: `|Rate(f0)|/|Ang(f0)| = 2π·F_true` survives the fold](accord-alias-resolution-via-derivative-ratio.md) — the EPS differentiates at 1 kHz **before** the 100 Hz sampling, so the fold hits angle and rate identically and the magnitude ratio still reports the TRUE frequency. **Closes an ambiguity the kit carried through a dozen handoffs as unresolvable.** Route 67: measured 119.2 rad/s vs 171.8 (27.34 Hz) / 456.5 (72.66 Hz) ⇒ **the 72.66 Hz twin is EXCLUDED at 3.8×**. Needs high angle↔rate coherence at the bin (0.997 here); at coherence 0.3–0.5 the ratio is meaningless. ⚠ A **31% magnitude deficit vs the 27.34 Hz prediction is UNEXPLAINED** — keep the caveat attached.
- ★★★★★ [RESOLVED: the aggregator DOES reach the motor — via the `gp-0x6acc` bridge](accord-aggregator-reaches-motor-via-gp6acc-bridge.md) — `gp-0x6b94` → governor `FUN_0004503c` → `gp-0x6ace` → comp-add `FUN_000456a4` → **`gp-0x6acc`** → shaper reads it @`0x431C4` → `gp-0x6b08` @`0x43206` → SM2/SM3 integrator → `gp-0x6b98` → FOC → PWM. **Orchestrator byte-verified the crux**: predicted encodings `244f3495` / `645ff894` both MATCH, writer `st.h r8,-0x6acc,gp` @`0x45932`, mode cal `0xC64C8` = **0x00 (pass-through) on stock and every build**. 🛑 **Eleven methods missed it because they all asked "who reads `gp-0x6b94`" — nobody asked about `gp-0x6acc`, two hops out**; and `gp-0x6b08`'s "self-referential, no external reader" characterisation was true and misleading (the function's OWN next instructions consume it). ⊕ The chain was **already documented** in `reference_accord_post_governor_comp_add.md` and never cross-checked against the newer "cannot reach" claim. **Explains V40's brick mechanistically** (removing the governor slew snaps `gp-0x6ace` to target → unbounded step into the integrator) **and the graded V74→V81 dose-response** (dose in, dose out, no threshold) ⇒ the DTC-side-channel hypothesis is **SUPERSEDED**. 🛑 `0xC64C8` mode 1 **discards** the aggregator for a static cal, mode 2 blends — a one-byte switch, UNTESTED, writer census not run. ⚠ Last hop `0x43226`→`gp-0x6b98` still inherited, not hand-walked. **Corrects `accord-v77-cannot-reach-the-monitors.md`.**
- 🛑🛑 [A bandpass ringdown Q is meaningless without a STEP CONTROL](accord-ringdown-q-needs-a-step-control.md) — **two agents independently fitted a filter's own step response and read it as plant damping** on the same route-67 edge (Q≈7 and Q≈10.4–10.8 at **R² 0.987**; a filter's step response IS a clean exponential, so high R² proves nothing). Bandpassing a *perfect step* with zero plant decay yields apparent **Q 9.0–11.6**; widening the band 8× shows no separation from that control. ⇒ **Q ≲ 6 upper bound; the 27.5 Hz ring STOPS (~1.8 cycles), it does not decay** ⇒ **not a forced resonance by anything** — every "drive × Q" bound is void — but an **actively sustained limit cycle with an engaged-only source**, and only openpilot's command (share ≲3–5%) and the engaged-only damper switch at that edge. 🛑 No cross-build Q ladder is obtainable (a gateless fitter returned Q=1028/tau 11.8 s); every disengage edge measures the **DISENGAGED** plant (mode 24, byte-stock on V76/V80/V81). ⚠ Full-event linewidth Q=103 is f₀ wander (**0.576 Hz**), not damping — which also breaks harmonic fold arithmetic for N≥5 (±4 Hz smear at 7f); use a drift-tolerant phase-lock test instead.
- 🛑🛑 [~~FactorD is the ANGLE-ERROR lever — the only frequency-selective one~~ **HEADLINE REFUTED 2026-08-09**](accord-factord-is-the-angle-error-lever.md) — **`gp-0x6a10` is ABSOLUTE STEERING ANGLE, not a tracking error** (99.94%, step on the threshold's own value, holds in the MANUAL arm) ⇒ **"the only frequency-selective lever" is REFUTED — THIS FIRMWARE HAS NONE**, which also removes the argument that FactorE cannot do what FactorD can. **And FactorD is STRUCTURALLY INERT where the symptoms live**: FactorC multiplies in BEFORE it with `X[0]`=34.97 km/h, `Y[0]`=0, in all four modes. See [[accord-levers-killed-2026-08-09]]. **The disassembly, the +0x0C layout rule and the blast-radius work below are still correct.** ⚠ ORIGINAL TEXT FOLLOWS: `gp-0x6a10` = unsigned, unclamped **angle-tracking-error magnitude at 0.1°/count** (anchored two independent ways, not from FactorD's own breakpoints); X `[0,50,100,150,700]` = **0/5/10/15/70°**; gate `< 0x2711` is an overflow rail. **Flat-unity in every mode, never written by any build, n=5 already ⇒ no code edit needed.** ★ Structurally special: angle amplitude scales as 1/ω, so at 7.79 Hz the excursion is **3.6× larger** than at 27.75 Hz ⇒ **a FactorD rising with angle error preferentially damps LOW frequencies.** That is the only frequency selectivity in this firmware — FactorE cannot do it, because the damper's nonlinearity is **memoryless** ⇒ shape changes magnitude at every frequency identically and never phase. 🛑 Two measurements gate it: `gp-0x6a10`'s real excursion during a ring (column angle p-p 1.29° ⇒ ~6 counts, likely deep in the flat segment) and **`gp-0x67fe` ∈ {1,2}, which is NOT settled**. 🛑 `build_v30_tva.py`'s "driver torque `gp-0x69ca`" was WRONG — corrected 2026-08-08. ✅ **REINFORCED 2026-08-08**: flat unity verified over **2,924 records (86 images × 34 modes)**; layout rule **`X` @`base+2`, `Y` @`base+2+2·npt`** = `+0x0A` for 4-point B/C/E but **`+0x0C` for 5-point D** (a 4-point parse misreads `X[4]=700` as `Y[0]`); excursion **6.71 ct @27.75 Hz · 9.31 @20 Hz · 23.89 @7.79 Hz** ⇒ all inside the first flat segment ⇒ **`X` must be RESHAPED, not just `Y`**; sole table reader is `FUN_00034350` (the other 7 `gp-0x6a10` consumers read the raw cell) ⇒ **fault-isolated**.

## 2026-08-08 session — V83a flew (worst modern build), r24 is the grind-#1 actor, mode 27 found, V84 built

- ★★★★ [r24 IS the grind-#1 actor; r26 is nearly blind](accord-r24-is-the-grind1-actor-r26-nearly-blind.md) — mode-10 builds excluded as byte-stock (RULE 7); delivered dose at 7 km/h / 128 °/s engaged: V61 (r26 0.000, **r24 0.000**) → **2501** · stock/V69/V70 (1.000, 1.000) → 879/746/729 · **V72 (0.177, 1.000) → unmoved 0.953** · V62/V65 (2.000, **2.000**) → 168 · V67/V68 (0.177, **1.994**) → **109**. r24 **monotone** across 0→1→2×; r26 swings **11.3×** at fixed r24 and grind #1 barely moves; **V71B is the isolated r26 ×2 test and scored 545 — stock band.** ⇒ 🛑 **the long-carried "r26 ×2 AND r26 ÷6 BOTH helped, carry this unexplained" tension is RESOLVED: NEITHER r26 move helped.** [EVIDENCE for bytes/arithmetic; **BELIEF** for causal attribution — four cross-route medians, no covariate matching.]
- ★★★★ [V83a flew as route 68 — the worst modern build on both scored symptoms](accord-v83a-flew-worst-modern-build.md) — fault-free. Cell-stratified vs V81: **18–22 Hz 2.674 [1.956, 3.885]** against a split-half null of [0.63, 1.55], **10/10 cells > 1**; **6–9 Hz 1.526 [1.174, 2.019]**; **26–31 Hz ring 1.021 — FLAT.** 🛑 **Its own PRE-REGISTERED falsifier fired ⇒ the damper-dose model of the 26–31 Hz ring is FALSIFIED.** ⚠ Not a clean test either — see the mode-27 confound.
- ★★★★ [Mode 27 is a SECOND engaged column, and V83a flew with V81's relay damper on it](accord-mode-27-is-a-second-engaged-column.md) — `TVCA4` row 11 → modes [24,25,26,27]; **columns 2/3 are the engaged pair.** V83a reverted mode 26 and left **mode 27 byte-identical to V81's m26** — the 539 `FactorE` plateau, relay index `N(50)/N(500)` = **1.45** vs Honda's **0.00**, **9.5× mode 26's dose at 200 counts of motor rate.** ⊕ **Honda's pairing is 24↔26 and 25↔27** (stock `m24/m26` FactorC Y `[0,234,429,908]` vs `m25/m27` `[0,233,426,875]`) ⇒ 🛑 **a gate asserting "engaged ≡ mode 24" fails on Honda's own firmware; edit BOTH pairs or the edit is half applied.**
- ★★★★ [The CAN telemetry surface, settled empirically — 1.66 M frames](accord-can-telemetry-surface-census-1p66m-frames.md) — census over **1,662,374 `0x14A` frames, 30 routes, 29 builds**, two independent implementations agreeing exactly. ✅ **`0x14A` is PROVEN BYTE-TRANSPARENT** by a counterfactual: 93.2% of frames carry non-zero magprobe bits at positions **no DBC defines**, and a signal-list re-serialiser would have emitted a wrong checksum in **85.5%** of them — it never does. ✅ **`0x14A byte7[7:6]` is free AND survives to the wire** (both stock RMWs of `gp-0x1511` preserve 7:6 — `andi 0xcf` @`0x55BFC` pre-hook, `andi 0xf0` @`0x55C24` **post**-hook); **`byte7[5:4]` is NOT free** (`gp-0x6880 & 3` @`0x55BF6`/`0x55BFA`). 🛑 **`0x18F byte5[4]` is CONFIRMED LIVE ON THE WIRE** — 14,472 frames, but **only** on routes `5e`/`61` (the hard-fault flights), coincident with `STEER_STATUS`→7; flat zero on 28 of 30 routes ⇒ **the canonical proof that empirical-zero ≠ free** (`0x1AB byte0[1]` is worse: 27 frames in 831,200). 🛑 **`0x18F` DLC = 7 in 100% of frames — THERE IS NO BYTE 7**; `byte6[6]` is the last usable bit. ⚠ **`0x18F`'s transparency does NOT transfer from `0x14A`** [BELIEF] — settle it with one build writing a known non-stock pattern into `0x18F byte4[2:0]` plus a live `0x14A` write as an in-flight positive control. 🛑 **`0x1AB` `MOTOR_TORQUE` VARIES ⇒ forbidden as a channel.** Clean-tier total **16 bits**, matching the existing ledger exactly by a different method.
- 🛑 [Two V850 cave-encoding traps, both caught by build-time gates](accord-two-cave-encoding-traps-sar-floor-and-opcode-bit.md) — (i) **`sar` floors toward −∞**, so a naive `cmp -0x4`/`bgt` rung fires at r24 ≤ **−769**, not −1024 — a **255-count asymmetry**; correct immediates are `cmp 0x4`/`blt` → +1024 and `cmp -0x5`/`bgt` → −1025, found by an **exhaustive self-check over all 16,385 values of r24 in ±8192**. (ii) **`ld.h` op `0x39` vs `st.h` op `0x3B` differ by ONE BIT**, and for `gp-0x6ada` the firmware's only other instance of that displacement **is** the store. ⚠ Also: **`gp-0x6a10` is word-aligned**, so an `ld.w` slip would **not fault** — it would silently read `gp-0x6a10` and `gp-0x6a0e` as one word.
- ★★ [Two lockstep shadows not previously recorded](accord-lockstep-shadows-67fe-4c3a-and-6a10-4c90.md) — `gp-0x67fe` ↔ **`gp-0x4c3a`** (paired `st.b` at every writer, escalating via `FUN_0006b9fa` @`0x3BE68`) and `gp-0x6a10` ↔ **`gp-0x4c90`** (`FUN_0003fc16`). **Reading is safe; writing either requires the matched shadow write.** ⊕ `gp-0x67fe`'s value domain is exactly **{0,1,2}** across all five writers ⇒ `!= 0` and `∈{1,2}` are equivalent on this image, so FactorD's enable gate needs **one** telemetry bit, not two.
- ★★★★ [V84 BUILT, VERIFIED, UNFLASHED — engaged ≡ manual on BOTH mode pairs](accord-v84-built-engaged-equals-manual.md) — base V83a; 7 control cells + a probe repoint inside the **proven 68-byte cave**; CRC 50/50. `.rwd` sha **`5e830b2588b22fd6238c4bd376e602d603b5d25871368d08df7986519cda1bca`**, image sha **`344f22f7303f6b5b006b13d329192ce098d118c9ce149834cb3cc05899dc637a`**. ⚠ **RE-CUT** — an earlier control-only cut (`54985b45…`/`bdd857c9…`) is retained as `SUPERSEDED-DO-NOT-FLASH-…`. Edits: `0x3AA96` `C5`→`FB`, `0xC6446` 512→**5244**, FactorC **m26 and m27** `Y[0]` 566→0, FactorE **m27** → Honda. **After V84 every factor family is engaged ≡ manual, verified exhaustively over speed counts 0…14000, both pairs**; V84's damper surface is **byte-identical to V67/V68**. ⚠ Lever B is still not the highway answer.
- 🛑 [Read the REPO's `memory/`, not the stale auto-memory snapshot](feedback-read-the-repo-memory-not-the-stale-snapshot.md) — the snapshot at `C:\Users\dudei\.claude\projects\C--…\memory\` is **what auto-loads into a session** and is **NOT** the repo's `memory/`. A subagent grounded on it this session reproduced **six corrections' worth** of stale conclusions before catching it (the r26-kill attribution · state-4-vacuous · the two-ratchets terminology · the sampled-relay characterisation · the Q≈14 revision · the retracted dose-response fit). **Prime every subagent with the REPO path explicitly.**
- 🛑 CORRECTIONS APPLIED IN PLACE 2026-08-08 — [steeringTorqueEps](honda-op-steeringtorqueeps-always-zero.md) (`0x1AB MOTOR_TORQUE` **varies** ⇒ forbidden as a channel; "reads 0" was about openpilot's *decoded* value being small) · [V38 rebase](accord-v38-rebase-silently-reverted-three-levers.md) (**SEVEN levers not three**; the seventh, `gain_A` rec0/rec1 `0xC6A72`–`0xC6A8E` 512 → Honda's on V76/V78/V79/V80, **had never been logged anywhere** ⇒ **five confounds, not four**) · [r26 inert](accord-r26-is-structurally-inert.md) (its **"r24 is near-inert" leg is VOID** — it rested on the deleted mode-10 ladder) · [V42 = the r26 kill](reference-accord-v42-fix-was-the-r26-kill.md) (**`0xC643E`/`0xC6444` are UNREACHABLE** — `gp-0x671a`'s one writer @`0x42A12` is fed only by `gp-0x67df`, never non-zero ⇒ **V42's only live change vs stock was `gain_A` → 0**) · [task 5 = 100 Hz](accord-task5-is-100hz-damper-cannot-damp-21hz.md) (**confirmed and sharpened**: `FUN_00034350`'s sole caller is `FUN_00022ca0` = 100 Hz while `FUN_0003aa2c`/`FUN_0003a382` are task 1 = 1 kHz ⇒ **the "1 kHz damper" claim is a NAMING COLLISION**; ZOH penalty **14.0° @7.79 Hz · 37.6° @20.9 Hz · 99.7° worst-case @27.7 Hz, crossover 25 Hz** ⇒ **above 25 Hz a 100 Hz table damper is sampled into ANTI-damping**) · [`gp-0x6c2c`](accord-gp6c2c-is-the-detector-input.md) (**filtered motor ACCELERATION, not rate** — `FUN_00041464`, two cascaded IIRs on the **one-cycle delta** ⇒ friction lane `gp-0x6b26` ≈ 0 under steady motion at any speed or torque; **"remove friction to fix steady-state heaviness" is NOT supported**; scale **≈ 0.3016 ct per °/s²**, cross-validated 7,076 vs a measured 7,154 °/s² peak jerk) · [FactorD](accord-factord-is-the-angle-error-lever.md) (**reinforced**, see its own line above) · [two ratchets](accord-two-ratchets-micro-is-the-779hz-line.md) (**MICRO = the 7.79 Hz line; MACRO = the large hard-turn symptom, never successfully instrumented**).

### Ported from the auto-memory snapshot, 2026-08-08 — these were wikilinked from repo files but existed ONLY in the snapshot
🛑 A dangling repo wikilink usually means *"lives only in the snapshot"*, **not** *"was never written"*. See [[feedback-read-the-repo-memory-not-the-stale-snapshot]].
- ★★★ [r24 and r26 have SEPARATE selectors but ONE shared gate](accord-rate-lane-builds-were-never-single-variable.md) — `gain_B` vs `gain_A`, both armed off the single `lp` register (one writer, `setfne lp` @`0x3AAA8`) ⇒ **the rate-lane builds were never single-variable.** **V84 is built directly on this** — it is why one gate byte raises r24 and cuts r26 at the same time.
- ★★★ [Seven V850 scan traps that gave wrong answers](accord-v850-scan-traps-formatv-and-storezero.md) — Format-V aliasing, store-zero, `hw2 = (disp\|1)`, LERP-vs-(lo,hi), and the **`ld.h` `0x39` vs `st.h` `0x3B` one-bit** discrimination. **Two of these nearly bit the V84 cave build**; includes a validation harness.
- [FUN_00045a20 monitor + 4 shadow lockstep pairs](reference-accord-fun45a20-monitor-and-shadow-lockstep-pairs.md) — a hard-shutdown monitor feeding DTC `0x1d`; the 4 shadow pairs are ruled out as cal-edit-triggerable (both halves are written in the same instruction sequence).
- [V31P's DELIVER_CUT telemetry bit is BROKEN](eps-deliver-cut-gp6809-broken.md) — `330 byte7 bit7` = `gp-0x6809 != 0` was the wrong source cell. ⊕ **Read alongside the 2026-08-08 census: the bit was broken at its SOURCE, not on the path** — V31P was flashed and the write reached the comma, which is independent corroboration that `0x14A byte7[7:6]` is both free and transparent.
- [Gentle EME fires on a saturated LKAS command](gentle-eme-fires-on-saturated-lkas-command.md) — root cause relocated to the debounce SM (fixed at V37); visible only via raw CAN `399`, not `carState`.
- ★★★★★ [V87 BUILT: the first SUBTRACTIVE build](accord-v87-built-measurement-on-v38-base.md) — V38 base + V57 gain + V42 ratchet fix + steer-to-zero + **2 bytes** repointing 427 `MOTOR_TORQUE` to `|gp-0x6b98|`. 🛑 Will read as a NULL by design; the feel change is the REBASE, not an 8 Hz lever.
- 🛑 [`0xC63B8`'s 8 Hz band-pass is a RECTIFIED boost index, not a damper](accord-c63b8-8hz-bandpass-is-a-rectified-boost-index.md) — real structure (peak 8.13 Hz, Q 0.501, untouched in 88 images) but refuted **five ways**; already flown NULL as V60. ⊕ Excellent free *sensor* (`gp-0x6de8`, 1w/0r).
- 🛑🛑 [A filter in the shaper DISABLES THE POWER STEERING](accord-shaper-float-twin-blocks-filter-insertion.md) — `FUN_00043e44` float twin, ±5 counts, 10 ms → DTC 0xF00049. The two "best" hook sites are inside its coverage; phase budget there is **2.4°**. Only `0x453e0` is monitor-clean.
- ★★★★★ [`gp-0x6b26` is a REAL 6-9 Hz damper — measured, not calculated](accord-gp6b26-is-a-real-6to9hz-damper.md) — **+137°/+139° vs WHEEL rate on two independent drives** (|cos| = 0.73), ω-partialled share **−20 % [−37,−11] / −26 % [−59,−10]** = **+518/+565 counts of POSITIVE Re(Z)**. 🛑 **Retires the desk figure "+75°, 26 % dissipative, structurally cannot damp 6-9 Hz"** — that was the *producer's* filter phase vs *motor* rate; this is the *delivered lane* vs *wheel* rate with the plant in between.
- ★★★★ [`gp-0x6bbe` is RATE-derived, NOT the base assist](reference-accord-gp6bbe-is-rate-derived-not-base-assist.md) — `bbe←tq` is **0.01 ct/ct ∠+144°**, fully explained by both channels being driven by ω; `bbe←ω` is **flat 87-92 ct/(rad/s), 2-12 Hz, +18° at 6-9 Hz** = source-side. 🛑 **CONTRADICTS `STATE.md` §A1 and [[accord-gp6bbe-is-viscous-plus-dc-pedestal]]'s "base-assist output".** Dead as a lever: **9-15 %** of Re(Z) and its rate part is **4-9 % of a 73-80 ct DC assist pedestal**.
- ★★★★★ [Re(Z) anchored TWICE, and its detection floor](reference-accord-rez-anchored-on-car-and-its-floor.md) — sign convention fixed parameter-free by `mean(T·ω)` (**n = 20,159, P(>0) = 0.9238**, 8 routes/8 builds); and the metric ranks **V80 worst at 12-16/18-22 Hz (−8883/−3581)** — the build the operator called the worst grinding ever. Floor **~60 ct at ≥12 episodes, use 150**; 18-22 S/N ≈ 75×, 6-9 ≈ 2.5×. 🛑 **Never quote Re(Z) below 6 Hz from a `steeringPressed` mask** — 2-4 Hz reverses sign.
- 🛑 [Four 6-9 Hz stories killed by their own controls](reference-accord-controls-killed-four-6to9hz-stories.md) — assist-map operating point **flat across a 4× DC-bias range**; **Lever B `0xC6446` CLEARED** via pre-V74 builds that separate it from `0xD77DA` (⇒ **V88's grinding fix need not be traded away**); **0 of 41 varying cells** separate 6-9 Hz; and the hands-off coast test is **structurally guaranteed to null** (coh² 0.040 vs a 1/n ≈ 0.014 bias floor — the corpus already holds **856.7 s**, not 21.4 s).
