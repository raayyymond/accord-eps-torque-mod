# MEMORY — PART 6

🛑 **Continuation of `MEMORY-PART5.md`.** Split 2026-08-29 because PART5 reached 187 KB and
was growing ~2 KB per iteration; CLAUDE.md's hard cap is 256 KB and a file past the `Read` limit
loads with its tail **SILENTLY TRUNCATED**. Read PART5 and PART6 together — PART6 holds the LATER
entries, so a claim here supersedes the same claim in PART5.

- 🛑🛑🛑★★★★★ [**`0xC4936` is a PWM HARDWARE-TIMING cal — DO NOT TOUCH IT**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the **only** calibration operand in the entire FOC PI/SVPWM region (0.25 cals/KB) and the last open symptom-B candidate. Its single reader `0x6C486` is inside **`FUN_0006c446`, a PERIPHERAL-INITIALISATION routine** writing the motor timer/PWM block (region byte-identical stock vs V122): `_DAT_ffffcc58 = 0x1388` (period-like 5000), **`_DAT_ffffcc5c = cal(0xC4936)×2 + 0x50 = 580`**, `_DAT_ffffccb0/b4/b8 = 0x1428` (**three identical ⇒ 3-phase compare registers**), plus `_DAT_ff809220/24/28` and `_DAT_ff81c084/88` in peripheral space ⇒ **[EVIDENCE] it is NOT a control-law gain, it is PWM/timer HARDWARE CONFIGURATION** written once at init. 🛑🛑🛑 **HARD STOP**: a `2×cal + offset` field in a 3-phase PWM timer block is most plausibly **DEAD TIME or a phase offset**, and **shortening inverter dead time causes SHOOT-THROUGH, destroying the power stage** — **strictly worse than bricking**, which this kit has survived three times (V24/V27/V48B) precisely because a bricked ECU is reflashable and a destroyed inverter is not. **[DECISION] never change `0xC4936`** — virgin at 250 across all 155 images, and it stays that way. ⭐ **Recorded prominently because the trap is attractive**: a future scan will surface *“a VIRGIN cal, single reader, in the FOC region, untouched by 155 builds”* and read it as opportunity — **it is the opposite**; Honda left the drive stage uncalibratable on purpose. ✅ **SYMPTOM B ANALYSIS COMPLETE**: forward path has no active switching nonlinearity, `0xC6194` is already smooth at 1 kHz, the drive stage has no calibration surface, and its sole exception must not be touched ⇒ **not reachable by any calibration edit this kit may safely make**; the falsifier is down to **one** item — a broadband source that is engagement-conditional but **NOT** ∝ command amplitude, and the fully-traced forward path contains none. ⭐ **BYPRODUCT**: the golden model's **[OPEN] PWM carrier frequency** is **narrowed** — its register block is `FUN_0006c446` (`_DAT_ffffcc58`=5000 + compares `0x1428`); absolute Hz still needs the clock tree (PCLK 40 MHz would give 8 kHz edge-aligned / 4 kHz centre-aligned — **arithmetic, NOT verified**).

- ✅✅✅★★★★★ [**V156 — the DAMPER REACHES THE MICRO REGIME for the first time**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a **ζ 0.017–0.036** resonance sits where there is **no added damping at all**, because `ch0 = (FactorC(speed) × FactorE(rate)) >> 10` is a **PRODUCT OF TWO DEAD ZONES** and below `X[0]` a LERP returns `Y[0]`: **FactorC Y[0]=0 ⇒ zero below 35 km/h**, **FactorE Y[0]=0** with `X[0]=60` ↔ the recorded **12.73 °/s** dead zone (~0.212 °/s per count) ⇒ **the micro regime (1–13 °/s) sits ENTIRELY below X[0], exactly where Y[0] applies** ⇒ measured **zero on 100 % of the micro regime**. ⭐ **NEITHER FACTOR ALONE OPENS IT, and the kit tried both separately**: **V134** raised FactorC Y[0]→60 and measured **INERT** (its own header says FactorE Y[0]=0 leaves the product zero), and the **FactorE X[0] 60→12 variant was WITHDRAWN** as *“structurally vacuous at creep”* ⇒ **both Y[0] must move together and that build never existed.** ✅ **V156**: `0xD77DA`/`0xD77EE` FactorC Y[0] 0→60 **and** `0xD7816`/`0xD782A` FactorE Y[0] 0→539 on engaged modes 26/27 — **6 payload bytes, 60/60, CRC 50/50**, rwd `bc070cba…`. ⊕ **Addresses resolved TWO WAYS AGREEING**: the record block at stride `0x14` is **three FactorC then three FactorE records** (modes 25/26/27), giving FactorE m26 Y[0]=`0xD7816`, and that **matches the lineage's independent “FactorE m27 = `0xD7822`” anchor**. ⊕ **RULE 7 mode-proof**: `ENGAGED_MODES=(26,27)`, `MANUAL_MODES=(24,25)`, read from the builder. 🛑 **DOSE SIZED BY V80's CATASTROPHE** (FactorC FLAT 566 passed the ceiling, made the damper **BANG-BANG**, worst grinding in the kit's history): creep product **(60×539)>>10 = 31 = 6.1 % of the 512 ceiling**; FactorC 60 is V134's safety-checked value and FactorE 539 is FactorE's **own Y[2]**. ✅ **RESPECTS THE OPERATOR'S CONSTRAINT** — damping is added **only below 12.73 °/s**; above it FactorE is **byte-unchanged**, so **max angular velocity and acceleration are untouched** and the cost lands entirely inside the symptom's own regime. ⚠ **[BELIEF] 31 counts may be too small to feel** — no dose-response exists because no build ever had a live damper here. ⊕ **`0xC63A0` (the damper WEIGHT) HELD at 1024** — exonerated of V74's fault (that was `0xC407E`) and it **multiplies** what this admits ⇒ **the natural second dose.**

- ✅✅★★★★ [**V157 — the 4× dose of V156; and `MEMORY-PART4` split at 199.5 KB**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V156's creep product is **31 = 6.1 % of the bang-bang ceiling**, possibly too small to feel, so **V157 is the same lever, same four cells, 4× the dose**: FactorC `Y[0]` → **each mode's OWN `Y[1]`** (234 m26 / 233 m27) and FactorE `Y[0]` → its own `Y[2]` (539) ⇒ **product 123 = 24.0 %, a 4.2× margin to 512**; **6 payload bytes, 62/62, CRC 50/50**, rwd `65021b6d…`. 🛑 **The V80 distinction**: V80's catastrophe was FactorC **FLAT 566 across ALL FOUR knots**; V157 flattens **only the FIRST segment** and leaves **`Y[1..3]` byte-identical**, so **the high-speed ramp is untouched** — asserted in the builder. ⊕ **FLY ONE OF V156/V157**; **V157 is the better first flight** given the operator wants best results rather than a ladder. ✅ **HOUSEKEEPING**: `MEMORY-PART4.md` split at 199.5 KB → **PART4 100.7 KB / 88 entries + PART5 99.3 KB / 59 entries**, entry sets equal, **zero lost**; **`CLAUDE.md` repointed “PAGINATED IN FOUR” → “IN FIVE”** naming `MEMORY-PART5.md`.

- ✅✅★★★★★ [**CONSOLIDATED HANDOFF — eleven verified unflown builds, two symptoms, one place to act from**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/handoffs/2026-08/HANDOFF-2026-08-28-eleven-builds-two-symptoms.md) — **the analysis side is exhausted; every remaining question needs a drive.** All eleven rebuilt 2026-08-28 **bit-identically, all assertions passing** (V157 62/62 · V156 60/60 · V153/V152 54/54 · V149 52/52 · V139 49/49 · V155/V154 58/58 · V150 51/51 · V148 69/69 · V151 53/53), all on **base V122**, all `.rwd` present on disk. 🛑 **Carries the TWO-SYMPTOM split** (A = mechanical resonance, firmware reaches excitation/phase only; B = broadband, above every CAN Nyquist, **closed as unreachable by calibration**) ⇒ **all eleven target A**. ⭐ **V157 first** — damping is the textbook fix for an underdamped mode and it is the first build with a live damper in the micro regime; adds damping **only below 12.73 °/s** so max angular velocity/acceleration are untouched; **24 % of the 512 ceiling**, which itself must never be raised (it is the `0xC407E` fault interlock). ⊕ **PAIR RULE**: V157/V156, V153/V152, V155/V154 are one lever at two doses — **fly one of each**. ⊕ **STANDING ANSWERS not to re-derive**: 8× gain is a **measured NO** (6× 1.13 dB → 8× 2.24 dB); `0xC4936` never; the 512 ceiling never; **Ghidra's `code.bin` is STOCK**; tp-relative counts need the **`reg1 == tp`** filter. ⊕ **Judge by EAR and capture AUDIO** — justified by the matched contrast **1.12× [1.01, 1.27]** with a **passing positive control** ⇒ engagement adds **≤ ~2 % of RMS** ⇒ **no CAN statistic can rank these builds.**

- ⭐⭐⭐★★★★★ [**The LKAS AUTHORITY limiter is located — `0xC61BC`/`0xC61BE`, VIRGIN on all 157 builds**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — in `FUN_00028ea6`, **upstream of the 6× gain**: `uVar33 = (iVar31 × LERP) >> 8` then a **symmetric ±clamp to `cal(0xC61BC)` = 15360** (7 readers; `0xC61BE` is its twin, 8 readers). 🛑🛑 **THE KIT HAS BEEN MOVING THE WRONG CLAMPS**: `0xC61BC`/`0xC61BE` read **{15360: 157} — VIRGIN across every build**, while `0xC61B2`/`0xC61B4` have been set to **five different values** (3072/4096/2048/1024/512) across 157 images — and the record itself files that pair as **“INERT, 0 % of the effect”** precisely *because* the **“setpoint is LERP-clipped to 15360 upstream of the gain”** ⇒ **[EVIDENCE] the moved cells are the known-inert ones and the named upstream limit was never touched.** ⚠ **[UNRESOLVED] whether ±15360 BINDS** — it clamps a *product*, and *“81.5 % of rail”* is consistent with either a saturating setpoint or a LERP whose own max is 15360 (making the clamp redundant); **no probe has measured its duty**, and raising an inert clamp does nothing. 🛑🛑 **NOT BUILT — this is a SAFETY decision, not an engineering one: raising an authority clamp INCREASES the maximum torque LKAS can apply against the driver**, categorically unlike every other queued lever, which only ever *reduce*. **Needs the operator's explicit direction, and a probe first.** ✅ **SAFE NEXT ARTIFACT**: a cave rung reading **`|uVar33| ≥ cal(0xC61BC)`** — zero cal bytes, cave only, an instrument not a fix (V148/V98/V100 class) ⇒ **duty 0.0000 closes the cell; duty > 0 makes the dose meaningful and lets the operator decide knowingly.** ⊕ **The first genuinely NEW question in several turns, and it serves the operator's SECOND stated complaint rather than the first.**

- ✅★★★★ [**The authority clamp now has a concrete threshold — but the static shortcut FAILED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — tried to settle *“does ±15360 bind?”* **without** spending a cave edit (the bricking class). ✅ **ESTABLISHED**: the setpoint LERP is reached via a **pointer table at `0xCB994`** (`X` at `+2..+10`, `Y` at `+0xC..+0x14`); all six records decode to **Ymax 696–717** ⇒ with `uVar33 = (iVar31 × LERP) >> 8` clamped to 15360, **the clamp binds ⇺ `iVar31 ≥ 15360×256/717 = 5482`** — a single concrete threshold, and a **cheaper probe than instrumenting the clamp** (one comparison against a constant). 🛑 **DID NOT CLOSE STATICALLY**: `iVar31` is **not one semantic variable** — the decompiler reuses it across **16 assignments**; the one dominating the clamp is `iVar31 = ±uVar25` then `iVar31 = iVar31*0x20 - uVar35`, so bounding it needs `uVar25` **and** `uVar35` ⇒ **[UNRESOLVED] and NOT guessed** — a wrong static bound is exactly the kit's most expensive error class (a mis-read that reads as fact and propagates). ✅ **The probe remains the instrument**, but is now **simpler**: one rung `iVar31 ≥ 5482` ⇒ **duty 0.0000 closes `0xC61BC` exactly as `0xC61B2`/`B4` closed; duty > 0 makes the dose real.** 🛑 **Still not a fix, and still needs the operator's call before any dose** — raising an authority clamp **increases the max torque LKAS can apply against the driver**, unlike every other queued lever.

- 🛑🛑🛑★★★★★ [**The LKAS command RAILS at ±4096 — “peak command oscillation” is AUTHORITY SATURATION**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — first measured answer to the operator's THIRD complaint, from **cached data, no drive, no build**. `co_tqcan` (the command **as sent on the bus**), engaged frames, **114 routes / 1.37 M frames**: **overall rail duty 0.783 %**, **32 of 114 routes (28 %) EVER rail**, worst **8.633 % (r85s15)**, and the **LONGEST CONTINUOUS RAIL is 399 frames ≈ 4 SECONDS** (r9e), with `|max|` **exactly 4096** on every railing route. ⇒ **±4096 = 2^12, a 13-bit signed field — the CAN signal's own PROTOCOL MAXIMUM** ⇒ **[EVIDENCE] the command is pinned at the largest value the wire can carry, up to 4 s at a time** ⇒ **that IS peak command oscillation, and it is authority saturation.** 🛑🛑 **THE THREE COMPLAINTS SHARE ONE KNOB**: the field cannot carry more than ±4096, it is already railed, so more authority needs more **torque per count** = the **gain `0xC6CD0`** — but that is the grinding knob (**measured 6× = 1.13 dB, 8× = 2.24 dB**, vibration ∝ m^1.74 vs authority ∝ m^0.88) ⇒ **authority and grinding are in DIRECT tension through one cell**, which is why authority has been stuck the whole arc. **No openpilot fix exists — forbidden, and the field cannot represent a larger number anyway.** ⭐⭐ **THE ONE ESCAPE**: if `0xC61BC`/`0xC61BE` = ±15360 **binds**, raising it gives **more torque per command count ⇒ authority WITHOUT touching the gain ⇒ without the grinding penalty** — the only path off the tension this firmware offers, settled in one drive by the **`iVar31 ≥ 5482`** rung. ⇒ **the probe's value has gone UP: it is now the sole test of whether the authority/grinding tension can be broken at all.** ⚠ binding still **UNRESOLVED** and not statically guessable (`iVar31` is a 16-assignment temporary); 🛑 raising it stays a **safety decision for the operator**.

- ⚠★★★ [**The railed command is SUSTAINED ONE-SIDED saturation, not a rail-to-rail limit cycle**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — follow-up to the ±4096 finding. **Only 4 of 114 routes ever swing rail-to-rail**, yielding **12 intervals total** with a **4.7× spread** in implied frequency (r78 0.401 Hz · r85 0.086 · r96/r96s11 0.272) ⇒ **[NOT CLAIMED] a limit-cycle frequency** — 12 intervals across four disagreeing routes is not a measurement, and quoting “0.27 Hz” would be the kind of number this kit has had to retract. ✅ **WHAT IT DOES ESTABLISH**: the command **rails on ONE side and STAYS there**, up to **399 frames ≈ 4 s** continuous, rather than alternating ⇒ **“peak command oscillation” is SUSTAINED ONE-SIDED AUTHORITY SATURATION, not a controller limit cycle** ⇒ strengthens the authority diagnosis (openpilot holds the field's maximum because the plant under-delivers per count) and ⇒ **no oscillation-damping lever applies — there is no cycle to damp**; the fix is torque-per-count, i.e. the **gain (frozen)** or **`0xC61BC` (binding unknown)**.

- ⚠★★ [**The railed-command natural experiment is UNDERPOWERED — do not re-run**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a rail episode freezes the command at ±4096, so it should test whether the ratchet is command-driven. Ran it: **only 3 routes qualify with 3–9 railed windows each**, ratios spanning **1.73 / 2.21 / 19.05** ⇒ **[NOT CLAIMED]**. 🛑 **And the statistic is COMPOSITIONAL** — band *share* is normalised to 1–45 Hz, so 6–9 Hz rising **forces** the 26–31 Hz “control” down (0.31/0.32/0.04) arithmetically, so it is **not independent evidence** — exactly the trap [[feedback-run-the-control-before-the-measurement]] names. ⊕ Directionally all three exceed 1 with the command frozen, consistent with the ratchet **not** being command-driven — but that is **already settled by V87** (7.8 Hz line prominence **12.9 in the COLUMN vs 4.0 = chance in the COMMAND**), so the experiment **adds nothing.** ⇒ rail episodes are **0.78 % of engaged frames** on **28 % of routes**; closing it would need a drive deliberately sustaining saturation, to re-confirm a settled point. **Low value — recorded so it is not attempted again.**

- 🛑🛑🛑★★★★★ [**ALL THREE complaints are CREEP phenomena — and speed-scheduling the gain is DEAD**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — tried to **make** a lever: schedule the gain by speed, high where authority saturates and low where grinding lives, which would break the tension outright. **It requires them to live at different speeds. They do not.** Rail duty by speed, 1.6 M engaged frames pooled: **0–2 km/h 0.389 % · 2–8 km/h 6.367 % (THE PEAK) · 8–16 km/h 0.875 % · 16–25 km/h 0.169 % · 25–40 km/h 0.009 %** ⇒ **CREEP (0–8) 3.540 % vs HIGHWAY (≥16) 0.101 % = 35×** ⇒ **[EVIDENCE] authority saturation is a CREEP phenomenon** ⇒ **🛑 speed-scheduling the gain is DEAD — there is no band where authority is needed and grinding is absent; lever class closed before a build was spent on it.** ⭐ **BUT IT UNIFIES THE THREE COMPLAINTS**: peak command oscillation (rails 6.4 % at 2–8 km/h), LKAS authority (saturated in that same band), and grinding (symptom A's micro regime 1–13 °/s and symptom B's <10 mph acoustic excess) are **all the SAME OPERATING POINT — engaged creep, ~2–8 km/h.** Treated as three problems for the whole arc; they are **three observations of one regime**, and any real fix must act **at creep**. ✅ **SHARPENS THE FLIGHT ORDER**: **V157/V156 act ONLY at creep** (FactorC opens below 35 km/h **and** FactorE below 12.73 °/s ⇒ the damper is non-zero exactly in the 2–8 km/h band), while **V153/V152, V149, V150, V139, V155/V154 are not speed-gated** and spend most of their effect outside it ⇒ **V157 is the ONLY queued build confined to the band where all three symptoms live — now an argument from MEASUREMENT, not mechanism.**

- 🛑🛑★★★★★ [**The creep-authority chain is CLOSED — lockout already pulled, next conjunct not a cal**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — all three complaints live at **2–8 km/h**, so I followed the **low-speed steer lockout** to the end. **LINK 1**: `0xC62EA` is **0 on V122** (across 157 images **{0: 108, 320: 49}**) ⇒ **the lockout has been OFF for most of the arc**, so the 6.4 % railing at 2–8 km/h is **not** caused by it — **lever spent, do not re-propose.** **LINK 2**: that activates the memory's own pre-registered suspect ([[accord-low-speed-lockout-window-c62ea]]: *“if a lowered `0xC62EA` doesn't work, `gp-0x69aa` is the next suspect”*). Read at its site (region **byte-identical stock vs V122**): `ld.hu -0x69aa,gp,r14` · **`ori 0x8000,r0,r9`** · `cmp r9,r14` · `bh` → failure ⇒ **the 0x8000 threshold is a HARD-CODED IMMEDIATE, NOT `cal(0xC63F2)`** (which is read at a different site, `0x28ECE`) ⇒ **[EVIDENCE] the governor-derate conjunct is NOT reachable by calibration — the pre-registered suspect is closed as a cal lever.** ⚠ **CORRECTION 1**: the memory records `gp-0x69aa == 0x8000`; the instruction is `bh` (unsigned HIGHER) to the failure path ⇒ **the passing condition is `<= 0x8000`, not `==`**. ⚠ **CORRECTION 2**: I first wrote `tp+0x73F2` as `0xC73F2` — **the SIXTH recurrence of the off-by-0x1000 trap** `CLAUDE.md` lists five of — caught by **anchoring against the memory's stated 32768** (wrong address read 14, right one 32768). ✅ **WHAT REMAINS**: of the AND-chain gating control-active at creep, `0xC62EA` is spent, `gp-0x69aa` is an immediate, `gp-0x67fe` is a state, and `gp-0x69ae` plus a 5-channel validity test are unexamined ⇒ **the ONLY cal-reachable candidate left is `0xC61BC`**, exactly what the **`iVar31 ≥ 5482`** probe settles ⇒ **the probe is now the last cal-reachable question in the whole creep-authority chain.**

- ✅✅✅★★★★★ [**Control is FULLY ACTIVE where the command rails — the creep-authority chain is CLOSED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the memory's *“0 % control-active below 2 mph”* was measured with `0xC62EA` = 320; it is **0 on current builds**, so I redid it: **227 routes, 1.88 M engaged frames**, `STEER_STATUS` = **{0: 1,901,564 | 3: 14,538 | 4: 92}**. **ST=3 duty by speed: 0–2 km/h 5.868 % · 2–8 km/h 0.000 % · 8–16 0.000 % · 16–25 0.000 % · 25–40 0.000 %** ⇒ **[EVIDENCE] STEER_STATUS=3 is EXACTLY ZERO at 2–8 km/h**, the band where the command rails 6.4 % ⇒ **the lockout removal WORKED and the old “0 % below 2 mph / 88 % at 3–4 mph” figures are OBSOLETE for every build since.** ⭐⭐ **WHAT IT SETTLES**: lockout already 0 ⇒ not gating; ST=3 zero at 2–8 km/h ⇒ **control FULLY ACTIVE**; the command rails anyway ⇒ **the demand genuinely exceeds the 13-bit field** ⇒ **[EVIDENCE] the creep authority shortfall is NOT a dropout, lockout or gating failure** ⇒ **the ONLY remaining explanation is TORQUE PER COUNT** — the gain `0xC6CD0` (frozen both ways) or the virgin ±15360 clamp `0xC61BC` (binding unknown). **Every other hypothesis for creep authority loss is eliminated by measurement.** ⊕ **ST=3 survives only at 0–2 km/h** (standstill, governed by the `gp-0x68b3` bypass) — expected, not a complaint regime. ⊕ **ST=4 occurs 92 times in 1.88 M frames (0.005 %)** ⇒ **V42's state-4 governor ratchet fix is holding across the whole corpus**, independently confirming [[reference-accord-state4-governor-ratchet]] and that [[accord-v42-ratchet-fix-lost-since-v53]] is restored. ⇒ **the `0xC61BC` probe is the last standing question about creep authority.**

- 🛑★★★★ [**“Grinding continues after disengaging” — NO band-specific persistence on the bus**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the operator's V133 report claimed the grinding **outlasts disengagement**, which is structural (the command is gone ⇒ the mechanism would need **memory**) and had never been tested. Tested on **139 creep-ish disengage events across 76 routes**, aligning on the engaged→manual edge and tracking the 6–9 Hz Hilbert envelope of `tq` against its own engaged baseline. ⚠ **UNCONTROLLED IT LOOKED REAL**: 1.293 / 1.212 / 1.225 / 1.119 over the first 5 s ⇒ *“~25 % elevated for 5 s”*. ✅ **THE CONTROLS KILL IT**: 6–9 Hz **1.377 [0.918, 1.588]** (CI includes 1), 26–31 Hz **1.051 [0.948, 1.288]**, 32–38 Hz **1.117 [1.025, 1.249]**, and the band-specific **RATIO 6–9/26–31 = 0.911 [0.795, 1.132]** ⇒ **[EVIDENCE, with controls] NO band-specific persistence** — the elevation is in the control bands too, so it is **the driver taking over**, not ratchet memory. ⭐ **THE DURABLE POINT**: the same 139 events read as *“persists 25 %”* or *“no effect”* depending **only** on whether the control band is computed — [[feedback-run-the-control-before-the-measurement]] on a fresh question, and the uncontrolled number was **plausible, specific, and pointed at a mechanism that does not exist**. 🛑 **DOES NOT refute the operator**: either (a) the persistence is not a 6–9 Hz column phenomenon, or (b) **it is real but NOT OBSERVABLE ON THIS BUS** — and **(b) fits everything else** (motor/rack-side mode, engagement adds ≤~2 % of RMS) ⇒ **the ear remains the only instrument; the null is as much about the instrument as the symptom.**

- ⚠★★★ [**Audio envelope demodulation for the ratchet — NEW method, no signal, imperfect control**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the kit uses audio only for symptom B's **band power**, but **a ratchet is an impulse train**, so it would show as **AM of the audio ENVELOPE at 7.8 Hz**, never tried. ✅ **The instrument already exists**: caches store per-band envelopes at **62.5 Hz** (`wide` x 10 with `wide_lab`; the 100 Hz `a20_100...` format is only on r81/r82) => Nyquist 31.25 Hz, control moved to **20–28 Hz**; **5 routes carry creep-engaged audio** (r9e 12,952 · r96 9,803 · r97 5,495 · r85 5,238 · ra4 4,312). ⚠ **RESULT: every 6–9 Hz CI spans 1** — 15–21 0.971 · **28–40 1.309 [0.978, 1.627]** · 40–60 1.110 · 60–100 1.134 · 100–300 1.020 · 300–1000 0.969 · 1k–3k 1.031 · 3k–8k 1.030 => **no detectable ratchet AM.** 🛑 **BUT the 60–100 Hz CONTROL reads 0.804 [0.626, 0.923], EXCLUDING 1** — windows were matched on **creep speed only, not steering activity** => **[NOT A CLEAN NULL]**, underpowered at 5 routes with an imperfect control, and **must not be cited as evidence the ratchet is acoustically silent.** ⭐ **KEEP THE METHOD** — envelope AM demodulation is a legitimate, previously-unused instrument for an impulse-train symptom, and the most suggestive cell is the **28–40 Hz band at 1.309**, where a mechanical ratchet's carrier would plausibly sit. => **to close it: more routes with audio AND matched creep engaged/manual exposure, stratified on activity as well as speed — only 5 of ~230 routes have usable creep-engaged audio** => **capture audio on every future drive.**

- ✅✅★★★★★ [**The audio AM null is now CLEAN — and that is the THIRD artefact killed by a control**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — last turn's envelope-AM test had a **failing 60–100 Hz control** (0.804) because windows were matched on **creep speed only**. Re-ran with **(speed bin x |rate| RMS bin)** stratification: **ALL EIGHT control bands now span 1**, and **every 6–9 Hz AM estimate collapses to ~1** — 15–21 1.020 · **28–40 0.958 (was 1.309)** · 40–60 1.048 · 60–100 0.869 · 100–300 1.013 · 300–1000 1.004 · 1k–3k 1.038 · 3k–8k 1.058 ⇒ **[EVIDENCE, clean controls] NO engagement-conditional 6–9 Hz AM in the audio, in any band**; the previously “suggestive” 28–40 Hz cell was **pure matching artefact.** ⚠ 60–100 Hz excludes 1 *below* (0.869) but with 8 bands tested that is expected by chance — **not claimed.** ⇒ **both available instruments now return clean nulls on symptom A** (CAN bound ≤ ~2 % of RMS; audio AM ~1.0), consistent with the mode being **motor/rack-side and unobservable**. 🛑🛑 **THE PATTERN — three times in one session the control killed the effect**: engagement contrast **2.8× → 1.12 [1.01, 1.27]** (matched); post-disengage persistence **1.29× → ratio 0.911** (control band added); audio AM **1.309 → 0.958** (activity-matched). **Each was plausible, specific, and pointed at a real mechanism; none survived its control.** ⭐ **RULE: on this kit an uncontrolled engaged-vs-manual ratio is worth nothing** — the arms differ systematically in operating point (engaged = creep and steady, manual = faster and more active) and **that difference is larger than every effect measured this session**. **Compute the control first, or do not compute the number.** See [[feedback-run-the-control-before-the-measurement]].

- ✅✅★★★★ [**The ratchet is NOT episodic in band share — the tail looks like the median**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a flaw in my own method: every matched analysis took the **MEDIAN**, and the kit's characterisation says the ratchet hit **44 of 46 windows** on one route, so an **episodic** symptom would be washed out. Re-ran the matched contrast at the **TAIL**, 23 routes, stratified on (speed x |rate| RMS): **median 1.067 [0.966,1.226] · p75 1.073 · p90 1.160 [0.993,1.256] · p95 1.100 [1.039,1.243] · p99 1.110**, controls 0.987 / 1.082 / 1.022 / 1.065 / 1.024 ⇒ **[EVIDENCE] the tail is indistinguishable from the median** (1.10 vs 1.07) ⇒ **the engagement effect is a UNIFORM ~10 % elevation, NOT episodic concentration.** The kit's “44/46 windows” figure was about detecting a **LINE**, a different statistic from band share — **no conflict.** ✅ **It also FIRMS THE EFFECT UP**: at **p95 the 6-9 Hz CI EXCLUDES 1** while its control spans 1, converging with the independent median estimate **1.12 [1.01, 1.27]** ⇒ **engagement adds ~10 % to the 6-9 Hz band, robust across central AND tail statistics, controls clean at every percentile.** ⊕ **Closes the last live objection to the ≤ ~2 % of RMS line bound** — the natural challenge was *“an episodic symptom would be diluted”*; **it would not be, because the tail behaves like the centre.**

- ✅✅✅★★★★★ [**FIVE independent metrics converge on ~1.1x — the measurement side is EXHAUSTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the last objection was that **band power is the wrong perceptual quantity** (grinding = roughness = **modulation DEPTH**, and 7.8 Hz is in the fluctuation-strength range; depth normalises to mean level, share to the envelope spectrum). Tested: **depth 6-9 Hz 1.161 [1.049, 1.284]** (excludes 1) vs **depth 26-31 CONTROL 1.086 [0.912, 1.292]** (clean) vs **share 6-9 1.106 [0.998, 1.273]** => depth is marginally better but its **band-specific advantage over its own control is only 1.07x** => **[EVIDENCE] the perceptual-metric hypothesis does NOT explain the gap.** ⭐⭐ **CONVERGENCE**: CAN band share median **1.12 [1.01,1.27]** · CAN band share p95 **1.10 [1.04,1.24]** · CAN line prominence **1.17** (=> **<= ~2 % of RMS**, positive control PASSED) · CAN modulation depth **1.16 [1.05,1.28]** · AUDIO envelope AM **~1.00 across all 8 bands** — **five statistics, two instruments, all between 1.0 and 1.2 with clean controls.** 🛑 **[CONCLUDED] the measurement side is EXHAUSTED** — not *“we haven't found the right statistic”*: **five were tried, including the perceptually correct one, and they agree.** => the only consistent reading is the structural one — **the mode is motor/rack/tyre side and no channel this vehicle exposes observes it**; a symptom reading ~1.1x on every instrument while unmistakable to the driver is **an observability limit, not a statistics problem.** => **the ear is the instrument, now supported by five converging negatives rather than resignation. No further metric should be attempted on cached data.**

- ✅✅✅★★★★★ [**The damper is a FOUR-factor cascade, not two — and V157 is VALIDATED by it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the kit models the base damper as `ch0 = FactorC(speed) x FactorE(rate) >> 10`; its actual writer **`FUN_00034350`** computes `((((clamp(gp-0x698a,1024) * L1 >>10) * L2 >>10) * L3 >>10) * L4 >>10)` with **L1 = LERP [0xC9CCC] on torque · L2 = LERP[0xC9E9C] on `gp-0x6a5e` SPEED · L3 = LERP[0xC9DB4] on `gp-0x6a10` ANGLE · L4 = LERP[0xC9F84] on `gp-0x6ac0` MOTOR RATE**, then a **sign flip from `gp-0x6abe`** and a **ceiling LERP[`PTR_DAT_000c77a0`] on `gp-0x6ac2`**. ✅ **THE CHECK**: **L1 and L3 are FLAT UNITY [1024,1024,1024,1024] at every knot** ⇒ they can never zero the product; **only L2 [0,234,429,908] and L4 [0,140,539,927] have Y[0]=0** ⇒ **V157 opens EXACTLY the two that gate**, and the full four-factor arithmetic `((((1024*1024)>>10)*234>>10)*1024>>10)*539>>10 = 123` **reproduces the builder's asserted dose exactly** ⇒ **V157 is correctly and completely targeted**; the two-factor model gave the right answer **by luck**, because the omitted factors are unity. ⚠ **CORRECTIONS**: “FactorC” is **L2 (speed, via `0xC9E9C`)** and “FactorE” is **L4 (motor rate, via `0xC9F84`)** — two of four cascaded LERPs, not a bare pair; record layout is **`X[0]` at base+2, `Y[0]` at base+10** (L3 uses +0xC/+0x14), and **I first compared pointer-table BASES against V157's X[0] addresses and got “NOT FOUND” on all four — a 2-byte off-by-one that would have condemned a correct build**, caught by re-deriving the layout from the decompile. ⭐ **ADDS TO V157'S CASE**: the two factors it opens are the **only** gating ones, so **nothing else in the cascade can silently zero it** — the failure mode that made V134 inert **cannot recur**; and the branch conditions (**L4: `gp-0x6ac0 < 0x32C9` and `|gp-0x6abe| <= 13000`; L2: `gp-0x6a5e <= 0x7D00` and `gp-0x67f4 == 1`**) are all satisfied at creep.

- ✅★★★★ [**The damper ceiling read from its own records — V157's 24 % confirmed, 3.7x headroom left**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V134 cited the ceiling as `Y[512,1024]` without reading it per mode. Read: **m25/m26/m27 all X=[300,800] on `gp-0x6ac2`, Y=[512,1024]**, fallback **cal(`0xC6158`) = 512** when `gp-0x6ac2 >= 0x32C9` ⇒ **at creep the operative ceiling is 512** ⇒ **[EVIDENCE] V157's product of 123 is 24.0 % of it**, confirming the builder from the records rather than a citation, with **max safe product at 90 % = 460, i.e. 3.7x headroom.** ⭐ **PRE-COMPUTED NEXT DOSE** (so a good result is not followed by guesswork): **L2_Y0 → own Y[2] (429/426) and L4_Y0 → own Y[2] (539) ⇒ product 225 = 44 %, margin 2.3x** — both still the tables' **own knots**. 🛑 **NOT BUILT deliberately**: two doses of this lever are queued and **neither has flown**; a third artifact adds nothing a recipe does not. ✅ **THE DAMPER IS NOW FULLY CHARACTERISED** — product, the four LERPs (**L1/L3 unity, L2 [0,234,429,908], L4 [0,140,539,927]**), **sign from `gp-0x6abe`**, ceiling LERP + fallback, and both gate conditions (**L2: `gp-0x6a5e <= 0x7D00` and `gp-0x67f4 == 1`; L4: `gp-0x6ac0 < 0x32C9` and `|gp-0x6abe| <= 13000`**) — **all read from the image, and the only two zero knots are the two V157 opens.**

- ✅✅✅★★★★★ [**V158 — the GOLDEN MODEL's own damper prescription; it SUPERSEDES V156/V157**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **PROCESS FAILURE CORRECTED**: V156/V157 were designed from BUILD-LINEAGE and V134's docstring (a **two-factor** damper model); the **golden model** already had the **five-factor** structure **and a measured prescription**, so last turn's “four-factor discovery” was a **re-derivation**. 🛑 **V157 IS WRONG**: it sets FactorE `Y[0]=539` against `Y[1]=140` ⇒ **Y=[539,140,539,927] NON-MONOTONE**, which **flattens the rate factor across the symptom's own range — V72's flatten-to-relay error**, which the golden model explicitly warns against. ✅ **V158 follows the model**: `FactorC Y[0] := own Y[2]` (429/426) + `FactorE X[0] 60→12` + `FactorE Y[1] := own Y[2]` (539) ⇒ **Y=[0,539,539,927] MONOTONE**, `Y[0]` stays 0 and the dead zone is opened by **moving the AXIS**. **Priced at the model's MEASURED operating point `gp-0x6ac0`=99** (on FactorE's first rising segment, not flat at Y[0]): FactorE=120, FactorC=429 ⇒ **product 50, inside the model's own requirement ~43 [30,60]**, and **9.8 % of the 512 creep ceiling (10.2× margin)** ⇒ **V157's 123 is 2.4× the requirement, V156's 31 is below it — only V158 is inside.** **10 payload bytes, 67/67, CRC 50/50**, rwd `511c4a71…`. ⚠ **My own gate caught a subtlety**: `FactorC Y[0]:=Y[2]` is **also non-monotone** ([429,234,429,908], a dip at 35–60 km/h) — **accepted deliberately and asserted as such**, because FactorC is a **speed schedule** not the damping law, and the monotone alternative (234) gives **product 28, below the requirement**. 🛑 **LESSON: for any damper or lane work read `eps_chain_lanes.py` FIRST** — it had the structure, the measured operating point, the priced requirement and the shape rule all along.

- ⭐⭐⭐★★★★★ [**The AUTHORITY complaint has a specific mechanism — the operator drives on the collapse curve's KNEE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — auditing the queue against the golden model (the V158 lesson) surfaced an answer **already in the record**: the **LKAS authority COLLAPSE CURVE** (`0xE547C`/`0xE5404`/`0xE52FC`/`0xE5284`, mode 7) takes authority **254 -> 0 across raw driver torque 2240 -> 2560**, is **VIRGIN on all 90 images**, and **the operator's measured median override torque is 2235 — ONE COUNT below the first knot** => **[EVIDENCE] he drives on the knee**, so any small increase in his input collapses authority toward 0. ⊕ **A SECOND, DISTINCT mechanism from the +-4096 command rail**: the rail is the command hitting its **13-bit protocol max** (6.4 % of frames at 2-8 km/h); the curve is authority being **cut by the firmware** above driver torque 2240. 🛑 **THE RULE THAT BLOCKS THE FIX**: the golden model requires any change to be **MONOTONE-NON-INCREASING — never more authority than stock at any torque** — so **moving the knee up violates it**, and the rule exists because **raising the curve makes the car fight the driver harder during override.** **[DECISION] NOT proposed — like `0xC61BC`, an operator decision about how hard the car may resist him.** ⚠ **It will not help grinding**: filed as **“NOT a 6-9 Hz lever — refuted five ways; it drives the ~0.5-1 Hz SURGE.”** ☑ **RE-CONFIRMED `0xC64B8` IS DEAD** — at mode 7 both arms deliver 0 everywhere the branch could fire (all four records clamp to `Y[last]=0` above `X[last]`=80/112, below the gate's 113) => **stock and V37 are bit-identical on this car; do not re-propose it.** ✅ **All three complaints now have named mechanisms**, and two are blocked **by deliberate safety constraints rather than ignorance.**

- ⛔⛔ **CORRECTION TO THE ENTRY BELOW — THE THREE-TAP STRUCTURE IS A PURE GAIN OF 1.0, NOT 10.0.** `FUN_0003b8f6` reads `fVar14 = *(float*)(tp+0x5048)` = **1.0f**, and only LATER reuses the same variable for a **clamp constant `fVar14 = 10.0`**. The earlier read grabbed the clamp, not the coefficient. So `fVar14 = 0.0*hist + 1.0*fVar19 + fVar15*0.0` = **IDENTITY PASSTHROUGH** — 2 zeros, 0 poles, both memory taps x0.0, **it cannot ring**. ⚠ **A 10x error in a loop gain**: it matters because the Path-2 stability bound on the RAM-LERP slope divides by `G_gov*G_obs`. The coefficient VALUES in the entry below are all correct; only the 10.0 is wrong. ⊕ Also note only THREE of the eight are floats (`tp+0x5048/504C/5050`); the other five are `ushort` reads scaled in code, which is why a float32 read of them returns denormals.

- 🛑🛑★★★★★ [**V154/V155 SUPERSEDED — `0xC63A6` is a STRUCK cell; and the eight Path-2 coefficients are READ**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `eps_chain_lanes.py`: **“NO WEIGHT MAY BE MOVED”** because `gp-0x6b70` is a **PID reference that gets SUBTRACTED**, and **“Path 2 IS A REAL CLOSED LOOP... GATE 2 CANNOT BE CERTIFIED. 0xC63A6 was struck on exactly this.”** ⊕ my amplitude justification was **right open-loop** (the model: the two `sign(iVar6)` factors **square to +1 and cancel**) but **I reasoned open-loop about a CLOSED loop** — what GATE 2 exists to catch => **V154/V155 superseded.** ✅ **THE EIGHT COEFFICIENTS, byte-read for the first time**: `0xC404C` **0.0** · `0xC4050` **0.0** · `0xC40BC` 3000 · `0xC40D0` 408 · `0xC40D2` 1020 · `0xC40D4` 573 · `0xC40D6` 246 · `0xC40D8` 3686 => **two are EXACTLY 0.0 in stock and V122, and they multiply the HISTORY terms**, so `fVar14 = 0.0*gp-0xc9c8 + 10.0*fVar19 + fVar15*0.0 = 10.0*fVar19` => **that three-tap structure is a PURE GAIN of 10.0, both memory taps DEAD.** GATE 2 is **not closed** (the RAM LERP slope is still unextracted) but **the loop gain has two fewer live terms than assumed.** ⭐ **AUDIT PATTERN**: V156/V157 mis-shaped, V154/V155 on a struck cell, **V158 correct** — **three recommendations built from BUILD-LINEAGE instead of the golden model, two of them wrong; the cost of skipping required reading was four superseded artifacts.**

- 🛑🛑🛑★★★★★ [**V149 SUPERSEDED — it REMOVES Lever B, the kit's only measured grinding fix**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **my premise was wrong**: V149 was built to remove a *“5.12x switch driven by the fault counter `gp-0x671d` at task rate”*, but `0x3AA96` is **`fb` on V122**, i.e. `ld.bu -0x6806[gp]` — **the gate is LKAS CONTROL ACTIVE, not a counter**, so it toggles on engage/disengage and **there is no task-rate switching nonlinearity to remove.** 🛑🛑 **WORSE: 5244 IS THE FIX.** The golden model: *“THE FIX: V67 = V66 + the grind #1 fix GATED ON LKAS. `0x3AA96 c5->fb` + `0xC6446 512->5244`… the firmware already HAS a conditional-gain arm and it is merely wired to a dead cell… gate TRUE (LKAS on) -> flat 5244 = **2.00x the LERP at grind #1's operating point**”*, and **V88 = “Lever B restored” is the build that FLEW with “grinding FIXED”** ([[accord-v88-flew-grinding-fixed-command-intact]]; [[accord-v81-carries-neither-grind1-fix]] calls it **“best in kit”**) ⇒ **[EVIDENCE] Lever B is ACTIVE on the flying build and V149 collapses it ⇒ V149 REMOVES THE ONLY CHANGE THIS KIT HAS EVER MEASURED AS FIXING GRINDING. SUPERSEDED — never fly it.** ⚠ **V152/V153 carry the SAME open GATE 2**: `tp+0x50d0` = `0xC40D0` is **one of the eight uncertifiable Path-2 loop-gain coefficients**. Their argument is **better** than V154/V155's (a pure low-pass lowers HF loop gain, vs an unresolved sign) but **a low-pass also adds phase lag and margin cannot be checked without the RAM LERP slope** ⇒ **NOT superseded, but DEMOTED and flagged GATE-2-OPEN; do not fly ahead of V158.** 🛑 **FIVE builds superseded this session (V149/V154/V155/V156/V157), all one root cause: designed from BUILD-LINEAGE and my own decompiles instead of the GOLDEN MODEL**, which already held the structure, the prescription, the strikes and the fix.

- 🛑🛑★★★★★ [**V139 SUPERSEDED — and the `sar` pair is CLOSED at 6x gain, BOTH directions**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0x3AB76` and `0x3AC20` are **imm5 = 10 (stock) on V122**, so V62's sar pair — *“the kit's ONLY measured grind-#1 fix”*, 18-22 Hz down 8-42x — **is absent from the flying build**, and the obvious move is to restore it. **It is not the right move.** 🛑 **BOTH DIRECTIONS ARE WRONG ON A 6x BASE**: **sar 9** (double) is V62's fix **but was measured on a 4x-GAIN base**, and **V133 flew sar 9 at 6x => “massive, violent grinding”**; **sar 11** (halve) is V139, and memory records **“r24 HALF CAUSED grind #2”**. ⊕ **V139's OWN builder holds the decisive argument** I should have followed instead of promoting it: *“scaling V62's 4x optimum to V122's 6x base lands BETWEEN sar 9 and sar 10, which argues Honda sar 10 is already about right for a 6x base and sar 11 OVERSHOOTS.”* ⇒ **stock sar 10 is ~optimal at 6x; the sar pair is CLOSED as a lever. V139 superseded, and do NOT restore Lever A either.** ⭐ **GENERAL LESSON: the sar optimum is COUPLED TO THE GAIN** — V62's result came at **4x**, the car runs **6x**, and **a measured optimum does not transfer across a gain change** ⇒ **[RULE] before restoring ANY historical fix, check the gain base it was measured on**; this applies to every pre-V101 result, since `0xC6CD0` moved 3564->5346 at V101. 🛑 **SIX builds superseded this session** (V139/V149/V154/V155/V156/V157), all from **designing off BUILD-LINEAGE instead of the golden model** — **V158 is the one built from the model, and the one that survives.**

- ✅★★★★★ [**The audit is COMPLETE — V150 is inert hands-off, and the queue is final**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V150's structure is **right** (the model confirms the zero-force gate is **`gp-0x6b5e != 0` AND `assist_state_671a < cal 0xC64FA`**, and V150 makes the state term always true so the gate reduces to `gp-0x6b5e != 0`) — **but the model also says the gate does not fire where the symptom is**: *“Hands-off the margin sits ~24x above the kill threshold => THE GATE LEAVES r26 LIVE in ordinary driving and most strongly live in hands-off creep — exactly where the grinds and the ratchet occur.”* ⇒ hands-off `gp-0x6b5e == 0`, so V150's reduced condition is **already satisfied** ⇒ **[EVIDENCE] V150 changes behaviour ONLY under driver torque ⇒ INERT in hands-off creep**, able to touch **grind #2** (tq_avg 1600–2700) only ⇒ **NOT superseded but DEMOTED; do not call it a ratchet lever.** ✅ **FINAL QUEUE**: **FLY V158** (damper, the model's own measured prescription, GATE 2 closed) · V148 probe · V150 grind-#2 only · V151 marginal · V152/V153 GATE-2-OPEN · **SUPERSEDED V139/V149/V154/V155/V156/V157**. 🛑 **SESSION VERDICT: 12 builds made, 6 superseded (two would have REMOVED measured fixes — V149 removes Lever B which flew with “grinding FIXED”, V139 is the direction recorded as CAUSING grind #2), 4 demoted or inert, and 1 survives as a fix — V158, the only one designed FROM the golden model.** ⭐ **The audit was worth more than the builds it deleted. Read `eps_chain_*.py` FIRST.**

- ⭐⭐⭐★★★★★ [**The PARAMETRIC PUMP is located — lane gain A's knee straddles the operating point**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ⚠ first, a **correction: V158's GATE 2 is NOT fully closed.** The model warns that **all three PID lane gains are LERPs on `gp-0x6ac0`, the SAME rectified rate that indexes FactorE**, so *“size any FactorE edit against this, not just dose”* — **I priced V158 by dose alone and called its gate closed; that was overstated.** ⭐ **SIZING IT PRODUCED A FINDING**: `0xC671E` (lane gain A) has **X=[96, 104, 608, 704], Y=[704, 832, 832, 832]** — its **first segment spans 96→104 and the measured operating point `gp-0x6ac0` = 99 sits DEAD CENTRE**, an **18 % gain swing over 8 counts, slope 6.74/count**; lanes B and C are **flat** there. Since `gp-0x6ac0` is **RECTIFIED**, it **sweeps at 2f = 15.6 Hz** across that window during a 7.8 Hz oscillation ⇒ **[EVIDENCE] the PID's lane gain A is PARAMETRICALLY MODULATED ~18 % at 2f at the symptom's own operating point, and it is STRUCTURAL — present on STOCK.** ⊕ a named source at last for [[accord-v59-parametric-pump-marginal]]. ✅ **THE LEVER: flatten the knee DOWNWARD** — `0xC671E` **Y[1] := Y[0]** ⇒ Y=[704,704,832,832], so the 96–104 segment is flat and the 2f sweep sees **no gain change**; downward **lowers** PID gain rather than raising it, the ramp moves to 104–608 (a **6x wider, gentler** span), and **monotonicity is preserved** — the rule V157 broke. ⚠ **[BELIEF]** that removing an 18 % modulation is audible; ⚠ **[UNVERIFIED] readers, mode-indexing and record layout** — the 4-knot pattern fits lane A but gives implausible X for B and C (`[256,256,0,8]`, `[717,0,0,5]`), so **the layout is probably not uniform. RULE 7 is NOT satisfied — verify before building.**

- 🛑🛑★★★★★ [**RETRACTED — the “parametric pump at lane gain A” was a table-layout artefact**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I reported an 18 % parametric gain modulation at 2f from lane gain A's knee straddling the operating point, **flagged RULE 7 unsatisfied, and said verify before building. Verified — it is WRONG.** The raw halfwords are `... 0, 8, | 64,65,67,73,80,88,96,104 | 608,704,704,832,832,832,832,832 | 0,4 ...` ⇒ **`0xC671E` = 96 sits in the MIDDLE of one ascending run of 8, not at a record base**, so my 4-knot reading **straddled the X/Y boundary** and the “steep knee” follows from that mis-split alone. ⊕ **The tell was in my own output and I noted it without heeding it**: the same layout gave lanes B and C the impossible axes `[256,256,0,8]` and `[717,0,0,5]` — **a layout that produces nonsense on two of three records is not a layout.** ⚠ **The plausible reading REVERSES it**: as count=8 then X[8]=[64…104] then Y[8]=[608…832], the operating point 99 falls between X[6]=96 and X[7]=104 where **Y = 832 on both sides — FLAT**, so no modulation there at all (**also unverified — stated as the alternative, not a finding**). 🛑 **And it cannot be verified by scanning**: the `reg1==tp` scan finds **NO readers** for `0xC671E`/`0xC670A`/`0xC66DE` ⇒ **[UNRESOLVED] layout, readers and mode-indexing of all three lane-gain tables; all three of last turn's readings are VOID**, including “lanes B and C are flat”. ✅ **SURVIVES**: the golden model's own warning that the three lane gains share `gp-0x6ac0` with FactorE and *“size any FactorE edit against this, not just dose”* — **so V158's shared-axis GATE 2 remains OPEN**, because I could not read the tables it refers to. ⭐ **Cost: nothing on the car** — the verification caught it **before** a build, unlike V156/V157/V149/V139 where I built first.

- ✅✅✅★★★★★ [**UN-RETRACTED and BUILT — V159 removes an 18 % PARAMETRIC modulation of K_p at 2f**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **I over-corrected: the retraction was itself WRONG.** Decompiling `FUN_0003a382` gives **X[0]=tp+0x7b1e, X[last]=tp+0x7b24, Y[0]=tp+0x7b26, Y[last]=tp+0x7b2c** ⇒ **X is 4 halfwords at `0xC671E`, Y at `0xC6726`** — exactly the layout I first assumed. **What went wrong: lanes B/C read non-ascending X, and I let that anomaly override DIRECT INSTRUCTION EVIDENCE for lane A.** ⭐ **MECHANISM**: K_p is a LERP on **`gp-0x6ac0`, the RECTIFIED motor rate**; the model's **measured** operating point **99 [94,113]** lies **inside segment 0 (X 96→104)** where Y rises **704→832 = 18.2 % across 8 counts**, and a rectified index **sweeps at 2f = 15.6 Hz** during a 7.8 Hz ratchet ⇒ **[EVIDENCE] the PID's proportional gain is parametrically modulated ~18 % at 2f at the symptom's own operating point, STRUCTURALLY, on STOCK** — a **named candidate source** for [[accord-v59-parametric-pump-marginal]]. ✅ **V159: `0xC6728` 832→704** ⇒ Y=[704,704,832,832], **2 payload bytes, 54/54, CRC 50/50**, rwd `7c51b28b…`; swing **18.2 %→0.0 %**, **downward** (no new clamp reachable), **monotone** (the ramp moves to X 104–608, a **63× wider** span), **RULE 7 satisfied BY STRUCTURE** (bare `tp` displacements, **no index register** ⇒ flat table, all modes), and **VIRGIN** (832 on all 158 images). ⚠ **[BELIEF]** audibility; ⚠ **[OPEN]** lanes B/C's non-ascending X; ⚠ **V159 does NOT close V158's shared-axis GATE 2** — it addresses the **PID side**, not the FactorE side ⇒ **independent builds, do not stack.**

- 🛑🛑🛑★★★★★ [**V159 SUPERSEDED — an OFF-BY-0x400 address error, and the lane gains are FLAT**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **`tp+0x7B1E` = `0xC6B1E`, but V159 edited `0xC6728` = `tp+0x7728`, an UNRELATED cal**: I confused `0x7B1E` with `0x771E`, an **off-by-0x400**, same family as the off-by-0x1000 trap recorded six times. 🛑 **THE REAL TABLES KILL THE FINDING**: **K_p `0xC6B1E` X=[0,300,2000,4000] Y=[256,256,225,153]** · **K_i `0xC6B0A` Y=[98,98,98,98]** · **K_d `0xC6ADE` Y=[2048]x4** ⇒ the operating point **99 [94,113] lies in segment 0 of all three**, where K_p is **256→256 FLAT** and K_i/K_d are flat everywhere ⇒ **[EVIDENCE] ALL THREE PID LANE GAINS ARE FLAT AT THE OPERATING POINT — there is NO parametric modulation there.** The whole “parametric pump at lane gain A” line is **VOID**, and the hypothesis is now **CLOSED on the correct tables.** 🛑🛑 **THE PROCESS FAILURE IS WORSE THAN THE ARITHMETIC**: lanes B/C read **non-ascending X** — the **correct symptom of a wrong BASE** — and I read it twice and misdiagnosed it twice, first blaming the LAYOUT (retracting for the wrong reason), then **“verifying” against the decompile by RE-DERIVING THE SAME WRONG ADDRESS** and un-retracting. ⭐ **RULE: when a neighbouring record of the same family decodes as nonsense, suspect the BASE ADDRESS before the LAYOUT** — a wrong base garbles the whole family, and **lane A “working” while B/C were garbage was itself the tell.** ⭐ **RULE: re-deriving an address the same way is NOT verification** — **add the offset to `tp` explicitly and print both**, which is what finally caught it. ✅ **Nothing reached the car**, and ✅ **V158's shared-axis GATE 2 is substantially ADDRESSED**: the model demanded FactorE edits be sized against the PID schedule on the same axis, and **that schedule is FLAT at 99 counts**, so the coupling does not bite ⇒ **V158 is the lead build again.**

- ✅✅★★★★ [**V158 address-verified independently — and why it was immune to V159's error**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — after the off-by-0x400 that killed V159, the lead build's addresses were re-derived by **walking the pointer tables** (`0xC9E9C` -> records `0xD77BC/D0/E4`; `0xC9F84` -> `0xD77F8/80C/820`). **All six edits match their re-derived roles exactly** — `0xD77DA`/`0xD77EE` FactorC Y[0] 0->429/426, `0xD780E`/`0xD7822` FactorE X[0] 60->12, `0xD7818`/`0xD782C` FactorE Y[1] 140->539 — with an image diff of **14 bytes = 10 payload + 4 CRC, ZERO unattributed.** ⭐ **THE STRUCTURAL REASON IT WAS IMMUNE**: V159's addresses were **computed as `tp + offset`** (one wrong digit = a wrong cell, silently), while **V158's are READ as ABSOLUTE POINTERS out of the image** and then walked by a known layout => **no offset was ever added, so the off-by-0x400/0x1000 class cannot occur**, and a bad pointer would fall outside the image and be caught by the range filter. ⭐ **RULE: PREFER POINTER-DERIVED ADDRESSES OVER OFFSET-DERIVED ONES** — walk the in-image pointer array where one exists, because it is **self-validating**; reserve `tp+offset` for scalars with no pointer, and then **add it to `tp` explicitly and print both.** ⊕ This retrospectively explains why the **damper** work survived the audit and the **lane-gain** work did not: **the damper's records are pointer-reachable, the PID's lane gains are not.** ✅ **V158's standing: addresses verified · dose 50 inside the model's own [30,60] · monotone · 9.8 % of ceiling (10.2x margin) · RULE 7 read not assumed · shared-axis coupling shown not to bite** => **every gate the model raised is now addressed.**

- ✅✅★★★★★ [**The damper is FIVE tables, the record header is the KNOT COUNT, and V158 verifies from its own bytes**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00034350` decompiled 2026-08-28 carries **L1 `0xC9CCC` · L2 `0xC9E9C` · L3 `0xC9DB4` · L4 `0xC9F84` AND the OUTPUT CEILING `0xC77A0`** (2-knot, X[0]@+2 Y[0]@+6). ⭐ **THE RECORD'S FIRST HALFWORD IS THE KNOT COUNT** (2/4/4/5/4) — a **self-validating invariant**: any correct read must have `hdr == len(X)` with X strictly ascending, and **it would have caught V159's off-by-0x400 instantly.** Put it in every build script. ✅ The ceiling's **512** floor is now READ FROM THE IMAGE, not prose: `gp-0x6ac2` is a sign-gated kickback detector (0 in same-sign driving) so the LERP clamps flat to Y[0]=512, and the `>=0x32c9` bypass hits `0xC6158`=512 — **both paths agree.** ✅ **V158 re-verified by exact integer arithmetic on its shipped image**: dose **27 / 50 / 109** at rate **60 / 99 / 200** ct (stock **0** at every creep case) ⇒ **GENUINELY RATE-PROPORTIONAL, NOT A RELAY** — the substantive GATE 2 test — and **dose 50 at the measured operating point** is both the model's design target and the value V74 flew with 67.4 % engaged-creep liveness / 0 frames at the ceiling. Build-time rule `(FactorC×FactorE[3])>>10 ≤ 512`: **m26 388, m27 385, PASS**.

- ⚠★★★★ [**V158's FactorC arm is NOT monotone — and that is CORRECT; name the AXIS when applying a shape law**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V158 leaves FactorC `Y=[429,234,429,908]` (m26), which **dips between 35 and 60 km/h**. I certified it "MONOTONE"; **the wording was wrong, the build is right.** The golden model prescribes `FactorC Y[0] := Y[2]` **explicitly**, and separately records that `Y[0] := Y[1]` is *“the largest monotone lift of Y[0] alone”* ⇒ it knew it was exceeding the monotone limit. ⭐ **The shape law exists to stop FactorE being flattened across the RATE axis into a bang-bang relay** (V72's error — a limit-cycle generator at a lightly-damped resonance). **FactorC is SPEED-indexed and “costs NO rate-proportionality”**; vehicle speed varies over seconds and **cannot pump a 7.8 Hz ratchet.** ⇒ **“Monotone” is load-bearing on a FAST axis and cosmetic on a SLOW one** — asserting it unqualified nearly cost the lead build a false supersede. [[accord-v158-damper-golden-model-shape]]

- ⛔⛔ **SUPERSEDED — THE CLAIM BELOW IS WRONG; `0xCC914` IS LIVE.** It is the speed BREAKPOINT VECTOR of `FUN_000348e0`, read at `0x34936` as `ld.w 0xd914[r16]` with a COMPUTED base register, which is why both scans missed it. See the retraction entry further down this file. Kept as a record of the reasoning, NOT as a finding.

- ⛔★★★ [**`0xCC914` is a DEAD table — and `movea` immediates cannot be scanned for table bases**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the pointer-table family is now CLOSED: **35 of 37 are loaded by exactly one `mov imm32` literal**, each located; **`0xCC214` and `0xCC914` have NO reference anywhere in the image** (no literal, no `movhi`+`movea` pair). `0xCC914`'s **double dead zone** (`Y=[0,0,512,2560]`, zero below ~40 km/h) looked like the perfect creep lever and **is not one — nothing reads it.** ⚠ **The “single reference `movea 0xC914` at `0x3938E`” was a FALSE POSITIVE**: the instruction is `movea 0xC914, r4, r30` with **r1 = r4 = gp**, i.e. **`gp - 0x36EC`**, a RAM cell all over `FUN_000389ec` — not a table base. **A raw byte scan for an immediate cannot distinguish a BASE REGISTER from a `movhi` partner**, and sign-extension makes `0xC914` read as `-0x36EC`. One `decompile_function` call settled it. [[feedback-decompile-first-then-assembly]]

- ⭐✅★★★★★ [**The `(0, N)` KNOT-COUNT HEADER — put this check in every build script**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every cal LERP is anchored by a 2-halfword `(0,N)` header (inline tp-relative cals) or a bare `N` at +0 (pointer-table records), **N = the knot count**. Validated across `0xC6000-0xC7000`: **54 well-formed vs 8 false positives.** ⇒ a correct read must satisfy **`hdr == len(X)` with X STRICTLY ASCENDING** — one assertion that catches a **wrong address, a wrong knot count and a wrong stride** at once, which are all three of the failure modes that cost this session builds. [[accord-headerless-scratch-blob-offsets-are-not-addresses]]

- ⛔★★★★ [**V159's mechanism does not exist — the lanes B/C anomaly was an off-by-2-halfword read**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — anchored on the header, all three PID lanes are well-formed: **A `0xC6B1E` X=[0,300,2000,4000] Y=[256,256,225,153]**, **B `0xC6B0A` Y=[98,98,98,98] FLAT**, **C `0xC6ADE` Y=[2048]x4 FLAT**. V159 was built on *“an 18.2 % parametric modulation of K_p at 2f at the symptom's own operating point”* from a claimed `X=[96,104,608,704]` — **that table is not lane A**, and at the measured `gp-0x6ac0` = 99 [94,113] lane A sits in segment 0 where **Y is FLAT at 256** ⇒ **no gain swing exists at the operating point, at 2f or any frequency.** ✅ **The lane-gain parametric-pump hypothesis is closed by DIRECT BYTE EVIDENCE**, independently agreeing with how V158's shared-axis GATE 2 was closed. ⊕ `0xC6728` is `Y[3]` of an unrelated **8-knot** table at `0xC6712` ⇒ the supersede was right and the blast radius is now known. [[accord-v59-parametric-pump-marginal]]

- ⚠⚠ **SUPERSEDED IN PART — V160 IS NOT THE LEAD BUILD.** A power calculation demoted it: its Lever B increment predicts 1.4-5.6 % against a ~20-40 % instrument floor, i.e. 4-30x below resolvable. **Fly V158 first.** V160 is the follow-up for the "better, effort fine" branch. The build itself is sound; only its priority changed.

- ✅✅★★★★★ [**V160 BUILT — Lever B to its int16 ceiling, the new lead build**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0xC6446` **5244->6553**, one halfword on a V158 base, 51/51, CRC 50/50, 2 payload bytes. Lever B is the **r24 derivative-feedback gain used WHEN LKAS IS ENGAGED**, and V88-vs-V87 measured it **single-variable**: **6-9 Hz 0.859 - 9-12 Hz 0.604 - 15-22 Hz 0.549 - 0.5-3 Hz 1.192 = NULL** => *“more r24 derivative feedback = more loop damping = less HF everywhere, at zero LF cost”* — **the ONLY lever measured to cut both the ratchet band AND the grind band while leaving LKAS authority untouched.** ⭐ Across **all 159 images** this cell has had **only three values** (512/5244/1024) => the dose-response has **two points** and the flown step was **10.24x**; **6553 is the EXACT int16 ceiling** ((5120x6553)>>10 = 32765, 6554 overflows) => a **1.25x** increment on a hard boundary. ✅ **AUTHORITY IS SAFE BY CONSTRUCTION**: r24's rail (+-8192, four immediates `0x3AC42-0x3AC54`) is byte-identical — raising the **RAIL** is the one change that could cut peak LKAS steering; we raise the **GAIN**. ⚠ **[BELIEF]** monotonicity beyond 5244 — only two points exist and V62's lesson is *“2x = OPTIMUM, not a point on a ramp”* => **V160 is a dose probe as much as a fix.** [[accord-v88-flew-grinding-fixed-command-intact]]

- ⛔⛔★★★★ [**RETRACTION: `0xCC214`/`0xCC914` are NOT dead — a null is only as good as its scan's encoding coverage**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0xCC214` is **LIVE**: the **4th pointer array of gain_B (r24)**, the 100 km/h speed-blend records, reached as **`tp+0xD214`** hard-coded in the instruction stream, which is why it has no `mov imm32` literal. My null scanned only literals and 16-bit `movhi`+`movea` pairs => **blind to the long tp-relative form**. ⚠ **`0xCC914` is UNRESOLVED, not dead — the question is OPEN again.** ⭐ **TWO encoding traps bit in ONE session**: `hw2 = (disp|1)` on `ld.hu` (scanning for `hw2 == disp` returns **ZERO** readers for a cell that has one), and `disp > 0x7FFF` cannot be a disp16 at all. ⭐ **VALIDATE ANY SCANNER AGAINST A CELL WHOSE ANSWER IS ALREADY KNOWN BEFORE TRUSTING ITS NULL** — that is what caught both (`0xC6440`->`0x3AC12`, `0xC6442`->`0x3ABFE` reproduced the model exactly). [[accord-gp4f60-two-encodings-enumeration-trap]]

- ✅✅★★★★ [**`0xCC914` is LIVE — a BREAKPOINT VECTOR; and scanning by base-register identity is structurally incomplete**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — read at **`0x34936`**, `ld.w 0xd914[r16]` with **`r16 = tp + mode*4`**, the same idiom as gain_B's 4th array at `0x3ADC2` (`tp+0xD214`), which validated the decoder `disp23 = (sext(hw3)<<7) | ((hw2>>4)&0x7F)` before it was trusted. ⭐ **BOTH earlier scans missed it because the BASE REGISTER IS COMPUTED**: a `mov imm32` literal scan misses it (the other five arrays are literals) and a tp-relative scan misses it (`reg1` is `r16`, not `tp`). **Three encoding traps in one session** — `hw2=(disp|1)`, `disp>0x7FFF` cannot be disp16, and a computed base. ✅ **What it is**: the **five SPEED BREAKPOINTS** `[0,512,2560,5120,8960]` = **[0,8,40,80,140] km/h** for `FUN_000348e0`'s 5-curve blend (same architecture as gain_B), writing a runtime X row to `gp-0x6394` and Y row to `gp-0x63a8`. ⚠ **The model's “flat zero at creep” is only true AT A STANDSTILL** — curve 1 is all-zero Y so the term ramps `frac = speed/512`: **25 % at 2 km/h, 62.5 % at 5 km/h**. Golden model corrected in place; **contract re-run 87 symbols / 2512 B / `740f4bcd…` exact.** [[accord-gp4f60-two-encodings-enumeration-trap]]

- ⛔⛔ **SUPERSEDED — DO NOT FLY V162/V163.** `gp-0x6ad4` is STIFFNESS, not damping: at 7.8 Hz it is P 99.88 % @ -1.7 deg with D contributing 0.02 %. Raising its ceiling adds loop gain with NO phase lead into a Q 14-29 resonance and would most likely make the ratchet WORSE. Artifacts renamed `SUPERSEDED-DO-NOT-FLASH-PSTIFFNESS-*`. Kept as a record of the reasoning.

- ✅✅★★★★★ [**V162/V163 — the resonance PID gets its authority back at creep (`0xC67C4` 1280->512)**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the golden model's OWN recommendation: the ringing **enters through a SENSOR-FED lane, not the command lane**, leaving `{r24/r26, gp-0x6ad4, gp-0x6b26, gp-0x6bbe, V89}`, of which `gp-0x6ad4` is *“the most reachable authority of any gated lane”*. ⚠ **THIS OVERTURNS `accord-v56-flashed-mute-null-and-costs-damping`**, which recorded `gp-0x6ad4` as eliminated — **that mute was scored at ~21 Hz and the ratchet is 6-9 Hz**; the model states the lane *“has NEVER been scored at 6-9 Hz, so it is OPEN, not eliminated.”* Treat the memory's elimination as **scoped to ~21 Hz.** ✅ Stock `X=[128,1280,3200]`=[2,20,50] km/h, `Y=[0,1024,1024]` on voted speed ⇒ **the lane whose job is damping resonance runs at 170/1024 = 16.6 % authority at 5 km/h**; the model's quoted 164-341 for the ratchet episodes **reproduces from these bytes exactly.** X[1]->512 gives **x3.00 at 3/5/8 km/h**, **0 at 2 km/h unchanged** (parking intact), **unchanged ≥20 km/h**. ✅ Y untouched (Honda's own 1024; only the SPEED to reach it moves) and Honda **already runs this lane at full authority above 20 km/h without ratchet** ⇒ moving creep toward a known-good config. ✅ **VIRGIN on all 161 images**; axis is SPEED (seconds-scale) ⇒ **no parametric pump**. ⚠ **[BELIEF] the PID's PHASE at 6-9 Hz** — if it is tuned for ~21 Hz the wrong phase makes the ratchet WORSE; bounded to 2-20 km/h, revert is one halfword, `X[1]=768` gives 2x instead. [[accord-ratchet-is-a-lightly-damped-resonance]]

- ⛔⛔★★★★★ [**`gp-0x6ad4` is STIFFNESS, not damping — structurally eliminated at 6-9 Hz; V162/V163 superseded before flying**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — gains at the ratchet's own `gp-0x6ac0`=99, all flat there: **Kd=0.250 (`0xC6B1E`), Ki=0.0957 (`0xC6B0A`), Kp=2.0 x32 = 64 (`0xC6ADE`)**, and 🛑 **the P-term's IIR pole `0xC6450` = 1024 => a = 1.0 => a PASS-THROUGH, no smoothing.** At 7.8 Hz: **P |64.00| @0.0 deg = 99.88 %**, I |1.95| @-88.6 deg, **D |0.012| @+88.6 deg = 0.02 %**, **sum @ -1.7 deg** ⇒ **a 0-degree-phase term is STIFFNESS, NOT DAMPING.** Raising its ceiling raises loop gain with **no phase lead** into a **Q 14-29** resonance ⇒ would most likely make the ratchet **WORSE**. ⭐ **ELIMINATED ON STRUCTURE, NOT A NULL**: for D to matter needs `Kd~1306` = a Q10 Y of ~1.34 MILLION, but **the cell is u16** — max 65535 gives only +1.06 deg ⇒ **the derivative path is ~1300x too weak BY DESIGN and the register width cannot close it.** Survivors now **{r24/r26, gp-0x6b26, gp-0x6bbe, V89}**. ⚠ **THE MISREADING**: the model said *“the most reachable AUTHORITY”* — **authority, not damping**; I read “resonance PID” and supplied “therefore it damps.” ⭐ **A LANE'S NAME IS NOT ITS TRANSFER FUNCTION** — compute magnitude AND phase at the symptom's frequency BEFORE building; it took ~20 lines of Python. ⊕ V158/V160/V161 unaffected (Lever B's basis is MEASURED, not inferred). [[accord-ratchet-is-a-lightly-damped-resonance]]

- ✅✅★★★★★ [**Which lanes actually DAMP at 7.8 Hz — and V158 sized in physical units**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — phase computed at the symptom's own frequency for every sensor-fed survivor: **`gp-0x6ad4` -1.7 deg = STIFFNESS (eliminated)** · **`gp-0x6b26` = -K·ACCELERATION = added INERTIA, lowers f0, does not damp** · **`gp-0x6bbe` = TRUE VISCOUS** (measured 90 ct/(rad/s) @~0 deg vs rate) · **`gp-0x6bd0` = viscous iff f is linear in |rate|** (V158's target) · **r24 = K·d(torque)/dt, +90 deg = damping** (Lever B, measured by V88). ⭐ **The kit's two levers are exactly the two lanes that actually damp** — which is why they are the two that measured well. ✅ **V158 is genuinely VISCOUS, not a relay**: `dose/rate` is near-constant **0.375 -> 0.554 across a 6.5x rate span** (a relay would fall 6.5x) ⇒ GATE 2's rate-proportionality satisfied **empirically**. ⭐ **SIZED AGAINST AN ON-CAR MEASUREMENT**: stock creep **0.000**, `gp-0x6bbe` **1.571**, V158 adds **2.733** ⇒ total **1.571 -> 4.304 ct/(deg/s) = x2.74**, in the SAME aggregator counts. ⚠ **[BELIEF]** ζ would go **0.017-0.036 -> 0.047-0.099** ONLY if the firmware term dominates damping — unresolvable without a drive, so treat 2.74x as the **firmware-side increment, not a ζ prediction.** ⚠ Damping fades as rate→12 (`dose ∝ rate-12`); `X[0]=0` would linearise it but **the model argues 12 deliberately** — not changed. ⊕ Headroom exists (rule reads 388/512) but **the model's requirement is ~43 [30,60] and 50 sits inside it** — left alone. [[accord-gp6bbe-is-viscous-plus-dc-pedestal]]

- ⛔★★★★ [**r26's arm and the FUN_00036682 filter pole — BOTH CLOSED, both nearly built**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **(1) `0xC6444`**: the model strikes it as *“reachable only on a build whose control path is already ruled out”*, and **that premise is stale** (the repointed `0x3AA96=0xfb` path has flown since V88). But re-deriving reachability shows **V71c ALSO carried `0xfb`**, so `0xC6444=3072` **was genuinely read on-car** ⇒ the falsification is **valid**, and the **6x cut back to 512 is LOAD-BEARING** ⇒ **raising r26 is the wrong direction and I was one step from re-flying V71c.** ⭐ A model's *strike* and its *reason* can both be stale while the verdict still stands — re-derive reachability, then trust the flight result. **(2) `0xC63D2`**: `FUN_00036682` is a **backlash band + one-pole LP** writing `gp-0x6b46`; the pole is **6 => corner 0.93 Hz**, giving **81.8 deg lag but |H| = 0.119** at 7.8 Hz — a **deliberately slow trim**, attenuated 8x. **Raising the pole to cut the lag would raise a lagging lane's gain 8x into the resonance = EXACTLY the V162 error**; lowering does nothing. **Not a lever either way.** ⊕ The backlash itself is the textbook small-amplitude ratchet generator but sits **BEFORE** the LP, and its half-width comes from RAM (`gp-0x6b44`), not a cal — **[OPEN]** find that writer. ✅ **Damping levers now exhausted**: r24 at its int16 ceiling, `gp-0x6bd0` at the model's prescription, r26 falsified, `gp-0x6ad4` structurally eliminated, `gp-0x6b26` is inertia. **V160 carries both mechanisms that actually damp.**

- ⛔★★★★ [**The backlash band IS cal-reachable — and closed by the limit-cycle exclusion; the static search is COMPLETE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `gp-0x6b44` has **1 reader (`0x36760`) / 1 writer (`0x36BB0` in `FUN_00036828`)** and the writer is pure cal arithmetic ⇒ the band width **is** cal-reachable: `clamp((uVar20-102)*1024>>10, lo=cal 0xC61A0[123], hi=cal 0xC619E[307])`, so **the half-width never narrows below 123 counts** and **all five cals are VIRGIN on 163 images**. ⭐ It looked like a clean lever — a backlash's phase lag is **worst at small amplitude** (the creep regime) and narrowing it *reduces lag* rather than adding gain, so it avoids the V162 error. ⛔ **But it is closed**: the kit's strongest ratchet result is *“a lightly-damped RESONANCE, Q 14-29, ring-down ζ 0.017-0.036 — **limit cycle EXCLUDED**”*, and **a backlash-driven oscillation IS a limit cycle** ⇒ the backlash is not generating the ratchet; narrowing it would mainly admit the small-signal noise the band exists to reject, and the lane is attenuated **8x** at 7.8 Hz anyway. ✅ **THE STATIC SEARCH IS NOW COMPLETE** — every aggregator lane has a phase or structural verdict: **r24 and `gp-0x6bd0` are the only two that damp**, both carried by V160 at their limits; r26 falsified, `gp-0x6ad4` stiffness, `gp-0x6b26` inertia, `0xC63D2` slow trim, return-centre dead, `gp-0x6ade` dead, LKAS excluded. ⚠ **Further progress is MEASUREMENT-limited**: the instrumented contrast collapses to ~1.1x under controls yet V88 changed the felt symptom ⇒ **the bus instrument is the weak link**, and the next real information is a creep drive WITH AUDIO. [[accord-ratchet-is-a-lightly-damped-resonance]]

- ✅✅★★★★★ [**V158 is the flight, not V160 — a power calculation demoted the lead build**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/SCORING-V158-preregistered.md) — V88 measured Lever B across a **10.24x** step; V160 adds **1.2496x**, which log-linear extrapolation of V88’s own ratios predicts is worth **1.4 % (6-9 Hz) / 4.7 % / 5.6 % (15-22 Hz)** against a same-firmware floor of **~40 % (18-22 Hz)** and **~3-5x (6-9 Hz)** ⇒ **4-30x BELOW the floor, unmeasurable on one drive**, while adding an untested dose and **destroying attribution if the drive is worse.** ✅ **Fly V158** — single-variable vs V122, and creep damping **0 → 2.733 ct/(deg/s)** is the only change large enough to resolve; V160 is the follow-up. ⭐ **RULE: a build is only worth a drive if its predicted effect exceeds the instrument floor** — stacking a sub-floor increment onto a resolvable one buys nothing and costs attribution. Pre-registration also fixes the **decision tree** (better+heavy → FactorC Y[0] 429→234 halves the drag; unchanged → dose ladder, which would overturn the model’s “err low” argument WITH DATA; worse → revert). [[accord-v88-flew-grinding-fixed-command-intact]]

- ✅✅★★★★★ [**V164/V165 built — every branch of the decision tree now has a flyable image**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a **dose ladder in physical units**, all on the V158 base: **V122 dose 0 (viscous 1.571 total, x1.00)** · **V164 dose 27 (3.047, x1.94)** · **V158 dose 50 (4.304, x2.74)** · **V165 dose 65 (5.124, x3.26)**, against an on-car measured `gp-0x6bbe` baseline of 1.571 ct/(deg/s) and stock creep damping of EXACTLY 0.000. ✅ **V164 is a free bonus**: Y[0]:=Y[1] makes FactorC **MONOTONE** `[234,234,429,908]`, removing the 35-60 km/h dip V158 deliberately accepts ⇒ lighter AND better-shaped. ✅ **V165 holds Y[3]=927 deliberately** — a rising segment must survive on X 2500..4000 or FactorE flattens into a near-**BANG-BANG RELAY** (V72’s error, a limit-cycle generator), and holding Y[3] keeps the build rule at V158’s 388. ⚠ V165’s 65 is just ABOVE the model’s ~43 [30,60] — **conditional, fly only if V158 measured insufficient.** ⭐ **Flight plan: fly V158 first**; better+OK→V160, better+heavy→V164, unchanged→V165, worse→V122, no creep episodes→re-drive. **No branch can leave the kit without a prepared next step.**

- ✅✅★★★★★ [**HANDOFF 2026-08-29 — the phase test, an exhaustive adjudication, and a dose ladder**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/handoffs/2026-08/HANDOFF-2026-08-29-phase-test-and-the-dose-ladder.md) — the session’s deliverable is a **METHOD**: compute a lane’s magnitude AND phase at the symptom’s own frequency, from the bytes, **BEFORE** building. ~20 lines of Python; it killed V162/V163 after they were cut. Applied exhaustively, **exactly two aggregator lanes damp** (r24 and `gp-0x6bd0`) and V160 carries both. **Fly V158** — first build ever to deliver ANY creep damping (stock is exactly 0.000 ct/(deg/s)); V160 demoted because its increment is **4-30x below the instrument floor**. Dose ladder built: **V164 27 · V158 50 · V165 65**, so every drive outcome has a prepared next build. Carries all negatives (`gp-0x6ad4` u16-bound, r26 falsified via V71c, `0xC63D2` slow trim, backlash closed by limit-cycle exclusion), all three retractions, and every open item with what would close it.

- ✅★★★★ [**The pointer-table map is COMPLETE — all 37 attributed, no new torque-path lever**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — triaged by **what each function WRITES** (cheaper than decompiling, and decisive): **`FUN_000382d8`** (8 tables) has **ZERO aggregator-lane stores** and feeds `FUN_000389ec`’s plausibility monitor (fault `0x4377`) ⇒ **monitor-side**; `FUN_0003b338`/`FUN_0003b416` write non-lane cells; **`FUN_0003b49a`** writes `gp-0x6b28` — **0 readers image-wide ⇒ write-only telemetry, DEAD** — and `gp-0x6b2a`, whose single reader is `FUN_00037fe6`, the unity-weighted **Path-2 term sum** ⇒ the plant-model/observer path where **V89 already flew and its null is explained by f′ compression.** ✅ **No loop-wide lag source at the sensor either**: `gp-0x4f60` has 64 readers / 5 writers all in the 0x7Fxxx acquisition layer, and the model confirmed “`FUN_0007e74a` has NO EMA/IIR anywhere” ⇒ **there is no filter to de-lag**, so the last class of change that could add phase margin loop-wide does not exist. ⚠ **SCANNER FIX: V850 stores are 0x3A/0x3B ONLY — 0x38/0x39 are LOADS**; my triage over-included and briefly showed `FUN_0003b49a` “writing” the torque sensor. ⊕ The verdict survived because the error ran in the **safe direction** — **state which way a filter error runs before deciding whether a conclusion survives it.**

- ⛔★★★★★ [**The authority collapse curve admits NO beneficial change within the safety rule**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — mode 7, all four records **VIRGIN on 90 images**: primary `0xE547C`/`0xE5404` **X=[70,72,78,80] Y=[254,234,12,0]**, blend `0xE52FC`/`0xE5284`. Authority falls **254→0 across TEN byte-counts** (torque 2240→2560) and the **measured median override torque is 2235 = byte 69, ONE COUNT below X[0]** ⇒ **the operator drives on the knee**, which is the “authority disappears” mechanism. ⛔ **Enumerated, not argued**: hold-longer, gentler-slope and raise-mid-Y **all violate** monotone-non-increasing; collapse-earlier is legal but gives **less** authority. Because authority is monotone-decreasing in torque, *holding it up longer anywhere* **IS** *more than stock somewhere* — the same statement ⇒ **no legal change improves it.** ⚠ **The trade is the operator’s**: Honda collapses authority BECAUSE the driver is pushing, so raising it means the driver must push harder to take the wheel back. Minimal bounded form if he wants it: `X[0]/X[1]` **70,72→72,74**. **NOT BUILT.** ⛔ The `0xC61BC` cave probe is also deferred — a **probe, not a fix**, and caves are the kit’s **only bricking class** (V24/V27/V48B); spending a brick risk on a measurement before taking the cheap measurement (a drive) is the wrong order.

- ⛔★★★★★ [**Peak command oscillation has NO remaining firmware lever — the setpoint limit is ALREADY at openpilot's rail**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V166 was designed and **killed by its own base assertion before emitting an image**. The cascade is `setpoint = clamp(STEER_TORQUE x -4, ±arb_setpoint_limit)` then `lkas_max = min((setpoint x gain)>>15, clamp 3072)`, and the model's default field reads 15360 ⇒ it looked like the last untouched lever. ⛔ **But V38 already raised 8 of the 28 records to 16384, including `0xE41A8`, the A160 record OUR CAR uses**, and V122/V158/V160 all carry it: `(16384×5346)>>15 = 2673 < 3072` ⇒ **protocol reach 4096/4096, nothing is clipped**, and my edit was a **no-op**. ⭐ **THE RULE I BROKE**: I read the model's **Calibration DEFAULT (15360 = STOCK)** and scanned the **stock image**, then reasoned about the flying build — CLAUDE.md says verbatim *“check build lineage before proposing a cal lever… state its on-car result.”* ⊕ **The base assertion caught it** (20 flat records found where stock has 28) — a base-value check is what stops a no-op reaching the car. ⭐ **A model's Calibration defaults are STOCK, not the flying build. Read the image.** ✅ **Third complaint CLOSED**: the firmware already delivers the whole range; openpilot rails because it wants **more than the CAN signal's 13-bit 4096** — a **protocol** limit — and the only firmware quantity that could deliver more per count is the **gain**, which measured **worse at 8x** (1.13 dB vs 2.24 dB) and is frozen by the operator's own instruction. [[accord-check-build-lineage-before-proposing-lever]]

- ⚠⚠★★★★★ [**V158's NAMED RISK: `gp-0x6bd0` feeds BOTH aggregators and Path 2 inverts its sign**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — from the golden model's **facade header** (the four modules do not repeat it, so reading only `lanes`/`control` misses it): *“`gp-0x6bd0` is ‘damping’ for **PATH 1 only**; `FUN_00038148` (Path 2) applies its **own extra `pol` multiply**, so at **pol = −1 the same cell arrives PUMPING-signed.**”* ✅ **Byte-confirmed**: 5 readers, two of them the aggregators — **`0x3AC78` Path 1 (damps)** and **`0x38150` Path 2 (pumps)** — and `gp-0x6752` is −1 on this car. ⊕ **A caveat, not a cancellation**: Path 1 is the primary aggregator, Path 2 is the observer loop whose contribution reaches the car through **f′, compressed 6.3x** (the mechanism behind V89/V97's nulls); **V74 already flew this dose** at 67.4 % engaged-creep duty; and the model prescribed the edit knowing the architecture. ⭐ **It is the NAMED MECHANISM for the “worse” branch** — the pre-registered tree already routes worse→revert, now with a specific predicted cause. ⛔ **AND A MODEL CORRECTION**: Path 2's *“eight float coefficients at `tp+0x50d4/…`, never byte-read”* are **NOT there** — all eight read as **DENORMALS** and three addresses are the kit's own u16 cals (`0xC40BC` knee, `0xC40D0` pole, `0xC40D2` K1). **The trap**: a future session reads ≈ 0 and concludes “Path 2 is dead”, which is wrong — Path 2 demonstrably runs. **Path 2's loop gain remains UNLOCATED**; closing it needs the real float block from `FUN_0003b8f6`'s decompile. [[accord-fprime-compression-explains-v89-and-v97]]

- ✅✅★★★★★ [**Path 2's coefficients are LOCATED — the FIR cannot ring; the model's “never byte-read / GATE 2 uncertifiable” note is STALE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_0003b8f6`'s decompile shows **only THREE of the “eight floats” are floats**: `tp+0x5048/504C/5050` = **1.0 / 0.0 / 0.0**; the rest are **`ushort` reads scaled in code** (which is why a float32 read returned denormals). ✅ **The 3-tap FIR is an IDENTITY PASSTHROUGH with both history taps ×0.0 ⇒ 2 zeros, 0 poles, no feedback ⇒ IT CANNOT RING.** ✅ Every Path-2 pole quantified: `0x50D4`=573 (22.3 Hz) · `0x50D8`=3686 (143 Hz) · `0x50D0`=408 (15.9 Hz) · **`0x50D6`=246 (9.6 Hz)** · `0x50D2`=1020 is K1 (a gain) · `0x50BC`=3000 is the knee. ⚠ **This SHARPENS V158's risk**: `0x50D6`'s **9.6 Hz corner sits IN the ratchet band and is applied TWICE** ⇒ |H| 0.615, **74° lag** at 7.8 Hz. 🛑 **And f′ runs the WRONG way**: p50 **2.174 hands-off vs 0.346 hands-ON** ⇒ the observer is **6.3x MORE sensitive hands-off**, and the ratchet IS hands-off creep ⇒ **Path 2's pumping copy of `gp-0x6bd0` is LARGEST exactly where the ratchet lives.** I had cited f′ compression as reassurance; read correctly it is the opposite. ⊕ Still open: the relative WEIGHT of Path 1 vs Path 2 into the motor command, which needs `gp-0x6b94`'s 4 unchecked readers decompiled. **V158 remains the right build to fly**, with a now-quantified “worse” mechanism. [[accord-fprime-compression-explains-v89-and-v97]]

- ✅✅★★★★★ [**V167 built — `0xC63A0` 1024→512, the knob for V158's one named risk**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00038148` applies **PER-TERM WEIGHTS** before the sign-inverting pol multiply, and **`0xC63A0` is `gp-0x6bd0`'s PATH-2 weight and nothing else** (siblings: `0xC63A2` 6bbe · `0xC63A4` 6b46 · `0xC63A6` 6b26 · `0xC63A8` 6b4e · `0xC63AA` LKAS). Halving it **halves the PUMPING copy while Path 1's damping stays byte-identical** (Path 1 reads the same cell in `FUN_0003aa2c` @`0x3AC78`, no such weight). ✅ **Lowering is safe by construction** — the model's own words for the sibling: *“reducing a feedback magnitude cannot destabilise a stable loop whatever its phase; RAISING is the classic destabiliser.”* History **1024 ×137, 2048 ×5 (V72-75/81) — raised and flown, NEVER lowered.** ✅ **Inert without V158** (on V122 the damper is 0 at creep, so the weight multiplies zero). ⭐ **It REPLACES “revert” as the answer to V158's worse branch**: a revert discards Path-1 damping together with Path-2 pumping and discriminates nothing; V167 keeps the damping and removes half the pumping. ⚠ **[BELIEF]** the pumping may not matter — Path 2 is throttled to **16.6 %** at creep by `0xC67C2`, but f′ is **6.3x MORE sensitive hands-off** where the ratchet lives; **net unresolved**, and 512 is a halving, not a computed optimum. ⊕ **CORRECTION**: `FUN_000382d8`/`FUN_000389ec` are **NOT purely monitor-side** — `FUN_000389ec` writes the very RAM LERP rows (`gp-0x64b8`/`gp-0x641c`) that `FUN_00038148` reads ⇒ **a function can be in the loop without writing a lane cell; follow the RAM it writes.**

- ⛔★★★★★ [**The Path-2 weights are NOT a lever family — and why `0xC63A0` is the one exception**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every Path-2 term gets the **extra `pol` multiply**, so a Path-1 damper arrives as a Path-2 pumper; `gp-0x6bbe` is a **measured** viscous damper **live on stock at creep** whose weight `0xC63A2` is **VIRGIN on 142 images** ⇒ it reads as a standalone ratchet lever needing no V158. ⛔ **It is not one. Path 2 is a DISTURBANCE OBSERVER**: it sums the assist lanes to PREDICT motor torque and compares against measurement, so lowering a weight does not remove pumping — **it makes the observer's model WRONG and biases the residual.** The pumping-signed arrival is **what an observer subtracting a predicted contribution is SUPPOSED to look like.** ⊕ Retro-justifies the strike on `0xC63A6` (V154/V155): the objection is not just uncertifiable GATE 2, it is that the edit **de-tunes an observer**. ⭐ **`0xC63A0` is different**: on V122 `gp-0x6bd0` is **exactly 0 at creep**, so the observer's creep sum has **NEVER contained a damper term**, and V158 introduces one at full weight that the observer was never tuned to see ⇒ **V167's 512 is CLOSER to pre-V158 observer behaviour than 1024 is.** That argument holds on this lane ALONE. ⚠ It still cuts both ways, which is why **V167 is a DISCRIMINATOR for the “worse” branch, not a predicted improvement.** ⭐ **RULE: before lowering a weight, ask what the sum is FOR** — in an aggregator a weight is a gain; in an **observer** it is a MODEL, and “less of the bad-signed thing” is the wrong frame entirely.

- ✅✅★★★★★ [**The “unresolved hop” is CLOSED, and V158's Path-2 risk is bounded at ~20 %**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — triaging `gp-0x6b94`'s four unchecked readers by what they WRITE: **`FUN_0004503c` writes `gp-0x6ace` ⇒ it IS the governor hop**, matching the byte-verified memory bridge `gp-0x6b94→governor→gp-0x6ace→gp-0x6acc→shaper→gp-0x6b08→gp-0x6b98→FOC` ⇒ **the model's [OPEN] note is STALE, memory had it.** ⊕ New: `gp-0x6b94` ALSO drives the **backlash** fn via `FUN_00036bec`'s EMA into `gp-0x6b48`. ⭐ **Consequence: Path 2 does NOT reach the motor independently** — it feeds `gp-0x6ad4` back into the SAME aggregator, so both routes exit through `gp-0x6b94` and are directly comparable: **Path 1 = 1.000**, **Path 2 = 0.204 × s** (pol · double-9.6 Hz-EMA 0.615 · RAM-LERP slope s · err-inversion · PID 2 · ceiling 0.332 · pol). ⚠ **[BELIEF]** net sign −1 (three inversions, hand-traced) ⇒ still pumping. ✅ **[EVIDENCE]** **Path 1 DOMINATES unless s > 4.9**, and that LERP is a bounded shaping curve, not a gain stage ⇒ implausible. ✅ **So V158's named risk is BOUNDED at ~20 % of the damping it buys, not unresolved** — a materially better profile than it looked, and worth stating as plainly as the risk was. V167 stays the right “worse” branch since halving `0xC63A0` halves exactly that 0.204 term. ⚠ **[OPEN] the slope `s`** — needs the `gp-0x64b8`/`gp-0x641c` rows the model records two failed attempts at. [[accord-aggregator-reaches-motor-via-gp6acc-bridge]]

- ✅✅★★★★★ [**The last unknown is BOUNDED — V158 delivers ×1.7 to ×2.7, and Path 2 cannot cancel it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the RAM-LERP slope `s` did NOT need extracting (the model records two failed attempts): **the loop's own stability bounds it.** ✅ **The Path-2 loop is CLOSED, all 13 hops byte-verified** — `gp-0x6ad4`→`gp-0x6b94`→governor `0x453E0/0x454D2`→`gp-0x6b98`→observer→`gp-0x6bfc`→`gp-0x6bfe`→`FUN_00038148` (where the damper enters @`0x38150`)→`gp-0x6b70`→`gp-0x6ad6`→PID. ⚠ My first attempt failed two hops because I **guessed** `FUN_0003a382`'s extent as `0x3A620`; it is `0x3A8A7`. **Ghidra had it — ask, don't guess.** ⭐ **The bound**: the same `0.332×s` segment is in BOTH Path 2's route to the aggregator and the loop's forward path, and the `gp-0x6bfe` entry coefficient is exactly 1 ⇒ `L = G_gov·G_obs·0.332·s < 1` because the car is **stable** ⇒ with `G_gov·G_obs ≥ 1` (f′ alone is 2.174 hands-off), **s < 3.01 ⇒ Path 2 ≤ 0.614× Path 1 ⇒ net ≥ 39 % of nominal.** ✅ **V158's net creep damping is 1.05–1.96 ct/(deg/s), total 2.63–3.53 vs the 1.571 baseline = ×1.67–×2.25** — restate the ×2.74 headline as **×1.7–×2.7**. **The pumping does NOT cancel the damping.** ⚠ Still [BELIEF]: exact `s`, and the hand-traced net sign. ⊕ V167 halves the `0.204×s` term directly (net → 0.69–0.86), the sharpest test of this bound. ⭐ **METHOD: when a coefficient resists extraction, ask what the system's OBSERVED behaviour implies — a loop that demonstrably does not diverge bounds every gain inside it.**

- ⛔★★★★ [**`0xC63AE` is not a clean loop-gain lever either — same trap, different cell**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00038148` scales the whole Path-2 forward path by `cal(0xC63AE)` before the LERP (**virgin, 1024 on 142 images**), which unlike a per-term weight preserves the observer's relative weighting => it looked like THE clean loop-gain reduction, needing no V158 base. ⛔ **But `gp-0x6ad6` is the PID's FEEDBACK term**: `err = gp-0x4f60 - clamp(gp-0x6ad6)`, so shrinking it **moves a SUBTRACTION** — smaller feedback gives a **LARGER** error and a **LARGER** PID output. Loop-gain reduction and error growth push **opposite ways** and the winner depends on the same unknown `s` => **net ambiguous, not built.** ⭐ **The rule applied to myself**: *before lowering a scalar, ask what the sum is FOR* — in an aggregator it is a **gain**, here it feeds a **subtraction** and sets an **operating point**. ⊕ **Path 2 has NO clean cal lever**: per-term weights corrupt the model, the output scale moves an operating point, and `0xC63A0` is the sole exception only because `gp-0x6bd0` was exactly 0 at creep pre-V158.

- ⛔⛔★★★★★ [**I had the PID's P and D SWAPPED — the standing record caught it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I assigned `FUN_0003a382`'s three gain LERPs from **Ghidra's variable numbering** instead of the record. `MEMORY-PART3` says *“Stage A = P (153-256/1024), B = I (98/1024), C = D at 2048/1024 = 2.0”* and `MEMORY.md` says *“net lag −11 to −27 deg at 6-9 Hz, P:(I+D) ~2-5:1”* ⇒ **`0xC6B1E`=256 is P (not D) and `0xC6ADE`=2048 is D (not P).** Corrected: **|P| 8.00 · |I| 1.953 · |D| 0.098 ⇒ P:(I+D) 4.3:1, net −13.0°** — **inside both recorded ranges**, against my −1.7° / 33:1. ⭐ **Cross-check a fresh derivation against the standing record before publishing** — it is a control, and here the only thing between a wrong number and the handoff. ✅ **The verdict is UNCHANGED and STRONGER**: −1.7° was near-zero stiffness, **−13.0° is a LAG** — strictly worse for stability ⇒ V162/V163 stay superseded. ⚠ **But restate the margin**: the D gain `0xC6AE6` at u16 MAX (65535) yields **+8.3°**, not the +1.06° I claimed; it needs Kd~163 (Q10 167,170) to match P. Elimination holds (damping needs +90°) but the honest phrasing is *“tops out at +8°”*, not *“1300x too weak”*. ✅ **The Path-2 bound is INVARIANT** — `G_pid` appears in both Path 2's route and the loop supplying the bound, so it **cancels**: Path2 ≤ 0.615/(G_gov·G_obs), **V158 stays ×1.7–×2.7**. ⭐ A bound built from a STRUCTURAL relation survived an 8x error in one of its factors.

- ✅✅★★★★ [**Final verification: all six flyable builds check out cell by cell; and `gp-0x6bbe`'s “76 % of rail” is reconciled**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V158/V164/V160/V165/V167/V161** all match their build-report SHAs, every cell is as designed, and the V67 gate repoint `0x3AA96 = 0xfb` is present on all six so Lever B is reachable everywhere; doses at the operating point read **50 / 27 / 50 / 65 / 50 / 0** (V161's 0 is correct — it is the no-damper twin). All six `.rwd` present; no mandatory file over 200 KB. ✅ **Reconciliation**: the facade's *“`gp-0x6bbe` … already at 76 % of rail”* (≈389 counts) does **not** contradict the memory's *“~90 ct/(rad/s) + a 73.6 ct DC pedestal”* — the **damping** part is the **SLOPE** (1.571 ct/(deg/s)) and the **pedestal damps nothing**, so comparing V158 **by slope** (2.733 vs 1.571 ⇒ the ×1.7–×2.7 net) is right and comparing **by absolute counts** (50 vs 389) would wrongly credit a constant as damping. ⭐ **When two records give different numbers for one lane, check they are the same QUANTITY before calling it a contradiction — or before averaging them.**

- ✅★★★★ [**The cross-check audit is COMPLETE — every load-bearing number traced to its source**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ran the audit that caught the P/D swap over the rest of the session's figures. **6-9 Hz 0.859 · 9-12 Hz 0.604 · 15-22 Hz 0.549 · 0.5-3 Hz 1.192 NULL** (the V160 power calculation) and the **[0.18, 5.51]** split-half floor all reproduce **verbatim** from `eps_chain_lanes.py`; `gp-0x6ac0` = 99 [94,113], the 512 ceiling, dose 50, and f′ 2.174/0.346 all trace to the model or to on-car memory. ✅ **Only ONE fresh derivation disagreed with the record, and the record was right** — the P/D swap, which I had already written into a handoff. ⭐ **A fresh derivation is a HYPOTHESIS until checked against the standing record**; one in nine of mine was wrong. ✅ **The calibration search is COMPLETE**: every aggregator lane has a phase or structural verdict, every pointer-table family is attributed, all three complaints have a firmware answer or a reason there is none, and six cell-verified images cover every outcome of one drive. **The only remaining input is the drive.**

- ⛔✅★★★★★ [**The pre-registered scorer had a WINDOW bootstrap — fixed before the drive, not after**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the V158 pre-registration named `score_v133_creep.py`, whose contrast and guards are sound but whose `boot()` does `rng.choice(e[:, i], len(e))` ⇒ it resamples **INDIVIDUAL WINDOWS**, which overlap by `NW//2` and come from a few contiguous stretches. That is exactly what `feedback-episodes-not-windows` forbids. ✅ **Quantified**: on 6 episodes × 12 windows with two same-distribution arms, the **episode** bootstrap gives **[0.23, 2.61]** and the **window** bootstrap **[0.58, 1.48]** ⇒ the window CI is **0.38× as wide, 2.6× too confident**; both span 1.0 here but an effect near the boundary would be called significant when it is not. ✅ **`rlog-tools/score/score_v158_creep.py`** fixes it: episode bootstrap (unit-tested), **primary band 6-9 Hz** (the ratchet, what the damper targets) with 18-22 Hz as a **built-in control** since Lever B is unchanged V122→V158, retained 30-40 Hz guard that now refuses to print a verdict on failure, a **per-window speed census**, a **`--null` split-half self-test**, and a refusal below 4 episodes per arm that tells the operator **more windows will not help — alternate engaged and manual STRETCHES**. ⭐ **Audit the tooling a pre-registration NAMES before the drive** — auditing it afterwards is how a null becomes a result.

- ✅✅★★★★★ [**The scorer is validated on real data — and it caught itself over-claiming**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ran the new pipeline end-to-end on **r24, a real V122 creep drive**. ⛔ **My own scorer over-claimed**: it printed *RESOLVED* for 6-9 Hz **0.20 [0.04, 0.86]** while its own split-half null read **0.41 [0.06, 17.06]**, a floor 280× wide — it tested only *does the CI exclude 1.0* and ran the control **only when asked**, exactly what `feedback-run-the-control-before-the-measurement` forbids. **Fixed: the null is computed automatically and GATES the verdict.** ✅ **Validated 3 ways on r24**: reproduces the recorded **3.88** for 18-22 Hz; **RESOLVES** that known-real excess (null 0.19 [0.02,0.76]); and **REFUSES** the 6-9 Hz 0.20 that a naive CI would have claimed (null 0.20 [0.00,3.22]). Control flat, speed gap 0.14 km/h. 🛑 **DRIVE REQUIREMENT**: on r24's **10 engaged / 5 manual episodes the 6-9 Hz band — V158's PRIMARY target — cannot be resolved at all**, and the manual arm could not even support a null (5 episodes, needs 8) ⇒ **a V158 drive shaped like r24 would produce NOTHING on the band that matters.** The drive must **alternate engaged and manual creep 8+ times each**; more *windows* do not help, only more *episodes*. ⭐ **A new instrument is not trustworthy because it fixed the old one's bug** — run it on a route whose answer you already know.

- ✅✅★★★★ [**The drive-side toolchain is complete and de-hardcoded — three commands**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **(1)** `extract_route.py --route N --prefix ... --segments n --build V158` writes the cache **and verifies it is scoreable**, failing loudly at extract time rather than after the drive; **(2)** `score_v158_creep.py rN` gives the episode bootstrap with a **split-half null gating every verdict**; **(3)** `audio_engaged_vs_manual.py rN` gives the acoustic channel, PCM aligned by `logMonoTime` and split on `cc_lat`. ✅ Dependencies verified present (`zstandard`, `cereal`, numpy, scipy), **635 rlog segments**, **23 routes** with both rlogs and a cache. ⛔ **Each had the SAME class of defect — a per-drive constant nothing checks**: one extractor FILE per drive (with `extract_r24.py`'s docstring still saying *“Cache routes 22 and 23”*), a **window bootstrap 2.6× too confident**, and a **hardcoded `ROUTES = {r22, r23}`** in the audio tool that would silently analyse the WRONG drive. All three now derive the constant from the data on disk or refuse loudly; the audio resolver was verified to reproduce both hardcoded prefixes exactly. ⭐ **A pipeline that cannot be run without editing it will eventually be run after editing it wrong.**

- ✅✅★★★★ [**The audio channel is validated — and it was pointed at the wrong band and speed**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — audio runs (**16,364 blocks aligned on r24**, `zstandard`/`cereal` present, 635 segments), but `audio_engaged_vs_manual.py` deliberately answers a different question: it abandoned engaged/manual on r24 as *“hopelessly speed-confounded … uniform +10 dB”* and substituted a within-engaged **21-26 Hz** contrast at **28-82 km/h** — the **vibration** band at a speed where **V158 is architecturally inert**. ✅ **`audio_creep_v158.py`**: primary = **engaged vs manual at CREEP (1-24 km/h), speed-matched**, which the drive card's matched manual creep segment makes valid; **REFUSES if the arm medians differ by >2 km/h**, naming the r24 artefact; fallback = within-engaged **high-vs-low 6-9 Hz** (the validated design retargeted to the ratchet), labelled as unable to separate *“damper worked”* from *“less ratchet occurred”*. 🛑 **It caught a real confound on first run**: r24 has 271 engaged / 325 manual creep audio windows, yet the arms sat **2.43 km/h** apart after matching ⇒ refused. **So “some creep engaged and some creep manual” is NOT enough — both arms must be the SAME STRETCH at the SAME SPEED.** Now in the drive card.

- ⛔✅★★★★ [**No stable acoustic marker across routes — and speed-matching alone is not enough**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — r24's **2389-2688 Hz +4-5.5 dB** cluster looked like a ratchet signature; it does **not replicate**: r23 shows **408-416 Hz**, ra6 **908-920 Hz**, r22 a scatter. ⇒ **there is NO stored acoustic signature to compare a V158 drive against; the acoustic endpoint must stay WITHIN-DRIVE**, like the band scorer. ⛔ **And ra6 exposed a hole in my own guard**: it **passed** the 1.18 km/h speed check yet showed **+3.00 dB at 20-50 Hz AND +3.21 dB at 2000-5000 Hz** — the whole spectrum lifted together, i.e. a global level difference (mic gain, window, engine load, surface). A speed match does not exclude that. ✅ **UNIFORMITY GUARD added and validated**: **ra6 spread 1.55 dB at level +3.21 ⇒ FAILS**; **r22 spread 1.23 dB at level +0.34 ⇒ PASSES**. The test is **large level AND small spread**, not spread alone. ⭐ **Every contrast needs a negative control, including the one you just built** — I added the null gate to the band scorer after it over-claimed, then built the acoustic tool with no equivalent and it over-claimed the same way on the first route that could expose it.

- 🛑🛑★★★★★ [**Power analysis on real data — the 6-9 Hz scorer resolves 1 route in 23**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ran the validated scorer over **every cached route**: **10 of 23 have scoreable creep arms, and exactly ONE (ra6, 11.58 [3.53, 33.37]) resolves 6-9 Hz against its own null.** Seven of the ten have CIs spanning 1.0 ⇒ **the endpoint is dominated by between-episode variance, not firmware.** ✅ **Set expectations before the drive: the band scorer is UNLIKELY to detect V158.** That is the measured noise floor, not a defect in the build or the tool, and it is why **the operator's report is the primary endpoint and usually the only one.** ⭐ **The binding constraint is identified exactly**: engaged episodes run **9-22** and are never the limit; **manual episodes run 3-7 and are the limit on every scoreable route**, 13 routes have **no matched creep arms at all**, and the one route that resolved has **the most manual episodes (7)**. ⇒ **8+ separate MANUAL creep passes is the single cheapest change that most improves the drive's power** — more than any additional engaged driving. ⊕ r24's 0.20 [0.04, 0.86] would read as a strong engaged reduction on a naive CI; the null correctly rejects it.

- 🛑🛑★★★★★ [**Score V158 at 18-22 Hz, NOT 6-9 Hz — the instrument can only see one of them**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — both bands through the null gate on 9 routes: **18-22 Hz resolves 7/9, 6-9 Hz resolves 1/9** ⇒ the grind band is a **~7x more sensitive instrument**. ✅ And it carries a **large REPLICATED phenomenon**: engaged/manual at 18-22 Hz is **>1 on ALL NINE routes, spanning 3.88 to 742** — engagement multiplies that band at creep on every drive measured — while **6-9 Hz scatters around 1 (0.20-11.58) with no consistent direction.** ⚠ **CORRECTION TO MY OWN PRE-REGISTRATION**: I called 18-22 Hz a *“built-in control that should not move”* since Lever B is unchanged V122→V158. **Wrong** — V158's damper is `-sign(rate)·f(|rate|)`, a **BROADBAND VISCOUS** term whose LERP is on rate MAGNITUDE not frequency, so it opposes motion at ALL frequencies and should reduce 18-22 Hz too. ⇒ **18-22 Hz is an ENDPOINT, not a control, and it is the only one the instrument can resolve.** ✅ Revised plan: **PRIMARY instrumented = 18-22 Hz null-gated**; 6-9 Hz secondary (expect NOT RESOLVED — that is the floor); 30-40 Hz control; operator's report still primary overall. ⚠ Attribution holds **only because Lever B is byte-identical across V122/V158** (verified); a future build touching both makes 18-22 Hz unattributable.

- ✅✅★★★★★ [**There IS a working cross-build instrument — 18-22 Hz eng/man has tracked the kit for six builds**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — pairing each route's 18-22 Hz within-drive engaged/manual ratio with its cached build tag: **V102 742 → V104 328 → V106 87 → V107 58 → V112 7.1 → V122 3.9** — **six consecutive builds, strictly monotone, 191x, corr(build, log excess) = -0.920**, against a within-build scatter of **1.84x** (V96's two routes) ⇒ **signal/noise ~104x**. ✅ **It tracks the operator's own verdict** (V112 *“best yet, least ratcheting ever”*, V122 better still). ⭐ **This does NOT contradict the recorded 20-36x between-build floor**: that floor was on **absolute band amplitude between routes**, this is a **within-drive RATIO** that cancels road, tyre and weather first — **same lane, different quantity**, the third such case this session. ✅ **So V158 CAN be scored against V122's 3.88**: predict LOWER (the damper is broadband viscous), detectable if the move exceeds ~1.84x; no effect looks like ~3.9. ⚠ **[BELIEF] the 1.84x rests on ONE within-build replicate** and everything inherits it. 🛑 **TIME IS COLLINEAR WITH BUILD NUMBER** — tyre wear, season and the operator's driving all advance with build order, so *“the builds caused it”* and *“something else advancing with time caused it”* are **not separated** by this data.

- ⚠⚠★★★★★ [**The 18-22 Hz instrument is running out of range — walking back last tick's detection threshold**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — replaced the single cross-route replicate with **nine within-drive split-half replicates**: **median 1.72x, p90 2.93x, max 3.60x** — and **r24 (V122, THE REFERENCE) is the worst at 3.60x** (halves 2.10 vs 7.56). The old 1.84x was accurate but was **the wrong number to use**: reproducibility is worst where the excess is smallest, and V122 is the smallest. ⚠ **So V158 must read ≤ 2.26 (median floor), ≤ 1.32 (p90), or ≤ 1.08 to clear r24's own — and 1.0 means the excess is GONE.** ⇒ **V158 is instrumentally detectable only if it very nearly ELIMINATES the engaged excess.** Last tick's *“detectable if the move exceeds ~1.84x”* was **too generous; corrected.** ✅ **And it reframes the six-build trend**: per-step, V102→V104 2.27x marginal, **V104→V106 3.76x RESOLVED**, V106→V107 1.50x below floor, **V107→V112 8.16x RESOLVED**, V112→V122 1.83x marginal ⇒ **individual steps are mostly NOT resolved; the CUMULATIVE 191x is.** Notably **V112→V122 is marginal**, so the endpoint is *consistent with* the operator's V122 verdict rather than confirming it. ⭐ **An instrument validated over a 191x range is NOT thereby validated at the bottom of that range — calibrate the floor AT the operating point you will use it at.**

- ⛔★★★★ [**Paired speed-matching is WORSE — and the estimator tuning stops there**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — matching each engaged window to a nearest-speed manual partner should cancel more variance than the current percentile filter. Tested on the same nine routes it is **~1.7x worse at the median and 2x worse at p90**: **UNPAIRED median 1.72x / p90 2.93x / max 3.60x** vs **PAIRED 2.97x / 6.27x / 11.43x**. Two causes: it **discards every window without a partner within 0.75 km/h** so *n* collapses, and a **median of individual ratios** is noisier than a **ratio of medians** since each pair carries two windows' noise instead of two pooled distributions. ⭐ **Stopping the tuning deliberately**: narrower bands, log-space averaging and trimmed means remain untried, and with **nine routes** differences under ~20 % are indistinguishable — **selecting the best of several estimators BY THE SAME STATISTIC used to judge them is the selection that manufactures an apparent improvement**, the tuning analogue of the window bootstrap already removed this session. **One principled alternative was pre-specified, tested and lost; the floor stays 1.72x / 2.93x / 3.60x, fixed BEFORE the drive rather than chosen after it.**

- ⚠★★★★ [**An alternative first flight exists — V137, already built and unflown**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — correlating the measured 18-22 Hz excess against pre-specified load-bearing cals over the 8 flown builds: **knee r=-0.730, K1 r=-0.648, alpha2 r=+0.657**, everything else constant. 🛑 **EXPLORATORY: n=8 and knee/K1/alpha2 moved TOGETHER at V112 and V122 ⇒ collinear, not separable, no causal claim.** ✅ Two things survive: **(1) Lever B off vs on** — V102 with `0xC6446`=512 reads **742**, the worst in the set, and restoring it at V104 drops to **328**, consistent with V88's single-variable result ⇒ **Lever B stays**; **(2) the knee/K1/alpha2 axis moved the endpoint twice** (57.9 → 7.1 → 3.88) and is **the only axis with a replicated on-car association** with it. ⚠ **So V137 (V122 + alpha2 8→5, ONE halfword) and V138 (→2) are built, unflown, and sit on that axis.** V158 vs V137 answer different questions: V158 has a **strong mechanism** (broadband viscous damping, quantified) but **no on-car history at creep**; V137 has **two on-car steps in the right direction** but **confounded support**. **V158 stays the recommendation**; V137 is the defensible alternative if the operator prefers continuing what has been working. Both cal-only and reversible.

- ✅✅★★★★★ [**The V158-vs-V137 ambiguity is RESOLVED — V137 is predicted BELOW the instrument floor**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — alpha2 has a real mechanism (an **EMA coefficient `>>6` read at `0x41626`**; a = alpha2/64, so V122's 8 puts the corner at **19.9 Hz, INSIDE the 18-22 band**), but **open-loop it is 20x too small**: 22→8 gives x0.76 while the measured excess fell x0.067. ✅ **Priced with the kit's own `eps_closed_loop_sim.ratio_filter`** — lane-only, avoiding the plant, which its header says is identified **only 5-13 Hz** and raises above that: **V112→V122 lane 0.8172 vs measured x0.55 (validates — same direction, right order, loop makes it slightly larger)**, **V122→V137 = 0.7462**, **V122→V138 = 0.3364**. ⛔ **Against the measured floor (median 1.72x): V137's 1.34x reduction is BELOW it — not detectable — while V138's 2.97x is at p90, marginal.** ⇒ **V137 would produce “not resolved” and teach nothing; I overstated it as a defensible alternative last tick and withdraw that.** **V158 is the flight**; if the alpha2 axis is wanted later, **V138 is the version worth a drive** and is already built. ⚠ `ratio_filter` prices the `gp-0x6b26` LANE, not the measured `cs_rate` band — input-side prediction, not a forecast. ⭐ **Before handing the operator an either/or, check whether an existing tool can price both options.**

- ✅★★★★ [**All 24 unflown builds triaged against the detection floor — only V138 clears it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — swept every built-but-unflown image through `eps_closed_loop_sim.ratio_filter`: **V137 x1.34 (below the 1.72x floor)**, **V138 x2.97 (CLEARS)**, and **the other 22 — including V158/V160/V164/V165/V167 — do not touch the `gp-0x6b26` lane at all**, so the simulator cannot price them. That is a **limit of the tool, not a defect in the builds**: it covers the b26 lane only, V158's damper is `gp-0x6bd0`, and the golden model takes `gp-0x6bd0` as an *input* rather than simulating the cascade ⇒ **V158's prediction rests on the physical-units argument (0.000 → 1.05-1.96 ct/(deg/s) net) and no kit tool converts that into a predicted band ratio.** ⚠ **Real tension, stated rather than hidden**: **V158 has the strongest mechanism but an unpredictable, likely-sub-floor instrumented outcome**; **V138 has a weaker mechanism (an EMA corner 19.9→5.0 Hz) but a tool-backed prediction that clears the floor.** ⇒ **to FIX the car, fly V158; to LEARN something instrumented, V138 is more informative.** V137 stays withdrawn. **V158 remains the recommendation because the PRIMARY endpoint is the operator's report, not the instrument** — what he feels can move when the band ratio cannot resolve it. ⭐ **“Which build should fly” and “which build will produce a readable measurement” are DIFFERENT questions; this kit has been conflating them.**

- ✅✅★★★★★ [**V158 DOES have a falsifiable prediction — it is just one-sided**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the damping arithmetic gives total creep viscous **1.571 → 2.637-4.304 (x1.68-2.74)**, so **IF** firmware damping is a real share of the plant **and** 18-22 Hz is resonance-limited, the band goes **3.88 → 1.42-2.32**; if mechanical damping dominates it collapses toward **x1.00**. ⇒ honest range **x1.00-2.74 reduction**, and against the floor (1.72x median / 2.93x p90 / **3.60x on r24 itself**) **a REDUCTION IS LIKELY UNRESOLVABLE.** ⭐ **But the other direction is sharp: NOTHING in the damping account predicts an INCREASE** — every V158 mechanism adds a term opposing motion in Path 1. So **V158 reading clearly ABOVE 3.88** (outside [1.60,10.87] high, or above r24's own half-split 7.56) **falsifies the damping account and is evidence for the Path-2 PUMPING copy dominating** — the one named risk, which has a **built answer in V167** (`0xC63A0` 1024→512) that halves exactly that term. ✅ **An underpowered two-sided test becomes a usable ONE-SIDED discriminator, and the branch it discriminates into already has its build cut.** ⚠ Assumptions stated: firmware damping is a non-trivial share (unmeasured), the band is resonance-limited (unverified there), Path-2 net is in the 39-100 % window. **Any failing collapses the reduction toward x1.00; none can produce an increase** — that asymmetry is the trustworthy part.

- ✅✅★★★★★ [**A better endpoint: Q of the 15-25 Hz resonance — and it makes V158 DETECTABLE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — testing whether the band is resonance-limited both **confirmed the assumption** and produced a better instrument. ✅ **Strongly peaked on all routes** (prominence **32-610x** above the 28-40 Hz floor) ⇒ amplitude does scale as 1/damping there. ✅ **Q has fallen 9.00 (V102) → 4.50 (V122)** ⇒ **ζ = 1/(2Q) rose 0.056 → 0.111, a 2x damping increase** — a DIRECT measurement of the kit's builds adding damping, independent of the engaged/manual ratio. ⭐ **And Q is a far better instrument**: split-half **median 1.20x / p90 1.50x / max 2.00x** vs the band ratio's **1.72x / 2.93x / 3.60x**, because Q is a **SHAPE** parameter immune to the level shifts that inflate the ratio (the same shifts the acoustic uniformity guard exists to catch). ✅ **V122 Q = 4.50; V158 predicts 1.64-2.68, a x1.68-2.74 change against a 1.20x floor ⇒ DETECTABLE**, where the band ratio was not. Added to the scorer as the PRIMARY instrumented endpoint with per-drive split-half and a prominence<3 guard. ⚠ **Wrinkle**: r24's POOLED Q is 4.50 while its halves give 6.00/6.75 — pooling shifts the half-power points, so the pooled estimate is not the average of its halves; the comparison stays valid (same procedure both sides) but split-half may **understate** pooled uncertainty. ⊕ One-sided logic unchanged: **Q RISING above 4.50 falsifies the damping account** → Path-2 pumping → V167.

- ✅★★★★ [**Q's build-ordering is NOT gain-confounded — and the gain cost ~2x in damping ratio**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the gain is **constant at 5346 across V102→V122**, so Q's fall **9.00 → 4.50** is attributable to the other cals, not the gain; and **V158 does not touch `0xC6CD0`**, so the V122→V158 comparison is clean on this axis. ✅ **V122's Q = 4.50 is BELOW the 4x-gain baseline mean of 4.94** ⇒ the builds recovered the damping the gain raise cost, and slightly exceeded it. ⚠ **This CHALLENGES a standing claim**: memory says the LKAS gain *“scales EXCITATION, not loop gain”*, but **Q is a SHAPE parameter** — pure excitation scaling would leave Q unchanged, and Q went **4.67 → 9.00** at the 4x→6x step: the peak got **sharper**, not just taller. **A gain that changes Q is acting inside the loop**, which gives the operator's 8x experience (more grinding) a mechanism it lacked — raising gain **reduces the damping ratio** rather than merely amplifying. ⚠ **[BELIEF] confounded**: V96→V102 spans V100/V101/V102, not the gain alone, and only **two routes** sit at 4x. **What would close it**: a 4x-gain route with otherwise V122-like cals, or a single-variable gain build the operator has ruled out. ⊕ **No change to the recommendation** — the gain stays frozen at 6x; this only supplies the missing *why* behind the existing rule against 8x.

- 🛑🛑★★★★★ [**My ratchet Q was a WINDOW artefact — and it explains every 6-9 Hz failure**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — there **is** a real resonance at 5-12 Hz (peak 5.5-8.6 Hz, prominence **17-68x** on all 9 routes), but Welch at `nperseg=128`/100 Hz gives **df = 0.781 Hz**, so the largest resolvable Q is `f/(2df)`: **5.0 at 7.8 Hz** (I measured 1.17-5.50 — **pinned at the ceiling**) vs **13.4 at 21 Hz** (measured 4.50-9.00 — **valid**). Memory's ring-down gives the ratchet **Q = 14-29**, whose half-power width is 0.27-0.56 Hz, **narrower than one bin** ⇒ **my 5-12 Hz Q values are the WINDOW's width and are withdrawn; the 15-25 Hz Q stands** and everything built on it is unaffected. ⭐ **This explains a long-standing puzzle**: every 6-9 Hz endpoint has performed badly (the ratio resolves 1 route in 9) **because the ratchet is too NARROW to resolve in a 2.56 s window** — a **resolution** problem, not a noise problem, so **more episodes cannot fix it; only a longer window can.** ✅ **Drive requirement**: resolving Q=29 at 7.8 Hz needs `df ≤ 0.134 Hz` ⇒ `nperseg ≥ 744` ⇒ **≥ 7.4 s of CONTINUOUS engaged creep per window**, so passes should be **10-15 s each**, which sits alongside the existing 8+-passes rule. ⊕ `peak_q` now returns **NaN when Q exceeds 0.6x the window ceiling**, so a window artefact can never again be reported as damping.

- 🛑🛑★★★★★ [**The spectral TILT confounded every 6-9 Hz endpoint — three measures withdrawn, one survives**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the wheel-rate signal is RED, **1/f^0.80 to 1/f^2.37** across routes, and coloured noise with **NO resonance** reproduces what the old measures reported (1/f^2.0 gives prominence **64.9**, fitted Q **1.00**, r² **0.710**; real routes gave 12.2-173.3 / 1.0-17.6 / 0.28-0.87). ❌ **Withdrawn**: fixed-floor prominence (large by construction on a red spectrum), fitted Lorentzian Q, and long-window half-power Q (**white noise alone returns Q 21.7-29.1** at nperseg=1024 and **cannot separate true Q=5 from Q=20**) ⇒ the 0.6×ceiling guard is **insufficient**, it rejects white but not coloured. ✅ **SURVIVES: slope-corrected excess with a SLOPE-MATCHED null** — fit the route's own power law over 3-40 Hz excluding the test bands, then null at that route's slope: **GRIND 15-25 Hz excess 9.9-421.9 vs null 2.6-4.1 = REAL on 9/9** (3-100x margin); **RATCHET 5-12 Hz 2.0-8.9 vs 2.7-4.1 = real on only 6/9** (1.1-2.3x). ⇒ **the ratchet is 10-50x weaker IN THE WHEEL-RATE CHANNEL** — a signal-strength problem, so more episodes cannot fix it, a different channel might. ✅ **Excess orders by build: V91 9.9 → V102 421.9 → V122 23.2**, the 42x jump landing on the 4x→6x gain step, clawed back to 2.3x above V91. 🛑 **RETRACTS a committed inference rule**: *“Q rising falsifies the damping account”* is wrong because **half-power Q is NON-MONOTONE** — its null sits ABOVE the data (real 13.7-34.7 vs null p95 **58-78**), so a rise cannot distinguish “damping failed” from “damping worked”. ✅ **Drive requirement is now a number**: split-half floor **2.23x at 3 windows/half, 1.57x at 6**; a 15 s pass yields 4 windows and a 10 s pass 2 ⇒ **8 passes of 15 s = 32 windows (adequate), 8 of 10 s = 16 (marginal)**. ⊕ `rlog-tools/score/score_band_excess.py`.

- 🛑🛑⭐★★★★★ [**The ratchet is 100 % FIRMWARE-CAUSED — and untouched by every lever the kit has pulled**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — three results, one conclusion. ✅ **(1) It is in the TORQUE channel, not wheel rate**: ratchet margin over each channel's own slope-matched null is **tq 7.62 · cs_tq 7.42 · ws_fr 4.41 · cs_rate 1.03 (CHANCE)** — REAL on **9/9 routes** in `cs_tq` (9.8-67.8 vs null 2.6-4.1) and **absent from all three COMMAND channels** (0.56-0.67). ⚠ **Retracts the “and ANGLE-RATE” half** of the old claim — every 6-9 Hz endpoint was reading the wrong channel. 🛑 **(2) Thirty-plus builds have not moved it**, and the test had power: **GRIND post-V102 ρ = −0.94, p = 0.005 in THREE channels** (the kit's builds are working), but **RATCHET ρ = −0.14 (p 0.79)** with spread 6.9-8.3x against a **1.63-1.91x** floor, and frequency pinned at **8.64 Hz ± 7.4 %** across V91→V122; the weak full-range trend is **positive**. ⭐ **(3) And yet it exists ONLY when engaged**: engaged arm beats its null **7/7**, manual arm **0/7** (manual sits BELOW its null on every route), speed-matched ratio **median 19.9x [4.82, 35.64]** ⇒ **engagement CREATES it, it is not a mechanical mode being amplified.** ⚠ **Supersedes “engaging amplifies 6-9 Hz by 2.8x”** — that came from the tilt-confounded band ratio. 🛑 **The search is reframed**: not *“is there a lever?”* but **“which engaged-only path is live at 8.6 Hz that V91-V122 all left byte-identical?”** — intersect engaged-gated code with cells no build has touched, then phase-test each at 8.6 Hz.

- 🛑🛑★★★★★ [**The Coulomb relay is a GRIND lever, not a ratchet lever — the two symptoms dissociate**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the nine scored routes span the relay knee `0xC40BC` over **10x (300-3000)** and the ratchet does not respond: **ρ = −0.06, p = 0.874**, while the **grind does: ρ = −0.69, p = 0.039**. ✅ **Gain-matched (all at 5346, so the 4x→6x step cannot explain it): knee 300 → 1800/3000 cuts the GRIND 41.2 → 14.5 = 2.8x and moves the RATCHET 39.1 → 33.2 = 1.18x, inside its 1.63x floor.** ⚠ Confound stated: knee and friction `0xC40D2` move together there, so the *grind* effect is knee-or-friction — but neither moves the ratchet (friction ρ +0.30, p 0.44). ⇒ **the relay attribution for the RATCHET is withdrawn**; it stays a good grind lever. ⭐ **Across 31 images V91→V122 only 278 bytes EVER changed** (0.027 %); `0xC5000` model coeff, `0xC7000`, `0xC9000` damper tables and `0xCC000` gain arrays are **100 % untouched**. Those 278 bytes include the LKAS gain, Lever B, the relay knee and friction — they moved the **grind 42x and the ratchet nothing** ⇒ **the two are DISSOCIATED and every lever found so far is the GRIND's.** ⭐ **Sharpest clue**: the ratchet is in **torque (tq 7.62, cs_tq 7.42)** and the wheels (ws 3.9-4.4) but **NOT in angle (0.79-1.03) and NOT in command (0.56-0.67)** ⇒ **a torque oscillation that twists the bar without moving the wheel** — which is what *ratcheting* rather than *shaking* feels like. ⚠ **[BELIEF]** a motor-torque disturbance downstream of the command, upstream of the bar, engaged-only. **What would close it**: phase-test at 8.64 Hz each engaged-gated contributor to the motor-torque sum, restricted to the untouched set.
- ⚠★★★★ [**Driven vs self-excited: INCONCLUSIVE, leaning driven**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — band-specific coupling (coherence 7-10.5 Hz minus a 30-40 Hz control band, `co_tqcan`→`cs_tq`, vs phase-shuffled surrogates) is **specific on 6/9 routes** but the pooled **95 % CI [−0.021, +0.176] CROSSES ZERO**. ⊕ **Raw coherence is worse than useless here** — the control band scores as high as the ratchet band, so command→torque coupling is **broadband** and any coherence claim without the control-band subtraction is meaningless. ⭐ **`r1e`, the only route with 14 windows, gives +0.522 against a shuffled p95 of +0.097 (5x margin)** while the 7-window routes scatter around zero — the signature of a real effect at insufficient power, not a null. ⇒ **closes with more continuous engaged-creep windows**, the same requirement every open question here has. ⊕ `rlog-tools/score/ratchet_command_coupling.py`.
- ✅✅★★★★★ [**The ratchet scores from ONE 15-second episode — the 8-pass spec was the wrong endpoint**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the 8-passes-of-15 s spec was **unbuildable** under the design law (the operator stops the drive the moment the symptom persists) **and aimed at the wrong statistic**. The engaged-vs-manual result is **PRESENCE/ABSENCE** (7/7 engaged clear their null, 0/7 manual), so killing the ratchet is an **~8x** move from excess ≈33 to below null ≈4, not a 1.7x ratio. ✅ **Measured: a single continuous 15 s engaged creep episode detects the ratchet in 11/11 episodes = 100 %**, excess **25.5-155.7** vs slope-matched null **1.9-4.9** = a **5-65x margin** (20 s: 5/5; 30 s: 4/4). ⇒ **ONE pass answers the primary question**; more passes only sharpen the graded *how much smaller*. ⚠ The **grind's** margin on the flying build is only 3.5x (V122 excess 14.0 vs null ≈4), so a marginal grind read from one episode is **inconclusive, not negative** — the ratchet's is not.
- ⭐★★★★★ [**The ratchet's prime suspect is the BASE-ASSIST MAP — and its lane is NOT memoryless**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the DATA (engaged-only, in torque not angle, untouched by all 278 changed bytes) and the CODE (the loop-topology census: `Z=(Z0+P·F)/(1−P·L)`, `Q_eff/Q_passive = 40/2.8 = 14.3`, and **`gp-0x6b86` is the largest torque-fed term — ±0x3000, 5.8-7.8x the ENTIRE PID**) point at the same lane. ✅ **Its slope cap `0xC6384` = 2048 is byte-identical on ALL 161 IMAGES** (as are `0xC6382`=41 and `0xC6200`=8192) ⇒ **the largest `L` lever has never been moved** — exactly the profile of a cause 30+ builds could not have touched. 🛑 **CORRECTS the census**, which priced this lane as MEMORYLESS: `FUN_000352b4` adds a **parallel LAGGED branch** (`iVar24 += (diff*0x80 − iVar24)*k >> 11`, state `gp-0x381c`, 1 kHz) to the direct path ⇒ a lead-lag compensator, so `|L|` from the slope alone is incomplete. ⭐ **Its pole is ENGAGEMENT-SELECTED** — k = `0xC6382`=41 when return-centre≠0 (manual), else the flat LERP `0xC6906..`=20 (engaged, since return-centre is dead there): at 8.64 Hz **engaged |H| 0.1779 ∠−78.20° vs manual 0.3491 ∠−68.02°** (closed form and the integer recursion agreeing to 4 dp), so engaged lags **10.18° more**, moving `1−P·L` the right way (1.798→1.713). 🛑 **But that is only ~5 % against an observed ~20x contrast — REAL, right-signed, and FAR TOO SMALL to be the mechanism.** ❌ **The float 2nd-order section is not it either**: it is **Honda's, shipped DISABLED** (`0xC649B` stock = 0), kit-enabled on 58/161 images, and vs the ratchet **ρ +0.26 p 0.500** (coeffs ρ −0.31..−0.45, all p≥0.226). 🛑 **Blocker**: the 10-knot curve is **RAM-resident** (`gp-0x641e..`/`gp-0x6444..`) so the local slope — which sets `|L|` — is not image-readable and GATE 2 on `0xC6384` cannot be completed. ⊕ **`search_instructions` returned ZERO for stores there; a raw byte scan across BOTH gp encodings found 27 accesses** — another false null.
- ⚠⭐★★★★ [**The assist-curve initialiser is unlocated — but `gp-0x69a6` answers GATE 2 directly**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — both byte-scan leads are ruled out: `0x38FD0`/`0x38FEE`/`0x39522` are **`st.h r0`, store-ZERO with a lockstep shadow** (shadow = knot − 0x184C, mismatch → `FUN_0006b9fa`) = a CLEAR path; and `0x39A0C` blends the knots toward `ep = tp+0x7564 = 0xC6564`, which is **ZERO on all 161 images** ⇒ a **fade-out ramp**, not an initialiser. ✅ **Anchor confirmed**: `0xC6384` reads 2048 ⇒ tp = 0xBF000, so the 0x1000 trap is not in play. ⊕ **`0xC6520-0xC6560` is a float32 array stored as (lo16,hi16) halfword pairs** (`0 16840`=25.0, `0 16256`=1.0) — a naive u16 read of it looks like noise. ❌ No monotone 10-knot table bounded by the 8192 input clamp exists in `0xC6400-0xC6700`. ⭐ **CHEAPER ROUTE**: the gate only asks *does the 2.000 cap BIND at creep?*, and `FUN_000352b4` already keeps the running max of the capped per-segment slope in **`gp-0x69a6`** ⇒ **`gp-0x69a6` == 2048 means the cap binds** — a ONE-CELL telemetry read, answerable on-car in a single episode. ⚠ **[BELIEF]** it does bind: domain-average slope `12288/8192 = 1.5` vs cap **2.000**, so it bites only on segments steeper than 1.33x average, the normal shape of a power-assist curve. ✅ **Recommendation unchanged: V158 flies first** — it targets the GRIND, which is moving (ρ = −0.94, p = 0.005, three channels).
- ⭐⭐★★★★★ [**The assist curve IS in the image, the 2.000 slope cap BINDS, and GATE 2 passes**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the curve is **initialised data copied ROM→RAM at boot** (hence only 3 `st.h` hit the 20-knot block, 2 of them clears). Found image-wide by its required shape: **`0xCE47A` X `0,25,60,100,150,250,450,900,1800,4150` / Y `0,154,338,460,549,635,702,766,824,857`, slopes `6.16 5.26 3.05 1.78 0.86 0.34 0.14 0.06 0.01`** ⇒ **cap 2.000 BINDS on 3/9 over X 0-100**; `0xCF372` max 16.37 binds 4/9; `0xCF3CA` max 11.97 binds 3/9 (+3 duplicates = the mode pointer-table family). ✅ **The cap is NOT inert — it pins the small-signal gain at exactly 2.000, the CEILING value of `s` in the census**, so the loop's largest term sits permanently at its maximum. ✅ **All six records byte-identical across 161 images**; `0xC6384` = 2048 on all 161 (exact u16 — my earlier 40-byte window check spanned `0xC63A0`/`0xC63AC` and wrongly showed 5 variants; **my error**). ✅ **GATE 2 PASSES, anchored on the MEASURED Q ratio** (the census phase gives the WRONG SIGN — `|1−P·L|`=1.92>1 contradicts the measured 93 % cancellation, and `P`'s phase is not in the image): from `Q_eff/Q_passive = 14.3` ⇒ `|1−P·L| = 0.0700`, `P·L = 0.9300`; then **cap 1536 → 3.4x more damped, 1024 → 5.7x**. Phase passes because the map term is a **real gain** — lowering the cap scales `|L|` **without rotating it** ⇒ monotone, no reversal. ⚠ [ASSUMPTION] `P·L` real-positive at the peak, which the measurement requires. 🛑 **FEEL TRADE**: the cap binds at LOW torque ⇒ **heavier steering near centre**, against a standing constraint — but the curve is **uncapped above X≈450 so peak authority and max rates are untouched**, and the map is **driver-torque fed**, not the LKAS lane ⚠ [BELIEF — `gp-0x6b4a`'s provenance unestablished]. ⭐ **First dose 1536 (1.5x), predicted 3.4x** — smallest step clearing the one-episode margin.
- ✅✅★★★★★ [**V168 BUILT — the base-assist SLOPE CAP, a lever class the kit has never tried**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V158 + `0xC6384` 2048→1536** (2.000x→1.500x), image `058dd64a…` .rwd `0f0ace3b…`, **ONE payload byte** (`0xC6385` 08→06 — high byte only; the build asserts the VALUE, since the hardcoded-2 byte-count assertion fired here on the first run), 35/35 assertions, CRC 50/50, all six curve records untouched. ⭐ **New lever CLASS**: the arc is V38-52 authority/caves · V53-61 probes · V62-73 rate lane · V74-84 damper · V85-122 friction/knee/relay · V133-167 damper shape — **all on non-dominant terms**; `gp-0x6b86` is **5.8-7.8x the entire PID**. ✅ **Aimed at the RATCHET specifically**, because grind and ratchet dissociate (grind ρ=−0.94 p 0.005 post-V102; ratchet ρ=−0.14 p 0.787). Built on V158 so **both symptoms score from the SAME 15 s episode in different bands** — separated by the instrument, not the build. ✅ **Pre-registered three-way outcome**: excess below null = ratchet gone; unchanged = **falsifies the real-positive `P·L` assumption** and exonerates the map; RISES = `P·L` not real-positive, revert. **All three readable from one pass** (11/11 detection, 5-65x margin) — no uninterpretable branch. 🛑 **Feel cost**: heavier steering near centre; uncapped above X≈450 so peak authority/max rates untouched, and `0xC616C`=0 ⇒ `gp-0x6b4a`≡0 ⇒ **LKAS lane not touched**. 1536 is the SMALLEST dose clearing the margin; 1280/1024 remain.
- ✅★★★★ [**The slope-cap DOSE LADDER is cut — all four directions ready**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V169 cap 1792 (2.2x) · V168 1536 (3.4x, FLY FIRST) · V170 1280 (4.5x) · V171 1024 (5.7x)**, images `ed9e5fec` / `058dd64a` / `0c923c36` / `e3cbc92d`. All four are **V158 + one cal cell**, built through **V168's own verified builder** (one builder, four build numbers, so assertions and the CRC/readback path cannot drift), **35/35 assertions each**. Feel cost rises monotonically with dose; **peak authority and max rates untouched at every dose** and **no dose touches the LKAS lane**. ⇒ whichever way the V168 drive reads — clean but incomplete, or effective but too heavy — **the next build already exists.**
- ⭐★★★★★ [**Peak command oscillation is in the GRIND's band — it shares the grind's lever**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — sweeping all three COMMAND channels with the validated estimator (excess over each channel's OWN power law, nulled at its OWN slope), **the command clears its null in EXACTLY ONE band: 15-25 Hz** — `cc_req` **1.97** · `co_tqcan` **1.65** · `sc_tq` **1.55** median — and is **below null everywhere else**, ratchet band included (0.48-0.54). ⭐ **Decisive asymmetry**: at 5-12 Hz the CAR's peak is **larger** (9.15) than its 15-25 Hz peak (5.40), yet the command is below null there ⇒ **“openpilot re-emits everything” is REFUTED**. ⊕ Explanation: openpilot steers on **angle/curvature** and the ratchet is a **TORQUE-only mode with no angle signature** (angle channels 0.79-0.83 = chance) ⇒ invisible to it, while a mode that moves the wheel is not. ⚠ **NOT established**: the correlation is **ρ +0.54/+0.61, p 0.215/0.148** (n=7), and 🛑 **my 5-12 Hz negative control FAILED to discriminate** (ρ +0.54/+0.68, as high as the test band — it was correlating sub-null noise), so **causal direction is NOT established**. Build trend agrees weakly: command post-V102 ρ −0.70/−0.80 (p 0.188/0.104) vs the car's −0.90 (p 0.037). ✅ **ACTIONABLE**: it is **not a separate mechanism** — same band, same direction, and openpilot must not be modified ⇒ **V158's damper shape, already on the fly-first build, is the lever**. No new build needed, and **the V168 drive scores it for free** from the same episode.
- ✅★★★★★ [**LKAS authority is NOT the binding constraint — the command is delivered and rarely rails**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — measured directly for the first time. ✅ **The command sits at its 4096 ceiling only ~3 % of engaged frames** (rail duty 0.0145-0.0648 over 7 routes, 48k-124k frames each) and its **median is 133-247 counts = 3-6 % of the ceiling** ⇒ **raising a ceiling cannot add what was never requested.** ✅ **And what IS requested is DELIVERED**: command→steering-RATE correlation clears its phase-shuffled control on **7/7 routes** (r −0.167 to −0.441 vs shuf p95 0.054-0.102, best lags 20-390 ms), and the **negative sign matches the operator-confirmed convention** (+LKAS demands negative angle) — a free sanity check that passes. 🛑 **One measure of mine does NOT work**: stratifying mean|rate|/mean|cmd| by command magnitude to look for stiction shows the OPPOSITE (0-200 stratum highest at 13.7-57.0 vs 6.2-26.6 at 200-800) — **an artefact**, since small commands leave the motion dominated by driver and road, so it is small-denominator noise. **No stiction conclusion may be drawn from it.** ✅ **Conclusion: no authority lever is proposed because none is needed.** The firmware already multiplies the command **6x** (`0xC6CD0`=5346); more would be **openpilot-side (forbidden)**; and the one firmware ceiling tried is closed — **`0xC407E` is the hard-fault interlock at 511, one count under its own 512 trip, and V73 raised it ⇒ V74/V75 FAULTED.** ⇒ if weak lane-keeping persists, capture **which manoeuvre** — the 3 % railed frames are the only ones where a ceiling could matter.
- ✅✅★★★★★ [**The ratchet is a FIXED resonance with COMMAND-PROPORTIONAL drive — the `1−P·L` signature**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 244 pooled engaged-creep windows, each assigned to a stratum by its OWN mean operating point (requiring contiguous runs *within* a stratum fragments the data and left 6/7 strata empty — that cut is superseded). ✅ **FREQUENCY near-invariant**: CV **5.5 %** across speed (7.81-8.98 Hz), **7.0 %** across command, **12.3 %** across rate. ✅ **AMPLITUDE MONOTONE in command**: excess **17.0 → 19.4 → 39.4 → 58.1 = 3.4x** across the command range; worst rate band is **12-25 deg/s (excess 143.1)**. ⇒ **fixed resonance + command-proportional drive = exactly the `Z=(Z0+P·F)/(1−P·L)` signature**; **a moving loop pole would have shifted the frequency and it does not.** ⭐ **Sharpens engaged-only**: the loop-gain account alone predicted 4.88x vs a measured 19.9x [4.82,35.64]; in manual the command is ZERO so the excitation `F` is absent too — the two together land much closer, though ⚠ **not a clean product** since both are engagement-driven. ✅ **Confirms V168's logic**: `1−P·L` divides the WHOLE response, so reducing `|L|` attenuates at EVERY command level; and the drive should show it most clearly at **high command and 12-25 deg/s**. ⚠ **[BELIEF, weak]** the LKAS gain costs ratchet — 4x median **16.5** vs 6x **34.5**, ratio **2.09x**, but **p = 0.167, n=3 vs 6, CONFOUNDED**. 🛑 **NOT a recommendation to lower it** (standing instruction; operator wants 8x). **The constructive reading: damping the resonance is what BUYS headroom for more gain.**
- ⭐⭐★★★★★ [**LEVER #2: the assist map's own second-order section IS a retunable NOTCH**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **this RETRACTS two recorded claims**: *“this firmware has NO frequency-selective lever”* and *“no notch filter exists anywhere”*. `FUN_000352b4` carries a genuine biquad (states `gp-0x3814`/`gp-0x3818`, coeffs `0xC60A8/AC/B0/B4` float32, enable `0xC649B`, 1 kHz) **in the dominant torque-fed lane**, and it is **already enabled on the flying build** (stock ships `0xC649B`=0; the kit turned it on at V104). ✅ Simulated from the decompiled operand order (not hand-derived — a sign slip inverts it): **flying coeffs give 0.9788 at 8.64 Hz** and V106/V107 give 0.9823 — **both PASS the ratchet** — while **V106 placed a PERFECT NULL (gain 0.000) at 25.5 Hz**, proving the structure can notch. ⭐ **Better than the slope cap on FEEL**: the cap cuts gain at EVERY frequency including DC (heavier steering near centre), a notch cuts only at the resonance ⇒ **DC gain 1.0000, no steady-state feel cost**; and it **does not rest on the real-positive `P·L` assumption**, since a notch removes gain without adding any. 🛑 **A razor notch is the WRONG design**: −96 dB at exactly 8.64 Hz is also that narrow, and **the ratchet spans 7.81-10.74 Hz across strata** — it would miss the mode on drift, and it **amplified 40 Hz by 1.36x**. ⇒ design for **attenuation across 7-11 Hz** with pole-radius margin, unity DC and **no gain increase anywhere**; GATE 2 on a notch must cover **phase and out-of-band gain**, since a notch flips phase across itself.
- ✅✅★★★★★ [**V172 BUILT — two independent ratchet levers now exist, with DIFFERENT feel costs**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V158 + four float32 coeffs at `0xC60A8/AC/B0/B4`**, image `ff8d07e6…` .rwd `c0ed77b7…`, 13 payload bytes, **23/23 assertions**. 🛑 **A mistake I nearly shipped as a negative**: a frontier sweep seemed to kill this lever (*“ring Q 30-53”*, 230-413 ms settling), **but Q is MEANINGLESS FOR REAL POLES** and the rows that mattered had real ones. Constraining to **real poles at r ≤ 0.97** gives `poles [0.97, 0.47509]`, **0.0 % overshoot, ZERO oscillation cycles** — overdamped, not a resonator. ⊕ **Never read Q off a pole pair without checking it is complex.** ✅ **What it does**: DC **1.0067 (unchanged)**, 3 Hz 0.850, 5 Hz 0.680, **8.64 Hz 0.9790→0.4441 = 2.2x**, **21 Hz 0.8659→0.0902 = 9.6x** ⇒ effective map `s` 1.958→0.888 ⇒ `|1−P·L|` 0.0700→0.4360 = **6.2x more damped**. ⭐ **THE CHOICE**: V168 (`0xC6384`) costs **static weight near centre**, 3.4x; V172 costs **~130 ms of assist LAG on fast inputs** and 15-32 % of 3-5 Hz driver content, 6.2x **plus 9.6x on the grind**. **V172 is stronger on paper but its risk is dynamic** — late assist on turn-in could itself read as notchiness. ⭐ **RECOMMEND V168 FIRST for a legible failure mode**, V172 next if V168 is insufficient or too heavy. ⊕ **They do NOT stack** (V172 asserts the cap is stock). ⊕ **Both rest on the same real-positive `P·L` assumption**, so a null on either falsifies it for both; and V172 adds a discriminator — **the grind should fall FURTHER than the ratchet** (9.6x vs 2.2x).
- 🚩★★★★★ [**FLIGHT ORDER REVISED — V172 first; my “130 ms lag” was the wrong metric**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I recommended V168 first because V172 added *“~130 ms”* of lag. **That was the STEP SETTLING TIME, which is not what a driver feels** (settling is dominated by the slowest pole's tail regardless of signal energy). The right metric is **group delay in the driver's band**: **+30.1 ms at 0.5 Hz, +21.4 at 3 Hz, +5.4 at the ratchet** — falling with frequency, and 30 ms is at the low end of noticeable. ⇒ **V172's risk is materially lower than I stated.** ⭐ **V172 now flies FIRST**: it predicts **6.2x** on the ratchet **plus 9.6x on the grind** (V168: 3.4x, ratchet only), and — decisively — the operator's standing constraint is about **apparent MASS AND FRICTION**, which **V168 raises uniformly and V172 leaves untouched** (DC gain 1.0067). ⊕ **V168 and its 4-dose ladder stay cut and ready** if V172's lag is the problem. ❌ **A “protected” variant is DOMINATED**: `poles [0.95916, 0.63265]` holds 3 Hz at 0.9322 but only reaches 0.5716 at 8.64 Hz = **5.0x**, buying **4 ms** of delay for a **1.2x loss of damping** — not built.
- ✅★★★★ [**Do NOT stack the two ratchet levers — and V172 already passes the target**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ❌ **stacking is a bad trade**: the cap on top of V172 buys **1.17x** (cap 1536) or **1.35x** (1024) while adding the **FULL static weight cost**; V172's build asserts the cap is stock so they cannot stack by accident. ✅ **V172 alone passes**: a Q ratio of 3.0 needs `|L| ≤ 2.025` and V172 leaves **1.732**. ⭐ **After V172 the loop is 52 % assist map / 48 % everything else**, and that 48 % is the census's **engagement-conditional** terms (PID 0.2565, r24 0.049-0.293, r26 0.098-1.17 while `gp-0x6b5e==0`, `FUN_00036682` 0.0032) ⇒ **a third lever must come from those, not the map**. 🛑 **Not worth starting before a drive**: two independent levers already exceed the target on paper, both rest on the same `P·L` assumption that ONE pass tests, and **six builds are cut with only one flyable at a time** — more builds now would be speculation. ⊕ `docs/scoring/BUILD-INVENTORY.md` carries the decision table and hashes.
- ⚠★★★★ [**The `P·L` account cannot be tested from existing data — the drive is necessary**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the account predicts the ratchet should WEAKEN at higher driver torque, because the map's local slope falls along its own curve (6.16→0.86→0.01, capped at 2.000 below X≈100). ❌ **Raw stratification gives the WRONG sign** (excess vs slope ρ = −1.00: 10.7→22.9→44.2 as slope goes 1.807→0.768→0.140) **but |cmd| and |rate| are ρ = +1.00 with the stratum**, so it is uninformative. Stratifying torque within narrow command bands splits 2-supporting / 1-contradicting, **also fully explained by the rate confound alone**: excess peaks at **12-25 deg/s and falls above it**, and the supporting rows sit at mean rate 40.6 and 59.7 (past the peak) while the contradicting one runs 2.4→7.0 (climbing to it). 🛑 **[INCONCLUSIVE, and STRUCTURALLY so]** — driver torque, command and rate are all driven by how hard the wheel is worked, so **observational data cannot separate the map's slope from the rate. More windows would NOT fix this.** ⇒ **the assumption is testable only by intervention** (fly V172 or V168 and see whether the peak moves), which is what the pre-registered outcomes cover. **Recorded so it is not re-attempted.**
- ✅★★★★ [**GATE 1 passes for V172 — `gp-0x6b86` has exactly ONE consumer and it is not a monitor**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V172 low-passes the assist output hard (21 Hz −9.6x, 40 Hz −3.3x), and the risk was a **stuck-signal / plausibility / rate-of-change monitor**, since filtering a watched signal can look like a frozen sensor. Raw LE byte scan across **both** gp encodings finds **4 accesses, all accounted for**: `0x35AB6 ld.h` and `0x35AC0 st.h` inside `FUN_000352b4` (the producer's own lockstep compare + the write), `0x35ACE st.h r0` (the out-of-range store-zero), and **`0x3AC7C ld.h` in `FUN_0003aa2c`, THE AGGREGATOR — the only consumer outside the producer.** ⇒ **GATE 1 PASSES**: nothing watches it for liveness, so heavy filtering cannot trip a fault path. ⊕ The lockstep shadow `gp-0x4cde` (3 accesses, all inside the producer) is **unaffected by a coefficient change** — the value is still written to both cells in the same pair, preserving what trips `FUN_0006b9fa`. ⊕ Same clearance covers V168, which changes how the value is COMPUTED, not who reads it.
- ✅✅★★★★★ [**The GRIND is engaged-only too — and a sub-band split attributes it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ✅ **the grind clears its slope-matched null in MANUAL creep on 0 of 7 routes** (manual 1.5-3.0 vs nulls 3.8-4.9) ⇒ **like the ratchet, it is FIRMWARE-CREATED BY ENGAGEMENT, not a mechanical mode being amplified.** The two symptoms differ in frequency and in which levers move them, **not in class**. **New** — the ratchet was tested this way earlier, the grind never had been. ❌ It also kills the manual-arm discriminator between V158's damper (engaged modes 26/27 only) and V172's filter (always active): neither symptom exists in manual to compare. ⭐ **What DOES discriminate is the SHAPE across the band**: V172's attenuation is frequency-sloped (15 Hz 0.2298 → 25 Hz 0.0359; mean **0.1737** over 15-20 vs **0.0784** over 20-25, so **the top falls 2.21x more**), while V158's damper is rate-proportional and dose-set, hence roughly flat. ⇒ **score grind excess in 15-20 and 20-25 SEPARATELY**: top falls much more = V172's filter; both fall equally = V158's damper. **Free from the same episode.**
- ✅★★★★ [**Same class, different drive law — the grind SATURATES where the ratchet keeps growing**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 244 pooled windows by own operating point. ✅ **Both are FIXED-FREQUENCY** (grind CV **9.6 %** speed / **11.0 %** rate / **2.1 %** command — the tightest figure either symptom gives; ratchet 5.5 / 12.3 / 7.0 %) ⇒ both carry the `1−P·L` signature of a plant mode whose damping the loop cancels. 🛑 **But their DRIVE LAWS differ**: ratchet excess is **MONOTONE in command** (17.0→19.4→39.4→58.1, 3.4x) while grind excess **peaks MID-command (12.6 at 600-1500) and FALLS at high command (6.0 at 1500+)**. ⊕ Consistent with the kit's recorded *“saturation suppresses it 141x”* — the grind is higher frequency so the same command produces more rate and rails sooner. ⚠ **[BELIEF]** the saturation reading; strata are n=46 and n=100 and a single-drive artefact is not excluded. ⭐ **SCORING CONSEQUENCE**: the two read best at **different command levels** — a pass spent all at high command reads the ratchet well and **UNDER-READS the grind**. Take the **grind verdict from mid-command windows**, not pooled. A slow varied lap covers both, which is what the drive card asks for; this is why.
- 🚩✅★★★★★ [**V173 built and SUPERSEDES V172 — the same lever without giving up Honda's notch**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — image `a9877aee…` .rwd `5d213cf8…`, **3 float32 cells**, 25/25. ⭐ **The section collapses to `H(z)=C_B4(z²+C_B0 z+1)/(z²+C_A8 z+C_AC)`** ⇒ the numerator's roots have **product 1**, so the zeros are **always on the unit circle** (**always a true notch**) at `2cosθ=−C_B0` ⇒ **`C_B0` ALONE sets the notch, the poles are set by `C_A8`/`C_AC` alone — they are INDEPENDENT.** 🛑 **Which exposed a defect in V172**: stock `C_B0` puts Honda's notch at **55.23 Hz (−43.9 dB)** and **V172 moved it to 27.17 Hz**, giving it up entirely (0.000128→0.251316) — **and we do not know what it is FOR.** ✅ **V173 keeps `C_B0` bit-for-bit, moves only the poles**: 8.64 Hz **0.476** (V172 0.444), 21 Hz 0.189 (V172 0.090), **55.23 Hz 0.000013 — notch KEPT and deeper**, group delay **+30.1 ms, IDENTICAL to V172**, max |H| 0.9946 ⇒ never amplifies. Trade: **half the grind attenuation**, the right side to err on since the ratchet is unsolved and V158's damper is on the same base. ❌ **A notch ON the ratchet is STRUCTURALLY impossible**: `C_B4=DC(1+C_A8+C_AC)/(2+C_B0)` and `2+C_B0=2−2cosθ→0` ⇒ **C_B4 ~ 1/f²** — 8.64 Hz needs C_B4 **13.576** and amplifies out-of-band **1503x**; 55.2 Hz needs 0.336 and amplifies **1.0x** ⇒ **Honda put it at 55 Hz because that is where it costs nothing.** 🛑 **CORRECTION**: I claimed this session retracts *“no notch exists”* — **`MEMORY-PART5` already carries that retraction** with the same coefficients and ±19.88° zeros. **The kit found it first; I re-derived without checking.** ✅ **But it UNBLOCKS that memory's parked question**: `get_function_callers(0x352b4)` = **`FUN_0002214a` = TASK 1, the CONFIRMED 1 kHz task**; the rate uncertainty belongs to **task 5** (`FUN_00022ca0`), a different task ⇒ **the section runs at 1 kHz and the notch is at 55.23 Hz.**
- ✅★★★★ [**V173 is on the efficient frontier — assist-lane design is COMPLETE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — with Honda's notch fixed and the structure separating, the design reduces to choosing **one real pole pair**. Sweeping every pair that holds DC within 2 % and never amplifies: at V173's **30.1 ms** lag the best achievable 8.64 Hz gain is **0.4691** vs V173's **0.4761** — a **1.5 %** difference, far inside any measurement floor. **Its poles were inherited from V172, not chosen, and are essentially optimal. No rebuild.** ⊕ The trade is priced if more is wanted: **0.3383 at 46.6 ms** (poles 0.980/0.60) or **0.2832 at 54.6 ms** (0.975/0.95) ⇒ **another 1.4-1.7x of ratchet attenuation costs 16-25 ms of extra assist lag** — a FEEL decision for the operator, one coefficient triple away. ✅ **Design work on the assist lane is finished**: both lever classes cut and verified, both gates closed, all 7 artifacts re-hashed from disk, and the remaining assumption testable only by driving.
- ❌★★★★ [**r26 is ALREADY gated OFF engaged — no lever, and my loop decomposition overcounted**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — chasing the ~46 % of `|L|` outside the assist map: the census lists **r26 at 0.098-1.17**, *“LIVE only while `gp-0x6b5e==0`”*, a **gate never examined** (`0xC6444` is falsified but that was a magnitude cut). `FUN_000361c8` (writers `0x36256`/`0x36264`) computes **`gp-0x6b5e = ±POL·(LERP(gp-0x6bda)·cal(0xC63C2))>>10`**, X knots `0xC66CE` **[-384,-128,128,294,384]**, Y `0xC66D8` **[0,4762,4762,717,0]**, scale **1024**. Engaged `gp-0x6bda`≡0 over 75,227 frames falls in segment **[-128,128]** ⇒ LERP returns **4762** (NOT Y[0]) ⇒ **`gp-0x6b5e` = ±4762, NON-ZERO** ⇒ **r26 is already gated OFF by Honda. No lever.** 🛑 **Corrects my own numbers**: I carried the census's `L_other` = **0.825** with r26 inside it; if r26 is gated off, the engaged remainder is **PID 0.2565 + r24 0.049-0.293 + 0.0032 ≈ 0.31-0.55**. ⊕ **Does not change the lever choice** — a smaller `L_other` makes the assist map an even LARGER share, strengthening V173 — but **the absolute Q-ratio figures carry more uncertainty than two decimals suggest.** ⚠ `gp-0x6b5e` has **4 readers** (`0x36390`, `0x3AA8E`, and **`0x4DA92`/`0x4DFAE` outside the aggregator**), so had the gate been open it would NOT have been a clean edit. 🛑 **tp OFF-BY-0x1000 RECURRED, 8th time**: read `0xC76CE` instead of `0xC66CE` and got `[15,4100,15,4102,...]` — **the interleaved 15s were the tell** (a knot table is monotone, not alternating), and it gave the OPPOSITE conclusion from the wrong cells.
- ✅✅★★★★★ [**Every remaining lever is BELOW the measurement floor — V173 is the whole available fix**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — pricing what is left after V173 with the corrected `L_other` (r26 gated off ⇒ 0.31-0.55, not 0.825). ✅ **The prediction survives my own correction**: V173 gains **5.8x** at the wrong `L_other` and **6.9x** at the low end of the right one ⇒ **insensitive, and the correction moves it the RIGHT way by less than its own uncertainty.** 🛑 **Every remaining lever is unmeasurable**, as marginal gain on top of V173: kill the PID **1.21x** · kill r24 **1.14x** · kill both **1.35x** · add cap 1536 **1.20x** · add cap 1024 **1.40x** — **all at or below the 1.63x single-episode split-half floor**, so none could be told from noise on a drive **even if built and even if the model is exactly right.** ⇒ **V173 captures essentially all the available loop-gain reduction; the assist map IS the loop.** ✅ **No eighth build**: the analysis is not out of ideas, it is **priced**, and every remaining idea is worth less than the noise on the only measurement available. **The next information can only come from the car.**
- 🛑✅★★★★★ [**The scorer was reading the WRONG CHANNEL — caught by running the drive card's own command**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — with the design closed I ran the pipeline end-to-end as it would run on a fresh route. `score_band_excess.py` read **`cs_rate`**, the channel that scores at **CHANCE for the ratchet** (margin **1.03** vs `cs_tq`'s **7.42**) ⇒ **the tool the drive card points at would have produced a WRONG VERDICT.** ✅ Fixed to `cs_tq`; r24/V122 now reads **ratchet 33.2x (split-half 1.21x)** and **grind 14.0x (3.29x)** where before it gave 4.4x and 23.2x. ⊕ The split-halves **swap** — the ratchet becomes the *tighter* endpoint, which is the right way round for this channel. ✅ **Both drive-card discriminators are now IN the tool**: the **15-20 vs 20-25 Hz sub-band split** (V173's filter is sloped, 2.2x more at the top; V158's damper is flat — reference **5.8x / 14.0x, ratio 2.39**) and the **command-level stratification** (grind saturates above 1500 ct, ratchet grows). ⊕ **Both carry an 8-window guard**: unguarded, r24's 4-window stratum emitted **198x / 1270x** from a background fit on four windows — **not credible**. 🛑 **Run the pipeline BEFORE the drive, not after.**
- ✅★★★★ [**Tool audit: one more live hazard found and closed**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — after the primary scorer was found reading the wrong channel, I audited **all 43 scoring tools** for the same defect. ✅ **No other tool has it** — every remaining ratchet/grind endpoint reads `cs_tq`, sweeps channels deliberately, or is a probe-specific scorer. 🛑 **But `DRIVE-CARD-V158.md` and `SCORING-V158-preregistered.md` still pointed at `score_v158_creep.py`**, the scorer this session superseded — so flying V158 from its own card would have scored the drive with the **withdrawn half-power Q** (non-monotone), **withdrawn 5-12 Hz Q** (a window artefact; white noise returns 21.7-29.1), **withdrawn fixed-floor prominence**, **non-continuous windows**, and **`cs_rate`**. ✅ Closed three ways: the tool carries a superseded banner **and raises on import**; both documents redirect to `score_band_excess.py`; and no doc outside STATE's own retraction text mentions it. ⊕ **Pattern worth keeping**: both defects were in the **plumbing between the analysis and the drive**, not the analysis, and both were found by **running the drive card's own command verbatim** rather than by reading code. **That check belongs in every close-out.**
- ❌★★★★ [**Ring-down does not test the `P·L` assumption either — and my first control was VACUOUS**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the last idea for testing `P·L` without driving: a **time-domain ring-down**, immune to the spectral tilt, triggered on an abrupt command collapse. 🛑 **My first control could not have worked**: fitting the **time-REVERSED** envelope returned **0.64/0.64, 0.61/0.61, 0.80/0.80 … identical on every route** because **reversing a linear fit's x-ordering flips the slope but leaves residuals unchanged — r² is invariant BY CONSTRUCTION.** Replaced with the same fit on engaged segments having no collapse. ❌ **With a real control the measurement is too weak**: clears on only **6/9 routes by 1.5-3.3x**, decay alphas **1.17-4.92 overlap control alphas 1.35-2.16**, **one route's control is NEGATIVE (−0.95)** so its no-collapse envelopes *grow*, **ζ = 0.022-0.091 does not match the recorded ring-down 0.017-0.036**, and **there is no build ordering**. ✅ **THIS SETTLES IT**: three independent estimator families have now failed on the `P·L` question — frequency-domain Q (tilt-confounded), slope-corrected excess stratified by torque (rate-confounded, structurally), and time-domain ring-down (fails its own control). **The assumption is testable ONLY by intervention** — no longer a judgement but a result. **Line closed.**
- ✅✅★★★★★ [**The corpus was TWICE what I was using — and it CLOSES the driven/self-excited question**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **I scored 9 routes; there are 19** whole-route caches with the core channels, several far richer than any I used — **`r77` holds 97 engaged-creep windows against `r1e`'s 42**. (`sNN` entries are per-segment sub-caches of the same drives, not independent.) **The analysis was fine; I never enumerated the cache.** ✅ **Ratchet REAL on 19/19 routes** (excess 4.4-243.7 vs nulls 1.6-3.4), peak **7.42-10.16 Hz**, consistent with 8.64. ✅ **And it ANSWERS the one question that needed power**: driven-vs-self-excited was inconclusive at n=7 (CI [−0.021,+0.176] crossed zero); at **n=17** the same statistic gives **median +0.1148, 95 % CI [+0.0274, +0.1682] — EXCLUDES ZERO** ⇒ **THE RATCHET IS DRIVEN BY THE COMMAND, not self-excited.** Specific on 10/17, and **specificity vs window count ρ = +0.31**, exactly what an underpowered real effect predicts. ⚠ **Not universal** — `r85`/`r95`/`r97` are negative; the claim is about the population. ⭐ **V173 works either way** (the map amplifies whatever reaches the bar) so **no build changes**; what it changes is that **a null can no longer be blamed on absent excitation**, sharpening the pre-registered outcomes. **The open item recorded as “closes with more continuous windows” — closed.**
- 🛑🛑★★★★★ [**CORRECTION: “the ratchet has never moved” was an n=5 artefact**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the session's headline dissociation (grind falls post-V102, ratchet does not) rested on **5 post-V102 routes**. With build attribution recovered for the rest of the corpus it rests on **11**, and **it does not hold as stated**: **RATCHET ρ = −0.60, p = 0.052** (was −0.14, p 0.787) against **GRIND ρ = −0.84, p = 0.001**. ✅ **Robust to attribution confidence** — hard-attribution-only (n=7) gives **−0.59** for the ratchet, so the effect size is unchanged and only the p-value moves with n; **not an artefact of filename-inferred labels.** ❌ **NOT surviving**: *“30+ builds have not moved the ratchet”* and the strong dissociation. **Both fall post-V102.** ✅ **Surviving**: the grind falls **more strongly and more significantly** ⇒ a difference of **DEGREE, not kind**. ✅ **Surviving untouched**: ratchet in **torque not wheel rate**, **engaged-only** (7/7 vs 0/7), **command-driven** (CI excludes zero), assist map the **largest torque-fed term**, its **cap binds**, `0xC6384` **byte-identical across 161 images**. ⭐ **V173 stands** — its case is the dominant term and the binding untouched cap, not elimination; and the correction makes the ratchet **demonstrably reachable** rather than untouchable. ⚠ 8/18 attributions are filename-recovered, not image-verified; **`r77` (97 windows, the richest route) is EXCLUDED** — candidates span V31-V91.
- 🛑★★★★★ [**CORRECTION 2: “engagement CREATES the ratchet” is too strong — it AMPLIFIES it ~15x**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the same corpus enlargement that corrected the build trend softens this one. **Engaged clears its null 15/15** (was 7/7) but **manual clears it 6/15** (was **0/7**). ✅ It clears **marginally** in 5 of those 6 (3.3 vs 2.8, 2.7 vs 2.5, 2.3 vs 2.2, 3.8 vs 2.7, 2.9 vs 2.4), only `r21` clearly (8.2 vs 4.7), while **engaged clears by 10-100x**, up to **339.3 vs 2.7** on `r82`. ⇒ **engagement AMPLIFIES the ratchet ~15x; it does not CREATE it.** ⊕ Ratio **tightens to 15.1x [6.0, 38.9] on 12 matched pairs** (was 19.9x [4.82, 35.64] on 4). ⊕ **Drive endpoint unaffected** — engaged 12-339 vs null 2-5, so presence/absence holds with an enormous margin. 🛑 **But the MECHANISTIC claim changes**: the mode exists faintly without the loop, so the loop **amplifies an existing mode** — which is *more* consistent with `1−P·L` than outright creation. **No build changes**; V173 reduces `|L|`, which is the amplification. ⚠ **PATTERN**: both fallen claims were **presence/absence statements at n≤9, both failing in the direction of my own hypothesis**. **Small-n presence/absence is the failure mode here** — the effect sizes have held.
- ✅✅★★★★★ [**The CHANNEL result HOLDS on the full corpus — the one that matters most survived**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — two n≤9 claims had already fallen, so the survey that set the scorer's channel was re-tested. Built on **4** routes; on **19**: **`tq` 28.20 · `cs_tq` 25.80 · `ws_fl` 6.13 · `ws_fr` 5.15 · `cs_rate` 2.60 · angle 1.5-1.9 · commands 0.88-0.92** (was 7.62 / 7.42 / 3.95 / 4.41 / 1.03 / 0.79-0.83 / 0.56-0.67). ✅ **Ordering unchanged and every margin GREW**; torque leads the next-best channel by **5x** and wheel rate by **10x**. ✅ **All three COMMAND channels below 1.0 on 19 routes** ⇒ the ratchet is not in the command, confirmed at full n. ⇒ **the scorer's channel is right and the “wrong channel” claim stands.** ⚠ **One phrasing softens**: I said `cs_rate` is **“at CHANCE (1.03)”**; on 19 routes it is **2.60 mean / 1.66 median**, so wheel rate carries a **real but ~10x weaker** signal rather than none. Operationally unchanged. ⊕ **THREE FOR THREE: every claim needing correction was a CATEGORICAL statement at n≤9** (*never moved*, *absent in manual*, *at chance*) — **every effect SIZE has held or grown.**
- ✅✅★★★★ [**Both drive-card instructions CONFIRM at full n — and the ratchet centres at 8.2 Hz, not 8.64**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — re-tested on **544 windows / 19 routes** (was 244 / 9). ✅ **Ratchet MONOTONE in command**: 14.0 → 15.7 → 32.4 → 47.8 (**3.4x**) ⇒ *“include real curvature”* **stands**. ✅ **Grind PEAKS mid-command and DIES above 1500 ct**: 4.6 → 8.1 → **14.5** → 7.0 ⇒ *“grind verdict from the MID-command windows”* **stands**. ✅ Worst rate band **12-25 deg/s strengthens to 155.2** (was 143.1). ✅ **Frequency better pinned**: CV **3.5 %** speed / **4.9 %** rate / **8.1 %** command (n=9 gave 5.5/12.3/7.0); grind peak 19.92-20.90 Hz. ⚠ **The centre is ~8.2 Hz, not 8.64** — 8.64 was the 9-route pooled estimate. ⊕ **No design impact**: V173 targets **7-11 Hz**, bracketing 8.2 (response ≈0.50 vs 0.476 at 8.64, a 5 % difference), and notch placement does not arise since Honda's notch was kept. **Build and drive-card bands unchanged.** ⊕ **All three operational results held** — effect sizes and shapes survive; only categorical small-n phrasings have needed correcting.
