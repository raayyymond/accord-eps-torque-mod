# MEMORY — PART 7

🛑 Continuation of `MEMORY-PART6.md`, split 2026-08-29 at 250 KB (hard cap 256 KB — a file
past the `Read` limit loads with its tail SILENTLY TRUNCATED). **Read this together with
PART2..PART6.**

- ⭐⭐★★★★★ [**The assist curve IS in the image, the 2.000 slope cap BINDS, and GATE 2 passes**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the curve is **initialised data copied ROM→RAM at boot** (hence only 3 `st.h` hit the 20-knot block, 2 of them clears). Found image-wide by its required shape: **`0xCE47A` X `0,25,60,100,150,250,450,900,1800,4150` / Y `0,154,338,460,549,635,702,766,824,857`, slopes `6.16 5.26 3.05 1.78 0.86 0.34 0.14 0.06 0.01`** ⇒ **cap 2.000 BINDS on 3/9 over X 0-100**; `0xCF372` max 16.37 binds 4/9; `0xCF3CA` max 11.97 binds 3/9 (+3 duplicates = the mode pointer-table family). ✅ **The cap is NOT inert — it pins the small-signal gain at exactly 2.000, the CEILING value of `s` in the census**, so the loop's largest term sits permanently at its maximum. ✅ **All six records byte-identical across 161 images**; `0xC6384` = 2048 on all 161 (exact u16 — my earlier 40-byte window check spanned `0xC63A0`/`0xC63AC` and wrongly showed 5 variants; **my error**). ✅ **GATE 2 PASSES, anchored on the MEASURED Q ratio** (the census phase gives the WRONG SIGN — `|1−P·L|`=1.92>1 contradicts the measured 93 % cancellation, and `P`'s phase is not in the image): from `Q_eff/Q_passive = 14.3` ⇒ `|1−P·L| = 0.0700`, `P·L = 0.9300`; then **cap 1536 → 3.4x more damped, 1024 → 5.7x**. Phase passes because the map term is a **real gain** — lowering the cap scales `|L|` **without rotating it** ⇒ monotone, no reversal. ⚠ [ASSUMPTION] `P·L` real-positive at the peak, which the measurement requires. 🛑 **FEEL TRADE**: the cap binds at LOW torque ⇒ **heavier steering near centre**, against a standing constraint — but the curve is **uncapped above X≈450 so peak authority and max rates are untouched**, and the map is **driver-torque fed**, not the LKAS lane ⚠ [BELIEF — `gp-0x6b4a`'s provenance unestablished]. ⭐ **First dose 1536 (1.5x), predicted 3.4x** — smallest step clearing the one-episode margin.
- ✅✅★★★★★ [**V168 BUILT — the base-assist SLOPE CAP, a lever class the kit has never tried**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V158 + `0xC6384` 2048→1536** (2.000x→1.500x), image `058dd64a…` .rwd `0f0ace3b…`, **ONE payload byte** (`0xC6385` 08→06 — high byte only; the build asserts the VALUE, since the hardcoded-2 byte-count assertion fired here on the first run), 35/35 assertions, CRC 50/50, all six curve records untouched. ⭐ **New lever CLASS**: the arc is V38-52 authority/caves · V53-61 probes · V62-73 rate lane · V74-84 damper · V85-122 friction/knee/relay · V133-167 damper shape — **all on non-dominant terms**; `gp-0x6b86` is **5.8-7.8x the entire PID**. ✅ **Aimed at the RATCHET specifically**, because grind and ratchet dissociate (grind ρ=−0.94 p 0.005 post-V102; ratchet ρ=−0.14 p 0.787). Built on V158 so **both symptoms score from the SAME 15 s episode in different bands** — separated by the instrument, not the build. ✅ **Pre-registered three-way outcome**: excess below null = ratchet gone; unchanged = **falsifies the real-positive `P·L` assumption** and exonerates the map; RISES = `P·L` not real-positive, revert. **All three readable from one pass** (11/11 detection, 5-65x margin) — no uninterpretable branch. 🛑 **Feel cost**: heavier steering near centre; uncapped above X≈450 so peak authority/max rates untouched, and `0xC616C`=0 ⇒ `gp-0x6b4a`≡0 ⇒ **LKAS lane not touched**. 1536 is the SMALLEST dose clearing the margin; 1280/1024 remain.
- ✅★★★★ [**The slope-cap DOSE LADDER is cut — all four directions ready**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V169 cap 1792 (2.2x) · V168 1536 (3.4x, FLY FIRST) · V170 1280 (4.5x) · V171 1024 (5.7x)**, images `ed9e5fec` / `058dd64a` / `0c923c36` / `e3cbc92d`. All four are **V158 + one cal cell**, built through **V168's own verified builder** (one builder, four build numbers, so assertions and the CRC/readback path cannot drift), **35/35 assertions each**. Feel cost rises monotonically with dose; **peak authority and max rates untouched at every dose** and **no dose touches the LKAS lane**. ⇒ whichever way the V168 drive reads — clean but incomplete, or effective but too heavy — **the next build already exists.**
- ⭐★★★★★ [**Peak command oscillation is in the GRIND's band — it shares the grind's lever**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — sweeping all three COMMAND channels with the validated estimator (excess over each channel's OWN power law, nulled at its OWN slope), **the command clears its null in EXACTLY ONE band: 15-25 Hz** — `cc_req` **1.97** · `co_tqcan` **1.65** · `sc_tq` **1.55** median — and is **below null everywhere else**, ratchet band included (0.48-0.54). ⭐ **Decisive asymmetry**: at 5-12 Hz the CAR's peak is **larger** (9.15) than its 15-25 Hz peak (5.40), yet the command is below null there ⇒ **“openpilot re-emits everything” is REFUTED**. ⊕ Explanation: openpilot steers on **angle/curvature** and the ratchet is a **TORQUE-only mode with no angle signature** (angle channels 0.79-0.83 = chance) ⇒ invisible to it, while a mode that moves the wheel is not. ⚠ **NOT established**: the correlation is **ρ +0.54/+0.61, p 0.215/0.148** (n=7), and 🛑 **my 5-12 Hz negative control FAILED to discriminate** (ρ +0.54/+0.68, as high as the test band — it was correlating sub-null noise), so **causal direction is NOT established**. Build trend agrees weakly: command post-V102 ρ −0.70/−0.80 (p 0.188/0.104) vs the car's −0.90 (p 0.037). ✅ **ACTIONABLE**: it is **not a separate mechanism** — same band, same direction, and openpilot must not be modified ⇒ **V158's damper shape, already on the fly-first build, is the lever**. No new build needed, and **the V168 drive scores it for free** from the same episode.
- ✅★★★★★ [**LKAS authority is NOT the binding constraint — the command is delivered and rarely rails**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — measured directly for the first time. ✅ **The command sits at its 4096 ceiling only ~3 % of engaged frames** (rail duty 0.0145-0.0648 over 7 routes, 48k-124k frames each) and its **median is 133-247 counts = 3-6 % of the ceiling** ⇒ **raising a ceiling cannot add what was never requested.** ✅ **And what IS requested is DELIVERED**: command→steering-RATE correlation clears its phase-shuffled control on **7/7 routes** (r −0.167 to −0.441 vs shuf p95 0.054-0.102, best lags 20-390 ms), and the **negative sign matches the operator-confirmed convention** (+LKAS demands negative angle) — a free sanity check that passes. 🛑 **One measure of mine does NOT work**: stratifying mean|rate|/mean|cmd| by command magnitude to look for stiction shows the OPPOSITE (0-200 stratum highest at 13.7-57.0 vs 6.2-26.6 at 200-800) — **an artefact**, since small commands leave the motion dominated by driver and road, so it is small-denominator noise. **No stiction conclusion may be drawn from it.** ✅ **Conclusion: no authority lever is proposed because none is needed.** The firmware already multiplies the command **6x** (`0xC6CD0`=5346); more would be **openpilot-side (forbidden)**; and the one firmware ceiling tried is closed — **`0xC407E` is the hard-fault interlock at 511, one count under its own 512 trip, and V73 raised it ⇒ V74/V75 FAULTED.** ⇒ if weak lane-keeping persists, capture **which manoeuvre** — the 3 % railed frames are the only ones where a ceiling could matter.
- ✅✅★★★★★ [**The ratchet is a FIXED resonance with COMMAND-PROPORTIONAL drive — the `1−P·L` signature**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 244 pooled engaged-creep windows, each assigned to a stratum by its OWN mean operating point (requiring contiguous runs *within* a stratum fragments the data and left 6/7 strata empty — that cut is superseded). ✅ **FREQUENCY near-invariant**: CV **5.5 %** across speed (7.81-8.98 Hz), **7.0 %** across command, **12.3 %** across rate. ✅ **AMPLITUDE MONOTONE in command**: excess **17.0 → 19.4 → 39.4 → 58.1 = 3.4x** across the command range; worst rate band is **12-25 deg/s (excess 143.1)**. ⇒ **fixed resonance + command-proportional drive = exactly the `Z=(Z0+P·F)/(1−P·L)` signature**; **a moving loop pole would have shifted the frequency and it does not.** ⭐ **Sharpens engaged-only**: the loop-gain account alone predicted 4.88x vs a measured 19.9x [4.82,35.64]; in manual the command is ZERO so the excitation `F` is absent too — the two together land much closer, though ⚠ **not a clean product** since both are engagement-driven. ✅ **Confirms V168's logic**: `1−P·L` divides the WHOLE response, so reducing `|L|` attenuates at EVERY command level; and the drive should show it most clearly at **high command and 12-25 deg/s**. ⚠ **[BELIEF, weak]** the LKAS gain costs ratchet — 4x median **16.5** vs 6x **34.5**, ratio **2.09x**, but **p = 0.167, n=3 vs 6, CONFOUNDED**. 🛑 **NOT a recommendation to lower it** (standing instruction; operator wants 8x). **The constructive reading: damping the resonance is what BUYS headroom for more gain.**
- ⭐⭐★★★★★ [**LEVER #2: the assist map's own second-order section IS a retunable NOTCH**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **this RETRACTS two recorded claims**: *“this firmware has NO frequency-selective lever”* and *“no notch filter exists anywhere”*. `FUN_000352b4` carries a genuine biquad (states `gp-0x3814`/`gp-0x3818`, coeffs `0xC60A8/AC/B0/B4` float32, enable `0xC649B`, 1 kHz) **in the dominant torque-fed lane**, and it is **already enabled on the flying build** (stock ships `0xC649B`=0; the kit turned it on at V104). ✅ Simulated from the decompiled operand order (not hand-derived — a sign slip inverts it): **flying coeffs give 0.9788 at 8.64 Hz** and V106/V107 give 0.9823 — **both PASS the ratchet** — while **V106 placed a PERFECT NULL (gain 0.000) at 25.5 Hz**, proving the structure can notch. ⭐ **Better than the slope cap on FEEL**: the cap cuts gain at EVERY frequency including DC (heavier steering near centre), a notch cuts only at the resonance ⇒ **DC gain 1.0000, no steady-state feel cost**; and it **does not rest on the real-positive `P·L` assumption**, since a notch removes gain without adding any. 🛑 **A razor notch is the WRONG design**: −96 dB at exactly 8.64 Hz is also that narrow, and **the ratchet spans 7.81-10.74 Hz across strata** — it would miss the mode on drift, and it **amplified 40 Hz by 1.36x**. ⇒ design for **attenuation across 7-11 Hz** with pole-radius margin, unity DC and **no gain increase anywhere**; GATE 2 on a notch must cover **phase and out-of-band gain**, since a notch flips phase across itself.
- ✅✅★★★★★ [**V172 BUILT — two independent ratchet levers now exist, with DIFFERENT feel costs**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V158 + four float32 coeffs at `0xC60A8/AC/B0/B4`**, image `ff8d07e6…` .rwd `c0ed77b7…`, 13 payload bytes, **23/23 assertions**. 🛑 **A mistake I nearly shipped as a negative**: a frontier sweep seemed to kill this lever (*“ring Q 30-53”*, 230-413 ms settling), **but Q is MEANINGLESS FOR REAL POLES** and the rows that mattered had real ones. Constraining to **real poles at r ≤ 0.97** gives `poles [0.97, 0.47509]`, **0.0 % overshoot, ZERO oscillation cycles** — overdamped, not a resonator. ⊕ **Never read Q off a pole pair without checking it is complex.** ✅ **What it does**: DC **1.0067 (unchanged)**, 3 Hz 0.850, 5 Hz 0.680, **8.64 Hz 0.9790→0.4441 = 2.2x**, **21 Hz 0.8659→0.0902 = 9.6x** ⇒ effective map `s` 1.958→0.888 ⇒ `|1−P·L|` 0.0700→0.4360 = **6.2x more damped**. ⭐ **THE CHOICE**: V168 (`0xC6384`) costs **static weight near centre**, 3.4x; V172 costs **~130 ms of assist LAG on fast inputs** and 15-32 % of 3-5 Hz driver content, 6.2x **plus 9.6x on the grind**. **V172 is stronger on paper but its risk is dynamic** — late assist on turn-in could itself read as notchiness. ⭐ **RECOMMEND V168 FIRST for a legible failure mode**, V172 next if V168 is insufficient or too heavy. ⊕ **They do NOT stack** (V172 asserts the cap is stock). ⊕ **Both rest on the same real-positive `P·L` assumption**, so a null on either falsifies it for both; and V172 adds a discriminator — **the grind should fall FURTHER than the ratchet** (9.6x vs 2.2x).
- 🚩★★★★★ [**FLIGHT ORDER REVISED — V172 first; my “130 ms lag” was the wrong metric**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I recommended V168 first because V172 added *“~130 ms”* of lag. **That was the STEP SETTLING TIME, which is not what a driver feels** (settling is dominated by the slowest pole's tail regardless of signal energy). The right metric is **group delay in the driver's band**: **+30.1 ms at 0.5 Hz, +21.4 at 3 Hz, +5.4 at the ratchet** — falling with frequency, and 30 ms is at the low end of noticeable. ⇒ **V172's risk is materially lower than I stated.** ⭐ **V172 now flies FIRST**: it predicts **6.2x** on the ratchet **plus 9.6x on the grind** (V168: 3.4x, ratchet only), and — decisively — the operator's standing constraint is about **apparent MASS AND FRICTION**, which **V168 raises uniformly and V172 leaves untouched** (DC gain 1.0067). ⊕ **V168 and its 4-dose ladder stay cut and ready** if V172's lag is the problem. ❌ **A “protected” variant is DOMINATED**: `poles [0.95916, 0.63265]` holds 3 Hz at 0.9322 but only reaches 0.5716 at 8.64 Hz = **5.0x**, buying **4 ms** of delay for a **1.2x loss of damping** — not built.
- ✅★★★★ [**Do NOT stack the two ratchet levers — and V172 already passes the target**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ❌ **stacking is a bad trade**: the cap on top of V172 buys **1.17x** (cap 1536) or **1.35x** (1024) while adding the **FULL static weight cost**; V172's build asserts the cap is stock so they cannot stack by accident. ✅ **V172 alone passes**: a Q ratio of 3.0 needs `|L| ≤ 2.025` and V172 leaves **1.732**. ⭐ **After V172 the loop is 52 % assist map / 48 % everything else**, and that 48 % is the census's **engagement-conditional** terms (PID 0.2565, r24 0.049-0.293, r26 0.098-1.17 while `gp-0x6b5e==0`, `FUN_00036682` 0.0032) ⇒ **a third lever must come from those, not the map**. 🛑 **Not worth starting before a drive**: two independent levers already exceed the target on paper, both rest on the same `P·L` assumption that ONE pass tests, and **six builds are cut with only one flyable at a time** — more builds now would be speculation. ⊕ `docs/scoring/BUILD-INVENTORY.md` carries the decision table and hashes.
- ⚠★★★★ [**The `P·L` account cannot be tested from existing data — the drive is necessary**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the account predicts the ratchet should WEAKEN at higher driver torque, because the map's local slope falls along its own curve (6.16→0.86→0.01, capped at 2.000 below X≈100). ❌ **Raw stratification gives the WRONG sign** (excess vs slope ρ = −1.00: 10.7→22.9→44.2 as slope goes 1.807→0.768→0.140) **but |cmd| and |rate| are ρ = +1.00 with the stratum**, so it is uninformative. Stratifying torque within narrow command bands splits 2-supporting / 1-contradicting, **also fully explained by the rate confound alone**: excess peaks at **12-25 deg/s and falls above it**, and the supporting rows sit at mean rate 40.6 and 59.7 (past the peak) while the contradicting one runs 2.4→7.0 (climbing to it). 🛑 **[INCONCLUSIVE, and STRUCTURALLY so]** — driver torque, command and rate are all driven by how hard the wheel is worked, so **observational data cannot separate the map's slope from the rate. More windows would NOT fix this.** ⇒ **the assumption is testable only by intervention** (fly V172 or V168 and see whether the peak moves), which is what the pre-registered outcomes cover. **Recorded so it is not re-attempted.**
- ✅★★★★ [**GATE 1 passes for V172 — `gp-0x6b86` has exactly ONE consumer and it is not a monitor**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V172 low-passes the assist output hard (21 Hz −9.6x, 40 Hz −3.3x), and the risk was a **stuck-signal / plausibility / rate-of-change monitor**, since filtering a watched signal can look like a frozen sensor. Raw LE byte scan across **both** gp encodings finds **4 accesses, all accounted for**: `0x35AB6 ld.h` and `0x35AC0 st.h` inside `FUN_000352b4` (the producer's own lockstep compare + the write), `0x35ACE st.h r0` (the out-of-range store-zero), and **`0x3AC7C ld.h` in `FUN_0003aa2c`, THE AGGREGATOR — the only consumer outside the producer.** ⇒ **GATE 1 PASSES**: nothing watches it for liveness, so heavy filtering cannot trip a fault path. ⊕ The lockstep shadow `gp-0x4cde` (3 accesses, all inside the producer) is **unaffected by a coefficient change** — the value is still written to both cells in the same pair, preserving what trips `FUN_0006b9fa`. ⊕ Same clearance covers V168, which changes how the value is COMPUTED, not who reads it.
- ✅✅★★★★★ [**The GRIND is engaged-only too — and a sub-band split attributes it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ✅ **the grind clears its slope-matched null in MANUAL creep on 0 of 7 routes** (manual 1.5-3.0 vs nulls 3.8-4.9) ⇒ **like the ratchet, it is FIRMWARE-CREATED BY ENGAGEMENT, not a mechanical mode being amplified.** The two symptoms differ in frequency and in which levers move them, **not in class**. **New** — the ratchet was tested this way earlier, the grind never had been. ❌ It also kills the manual-arm discriminator between V158's damper (engaged modes 26/27 only) and V172's filter (always active): neither symptom exists in manual to compare. ⭐ **What DOES discriminate is the SHAPE across the band**: V172's attenuation is frequency-sloped (15 Hz 0.2298 → 25 Hz 0.0359; mean **0.1737** over 15-20 vs **0.0784** over 20-25, so **the top falls 2.21x more**), while V158's damper is rate-proportional and dose-set, hence roughly flat. ⇒ **score grind excess in 15-20 and 20-25 SEPARATELY**: top falls much more = V172's filter; both fall equally = V158's damper. **Free from the same episode.**
- ✅★★★★ [**Same class, different drive law — the grind SATURATES where the ratchet keeps growing**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 244 pooled windows by own operating point. ✅ **Both are FIXED-FREQUENCY** (grind CV **9.6 %** speed / **11.0 %** rate / **2.1 %** command — the tightest figure either symptom gives; ratchet 5.5 / 12.3 / 7.0 %) ⇒ both carry the `1−P·L` signature of a plant mode whose damping the loop cancels. 🛑 **But their DRIVE LAWS differ**: ratchet excess is **MONOTONE in command** (17.0→19.4→39.4→58.1, 3.4x) while grind excess **peaks MID-command (12.6 at 600-1500) and FALLS at high command (6.0 at 1500+)**. ⊕ Consistent with the kit's recorded *“saturation suppresses it 141x”* — the grind is higher frequency so the same command produces more rate and rails sooner. ⚠ **[BELIEF]** the saturation reading; strata are n=46 and n=100 and a single-drive artefact is not excluded. ⭐ **SCORING CONSEQUENCE**: the two read best at **different command levels** — a pass spent all at high command reads the ratchet well and **UNDER-READS the grind**. Take the **grind verdict from mid-command windows**, not pooled. A slow varied lap covers both, which is what the drive card asks for; this is why.
- 🚩✅★★★★★ [**V173 built and SUPERSEDES V172 — the same lever without giving up Honda's notch**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — image `a9877aee…` .rwd `5d213cf8…`, **3 float32 cells**, 25/25. ⭐ **The section collapses to `H(z)=C_B4(z²+C_B0 z+1)/(z²+C_A8 z+C_AC)`** ⇒ the numerator's roots have **product 1**, so the zeros are **always on the unit circle** (**always a true notch**) at `2cosθ=−C_B0` ⇒ **`C_B0` ALONE sets the notch, the poles are set by `C_A8`/`C_AC` alone — they are INDEPENDENT.** 🛑 **Which exposed a defect in V172**: stock `C_B0` puts Honda's notch at **55.23 Hz (−43.9 dB)** and **V172 moved it to 27.17 Hz**, giving it up entirely (0.000128→0.251316) — **and we do not know what it is FOR.** ✅ **V173 keeps `C_B0` bit-for-bit, moves only the poles**: 8.64 Hz **0.476** (V172 0.444), 21 Hz 0.189 (V172 0.090), **55.23 Hz 0.000013 — notch KEPT and deeper**, group delay **+30.1 ms, IDENTICAL to V172**, max |H| 0.9946 ⇒ never amplifies. Trade: **half the grind attenuation**, the right side to err on since the ratchet is unsolved and V158's damper is on the same base. ❌ **A notch ON the ratchet is STRUCTURALLY impossible**: `C_B4=DC(1+C_A8+C_AC)/(2+C_B0)` and `2+C_B0=2−2cosθ→0` ⇒ **C_B4 ~ 1/f²** — 8.64 Hz needs C_B4 **13.576** and amplifies out-of-band **1503x**; 55.2 Hz needs 0.336 and amplifies **1.0x** ⇒ **Honda put it at 55 Hz because that is where it costs nothing.** 🛑 **CORRECTION**: I claimed this session retracts *“no notch exists”* — **`MEMORY-PART5` already carries that retraction** with the same coefficients and ±19.88° zeros. **The kit found it first; I re-derived without checking.** ✅ **But it UNBLOCKS that memory's parked question**: `get_function_callers(0x352b4)` = **`FUN_0002214a` = TASK 1, the CONFIRMED 1 kHz task**; the rate uncertainty belongs to **task 5** (`FUN_00022ca0`), a different task ⇒ **the section runs at 1 kHz and the notch is at 55.23 Hz.**
- ✅★★★★ [**V173 is on the efficient frontier — assist-lane design is COMPLETE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — with Honda's notch fixed and the structure separating, the design reduces to choosing **one real pole pair**. Sweeping every pair that holds DC within 2 % and never amplifies: at V173's **30.1 ms** lag the best achievable 8.64 Hz gain is **0.4691** vs V173's **0.4761** — a **1.5 %** difference, far inside any measurement floor. **Its poles were inherited from V172, not chosen, and are essentially optimal. No rebuild.** ⊕ The trade is priced if more is wanted: **0.3383 at 46.6 ms** (poles 0.980/0.60) or **0.2832 at 54.6 ms** (0.975/0.95) ⇒ **another 1.4-1.7x of ratchet attenuation costs 16-25 ms of extra assist lag** — a FEEL decision for the operator, one coefficient triple away. ✅ **Design work on the assist lane is finished**: both lever classes cut and verified, both gates closed, all 7 artifacts re-hashed from disk, and the remaining assumption testable only by driving.
- ❌★★★★ [**r26 is ALREADY gated OFF engaged — no lever, and my loop decomposition overcounted**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — chasing the ~46 % of `|L|` outside the assist map: the census lists **r26 at 0.098-1.17**, *“LIVE only while `gp-0x6b5e==0`”*, a **gate never examined** (`0xC6444` is falsified but that was a magnitude cut). `FUN_000361c8` (writers `0x36256`/`0x36264`) computes **`gp-0x6b5e = ±POL·(LERP(gp-0x6bda)·cal(0xC63C2))>>10`**, X knots `0xC66CE` **[-384,-128,128,294,384]**, Y `0xC66D8` **[0,4762,4762,717,0]**, scale **1024**. Engaged `gp-0x6bda`≡0 over 75,227 frames falls in segment **[-128,128]** ⇒ LERP returns **4762** (NOT Y[0]) ⇒ **`gp-0x6b5e` = ±4762, NON-ZERO** ⇒ **r26 is already gated OFF by Honda. No lever.** 🛑 **Corrects my own numbers**: I carried the census's `L_other` = **0.825** with r26 inside it; if r26 is gated off, the engaged remainder is **PID 0.2565 + r24 0.049-0.293 + 0.0032 ≈ 0.31-0.55**. ⊕ **Does not change the lever choice** — a smaller `L_other` makes the assist map an even LARGER share, strengthening V173 — but **the absolute Q-ratio figures carry more uncertainty than two decimals suggest.** ⚠ `gp-0x6b5e` has **4 readers** (`0x36390`, `0x3AA8E`, and **`0x4DA92`/`0x4DFAE` outside the aggregator**), so had the gate been open it would NOT have been a clean edit. 🛑 **tp OFF-BY-0x1000 RECURRED, 8th time**: read `0xC76CE` instead of `0xC66CE` and got `[15,4100,15,4102,...]` — **the interleaved 15s were the tell** (a knot table is monotone, not alternating), and it gave the OPPOSITE conclusion from the wrong cells.
- ✅✅★★★★★ [**Every remaining lever is BELOW the measurement floor — V173 is the whole available fix**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — pricing what is left after V173 with the corrected `L_other` (r26 gated off ⇒ 0.31-0.55, not 0.825). ✅ **The prediction survives my own correction**: V173 gains **5.8x** at the wrong `L_other` and **6.9x** at the low end of the right one ⇒ **insensitive, and the correction moves it the RIGHT way by less than its own uncertainty.** 🛑 **Every remaining lever is unmeasurable**, as marginal gain on top of V173: kill the PID **1.21x** · kill r24 **1.14x** · kill both **1.35x** · add cap 1536 **1.20x** · add cap 1024 **1.40x** — **all at or below the 1.63x single-episode split-half floor**, so none could be told from noise on a drive **even if built and even if the model is exactly right.** ⇒ **V173 captures essentially all the available loop-gain reduction; the assist map IS the loop.** ✅ **No eighth build**: the analysis is not out of ideas, it is **priced**, and every remaining idea is worth less than the noise on the only measurement available. **The next information can only come from the car.**
- 🛑✅★★★★★ [**The scorer was reading the WRONG CHANNEL — caught by running the drive card's own command**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — with the design closed I ran the pipeline end-to-end as it would run on a fresh route. `score_band_excess.py` read **`cs_rate`**, the channel that scores at **CHANCE for the ratchet** (margin **1.03** vs `cs_tq`'s **7.42**) ⇒ **the tool the drive card points at would have produced a WRONG VERDICT.** ✅ Fixed to `cs_tq`; r24/V122 now reads **ratchet 33.2x (split-half 1.21x)** and **grind 14.0x (3.29x)** where before it gave 4.4x and 23.2x. ⊕ The split-halves **swap** — the ratchet becomes the *tighter* endpoint, which is the right way round for this channel. ✅ **Both drive-card discriminators are now IN the tool**: the **15-20 vs 20-25 Hz sub-band split** (V173's filter is sloped, 2.2x more at the top; V158's damper is flat — reference **5.8x / 14.0x, ratio 2.39**) and the **command-level stratification** (grind saturates above 1500 ct, ratchet grows). ⊕ **Both carry an 8-window guard**: unguarded, r24's 4-window stratum emitted **198x / 1270x** from a background fit on four windows — **not credible**. 🛑 **Run the pipeline BEFORE the drive, not after.**
- ✅★★★★ [**Tool audit: one more live hazard found and closed**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — after the primary scorer was found reading the wrong channel, I audited **all 43 scoring tools** for the same defect. ✅ **No other tool has it** — every remaining ratchet/grind endpoint reads `cs_tq`, sweeps channels deliberately, or is a probe-specific scorer. 🛑 **But `DRIVE-CARD-V158.md` and `SCORING-V158-preregistered.md` still pointed at `score_v158_creep.py`**, the scorer this session superseded — so flying V158 from its own card would have scored the drive with the **withdrawn half-power Q** (non-monotone), **withdrawn 5-12 Hz Q** (a window artefact; white noise returns 21.7-29.1), **withdrawn fixed-floor prominence**, **non-continuous windows**, and **`cs_rate`**. ✅ Closed three ways: the tool carries a superseded banner **and raises on import**; both documents redirect to `score_band_excess.py`; and no doc outside STATE's own retraction text mentions it. ⊕ **Pattern worth keeping**: both defects were in the **plumbing between the analysis and the drive**, not the analysis, and both were found by **running the drive card's own command verbatim** rather than by reading code. **That check belongs in every close-out.**
- ❌★★★★ [**Ring-down does not test the `P·L` assumption either — and my first control was VACUOUS**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the last idea for testing `P·L` without driving: a **time-domain ring-down**, immune to the spectral tilt, triggered on an abrupt command collapse. 🛑 **My first control could not have worked**: fitting the **time-REVERSED** envelope returned **0.64/0.64, 0.61/0.61, 0.80/0.80 … identical on every route** because **reversing a linear fit's x-ordering flips the slope but leaves residuals unchanged — r² is invariant BY CONSTRUCTION.** Replaced with the same fit on engaged segments having no collapse. ❌ **With a real control the measurement is too weak**: clears on only **6/9 routes by 1.5-3.3x**, decay alphas **1.17-4.92 overlap control alphas 1.35-2.16**, **one route's control is NEGATIVE (−0.95)** so its no-collapse envelopes *grow*, **ζ = 0.022-0.091 does not match the recorded ring-down 0.017-0.036**, and **there is no build ordering**. ✅ **THIS SETTLES IT**: three independent estimator families have now failed on the `P·L` question — frequency-domain Q (tilt-confounded), slope-corrected excess stratified by torque (rate-confounded, structurally), and time-domain ring-down (fails its own control). **The assumption is testable ONLY by intervention** — no longer a judgement but a result. **Line closed.**
- ✅✅★★★★★ [**The corpus was TWICE what I was using — and it CLOSES the driven/self-excited question**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **I scored 9 routes; there are 19** whole-route caches with the core channels, several far richer than any I used — **`r77` holds 97 engaged-creep windows against `r1e`'s 42**. (`sNN` entries are per-segment sub-caches of the same drives, not independent.) **The analysis was fine; I never enumerated the cache.** ✅ **Ratchet REAL on 19/19 routes** (excess 4.4-243.7 vs nulls 1.6-3.4), peak **7.42-10.16 Hz**, consistent with 8.64. ✅ **And it ANSWERS the one question that needed power**: driven-vs-self-excited was inconclusive at n=7 (CI [−0.021,+0.176] crossed zero); at **n=17** the same statistic gives **median +0.1148, 95 % CI [+0.0274, +0.1682] — EXCLUDES ZERO** ⇒ **THE RATCHET IS DRIVEN BY THE COMMAND, not self-excited.** Specific on 10/17, and **specificity vs window count ρ = +0.31**, exactly what an underpowered real effect predicts. ⚠ **Not universal** — `r85`/`r95`/`r97` are negative; the claim is about the population. ⭐ **V173 works either way** (the map amplifies whatever reaches the bar) so **no build changes**; what it changes is that **a null can no longer be blamed on absent excitation**, sharpening the pre-registered outcomes. **The open item recorded as “closes with more continuous windows” — closed.**
- 🛑🛑★★★★★ [**CORRECTION: “the ratchet has never moved” was an n=5 artefact**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the session's headline dissociation (grind falls post-V102, ratchet does not) rested on **5 post-V102 routes**. With build attribution recovered for the rest of the corpus it rests on **11**, and **it does not hold as stated**: **RATCHET ρ = −0.60, p = 0.052** (was −0.14, p 0.787) against **GRIND ρ = −0.84, p = 0.001**. ✅ **Robust to attribution confidence** — hard-attribution-only (n=7) gives **−0.59** for the ratchet, so the effect size is unchanged and only the p-value moves with n; **not an artefact of filename-inferred labels.** ❌ **NOT surviving**: *“30+ builds have not moved the ratchet”* and the strong dissociation. **Both fall post-V102.** ✅ **Surviving**: the grind falls **more strongly and more significantly** ⇒ a difference of **DEGREE, not kind**. ✅ **Surviving untouched**: ratchet in **torque not wheel rate**, **engaged-only** (7/7 vs 0/7), **command-driven** (CI excludes zero), assist map the **largest torque-fed term**, its **cap binds**, `0xC6384` **byte-identical across 161 images**. ⭐ **V173 stands** — its case is the dominant term and the binding untouched cap, not elimination; and the correction makes the ratchet **demonstrably reachable** rather than untouchable. ⚠ 8/18 attributions are filename-recovered, not image-verified; **`r77` (97 windows, the richest route) is EXCLUDED** — candidates span V31-V91.
- 🛑★★★★★ [**CORRECTION 2: “engagement CREATES the ratchet” is too strong — it AMPLIFIES it ~15x**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the same corpus enlargement that corrected the build trend softens this one. **Engaged clears its null 15/15** (was 7/7) but **manual clears it 6/15** (was **0/7**). ✅ It clears **marginally** in 5 of those 6 (3.3 vs 2.8, 2.7 vs 2.5, 2.3 vs 2.2, 3.8 vs 2.7, 2.9 vs 2.4), only `r21` clearly (8.2 vs 4.7), while **engaged clears by 10-100x**, up to **339.3 vs 2.7** on `r82`. ⇒ **engagement AMPLIFIES the ratchet ~15x; it does not CREATE it.** ⊕ Ratio **tightens to 15.1x [6.0, 38.9] on 12 matched pairs** (was 19.9x [4.82, 35.64] on 4). ⊕ **Drive endpoint unaffected** — engaged 12-339 vs null 2-5, so presence/absence holds with an enormous margin. 🛑 **But the MECHANISTIC claim changes**: the mode exists faintly without the loop, so the loop **amplifies an existing mode** — which is *more* consistent with `1−P·L` than outright creation. **No build changes**; V173 reduces `|L|`, which is the amplification. ⚠ **PATTERN**: both fallen claims were **presence/absence statements at n≤9, both failing in the direction of my own hypothesis**. **Small-n presence/absence is the failure mode here** — the effect sizes have held.
- ✅✅★★★★★ [**The CHANNEL result HOLDS on the full corpus — the one that matters most survived**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — two n≤9 claims had already fallen, so the survey that set the scorer's channel was re-tested. Built on **4** routes; on **19**: **`tq` 28.20 · `cs_tq` 25.80 · `ws_fl` 6.13 · `ws_fr` 5.15 · `cs_rate` 2.60 · angle 1.5-1.9 · commands 0.88-0.92** (was 7.62 / 7.42 / 3.95 / 4.41 / 1.03 / 0.79-0.83 / 0.56-0.67). ✅ **Ordering unchanged and every margin GREW**; torque leads the next-best channel by **5x** and wheel rate by **10x**. ✅ **All three COMMAND channels below 1.0 on 19 routes** ⇒ the ratchet is not in the command, confirmed at full n. ⇒ **the scorer's channel is right and the “wrong channel” claim stands.** ⚠ **One phrasing softens**: I said `cs_rate` is **“at CHANCE (1.03)”**; on 19 routes it is **2.60 mean / 1.66 median**, so wheel rate carries a **real but ~10x weaker** signal rather than none. Operationally unchanged. ⊕ **THREE FOR THREE: every claim needing correction was a CATEGORICAL statement at n≤9** (*never moved*, *absent in manual*, *at chance*) — **every effect SIZE has held or grown.**
- ✅✅★★★★ [**Both drive-card instructions CONFIRM at full n — and the ratchet centres at 8.2 Hz, not 8.64**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — re-tested on **544 windows / 19 routes** (was 244 / 9). ✅ **Ratchet MONOTONE in command**: 14.0 → 15.7 → 32.4 → 47.8 (**3.4x**) ⇒ *“include real curvature”* **stands**. ✅ **Grind PEAKS mid-command and DIES above 1500 ct**: 4.6 → 8.1 → **14.5** → 7.0 ⇒ *“grind verdict from the MID-command windows”* **stands**. ✅ Worst rate band **12-25 deg/s strengthens to 155.2** (was 143.1). ✅ **Frequency better pinned**: CV **3.5 %** speed / **4.9 %** rate / **8.1 %** command (n=9 gave 5.5/12.3/7.0); grind peak 19.92-20.90 Hz. ⚠ **The centre is ~8.2 Hz, not 8.64** — 8.64 was the 9-route pooled estimate. ⊕ **No design impact**: V173 targets **7-11 Hz**, bracketing 8.2 (response ≈0.50 vs 0.476 at 8.64, a 5 % difference), and notch placement does not arise since Honda's notch was kept. **Build and drive-card bands unchanged.** ⊕ **All three operational results held** — effect sizes and shapes survive; only categorical small-n phrasings have needed correcting.
- ⭐⭐★★★★★ [**A blind cal scan finds the GRIND's lever — and NOTHING for the ratchet**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every lever this session came from structure; this is the complementary search. With **18 build-attributed routes**, correlate **every u16 cal that varied** (**88 cells**, ≥3 distinct values) against measured excess, controlled by a **permutation null on the MAXIMUM |ρ| across all cells** (family-wise, not per-test). ⭐ **GRIND: exactly ONE cell survives — `0xC40BC`, ρ = −0.715 vs threshold 0.687** — **the Coulomb relay knee**, the same cell identified **independently by structural reasoning** earlier (ρ = −0.69, p 0.039). **Two unrelated methods, one answer.** ⚠ Survives **marginally**. ✅ **RATCHET: ZERO cells survive** (best ρ = −0.620 vs threshold 0.700) ⇒ **no cal that has ever varied tracks the ratchet.** ⭐ **This REPLACES the retracted claim**: *“30+ builds have not moved the ratchet”* was an n=5 artefact; **“no varied cal tracks the ratchet under family-wise control at n=18”** is better-founded and **supports V173 more directly** — the lever must be a cell never varied, which is exactly `0xC6384` / `0xC60A8..B4` (byte-identical across 161 images). ⊕ Consistent with the corrected trend: the ratchet falls with build INDEX (ρ −0.60) while tracking **no individual cal** — many cells moved together and none alone explains it. ⊕ **EXTENDED AND UNCHANGED**: the first pass missed `0xD7000` (damper records) and `0xE4000`/`0xE5000` (arbitration limits); rerun over **all three regions, 94 cells**, gives an **identical verdict**, the only new top-six entry being a **CRC trailer** ⇒ **the null is not an artefact of where I looked.**
- 🛑★★★ [**Two commits carry the WRONG message — stale files in the job tmp directory**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **`068aace3` is titled *“The 511 ceiling … V132 lifts both”* but actually adds the flash-readiness section to `DRIVE-CARD-V173.md`** (30 insertions, one file); an earlier one had the same defect and was caught pre-push. **Cause**: the job tmp dir still holds `m7x`/`m8x` message files from a **previous session** (`m84`-`m89` present), so *write-a-file-then-`git commit -F`* **silently used stale content** whenever the write step was skipped — `commit -F m80.txt` **succeeded** against a V132 message rather than failing on a missing file. ✅ **Fixed forward, NOT by force-push**: the message is already on origin and the **tree is byte-identical to what was intended** (`git diff --stat HEAD origin/main` empty), and rewriting pushed history for a cosmetic message is not worth the risk. ⊕ **Message files now named `msg-<topic>.txt` and written immediately before use.** ⊕ **Reading the log: ignore `068aace3`'s title.**
- ⚠★★★★ [**The grind metric wants knee 1800; the operator already chose 3000 — his choice wins**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0xC40BC` is the **only** cell surviving family-wise control against the grind, so its headroom is the obvious question. The shape: knee **300 → grind 27.4** (n=9) · **600 → 9.5** (n=6) · **1800 → 5.1** (n=2, the MINIMUM) · **3000 → 15.5** (n=1) ⇒ the flying value looks **past** the optimum, and V112 at 1800 gave the two lowest grind figures in the corpus (7.9, 2.2). 🛑 **But 3000 is ONE route** — `r24`, with **9 windows, the fewest in the corpus** — so the noisiest estimate of any. 🛑 **And the decisive point is not statistical**: the flying image is named **`_v122_V122-V112BASE-KNEE3000…-BEST`** ⇒ **V122 IS V112 with the knee moved 1800→3000, labelled BEST** — the operator drove both and **chose 3000 on feel**. ✅ *The operator's lived experience overrides analyst recommendations* ⇒ **no build is proposed to revert the knee.** ⊕ The knee trades **grind against feel**: raising it widens the linear/viscous region and shrinks the Coulomb one, which is why the wheel feels less notchy even where the 15-25 Hz metric is slightly worse.
- 🛑🛑★★★★★ [**I OVER-CORRECTED — the ratchet trend is UNRESOLVED, and two route labels were wrong**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — searching memory for **operator verdicts** surfaced two hard route→build statements contradicting labels I had inferred from filenames: 🛑 **`r95` = V101, NOT V102** (*“V101 flew it at 8× (7128) as route 0x95”*; `r95_v102_prereg.py` is a pre-registration **for** V102 that **used** route 95, not an attribution) and ✅ **`r77` = V90** (*“V90 flew as route 77”*) — which I had **excluded**, and it is the **richest route in the corpus at 97 windows**. 🛑 **With ALL-HARD labels my own correction does not hold**: RATCHET post-V102 goes **−0.14 (n=5) → −0.60 (n=11, mixed) → −0.40 p 0.320 (n=8, hard)** — **never significant**, so *“the ratchet is trending down”* was as over-stated as *“it has never moved”*. **The honest statement: the ratchet's build trend is UNRESOLVED at every n tried.** ✅ The **GRIND trend is ROBUST** (−0.76 to −0.86, significant in every set). ⊕ **V173 is untouched** — its case never rested on the trend; torque-channel, engaged-only, command-driven and the cal-scan null all stand. ⭐ **And V101 PRICES THE 8× GAIN**: ratchet **193.2 vs median 34.5 (5.6x worse)**, grind **38.7 vs 15.6 (2.5x worse)** ⇒ **the first measurement behind the standing rule never to raise the LKAS gain**, matching the operator's own report.
- 🛑🛑🛑★★★★★ [**RETRACTED: the cal association scan is INVALID as I ran it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I headlined *“a blind cal scan finds the grind's lever”*. **It does not.** (1) **Unstable**: correcting `r95` (V102→V101) and adding `r77` (V90) **flipped both verdicts** — ratchet 0→2 survivors, grind 1→0, with `0xC40BC` falling −0.715 → −0.662. (2) **Leave-one-out**: `0xC40BC` survives **1/17** subsets — the “confirmation” was never stable. (3) 🛑 **The stable ratchet hits are ARTEFACTS**: `0xC4B58` (ρ +0.783, **17/17**) takes values **1443, 1542, 12803, 14022, 14212, 60140, 60141** — jumping with no ordering, the signature of a **mantissa half**; float32 reads of the region give **−1.43e+26** and denormals at every alignment, and the raw bytes are packed/structured. ⇒ **not a calibration scalar, so correlating it is meaningless.** 🛑 **ROOT CAUSE: the method assumed every 2-byte-aligned pair in the cal region is a u16 scalar. It is not** — that region holds floats, packed structs and pointer tables. **Every result it produced, both bands, is WITHDRAWN.** ✅ **SURVIVES separately**: `0xC40BC` as the grind lever, from the **knee sweep** where values are sensible and ordered (300/600/1800/3000) and the role is in the **decompiled relay arithmetic** (ρ −0.69, p 0.039). ✅ **V173 untouched** — its case is structural. ⊕ **Fix if re-run**: restrict to cells **verified as u16 cals** (knot-count header, decompiled read site, or ordered values), as `verify/check_lever.py --record` already does.
- ⚠★★★★★ [**PRECISION: “command-driven” does NOT mean the command carries 8.2 Hz**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — read carelessly, *“the ratchet is driven by the command”* says the command **contains** 8.2 Hz and injects it, making *“filter the command at 8 Hz”* an obvious lever. **It would be INERT.** ✅ All three command channels sit **BELOW their own nulls** in the ratchet band across 19 routes (`sc_tq` 0.92 · `co_tqcan` 0.91 · `cc_req` 0.88), and the kit's record has the **LKAS lane as a ~1-5 Hz LOW-PASS**, so the command cannot carry 8.2 Hz even in principle. ✅ **What the coupling result shows** is band-specific coherence at 7-10.5 Hz (median **+0.115**, CI **[+0.027,+0.168]**, n=17) — and **coherence at a frequency does not require the input to peak there**: broadband command activity moves the wheel, the motion excites a **plant resonance**, and the response appears at the PLANT's frequency. ⇒ **correct statement: a PLANT resonance EXCITED by command activity and amplified ~15x by the engaged loop.** ❌ **Do NOT propose notching the LKAS command near 8 Hz** — inert by construction, and its null would be uninterpretable. ✅ **The lever stays on the RESPONSE path** (V173), which works whatever the excitation is. ⊕ Also explains the monotone command scaling (14.0→47.8): more broadband excitation of a fixed-frequency mode, **not** more energy at 8.2 Hz.
- ⚠★★★★ [**HYPOTHESIS: is the ratchet at exactly 100/12 Hz? The drive can settle it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **every ratchet frequency quoted this session (7.81, 8.01, 8.20, 8.40, 8.59, 8.79) is an exact FFT BIN CENTRE** at 0.1953 Hz spacing; the frequency was never resolved. **100/12 = 8.3333 Hz** sits in range, and an exact submultiple of the 100 Hz frame rate would mean a **FIRMWARE CYCLE, not a mechanical mode**. Parabolic interpolation gives mean **8.1682 Hz (sd 0.70, n=19)** at nperseg=512 and **8.2958 Hz (sd 0.80, n=12)** at 1024 — **100/11 and 100/13 excluded at 2 SE, 100/12 consistent (0.5 % away)**. ⚠ **HYPOTHESIS, NOT EVIDENCE**: (1) I chose the candidate submultiples **after** seeing the range; (2) per-route sd **0.80 Hz** far exceeds the interpolation error, arguing the frequency genuinely varies — though a peak-hopping estimator on a noisy median PSD would look the same. **Not distinguishable at this window length.** ✅ **What settles it**: no route has **20 s unbroken engaged creep** (hence the 0.098 Hz ceiling); **one 20 s continuous pass gives df 0.05 Hz** and pins the frequency to ~0.5 %. **The drive card now asks for 20 s if easy** — free, since 15 s was already required. ⊕ **Changes nothing about V173 or the flight order** — it attenuates the response whatever the mode's origin.
- 🛑★★★★★ [**100/12 Hz EXCLUDED; the ratchet mode WANDERS +-0.71 Hz; and the assist section runs at 1 kHz not 100 Hz**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ✅ **a synthetic FIXED 8.3333 Hz line reproduces to sd 0.005 Hz** through the same estimator, vs **observed sd 0.79 Hz** (within-route split-half 0.354 => **true route-to-route sd 0.707 Hz**) => **150x discrepancy, the spread is REAL and a firmware divider is DEAD.** Image scan agrees: `cmp 12,rX` next to `add 1,rX` has only 4 sites (vs 22 for N=8), all at regular strides => **unrolled loop bounds, not dividers.** 🛑 **RETRACTION 3 (same FS root cause): a re-centred notch dies on GATE 2 MAGNITUDE, NOT on narrowness.** At 1 kHz it DOES attenuate the wander core (−15.4 dB at −1 sd), but `C_B4=(1+C_A8+C_AC)/(2+C_B0)` blows up to **36.98** => **max |H| = 46.91 where GATE 2 needs <=1**, **AMPLIFYING the grind 6.5x (+16.3 dB)** and Honda's 55 Hz null by 10^5 => **independent 2nd confirmation of the standing rule "THE NOTCH LEVER IS SPENT"** and of V88's wire result. => **the ratchet lever MUST be broadband.** 🛑 **TWO OF MY OWN CLAIMS RETRACTED (FS=100 error):** *"Honda's notch is at 5.53 Hz"* — **wrong, it is 55.226 Hz, the record was right**; and *"V173 guts the LKAS band 6.5x"* — **wrong, LKAS 0.5–3 Hz is −0.05..−1.42 dB, intact.** ✅ **V173 re-priced: grind 15–25 −10.4..−14.7 dB · ratchet −4.5..−8.0 dB · LKAS intact · cost +30 ms group delay @1 Hz (−10.8 deg).** **GATE 2 magnitude PASS: max|H_V173/H_stock| = 0.999753 over 0.1–499 Hz** => can only remove loop gain. ➕ **FRONTIER — ~4.8 ms of 1 Hz lag per dB of ratchet**; if V173 is "better but still there", **next is p_slow 0.970->0.980** (−2.9 dB more for +14 ms). **Do not pass 0.985 without an operator lag verdict.**
- ✅★★★★ [**V174 BUILT — the pre-registered 2nd point on the lag/attenuation frontier; V173 still flies first**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/BUILD-INVENTORY.md) — ONE knob vs V173: **slow pole 0.970 -> 0.980**, `C_B0` byte-identical so **Honda's 55.23 Hz notch is KEPT and deeper**. `0xC60A8` BFBA3D71 · `0xC60AC` 3EEE5604 · `0xC60B4` 3DB466E4, **re-derived from the formula inside the builder and asserted against the pinned words**. **Ratchet 8.64 Hz 0.979->0.339 (2.9x, vs V173's 2.1x) · grind 21 Hz 0.866->0.127 (6.8x, vs V173's 4.6x).** 27/27 assertions, 12 payload bytes, CRC 50/50, readback identical, base V158. **GATE 2 magnitude PASS, max |H| = 0.9880.** ⚠ **COST +42.8 ms group delay @1 Hz** (V173 +29.1) — the operator feels this as **steering weight**, the exact thing he said must not be the price, so it is **his call**. 🛑 **NOT an alternative to V173 and must not be flown first** — it is the expensive point on the same curve, and flying it first destroys the ability to tell which point the car needed. 🛑 **Do not cut past p_slow 0.985 without an operator lag verdict.** image `c3d6776cc72d4657…` rwd `5e4ba53db14442cb…`
- 🛑🛑★★★★★ [**THE ENGAGED RATCHET MAY BE OURS — we amplify a DESTABILISING ω²-weighted inertia term 3-8x, ENGAGED-ONLY; V175 reverts it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — ✅ **`FUN_00036c12` is the sole writer of `gp-0x6b26`** (`st.h` @`0x36CF0`; other 5 disp16 sites are `ld.h`): `gp-0x6b26 = clamp(((gp-0x6c2c · gate) · LERP_0xCBE74[mode](gp-0x6a5e) >>6) · 0x111 >>0x12, ±cal 0xC407E)`. **`gp-0x6c2c` is ACCELERATION** (`0x41602 sub r7,r9`, first difference of the filtered resolver rate) **and it enters LINEARLY** — the LERP is indexed by `gp-0x6a5e`, a SCHEDULING variable ⇒ **`gp-0x6b26 = K · α`, pure apparent inertia ⇒ loop contribution ∝ ω², 66.7x stronger at 8.17 Hz than 1 Hz, with NO filter and NO added phase.** ✅ **THE GATE CAN NEVER CLOSE**: `FUN_00038148` admits it while `|x| <= 1024` but the writer clamps to **511** ⇒ `w[3]` (`0xC63A6`) is an **unconditional** multiplier. 🛑 **THE SIGN: Y rows are NEGATIVE ⇒ α UP → MODEL DOWN → gp-0x6b70 UP (`f'>=0`) → target effort DOWN → MORE ASSIST ⇒ POSITIVE ACCELERATION FEEDBACK = NEGATIVE apparent inertia = DESTABILISING.** 🛑🛑 **And the flight build runs it at 3.0/3.0/8.14x on m26/m27 ONLY** (`(-9830,-5734,-1966)` → `(-29490,-17202,-16000)`; m24 MANUAL untouched) — **the one destabilising ω² term amplified on exactly the modes where the ratchet is amplified ~15x** [BELIEF, structural match]. 🛑 **RETRACTION — I OVERSTATED THE RELAY HAZARD; IT IS MEASURED AT 0.49 % DUTY.** Route `77` (`probe_build`=**V90**) carries `gp-0x6b26` on CAN 427 at **Honda's K**, 52,926 engaged frames; the ±511 hard clamp pins the packer shift to s∈{0,1}. At the tightest s=1: **saturation duty 0.0000 % @1.0x · 0.0094 % @1.5x · 0.4875 % @3.0x**, p99 136/204/**408**. ⇒ **rare tail clipping, NOT V80's near-unity relay — the relay argument is WITHDRAWN and is not part of the case for V175.** ⚠ r78/r79 are **NOT** comparable to r77 (the 427 packer scaling changed across V91/V92) — not a dose-response. ✅ **What survives IS the case**: p99 **408 against a 511 clamp** = a real **3x amplification of a destabilising ω² term, engaged-only**, and it is **highly intermittent** (p50 ≈18, p99 408) ⇒ negligible in steady driving, large **exactly in the fast transients where the ratchet lives**. ✅ **`0xC63A6` UN-STRUCK** — its release condition (re-derive the LERP slope) is met. ✅ **V175 BUILT**: base V173, **12 bytes**, engaged Y → Honda's row read from the stock image, 26/26 assertions, CRC 50/50, m24 + `0xC407E` + `0xC63A6` asserted frozen. image `a4e0dc4254ad8559…` rwd `5bf63d0ea539fd18…`. ✅ **DISCRIMINATOR vs V173's poles = ENGAGED vs MANUAL** — V173 acts in both, this revert **cannot act in manual**. ⚠ **Removes drag; creep will feel lighter.**
- ✅★★★★ [**THE SIX-TERM PATH-2 SUM IS FULLY CLASSIFIED — `gp-0x6b26` is its ONLY frequency-selective lane**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — a CLOSING result; **no second ω-weighted lever exists in this structure**, so do not re-open it. **w[0] `0xC63A0` gp-0x6bd0** base-assist damper (zero on 95.91 % engaged / 100 % micro) · **w[1] `0xC63A2` gp-0x6bbe** viscous **+ DC pedestal** (ω¹+DC) · **w[2] `0xC63A4` gp-0x6b46** an EMA'd, deadbanded torque-ERROR tracker ⇒ **a LAG, not a derivative** (`FUN_00036682`, self-referential; census contribution **0.0032**) · **w[3] `0xC63A6` gp-0x6b26 = K·ACCELERATION ⇒ ω², THE UNIQUE ONE** · **w[4] `0xC63A8` gp-0x6b4e** and **w[5] `0xC63AA` gp-0x6b4c** both written by `FUN_00026c80`, the **11-slot aggregator**, as **sums over the slots ⇒ ω⁰**. ❌ **DO NOT propose raising `0xC63A2` as a damping lever** — it is viscous (ω¹, 8x at 8 Hz) but its **DC pedestal (p50 73.6 ct flat 0–6 °/s)** means it adds **static friction at every frequency**, the exact trade the operator ruled out. ➕ **`0xC63AC`** (EMA alpha 102 ⇒ corner ≈16.9 Hz) is a shared low-pass on all six lanes — **broadband with V173's lag cost ⇒ strictly worse than V173, NOT a new direction.** 🛑 `0xC63A6` stays **HELD** as the fine adjustment after V175's drive.
- ❌★★★★ [**THE RESIDUAL LERP IS NOT A CALIBRATION — it is REBUILT EVERY CYCLE from two adaptation states**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **closes the "reshape the 9 knots as a cal" lead: THAT TABLE DOES NOT EXIST IN FLASH.** `FUN_000389ec` builds it into scratch (`gp-0x373c` X / `gp-0x3714` Y) as **`X[i]=(raw<<10)/iVar32`, `Y[i]=(raw*iVar33)>>10`** with `iVar32`/`iVar33` = `FUN_0003897a(gp-0x6982 / gp-0x6984, clamped by cals)`, then publishes to `gp-0x64b8`/`gp-0x641c`. ➕ **This explains the measured `f'` compression** (p50 2.174 hands-off vs 0.346 hands-ON): the curve is not traversed at different points, **it is RESCALED by the adaptation.** ⚠ **Salvageable sub-lead**: the scale factors are bounded by STATIC cals — `0xC6390`/`0xC6392` (upper), `0xC639A`/`0xC639C` (lower), `0xC617A`/`0xC617C` (small-signal Y floors), `0xC613E`/`0xC6140` (their thresholds), `0xC6178` (per-knot clamp) — and since `f' ∝ iVar33·iVar32/1024²` bounding the adaptation does move small-signal gain. 🛑 **But it is a lever INSIDE AN ADAPTATION LOOP — a new, materially riskier class**: needs the floors' knot-index gating traced, `FUN_0003897a` decompiled, GATE 1 on `gp-0x6982`/`gp-0x6984`, and **GATE 2 against an ADAPTIVE plant** (never done here). **A wrongly bounded adaptation can wind up or chatter — it would LOOK like new ratcheting.** => **do not start before V175 flies**; a falsified polarity chain falsifies this path too.
- ⚠🛑★★★★ [**POWER-CHECK EVERY RATIO ENDPOINT BEFORE THE DRIVE — the V175 card was underpowered for attribution**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V175.md) — the card asked for **one** 15 s engaged + **one** 15 s LKAS-off pass and attributed via the engaged/manual ratio. Resampling real 15 s creep windows: engaged **p50 214.3, log10 sd 0.332** (n=27) · manual **p50 17.2, sd 0.270** (n=22) · **single-pair ratio p50 10.5, 95 % [1.33, 56.5], log10 sd 0.418** ⇒ **one pair resolves only a change > 6.6x**; 3 pairs → 2.97x, 4 → 2.57x. **V175's predicted move is well under 6.6x** ⇒ a single pair could NOT have attributed it. ✅ **The PRIMARY question is unaffected** — the ratchet endpoint is **presence/absence (~8x), resolved by ONE 15 s engaged window 11/11**. ✅ **Card now STAGED**: Stage 1 = one engaged pass then STOP (matches the operator's own *"I stop instantly"* rule); **Stage 2 = 3 alternating engaged/LKAS-off passes (~90 s), driven ONLY if Stage 1 shows a win** — attribution only matters when there is something to attribute. ⊕ **GENERAL RULE: a RATIO of two separately-driven conditions costs ~4x the exposure of a presence/absence endpoint — stage it behind the presence check, and power-check it BEFORE the drive, not after.**
- ✅🛑★★★★ [**EVERY drive-card endpoint power-checked — and the LKAS-authority claim was UNSUPPORTABLE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V175.md) — against 27 real 15 s engaged creep windows, detectable change from ONE pass: **GRIND 15–25 Hz 5.96x vs predicted 0.058x ⇒ ANSWERABLE (margin 2.91x)** · **lane-change 26–31 Hz 2.04x vs 0.029x ⇒ ANSWERABLE (margin 17x)** · **ratchet 6.5–11 Hz 4.47x vs 0.260x ⇒ margin 0.86x, needs 2 passes** · **LKAS 0.5–3 Hz 19.16x vs 0.846x ⇒ needs 54 passes.** 🛑 **RETRACTION: I claimed the drive would show LKAS authority unchanged — IT CANNOT.** One pass bounds it only to **19.2x**, so a measured null there is worthless; authority-intact is an **ANALYTIC** claim (−0.05..−1.42 dB) and **the operator's impression is the better instrument.** ✅ **Structurally good news: the build's LARGEST predicted effect (grind, 17x cut) is also its BEST-POWERED endpoint** ⇒ **if grinding does not measurably fall on one pass, the pole-retune account is in trouble** — a real pre-registered failure mode. ⚠ **Keep 'is the ratchet GONE' (presence/absence, ~8x, one window resolves it) separate from 'by how much did the band fall' (needs 2 passes).**
- ❌★★★ [**Covariate adjustment does NOT rescue the one-pass ratchet endpoint**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — tried to buy power for free instead of asking for more driving. **LEAVE-ONE-OUT** residual log10 sd: none **0.3317** (detect 4.47x) · `log|wheel rate|` **0.3106** (4.06x, **permutation-controlled**, real 0.3106 vs shuffled p5 0.3320) · `log|command|` 0.2990 (3.85x, **UNCONTROLLED — not usable**) · **all three 0.3324 = WORSE than raw** (overfitting at n=27, caught by LOO; **in-sample R² would have flattered it**). 🛑 **V175 predicts a 3.85x cut ⇒ even the best adjustment lands the effect EXACTLY ON the threshold (~50 % power)** ⇒ **the 2-pass ask stands.** ⇒ **the instrument is near its limit; buying power here means EXPOSURE, not cleverness.**
- 🛑★★★★★ [**MY gp-RELATIVE SCANNER HAD TWO HOLES — GATE 1 re-verified, and the LERP scale branch is closed**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — chasing `gp-0x6982`/`gp-0x6984`, which `FUN_000389ec` demonstrably reads, my scan returned **ZERO sites in both encodings**. Ghidra: `ld.hu -0x6984,gp,r7` = `e4 3f 7d 96` ⇒ **hw2 = 0x967D, not 0x967C**. **HOLE 1: `hw2 = (disp|1)` for these load forms** — the recorded trap in [[accord-v850-scan-traps-formatv-and-storezero]]. **HOLE 2: my opcode whitelist omitted `ld.hu` (0x3F).** ⚠ **Either alone manufactures a FALSE NULL.** ✅ **Re-scanned with NO whitelist and hw2 ∈ {D, D|1}: `gp-0x6b26` still has EXACTLY 1 writer (`0x36CF0`) + 4 readers, and all six lane writer-counts are unchanged** ⇒ **V175's mechanism claim and the six-lane classification both SURVIVE the stricter method.** (Still blind to register-indirect stores — unchanged limitation.) 🛑 **RETRACTION: I called `FUN_0003897a` an ADAPTATION that could "wind up or chatter". It is a RANGE-CHECK + CLAMP + TWO-RATE SLEW LIMITER** — deterministic, one state, bounded by construction, no integrator. **Warning withdrawn.** ❌ **The scale-factor branch is dead both ways**: `gp-0x6982`/`gp-0x6984` have **ZERO gp-relative writers** ⇒ if constant, the check `(x−0xcc)<0x735` fails and both scales default to **0x400 = unity**, making `0xC6390/92/9A/9C` and `0xC6394/96/98/9E` **INERT**; if a coding write does move them, editing their bounds **rescales the whole LERP globally = BROADBAND**, same class as V173's poles and strictly worse. ➕ **Only the small-signal floors `0xC617A`/`0xC617C` (+ thresholds `0xC613E`/`0xC6140`) survive as the sole amplitude-selective candidate.**
- ❌❌★★★★ [**THE AMPLITUDE-SELECTIVITY LEAD IS FULLY CLOSED — all three branches**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **the 9-knot table** is NOT a cal (`FUN_000389ec` rebuilds it EVERY CYCLE into scratch, then publishes it) · **the scale factors** `gp-0x6982`/`gp-0x6984` have **zero gp-relative writers** ⇒ constant at unity, or — if written by a coding path the scan cannot see — editing their bounds is a **BROADBAND rescale**, same class as V173's poles · **the Y floors are dead TWICE**: `0xC617A` = `0xC617C` = **0** (so `max(Y,0)` is a **no-op**) **and** their arming thresholds `0xC613E`/`0xC6140` = **15000 = 183 % of the ±8192 residual clamp** (`0xC6200`), which the hard-clamped residual can **never reach** ⇒ **the floors cannot arm.** All six cells VIRGIN on stock/V122/V158/V173/V175. ⇒ **there is NO amplitude-selective lever in the assist-residual path — do not re-open it.** ➕ **WHERE TO GO INSTEAD**: the ratchet is **MOTOR/RACK-SIDE** ([[accord-ratchet-is-a-lightly-damped-resonance]]) yet this whole session worked the **assist/observer** path. The mapped bridge **`gp-0x6acc` → SHAPER → `gp-0x6b08` → INTEGRATOR → `gp-0x6b98` → FOC** ([[accord-aggregator-reaches-motor-via-gp6acc-bridge]]) is **downstream of everything examined and on the side the resonance lives** — the next territory. ⚠ Nearer the current loop ⇒ **GATE 2 there is a STABILITY question, not a feel one.**
- ❌★★★★ [**THE DELIVERY PATH HAS NO DAMPING LEVER — the shaper is a pure pass-through**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — followed the bridge to the MOTOR side where the resonance lives. **SHAPER** (`gp-0x6acc`→`gp-0x6b08`, `FUN_00042af8` @`0x43206`, ONE writer): `mode = cal[0xC64C8]` selects {1: a constant `cal[0xC61D4]`; 2: `clamp(offset+gated, ±12288)`; else pass-through} with a **hardcoded** `|gp-0x6acc| <= 8192` store-zero gate. **`0xC64C8` = 0 and `0xC61D4` = 0, both VIRGIN on stock/V122/V158/V173/V175 ⇒ LIVE MODE 0 ⇒ a PURE PASS-THROUGH with nothing to tune.** **INTEGRATOR** (`gp-0x6b08`→`gp-0x6b98`): accumulator at `gp-0x3570`, saturated against `cal[0xC61DC]<<15`, output `>>15`; **every gain is a hardcoded shift**, the only cals being an **anti-windup LIMIT** (`0xC61DC`) and a monitor-feeding post gain (`0xC61DA`=1092) ⇒ **governs large-signal windup, not small-signal damping** ⇒ lowering it clips authority without touching the resonance. ⊕ It is also the region whose **motor-rate cap V41 already FALSIFIED**. 🛑 **BOTH SIDES OF THE CHAIN ARE NOW ENUMERATED AND CLOSED** — assist/observer (six-term sum, notch, residual LERP) and delivery/motor (shaper, integrator). **The only untouched territory is the FOC / current loop**, where a mistake is a **motor stability** problem, not a feel one, and where the kit has never edited.
- ✅★★★★ [**V176 BUILT — both levers at the stronger dose; the four-build choice is complete**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/BUILD-INVENTORY.md) — V176 = **V175 + V174's pole**, i.e. the engaged inertia revert **and** `p_slow` 0.980 in one image. **Ratchet @8.64 Hz 0.9789→0.3393 · grind @21 Hz 0.8659→0.1275**, identical section response to V174 (the revert is a different lane and does not touch the biquad). **28/28 assertions, 12 payload bytes, CRC 50/50, base V175, `C_B0` untouched, GATE 2 max |H| = 0.9880.** image `bba4cd5a92c5186f…` rwd `7beac7510411c7ec…`. ⚠ **COST +42.8 ms group delay @1 Hz vs V175's +29.1** — felt as **steering weight**, the exact price the operator ruled out ⇒ **V175 stays FLY-FIRST; V176 is his choice** if he wants the strongest attack and will judge the lag on the same drive. 🛑 **Deliberately NOT spent (asserted frozen):** `0xC63A6` w[3] stays 1024 — stacking it would push the product **below Honda's own value** on a nine-link sign chain with no new information; it is the fine adjustment **after** a drive. `p_slow` stops at 0.980, the last point under the **do-not-pass-0.985-without-a-lag-verdict** rule. Nothing in the FOC.
- 🛑★★★★★ [**THE FIRMWARE SEARCH IS COMPLETE — every stage from CAN intake to motor PWM is enumerated**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the last territory was the **FOC**, and it is **CLOSED**: the golden model's own [VERIFIED] notes say the FOC/PWM ISRs (EIIC 0x600/0x970) run **far faster** than the steering task and the q-current reference merely **TRACKS** the merged command (a PI regulator + SVPWM, **not a shaper**) ⇒ **an 8 Hz resonance is damped by the torque COMMAND, not by the current controller** ⇒ closed, and not worth the motor-stability risk. ✅ **TWO lever families exist in this firmware and BOTH ARE BUILT**: the **assist-section poles** (`0xC60A8`/`AC`/`B4` → V173/V174/V176) and the **engaged apparent-inertia revert** (`0xD7A5C`/`0xD7A6C` → V175/V176). ❌ **Everything else is enumerated and closed**: the six-term Path-2 sum (only w[3] ω-weighted, HELD) · the residual LERP and all three of its branches · Honda's 55 Hz notch · the shaper (pure pass-through) · the integrator (hardcoded shifts, limit only, V41 falsified) · the FOC. **The only unspent cell is `0xC63A6` (w[3]), held as the fine adjustment AFTER a drive.** 🛑 **What remains is not analysis — it is one 15 s engaged creep pass.**
- 🛑🛑★★★★★ [**WE HAVE BEEN DRIVING A RELAY AT 10x HONDA — `0xC40D2` K1; V177 reverts it and is the new FLY-FIRST**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — found by re-reading the kit's own non-stock delta, not by new tracing. `friction = |model| · **sign(motor rate)** · K1/1024` ⇒ **every velocity reversal steps it by `2·|model|·K1/1024`**: Honda 102 → **0.199×|model|**; V89 204 → 0.398×; **V122..V176 = 1020 → 1.992×|model|**. Read from images: stock/V81/V87/V88 **102** | V89..V108 **204** | **V122..V176 1020 (10× Honda)**. 🛑 **V89's own docstring PRE-REGISTERED the risk** — *"larger K1 = a larger STEP at each reversal — notchiness on turn-in … transient, UNMEASURED"* — **and V122 then took it to 5× that value, still untested.** At 8 Hz the motor rate reverses **~16×/s** ⇒ a ~2×|model| step injected 16×/s **synchronised to the mode**. ⊕ **This is V80's relay failure in another lane**, and a relay's describing function **does not shrink with amplitude** — which is exactly why no amount of linear transfer-function work would have found it. ✅ **V177 = V175 + `0xC40D2` 1020→102** (Honda's, read from the stock image): **21/21 assertions, 2 payload bytes**, `0xC407E` frozen at 511, `0xC63A6` frozen, both prior reverts + all four section coefficients CARRIED. image `fc93255645014a0f…` rwd `86cd9394c0f426fe…`. ✅ **It makes the drive MORE interpretable**: `0xC40D2` is a **bare `tp` scalar ⇒ live in MANUAL AND ENGAGED** (RULE 7), while the inertia revert is engaged-only ⇒ **both modes fall = K1; engaged only = inertia; ratio unchanged = the poles.** ⚠ **FEEL COST: steady effort gets slightly HEAVIER** (the verified chain is more friction → more assist → lighter). ➕ **OPEN: `0xC40DC` accel EMA alpha, which V122 also moved 22→8** — a PHASE change, direction unestablished, deliberately excluded to keep V177 single-cell.
- 🛑★★★★★ [**RETRACTION: the Coulomb term is NOT a relay — V122 widened the ramp by the same 5x**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I claimed `0xC40D2` at 10x Honda makes a **relay** injecting a **1.99x|model| STEP** ~16x/s. `FUN_0003b8f6` shows that is **WRONG**: `ramp = clamp(motor_rate*12 / cal[0xC40BC], ±1)`, `friction = ramp * (|model|*K1/1024 + K0/1024)` — a **SATURATED RAMP**, and **V122 raised the ramp width 600→3000 by the SAME 5x it raised K1 204→1020**. ⇒ **no step**; the transition spans ±250 rate counts. ⊕ **`K0` = 0 (VIRGIN)** ⇒ friction → 0 as |model| → 0, killing the small-signal step too. **The "V80 relay in another lane" framing is WITHDRAWN.** ✅ **What survives**: saturated amplitude is genuinely **10x Honda's** and the zero-crossing slope **2x Honda's** — a real oversized velocity-dependent term in the assist path, never tested above 204. ✅ **V177 stands and stays fly-first**: it gives **Honda's amplitude at 1/5 Honda's SLOPE** (K1 reverted, V122's wider ramp kept) — the **gentlest of the three configurations**. 🛑 **DO NOT also revert `0xC40BC` to 600** — that makes the zero crossing **5x sharper** and undoes the one thing V122 got right; asserted untouched in V177. ➕ **METHOD: I found the oversized cell from the delta, then overstated its mechanism FROM THE VALUE ALONE. Read the code before naming the mechanism.**
- 🛑🛑★★★★★ [**THE LINEAGE STOPS AT V121 — V122, THE FLYING BUILD, MADE FOUR UNDOCUMENTED CHANGES**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **`grep V122` in `BUILD-LINEAGE*.md` returns ZERO.** Highest documented build is **V121**; nothing V122–V178 has a row. 🛑 **So the standing rule "grep the lineage before naming any address" SILENTLY PASSES for every cell V122 moved** — which is why these only surfaced from a raw byte diff. **Until backfilled, diff against the STOCK IMAGE, not the lineage.** V122's delta: `0xC40D2` 204→1020 (→V177 reverts) · `0xC40BC` 600→3000 (**KEEP — protective**) · `0xC40DC` 22→8 (**OPEN**, phase) · `0xC6598/9C/AC/B0/C4/C8/CC` three LERPs **flattened to ±5.0** (→V178 reverts). ✅ **THE FLATTENING, pinned by disassembly at `0x44374..0x443EE` in `FUN_00043e44`**: `X=[700,800,1100]`, and `0x4438e ld.w 0x75c4,tp,r13` is the **BELOW-RANGE FALLBACK** ⇒ **below input 700 stock returns 0.0 (a DEADBAND) and the car returns 5.0 (MAXIMUM)**; stock rises **0.0→1.5→2.0**, the car is a **FLAT 5.0**. That is exactly [[accord-v80-damper-relay-and-grind1-inert]]'s *"restore the RAMP, don't merely lower k"*. ⚠ **HONEST LIMIT: the SHAPE is pinned, the QUANTITY is NOT** — the input arrives in `r9` and `gp-0x6d94` has 1 writer / 0 readers (a diagnostic mirror) ⇒ **V178 is justified as a REVERT TO HONDA, not as an understood lever.** ✅ **V178 built**: 7 float32 cells, 28 B, base V177, 23/23 assertions, image `2a78d9241b9db4bc…`. 🛑 **NOT fly-first — V177 is one cell and attributable.**
- 🛑🛑★★★★★ [**V178 RETRACTED — `0xC6598`… is the V31/V38 AUTHORITY LADDER, not V122's flattening**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **I built an image on a wrong premise and nearly handed it over flashable.** The full **V108-vs-V122 diff is TWELVE BYTES** (`0x55DF2`, `0x55E10`, `0xC40BC` 600→3000, `0xC40D2` 204→1020, `0xC40DC` 22→8) and the float block is **not among them**. Across every image: **stock 1.0/-1.0/0.0/1.5/2.0 → V29 2.0 → V30 4.0 → V31 4.0 flat → V38 5.0 flat**, unchanged since ⇒ **a deliberate GAIN/AUTHORITY LADDER**, almost certainly how the kit gets LKAS authority at all. **Reverting it to Honda's 1.0 would cut authority ~5x** — the opposite of the operator's goal. Artifacts renamed `SUPERSEDED-DO-NOT-FLASH-AUTHORITY-*`; the builder raises on entry. 🛑 **THE METHOD ERROR IS THE LESSON: I asked “did V122 change this?” when the question was “WHEN did this change?”** Having just found the lineage gap, I attributed everything unfamiliar to V122. **A four-line loop printing the value across all images settled it and would have prevented the build.** ➕ **STANDING RULE: before reverting ANY cell, print it across every image IN BUILD ORDER. A cell that steps a LADDER (1→2→4→5) is a deliberate tuning axis — do not revert it. A cell that jumps ONCE is the candidate.** ✅ **V177 is UNAFFECTED and still fly-first** — `0xC40D2` 204→1020 is genuinely V122's, confirmed by this diff.
- ✅★★★★ [**The non-stock delta is FULLY AUDITED (139 images) and V179 built — Honda's acceleration filter**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — applied the LADDER-vs-SINGLE-JUMP rule to every cell. **All SINGLE JUMPs resolved**: `0x14120`/`0xC64DE` V2 noise · `0x35A08/12/18` V103 biquad arm (documented) · **`0xC61C0` + `0xC64B4` V36/V37 — read together at the SAME four sites = the gentle-EME debounce disable that FIXED the problem on-car, **DO NOT revert** · **`0xC40DC` the only unexplained one.** => **delta fully accounted for; no unexplored cells remain.** ✅ **V179 = V177 + `0xC40DC` 8->22**: `FUN_00041464` computes `gp-0x6c2c = EMA(accel, cal>>6)>>9`, the inertia term's input — **V122 slowed it 67->21 Hz, adding 14.1 deg of phase lag at 8.17 Hz.** ⚠ **Magnitude exact, SIGN of the damping effect NOT established** => justified as a **revert to Honda that makes the lane self-consistent** (V175 gave it Honda's GAIN, this gives Honda's FILTER), **not** as an understood lever. ONE byte, 19/19, image `c1e07f2d6e86bc31…`. ➕ **The builder ASSERTS the V31/V38 authority ladder intact at 5.0** so V178's error cannot recur. 🛑 **V177 stays FLY-FIRST** — its case is quantitative; V179's rests on coherence with an unestablished sign.
- ✅✅★★★★★ [**EVERY BYTE of the non-stock delta is accounted for — the audit is COMPLETE**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — enumerated and classified, not merely searched. **cal cells**: value across **139 images in build order**, every SINGLE JUMP resolved. **`0xE4195..0xE5FFF`**: 80 bytes, dominant run **15360->16384 at V38 = an AUTHORITY raise, DO NOT revert**. **float block `0xC6598..CC`**: the **V31/V38 AUTHORITY LADDER** (V178 tried to revert it, RETRACTED). **cave `0xC4B34` (164 B)**: disassembled every gp/tp access — **7 READS of control cells, all 5 WRITES to `gp-0x1511/13/14`, the CAN scratch it owns ⇒ TELEMETRY-ONLY, CLEAN**. **code bytes**: V103 arm, Lever B, V42 fix, telemetry — all documented. 🛑 **The firmware search is COMPLETE and this time VERIFIED complete rather than declared** — twice today I said it was finished and was wrong, and both gaps were found by reading BYTES, not the record. ✅ **SIX builds: V173/V174 (poles) · V175 (poles + engaged inertia revert) · V176 (strongest) · **V177 = FLY FIRST** (one cell, K1 10x->Honda) · V179 (completes Honda's inertia lane). V178 RETRACTED.** 🛑 **Nothing further can be settled without the car.**
- ✅🛑★★★★★ [**MODE-PROOFED FINAL: the damper is LIVE at creep when ENGAGED, DEAD in manual**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **this flipped three times; this is the pinned answer.** Index selector, by disassembly: `0x34502 ld.bu 0x63fd,gp,r13` · `0x34506 mov 0xc9e9c,r16` · `shl 2` · `add` · `ld.w` ⇒ **`gp+0x63FD`, the SAME byte `FUN_00036c12` uses for `0xCBE74`**, and the car runs **m24 = MANUAL, m26/27 = ENGAGED**. Records: **m24 FactorC `Y=[0,234,429,908]` STOCK** · **m26 `Y=[429,...]`, m27 `Y=[426,...]` (Y[0] 0->429/426)** · FactorE m26/27 **X[0] 60->12, Y[1] 140->539**; m24 stock. **X[0]=2240 = 35.0 km/h and Y[0] is the BELOW-RANGE FALLBACK** ⇒ below 35 km/h **manual = 0 (dead), engaged = 429**; an 8 Hz ratchet makes ~50 deg/s which clears FactorE's knee ⇒ **ch0 = (429 x ~310)>>10 = ~129, the damper IS working at creep WHEN ENGAGED.** 🛑 **THE THREE FLIPS: (1) read `0xD77xx` directly, said live — RIGHT; (2) resolved the pointer table at indices 0..3, found stock, retracted — WRONG; (3) resolved at the ACTUAL mode indices 24/26/27 — returns the same `0xD77xx` records as (1).** ⊕ **THE LESSON IS NOT "resolve the pointer table" — I did and still got it wrong. It is: RESOLVE IT AT THE MODE INDEX THE CAR ACTUALLY RUNS.** ✅ **Still available, mode-proofed**: FactorC m26/27 Y[0] 429/426 vs an in-range max of 908 ⇒ creep damping can be ~2x'd; and the knot step is **measured harmless** (272 crossings vs 1069 controls, ratio 1.030, null [0.863,1.190]). ⊕ **ENGAGED-ONLY**, so it is separable by the card's existing engaged-vs-manual contrast.
- 🛑🛑★★★★★ [**V182 RETRACTED — FactorC's axis is `gp-0x6a5e`, NOT speed; the damper is a FIVE-factor product**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00034350`: `gp-0x6bd0 = (((clamp(gp-0x698a,0x400)*L1>>10)*FactorC>>10)*L3>>10)*FactorE>>10`, negated when `gp-0x6abe>0`, then clamped. **Indices: L1 by |gp-0x6bcc| · FactorC (`0xC9E9C`) by `gp-0x6a5e` · L3 (`0xC9DB4`) by `gp-0x6a10` (abs steering angle) · FactorE (`0xC9F84`) by `gp-0x6ac0` (resolver/FOC ELECTRICAL RATE) · L5 clamp by `gp-0x6ac2`.** **GATES: FactorC needs `gp-0x67f4==1` and `gp-0x6a5e<=0x7d00` else it is 1024 (UNITY, not 0); FactorE needs `gp-0x6ac0<0x32c9` and `|gp-0x6abe|<=0x6590` else THE WHOLE PRODUCT IS ZEROED.** 🛑 **V182 raised FactorC's fallback believing X[0]=2240 = 35.0 km/h — NUMEROLOGY (2240/64=35 by coincidence); the axis is `gp-0x6a5e`.** Artifacts `SUPERSEDED-DO-NOT-FLASH-WRONGAXIS-*`. ❌ **The 272-crossing knot null is VOID for this build** — it tested speed crossings against a non-speed knot. 🛑 **FOURTH such error today** (V178 authority ladder · the damper flip-flop · FS=100 · this). **STANDING RULE, superseding the softer ones: BEFORE ANY EDIT TO A LERP, QUOTE THE INDEX EXPRESSION FROM THE DECOMPILE.** Not the X values, not a unit conversion, not a neighbouring table. If it cannot be quoted, the axis is unknown and the edit is a bet. ✅ **Correctly established**: `w[0]` (`0xC63A0`) IS a real second multiplier on this product (1024; V72/V77 moved it 2x on-car fault-free) · the damper has a **hard OFF switch** · FactorC's gate **fails open to UNITY** · all five indices are now named. 🛑 **No damper build until `gp-0x6a5e` and `gp-0x6ac0` are characterised on the corpus.**
- ✅★★★★ [**Integrity check after two retractions — the shelf is CLEAN**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — all eight surviving builders re-run **bit-for-bit with every assertion passing** (V173 25/25 · V174 27/27 · V175 26/26 · V176 28/28 · V177 21/21 · V179 19/19 · V180 30/30 · V181 27/27), every artifact matches its recorded hash, **exactly one `.rwd` each**, and **none touches a retracted cell relative to its own base** (V181 is byte-identical to V158 at `0xD77DA`/`0xD77EE`/`0xC6598`/`0xC65C4`). Both retracted images are `SUPERSEDED-DO-NOT-FLASH-*` with builders that raise on entry. ⚠ **My first pass FLAGGED ALL EIGHT because it compared against the FLYING build instead of each build's own base** — it caught V158-era inheritance and called it a defect. **A comparison is only as good as its reference.** 🛑 **AND IT SURFACED A REAL FACT: `0xD77DA`/`0xD77EE` are 0 on stock AND on V122 (the FLYING build), but 429/426 on V158 and therefore on EVERY build V173..V181** ⇒ **all my builds already carry a V158-era FactorC damper edit that the car does not have.** ⊕ That partly rehabilitates the damper direction (V158 already moved this fallback the way V182 tried to move it further), **but the axis is `gp-0x6a5e`, not speed, so V182 stays retracted.**
- 🛑★★★★★ [**THE BIQUAD IS ENGAGED-GATED — so every pole edit is ENGAGED-ONLY, and V184 is a TWO-variable test**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I claimed in V184's docstring that the assist-section poles *"act in both modes"*. **WRONG.** From the images: stock has `0xC649B`=0 with arm source `gp-0x671a`; **V103 onward (including V122 FLYING and V184) has `0xC649B`=1 with the arm source repointed to `gp-0x6806`, the LKAS engagement flag** ⇒ **the biquad runs ONLY while engaged.** ➕ That is a **SECOND kit-created engaged/manual asymmetry on the car**, which the mode-record enumeration could not find because it is a **code path, not a data table** — the enumeration was sound for what it covered; I overstated its scope. ❌ **CONSEQUENCE: the engaged-vs-manual ratio no longer isolates the inertia dose**, since V184 carries two engaged-only changes. ✅ **REPLACEMENT DISCRIMINATOR — use the BAND signature instead**: poles = **grind −16.0 dB / ratchet −8.8 dB** (hits the GRIND hardest); inertia revert = **ratchet only, grind ~unchanged**. So *grind falls hard + ratchet modestly* → poles; *ratchet falls, grind ~flat* → inertia dose; *both in proportion* → both. **This needs no manual pass.** ➕ **Is the engaged-only biquad itself the ~15x amplifier? Probably not**: unarmed is a BYPASS (`H≡1`) and armed `|H|<=1` everywhere, so arming only REMOVES gain ⇒ engaged sees LESS HF gain than manual. ⚠ **But PHASE is the exception** — an engaged-only lag costs stability margin even when magnitude falls; small with Honda's coefficients, **large with V184's retuned poles**, which is a real previously-unstated engaged-only cost of the pole lever. [BELIEF, structural.]
- ✅★★★★★ [**GATE 2 PHASE passes for V184 at the ratchet — and the engaged-only cost is quantified**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the biquad being engaged-gated forced a phase check the earlier magnitude-only GATE 2 never did. At **8.17 Hz** the loop-path change is **|r| = 0.3642, arg(r) = −61.51°, Re(r) = +0.1737** ⇒ since the destabilising direction is **L → +1**, both |r| < 1 AND Re(r) < 1 mean the rotation moves L **away from +1 on both axes** ⇒ **the phase lag does NOT give back the 64 % gain reduction.** **max |H_V184/H_flying| = 0.9995 over 0.1–499 Hz ⇒ never amplifies anywhere.** ⚠ **THE COST, as a number**: engaged-vs-manual phase (manual is a BYPASS, H=1) goes **1 Hz −1.35° → −17.78° (+16.4°)**, 8.17 Hz −11.1° → −72.7°, 21 Hz −30.0° → −91.8°. **+16.4° of engaged-only lag at 1 Hz is a real phase-margin cost** and bears on the operator's THIRD goal, since added loop lag is what worsens command oscillation. 🛑 **Whether it reaches openpilot's loop is NOT established** — the biquad is on the **torque-fed** path (`gp-0x6b86`) and openpilot's command uses a different lane; coupling is **unestablished, not absent** [BELIEF small]. **PRE-REGISTERED: if peak command oscillation WORSENS while the grind improves, suspect this first.** ⊕ The lag is nearly flat above ~8 Hz (+61.5° at 8.17, +61.8° at 21) — the pole sits well below the band, so ratchet and grind see the same rotation.
- ❌✅★★★★★ [**Engagement excites a NARROW mode at 8.40 Hz (33.1x, speed-matched) — and the frequency signature does NOT settle the V184/V185 fork**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — speed-matched 300/300 windows (15.2 vs 14.6 km/h) + permutation null. **engaged/manual PSD ratio: 3.91 Hz 0.79 · 8.20 Hz 30.56 · 8.40 Hz 33.06 · 15.0 Hz 8.72 · 25.0 Hz 5.48.** 🛑 **MY FIRST VERDICT WAS WRONG**: the script said “slope +0.461 exceeds its null [−0.119,+0.113] ⇒ inertia fingerprint ⇒ V185”, but **an ω² term needs b ≈ +4 in PSD, not 0.46**, and the **SHAPE test kills the power-law model: peak 33.06x at 8.40 Hz vs band-edge mean 1.48x = a 22.3x PEAK.** Fitting a line to a peaked function manufactures a slope; its significance says nothing about ω². Verdict logic now tests SHAPE first. ⇒ **the V184/V185 fork stays OPEN — only the car settles it.** ✅ **What it DID buy**: the record's *~15x* engagement figure is superseded by a much sharper one — **33.1x at 8.40 Hz, speed-matched, and NARROW (22.3x above the band edges), with the ratio BELOW 1 (0.79) at 3.9 Hz** ⇒ **engagement does not raise torque broadly; it SUPPRESSES ~4 Hz and EXCITES a specific mode at ~8.4 Hz.** A resonance being driven, not a gain change. ⊕ Independently re-confirms the mode centre at **8.40 Hz**, inside the ±0.71 Hz wander band.
- 🛑★★★★★ [**RECALIBRATION: engagement amplifies 8.4 Hz by ~3.6x, NOT 15-33x — I quoted the confounded figure all session**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `cs_tq` is the **DRIVER torque sensor**, and engaged means the driver largely is not steering, so an engaged-vs-manual torque ratio conflates **engagement** with **hands-off**. Stratified on `cs_press`: **ALL 20.94 [16.29,41.43]** · **hands-ON 3.58 [1.36,14.92] ← THE FAIR NUMBER** · hands-OFF 18.34 [5.44,68.85]. ✅ **Real** (hands-on CI excludes 1) but **~3.6x**. ⊕ **The kit's record HAD IT RIGHT** — [[accord-engagement-amplifies-6-9hz]] says **2.8x**, inside the CI. **My session figures drifted; the record did not. Read every earlier “~15x” in this session as ~3.6x [1.36,14.92].** ⚠ hands-on cell is small (68/77) hence the wide CI; tightening needs matched hands-on exposure = a drive request. ❌ **The 4.7 Hz “crossover” is DEAD, same confound**: hands-ON finds **NO crossover** in 2–20 Hz (CI [5.83,18.97] spans the band) while hands-OFF gives 5.38 [4.59,6.12] ⇒ the sub-4.7 Hz suppression was **the driver not steering**, not loop dynamics. Withdrawn before anything was built on it. ➕ **Consequence for builds**: the target is **~3.6x**, so a lever removing a **3.0x engaged-only dose** (the inertia revert, V185) is exactly the right order of magnitude — this STRENGTHENS that build.
- 🛑★★★★★ [**The corpus CANNOT answer hands-matched questions — only 21 engaged hands-on creep windows exist**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — three tests in a row failed their controls for one structural reason. Asking *does the ratchet exist in MANUAL?* (which decides whether an engaged-only lever can ever eliminate it): **hands-ON windows in the whole corpus are 21 engaged / 11 manual**, and at a FIXED 8.40 Hz the excess is **ENGAGED 0.71x CI [0.43,1.31]** and **MANUAL 1.45x CI [0.80,2.72]** — **neither significant.** 🛑 **The ENGAGED arm is the POSITIVE CONTROL and it FAILS**, so the manual null means nothing and the auto-verdict is **withdrawn**. ➕ **METHOD FIX**: the first version used `argmax` over the band and the point estimate landed **OUTSIDE its own bootstrap CI** (1.47 vs [1.48,3.71]) — a max-over-band statistic is **upward-biased under resampling**. Switched to a FIXED frequency, which also fixed a backwards manual>engaged ordering. **RULE: never bootstrap a max-over-band statistic; if the point estimate falls outside its own CI the STATISTIC is biased, not the data interesting.** ✅ **ACTIONABLE**: the corpus is 1606 hands-off vs 21 hands-on engaged creep windows, so **the Stage 1 drive must be HANDS ON THE WHEEL** — added to the cards. `cs_tq` is the DRIVER torque sensor, so a hands-off pass measures mostly the absence of driver input.
- 🛑★★★★★ [**Changing a drive card's EXPOSURE silently invalidates its power figures — I did this and caught it one round later**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I changed the card to demand HANDS ON because of the `cs_tq` confound. **Continuous 15 s engaged creep windows: ALL = 27, HANDS-ON = ZERO.** So (1) every threshold on the card (grind ANSWERABLE margin 2.89x · ratchet needs 2 passes · LKAS not measurable) was computed on **hands-OFF** windows and **nothing supports it for a hands-on pass**, with **no data to recompute from**; and (2) it **broke comparability with the entire 27-window historical baseline**, which is hands-off. ✅ **FIX: ask for BOTH, ~30 s** — **1a** 15 s driven as he normally does = **SCOREABLE today**, thresholds apply, comparable to history; **1b** 15 s **HANDS ON** = answers the confound and builds the first hands-on baseline, **explicitly labelled thresholds-UNKNOWN and baseline-building, NOT scored**, so it cannot be over-read. ⊕ **GENERAL LESSON: changing what a drive asks for invalidates every power figure computed on the old exposure. Re-run the power check against the NEW exposure, or the card promises a result the drive will not deliver.**
- ✅★★★★★ [**THE GRIND IS ENGAGED-ONLY — 7/7 routes, manual below its own null every time**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `grind_engaged_vs_manual.py`, per-route slope-matched nulls: **r78/V91 6.1 vs 3.5 · r7e/V96 28.9 vs 3.2 · r7f/V96 14.3 vs 3.5 · r96/V102 248.2 vs 4.0 · ra6/V106 25.3 vs 4.0 · r1e/V107 27.7 vs 2.7 · r24/V122 (FLYING) 14.0 vs 3.9** — and **the MANUAL arm is BELOW its own null on ALL SEVEN** (1.5–3.0 vs nulls 3.8–4.9). ⇒ **the grind does not exist without engagement**, replicated across six builds including what is on the car ⇒ **an ENGAGED-ONLY lever CAN eliminate it**, and V184's poles (engaged-gated) are correctly targeted. ⚠ **This is the GRIND, not the ratchet** — the same question for the ratchet is still unanswered because the corpus has only 21/11 hands-on windows; that is why Stage 1b exists. ✅ **Both scorers dry-run clean**, but `score_band_excess.py` carried **stale attribution naming V172/V173/V158**, none on the shelf — it would have misled a V185 drive. Updated to the real fork (grind moves ⇒ poles ⇒ V184; grind flat ⇒ expected on V185, read the ratchet; ratchet down + grind flat ⇒ the inertia revert) and to note 427 now carries `gp-0x6ac0 >> 4` with the gate at field 812. ⊕ **Testing the instrument before the drive is worth as much as another lever.**
- 🛑🛑★★★★★ [**The slope-corrected EXCESS endpoint CANNOT see a broadband filter — it measures PEAKINESS, not LEVEL**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — applying each build's `|H|²` to the REAL flying spectrum (r24/V122) and re-running the scorer's own estimator: **FLYING grind 11.1x / ratchet 26.7x · V185 11.1 / 26.7 · V184 11.3 / 30.0.** 🛑 **V184's −16 dB grind attenuation is INVISIBLE to it**, because the excess divides band power by a power law fitted OUTSIDE the band and a low-pass **steepens that fit too**. ❌ **The card rule “grind moved ⇒ the poles did it” is WRONG and withdrawn** — and the scorer text I wrote one round earlier said the same wrong thing; both corrected. ✅ **The endpoint that DOES see them is the SPECTRAL SLOPE, which the scorer already prints**: **FLYING 1/f^2.671 · V185 1/f^2.671 (Δ 0.000) · V184 1/f^4.531 (Δ +1.860)** — and **4.53 is far outside the entire historical range 0.80–2.37**, so one pass is unmistakable. ✅ **CORRECTED DISCRIMINATOR: slope → ~4.5 = poles live (V184) · slope ~2.7 = V185 · excess falls = the INERTIA REVERT (both carry it) · nothing moves = both accounts fail.** ➕ **CONSEQUENCE FOR THE FORK: V184 and V185 are INDISTINGUISHABLE on the ratchet/grind excess**, because those respond only to the inertia revert. The only thing V184 buys is the slope change — bought with **+16.4° of engaged-only phase lag** ⇒ **V185 is the better first drive: same measurable ratchet effect, none of the phase risk.**
- ✅★★★★★ [**The pre-registration is COMPLETE — one binary threshold, and MANUAL proves it is reachable**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — on `r24` (the FLYING build), scorer's own estimator, 9 engaged / 26 manual creep windows: **GRIND 15–25 engaged 11.1x vs manual 2.3x** · **RATCHET 5–12 engaged 26.7x vs manual 2.8x**, **null ~3.9 — BOTH manual arms BELOW it.** ⇒ “excess below 3.9” is a **demonstrated reachable state**, not an aspiration: the car reaches it whenever LKAS is off. ⊕ Also a **single-route answer to the question the hands-on test was too underpowered to settle**: on r24 the RATCHET is engaged-only too (manual 2.8 < null 3.9, 26 windows) — not hands-matched so it does not replace Stage 1b, but it points the same way as the 7/7 grind result. ⇒ **THE DRIVE REDUCES TO ONE BINARY QUESTION: does the engaged excess fall below ~3.9x?** below ⇒ **symptom GONE by the instrument** · falls but above ⇒ the inertia lane contributes but is not the whole story · unchanged ⇒ **the inertia-dose account FAILS**. ⊕ The POLES are tested separately by the **spectral slope** (2.671 → 4.531 on V184, outside the whole 0.80–2.37 history) because they cannot move the excess numbers at all.
- 🛑🛑★★★★★ [**EVERY endpoint in this kit is RELATIVE — the control-band ratio INVERTS V184's verdict**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — both families divide by something a broadband filter also attenuates: the slope-corrected excess by a fit OUTSIDE the band, ~every other scorer by 30–40 Hz. On real r24 data V184 gives **grind ABSOLUTE x0.025 (−15.9 dB, a 40x WIN)** but **ctrl-band ratio x3.0 UP** and excess x1.02 flat, because **the control band itself falls −20.8 dB.** 🛑 **I would have reported a 40x fix as a 15x REGRESSION.** ✅ **FIXED: absolute band power restored to `score_band_excess.py`** with the worked example printed inline. Raw power had been withdrawn for spectral tilt — **that was the mistake**; tilt is handled by REPORTING THE SLOPE, not deleting the level. ➕ **RULE: compare ABSOLUTE across builds; a ratio is valid only WITHIN a build, where the divisor is common.**
- ✅★★★★★ [**V187 — a NEW LEVER CLASS: Honda's NOTCH moved onto the ratchet**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V187.md) — every filter build in the arc (V43, V173/V174/V184) moved the **denominator** (poles ⇒ a low-pass). **V187 moves the NUMERATOR — never done.** `z²+B0·z+1` has roots of product 1 ⇒ **always on the unit circle** ⇒ the numerator is a **perfect notch** and **B0 alone sets its frequency**. Honda: 55.226 Hz; V187: **8.80 Hz**. ➕ **The tradeoff is FORCED, not a search failure**: unity DC gain pins `B4=(1+A8+AC)/(2+B0)`, so a notch near 8 Hz forces the poles within ~0.05 of the unit circle ⇒ REAL poles land their corner at 8 Hz too, reproducing V184's −40.5°. **One biquad cannot serve LKAS phase + ratchet attenuation + 55 Hz protection.** A notch escapes because its phase returns to ~0 away from itself: **−7.8 dB for −9.95° = 0.78 dB/deg vs V184's 0.22.** ✅ **FITTED MINIMAX over 67 routes** (per-route peaks p10 7.34 / med 7.81 / p90 8.59 Hz) — beats the pooled-average fit on **both** median (6.0x vs 4.0x) and p90, so not a criterion artifact. Gates: DC 0.999972 · max|H| 1.1403 · cal-only. ⚠ **COST: Honda's 55 Hz null given up** — alias test (55.226 folds to 44.774 Hz) over **295 routes** finds median 0.99 / max 2.69 / zero above 3 vs controls 3.6–6.5 ⇒ no road-excited plant mode; **but the notch is active in every drive so a COMMAND-excited mode is NOT excluded.** BELIEF. Reflash V185 recovers.
- ✅★★★★★ [**V188 — the notch on the GRIND; one biquad, and the MECHANISM decides where it goes**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V188.md) — there is **exactly one biquad**, so one notch, and the middle ground is **DOMINATED**: notch 8.80 Hz ⇒ ratchet 6.0× / grind 0.9× / −10.0° · notch **19.40 Hz ⇒ ratchet 1.3× / grind 14.3× / −3.8°** · middle 14.10 Hz ⇒ 2.2× / 2.3× (**worse than both**). ➕ **THE MECHANISM DECIDES: the GRIND is a CLOSED-LOOP INSTABILITY** (21.09 Hz, **9,200× less power LKAS-off**, 2×2 attribution to `0xC6CD0`, 2.7–3.9×) ⇒ **a notch in the loop AT the unstable frequency is a CURE**; the **RATCHET is a PLANT resonance** (ζ 0.017–0.036, limit cycle excluded) ⇒ a notch only reduces **excitation**, and it already has its own lever (the engaged inertia revert). ⊕ The biquad is **engaged-gated** and the grind is **engaged-only 7/7** — an engaged-only filter against an engaged-only instability. ✅ 19 Hz is far from openpilot's band, so the notch can be **WIDE** (r 0.9300) and still cost only **−3.84° @3 Hz** — grind peaks run p10 15.74 / med 19.92 / p90 21.68 Hz, so **width matters more than depth**. Gates: DC 1.000002 · max|H| 1.3533 · cal-only · 30/30. `81c0845fdf22c3af…`
- ⚠★★★★ [**`0xC6CD0` was 4× when the grind fix was CONFIRMED, and is 6× now — but do NOT lower it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the two measured grind fixes **are** still carried (checked, not assumed, because V42's fix was once lost to a rebase): `0xC6446`=5244, `0x3AA96`=251, `0x454FE`=181. But the **gain the 2×2 named as the CARRIER of the ~23 Hz vibration** was **3564 (4×) when V88's grind fix was operator-confirmed** and is **5346 (6×) now** (V101 →8×, V102 →6×). 🛑 **Lowering it back cuts LKAS authority by a third** — reach is `(clip × cal(0xC6CD0)) >> 15` — which is the OPPOSITE of the operator's stated goal. **That tension is exactly why the answer is a NOTCH: keep the gain, remove its 23 Hz consequence.** ⊕ Supersedes the stale *“the 4× LKAS gain is frozen on every build”* memory, which predates V101.
- 🛑★★★★★ [**We created an engaged-only damper RELAY by accident — FactorC m27 `Y[0]` 0→426, and the FLYING build does NOT have it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V189.md) — auditing **every** FactorC mode record against stock, **exactly one deviates**: record `0xD77E4` (mode 27), `Y[0]` at **`0xD77EE`** is **426** where stock is **0**. **V122 MATCHES STOCK** ⇒ introduced in the V177–V183 chain and inherited by V185/V186/V187/V188. 🛑 **V184's “engaged == manual in every data table” fixed m26 and MISSED m27.** ➕ **WHY IT MATTERS:** FactorC gates the base-assist damper (`ch0 = FactorC×FactorE >> 10`) and **`Y[0]==0` in ALL 13 stock records** — dead at low index BY DESIGN, which is what makes Honda's surface **viscous not switched**. A non-zero `Y[0]` is **a RELAY**, and a relay here is what V80 shipped ⇒ **the worst grinding in the arc**. ⚠ Worse than a plain relay: **426 > Y[1]=233**, so the curve steps UP then FALLS — **a defect, not a calibration.** ✅ **V189 = V188 + `0xD77EE` 426→0** (one int16, 2 payload bytes, 38/38). All six damper tables now m26==m24 and m27==m24-or-stock. ⊕ **Inert if mode 27 is unreachable, removes a live relay if not — strictly toward stock, so no configuration is worse.** `71a7032a485ec825…`
- ✅★★★★ [**A notch cannot easily destabilise its own low shoulder — lag and attenuation grow TOGETHER**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the worry was that a notch inside a loop adds lag BELOW itself and could grow a new mode there. On the pooled 67-route engaged spectrum with V188's notch: **9.2 Hz excess 8.71 / |H| 0.852 / lag −14.0°** · 12 Hz 2.81 / 0.709 / −21.0 · 15 Hz 2.26 / 0.486 / −29.9 · **16.2 Hz excess 2.97 / |H| 0.372 / lag −34.0°**. ✅ **No frequency has high excess, high retained gain AND large lag at once** — because for a notch the loop gain is cut in proportion to the phase spent. That is exactly why a notch is the standard tool here, and why a LOW-PASS (V184, −40.5° at 3 Hz with gain intact below) is the dangerous shape. 🛑 Still falsifiable on the drive: **a NEW peak at 13–16 Hz** means back the pole radius off.
- ✅★★★★★ [**The engaged/manual asymmetry space is EXHAUSTED on V189 — and mode 27 is UNREACHABLE, so that revert is INERT**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 🛑 **CORRECTION:** V73's probe settled the mode over **104,061 frames**: row 11 `TVCA4`, **e012=24 disengaged / e014=26 engaged**; mode 27 would read **11** in the 4-bit field and **only 8 and 10 ever appeared** ⇒ **V189's FactorC m27 revert is INERT** (still correct, but a cleanup not a fix). ✅ **The sweep that matters: EVERY mode-indexed table, m24 vs m26** — strict scan (≥3 breakpoints, STRICTLY increasing X, real span) finds 7 tables, of which only `0xC7B40` differs **and it differs ON STOCK TOO** (4181 vs 4114) ⇒ **Honda's own**; plus the six damper tables asserted in the builder, all `== m24`. ⇒ **NO mode-indexed table on V189 differs 24-vs-26 that stock does not.** ⚠ A looser first pass reported six “asymmetries”, **five of them junk from my own heuristic** — `X=(3,3,3)`, `X=(5,5,5,5,5)` passed because it only required NON-decreasing X. **Require STRICT increase and a span floor, or the scan manufactures tables out of arbitrary data.** ➕ **WHAT THIS PINS: only TWO things remain engaged-only — the LKAS command, and the biquad ARM (ours since V103, now the grind notch).** So the grind rests on the notch (a closed-loop instability, broken at its own frequency) and the **ratchet rests entirely on the INERTIA REVERT**, the flying build's only engaged-only dose. 🛑 **If the ratchet survives V189, its engaged-only cause is NOT in the calibration — it is in openpilot's command loop, and no firmware cal lever reaches it.**
- ✅★★★★★ [**The biquad gate IS engagement-gated — and V103's patch has THREE sites, not the two the lineage names**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — stock: `ld.bu -0x671a,gp,r9` · `cmp r12,r9` · `setfnc r6`. **Ours: `ld.bu -0x6806` (the LKAS flag, disp = `sext16(hw2 & 0xFFFE)`) · `cmp r0,r9` · `setfne r6`** ⇒ live gate = **`cal(0xC649B)==1 AND gp-0x6806 != 0`**. ⚠ **I asserted mid-session the gate was BROKEN and the biquad always-on — WRONG**: I compared only the two sites `BUILD-LINEAGE.md` lists, and `setfnc` after `cmp r0,r9` *would* be always-true; **the third site (`e9 37`→`ea 37`) is what makes it correct.** 🛑 **METHOD: the condition nibble was confirmed EMPIRICALLY** — scan the setf family (`hw1 bits4-10 == 1111110`, `hw2 == 0`), then let Ghidra name a real instance: nibble **`0xA` at 0x16034 = `setfne`**. **Never hand-decode a condition nibble.** ✅ **PAYOFF: the section runs ONLY engaged, so V188/V189 give up Honda's 55 Hz null ONLY WHILE ENGAGED — manual driving is bit-for-bit stock.** That confines the one unquantifiable risk on the notch builds to engaged driving.
- ❌★★★★★ [**NO SECOND DORMANT FILTER EXISTS — 48 dormant cals, all resolved**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — hunted every cal with the biquad's gate signature (**tp-relative BYTE cal reading 0 in stock**). Of the 48, those touching steering are: **`0xC649B`** the biquad arm (used at V103) · **`0xC64AB`/`0xC64AC`** MUTE switches (`cal==0` ENABLES the term) in the `gp-0x67ac==1` branch gating the **return-centre/detent term, already measured DEAD ENGAGED** (0.0000 / 75,227 frames) · **`0xC40EB..EE`** **DIAGNOSTIC SENSOR OVERRIDES** — `if (magic==0x49d6b173 && cal==0xE9) gp-0x6abc = base + value*cal(0xC6134)/1000;` — Honda's factory injection path for the four sensor channels, **not a filter**. ⇒ **ONE biquad, ONE notch: the V188/V189 allocation is FINAL.** ✅ **The delivery path is now decompile-confirmed end to end**: `FUN_00041464` (sensors; `gp-0x6c2c = EMA(accel)>>9`, coefficient `tp+0x50DC` = **`0xC40DC`**, the cell V179 moved) → `FUN_000352b4` (biquad) → **`gp-0x6b86`** → `FUN_0003aa2c` aggregator (clamp ±12288) → `gp-0x6b94` → governor → motor ⇒ **the notch really does reach the motor.** ⊕ A second parallel accel EMA exists: `>>7`, coefficient `tp+0x50DA` = **`0xC40DA`** → `gp-0x6c2e`. Unexplored. 🛑 **TRAP HIT AND FIXED: `ld.bu disp16[tp]` has TWO opcode fields — bits5-10 `0x3D` ⇒ disp ODD `(hw2&0xFFFE)|1`, `0x3C` ⇒ EVEN `hw2&0xFFFE`.** Filtering on 0x3D alone caught only the odd half **and reported every address one too low**, inventing a phantom `0xC649A`. ✅ **Validate any cal scan by requiring a KNOWN cell to appear** — here `0xC649B` at `0x359FE`.
- ✅★★★★★ [**V190 — a SECOND ω² acceleration feedback, never touched, and it PEAKS AT CREEP**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V190.md) — tracing the 2nd accel EMA (`gp-0x6c2e`, cal `0xC40DA`) found a whole path: **`FUN_00036f30`** builds `gp-0x6bc2 = clamp(((L·a)>>6)·sign(gp-0x6752)·gp-0x69be>>6, ±gp-0x6bc0)` with **L = LERP(0xC68EA/0xC68F2, speed)**, and **`FUN_00037fe6`** sums it into **`gp-0x6ad6`, the torque-tracking REFERENCE**, via enable flag **`0xC64AE`**. 🛑 **THE RECORD WAS WRONG about this sum**: not *“six terms in FUN_00038148, weights 0xC63A0..0xC63AA, only w[3] frequency-selective”* — it is **`FUN_00037fe6`, SEVEN terms, flags `0xC64AD..0xC64B3`, and they are ENABLE FLAGS (0/1) not gains**, all 1 in stock/V122/V189 (siblings `0xC64AB`/`AC` ship at **0**, proving 0 is supported). ⇒ **TWO ω² terms exist, not one.** ✅ **Right shape for the ratchet:** ω² ⇒ 66× stronger at 8.2 Hz than 1 Hz; speed weight **64 at 1 km/h → 41 at 24 → 32 above 40 km/h = 2× STRONGER AT CREEP**, where the ratchet lives; and **ZERO at DC**, so it costs **no LKAS authority and no apparent mass** — the operator's standing constraint. **V190 = V189 + `0xC64AE` 1→0, one byte, 41/41.** `ab75a383fad5c65a…` ⚠ **SIGN IS BELIEF:** the destabilising conclusion rests on a 5-link polarity chain. **If inverted, the term was DAMPING and the ratchet gets WORSE** ⇒ revert to V189. Pre-registered.
- 🛑❌★★★★★ [**V190 RETRACTED — I verified the sign and it went the OTHER way; the term is DAMPING**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V190 disabled `gp-0x6bc2` on the BELIEF it was destabilising. **Decompiling `FUN_0003a382`, the only reader of `gp-0x6ad6`, killed that belief:** `iVar30 = gp-0x4f60 - clamp(gp-0x6ad6)` ⇒ **error = MEASURED − REFERENCE (record CONFIRMED)**, then PID(error) × `gp-0x6752`(=−1) → `gp-0x6ad4`, which is an additive aggregator term. ⇒ chain **now 5 links PROVEN**: `gp-0x6bc2 ~ −a` → `gp-0x6ad6 ~ −a` → `error ~ +k·a` → `gp-0x6ad4 ~ −k·a` → **the aggregator OPPOSES acceleration = POSITIVE DAMPING, stabilising.** ⇒ **disabling it would most likely make the ratchet WORSE.** ⊕ Honda ships the flag **enabled**, which agrees — a manufacturer adds accel feedback for damping. ✅ **V189 restored as the recommendation; the V190 card is marked NOT RECOMMENDED** (artifact kept as a deliberate probe, expected to worsen). 🛑 **PROCESS: the lever was labelled BELIEF with its failure mode pre-registered, and verification killed it BEFORE it cost a drive. Do not ship a lever whose sign rests on an unverified chain when the chain is decompilable in one tick.**
- ✅★★★★★ [**V190 UN-RETRACTED — judge a sign RELATIVE to a characterised term, never in isolation**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I retracted V190 after deciding `gp-0x6bc2` was damping. **That was wrong, and the error is instructive:** I asked *“does opposing acceleration mean damping?”*, which **depends on the aggregator→plant sign I had myself flagged as UNPROVEN.** ✅ **The answerable question is RELATIVE:** `d(gp-0x6ad4)/d(gp-0x6ad6) = (−K)(−1) = +K` — **the two inversions CANCEL** — so `gp-0x6bc2 → gp-0x6ad4 ~ −a`, while **`gp-0x6b26 ~ −a` added directly**. ⇒ **SAME SIGN, same class.** Since [[accord-gp6b26-is-inertia-not-damping]] (★★★★★) makes `gp-0x6b26` **anti-damping**, so is `gp-0x6bc2` ⇒ **V190 is directionally correct.** ⊕ Empirical support: the flying build carries **3×** Honda's inertia dose **and ratchets 3.58× more engaged** — if these were damping, tripling one would have REDUCED the ratchet. ➕ **GENERAL RULE: when an absolute sign depends on an unproven link, ask whether the new term matches a term already characterised THROUGH THE SAME LINK — the unknown link cancels.** ✅ Genuinely gained this tick regardless: **`error = measured − reference` and `gp-0x6ad4 = −K·error` are now PROVEN** from `FUN_0003a382`, not BELIEF.
- ✅★★★★★ [**V191 — the firmware BOOSTS its anti-damping 4.2× after its OWN detector sees oscillation**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V191.md) — `gp-0x671a` is Honda's **hard-reversal counter** (oscillation detector, CEIL=5 at `0xC64FA`). `FUN_00036c12`: **counter < `cal(0xC64FD)`=5 ⇒ L = LERP(`0xCBE74`[mode]); counter saturated ⇒ L = `cal(0xC640A)` = −8192**, and −8192 is **4.2× stronger than the LERP weak end −1966** ⇒ **detecting oscillation can make the anti-damping term 4.2× STRONGER — positive feedback on the thing it just detected**, a plausible reason the ratchet SUSTAINS (ζ 0.017–0.036). ✅ **V191 = V190 + `0xC640A` −8192→0**, one halfword, 30/30. `82ce1db4e7309937…` ✅ **BEST-SHAPED LEVER IN THE ARC: the cell is read ONLY on the counter≥5 branch, so it is PROVABLY INERT outside an oscillation event** — no steering-feel or LKAS-authority change on a calm road, by construction rather than measurement — **and it acts exactly during the symptom.** ⊕ Does not depend on `gp-0x6a5e` at the operating point (zeroing removes the term outright). ✅ **IT ALSO SETTLED A REAL WORRY ABOUT V189**: the same branch decides whether the inertia LERP is used at all — had the counter normally sat ≥5, **V184/V189's inertia revert would have been INERT** (the mode-27 failure class). It is not: the counter is a reversal count clamped at 5, so normal driving is BELOW threshold and **the LERP path IS live.** ⚠ Sign basis shared with V190; if inverted the term was DAMPING and the ratchet worsens ⇒ revert to V189.
- 🛑✅★★★★★ [**The gp-0x671a detector map is COMPLETE — Honda uses it to DAMP; and V191's “4.2× boost” claim is CORRECTED**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — all three consumers read: **`FUN_00036c12`** counter≥5 ⇒ `L = cal(0xC640A) = −8192` instead of the LERP (**the only place the assist gain itself changes** — V191 zeroes it) · **`FUN_0003a382`** two counter-indexed LERPs with X = [5,10,15] and [5,8,10] — **the counter is CLAMPED AT 5 and the first breakpoint IS 5, so `5 < counter` is never true ⇒ both return Y[0] permanently, INERT** (the recorded worry that T is “a shape parameter on a load-bearing lane” holds only if T or CEIL move) · **`FUN_00035b20`** switches the slew-limit curve: normal `Y=[358,358,461,512]` vs **oscillating `Y=[358,307,307,307]` with breakpoints stretched 2×** ⇒ **Honda TIGHTENS the slew limit when it detects oscillation — the detector is a DAMPING mechanism.** 🛑 **CORRECTION: I justified V191 as undoing a “4.2× boost”. That compares against the LERP's HIGH-index end.** `gp-0x6a5e` is FactorC's index and the record shows it below FactorC's first breakpoint 2240 across **100% of the micro regime**, so **at creep the LERP returns −9830…−5734 and −8192 sits INSIDE that range — not a boost.** V191 remains valid but its honest description is **“remove the anti-damping term when the detector saturates”**. ⭐ **NEXT LEVER, same ideal shape: `0xC691A..0xC6920`, the OSCILLATING slew curve** — lowering it damps harder during a detected oscillation, is **provably inert in normal driving** (read only on the counter≥5 branch), and pushes the direction **Honda already chose**, so it is far safer than a sign bet.
- ✅★★★★★ [**V192 — Honda's oscillation response gives NO tightening at low index; V192 closes the gap**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V192.md) — `FUN_00035b20` switches the slew limit `gp-0x69a0` on the reversal counter: **NORMAL `Y=[358,358,461,512]` on `X=[320,1600,3200,4480]` vs OSCILLATING `Y=[358,307,307,307]` on `X=[640,3200,6400,12800]`.** 🛑 **The two are IDENTICAL at the low index (358)** ⇒ Honda's oscillation response does **nothing** there, and the oscillating breakpoints are **stretched 2×**, pushing what tightening exists further out. ✅ **V192 applies Honda's OWN ratio (512→307 = 0.600) once more to the whole oscillating curve → `[215, 184, 184, 184]`.** Four halfwords at `0xC691A`, 32/32. `c36b6ca12e27633f…` ⭐ **SAFEST LEVER OF THE SESSION: (a) the curve is read ONLY on the counter≥5 branch so it is provably inert in normal driving — the NORMAL curve is byte-untouched; (b) THE DIRECTION IS HONDA'S OWN — Honda tightens on detection, V192 tightens more, so unlike V190/V191 it is NOT a polarity gamble; (c) mechanism explicit — `gp-0x69a0` rate-limits the boost-table walk in `FUN_000352b4`, `delta = ((step * limit * 4) >> 12)`.** ⚠ Watch for a brief **HESITATION** replacing the ratchet ⇒ too tight; back off to ~0.8.
- 🛑⭐★★★★★ [**Honda's oscillation detector has a FREQUENCY WINDOW and the ratchet falls OUTSIDE it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V193.md) — `FUN_000428d4` counts reversals of **`gp-0x6c2c` (the acceleration EMA)** past ±**T=12800** (`0xC620A`), but **a reversal only counts if the opposite peak arrives within HYST ticks** (`0xC64DD` = **50**). `FUN_000428d4`/`FUN_00041464`/`FUN_000352b4` **share the single caller `FUN_0002214a`** ⇒ same **1 kHz** task ⇒ HYST = **50 ms** ⇒ **countable only above 10 Hz.** 🛑 **Ratchet 7.34–8.59 Hz (half-period 58–68 ms) is OUTSIDE; grind 15–25 Hz is inside.** ⇒ **the detector NEVER fires for the ratchet, so V191 and V192 — both gated on counter≥5 — are INERT FOR IT** (predictable BEFORE the drive). ➕ **CORRECTS A RECORDED ASSUMPTION: the lineage treats T as the knob (*“lowering T changes five things”*). T is the WRONG knob — no amount of lowering an AMPLITUDE threshold makes an 8 Hz oscillation countable when the DWELL expires. HYST is the binding constraint and had never been touched.** ✅ **V193 = V192 + `0xC64DD` 50→100** ⇒ window **f > 10.0 Hz → f > 5.0 Hz**, whole ratchet band inside; one byte, 31/31. `0f1a7bb6849f1782…` ⚠ **RISK DIFFERS IN KIND: V189–V192 are conditional on a state that never occurs, so they cannot affect normal driving. V193 makes that state REACHABLE** — spurious detection tightens the slew limit and may read as brief heaviness (bounded: still needs |gp-0x6c2c| > 12800 both sides). **V192 remains the conservative choice.**
- ✅★★★★★ [**V194 — probe `gp-0x6c2c`, the detector's OWN input: the number that decides whether V191/V192/V193 can work**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V194.md) — V193 opened the detector's FREQUENCY window; the **AMPLITUDE** gate has never been measured: the counter increments only when **|gp-0x6c2c| > T = `cal(0xC620A)` = 12800**. ⇒ **if the ratchet never reaches T, V191+V192+V193 are ALL inert and T is the next lever.** ✅ **V194 repoints the 427 probe** `0x55DF2` **0x9540 (gp-0x6ac0) → 0x93D4 (gp-0x6c2c)** and `0x55E10` **sar 4 → sar 6**. 🛑 **THE SHIFT IS NOT COSMETIC: `gp-0x6ac0` was UNSIGNED, `gp-0x6c2c` is SIGNED.** The packer `andi 0xffff` zero-extends then `sar N` then masks to 10 bits, so a signed source needs the shift chosen to carry the sign — **sar 6 gives positives raw 0..511 and negatives raw 512..1023**, decode `x = (raw<512 ? raw : raw-1024) * 64`, resolution 64, range ±32704, **T lands at raw 200**. A smaller shift wraps negatives into the positive range and makes the channel unreadable. Round-trip asserted over ±1000/±12800/±32704. 40/40. `2adde4ec37be9150…` ⊕ Decoder: `rlog-tools/probe/decode_v194_detector_input.py <tag>` prints percentiles and the verdict. ➕ **RULE CONFIRMED: when repointing a probe, re-size the SHIFT with the SOURCE — signedness and range are properties of the source, not of the channel.**
- 🛑★★★★★ [**`0xC61BE` is the BASE-ASSIST clamp, not “the LKAS request clip” — and a false null was caught by the two-method rule**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_0002a93a` (driver torque → pointer-table assist map) ends `gp-0x6b2e = clamp(x, ±cal(0xC61BE))`, consumed at `0x2A896` as `(gp-0x6b2e × cal(0xC63EE)) >> 10`. ⇒ **it clamps the BASE-ASSIST path, so raising it adds MANUAL assist, not LKAS authority. The lineage label is wrong.** 🛑🛑 **NEAR-MISS: `search_instructions` returned 2 hits for `gp-0x6b2e`, BOTH STORES** — which reads as “dead cell ⇒ `0xC61BE` provably inert”, a clean quotable WRONG answer. **The raw byte scan found a THIRD site, `0x2A896` (hw1=0x4F24, opcode 0x39 = `ld.h`) — a READER — in a region Ghidra has NOT ANALYSED.** Exactly the recorded undercount trap. ⚠ **The xref counts behind V190 (`gp-0x6bc2`, `gp-0x6ad6`) came from the same tool and were NOT byte-confirmed ⇒ their completeness is BELIEF.** (`gp-0x671a`, the setf family and the dormant-cal sweep WERE byte-scanned and stand.) ✅ **AUTHORITY IS SEQUENCED, NOT BLOCKED: the real knob is `0xC6CD0` (reach = `(clip × cal) >> 15`) — 4× when V88 CONFIRMED the grind fixed, 8× at V101 when it came back, 6× now — and it is the SAME cell the 2×2 named as the grind's CARRIER. Authority and grind are one lever pushed opposite ways.** ⇒ **the notch BREAKS that coupling**: fly it, confirm the grind is gone (V194), **then** 6×→8× restores what V102 gave up. 🛑 **Do NOT raise the gain before the grind result is in — that is precisely the V101 mistake.**
- ✅🛑★★★★★ [**Ghidra UNDERCOUNTS and a naive byte scan OVERCOUNTS — adjudicate, never union**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — re-derived every xref this session leaned on: **`gp-0x6bc2` 2/2 · `gp-0x6ad6` 3/3 · `gp-0x6c2e` 5/5 · CONTROL `gp-0x6b26` 5/5** ⇒ **V190's chain byte-confirms; its BELIEF caveat is LIFTED.** Only `gp-0x6b2e` was truly undercounted (2 vs 3, the `0x2A896` reader in an unanalysed region). 🛑 **MY OWN SCANNER MANUFACTURED THE OTHER TWO:** `0xBCC52` disassembles as **`st.b r7, -0x6ad5, gp`** — the NEIGHBOURING cell — because the scan accepted **both** `(hw2&0xFFFE)` and `|1` for every opcode; and its surroundings are six consecutive `st.b r7` to scattered unrelated displacements, i.e. **DATA force-disassembled, not code.** ➕ **THE RULE, CUTTING BOTH WAYS: Ghidra sees only analysed code (and still says `truncated:false`); a byte scan cannot tell code from data and a loose displacement rule matches neighbours. NEITHER IS AUTHORITATIVE ALONE — adjudicate every disagreement by disassembling the disputed address and checking it sits in a SENSIBLE INSTRUCTION STREAM.** That is how `0x2A896` was confirmed and `0xBCC52` rejected. ✅ **Scanner fix owed: derive the odd/even displacement bit from the OPCODE FIELD (`0x3D`⇒odd, `0x3C`⇒even), never accept both.** ⊕ Ghidra says `analyzed:true` / 2086 functions yet `0x2A896` has no function — **“analysed” ≠ complete coverage, and re-running analysis will not close the gap.**
- 🛑★★★★★ [**72 DEAD bytes on the flying build — V108 raised the assist-map tables and PULLED the clamp**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the first full cumulative diff vs stock found an unexplained block: **8 runs × 9 entries = 72 halfwords, every one 15360 → 16384** (`0xE4194..0xE521C`), on **V108/V122/V158/V189/V194** ⇒ introduced at V108. Context is a LERP with `X = [3413,3627,3840,4736,5632,6528,7424,8320]`, `Y = [15360×8]`. 🛑 **THEY ARE DEAD: `0xC61BE`, the clamp on that path, is BYTE-STOCK at 15360**, so every raised entry is cut straight back. The lineage says V108 built the `0xC61BE`→16384 raise **and PULLED it** on a pre-registered null ⇒ **it raised the TABLES and pulled the CLAMP, and 72 bytes of half-applied edit have ridden every build since, including the one on the car.** ⊕ Exactly the *“carried by accident”* category the close-out contract exists to catch, missed because **nobody had ever run a full cumulative diff against stock.** ✅ **V194 changes only 9 cells relative to the FLYING build** (V122 vs stock 310 payload bytes; V194 319): 4 = the grind notch `0xC60A8/AC/B0/B4`, 3 = detector-conditional (`0xC64AE`, `0xC691A`, `0xC64DD`), 2 = pure instrument (`0x55DF2`, `0x55E10`). ✅ Tool kept: **`analysis-2020accord/verify/cumulative_delta_vs_stock.py`**, which **refuses to stay silent about anything it cannot attribute** — that is how the 72 bytes surfaced.
- ✅★★★★★ [**V194's delta is 100 % attributed — and V57 is a large LKAS-AUTHORITY build the lineage does not describe**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — last two stragglers were the **part-number marker** `39990-TVA-A160`→`39990-TVA,A160` (2 copies, UDS-visible “this ECU is modified” flag). 🛑 **V57 carries far more than the `0xC646C` decoupling it is credited with**: **`0xC62EA` 320→0 = the LOW-SPEED STEER LOCKOUT DISABLED** · **`0xC659A..0xC65CE` float32 ±1.0→±5.0** and **`0xC674E..0xC676C` int ±1024→±5120** = a saturation-limit family raised **5×** · **`0xC61C0/C2/C4`** 1600/896/1280→−1 and **`0xC64B4/B6/B8`**→−1/255 = saturated away. ⇒ **substantial authority work has been ON THE CAR since V57.** ✅ **HEADLINE: V122 vs stock = 310 payload bytes, V194 vs stock = 319 ⇒ V194 changes only NINE cells relative to what he drives** — 4 the grind notch (`0xC60A8/AC/B0/B4`), 3 detector-conditional (`0xC64AE`, `0xC691A`, `0xC64DD`), 2 pure instrument (`0x55DF2`, `0x55E10`). Tool: `analysis-2020accord/verify/cumulative_delta_vs_stock.py`.
- 🛑⭐★★★★★ [**THE RATCHET IS A TORQUE-PATH EFFECT, NOT A MOTION OSCILLATION — it re-aims the whole chain**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — same slope-corrected excess across ALL channels, 1080 pooled engaged-creep windows, null ~3.9×: **`cs_tq` ratchet 13.5× / grind 5.1× · `cs_rate` ratchet 1.7× (BELOW NULL) / grind 7.3× · `sc_tq` 1.2× / 3.3×.** ✅ **The GRIND is a genuine motion oscillation — STRONGEST IN RATE** ⇒ the closed-loop model holds and **the notch is well aimed.** 🛑 **The RATCHET is NOT in the motion**: 13.5× in torque, **1.7× in rate.** The wheel is not oscillating at 8 Hz — the TORQUE is. That is a **friction/stiction** signature. ➕ **CONSEQUENCE 1: the detector watches `gp-0x6c2c`, an ACCELERATION EMA — no 8 Hz in rate ⇒ none in its derivative ⇒ the amplitude gate is not crossed either.** V191/V192/**V193** are inert for the ratchet on **both** frequency AND amplitude. ➕ **CONSEQUENCE 2: the FLYING build runs `0xC40D2` K1 = 1020 vs Honda's 102 — TEN TIMES the modelled Coulomb friction**, and Coulomb friction is precisely what makes torque ripple without motion. **V177's K1 revert, carried on V189–V194, is aimed straight at it.** ⚠ `0xC40BC` knee is 3000 vs Honda 600, never reverted (was 600 at V108) — shallower ramp, aligned with the low-friction requirement, but non-stock and unattributed. ⭐ **RE-ORDERS THE RECOMMENDATION TO V189**: notch (motion/grind) + inertia and K1 reverts (torque/ratchet), no sign bets, nothing that can change normal driving. Everything after V189 aims at the motion path, where the ratchet is not.
- 🛑★★★★★ [**Coulomb REFUTED, and “the ratchet is not in the motion” was OVER-CLAIMED — two of three claims died to their own controls**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **(1)** Coulomb must flip sign at rate zero-crossings; with a MATCHED control (dwelling at similarly low |rate|, no sign change): **RATCHET cross/dwell 3.73 [2.97,4.35] but the GRIND CONTROL 4.96 [3.84,5.92] — HIGHER** ⇒ crossings excite everything; **no Coulomb-specific preference. The friction explanation is NOT supported.** **(2)** the rate-scaling test is CONFOUNDED — ratchet/ctrl rises 63.7→211 peaking at 20–40 °/s **but the grind control does the same**, and ratchet/grind stays flat 6.2–8.5 ⇒ the rise is the NORMALISATION. **(3)** 🛑 **I OVER-CLAIMED:** the first coherence run gave **1.000 for everything INCLUDING the shuffled surrogate** (degenerate — one sub-window per segment). Redone properly (2048-sample episodes, 256-sample sub-windows): **cs_tq×cs_rate 0.888 @8 Hz / 0.842 @20 Hz vs shuffled floor 0.049/0.053** ⇒ **torque and motion ARE strongly coupled at 8 Hz.** The ratchet's motion is **SMALL, NOT ABSENT** — the rack is stiff at 8 Hz — consistent with the recorded Q 14–29 motor/rack resonance, and **not** a sensor artifact. ⇒ **`gp-0x6c2c` DOES contain 8 Hz, so the detector's amplitude gate is NOT provably uncrossable; V193 is not dead and V194's probe is still the decider.** ⊕ Unchanged: **the GRIND is a real motion oscillation, strongest in RATE (7.3×)** ⇒ the notch stays well aimed. ⊕ `d(cs_ang)/dt` gives 1.5×/2.6× — corroborates the small ratchet but degrades the grind (differentiating a quantised angle amplifies HF noise), so **`cs_rate` is the better motion instrument.**
- ✅★★★★★ [**V195 — the grind notch re-fitted on `cs_rate`, the channel where the grind actually lives**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/scoring/DRIVE-CARD-V195.md) — V188 fitted the notch by minimax over **`cs_tq` (driver torque)**, but the cross-channel work showed the grind is a MOTION oscillation strongest in **`cs_rate`** (7.3× vs 5.1×) ⇒ **the fit had used the weaker instrument.** Re-fit on `cs_rate` over 67 routes (peaks p10 16.33 / med 20.12 / p90 22.15 Hz vs torque's 15.74 / 19.92 / 21.68): **V188/V189 19.40 Hz r 0.9300 → median 15.0×, p90 10.4×, −3.8°** vs **V195 19.75 Hz r 0.9000 → median 21.5×, p90 14.3×, −4.6°** ⇒ **1.43× more grind power removed for 0.8° more phase.** ⊕ **The substantive change is the POLE RADIUS, not the centre**: 0.9300→0.9000 widens the notch because the rate-channel peak spread is wider; the 0.35 Hz shift is minor. ✅ V195 = V189 + 4 float32 cells, 11 payload bytes, 30/30, DC 1.000003, max|H| 1.7177, added lag −0.27°@1 Hz / −0.77°@3 Hz. `a3ea8683df48c6b3…` ⇒ **REPLACES V189 as the recommendation** — same levers, same risk, better aim. ➕ **THE LESSON: fit a filter against the channel the symptom is STRONGEST in. A 1.43× improvement came from changing the measurement channel, not the firmware understanding.**
