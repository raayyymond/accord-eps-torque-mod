# MEMORY.md — PART 3 (continuation, NOT an archive)

🛑 **Split verbatim out of `MEMORY.md` on 2026-08-21.** Nothing was removed, reworded, reclassified or
marked superseded. **Every pointer below is LIVE.**

**What is here:** the DATED SESSION SECTIONS — the oldest material in the index — `## 2026-05-30`
through `## 2026-07-31`, covering lateral PID tuning, the UDS access surface, the radar recovery fork,
the V46 → V52C cave arc and the V53 → V67 probe arc. The seam is structural: every `## <date> session`
heading moved, and nothing else did.

**Why split:** `MEMORY.md` had reached **180 KB against the 256 KB `Read` cap** in `CLAUDE.md`. Past
that cap a file loads with its tail **SILENTLY TRUNCATED and no warning** — the failure `MEMORY.md`
itself hit once at 287 KB (that is what produced `MEMORY-PART2.md`), and `docs/STATE.md` hit at 506 KB.
This was a controlled split done with headroom, not a rescue.

**Read this file together with `MEMORY.md` and `MEMORY-PART2.md` — no part is complete on its own.**

| file | contents |
|---|---|
| **[`memory/MEMORY.md`](MEMORY.md)** | **ENTRY POINT** — the newest and highest-starred pointers, plus the trailing PROMOTED block |
| [`memory/MEMORY-PART2.md`](MEMORY-PART2.md) | continuation, split 2026-08-12 at a bullet boundary |
| `memory/MEMORY-PART3.md` | **this file** — the dated session sections, 2026-05-30 → 2026-07-31 |

---

## 2026-05-30 session — lateral PID tuning / fleet data / auto-tune surrogate

- [Talk straight — no hedge-armor](feedback/process/feedback_direct_no_hedging.md) — Joey wants direct, peer-level communication; drop caveat-stacking; he's comfortable with uncertainty, reads hedging as friction
- [Verify load-bearing sub-agent claims — don't relay](feedback/process/feedback_verify_subagent_claims.md) — re-derive before car-affecting/durable acts; sub-agents were confidently WRONG twice this session (false "route-07 degenerate"; wrong-branch gas-ceiling inversion), caught by direct reads

## 2026-06-02/03 session — UDS access surface / SA cipher families / engineering sessions / read-only telemetry (omega horde-of-hordes)

Detail of record lives in `.claude/agent-memory/firmware-codepath-tracer/reference_*` (the tracer's own index). These are the load-bearing facts:


## 2026-06-03 session — EPS observability stub / RDX recovery fork / radar dump posture (multi-horde)


## 2026-06-04 session — radar SIGNAL PRESERVATION + EPS stub v8 QA + no-flash diag read-primitive hunt (3 hordes)

Detail of record: `docs/SESSION-SYNTHESIS-2026-06-04.md`. Trust the bytes/live probe over the narrative (operator: prior radar memories are a false-start chain).

- Corrections of record: radar chip = file+**0x60000** (not +0x30000) [⚠ REVERSED 2026-06-05: canonical is +**0x30000** — the +0x60000 was a Ghidra import-base artifact; see the 2026-06-05 section]; radar **DSP island = e200z7 PowerPC** (app core still ARM Cortex-M+Helium/MVE); M_CAN base **0x44010000 REFUTED** (BE index 0x0144; real bases 0x4005xxxx); packer hook-site 0x10607C **UNCONFIRMED** (MVE wall — needs llvm-objdump --mve or live vtable read).

## 2026-06-05 session — radar recovery + 0x211FF000 stack autopsy + re-patch kit

A fine-range packer-hook patch broke comms (wrote the CPU stack); the stock revert did NOT take (NRC uncaptured). Recoverable, not bricked. Detail: `docs/HANDOFF-2026-06-05-radar-recovery.md`. ⚠ Addressing CORRECTED: chip = file + **0x30000** is canonical (the 2026-06-04 +0x60000 was a Ghidra import-base artifact).

## 2026-07-21 session — V46 (lever A) falsified, V47 dampers-only, whole-system 21 Hz audit

- [project_v46_falsified_v47_dampers_only](project/project_v46_falsified_v47_dampers_only.md) — V46 (Stage A carrier low-pass) FLASHED, no effect → lever A falsified (that half stands, `0xC6450` is mode-proof); V47 = ratchet + dampers-only (Factor C + Factor E aggressive), BUILT + verified, UNFLASHED. 🛑 **BANNER 2026-08-06: the DAMPER half is VOID under RULE 7** — modes 10/11 on a modes-24/26 car ⇒ never in force. Handoff `docs/handoffs/2026-07/HANDOFF-2026-07-21-v46-v47-vibration.md`.
- [reference-accord-damper-two-deadzones-factorC-factorE](reference/firmware/reference-accord-damper-two-deadzones-factorC-factorE.md) — the damper is a 5-factor product with TWO hands-off deadzones (Factor C driver-torque AND Factor E motor-rate); V44 opened only C → failed. Output clamp dynamic ±512..1024, not ±2048.
- [reference-accord-no-vehicle-speed-input-5mph-is-plant](reference/measurement/reference-accord-no-vehicle-speed-input-5mph-is-plant.md) — NO vehicle-speed input anywhere in the command/base-assist path; only motor-rate adaptation. "5 mph" = openpilot minEnableSpeed=3 + plant physics, not a firmware gate.
- [reference-accord-dualpinion-arch-one-torsion-sensor](reference/measurement/reference-accord-dualpinion-arch-one-torsion-sensor.md) — dual-pinion EPS (motor off-axis on a 2nd rack pinion); Sensor A/B = MAIN/SUB channels of ONE torsion-bar sensor (DTC C1420); 21 Hz = rack-coupled driveline mode; resolver for motor angle; OP minEnableSpeed=3.
- [reference-accord-governor-g1-total-command-not-thermal](reference/firmware/reference-accord-governor-g1-total-command-not-thermal.md) — G1 (FUN_0004503c) clamps the TOTAL command (not LKAS-only); "energy budget" is NOT a thermal integrator (structurally unreachable) → relabel motor-rate-adaptive total-command ceiling; doesn't bind at resonance amplitude.
- [reference-accord-damping-clamp-dtc1d-trap](reference/firmware/reference-accord-damping-clamp-dtc1d-trap.md) — damping clamp bound (int 0xD209C/0xD20A8) has a float mirror (0xC6554/58/5C/60) with a NO-DEBOUNCE DTC-0x1d hard shutdown; never edit int clamp without a bit-exact float edit. V47 leaves it stock.

## 2026-07-21 LATE session — loop-gain characterization, collocation keystone, V48A (failed), V48B notch

★ Master reference for the vibration is now `docs/research/VIBRATION-DOSSIER.md`. Handoff: `docs/handoffs/2026-07/HANDOFF-2026-07-21-v48-vibration-loopgain-notch.md`.

- [reference-accord-collocation-motor-rate-damper-dead](reference/measurement/reference-accord-collocation-motor-rate-damper-dead.md) — ★ the 21 Hz vibration is a two-inertia torsional mode; the firmware's MOTOR-RESOLVER-RATE damper is NON-COLLOCATED with the wheel-side mode. The hand cures it because a grip is collocated at the antinode. 🛑 **CORRECTED 2026-08-06 — the EMPIRICAL leg is GONE and the conclusion is CONTRADICTED for grind #1.** ~~"V44/V47 nulls are that theorem confirmed on-car; STOP tuning the motor-rate damper"~~ — V44/V47 were **mode-inert** (modes 10/11 on a 24/26 car), so their nulls prove nothing; **V74 actually delivered damping and 18–22 Hz measured `d ln(y)/dk` = −0.599 [−0.856, −0.348], CI EXCLUDES ZERO**. The "wrong sensor at any gain" reading survives only for the **6–9 Hz micro-ratchet** (flat). Loop-gain model: bare-plant Q≈1.7, 4× drives ~0° anti-damping → Q=13.6, |L|=0.875, 1.16 dB margin, onset ~3×. `analysis-2020accord/studies/models/eps_loop_gain_model.py`.
- [project-v48-loopgain-v48a-failed-notch-next](project/project-v48-loopgain-v48a-failed-notch-next.md) — V48A (cal-only: mute type-8 `0xC4120` + `FUN_0003a382` `uVar27`→256) BUILT+VERIFIED+FLASHED → did NOT fix the vibration ⇒ anti-damping is DISTRIBUTED ⇒ the 21.4 Hz notch V48B is the remaining lever. **V48B is now BUILT** (see below). Route B ("4× via setpoint") is hygiene-only (ΔL=0).
- [reference-accord-v48b-notch-cave-build](reference/builds/reference-accord-v48b-notch-cave-build.md) — ⚠ **V48B WAS FLASHED → BRICKED (see the CATASTROPHIC entry in the next section).** 138-byte/41-instr DF-I Q12 biquad code cave @`0xC4B34` (coeffs `b0=4045 b1=-7949 b2=3977 a1=-7949 a2=3926`, unity at DC) filters a copy of `gp-0x4f60`→new RAM `gp-0x1500`; `jr` trampoline @`0x7FEAC` (return `0x7FEB0`, re-exec cmp/mov last for flags); 7 live carrier repoints `gp-0x4f60`→`gp-0x1500` (`FUN_0002c478/000352b4/0003a382/0003b49a/0003b66a`); 2 dormant reads (`0x34392/0x34ace`) left raw. RAM x1/x2/y2=`gp-0x14FC/FA/F8`. 50/50 CRC + RWD round-trip + every edit re-disassembled in Ghidra. Adversarial review: all monitor-asymmetry (V27) items CLOSED SAFE incl. the DTC-0x1c/0x1d matched int/float lockstep. `builds/v18_v49/build_v48b_tva.py`+`studies/caves/v48b_cave_asm.py`+`studies/models/eps_v48b_cave_model.py`; handoff `docs/handoffs/2026-07/HANDOFF-2026-07-21-v48b-notch-build.md`. ⚠ CODE CAVE (kit's only bricked class V24/V27) — flash only on explicit operator instruction.
- [reference-accord-gp4f60-notch-filter-feasibility-v48b](../.claude/agent-memory/firmware-codepath-tracer/reference_accord_gp4f60_notch_filter_feasibility_v48b.md) — (agent memory) gp-0x4f60 is shadow-lockstep (fault 0x17) + feeds 2 no-debounce hard-shutdown monitors + 2 CAN → notch must be a FILTERED COPY (new RAM), not source-filter; repoint the carrier reads; cave 0xC4B34.

## 2026-07-21 — 🛑 V48B FLASHED → CATASTROPHIC (root-caused; two mandatory cave gates added)

- [reference-accord-v48b-flashed-catastrophic-ram-collision](reference/builds/reference-accord-v48b-flashed-catastrophic-ram-collision.md) — 🛑 V48B notch cave FLASHED → full-authority wheel oscillation on startup, parked, NO LKAS (recovered by reflash; no hardware damage). Kit's THIRD cave brick (V24/V27/V48B). Root cause: (1) RAM COLLISION — biquad x2 cell `gp-0x14FA` high byte `0xFEDF6B07` aliases a live monitor/DTC status bitfield (readers `FUN_00051fbc`/`FUN_00053f32` case 8), b2≈0.97 near-unity → external write injects full-scale → clamp ±25600 → motor slams; cave also stomps the monitor byte 1000×/s; writer unlocated but aliasing confirmed; `gp-0x14FA` in poison region `gp-0x1401..0x1502`, safe alt `gp-0x14E0`. (2) lightly-damped resonator (r=0.979, Q≈3.2) dropped into the always-on base-assist loop (no LKAS/speed gate) with NO closed-loop stability check — only `|N(21.4)|` inserted into the LKAS loop-gain model. (3) clock rate EXONERATED (undivided 1 kHz, correct). Handoff `docs/handoffs/2026-07/HANDOFF-2026-07-21-v48b-flashed-catastrophic.md`.
- [feedback-cave-two-gates-ram-ownership-and-closed-loop](feedback/tooling/feedback-cave-two-gates-ram-ownership-and-closed-loop.md) — ★ HARD RULE (apply without being asked): before ANY code cave / filter / dynamics change is flash-ready it MUST pass GATE 1 (RAM ownership — every byte's full footprint proven free incl. writers + register-indirect/6-byte-disp; `gp-0x1401..0x1502` poison) AND GATE 2 (closed-loop stability — magnitude+phase/Nyquist of EVERY loop the signal is in, esp. the always-on base-assist loop, element inserted; not a single-frequency magnitude). Byte/CRC/disassembly/open-loop-DSP verification is necessary but NOT sufficient — V48B passed all of them and bricked.

## 2026-07-22 session — 21 Hz re-audit: comma is a PASSENGER, measurement corrected, V49B StageC-flip is the lead

- [feedback-default-maximal-thoroughness](feedback/process/feedback-default-maximal-thoroughness.md) — ★ standing operator instruction: DEFAULT to maximal thoroughness without checkpointing for permission on analysis/prep — run every thread, close every gate, verify subagent work byte-level, build the ready UNFLASHED candidate, keep records current. The flash/CAN/UDS gates are the ONLY hard stop and are NOT relaxed by this.
- [reference-accord-v49-stagec-flip-collocated-damper](reference/builds/reference-accord-v49-stagec-flip-collocated-damper.md) — ★★ the 21 Hz vibration is a FIRMWARE/PLANT loop: the openpilot bus command (0xE4) STRIPS 21.5 Hz via its slew-saturated rate limiter (52% of steps at ±123 cap) → comma is a PASSENGER, no Kp/LPF fix helps. Measurement corrected (broad shelf not Q=13.6; 21.5-vs-78.6 aliasing UNRESOLVED). Topology COMPLETE (gp-0x6ade dead, gp-0x6b62 new+slow). **V49 = ratchet + collocated damper via FUN_0003a382 StageC sign-flip (subr→sub @0x3a836) + band-limit (pole 0xC644A 1024→64); GATE-2 required the band-limit (bare flip creates HF anti-damping).** BUILT+VERIFIED, UNFLASHED — 🛑 GATED on polarity gp-0x6752 (0xFEDF18AE) = +1 (brick if −1; EEPROM-resident → needs live read). Models: `studies/models/eps_v49_a382_stagec_flip_model.py` + `studies/spectra/reanalyze_b9_vibration.py`.

## 2026-07-22 session (later) — FOC hypothesis closed; V50 = EMA low-pass cave, BUILT

- [feedback-account-for-prior-iterations-before-new-build](feedback/builds/feedback-account-for-prior-iterations-before-new-build.md) — ★ standing operator instruction: before any new RWD version, internalize EVERY prior iteration (what was flashed + falsified) and don't re-propose a lever already falsified on-car. Pairs with maximal-thoroughness + the two cave gates.
- [🛑 gp-0x1500 is a LIVE I/O-mailbox slot — the 0xb7260 array](reference/can/reference-accord-b7260-io-mailbox-array.md) — the "poison region gp-0x1401..0x1502" is a subset of a 40-slot×8B I/O registry at 0xb7260 (0xFEDF6AE0..0xFEDF6C18); gp-0x1500 (slot 5) + gp-0x14E0 (slot 9) are LIVE, written via table-dispatched pointers. Proven on-car by the V50P probe (gp-0x1500 nonzero 99.47% of the drive) ⇒ **V50 is NO-FLASH**. Static clearance failed 3/8 addresses (gp-0x1500/0x14E0/0x1700) → probe ANY candidate cell live. Replacement candidates: gp-0x1300+gp-0x1100 (roomy corridor).
- [reference-accord-v50-lowpass-ema-cave](reference/builds/reference-accord-v50-lowpass-ema-cave.md) — 🛑 **NO-FLASH — GATE-1 failed on-car (gp-0x1500 has a live writer; see the 0xb7260 mailbox memory above).** ★★ **V50 = V38 + ratchet + a FIRST-ORDER EMA LOW-PASS (fc~12 Hz, alpha=74/1024) on gp-0x4f60 (code cave), keeps 4x. BUILT + VERIFIED (50/50 CRC, RWD round-trip, all encoders Ghidra-verified), UNFLASHED.** Chosen over V48B's notch: the mode is SPEED-DEPENDENT (~21.7 Hz low-speed → ~8-12 Hz highway; fresh manual-drive data) + alias-unresolved → a low-pass covers the whole band and rolls off 78.6 Hz harder (−16 vs −6 dB). Single 16-bit cell gp-0x1500 (V48B-flash-proven) removes V48B's 4-cell RAM-collision failure mode; deadband is a FEATURE (fully suppresses small ripple, over-attenuates moderate). Polarity-INDEPENDENT. **FOC current-loop hypothesis RULED OUT** (no isolable PI; model-based coeffs in the risky 0xC5000 block; the 8 kHz loop is the actuator, not the source). GATE 2 closed (`studies/models/eps_v50_gate2_lowpass.py`); GATE 1 = gp-0x1500 best-evidenced but 2 pre-flash gates OPEN (built-image Ghidra re-disasm + the 0xbb640/0xb7260 table-walker register-indirect check). Also: the route the operator gave was NOT a V49P drive → polarity gate still unresolved (moot for V50). Build: `builds/v50_v79/build_v50_tva.py` + `studies/caves/v50_cave_asm.py`.


## 2026-07-24 session (later) — V51P GATE-1 PASS (both cells clean); V52 built-but-incomplete; gp-0x4f60 = 19-carrier surface

- [reference-accord-v51p-gate1-both-cells-clean](reference/builds/reference-accord-v51p-gate1-both-cells-clean.md) — V51P flashed+driven (rlog 7): BOTH gp-0x1300 and gp-0x1100 read 0/24000 (beacon 100%, 2 independent decoders, stock null distinguishable) → definitive live-probe RAM clearance that gp-0x1500 failed. V52 uses gp-0x1300. Clears the CELL, not the filter's completeness.
- [reference-accord-gp4f60-carrier-surface](reference/firmware/reference-accord-gp4f60-carrier-surface.md) — byte-scan: gp-0x4f60 has 64 raw readers (not ~12); ~19 command-path CARRIERS across the 1kHz control task AND the ~100Hz assist task; V52-as-built repoints only 10 (misses 9, incl. 3 self-filtering + 2 mode-gated) → INCOMPLETE + fragile + GATE-2-invalidating. NO monitor hazards (M1/M2/gate all compare raw vs literal constants → risk is feel, not brick). ⇒ empirical case for FFT-narrow (FOURFRAME) over the broad source-filter.

## 2026-07-24 session (third) — V52C: ALL 19 carriers filtered; every pre-flash gate PASSED

- [reference-accord-v52c-complete-broad-lowpass](reference/builds/reference-accord-v52c-complete-broad-lowpass.md) — **V52C BUILT + ALL GATES PASSED, UNFLASHED** (SHA `af01c8bd…`, 132B/24 runs, 50/50 CRC). ALL 19 gp-0x4f60 command-path carriers repointed to gp-0x1300. **Operator directive + governing principle: a MIXED raw/filtered population is ITSELF the hazard — that is how V27 bricked (ASYMMETRY, not magnitude).** GATE-1 closed FIVE ways (method reproduces V50's + V48B's failures as controls); GATE-2 edge 4.66×→**21.19×**, monotonic; no monitor asymmetry (3 shadow pairs all input-invariant); key-on transient can only SHRINK (RAM clears on the flash's power cycle). ⚠ gp-0x4f60 is SHADOWED (gp-0x4486 → fault 0x17 → hard motor-off) — filter a COPY, never the source.
- ⚠ **[reference-accord-gp4f60-carrier-surface](reference/firmware/reference-accord-gp4f60-carrier-surface.md) is now PARTLY SUPERSEDED** — its "broad and fragile ⇒ don't filter at the source" verdict is REVERSED, its "3 self-filtering lanes" claim was WRONG on all three (`0x3B908`'s biquad is DEGENERATE in stock cal → ~366 Hz passthrough; `0x36846` isn't a filter at all), and its 64-reader count was an undercount. Header rewritten in place.
- [accord-dtc-0x18-hard-eligible-cadence-watchdog](accord/firmware/accord-dtc-0x18-hard-eligible-cadence-watchdog.md) *(user-store)* — 🛑 **CORRECTED 2026-08-06 — WRONG AS WRITTEN.** ~~DTC 0x18 is the per-task cadence/overrun watchdog, so ANY cave on the 1 kHz path has a timing budget.~~ `0x18` is a **BOOT-TIME RESET-CAUSE REPORT, not a live per-task deadline monitor**: `FUN_00014b3e` (the 4-task liveness checker) **never calls the DTC latch chain** — it snapshots the faulting PC, writes an NVM cause-code and reboots; the real `FUN_00016de6(0x18,…)` lives in the sibling `FUN_00014ba0`, gated on **reading that NVM cause-code back**, reached only from **state 1** of the dispatcher (a boot pass) ⇒ **it cannot be tripped by a running task.** The record value (0xB7FDC = 0x3D01) and the 2-byte-slip warning still stand. ⊕ **The V75 probe cave is EXONERATED**: 45→68 B costs **+17 cycles ≈ 212 ns** (V74 18 cy → V75 35 cy at 80 MHz) — **4–5 orders of magnitude below any window.**
- [accord-gp4f60-two-encodings-enumeration-trap](accord/firmware/accord-gp4f60-two-encodings-enumeration-trap.md) *(user-store)* — gp-relative accesses exist in TWO encodings; "69 definitive" was wrong, true total **76**. Byte scan misses the 6-byte form and over-matches it **7.6×** (`hw2=0xff61` is shared across ~13 nearby displacements); `search_instructions` misses anything outside a function boundary (61 vs true 64, 4th occurrence). Register field is `(hw1>>11)&0x1F`.
- [feedback-stale-ghidra-import-defeats-hash-check](feedback/tooling/feedback-stale-ghidra-import-defeats-hash-check.md) *(user-store)* — an OPEN Ghidra program can hold an earlier revision of a file whose on-disk SHA verifies correctly, returning RAW bytes at edited sites. Re-import fresh + spot-check one edited site against a Python byte read before trusting any pre-flash re-disasm gate.
- 🛑 [Ghidra same-name import collision — `program:` by NAME is ambiguous](accord/firmware/accord-ghidra-same-name-import-collision.md) — re-importing under the artifact's own filename is **necessary but NOT sufficient**: a same-named import already in the project yields **two programs distinguishable only by PATH**, and the name now *looks* correct. Pin by path, then prove freshness with `read_memory` vs an independent Python byte read. Found closing V75's GATE 1, 2026-08-06.
- ★ [accord-low-speed-lockout-window-c62ea](accord/calibration/accord-low-speed-lockout-window-c62ea.md) *(user-store)* — **THE LOW-SPEED STEER LOCKOUT IS CAL `0xC62EA`=320 (~5 km/h)**, a two-sided window (hi `0xC62E8`=12800 ≈200 km/h) vs voted speed `gp-0x6a5e`; failing it writes `STEER_STATUS=3`, which via an **intra-function** `cmp 0x2` @`0x29382` blocks `STEER_CONTROL_ACTIVE` (`gp-0x6806`) and the authority ramp (`gp-0x69b0`). Cal-only, one reader each, **no float mirror**, `0xC6000` block. Proven on-car by 3 independent decoders (~305k frames): 0% control-active <2 mph, ≥99% >4 mph, **zero** co-occurrence with status 3. **openpilot is NOT the obstacle** (`CP.minSteerSpeed=0.0`; the 3 mph in `values.py:163` is `HondaCarDocs` metadata). Second gate `0xC62EE` is a **CAN-commanded** permissive (trigger = `0x17C` byte 5 bits 7/5) — leave stock, **never raise**.
- ★ [accord-v850-scan-traps-formatv-and-storezero](accord/firmware/accord-v850-scan-traps-formatv-and-storezero.md) — 🛑 **CORRECTED 2026-08-13 (later still): this pointer used to point into the auto-memory user-store; the file exists in repo `memory/` too and is now the one this pointer targets (the two had begun to diverge — see [[feedback-two-memory-stores-have-diverged]]).** **Eight byte-scan/method traps, each of which produced a confident WRONG answer.** Headline: **Format-V `jr`/`jarl` aliases `ld.bu`** (same opcode field `0x3C`, separated only by `hw2` bit 0) → **44% false positives, 10,105 of 22,850**; filtering `reg2==0` drops every store-of-zero; `ld.bu`/`ld.hu` use `hw2=(disp|1)`; never require "compare **then** boolean store"; `(lo,hi)` pairs are confounded by `[count][X][Y]` LERP records; an **external-readers sweep cannot prove report-only**; **"Ghidra defined no function" ≠ dead** (use the task table at `0xBB928`, stride `0x30`, entry `+0x08`, self-validating on `0x2214A`/`0x22CA0`); and **NEW: `search_instructions` returned a TOOL ZERO for `tp`'s own initialiser** — Ghidra never analysed that region; a raw Python scan found `gp` and `tp` set by the same idiom, four instructions apart, from the same `r1`. Includes a 4-point validation harness for any new scanner.
- ⚠ [accord-fun45608-authority-slots-not-motoroff](accord/firmware/accord-fun45608-authority-slots-not-motoroff.md) *(user-store)* — **`FUN_00045608` is a generic AUTHORITY-SLOT SETTER, not "motor off"**: 3 parallel 7-slot arrays (`gp-0x652c`/`0x64fc`/`0x6514`) consumed by the G1 governor as a **running MIN** applied as a Q15 scale on the total command. `(3,0,0x8000,0x8000)` = slot-3 target 0 with instant slew — effect right, label wrong. Also: **the governor DOES read vehicle speed** (`gp-0x6a64` vs cal `0xC6316`=640 ≈10 km/h, below which the slew limiter is BYPASSED) ⇒ **falsifies "no vehicle-speed input anywhere in the command path"**; and its slot loop covers **slots 0-5, not 0-3**.
- ★★ [reference-accord-vibration-requires-lkas-engaged](reference/measurement/reference-accord-vibration-requires-lkas-engaged.md) — **the 21.09 Hz Q≈14 resonance EXISTS ONLY WHILE OPENPILOT IS COMMANDING.** Matched test (hands-OFF, moving `vEgo>0.3`, same Nfft/speed gate, route 13): **9,200× less 21 Hz power with LKAS off**, and the disengaged pool carries **6× MORE** low-frequency energy — so it is NOT an excitation artifact. ⇒ **closed-loop LKAS instability, NOT the "command-independent base-assist limit cycle"** CLAUDE.md describes ⇒ **an openpilot-side 21 Hz notch/rolloff is a ZERO-BRICK-RISK experiment to run before any further `.rwd`.** Three method traps recorded: never mix hands-on/off (mixed data peaks at a spurious **7.42 Hz**, RETRACTED); the "steeringPressed is circular" objection is **testable and FALSE** (driver torque 2166 vs 328); and the raw `latOFF & handsOFF` cell is a **PARKED car** (median vEgo 0.00) — gate on `vEgo>0.3`. Mode spans **<1.5 → ~15 m/s** across 3 datasets; only >15 m/s becomes a broad low-Q 11-12.5 Hz shelf ⇒ "3-8 m/s worst regime" is **refined, not special**.
- ★ [reference-accord-can-tx-100hz-base-tick-and-gateway-evidence](reference/can/reference-accord-can-tx-100hz-base-tick-and-gateway-evidence.md) — **CORRECTION: the CAN-TX base tick is 100 Hz, NOT 62.5 Hz** (derived 3 ways from cadence × measured wire rate; CAN 399 fitted at exactly 100.000 Hz) ⇒ FOURFRAME transmits at 100 Hz. **Gateway whitelist now confirmed on 8 controls, not 1**: of 11 broadcast slots only `0x14A`/`0x18F`/`0x1AB` reach the comma; slot 8 (`0x19F`) is configured **identically** to slot 9 (`0x18F`) yet never appears ⇒ **FOURFRAME's silence says NOTHING about whether the cave fired.** ⚠ the planned red-panda confirmation **may not discriminate** — the red panda taps the SAME comma Bosch harness. **Better channel: `0x18F` byte5 (constant 0x00) + `0x14A` byte4 (constant 0x07) = a free 16-bit signal at 100 Hz on gateway-crossing frames**; `0x1AB` is a POOR carrier (DLC 3, bytes 0-1 a live saturated signal). Tables read with pure Python — no Ghidra.
- ★★ [reference-accord-fourframe-strb-ssam-defect](reference/can/reference-accord-fourframe-strb-ssam-defect.md) — **FOURFRAME NEVER TRANSMITTED, and it was OUR firmware.** The cave wrote `STRB = 0x80` to mailboxes 16-19; bit0 is `SSAM` and the V850E2 manual is explicit (`0 = Message buffer not used`), with §20.8.1 making SSAM=1 a hard precondition for joining the TX priority search. Stock `FUN_0001cf30` writes **0x01** to all seven TX mailboxes and **0x00** to the unused pool — `0x80` left ours configured exactly like the unused pool. Root cause: `builds/telemetry/build_vcantx_test_tva.py:54` mislabelled bit7 `SSOW` as "TX direction"; **the VCANTX seed carries the same defect.** Fix = `203e8000`→`203e0100` at 4 sites, 8 bytes, identical encoding. ⇒ **The gateway-whitelist theory is WEAKENED**: `0x19F` is gated at its own request site (`0x5559E`) while `0x18F` is not, so it was never a clean control.
- ★ [reference-accord-c646c-shared-gain-not-lkas-only](reference/firmware/reference-accord-c646c-shared-gain-not-lkas-only.md) — **`0xC646C` is NOT "the LKAS authority gain"** — it is the firmware's single shared Q15 sensor-to-command scale with **exactly 6 readers** across three subsystems (enumerated twice, both encodings, no float mirror, neither monitor among them). Two of them (`0x36686`, `0x3684a`) apply it to the **raw torsion bar `gp-0x4f60`** on a feedback path verified end-to-end into `gp-0x6b94` → governor → motor. So 4× LKAS authority silently quadrupled two feedback lanes. **Probably NOT the 21 Hz driver** (that lane is IIR'd at fc≈2.2 Hz = −19.7 dB, clamped to 5% of aggregator range) and **the saturation hypothesis is empirically DEAD** (0 of 10,178 active-LKAS frames reach the clamp). Minimal fix designed + verified: one 2-byte retarget of `0x2a1ee` to a confirmed-free cal word at `0xC6CD0`.
- ★★ [reference-accord-vibration-needs-applied-torque](reference/measurement/reference-accord-vibration-needs-applied-torque.md) — **the vibration needs the EPS to ACTUALLY APPLY LKAS torque, not merely to be commanded.** Three-way split on route 13: openpilot off → nothing; commanding *harder* but EPS in lockout → nothing (1.33× = noise); commanding **and applying** → **14,750×** more 21 Hz power. **Also: the 21 Hz IS in the openpilot command** — the "not in the output" premise was FALSE and came from reading `0xE4` on src=2/src=128, which are **identically zero**; only src=129 carries data. Coherence 0.685 @21 Hz vs 0.171 @1-3 Hz; **no OP-side low-pass at 21 Hz** (−2.70 dB) ⇒ **an OP notch is UNTESTED, not a no-op.** Retracts the "latency makes 21 Hz impossible" argument as bad control theory. Aliasing 21 vs 78.91 Hz still OPEN (IMU tie at the noise floor; leans 21.09 on implied Q).
- 🛑 [accord-check-build-lineage-before-proposing-lever](accord/instruments/accord-check-build-lineage-before-proposing-lever.md) — **two agents in one session proposed `0xC6450` 1024→32 as "never flashed". It is V46 — flashed, null.** A third nearly repeated it with `0xC644A` (V43, null). **Grep `build_v*_tva.py` and check `docs/BUILD-LINEAGE.md` before naming ANY cal address, and state its on-car result.** Structural plausibility is not evidence of novelty. This is why CLAUDE.md was restructured into an index — the fact was present but not findable.
- ★ [project-v53-fourframe2-plus-minsteerspeed0](project/project-v53-fourframe2-plus-minsteerspeed0.md) — **V53 = FOURFRAME2 byte-for-byte + `0xC62EA` 320→0 (min steer speed → 0). BUILT 2026-07-27, UNFLASHED**, exactly 6 bytes off FOURFRAME2. Chosen 0 (not the old "suggest 64") because stock **already** unlocks true standstill via `gp-0x68b3` — 0 removes the 1–319-count discontinuity instead of moving it. Sole-reader + LERP-masquerade + SNA checks all re-run in Python at build time. `builds/v50_v79/build_v53_tva.py` **imports** the cave from the FOURFRAME2 builder — zero transcription surface, reuse that pattern. No V42 ratchet fix (`0x454FE` stock, asserted).
- ★★ [reference-accord-v53-flashed-steer-to-zero-confirmed-telemetry-null](reference/builds/reference-accord-v53-flashed-steer-to-zero-confirmed-telemetry-null.md) — **V53 FLASHED 2026-07-27 and is the image on the car.** ✅ Steer-to-zero **CONFIRMED from raw CAN 399**: `STEER_STATUS=0` in 5,995/5,995 frames (ST=3 never fires) + **226 frames of `STEER_CONTROL_ACTIVE=1` below 5 km/h**, a cell identically EMPTY on V38 — the golden model's first successful advance prediction of an on-car state-machine change. 🛑 But the four-frame telemetry `0x6A0`-`0x6A3` was **still absent (0 frames / 301,824)**, and that null is **UNINTERPRETABLE**: six IDs the stock firmware genuinely broadcasts (`0x19F`,`0x32E`,`0x64D`,`0x660`,`0x722`,`0x723`) are equally absent, while non-DBC IDs (`0x669`,`0x750`,`0x674`) *are* logged — so "openpilot didn't know the ID" is excluded. **Never build another new-mailbox telemetry channel.** What V53 did to the vibration is UNANALYSED.
- ★ [reference-accord-piggyback-channel-audit-dbc-panda](reference/can/reference-accord-piggyback-channel-audit-dbc-panda.md) — full carrier audit of the 3 whitelisted EPS frames. **`0x14A` byte4 bits 7:3 = 5 free bits @100 Hz** is the only usable channel (4 successful flashes, proven to cross, openpilot-invisible, hook at `0x55C0E` before the checksum). ⚠ **`0x18F` byte5 bits 5:4 are LIVE in firmware** (`gp-0x6880&3`, packer `0x55CAE`) — a DBC-only read wrongly calls them free; it also has no located pre-checksum hook. 🛑 **opendbc VERIFIES Honda checksums** — a bad one drops `can_valid` = a disengage. Panda's Honda RX list (`0x1A6/0x296/0x158/0x17C/0x326/0x1BE`) gates none of them.
- 🛑 [feedback-telemetry-must-reserve-a-did-not-fire-value](feedback/measurement/feedback-telemetry-must-reserve-a-did-not-fire-value.md) — **any telemetry encoding must reserve a wire value a live probe can never emit.** Caught in V54: the unbiased encoding made a *dead* probe decode as "authority 0-127, lane at FULL bound — V55 = mute", a confident fabricated answer from a channel that never ran. Bias the live range to start at 1. Also: prefer an encoder that changes a *register field* of a verified instruction over one introducing a **new opcode value**.
- ★ [project-v54-authority-probe-built](project/project-v54-authority-probe-built.md) — **V54 BUILT 2026-07-27, UNFLASHED — the one to flash.** V38 + `0xC62EA` 320→0 + a 44-byte read-only cave packing `wire = min((gp-0x6966>>7)+1, 31)` into `0x14A` byte4 bits 7:3 @100 Hz. It is the instrument that unblocks the `0xC6AF0` direction: wire 1-25 ⇒ lane at full bound (mute it), wire 30-31 ⇒ already clamped to zero (hypothesis dies), mixed ⇒ the knee crossing is the trigger. **wire 0 ⇒ the cave did not fire, drive is VOID.** All gates + GhidraMCP re-disassembly of the written image passed.
- [reference-accord-v54-flashed-authority-is-zero-by-design](reference/builds/reference-accord-v54-flashed-authority-is-zero-by-design.md) — **V54 FLASHED; the probe FIRED** (byte4 `0x07`→`0x0F`, one bit, 5,989/5,989) — the kit's **first working firmware telemetry channel**. `gp-0x6966` is the soft-EME wind-up magnitude, **not** a speed-scheduled authority, and is ~0 **BY DESIGN** on V31+ (boost floor 4096, raised to 5120 at V38, makes wind-up unreachable). ⇒ `0xC6AF0` selects unity in 100% of normal operation, the residual lane runs at FULL bound, **mute is the only meaningful edit** — but **GATE 2 (damping sign) is still open**. Do NOT re-drive faster to move it.
- [reference-accord-vibration-creep-speedshift-and-saturation](reference/measurement/reference-accord-vibration-creep-speedshift-and-saturation.md) — the mode **moves with speed** (20.12 Hz @1.0 m/s → 21.68 Hz @4.0 m/s, 8 bins) so **no fixed-frequency notch can track it**; it **reproduces at 3.4 mph creep** (771× engaged/disengaged) in the cell V53 unlocked; and **saturation suppresses it 141× speed-controlled** — though stiction/backlash predicts that equally. All eight falsified levers share one assumption: that the ~20 Hz is *commanded*.
- [reference-accord-v55-dual-probe-built](reference/builds/reference-accord-v55-dual-probe-built.md) — **V55 BUILT, UNFLASHED, all gates + Ghidra re-disasm passed.** Dual report-only probe: bit7 = damper variant ≥10, bits 6:3 = 4-bit `gp-0x6b98` (final merged command). A **partition**, not a ninth lever. Carries three reusable encoding facts (`gp-0x6b98` is SIGNED; `ld.bu` puts disp bit0 in the opcode; `st.w` carries `|1` like `ld.w`) and the trap that **a V54 rlog decodes as a plausible V55 reading**.
- [feedback-no-openpilot-side-modifications](feedback/builds/feedback-no-openpilot-side-modifications.md) — standing operator instruction 2026-07-28. **Retires the long-running "openpilot 21 Hz notch" recommendation.** openpilot stays in scope as a *measurement instrument* only; the fix must be firmware-side.
- 🛑 [feedback-explain-with-python-mirroring-decompiled-arithmetic](feedback/tooling/feedback-explain-with-python-mirroring-decompiled-arithmetic.md) — **standing operator instruction 2026-07-28: explain firmware with simple Python that mirrors the decompiled arithmetic EXACTLY** (integer `>>`, real Q-format, real branch conditions, each line annotated with its instruction address, constants byte-read little-endian). Prose-only explanations hid the `0xC63D2`=14 error, the off-by-`0x1000` slips, and the "mute `0xC6AF0`" shorthand. dB/Hz comes *after* the code, never instead of it.
- ★ [accord-lerp-tables-count-word-first](accord/calibration/accord-lerp-tables-count-word-first.md) — **LERP tables start with a POINT-COUNT word; the base address is NOT the first X.** For `0xC6AF0`: count@`0xC6AF0`, X@`0xC6AF2..AFA`, **Y[0]@`0xC6AFC`, Y[1]@`0xC6AFE`**. Proved by the firmware's own `addi 0xc,r15,r13` (&Y[0]) and `addi 0x2,r15,ep` (&X[0]) at `0x3a63a`/`0x3a63e`, plus three sibling tables. **"Mute `0xC6AF0`" writes `0xC6AFC`/`0xC6AFE`** — writing the base clobbers the count.
- ★★ [reference-accord-v55-flashed-oscillation-is-internal](reference/builds/reference-accord-v55-flashed-oscillation-is-internal.md) — **V55 FLASHED, route `1c`: the ~21 Hz IS in `gp-0x6b98` and the loop is INSIDE the EPS.** Vibration 877× on the torsion bar / 996× on the independent angle-rate channel @20.90 Hz; command peaks in the SAME bin (coherence 0.93; route `1b` is a clean null control). **openpilot is 8.7× too small even with the LKAS low-pass deleted, and while RAILED its 21 Hz is exactly 0 yet the command still carries 105.8 counts.** Sensor→command transfer is **FLAT 0.19→0.22 from 1 Hz to 21 Hz** ⇒ the carrier is unfiltered. Damper bit7=1 ⇒ V44/V47 hit the LIVE tables.
- ★★ [reference-accord-gp6ad4-lane-and-c6af0-output-gate](reference/firmware/reference-accord-gp6ad4-lane-and-c6af0-output-gate.md) — `FUN_0003a382` has **three PARALLEL branches** (Stage A and C poles are exact 1024/1024 identities); the `0xC6AF0` LERP is a **Q15 multiplier on the ceiling that clamps the FINAL combined value**, so muting is a **complete, branch-agnostic kill** — which is why V43/V46/V48A (one branch each) were null without exonerating the lane. ⚠ `gp-0x6ad4` is **NOT LKAS-gated** (`gp-0x67fe` = FOC substate, live when disengaged) ⇒ muting changes manual feel. Monitor risk CLOSED (1 writer/1 reader, no lockstep); damping sign OPEN.
- ★ [accord-c6372-c636e-untested-wideband-assist-emas](accord/calibration/accord-c6372-c636e-untested-wideband-assist-emas.md) — both = 205/1024 → **only −1.29 dB at 21 Hz**, the only lanes besides `gp-0x6ad4` not band-limited below the mode. **Never flashed** — V44 pins them as "the rejected candidate B". 🛑 GATE 2 severe: `gp-0x6bbe` is base power steering, and adding 60-73° of assist-loop lag is the **V48B brick class**. Candidate #2, needs its own GATE 2 pass.
- ★ [project-v56-c6af0-mute-built](project/project-v56-c6af0-mute-built.md) — **V56 BUILT, UNFLASHED** = V55's probe + the mute, exactly 6 bytes off V55 (only 2 cal bytes move — `32768`=`00 80` LE). Builder is a POST-PROCESSOR over V55's SHA-verified image, zero transcription. ⚠ `V53.assert_stock_cals()` rightly refused it; **do not weaken that shared guard** — V56 runs it unmodified on the pre-edit source instead. Reversible experiment, not a known-good fix.
- ★★ [reference-accord-v56-flashed-mute-is-null-and-costs-damping](reference/builds/reference-accord-v56-flashed-mute-is-null-and-costs-damping.md) — **V56 FLASHED (route `24`, first ROAD drive with a probe): the `0xC6AF0` mute BOUGHT NOTHING ⇒ REVERT TO V55.** Speed-matched creep **786×** engaged/disengaged (V55 877×; replicated 1,878×/5,524×) and the command's in-band content did **not** drop ⇒ outcome (iii): **`gp-0x6ad4`/`FUN_0003a382` ELIMINATED as the driver of the 20-25 Hz mode, all three branches at once.** ★★ **The few-Hz resonance is WHEEL ORDER 1, not V56's doing** — `f = 0.489·v − 0.186`, r=+0.997, through the origin, implied circumference **2.088 m** ⇒ **tyre imbalance; get a road-force check.** A separate FIXED ~7-8 Hz resonance exists on every build and the wheel-order line sweeps up through it at 15-20 m/s. ⚠ "V56 removed damping" is **not supported** by the creep data (V56's 1-10 Hz power is 0.17× V55's; envelope Q not lowest) but **not closable** — no prior build has road-speed data. 🛑 A 50% partial restore is **not** a candidate: 0% and 100% already agree. 🛑 **Stop calling the mode "21 Hz"** — `f = 0.177·v + 20.48`, reaching **25.0 Hz** at 21 m/s, Q=160-228; and **steering angle shifts it ±2 Hz**, which confounds every cross-route comparison.
- ★ [reference-accord-fun3a382-is-a-real-pid](reference/firmware/reference-accord-fun3a382-is-a-real-pid.md) — **supersedes the "three parallel lag stages" framing.** It is a genuine discrete **PID**: Stage A = P (gain 153-256/1024, motor-rate scheduled), Stage B = I (98/1024, anti-windup), Stage C = **D, a raw backward difference at gain 2048/1024 = 2.0, UNATTENUATED**. The two `1024/1024` cals are each stage's *own extra* smoothing EMA collapsing to identity — so V43/V46 were re-introducing a defeated pole, not filtering the lane. Frequency table 3/5/8/21 Hz (P-dominated at few-Hz, D rivals P by 21 Hz, +41.8° lead). **Sign PROVEN: plain `add`, all 9 aggregator lanes, polarity `gp-0x6752` boot-fixed to +1, no inversion.** ⚠ contradicts the old "−3.3° to −5.4°" phase memory.
- 🛑 [reference-accord-probe-underranges-to-a-one-bit-comparator](reference/measurement/reference-accord-probe-underranges-to-a-one-bit-comparator.md) — **the `gp-0x6b98` probe UNDER-ranges to ~1.5 bits (we guarded against railing; the opposite happened).** 99.2% of engaged+hands-off frames in **two adjacent levels**; command inside ±512 while one LSB is 512. **An amplitude comparator but a usable SPECTRAL probe.** ⚠ **"120.5 counts at 21 Hz" is a REAL detection — keep it, restate it** (encoder gain 1.006; false-positive floor 10-18 counts ⇒ 7-12× above floor; but it is a **bin-RMS not an amplitude**, and **38× vs 66× depends on which estimator openpilot's 31.7 was — unresolved**). ⚠ **"flat H1" is UNCONFIRMED, not refuted** — quantisation is exonerated *by construction* (a memoryless nonlinearity cannot flatten a pole; coherence bias is DOWNWARD so 0.93 is a lower bound); the problem is dof: at ±19.6% a 16.8 Hz pole and flat are indistinguishable. 🛑 **The `0xC646C` elimination STANDS on its structural leg** (0 matches in 468 instructions). Also: **"9 independent segments" is wrong — K=3, significance 0.776**; re-scale to **`SHIFT=7/OFFSET=8`**, and 🛑 **`SHIFT=6` needs `OFFSET=9` or it collides with the `field==0` sentinel**; and the **5th harmonic of the mode folds into the few-Hz band**.
- ⚠ [accord-takeover-beep-is-openpilot-device-load](accord/signals/accord-takeover-beep-is-openpilot-device-load.md) — the intermittent "TAKE CONTROL IMMEDIATELY" is `commIssue`+`selfdrivedLagging` **softDisable that self-recovers** (once in 15:43, 1.04 s, `latActive` never dropped). **EPS/CAN is a clean null**: ST=0 throughout, `saturated` False 94,241/94,241, panda counters flat zero. Cause = **5-6 of 8 cores chronically near saturation** on a StarPilot fork; no single culprit process. Carries 4 rlog tooling traps: **`alertSound` is unpopulated route-wide**, `onroadEvents` DOES exist, **anchor the clock on each segment's first `carState`** (a bootstrap duplicate biases it +1.34 s), and `0x33D` byte4 is counter+checksum.
- 🛑★★ [reference-accord-gp6a5e-is-speed-reclassifies-v44-v47](reference/firmware/reference-accord-gp6a5e-is-speed-reclassifies-v44-v47.md) — **`gp-0x6a5e`/`-0x6a62`/`-0x6a64` are VOTED VEHICLE SPEED, settled** (voter `FUN_00041eec`, validity window −6400..32000, closest-to-previous fallback; zero overlap with `gp-0x4f60`'s cluster). ⇒ **retire the "Sensor A" label** — it was never a torque sensor. 🛑 **The damper Factor C LERP indexes on it**, pointer chase byte-verified (`0xC9E9C+10*4` → `0x000D27BC`), X=(2240,3840,5120,8960) ≈ **35/60/80/140 km/h** ⇒ **`Y[0]=0` means "below ~35 km/h", NOT "hands-off"** ⇒ **V44 and V47 were aimed at a mechanism that does not exist.** Their on-car results stand; their stated rationale is withdrawn. The "2240 counts driver torque" story is almost certainly a **collision** with the override curve's unrelated torque breakpoint of the same number. Also: invalid speed ⇒ factor defaults to **UNITY**, not zero.
- ★★ [reference-accord-driver-override-curve-kills-lkas-authority](reference/firmware/reference-accord-driver-override-curve-kills-lkas-authority.md) — the operator's *"significant driver torque in a direction kills the grinding"* is a **mapped authority cutoff** at `FUN_00028ea6` `0x29a74`: curve indexed by `gp-0x682f = |gp-0x4f60|>>5` with direction from `sign(gp-0x4f60)`, byte-read X=[70,72,78,80] Y=[254,234,12,0] ⇒ **LKAS authority collapses 254→0 between raw torque 2240 and 3584** (≈±2190..±3500 in DBC units). ⚠ The `0xC64B8` threshold branch is **DEAD** since V37 set it to `0xFF`. 🛑 It explains the **KILL, not the CREATION** — hands-off the curve is flat at 254. ⚠ It also means a raw-band-power driver-torque sweep **looks like it contradicts the operator**; you must use a **prominence** estimator.
- 🛑★★ [reference-accord-deadband-signgate-eliminated-on-car](reference/firmware/reference-accord-deadband-signgate-eliminated-on-car.md) — **`gp-0x6806` IS ON THE BUS: CAN `0x18F` byte4 bit3 = `STEER_CONTROL_ACTIVE`** (packer `FUN_00055c42`, matches opendbc `BO_ 399` exactly). Measured route 24: **==1 in 96.26%, TWO transitions in 180 s, max toggle 0.1 Hz** ⇒ the `0xC61B8` deadband + sign relay is **bypassed in steady engaged driving and CANNOT be a 20-25 Hz mechanism.** Kills the relay/limit-cycle hypothesis (which fit the *entire* symptom profile — a warning about explanations that fit too well). ⚠ **BUT the bus carries PARITY, not the gate's test** — the packer does `andi 0x1` (`0x55c7e`) while the gate tests exact equality (`cmp r0,r12 ; bne` @`0x2a1ba`); four of eight live writers store a REGISTER, so a 0↔2 toggle at 22 Hz would be invisible. **V57's probe bit6 is the exact test and closes it** (`rlog-tools/probe/decode_v57_deadband.py`). ⚠ It **IS** on the forward path (`r1`→`r16`→`st.h -0x6b3c @0x2a2ea`); the `-0x6b38` store at `0x2a23c` is a **diagnostic copy** and a subagent wrongly stopped there. ⚠ `0xC61B8`=102 was **never rescaled** while `0xC61B2/B4` went ×4 — a real defect, but on the **engage ramp** only. 🛑 **Split every `FUN_00028ea6` scan at `0x2a30d`**: `gp-0x6806` has 16 raw writers = **8 live + 8 dead-echo**; `gp-0x6b30` has 4 refs = 2 live + 2 dead.
- ★★ [accord-angle-rate-lane-gp6bbe-top-candidate](accord/firmware/accord-angle-rate-lane-gp6bbe-top-candidate.md) — **TOP surviving vibration candidate, UNBUILT.** `FUN_00034a72` (boost, summand `gp-0x6bbe`) reads the **steering ANGLE RATE `gp-0x6a56` UNFILTERED** (`0x34AB8`/`0x34E8E`, byte-verified by the lead after two subagents disagreed) as one side of an FSM-result subtraction, scaled by two **speed-indexed** LERPs. That signal is what the EPS **transmits** as `STEER_ANGLE_RATE` (0x14A[2:3] and the 10× finer 0x18F[2:3]) ⇒ **opendbc ground truth**, and it carries the mode at **996×** vs torque's 877×. ⇒ the rate path **bypasses the torque EMA**, so the −1.29/−14.91 dB figures and the unresolved `FUN_00022ca0` task rate **matter far less than they appeared**. 🛑🛑 **GATE 2 ANSWERED *AGAINST* CUTTING IT — THE LEVER INVERTS.** The torque EMA is a **multiplicative amplitude scale**, not an additive branch, and the core term is `rate_error = baseline − angle_rate` (`sub r6,r28` @`0x34e96`) ⇒ **`gp-0x6bbe` ≈ −(gain)·angle_rate = viscous DAMPING.** **Cutting it would REMOVE damping and likely make the grinding WORSE — the V56 error one build later.** The interesting direction is **RAISING** the gain: cleanest lever **`K1` @`0xD200C`=43** (1 hit image-wide). ⚠ **INFERRED, not simulated** — rests on `baseline` being slow at 22 Hz; **certify by simulation before building.** Also: **speedLERP2 `0xD20C0` is FLAT** (fixed ±512 clamp), speedLERP1 is a hump peaking at 40 km/h, not a monotonic speed rise. **First candidate ever in the angle-rate domain** — all ~50 falsified levers were torque-domain.
- ★ [v57-decouple-built](builds/v57-decouple-built.md) — **V57 BUILT, UNFLASHED** = V55 + the `0xC646C` decoupling: `0x2A1F0` disp `0x746C`→`0x7CD0` so the **forward** reader gets a private word at `0xC6CD0` = 3564 while the **four feedback readers revert to stock 891**. 14 bytes off V55, 50/50 CRC, RWD round-trip gated, verified against the built image. ✅ **no float mirror** (fresh 32-bit scan of `[0x7440,0x74A0)` → 0 hits) ⇒ no V27 desync risk. 🛑 **Correctness fix — expected NULL for the grinding** (≤0.28 dB at 22 Hz; only 1 of the **11** aggregator summands reads the cal, at −46..−58 dB). ⚠ Manual feel **will** change. ⚠ Reader #6 is **not** a second additive path — it modulates #5's hysteresis dead-band **width**. **Flash V55 FIRST** to undo V56 cleanly.

## 2026-07-29 session (documentation hygiene) — golden model distilled, no firmware change

- 🛑 [feedback-golden-model-distilled-keep-terse](feedback/builds/feedback-golden-model-distilled-keep-terse.md) — **`model/eps_lkas_chain_model.py` cut 4,709→2,200 lines (347→145 KB), comments/docstrings only.** Operator instruction: comments ballooned into dated changelogs + multi-hundred-line findings essays embedded in function docstrings (e.g. `vibration_hands_off_analysis` alone carried ~430 lines). Distilled to comments ≤1 sentence, docstrings ≤1 paragraph, address/Q-format/confidence-tag ties kept. **Verified two ways, not just claimed:** an AST-equality diff (docstrings stripped) proves zero code changed; `python model/eps_lkas_chain_model.py` produces byte-identical stdout before/after. No new firmware finding — this is a documentation-quality pass. Two content fixes made in passing: a stale "4-of-16 phase-gated" claim (already contradicted by the module docstring's own EXECUTION MODEL correction) was removed rather than perpetuated; two densest functions (`vibration_hands_off_analysis`, `motor_torque_governor`) were left at ~20-28 lines rather than a strict one-liner since they carry the two most load-bearing CONFIRMED root-cause chains in the file.

## 2026-07-30 session — V57 driven; the two symptoms separate; V58 probe built

- ★★ [accord-ratchet-and-grinding-are-two-symptoms](accord/mechanism/accord-ratchet-and-grinding-are-two-symptoms.md) — 🛑 **The ~7.4 Hz mode is the RATCHETING; the 20-25 Hz mode is the GRINDING.** Operator, authoritative. Everything written before 2026-07-30 conflates them, and a mid-session claim that "the kit has been optimising the 5.3% component for 50 builds" is **WITHDRAWN**. The ratchet rises **8.42×** with command rail duty while the grinding falls to 0.74×; it is **not** the V42 state-4 ratchet (`ST==4` fires 0/37,922). Angle excitation is correlation only, via return-to-centre.
- 🛑 [accord-telemetry-conventions-that-produced-wrong-answers](accord/instruments/accord-telemetry-conventions-that-produced-wrong-answers.md) — three rlog conventions that each flipped a headline: `cruiseState.enabled` is long+lat (reads **0.00%** on parking-lot routes; use `carControl.latActive`), raw `|tq|<=200` hands-off **discards the frames carrying 8.79× the oscillation**, and mean Welch power hides bursts (**median 0.419 vs p99 0.891**). Fourth, ✅ **SOLVED 2026-07-30** by route `2b` seg 13 (60 s moving-but-disengaged at 0.5–4.8 m/s) — see [[accord-v58-drive-grinding-engagement-gated-creep-only]]; the old 877×/786×/14,750× stay retired. Fifth and sixth, added the same day: use a **strict 18–26 Hz band** (a wider one catches the ratchet's 2nd harmonic and fakes a frequency slope) and **prominence, not envelope amplitude**, to separate a mode from broadband.
- 🛑★★ [accord-gp6a56-is-motor-rate-not-an-angle-sensor](accord/signals/accord-gp6a56-is-motor-rate-not-an-angle-sensor.md) — `STEER_ANGLE_RATE` is **synthesized from the MOTOR resolver rate** (`FUN_0003f776`, fixed Q15 scale of `gp-0x6abe`), not independently sensed. Kills "the only control-path signal with an external anchor", and since `baseline` is also `gp-0x6abe`-derived, `gp-0x6bbe`'s damping sign is **unresolved** after four static flip-flops.
- ⚠ SUPERSEDED [accord-v58-boostlane-probe-built](accord/builds/accord-v58-boostlane-probe-built.md) — **V58 has since FLASHED and flown clean**; see [[accord-v58-drive-grinding-engagement-gated-creep-only]]. Its bit5 result ELIMINATED the `0xD20C0` ceiling, but **bit6 returned nothing** and this file's "method pre-validated" claim did not transfer — see [[accord-sign-probe-needs-zero-crossings]].
- 🛑 [accord-c61d6-slew-is-rejected-not-fresh](accord/calibration/accord-c61d6-slew-is-rejected-not-fresh.md) — `0xC61D6` 0→14 is **REJECTED** ("highest-risk lever; last/never" — it activates an uncalibrated 2D map onto the live command), not untested. Re-proposed as fresh on 2026-07-30 because it appears nowhere in `BUILD-LINEAGE.md` and an agent-memory still recommended it. Both fixed. **`0xC64DE`=27 (V18) is carried forward correctly; there is no output rate limiter available as a cal.**
- ★★ [accord-v58-drive-grinding-engagement-gated-creep-only](accord/builds/accord-v58-drive-grinding-engagement-gated-creep-only.md) — **V58 FLASHED, flight-clean** (route `2b`, 83,959 frames, `ST==4` = 0). ★★ **The collinearity confound is BROKEN** — seg 13 gives 60 s moving-but-disengaged at creep speed ⇒ **13.4×** speed-matched, **184×** on occupancy, and the resonance is **ABSENT disengaged** (prominence 122.7× vs 3.6×). Three record corrections: the **frequency law does not reproduce** (fixed ~20.9 Hz; `a = 0.177` rejected at 3.2–7.1σ), the grinding is **CREEP-ONLY** not road-speed, and **~21 Hz IS in openpilot's command** (direction unresolved).
- 🛑★★ [accord-gp6ba6-is-the-boost-amplitude-index](accord/signals/accord-gp6ba6-is-the-boost-amplitude-index.md) — **`gp-0x6ba6 == |gp-0x6b9a|`** and it — not `gp-0x6b9a` — indexes both boost amplitude LERPs; **`0xD28DC` hangs off `0xca4f4`, not `0xca23c`**. `builds/v50_v79/build_v58_tva.py` was wrong on both counts. ⇒ the index is V58's 20.93 Hz signal **rectified**, sweeping the curve at **~2× the mode frequency on the BASE ASSIST path**. Depth unmeasured ⇒ V59. Also kills a subagent's "2-pole biquad" claim (it is the identity 3-tap FIR).
- 🛑 [accord-sign-probe-needs-zero-crossings](accord/instruments/accord-sign-probe-needs-zero-crossings.md) — **a 1-bit SIGN probe carries phase only for a signal that crosses zero at that frequency.** V58's headline bit6 returned nothing because `gp-0x6bbe` is DC-dominated (0–1.1 transitions/s vs the ~44/s a 22 Hz flip needs). ⚠ **Pooling runs manufactures coherence** out of splice steps. Probe **magnitude**, not sign. Phase resolution is also capped at ±77° by the one-sample mailbox skew.
- ★ [accord-v59-boostindex-depth-probe-built](accord/builds/accord-v59-boostindex-depth-probe-built.md) — **V59 BUILT, UNFLASHED**: thermometer on `gp-0x6ba6` at 512/1024/2048 + the `0xFFFF` fault-sentinel bit. **19 bytes off V58, CAL CRC unchanged.** No new encoder, no new condition code. Measures the one thing V58 could not — **depth**, i.e. how much gain swing the modulation actually delivers (`<512 ⇒ ≤1.12×` … `≥5120 ⇒ 2.00×`).
- ★★ [The grinding is an UNCOMPENSATED loop through the torque sensor](reference/firmware/reference_accord_loop_through_torque_sensor_uncompensated.md) — command→bar transfer peaks at 21.09 Hz (global max, 15.6–25.7×); no motor-command feedforward exists anywhere; damping is zero below 35 km/h. Displaces the parametric-pump story.
- ⚠ [`search_instructions` OVER-counts too — address-literal collisions](reference/tooling/reference_accord_search_instructions_overcounts_address_literals.md) — 21 hits reported vs 2 real for `gp-0x6b70` (`jarl 0x0006b700`). Both error directions now documented.

## 2026-07-31 session — V60 null closes the pump; the V52C "halving" was never a measurement

- 🛑🛑 [A caveat can mutate into a positive result](accord/instruments/accord-a-caveat-can-mutate-into-a-result.md) — "V52C halved the mode" was the FILTER'S OWN TRANSFER FUNCTION (-6.1 dB = 0.496x) quoted inside a caveat about why its NULL was weak, then restated as the kit's biggest positive result. No V52C rlog exists. Inverse of the withdrawn-rationale trap.
- ★★ [V60 FLASHED: null -> the parametric pump is CLOSED](accord/builds/accord-v60-null-closes-parametric-pump.md) — the discriminator returned the predicted null; V58/V59/V60's whole arc closes. gp-0x6ba6's index drives ONLY the boost/damping amplitude LERPs (readers byte-verified), so 0xC63BA is pre-falsified by the same null.
- ★★ [The torque RATE gp-0x4f62 is a second feedback path V52C could not reach](accord/firmware/accord-torque-rate-lane-v52c-structurally-blind.md) — 9 sites, 3 consumers, 2 of them gate-only; the ONE live magnitude path is r24/r26 via 0x3AA9C, ~264x gain at 21 Hz, clamped +/-5120. Its shadow gp-0x4488 never leaves the producer. r24 (V39, CONDITIONAL) and r26 (V42) were each killed alone -- never together.
- ★★ [Task 5 is 100 Hz — the damper cannot damp 21 Hz](accord/firmware/accord-task5-is-100hz-damper-cannot-damp-21hz.md) — rate divider FUN_00014be4, mod-100 on the 1 kHz tick; idx*0x30+0xBB920 reproduces all 7 TCB entries. Boost+damping run at 100 Hz ⇒ 37.6-75.2° ZOH lag at 20.9 Hz ⇒ a structural reason V44/V47 were null, independent of the FactorC speed axis. Also invalidates V59's eps table (bracketed 1 kHz/500 Hz).
- ★★★ [V61 FLASHED → WORSE: the rate lane is the DAMPER, not the amplifier](accord/firmware/accord-rate-lane-is-the-damper-not-the-amplifier.md) — the kit's first SIGNED on-car result. r24/r26 = +Kd·d(T_bar)/dt added IN PHASE with assist (polarity is one shared load, combine chain is ten `add`s, no `sub`) ⇒ viscous damping across the torsion bar; at Kd=0 the mode has no damping term at all. Grinding worsened engaged AND appeared in manual/reverse. A derivative is DC-neutral, so "V61 removed assist" is ruled out. 🛑 Falsifies the golden model's "r26 = excitation-to-amplifier" framing; V39/V42/V61 all tested the lane DOWNWARD and bracket the wrong side of the optimum.
- ★★ [V62 BUILT: double the rate lane via two `sar` immediates](accord/builds/accord-v62-doubles-the-rate-lane.md) — 0x3AC20/0x3AB76 `sar 0xa`→`sar 0x9`, 6 bytes off V59, CAL CRC unchanged. Chosen over the gain cals because the arm is a priority chain that can't be pinned statically, r24's default arm is MODE-INDEXED via gp+0x63fd, and gp-0x683c is a dead gate. 🛑 0xD6AEC is mode 22, NOT a redundancy twin — the V27-desync reading was wrong. 🛑 0xC6C42 lockstep objection RETRACTED (single reader, broadcast in sync); D is a real V63 PHASE lever, 4→2 halves the lead's lag 15.1°→7.6°.
- 🛑 [A strict band presence-tests, it cannot LOCATE a mode that moved](accord/firmware/accord-rate-lane-is-the-damper-not-the-amplifier.md#methodology-trap) — V61's first pass pre-restricted to 18-26 Hz and the argmax pinned to the band edge (18.04 Hz, sd 0.00) because the mode had shifted BELOW it. Locate over 12-30 Hz, then interpret. The mode moved 21.18 -> 18.25 Hz, which a pure gain change cannot do — that frequency shift is the structural proof the lane is a PHASE (lead) term.
- ★★★ [gp-0x671a is an OSCILLATION DETECTOR (hard-reversal counter)](accord/firmware/accord-state671a-is-an-oscillation-detector.md) — FUN_000428d4 resets the count to 0 EVERY TICK while neutral and only leaves neutral if |gp-0x6c2c| > 12800; a reversal increments it, 50 quiet ticks clear it. So `state>=5` = "an oscillation is happening", NOT "smooth steering". Both rate lanes already branch on it ⇒ 0xC6440/0xC643E are OSCILLATION-ONLY gain arms. 🛑 Polarity was DISPUTED by two subagents and resolved by the orchestrator in Ghidra — the wrong reading was plausible and would have inverted the build. ⚠ No LKAS-active signal exists in this path at all (gp-0x6806 has 7 readers, none of them FUN_0003aa2c/FUN_000428d4).
- ★★ [V63 BUILT: oscillation-gated rate damping, 2 cal bytes](accord/builds/accord-v63-oscillation-gated-rate-damping.md) — 0xC6440 2048→4096, 0xC643E 1536→3072; MAIN CRC unchanged. Supersedes V62: same damping increase, zero manual-feel cost, smaller edit. 3072 is already gain_A's stock max so there is no new arithmetic risk. 🛑 A null is AMBIGUOUS (the detector may never trip) — resolve by then flying V62, which cannot miss. Fly V63 FIRST.
- 🛑 [Describe builds FUNCTIONALLY and against STOCK, not as a per-build delta](feedback/builds/feedback-describe-builds-functionally-vs-stock.md) — standing operator preference 2026-07-31. One plain-language bullet per capability (4x LKAS gain, EME fixes, steer-to-zero, dampers, telemetry), cumulative vs stock, plus what is notably ABSENT. 🛑 Derive it from the IMAGE via `analysis-2020accord/verify/diff_build_vs_stock.py <build>`, which fails loudly on any unattributed byte — a doc-based answer would have missed that 0xC646C is back at stock and that V42's 0x454FE is not in this line.
- ★★★ [gp-0x671a is a ONE-WAY LATCH with a 5 s hold — V63/V64's decoupling is narrower than first claimed](accord/firmware/accord-state671a-is-an-oscillation-detector.md) — FUN_000428d4's output stage re-pins the counter to CEIL once reached; the only way down is 5000 consecutive ticks with driver torque ≥ 640 (cal 0xC62DE) AND no reversals, and torque dips below 640 on every direction change. ⇒ "V62, but only after an oscillation has happened", NOT "only while oscillating". 🛑 The "zero manual-feel cost by construction" claim is WITHDRAWN. ✅ But the latch is PROTECTIVE — a per-tick-gated gain would modulate AT the mode frequency, i.e. a parametric pump.
- ★★ [V64 BUILT: V63 + the probe repointed at the detector](accord/builds/accord-v63-oscillation-gated-rate-damping.md) — operator's proposal. The old probe measured gp-0x6ba6, the pump V60 falsified, so a V63 null would have been uninterpretable. 0x14A byte4: bit6 = state>=5, bit5 = state!=0, bit4 = FSM left neutral, bit3 = r24 override. Actionable in EVERY failure mode (bit4 clear ⇒ lower T 0xC620A; bit4 set + bit6 clear ⇒ lower CEIL 0xC64FA). 68/68 cave bytes, zero budget left. ⚠ V850 ld.bu carries disp bit0 in hw1 bit5, not hw2 — a naive decode reports false mismatches.
- ★★★ [V64 FLASHED: the null is on the GATE, not the hypothesis](accord/firmware/accord-state671a-is-an-oscillation-detector.md) — probe read a constant 0x87 across all 14,980 frames: gp-0x671a/gp-0x67df/gp-0x671d ZERO on every frame through 1,158 steering-rate reversals, so |gp-0x6c2c| never crossed T=12800 and 0xC6440/0xC643E were never in force. Spectra confirm V64 ≡ V59 (2-3 m/s bin: 20.98 vs 20.99 Hz, env99 1811 vs 1804). 🛑 Do NOT cite V64 as evidence against raising the rate lane. ⇒ V62 promoted to the recommended next flash. ⚠ Even if armed it delivers r24 x1.78 / r26 x1.00 — Honda's oscillation arms are gain REDUCTIONS (defaults 2305 and 3072 vs arms 2048 and 1536).
- ★★ [gp-0x6c2c is a MOTOR-RATE DERIVATIVE, not torque](accord/signals/accord-gp6c2c-is-the-detector-input.md) — the detector's only input; FUN_00041464 @0x4184E differences a smoothed gp-0x4f50, x32, twice EMA'd, >>9. Tripping T=12800 needs ~1683 counts at 21.3 Hz (frequency-domain cross-check: 1685), inside the ±13000 validity ceiling ⇒ the drive was ~1.7-2x short, NOT structurally blind. 🛑 Three bus-torque-derived numbers are VOID (T≈2048-2560 band, the 3.29x LSB bound, "per-tick ⇒ dead") — the bus bounds amplitude but not scale, and this firmware rescales by 8.000x between two copies of one signal.
- 🛑 [gp-0x671a has FOUR external consumers — T is not a free lever](accord/firmware/accord-state671a-is-an-oscillation-detector.md) — FUN_0003a382 uses it as a CONTINUOUS LERP index into the live P/I/D lane gp-0x6ad4, FUN_00036c12's friction-comp sums into the SAME aggregator, plus FUN_000352b4/FUN_00035b20. Lowering T changes FIVE things at once. gp-0x67df by contrast is clean (2 hits, both internal). 🛑 0xC64FA (CEIL) is a BYTE cal = 5 — a halfword read gives 517 and is wrong.
- 🛑 [Probe the gate and the input, not just the output](feedback/measurement/feedback-probe-the-gate-not-just-the-output.md) — V64 removed V63's ambiguity and created a new one a layer up (did T never trip, or did the detector never run?), closable only by a post-hoc trace. Every gated mechanism is enable → input → threshold → state → effect; a probe on the state collapses everything upstream into one null. Ask "if every bit reads zero, how many stories explain that?"
- ★★★ [V62 FLASHED: THE GRINDING IS FIXED](accord/builds/accord-v62-flashed-grinding-is-fixed.md) — route 37, 86,278 frames. 18-22 Hz creep **0.124 [0.036, 0.387]** vs V59 (8x), **0.024 [0.016, 0.234] at |rate| 16-32 deg/s (42x)**, with a 30-40 Hz negative control at ~1.0 ⇒ band-specific. Transient rates 0.793/0.486/0.338 at >200/>500/>1000 — monotonically cleaner; lowest p90/p99/>1000-rate of ANY build. **The kit's first measured fix.** 🛑 The reported "new grinding" is NOT an established regression: the 43 excursions >2000 are ONE 0.92 s burst (n=1), V62's burst-rate CI **[0.00004, 0.00793] sits inside V59's [0, 0.00986]**, and an exposure-matched test gives **p = 0.51**. Instant #2 is an ordinary burst V59 produces 3x MORE often. ⇒ **No new build — fly V62 again and count bursts.** 🛑 **TWO CORRECTIONS 2026-08-04:** (a) the companion *"r26 is structurally inert ⇒ `0x3AB76` was a no-op"* claim is **REVERSED** — V62 doubled **both** lanes; the measured results stand, the single-lane attribution does not. (b) **V62's dose is near the OPTIMUM, not on a ramp** — V69's 4× brought grind #1 back (median `e_18-22` engaged creep 2501/879/**168**/109/746 at 0×/1×/2×/2×-gated/4×) ⇒ **do not read 8–42× as "more would be better."**
- 🛑 [The "r26 is INERT" claim SPLITS — one leg reversed, one downgraded](accord/firmware/accord-r26-is-structurally-inert.md) — **filename kept so old links resolve. NOT a flat reversal** (that would be the mirror image of the original error). **LEG 1 — THE GATE: REVERSED [EVIDENCE].** `r26 == 0 ⟺ gp-0x6b5e != 0`, `gp-0x6b5e = ((LERP(gp-0x6bda) × 0xC63C2) >> 10) × polarity` (producer `FUN_000361c8` @`0x36256`/`0x36264`), trapezoid `0xC66CC` X = [−384,−128,128,294,384] Y = [0,4762,4762,717,0] ⇒ killed **only** at `|gp-0x6bda| ≥ 384` — and `gp-0x6bda` is a **margin to a peak-hold envelope of driver assist torque `gp-0x6bf0`** (`FUN_00036022` @`0x36068`–`0x3608C`; envelope half-width never < 9390), **≈ 9262 = 24× the threshold hands-off**. ⇒ **the gate does NOT kill r26 in ordinary driving**, least of all hands-off at creep; the kill window is a **~512-count sliver at the driver-override end** (cf. `0xC6156` = 9216). **LEG 2 — THE MAGNITUDE: STILL BELIEF, unresolved either way.** `0xC6564` really is 40 zero bytes with no writer found for the RAM adjustment (10 of 18 cells) ⇒ `stage1 ≈ 0` **IF** that cal base feeds `gp-0x69a4` — **but that link was NEVER VERIFIED**, and `gp-0x69a4`'s real producer is a **live runtime 10-segment LERP at `0x355C6` in `FUN_000352b4`** (1 writer / 3 readers: `0x355A4`, `0x3575A`, `0x3AB3A` = the aggregator). ⇒ **"r24 carries the entire lane" rests on LEG 2 alone, and the V42/V61/V62 re-attribution is CONTINGENT on it.** ★ **The one indirect argument that LEG 2 holds**: at `a ≈ 1` V67/V68's gate (gain_A 3072 → 512, a 6.00× cut) would put their engaged **total at ~0.94× stock** — essentially on stock — **yet V67/V68 measured the best grind #1 result in the kit (109 vs stock's 879)** ⇒ the empirical record argues **`a` is small**. ✅ **DIRECTLY MEASURABLE**: r24/r26 share **one** polarity load (`ld.b -0x6752[gp],r14` @`0x3AB78`, reused @`0x3AB7E`/`0x3AC3E`) so they **always carry the same sign** ⇒ `sign(gp-0x6adc)` vs `sign(gp-0x6ada)` is a matched pair — **bit4 pinned at 1 while bit3 toggles ⇒ r26 is zero; bit4 tracking bit3 ⇒ r26 is live.** V70 flies exactly that pair. ⚠ **`0xC6444` raising is genuinely UNTESTED** (V42 tested it **downward**, 512 → 0, falsified — the same "downward ≠ upward" distinction V61→V62 turned on); 1 reader / 0 writers, no float mirror, CRC block #48, ceiling ≤ 6553. 🛑 **A CANDIDATE, NOT A RECOMMENDATION — V70 does not take it**, because `a` is unmeasured and V67/V68's control path is the measured best. ✅ V62/V65's `sar` route is the **only** edit here that is dose-exact independent of `a`.
- ★★ [The ratchet is a SATURATED RESONANCE, not friction](accord/mechanism/accord-ratchet-is-a-saturated-resonance.md) — LKAS-gated at **Fisher OR 115.5, p = 1.09e-08** on episodes pooled across 5 builds (75.3% vs 2.1% presence); **waveform SYMMETRIC on every build** (skew(dx/dt) −0.16…+0.06 vs a −3.27 sawtooth calibration) ⇒ damping/loop gain, not stick-slip. Fixed in hertz (CV 0.211 vs order 0.829). ❌ Damper `gp-0x6bd0` **inert at both operating points** (f5 = 0 below 12.7 deg/s; ratchet median 2.0 deg/s) — a THIRD reason V44/V47 were null. ❌ Relay, friction-comp, and the motor-rate LERP as a discriminator all eliminated. ✅ Open: gain_B's X **breakpoints** are calibration and the modes DO separate on rate (9.4 vs 73.0 counts).
- 🛑 [Bootstrap over EPISODES, and measure the noise floor first](feedback/measurement/feedback-episodes-not-windows-and-the-noise-floor.md) — windows within a run are correlated; a window bootstrap shrinks CIs by ~√28 and manufactures significance. Split-half-by-episode null gave a **2.2-2.8x resolution floor**; judge ratios against that, not 1.0. Retracted three of the orchestrator's own claims in one session, incl. a Simpson's-paradox f0 "shift" and an effort-gating that was really **motor rate** (partial ρ(effort|rate) NEGATIVE, ρ(rate|effort) +0.57…+0.70). ★ Prefer `|d(tq)|` for cross-route work (null 0.87-0.92 vs 0.63-1.23 for band power).
- 🛑 [0x14A byte4 = 0x87 means the OPPOSITE thing on V62 vs V64](accord/builds/accord-v62-flashed-grinding-is-fixed.md) — V59's thermometer (which V62 carries) reads it as "boost index ≥ 2048", the DEEPEST reading; V64's detector reads it as "unarmed", the V64 null. It is 9.24% of route 37 and the V64 handoff primes everyone to see a null. Always confirm which probe the flown build carries before decoding byte4.
- 🛑 [Byte scans confirm instructions that DO NOT EXIST — 5 false positives in one afternoon](accord/firmware/accord-formatv-aliasing-five-false-positives.md) — building V65's cave, a raw LE scan "found" `cmp -0x2,rN` at four addresses and a `bgt +6` at a fifth; **all five were aliases inside `mov`/`jarl`/`dispose`**. On a short 2-byte pattern the hit rate was 5 of 6 WRONG. Pin every new encoder to a **boundary-confirmed** Ghidra instance, or by **three-way field decomposition** if none exists — and assert the near-miss (`sar` vs `shr`, `ld.h` vs `ld.hu`, `ld.h` 0x39 vs `st.h` 0x3B). ⚠ Different failure mode from `search_instructions`, which under/over-counts *analysed* instructions; a byte scan **hallucinates** them. ⊕ **Second, distinct trap in the same file:** `hw1` bit 5 is an `ld.bu`/`ld.hu` displacement's **bit 0**, but on `ld.h` (op 0x39) it is **just the opcode's low bit** — same position, opposite meaning. Encoding `-0x67ac` with it set reads **`gp-0x67ab`**, a real adjacent cell: a plausible wrong answer, not a crash. Always reconstruct `(hw2 & 0xFFFE) | (hw1 bit 5)` **as a live gate**.
- ★★ [V65 BUILT, UNFLASHED — the saturation ladder](accord/builds/accord-v62-flashed-grinding-is-fixed.md) — V62 + cave payload only; **CAL CRC and 0xD2000 byte-identical to V62**, 56 bytes off V62 (52 cave + MAIN CRC). A 4-level symmetric ladder on the aggregator output `gp-0x6b94` vs its own ±10240 clip: `0xE7` +RAIL / `0xA7` +HALF / `0x87` NEUTRAL / `0x97` −HALF / `0x9F` −RAIL, via one `ld.h` + one `sar 0xc` + 4 rungs, 62/68 bytes. Three decoder-checked invariants (bit6⇒bit5, bit3⇒bit4, **never both sides** — also the discriminator vs V59/V64 byte4). Headline = bit6↔bit3 alternation rate. 🛑 Draft 1 spent 2 bits on `gp-0x67ac`, which is **structurally pinned at 0** (cal `0xC4124` = (0,0,5,0,5,5,0,0,0,5,0), nothing in {2,3,4}) — the V64 mistake forming again; rebuilt. Image `f12171a8…`; RWD `dabc9241…`.
- 🛑 [The V62 "new grinding" is ONE phenomenon with ≥5 instances, and the RATCHET is loudest HANDS-OFF](accord/builds/accord-v62-flashed-grinding-is-fixed.md) — **operator corrected both, data confirmed both.** Ranking engaged windows by 30-49 Hz env99: 10:12:14-15 (3829/3794/3477), **10:23:21-24 (1225/946)**, 10:15:59 (1219), 10:14:18 (886) — all engaged, all under real driver effort, **1.9-17.7 m/s**. The "two different phenomena" split was an artifact of a `|d(tq)|>2000` threshold only the largest crossed; a sibling agent had already found them the same class at 3.7× severity. **Rank by a continuous statistic before classifying.** ⊕ RATCHET 6-12 Hz env99 at creep: V62 **hands-off 2665 (n=25) vs hands-on 392 (n=14) = 6.8×**; V59 1265 vs 506. **The driver's hands DAMP it** — use the hands-off arm as primary; a driver-load gate selects AGAINST it.
- ★★★★ [GRIND #2 IS V62's OWN FIX SEEN AT HIGH FREQUENCY](accord/mechanism/accord-grind2-is-a-45hz-mode-under-driver-load.md) — corner-conditioned tail maxima, Kd=1x vs Kd=2x, 219 blocks: **1-4 Hz 1.01 · 10-16 Hz 0.80 · 18-22 Hz 0.35 · 24-28 Hz 2.66 · 30-40 Hz 2.98 · 40-49 Hz 11.71 (p=0.0003)** — a **monotone response with a crossover at 22-24 Hz**, driver band flat as a control. **One knob cut grind #1 by 2.9x and raised grind #2 by 11.7x.** Cause: `gp-0x4f62` is a **4-sample finite difference**, so its gain RISES with frequency (1.93x at 41.6 vs 20.9 Hz) and V62's *flat* x2 is not frequency-selective. Grind #2 = **~44.9 Hz, Q≈37**, NOT a harmonic (slope 0.173 [-0.92,1.59] vs 2.0), **NOT LKAS-gated** (p99 1.33x vs grind #1's 6.63x), lives at creep under `tq_avg` 1600-2700. ✅ **Confirmed on the comma IMU** — 20-50x baseline during bursts, ρ 0.23-0.55, p≪1e-70 ⇒ a real whole-car vibration on a sensor independent of the EPS. 🛑 Frequency is **aliased and unresolved** (CAN ~100.5 Hz grid; IMU ~101 Hz is only 0.5 Hz away so it does NOT break the degeneracy). 🛑 **A filter cannot fix it** — differentiator +20 dB/dec vs one pole −20 dB/dec is FLAT above the corner; two poles cost −92° at 20.9 Hz and destroy the fix. Separation must come from an operating condition: **driver torque separates them >8x**, steering rate only ~2x.
- ★★ [The dead gp-0x683c gate is a free conditional-gain arm](accord/firmware/accord-gp683c-dead-gate-is-a-free-lkas-arm.md) — **ONE access image-wide** (`ld.bu -0x683c[gp],r15` @`0x3AA94`) and **ZERO writers**, raw-scanned in both encodings; its only consumer is `setfne lp` @`0x3AAA8`, and `lp` selects cal `0xC6446` for r24 and `0xC6444` for r26 (**one reader each**). ⇒ repointing that single load is a **ONE-BYTE edit** (`0x3AA96`, even displacements only) that makes the rate-lane gain conditional on any gp byte cell — the only conditional-gain surface here needing **no code cave**. 🛑 Chatter is the kill criterion (a gain switching at the mode frequency is a parametric pump); 🛑 `gp-0x671d` OUTRANKS the arm and would mask it.
- ★ [r24's gain_B uses FOUR pointer arrays, not four consecutive records](accord/calibration/accord-r24-gain-b-four-pointer-arrays.md) — `0xCBF5C`/`0xCC044`/`0xCC12C`/`0xCC214` at `mode*4`; mode 10 ⇒ `0xD2A74`/`0xD2AB0`/`0xD2AEC`/`0xD2B28`. Reading them **consecutively** from `0xD2AEC` picks up mode 11's interleaved rows and **understates the rolloff by 2x**. At creep the gain is **3072** (not the oft-quoted 2305, which is the 50 km/h record), flat to motor rate 400, falling to 1536 at 3000. gain_A (r26) is **not** mode-indexed. ⚠ `builds/v50_v79/build_v62_tva.py`'s tripwire watches only 2 of the 4 records.
- 🛑 [Report the MEAN and the TAIL together](feedback/measurement/feedback-mean-and-tail-must-be-reported-together.md) — the same data gave **0.913 [0.791, 1.026]** (a confident null) on matched-cell means and **27/219 vs 1/91 blocks** (a confident positive) on the extreme tail. The matched q99 threshold was **317** against burst amplitudes of **3000-4000** — the mean test was structurally blind. Print tail thresholds next to event amplitudes; mark **provoked** routes (a route driven to demonstrate a symptom cannot evidence its rate); report exposure **in seconds** in the conditioning corner.
- ★★★ [The aggregator NEVER rails — the loop is LINEAR there](accord/mechanism/accord-aggregator-never-rails-loop-is-linear.md) — V65's ladder on `gp-0x6b94`, **120,049 frames**: liveness 100%, zero invariant violations, **+RAIL 0 / −RAIL 0**, only **54** frames past ±4096, `bit6↔bit3` alternation **0.0000/s in every arm** (no rail frame exists, so no flip sequence exists). The sum never comes within 20% of its ±10240 clip ⇒ **no describing-function/saturation reasoning is needed anywhere in this chain, and a lane-gain change propagates faithfully** — which is why V62's flat ×2 produced its band table. ★ All 54 non-neutral frames sit **inside grind #2 bursts** at 36–106× the median 30-49 Hz envelope. 🛑 **DO NOT apply V65's pre-committed "all four quiet ⇒ NOT another lane gain" clause to grind #2** — that branch tested the RATCHET as a rail-to-rail limit cycle, and grind #2's attribution rests on an on-car **dose-response on exactly a lane gain**. An intervention outranks an inference from a different hypothesis. What it DOES close: the ratchet cannot be amplitude-saturated *at the aggregator*, and the `0xD2AEC` breakpoint lever loses its clipping rationale. ⚠ 100 Hz sampling a ~43 Hz burst is stroboscopic — quote the route-wide ±8192 null, never the 54 as a rate. ✅ V65 flight-clean, **ST==4 = 0** both routes (recounted off raw CAN) ⇒ zero-EME streak extends.
- ★★★ [gp-0x6806 IS the LKAS gate, validated on-car](accord/firmware/accord-gp6806-is-the-lkas-gate-validated-on-car.md) — V57's probe measured it in July and nobody correlated it. **99.90–99.94% agreement with `carControl.latActive`** over **37,914 frames** on two routes at very different duty (21.7%/49.9%), and **0.03–0.05 transitions/s**. ✅ It IS "LKAS is applying"; ✅ it does NOT drop out during steady engaged holding (the one ambiguity static analysis could not close — the ramp-FSM "settled" phases simply do not occur mid-drive); ✅ the parametric-pump kill criterion passes by **three orders of magnitude**. ⇒ V67's gate is closed BEFORE the flash. 🛑 The lesson is where to look: the answer sat in `_scratch/cache/r28`/`_scratch/cache/r29` since July while every session treated it as an open Ghidra question. **Check whether a past probe already flew on a signal before tracing it.**
- ★★★★ [V67 FLEW: both grinds fixed, and the highway symptom is NOT the rate lane](accord/builds/accord-v67-flew-both-grinds-fixed.md) — route `47`, 150,327 frames. Probe live: byte4 = {0x87,0xC7}, **bit6 == latActive 99.983%**, bit5/bit4 = **0 in every frame** ⇒ clean binary arm. **Grind #1 fixed with a WITHIN-ROUTE gate A/B**: engaged arm **0.524 [0.337, 0.804]** vs Kd=1, disengaged arm **1.055 [0.669, 1.354]** ⇒ suppression in ONE arm only (also the first evidence ever separating V66 from V67). **Creep grind #2 ELIMINATED**: 0 bursts vs Kd=2's 24, max 84 vs 1831 — 🛑 manual arm solid (P(0)=0.020), **engaged arm UNRESOLVED (22 s, P(0)=0.35)**. 🛑🛑 **The 2.44×-at-highway prediction was REFUTED**: three-dose highway null — 40–49 Hz **0.970 [0.787, 1.154]** and **0.938 [0.764, 1.184]** vs null [0.73, 1.37], manoeuvre-conditioned 0.999 / 0.884, and the **corpus-max highway envelope (851.5) is on the STOCK Kd=1.00 lane**. ✅ Positive control: 18–22 Hz IS suppressed at highway on Kd=2 (**0.509 [0.39, 0.92]**). ⚠ 10–16 Hz is **wheel order 1**, not a dose effect. 🛑 The ratchet lead is **CLOSED negative** (f0 9.13 Hz, moves to 11.13 in cruise, 15 Hz lock absent). ✅✅ **The microphone — no frequency ceiling, validated 4.14× on grind #2 — also sees nothing** (1.067×; the stock-Kd route r2b gives 1.071×), bounding the highway event at **≤~9% of grind #2's absolute acoustic amplitude**. Flight-clean, `ST==4` 0/150,327. ⇒ **KEEP V67; no control change supported.**
- ★★★ [r24's gain is a two-axis SPEED × RATE surface — and the units were got WRONG once](accord/calibration/accord-r24-gain-is-a-speed-rate-surface.md) — cross axis `0xC6010` = 0/10/50/100 km/h, inner axis `gp-0x6ac0`. 🛑🛑 **RETRACTED the same night: “bus = 8 × deg/s”, “the rate axis is dead” and “V67's arm delivers 1.94×” were ALL WRONG.** Settled two ways: regressing `rate_c` on the differentiated ANGLE channel gives slope **0.95–1.00, r ≥ 0.985** ⇒ the bus field IS deg/s; and at 4.7121 counts/deg-s the breakpoints are **85/297/637 deg/s**, which real driving reaches (max **521 deg/s** over 407,617 frames), whereas the wrong scale puts them at 679/2377/5093 where Honda's rolloff could never engage. ⇒ **V67's build note was CORRECT (LERP 2622 ⇒ exactly 2.00×)**, and the rate axis IS usable: grind #1 ~603, creep grind #2 ~1206 (both on the `[400,1400]` rolloff), highway ~141–198 (X1 = `0x0190` exactly, and Y0 == Y1 in every curve **except** mode-10's 50 km/h record `0xD2AEC` (2305 vs 2304 — a +1 rounding artifact, 0.04%, behaviourally nil, but an exact equality test breaks on it)). **The error was composing two unverified structural relations into a scale instead of measuring it.** ★ **Design A** — one halfword, `0xD2ABC` 2561→7051 — gives grind #1 **2.00×**, creep grind #2 **1.22×**, highway **1.00×**; blast radius clean two ways, no float mirror, CRC block #41 only. Not recommended while V67 already has both at zero.
- 🛑🛑 [Every VIBRATION instrument is blind above ~50 Hz — but the MICROPHONE is not](accord/instruments/accord-both-instruments-blind-above-50hz.md) — CAN **100.000 Hz exactly** (Nyquist **50.00**), comma IMU **101.02 Hz** (Nyquist **50.51**, settled by a lattice fit 77 µs vs 2889 µs and a synthetic fold test where **7/7 tones fold per 101.02**; the earlier 99.9–100.5 came from the dt *mean*, which ~1% dropped samples inflate). ★ Headroom is the **wrong quantity** for the alias: the discriminant is a **1.021 Hz** apparent-peak difference and the measured sem is **0.856** where ≪0.34 is needed ⇒ needs a log at a different IMU ODR (208/416 Hz). ★★ **The MICROPHONE has no ~50 Hz ceiling** (`soundPressure` — ⚠ **corrected 2026-08-03**: one RMS over 1600 samples of 16 kHz PCM ⇒ **0–8000 Hz analysed**, level at 10.000 Hz; this line previously said "audio at 16–48 kHz", which makes the channel look more capable than it is and causes its null to be **over-weighted** — the **26.4 dB** bandwidth-penalty argument depends on the band being 0–8 kHz) and a **working positive control**: creep grind #2 reads **4.14×** un-weighted p95 and **+9.7 dB(A)** burst-vs-quiet. ★★ It is also no longer the only above-50 Hz instrument — `gp-0x671a`'s input is a band-pass **peaking at ~61 Hz**, see [[accord-state671a-is-an-oscillation-detector]]. 🛑 A-weighting is the trap (−30 dB at 50 Hz) — use the **un-weighted** channel; *un-weighted up with A-weighted flat* IS the low-frequency signature.
- 🛑 [Check the data exists before concluding it doesn't](feedback/measurement/feedback-check-the-data-exists-before-concluding-it-doesnt.md) — two sessions asserted "no Kd=1 highway baseline exists"; route `2b` had **227 s** of it, and finding it refuted a confident, arithmetically-correct prediction. A scoping claim inherited from a previous session is **not evidence** — re-derive it. Same failure as V57's probe sitting unread for a month. **When your own arithmetic makes a tidy story with the operator's report, look HARDER for the disconfirming dataset.**

- 🛑 [No speed/torque byte exists to gate on, and the >50 Hz probe is dead at the cave site](accord/signals/accord-no-speed-byte-exists-in-this-firmware.md) — two search passes: every candidate is multi-valued, inline-only, standstill-only, dead (`0xC62EA`=0 since V53), or answers the same “LKAS applying” question V67 already found insufficient. Architectural reason: this firmware's idiom for speed is **“always LERP, never threshold-and-latch”**. 🛑 **Do NOT repoint the mask arm `gp-0x671d`** — a rising-edge counter driving **DTC 0x5e**, read by **8 functions** incl. 4 reads in the motor-off dispatcher `FUN_0003d4a2`; unlike the dead `gp-0x683c` it is a LIVE fault response. 🛑🛑 The hook `0x55C0E` runs at **100 Hz (task 5)**, NOT 1 kHz, and **no stock writer ever clears bits 7:3** of `gp-0x1514` (8 accesses, all masked RMW) ⇒ a sticky HF latch could never clear and the cave cannot see 1 kHz content at all. ⚠ **`gp-0x67ac` OPEN**: when it is 1, r24/r26 drop out of the aggregate entirely, regardless of gain arm.
- 🛑 [A hardcoded SEGS_2B list hid the only Kd=1 highway baseline for three sessions](accord/instruments/accord-segs2b-bug-hid-the-kd1-highway-baseline.md) — `_r31_common.SEGS_2B` **and** `_r37_ratchet_lib.ROUTES` both enumerate route `2b` as `[0,1,2,11,12,13]`, but it has **14 segments and 3–10 are the highway leg** = **227 s of Kd=1.00 highway**, the only stock-rate-lane highway exposure in the corpus. That absence is what let a confident wrong prediction stand unrefuted. **A hardcoded subset in a helper module is invisible to every analysis that imports it** — the code runs and the numbers look reasonable. Count exposure from the caches, not through a route table: `studies/sessions/r47/r47_orchestrator_checks.py exposure`.
- ⚠ [Two measurement-instrument corrections that move published frequencies](accord/instruments/accord-segs2b-bug-hid-the-kd1-highway-baseline.md) — **`fs_of()` is biased +0.5–1.4% route-dependently**; the true `0x14A` rate is **100.000 Hz on every route**, so grind #2's long-quoted "44.9 Hz" is **44.6 Hz** and the between-route frequency spread was the instrument, not the car. And **`0x18F`'s opendbc factor 0.1 is wrong — it should be 0.125** (`rate_f/rate_c` measured 0.756–0.799 ≈ 0.80 on four routes). Analyses using `rate_c` alone are unaffected.
- 🛑 **SUPERSEDED 2026-08-03 by [there is NO line at all in 30–49.5 Hz at highway](accord/mechanism/accord-highway-30-49hz-has-no-line.md)** — the order-3 half of the line below is an **estimator tautology**: `order = f0·CIRC/v` returns ≈3.00 whenever a band-limited argmax sits near the centre of 30–49.5 Hz at the corpus's median ~28 m/s, *whatever the spectrum contains*. Replaced by: peak of the **averaged** periodogram reads prominence **1.23–3.83** across 30–49.5 Hz on every route, build and channel, against the kit's **>4** criterion — no line, so not order 3 either. ⚠ **The 10–16 Hz order-1 half STANDS** (prominence up to **79**, order 1.00–1.02 in every speed bin), and so does the general warning. Original claim, kept visible:
- 🛑 [At highway, 40–49 Hz is WHEEL ORDER 3 and 10–16 Hz is ORDER 1](accord/builds/accord-v67-flew-both-grinds-fixed.md) — measured per-window order p50 **2.994** and **1.995** (n>600); at 30.8 m/s order 3 = **44.3 Hz, one bin from grind #2**. ⇒ **anyone peak-finding 40–49 Hz on a highway log will "find grind #2" and it will be a tyre.** The bursts themselves are NOT the order (on/off-order power ratio 6.94 quiet → **0.82 in bursts**). Order 1 explains the only band that reads outside the null in the three-dose table (10–16 Hz at 1.55/1.50) — tyre balance between drives months apart, **not** a dose effect. Second time this kit has nearly published a wheel order as a firmware effect.
- 🛑 [Ground EVERY session in the golden model AND the post-V38 arc](feedback/builds/feedback_ground_sessions_in_golden_model_and_post_v38_arc.md) — operator instruction 2026-08-03; `model/eps_lkas_chain_model.py` end-to-end **plus** the whole V38→present record read as one arc, not the latest handoff. Prime every subagent with both. Direction matters: V39/V42/V61 all pushed the rate lane DOWN, the gradient pointed UP.

- 🛑🛑 [There is NO spectral line at all in 30–49.5 Hz at highway — order 3 RETIRED](accord/mechanism/accord-highway-30-49hz-has-no-line.md) — the recorded *"40–49 Hz is wheel order 3, per-window order p50 **2.994**"* is an **arithmetic tautology**: `order = f0·CIRC/v` returns ≈3.00 whenever a band-limited argmax sits near the centre of 30–49.5 Hz at ~28 m/s. Replaced by the **averaged**-periodogram peak: prominence **1.32–3.83** (bar), **1.23–2.13** (`ay`), **1.26–1.76** (`gz`) against the kit's **>4** criterion — every route, every build, every channel. ✅ The instrument WOULD see a line: the 8–30 Hz positive control returns wheel order 1 at **prominence 15–79**, free-order fit **1.07**, slope **+0.4836 [+0.4806, +0.4863]** vs order 1's 0.4808. Event `f0` vs speed **+0.1139 [−0.0539, +0.3072]** excludes orders 1/2/3/4; on/off-order power **7.24 quiet → 1.97 at event peaks**. ⚠ **10–16 Hz order 1 still stands.**

- 🛑🛑★★★★★ [`0xC6AE6` is ONE KNOT of a FLAT four-knot Kd LERP, not a scalar gain](accord/calibration/accord-kd-is-one-knot-of-a-flat-lerp.md) — X=(50,400,1500,3000)@`0xC6ADE/E0/E2/E4` vs Y=(Y0..Y3)@`0xC6AE6/E8/EA/EC`, **all four 2048 in stock**, so the LERP is a CONSTANT. ⭐ **A one-knot edit converts a constant into a rate-dependent NONLINEARITY — it is not a gain cut.** `0xC6ADE` is **X[0], not a gate cal**. Killed V110 independently of the sign and **closes the Kd lever entirely** (+0.039 ratchet benefit vs +0.115 / +0.153 cost in the grinding bands). 🛑 **GATE 1 lesson: counting ACCESSES cannot tell a scalar from a table knot — read the READER'S STRUCTURE.**

- 🛑🛑★★★★★ [The low-speed steering-rate limit is **openpilot's `STEER_MAX` = 4096**, not the firmware](accord/mechanism/accord-low-speed-rate-limit-is-openpilot-steer-max.md) — hard clamp proven by the histogram (smooth decay 832/903/394/183/112/34/25/3 then **13,783 at exactly 4096**; zero frames above it in ~200 caches). Railed **40.4 % / 37.0 % / 21.5 %** of engaged time at <6 / 6-10 / 10-15 mph on r77, ~0 % above 30. ⭐ **The response is still CLIMBING at the rail** (p90 93.9 → 143.6) and the **driver slews the same rack 3-4× faster** (p90 103-162, max 402-459 °/s) ⇒ plant headroom exists, openpilot ran out of command. 🛑 **No firmware cal can raise it** — goal #5 vs goals #1-3 are in direct tension and the binding constraint is OUTSIDE the firmware. Explains why the symptom survived every build.
- ★★★★ [The `gp-0x69b0` authority ramp — all five rates, and a correction](accord/mechanism/accord-the-authority-ramp-five-rates.md) — `0xC63F4/F6/F8/FA/FC` = 328/16/33/66/328 (2 up, 3 down), all stock and virgin, fully mapped from `FUN_00028ea6`. 🛑 **`gp-0x6803` is a MODE fork, NOT left/right** — 3 values, two parallel SM chains; the record's "10× LEFT/RIGHT asymmetry" is a mis-identification. ✅ **Measured null:** STEER_STATUS identically 0 over 3,312 s engaged, 0 transitions, control passes ⇒ the ramp holds at full scale and is RETIRED as a rate-limit candidate.

- 🛑🛑⚠ [RETRACTED — "the gain stops delivering at low speed" does NOT survive speed-matching](accord/mechanism/accord-gain-stops-delivering-at-low-speed-high-command.md) — hands-off, the 4×→6× step — with **2 mph speed cells** the low-speed ratio is **1.292 [0.925, 1.673]** against an ideal 1.500 (**1.500 is INSIDE**), and the low/high contrast **0.711 [0.451, 1.032]** contains 1. The earlier 1.030 was an artifact of unmatched speed inside a wide bin (median 6.2 vs 8.3 mph). **UNDERPOWERED, not refuted** — the 6× arm carries 5–15 s per cell. 🛑 **RULE: match speed in cells ≤2 mph; a `<15 mph` bin is NOT a speed control.** 🛑 **The first pass omitted the HANDS-OFF mask and returned a WRONG stronger claim** ("scales nowhere") — at low speed the driver's torque swamps LKAS. `0xC520C` and the forward clamps `0xC61B2/B4` are both EXCLUDED (the clamps scale exactly 512/2048/3072/4096). **The saturating element is still unidentified.**
- 🛑🛑★★★★★ [Ratchet AND grind are COMMAND-GATED — one saturating nonlinearity, two bands](accord/mechanism/accord-ratchet-and-grind-are-command-gated-saturation.md) — 6–9 Hz band SHAPE rises **3.0× / 4.7× / 52.0×** with command while **3–5 Hz and 10–13 Hz FALL to 0.6–0.7×** and 14–18 Hz is flat; 20–26 Hz grind rides the same gate (4.0×/5.7×/11.8×). ⭐ **A confound lifts every band; two bands go DOWN.** Same regime as the gain failure ⇒ **[BELIEF] one mechanism.** 🛑 **Sixty builds hunted a LINEAR lever for a COMMAND-TRIGGERED nonlinearity — a linear lever cannot fix a relay.**

- 🛑🛑★★★★★ [THE COULOMB RELAY IS LOCATED — `0xC40BC` is its KNEE](accord/mechanism/accord-the-coulomb-relay-is-located-c40bc-is-its-knee.md) — `FUN_0003b8f6` @`0x3B8F6`: `fVar13 = clamp(POL*gp-0x6abc*12 / cal(0xC40BC), -1, +1)` times a `|model|`-proportional magnitude. **The kit has named this relay since V80 without ever pointing at the instruction.** Saturates at `|gp-0x6abc| >= knee/12` — 300→25, 600→50. 🛑 **600 is STOCK**; V99 halved it and it stayed halved for NINE builds (V99–V107), so **V108 RESTORED Honda's value** and raising ABOVE 600 exceeds Honda. ⭐ Its two factors map onto the two axes of the command-gate measurement (magnitude←command, shape←rate). ⚠ **Raising the knee REDUCES ASSIST** (clamp is monotonically decreasing in the knee × verified friction polarity) ⇒ trades against the 6× goal. ✅ **The scale IS now verified** — `FUN_00041464` writes `gp-0x6abc <- gp-0x4f50` raw while `gp-0x6ac0 <- |EMA(gp-0x4f50×1024)|>>10`, i.e. the SAME quantity, so the 4.7121 ct/(°/s) scale transfers: **the corner is 10.6 °/s, inside the 8–20 °/s band the ratchet was isolated in.** ✅ **GATE 1 and GATE 2 both CLOSED**; the trade is priced at **~1.28:1 against** (vs Kd's 2.9–4.4:1).

- ✅★★★★ [V111 BUILT — the relay probe: 3 bytes, no cave edit, no dose](../../docs/STATE.md) — `0x55DF2 d4 93→44 95` re-aims the CAN-427 magnitude tap from `gp-0x6c2c` to **`gp-0x6abc`, the Coulomb relay's INPUT**, + `0x55E10 a5→a3` (sar 3, peak 913/1023, 0.340 °/s per count). image `9c4865cf…` .rwd `221d99c6…`, 36/36, BASE = V109. **Every dynamics cell and the whole 164-byte cave asserted byte-identical to V109** ⇒ outside the bricking class. Gives the saturation duty at ANY candidate knee post-hoc; `(wire≥31) AND NOT(≥125)` is exactly the population a 600→2400 raise would affect. 🛑 **FLY V109 FIRST** — re-pointing the tap costs the `gp-0x6b26` Y-row solve V108's E5 was added for.

- ⚠★★★★ [SUPERSEDED FRAMING — it is **BROADBAND engaged noise**, not a ~100 Hz mode](accord/mechanism/accord-the-100hz-mode-is-ours-and-engagement-gated.md) — the operator’s reported low-speed modes are **above every CAN Nyquist** (angle 50 Hz, 427 24.9 Hz) ⇒ audio is the only instrument. Within-drive engaged-minus-manual <10 mph at the 100 Hz third-octave: **STOCK −0.03 dB** vs **+1.30 to +5.62 dB on every gain-modified build** ⇒ the mode is ours. ⚠ **Command dependence is a weak null** (−2.0 to +6.0 dB, no monotone rise, stock scatters the same) ⇒ **a DIFFERENT mechanism from the command-gated 7.8 / 20–26 Hz pair — do not assume one fix covers both.** ⭐ **V109’s α2 cut is aimed squarely at it** (−34 % at 100 Hz, −39 % at 200 Hz).

- 🛑⚠★★★ [QUALIFIED — a harmonic comb is real on V107, but **STOCK has one too**](accord/mechanism/accord-the-lowspeed-grind-is-an-83hz-harmonic-series.md) — V107 route 1e, engaged-minus-manual matched-speed <10 mph hands-off: comb fit **f0 = 83.50 Hz**, five harmonics (82/172/254/328/414 Hz), score **+1.682 dB** against an engaged-vs-engaged null **with the grid search inside it** (p95 +0.506, max +0.589, 200 draws) ⇒ **p = 0.000**. ⭐ **EVEN AND ODD harmonics ⇒ an ASYMMETRIC periodic process, NOT a symmetric relay.** ⚠ The instrument (`r1e_spec.npz`, 26,638×513 bins at 3.91 Hz) **already existed and was overlooked** — named `_spec`, not `_audio`. ⭐⭐ **Gives V109 a PRE-REGISTERED endpoint**: comb score drops ⇒ the `gp-0x6b26` lane feeds it and α2/knee is the lever; unchanged ⇒ it does not and α2 is dead here. 🛑 **The V109 drive MUST capture audio.**
  ↳ 🛑 **QUALIFIED WITHIN THE HOUR.** The same test across the ladder: STOCK **+1.457** (p=0.000), V107 **+1.682**, V100 +2.494, V106 +2.675, V101 +3.513 — **stock fires, and the scores do NOT order by gain.** ⇒ **"an engaged comb" is not by itself ours, and it is not shown to be the grinding.** ⚠ But stock’s comb sits at **49.5 Hz with a NULL 2× (-0.14 dB at 99 Hz)**, matching the third-octave stock reading (-0.03 dB at 100 Hz) — so **the narrower claim that the ~100 Hz content is ours SURVIVES**; it is the “83.5 Hz comb is ours” framing that does not. Also: `f0` suffers a **SUB-HARMONIC AMBIGUITY** (a comb at `f0` always scores at `f0/2`), so 83.5 Hz is not a frequency identification. ✅ What survives: the comb is real above its own null, and the V109 endpoint holds **only as a within-build before/after**. ⭐ **RULE: when a claim’s force is “this is OURS”, build the STOCK arm BEFORE publishing.**
  ↳ 🛑 **ELEVEN-ROUTE LADDER, fixed 90–110 Hz band, three control bands.** ✅ **STOCK is the ONLY route that fails its null** (−0.30 dB, p=0.890) while **9 of 10 gain-modified builds clear theirs at p<0.001** ⇒ **the engaged acoustic excess IS ours**, and the broadband level ladders (1× −0.04 · 4× 0.84 · 6× 1.13 · 8× 2.24 dB). ❌ **But the control bands rise equally** — after removing them the 100 Hz residual is ≤0 on 6 of 10 routes ⇒ **BROADBAND, not a mode.** ⭐ **LESSON: a narrow-band acoustic claim needs ADJACENT CONTROL BANDS; the third-octave caches cannot provide them and I published on them twice.**

- 🛑⭐★★★★★ [THE ACOUSTIC COST OF THE GAIN IS MEASURED — +1.16 dB from 4× to 6×](accord/mechanism/accord-the-acoustic-cost-of-the-gain-is-measured.md) — eleven-route spectrogram ladder with a STOCK arm. Statistic = **MECH (60–400 Hz) minus FAR (1200–2000 Hz)**, engaged-minus-manual, matched speed <10 mph, hands-off, within drive. 🛑 **FAR is NOT optional** — it rises too (+0.74 stock, +1.16 at 4×), proving every single-band claim on this corpus is confounded. **6× − 4× = +1.158 dB [+0.475, +1.817], P(>0)=1.000**, 8 of 9 routes outside their own null; ordering stock ≈ 4× < 6× < 8×. ⇒ **the 6× costs ~1.2 dB of steering-band noise — goals #1 and #4 are in tension THROUGH THE GAIN, now numerically.** ⊕ Agrees with `accord-the-8x-gain-is-the-carrier`, reached from a totally different instrument. ❌ **Supersedes three narrow-band framings that all died to adjacent-band controls** (≈100 Hz mode, 83.5 Hz comb, PMSM ripple). ⭐ **RULE: a narrow-band acoustic claim needs ADJACENT CONTROL BANDS, and an “it is ours” claim needs the STOCK arm BEFORE publishing.**
  ↳ ✅ **AND IT IS NOT COMMAND-MODULATED.** Within-ENGAGED high-cmd vs low-cmd (no engaged/manual confound at all): +0.81 stock · +0.21/+2.40/−0.72 at 4× · −0.13/−0.41/+1.31/−2.42 at 6× — **signs both ways, stock in range, no ordering**, 7 of 8 outside their own null (so real between-drive variation, not noise). ⇒ **a LEVEL SHIFT TIED TO THE GAIN.** ⭐⭐ **THE TWO SYMPTOM FAMILIES SEPARATE:** the 6–9/20–26 Hz steering-rate pair IS command-gated (48× at matched rate) and has a candidate lever; the 60–400 Hz acoustic band is NOT and **has no lever but the gain.** 🛑 **V111 is aimed at the ratchet family and must not be expected to inform the acoustic one.**
  ↳ ✅ **GATE 2 DONE.** The knee is an **odd, memoryless saturation** ⇒ its describing function is REAL, it adds **ZERO phase**, and **GATE 2 does not block it**. 🛑 **But the lane REVERSES SIGN between 16–18 and 18–22 Hz** (EMA `cal(0xC40D0)`=408, corner 15.9 Hz, rotated through the measured `arg Z`), so a magnitude-only knee raise **necessarily helps one symptom family and hurts the other**. Priced: benefit 0.784 at 6–9 vs cost 1.004 over 18–31 ⇒ **≈1.28:1 AGAINST — far better than Kd’s 2.9–4.4:1, which is why Kd is dead and this is not.** ⇒ **a genuine TRADE the operator must choose, not a fix.** ⭐ Also: **`0xC4080` = 0** ⇒ no Coulomb floor, the term vanishes with no command — *command-proportional* confirmed at the instruction level.
  ↳ 🛑 **KNEE CORRECTION: `0xC40BC` STOCK IS 600, NOT 300.** V99 halved it to 300 and it stayed there NINE builds (V99–V107); **V108 RESTORED Honda’s value.** ⇒ V108’s “best it’s ever been at ≥20 mph” has a candidate cause that is a REVERT; and raising above 600 **EXCEEDS Honda’s setting**, a materially bigger step than the earlier framing implied.

- 🛑🛑★★★★★ [DO NOT BUY THE RATCHET WITH MASS OR FRICTION — standing directive](feedback/builds/feedback-do-not-buy-ratchet-with-mass-and-friction.md) — operator after V111: *"Increasing mass and friction should not be our primary approach… We want both: low apparent steering mass and friction to LKAS AND no ratcheting (feedback from driver torque sensor)."* ⭐ **Well-posed, because the requirements live on DIFFERENT PATHS:** MOTION-fed lanes (`gp-0x6b26`, `gp-0x6bbe`, the base-assist damper) oppose ALL motion ⇒ **cap max angular velocity by construction**; TORQUE-fed lanes (the `FUN_0003a382` PID, the observer) **do not load the LKAS path.** 🛑 **The ratchet lever must be TORQUE-PATH.** Retires further `gp-0x6b26` dosing, and ⚠ **inverts the relay-knee reasoning** — raising the knee cuts friction COMPENSATION, so it feels HEAVIER.
- ★★★★★ [V111 FLEW — α2 is the ONLY delta, a clean single-variable experiment](accord/builds/accord-v111-flew-alpha2-is-the-only-delta.md) — 3 payload bytes vs V108, 2 of them telemetry ⇒ **`0xC40DC` 22→14 caused everything.** Grinding *"most of it resolved"* (grind #1 remains at 5–10 mph under strong command, higher-pitched but muted), **oscillations gone**, ratcheting reduced — ⚠ **but max angular velocity and acceleration LOST.** ⭐ **Mechanism: EMA lag rotates the `gp-0x6b26` INERTIA term into FRICTION**, `×1.87` at 1–5 Hz while mass is `×1.000` ⇒ his *"mass and friction"* is almost entirely FRICTION, which is what caps velocity. 🛑 **MAGNITUDE UNVERIFIED** (term ≤ 2.6 % of residual range; and less railing points the other way). ✅ **Route `21`, 18 segments uncached, may be the V111 drive — registering it is the highest-value action available.**

- ⭐⭐★★★★★ [V112 BUILT — the first lever that satisfies the both-at-once directive](accord/mechanism/accord-knee-and-k1-decouple-lightness-from-relayness.md) — `0xC40BC` 600→1800 AND `0xC40D2` 204→612, scaled TOGETHER so `gain=(K1/1024)(12/knee)` is held **EXACTLY** (0.0039844 both) while the saturation moves 10.6→31.8 °/s. ⇒ **bit-identical below 10.6 °/s**, and it adds **no impedance** (a feed-forward friction COMPENSATION, not a damper) ⇒ cannot cap max angular velocity. ⭐⭐ **MEASURED on route 21 = the V111 drive: the relay is SATURATED 74.4 % [0.669,0.815] of the time at 5–10 mph engaged hands-off cmd≥2048** — exactly the regime he named; V112 cuts it to 23.5 %. ✅ GATE 1 one reader each; ✅ GATE 2 memoryless ⇒ **zero phase**, DF bounded by the unchanged small-signal gain; ✅ **clamp objection DEAD** (`|model|≤0.4851` ⇒ 34× headroom). image `f032878c…` .rwd `64f2ee9e…`, 37/37.

- 🛑🛑★★★★★ [THE RATE DEFICIT IS REAL, UNIVERSAL, AND **NOT** V111](accord/mechanism/accord-the-rate-deficit-is-real-universal-and-not-v111.md) — measured on **18 routes** against openpilot's TRUE demand channel: achieved/demanded = **0.73 / 0.63 / 0.47 / 0.30** at 5-15 / 15-30 / 30-60 / 60+ °/s, with **rail duty 5.9 / 16.9 / 32.0 / 49.8 %** ⇒ **openpilot is command-STARVED at STEER_MAX and still gets 30 % of the motion it asks for.** ⊕ the driver reaches **335 °/s** where LKAS reaches **84.6** ⇒ not the plant; no pile-up at 84.6 ⇒ not a hard clip. ⭐ **Route 21 (V111) = 0.24 vs corpus median 0.26 ⇒ V111 did NOT cause it, and the α2 revert will NOT fix it.** 🛑 **EXCLUDED by arithmetic:** 0xC520C (already struck), 0xC6202 governor (2506 < 4762), 0xC61B2/B4 arb clamp (2506 < 3072), a hard 84.6 °/s clip. ⭐ **Independently confirms V112** — its 10.6→31.8 °/s corner is exactly the band where ach/dem falls 0.63→0.47. 🛑 **2 RETRACTIONS**: the "compression vs command" result (demand confound) and "the car delivers 89-107 %" (used ct_curv = CURRENT ⇒ circular; the demand channels are **ct_dcurv / cc_curv**).

- 🛑🛑★★★★★ [THE RATE DEFICIT IS **UPSTREAM OF THE FIRMWARE** — and the same data locates the oscillation](accord/mechanism/accord-the-rate-deficit-is-upstream-of-the-firmware.md) — 15-route Welch H1: **demandRate→CMD −16.0 dB** by 1–2 Hz vs **demandRate→rate −12.1 dB** ⇒ the command is attenuated MORE than the motion, so the loss is in **openpilot's controller**, and the firmware **over-delivers** (**CMD→rate +1.2 dB, coh 0.51**). ⊕ demand episodes above 15 °/s last **0.030 s** vs the arbitration IIR's tau **0.0315 s** ⇒ predicted 61 % vs measured **0.63**; ⊕ openpilot's slew limiter is **122.88 ct/frame** (full scale in 0.33 s) ⇒ only **9 % of scale** reachable in a 30 ms episode. 🛑 **⇒ 'higher max steering angular velocity' is NOT deliverable by firmware** — STEER_MAX/STEER_DELTA are openpilot-side and off-limits. ⭐⭐ **BUT the same measurement locates the OSCILLATION as ours**: `CMD→rate` **RISES +6.1 dB at 5–8 Hz and +7.5 dB at 12–20 Hz** while the driver's path through the SAME plant **FALLS** (−13.6 / −10.4 dB) ⇒ same plant, two inputs, opposite slopes ⇒ **HF emphasis is in the LKAS path.** That is the tractable half.

- 🛑🛑★★★★★ [V113 BUILT — and it **WITHDRAWS V112**](accord/builds/accord-v113-built-knee-with-k1-held.md) — knee `0xC40BC` 600→1800 with **K1 HELD at 204**, so the friction term is **≤ V111's at EVERY rate** (exhaustive sweep, worst excess +0.000000). 🛑 **V112 SHOULD NOT BE FLOWN**: scaling both cells delivers up to **2.93× MORE anti-damping above 10.6 °/s** (DF of the odd saturation; the EMA adds only −1.1° to −11.1° so the term is in phase with RATE) — **V94's failure mode**, and V94 was the drive the operator aborted as unsafe. ⊕ **real Coulomb friction IS constant-magnitude** ⇒ the saturation was the model, not the bug. V113 cuts relay duty 0.7439→0.2353 AND the small-signal slope ×0.333. ⚠ **Cost: the wheel will feel HEAVIER than V111 below ~30 °/s.** 3-way discriminator on the next drive. image `d2e86f82…` .rwd `07d64f50…`, 39/39, 2 payload bytes.

- 🛑🛑★★★★★ [THE OSCILLATION IS **NOT COMMAND-DRIVEN**](accord/mechanism/accord-the-oscillation-is-not-command-driven.md) — 15 routes: **only 6.90 % of total rate power above 5 Hz is COHERENT with the LKAS command** (rate above 5 Hz is 50.95 % of all power ⇒ ~86 % of it is incoherent), and **20–30 Hz alone carries 36.0 % of ALL rate power at coherence 0.100.** 🛑 **Kills the entire command-side filter class** — independently reproducing the struck verdict on `0xC63EC/EE`; `Kd` separately closed (flat 4-knot LERP ⇒ one-knot edit is *worse than inert*). ⭐⭐ **A dominant band that is uncorrelated with the forward input = a CLOSED-LOOP instability** (cf. 9,200× less power LKAS-off) ⇒ **the lever is loop DAMPING, not input filtering** — and V106's ×3.0 damping dose is the kit's only measured success. 🛑🛑 **AND IT CONTRADICTS THE DIRECTIVE'S PREMISE**: the firmware over-delivers, the deficit is openpilot-side, and doubling the viscous term costs **1.4 % of authority at openpilot's own p90 demand (11 °/s)**. Re-opens the damping class — **ask him.**

- 🛑🛑★★★★★ [THE ANTI-DAMPING IS CENTRED AT **9–12 Hz**, not where the power is](accord/mechanism/accord-antidamping-is-centred-at-9-12hz-not-20-30.md) — `Re(Z)` over 17 route-arms: **the MANUAL arm is damped at every band on every route (+3…+19); engaging drives 6–16 Hz deeply negative**, minimum **−67 at 9–12 Hz** (route 21). 🛑 **CORRECTS the previous note**: 20–30 Hz holds 36 % of the POWER but `Re(Z)` there is only −3…−5 and crosses positive at **f0 ≈ 23.3 Hz (corpus p50, n=17)** ⇒ **20–30 Hz is where it RINGS; 6–16 Hz is where energy is PUT IN. Size damping on 6–16 Hz.** ⭐⭐ `gp-0x6b26` measures **+518/+565 counts of POSITIVE Re(Z) at 6–9 Hz** — inside the deepest band — which explains V94 (removed it ⇒ unsafe) and V106 (tripled it ⇒ the kit's only clean band-power win). ⊕ Estimator validated: −43 × 57.3 = −2464 vs the published −3375/−3176/−3073.

- ⭐⭐★★★★★ [V114 — **ONE BYTE that raises damping AND lowers mass**](accord/builds/accord-v114-alpha2-rotates-mass-into-damping.md) — `0xC40DC` α2 **14→8**, 1 payload byte, 42/42. ⭐⭐ **α2 MOVES the `gp-0x6b26` bandpass rather than SCALING it** (peak 46.5→34.2 Hz), so splitting against the velocity phasor gives **6–16 Hz DAMPING ×1.252 while 6–16 Hz MASS ×0.796** — the first lever to satisfy the both-at-once directive from a single cell. Targets the band `Re(Z)` measures at **−33…−67**. ✅ **Cannot repeat V107**: peak |H| ×0.669, broadband ×0.604, 100 Hz 7.13→4.05 — **every magnitude falls**, so rail duty must fall. ✅ **GATE 1 is the cleanest in the kit** — exactly ONE access image-wide (`0x41626 ld.hu 0x50dc,tp,r11`), zero writers. Dose 8 not 6/4 because 21–27 Hz is where V106's win lives (−10 % give-back vs −23 %). image `8c4f53cc…` .rwd `26d2a6c1…`.

- 🛑🛑★★★★★ [V112 FLEW — BEST BUILD YET, and my withdrawal of it was WRONG](accord/builds/accord-v112-flew-best-yet-and-the-peak-turn-oscillation.md) — routes 22/23, fault-free. Operator: *"best firmware ever on 6x torque… smoothest… least ratcheting"*. 🛑 **The describing-function argument that withdrew V112 is REFUTED on-car** ⇒ **V113 is deprioritised** (it solved a problem that did not materialise). ⭐⭐ **THE PEAK-TURN OSCILLATION IS LOCATED**: route 23 t=445.6–448.2 s, **7.42 Hz**, 6–9 Hz RATE **16.86 °/s vs a corpus p99 of 3.98** — the corpus max on two sensors. 🛑 **Rail NOT causal** (0.76× [0.22,1.49]); **command magnitude gates it and saturates near 1500 ct** (19→125 p50); **driver torque has no reliable effect** (0.79× [0.67,1.01]). 🛑🛑 **METHOD RETRACTION the operator caught**: measuring the oscillation from `cs_tq` AND splitting by `cs_tq`'s rolling median gave a bogus 0.17×; on `cs_rate` it is 0.79×. ⊕ **`cs_press` is itself a ~1200-ct torque threshold** ⇒ **no channel in this kit can see LIGHT HANDS** — every hands-on/off claim here is really high-vs-low torque. ⇒ **V114 is the recommended next flight.**

- ⭐⭐★★★★★ [V115 — V112 (flown, best yet) + ONE byte; **the recommended next flight**](accord/builds/accord-v112-flew-best-yet-and-the-peak-turn-oscillation.md) — `0xC40DC` α2 **14→8** on the **V112** base, knee 1800 / K1 612 HELD, 1 payload byte, 42/42. **V114 is SUPERSEDED (V111 base, built before the V112 flight data); V113 is DEPRIORITISED.** ⭐ V112 measurably improved authority: ach/dem at 5–15 °/s **0.487 (V111) → 0.669 / 0.791 (V112)**. image `5f804a8a…` .rwd `f1a47bb7…`.

- 🛑🛑★★★★★ [THE ANTI-DAMPING IS A **STATE EFFECT OF ENGAGING**, and the damper lane is EXHAUSTED](accord/mechanism/accord-antidamping-is-a-state-effect-of-engaging.md) — at **|cmd| < 512** the 9–12 Hz `Re(Z)` is **−59 / −60 / −67** against MANUAL **+7** ⇒ **engaging alone flips the sign with no command**, which rules out the relay, the 6× gain and every command-proportional lane. ⭐ Prime suspect: the **mode index 24→26/27**, and in STOCK those records are byte-identical ⇒ **every engaged-vs-manual difference is OURS** — ⚠ but the `gp-0x6b26` damper is 3× LARGER engaged, which cuts the other way. **Open.** 🛑🛑 **AND THE DAMPER LANE IS EXHAUSTED**: feasible ceiling **+241 counts against a −2464 deficit (9.8 %)**; V115 gives **5.3 %**; closing it needs ×5.8 but **int16 caps the Y row at ×1.111** (already −29490/32767). ⇒ **V115 REDUCES the peak-turn oscillation, it does not eliminate it — do not oversell it**, and **do not spend the last ×1.111 on Y** (+2.4 %, four counts from the int16 edge, in V94/V107's cell).

- 🛑🛑★★★★★ [7.42 Hz IS **STOCK'S OWN MODE** — our Q is already LOWER than stock's](accord/mechanism/accord-the-742hz-mode-is-stocks-and-our-q-is-lower.md) — the peak-turn oscillation is **7.42 Hz**, exactly stock's ring-down f0 (zeta 0.0275–0.0321, Q 15.6–18.2); our builds measure **zeta 0.059–0.072, Q 7.0–8.5** ⇒ **we already added damping; the mode is OVER-EXCITED, not under-damped.** 🛑 **FOUR exciters tested and killed**: command rail (0.76×), driver grip (0.79×), command magnitude (saturates ~1500 ct), and **Coulomb relay switching — which looked convincing raw (corr +0.411, monotone 0.40→1.83, event at 15.6× baseline) but INVERTS to 0.14× [0.11,0.19] on a scale-free shape ratio: the relay threshold is defined on |rate| and the outcome is a rate quantity.** ⇒ **V116 should not be expected to fix the oscillation**, only grind #1. ⚠ **The exciter remains UNIDENTIFIED** — the open question is the **2.4–3.0× speed-dependent multiplication of Honda's own anti-damping**.

- 🛑🛑★★★★★ [The anti-damping multiplication is **~6× AT LOW SPEED**, falling with speed](accord/mechanism/accord-antidamping-multiplication-is-6x-at-low-speed.md) — measured against the cached **STOCK** arm (route 97): 🛑 **THE LOW-SPEED PEAK IS RETRACTED** — stock's 20–32 km/h bin is only **25 s**, and a 25 s window swings the estimate **1.6–3.8×**, so a 6–8× ratio on that denominator is unsupportable. **What survives: ~1.8–2.5× at 50–90 km/h**, agreeing with the published 2.39–2.60×. ✅ Closing it needs **stock data below 35 km/h**, which the corpus does not have. ⭐ **22–26 Hz is largely RECOVERED**: stock **+3…+8** (damped), V112 **~0** — no longer the sign-reversed −134/−99 of V102. ⊕ Full stock→V112 cal inventory: **81 runs**. 🛑 `gp-0x6b26` does NOT explain the shape — its speed knots are [0,20,90] km/h but its dose is ×3.00/×3.00/×8.14, **rising** with speed, the opposite. ⚠ Do not read `0xC6CD0` 3564→5346 as ×1.5 on stock — V57 REPOINTED the reader onto that cell.

- 🛑🛑★★★★★ [THE CORPUS CANNOT MAKE THE MEASUREMENT — drive card written](../../docs/scoring/DRIVE-CARD-manual-at-speed.md) — both routes to the remaining mechanism are blocked by the SAME gap: stock has **25 s** below 35 km/h, and **manual driving above 35 km/h is 174 s across the ENTIRE 22-route corpus**, spread over 10 routes and 3+ builds so it cannot be pooled. A 25 s window swings `Re(Z)` **1.6–3.8×** ⇒ neither denominator carries a ratio. 🛑 **No re-analysis fixes this.** ✅ The cheap unblock is a **no-flash data drive**: ≥150 s ENGAGED and ≥150 s MANUAL on the same stretch at 55–80 km/h, **hands resting lightly on BOTH arms** (every prior contrast compared engaged-hands-off vs manual-hands-on). ⭐ It tests a pre-registered prediction: `gp-0x6b26`'s Y row is the ONLY live mode-gated cell, knots 0/20/90 km/h at ×3.00/×3.00/×8.14 ⇒ **the engaged-minus-manual gap must GROW ~2.7× from 20→90 km/h**, or it is falsified.

- ⭐⭐★★★★★ [TWO SYMPTOMS, **TWO MECHANISMS** — the fine Re(Z) spectrum](accord/mechanism/accord-two-symptoms-two-mechanisms-rez-spectrum.md) — V112, routes 22+23, 0.5 Hz bins, **coherence 0.5–0.85**: ONE broad anti-damped feature **3.0–23.5 Hz, peak −81 at 8 Hz, f0 = 23.28 Hz** (matching the 17-route 23.29 Hz from a different method). 🛑 **The peak-turn oscillation (7.42 Hz) sits on its rising edge and IS this instability; grind #1's band (18–22 Hz) is −1 to −10 — essentially NEUTRAL — so grind #1 CANNOT be an Re(Z) instability.** ⇒ **stop treating them as one problem**: the oscillation needs positive Re(Z) at **8–12 Hz**; grind #1 needs a nonlinearity fix, and a damping lever cannot touch it. ⚠ A pure ~10.7 ms transport delay fits f0 but is **falsified by the shape** (it demands −81 at 3 Hz where the measurement is 0) — do not propose delay compensation on that coincidence. ⊕ `gp-0x6b26` is worth ~11 % of the deficit and α2 14→8 raises that ~2.8 % ⇒ **the lane is not the fix.**

- 🛑🛑★★★★★ [WE **ADD** A NEW 7–9 Hz ANTI-DAMPED FEATURE — above 12 Hz we are STOCK](accord/mechanism/accord-we-add-a-new-7to9hz-antidamped-feature.md) — same estimator on STOCK (route 97, 573 s) and V112 (routes 22+23, 809 s): **16–19 Hz ratio 1.06, 12–14 Hz 1.26 — essentially stock — but 8–9 Hz is 5.48×** (−14.1 → −77.2). **And the shape MOVES: stock's worst point 12.9 Hz at −60, ours 8.6 Hz at −90.** ⇒ **not a scaling of Honda's anti-damping — a NEW localized feature at 7–9 Hz, exactly where the peak-turn oscillation sits.** ⊕ f0 21.03→22.89 Hz reproduces the published 21.90→24.90 shift. ⭐ **Leading candidate, UNCONFIRMED: `0xC649B` 0→1 ARMS Honda's dormant biquad ENGAGED-ONLY** — the only live edit that is engagement-gated, command-independent AND frequency-localized; its coefficients are **byte-identical to stock**, so we switch on a filter Honda leaves off. ⚠ The filter's response is NOT computed (a guessed coefficient grouping gave nonsense). ✅ **One-byte falsification: `0xC649B` 1→0.**

- ★★★★ [Re(Z) is AMPLITUDE-DEPENDENT on STOCK too — six candidates eliminated](accord/mechanism/accord-rez-is-amplitude-dependent-on-stock-too.md) — Re(Z) at 7–12 Hz rises monotonically with rate amplitude on **both** arms (stock ×1.40, V112 ×1.23 at 7–9 Hz) ⇒ **the describing-function nonlinearity is HONDA'S**. 🛑 **But at MATCHED amplitude V112 is still 2.2× stock** (−25.5 vs −56.6), so it does not explain our excess. ✅ **ELIMINATED for the 7–9 Hz excess**: the command rail, driver grip, command magnitude, relay switching, the armed biquad, a gain law, and amplitude dependence — each with its own control. ✅ **SETTLED**: the excess is OURS (stock −13.1 outside the whole modified range −31.9…−74.8, 16 routes). ⚠ **The V57 gain repoint cannot be separated from 'being modified at all'** — every modified route carries it and stock is the only 1× point. ⊕ `0x13109`/`0x14120` are the ASCII **part-number string**, not code.

- ⭐★★★★ [V117 — DISARM THE BIQUAD, one reversible byte](accord/mechanism/accord-we-add-a-new-7to9hz-antidamped-feature.md) — `0xC649B` **1→0** on the V112 base, 41/41, all three biquad coefficients byte-identical. The filter is `y=0.81731x+1.53720y[-1]−0.63462y[-2]`, **DC gain 8.39** ⇒ a flat 8.4× through 7–12 Hz at 1 kHz, or a Q-2.46 resonator at 4.23 Hz at 100 Hz — **either way a large gain into the aggregator, and stock leaves it OFF.** Evidence: OFF median −37.7 vs ON −55.4 (**1.47×**), but **P=0.722, not separable, and the excess predates the biquad** ⇒ **this build IS the experiment.** image `ea5ad8d3…` .rwd `754f15a0…`.

- 🛑🛑★★★★★ [`0x454FE` **PERMANENTLY DELETES** Honda's state-4 governor call](accord/mechanism/accord-v42-permanently-deletes-hondas-state4-governor.md) — V42's `ba`→`b5` turns `bne` into an **unconditional `br`**, so `jarl FUN_00049A5A` at `0x45500` is **NEVER CALLED** on any build from V42 on. **Not a tuning change — a Honda control element deleted from the torque loop.** It satisfies every requirement the 7–9 Hz excess imposes: code in the governor, absent from stock, present in EVERY affected build, state-gated on `gp-0x67fa` (engagement-conditional, command-independent), and it REMOVES rather than scales. ⚠ **Do not blindly revert** — V42's change is a validated fix for the V38 macro ratchet, and **state-4 duty is UNMEASURED** (`gp-0x67fa` is not on the bus and no cached build telemeters it). ✅ **Probe first**: one cave rung on `gp-0x67fa == 4`, no dynamics change. ✅ **The 164-byte cave is ELIMINATED** — decoded with the correct opcode map, it reads 7 control signals and stores ONLY to gp-0x1514/gp-0x1512 (the CAN TX buffer) ⇒ GATE 1 clean.

- ⭐⭐★★★★★ [V118 — biquad disarm **+** the state-4 probe, one flight two answers](accord/mechanism/accord-v42-permanently-deletes-hondas-state4-governor.md) — `0xC649B` 1→0 (candidate fix) + 427 tap `gp-0x6ABC`→`gp-0x67FA` at `sar 0` (candidate diagnostic). 4 payload bytes, **NO CAVE EDIT**, 43/43. Probe: **state 4 = wire 20**; any `gp-0x67fb` contamination forces ≥1285 which **clips at 1023**, so it is self-identifying. Gives up the tap's rate, which is already on CAN as `cs_rate`. image `8a0f0080…` .rwd `92b798a1…`. **Supersedes V117 as the flight candidate.**

- ⭐⭐★★★★★ [THE MOD WORKS BY **DELETING HONDA'S LIMITERS** — the excess may be its PRICE](accord/mechanism/accord-the-mod-works-by-deleting-hondas-limiters.md) — **four** Honda limiters are disabled in every build showing the 7–9 Hz excess and none in stock: the **arbitration core's 3-way cascade** (`0xC61C0/C2/C4` 1600/896/1280→65535, so the limit-exceeded path at `0x29276` is NEVER taken), the **low-speed lockout** (`0xC62EA`→0), **`0xC64B4`**→0xFF, and **Honda's state-4 governor call** (`0x454FE` `bne`→`br`). ⭐ **This explains the non-ordering** — the excess does not track gain, build date or the biquad, which is what you expect if the cause is the shared SET of deletions rather than one tunable cell. ⇒ **the excess is plausibly the PRICE of the mod.** 🛑 A reframe, not a proof. ⚠ Predicts every restoration trades oscillation against authority; `0xC61C0`'s thresholds are 896–1600 of a 15360 scale ⇒ **expensive**. ✅ **Fly V118** — it tests the biquad and measures state-4 duty at once. 🛑 **Stop hunting for a single culprit cell**: eight candidates eliminated with their own controls.

- ✅★★★★ [V118 PRE-REGISTERED SCORING — written before the drive exists](../../docs/scoring/SCORING-V118-preregistered.md) — **identity**: the 427 wire becomes DISCRETE `{0,5,…75}`; if it is not, V118 is not on the car. ⊕ wire ≥1023 = `gp-0x67fb` contamination, **discard**. **§1 state-4 duty** (`state = wire/5`, **STATE 4 = WIRE 20**): ≥5 % engaged ⇒ `0x454FE` is the prime suspect and the fix is a MODIFIED restore of `FUN_00049A5A`, never a blind revert; **<1 % ⇒ ELIMINATED**. **§2 biquad**: primary = 7–9 Hz `Re(Z)` vs V112's own routes (−50.1, −74.8), **null = within-V118 split-half, quoted BEFORE the contrast**; inside the null ⇒ eliminated; more anti-damped ⇒ revert the byte. ⚠ **Corpus spread −31.9…−74.8 exceeds the effect sought** ⇒ a wide null means NOT RESOLVED, not "no effect". 🛑 **V118 is not expected to touch grind #1** (18–22 Hz `Re(Z)` is −1…−10, neutral).

- ⭐⭐⭐★★★★★ [V119 — **BOTH LEVERS + THE PROBE**, one flight](accord/mechanism/accord-the-mod-works-by-deleting-hondas-limiters.md) — knee **1800→2400** + K1 **612→816** (grind #1, predicted duty **0.0484** on a model that already predicted correctly) + `0xC649B` **1→0** (the oscillation) + the `gp-0x67FA` state probe. **8 payload bytes, NO CAVE EDIT, 42/42.** ⭐ **Two dynamics levers stay interpretable because the symptom separation is MEASURED**: 7.42 Hz sits on a −81 `Re(Z)` peak (linear instability) while 18–22 Hz is −1…−10 (neutral ⇒ nonlinearity) ⇒ the knee cannot fix the oscillation and the biquad cannot fix grind #1, so either report attributes itself. Gain held EXACTLY (0.0039844) ⇒ bit-identical below 31.8 °/s. Scoring pre-registered. image `a39801bc…` .rwd `18e3216f…`. ⚠ **Expect a reduction, not elimination.**

- ⭐★★★★ [`0xC64DE` is a **KNOT COUNT** — we read 14 knots where stock reads 9](accord/calibration/accord-c64de-extends-an-arbitration-table-9-to-14-knots.md) — `FUN_00028ea6` @`0x2976E` computes `n = cal/2 + 1`, so 17→27 takes a **speed-indexed** table in the **arbitration core** from **9 to 14 knots**. **8 readers, all in the arb core**, full scan. 🛑 **CORRECTED: the table address was WRONG (off-by-0x1000)** — `tp+0x7734` = **`0xC6734`**, not `0xC7734`; the whole "table data" analysis was of unrelated memory and is **VOID**. The real contents are **not monotone**, so the "longer axis" reading is unsupported and **`0xC64DE`'s meaning is OPEN again**. ✅ What the corrected dump DID find: `0xC674E`/`0xC6750` **1024→5120** and `0xC675A` **−1024→−5120** — the **DIRECTION CORRIDOR widened ×5** (the V29/V30/V31 cells), live on this car. Present in every build showing the 7–9 Hz excess, absent from stock. 🛑 **OPEN — do not revert it as a fix and do not dismiss it.**

- 🛑★★★★★ [HANDOFF 2026-08-28 — V112 flew, the two symptoms are SEPARATED, five builds on the shelf](../../docs/handoffs/2026-08/HANDOFF-2026-08-28-v112-flew-two-symptoms-separated.md) — carries **every** finding, **every** open item with what would close it, and **all eight retractions**. Headline: **V112 flew and is the best build yet** (authority tracking 1.37–1.62× better) **and I had wrongly withdrawn it**. ⭐⭐ **Two symptoms, two mechanisms**: 7.42 Hz on a −81 `Re(Z)` peak (linear instability; and 7.42 Hz is STOCK's own f0 with our ζ already DOUBLE stock's ⇒ **over-excited, not under-damped**) vs grind #1 at 18–22 Hz where `Re(Z)` is neutral (**a nonlinearity**). ⭐ **Thesis: the mod works by DELETING Honda's limiters** (four, plus the direction corridor ×5) ⇒ the excess may be its **price**. **Ten candidates eliminated with their own controls.** ✅ **V119 is the flight.**

- 🛑🛑★★★★★ [THE OSCILLATION EXCESS IS **ANGLE-GATED**](accord/mechanism/accord-the-oscillation-excess-is-ANGLE-GATED.md) — matched on steering angle: at **|ang| < 20° we ARE stock (1.06–1.08×)**; at **20–60° p90 1.74×, max 16.568 vs stock's 2.111**; at **60–400° p90 3.16×, max 10.244 vs 1.909**. ✅ **The exposure control INVERTS the confound** — stock drove the extreme regime MORE often (13.2 % vs 4.9 %) at HIGHER angle and command, and stayed calm. ⭐ **A TAIL phenomenon**: pooled 6–9 Hz power is only **1.11×**, but stock's whole distribution tops at **3.748** while V112 reaches **16.568**, exceeding stock's p99 **3.9× more often** ⇒ quote the kit's “16–30×” as a TAIL statistic, never a mean. ⊕ **Q is now EVIDENCE**: a spectral estimate over 573/809 s reproduces the n=1 ring-down (STOCK Q 19.65 ζ 0.0254; V112 Q 9.27 ζ 0.0539). ⭐ Points at **`0xC40D2` K1 ×6 on stock**, angle-gated through `|model|` ⇒ **rehabilitates V113's direction**. ⚠ NOT demonstrated; the plant confound stays open. ✅ Closable without a drive: measure `gp-0x6ae2` against |ang| on existing routes.

- 🛑🛑★★★★★ [K1 IS THE ANGLE-GATED ANTI-DAMPING — **V113 is the fix, already built**](accord/mechanism/accord-K1-is-the-angle-gated-antidamping-and-V113-is-the-fix.md) — chain closed with measurements: the excess is **angle-gated** (exposure-controlled, confound inverts); **`|model|` rises 7–9× with angle** (cave b5 rung, monotone over four bins on two routes); the friction term is `|model|·K1/1024·sat(rate)`, **in phase with rate ⇒ anti-damping**; and **K1 is ×6 on stock**. ⭐ **V113 = V112 with K1 612→204, six bytes**, cutting that term to **0.333×** exactly where `|model|` is largest — and it is **V111's K1 with V112's knee**, i.e. the two things that each worked. 🛑 **I deprioritised V113 and that was wrong.** ⚠ Cost: heavier below ~30 °/s. 🛑 Falsifier: unchanged large-angle oscillation ⇒ K1 is not it and the gating is plant-side.

- ⭐⭐★★★★★ [V120 — K1 612→306, **Honda-equivalent feel, half the anti-damping**](accord/mechanism/accord-K1-is-the-angle-gated-antidamping-and-V113-is-the-fix.md) — 🛑 **DOSE CORRECTION**: V111 and V112 deliver the SAME low-rate compensation (the gain-hold), so **V113's K1 204 is 0.333× — BELOW stock's 0.500× ⇒ heavier than HONDA**. Not intended. **K1 = 306 lands exactly on stock's 0.02816 at 3 °/s.** V120 cuts anti-damping **0.500× at every frequency** with no added inertia or phase, keeps the relay **corner at 31.8 °/s** (so V112's 1.37–1.62× authority win survives), and **self-targets** via `|model|`'s 7–9× angle rise. 2 payload bytes, 40/40. image `a588f936…` .rwd `9d646927…`. ⇒ **V120 is the flight; V113 is the fallback if 0.500× is not enough.**

- 🛑🛑★★★★★ [**THE K1 MECHANISM IS REFUTED** — V120/V113 are not mechanism-backed](accord/mechanism/accord-K1-is-the-angle-gated-antidamping-and-V113-is-the-fix.md) — V111 and V112 share a small-signal gain, so V112's friction term is **1.9–3.0× larger at high rate**; if it drove the anti-damping V112 must be 2–3× worse at large angle. **It is not** — p90 ratios 1.27 / 0.90 / **0.75** / 1.53 with CIs **excluding 2× in three of four bands**, and at 20–60° V112 is BETTER. ⊕ At 0–5°, where the term is IDENTICAL, V112 is still 1.27× worse [1.05,1.49] — unexplainable by the term. ✅ STILL SOLID: angle-gating vs stock, `|model|` rising 7–9×, K1 being ×6 on stock. 🛑 **REFUTED: the causal link.** ⇒ **V120/V113 are valid builds with an UNKNOWN effect on the symptom — do not present either as the fix.** ⚠ Open: something else differs between V111 and V112.

- 🛑🛑★★★★★ [ONE ROUTE PER BUILD CANNOT RESOLVE A BAND RATIO](feedback/measurement/feedback-one-route-per-build-cannot-resolve-band-ratios.md) — `r22` and `r23` are **both V112**, and at 20–60° they differ **2.74× [0.79, 8.87]** in 6–9 Hz p90. 🛑 **Window-level bootstrapping understates cross-build uncertainty** because the DRIVE is the unit. In one session this invalidated **both** a mechanism refutation (“V112's term is 2–3× larger yet not worse” ⇒ the K1 mechanism is **UNTESTED, not refuted**) **and** the residue it left (“1.27× at 0–5°” vs a same-firmware 1.07× [0.81,1.25]). ✅ **Resample ROUTES; ≥2 routes per arm; at large angle an effect must exceed ~2.7×.** ⚠ The angle-gating result **survives** (tail 4.4× / 7.9× clears the floor, exposure-controlled) but rests on ONE stock route — its 1.06–1.74× per-band ratios are NOT resolved.

- ✅★★★★★ [ANGLE GATING — confound removed, **9 of 9**; one stock route caps p at **0.100**](accord/mechanism/accord-the-oscillation-excess-is-ANGLE-GATED.md) — **within-drive** design (each route its own control ⇒ immune to route offset). The raw ratio was **confounded**: its denominator varies **10x** across builds, and all 3 mod routes below stock had small-angle p90 > 6. **Matched on small-angle p90** (stock ranks 3rd of 10 ⇒ exposure matched), **stock's large-angle p90 = 1.551 is BELOW ALL NINE mods (2.909-8.320)**; the **same-firmware** V112 pair both exceed stock by ≥ **1.88x**. 🛑 Exact permutation **p = 1/10 = 0.100** — **with ONE stock route the FLOOR is 1/(n_mod+1) and CANNOT reach 0.05; the limit is the DESIGN.** ✅ **Two stock routes ⇒ p = 0.0182.** ⇒ **the drive card is THE GATING ITEM — one stock drive, no build, no flash.**

- ⭐⭐★★★★★ [HONDA RATE-SCHEDULES THE PID — and `Kd` is FLAT](accord/calibration/accord-pid-gains-are-rate-scheduled-and-kd-is-flat.md) — all three PID gains in `FUN_0003a382` are four-knot LERPs on the **same axis `gp-0x6ac0`** (FOC electrical rate). **Kp is NOT flat: Y = 256/256/225/153 — Honda rolls it off 40 % at high rate**, which proves the machinery is live, wired and calibrated. **Ki and Kd are FLAT and byte-identical stock vs V112**, virgin across the whole build history. 🛑 ⇒ **[[accord-factord-is-the-angle-error-lever]]'s “no frequency-selective lever” is TOO STRONG** — scope it to FactorD. ⊕ Archived Re(Z): **D PUMPS 2-12 Hz, DAMPS 16-35 Hz** ⇒ Kd anti-damps at the symptom band, and a **rate-scheduled** rolloff costs nothing where the LKAS command lives. 🛑 **CRUX TEST RAN AND FAILED — PARKED**: on 8,200 windows the rate axis gives **AUC 0.630**; a knot catching 60 % of oscillation also catches **37 % of normal hard curves**, and at 60 deg/s the ratio *inverts*. Medians do separate 1.9× ⇒ **weak, not zero**. ⚠ proxy is WHEEL rate; the real axis is MOTOR rate ⇒ may understate, but resolving it needs a cave probe (the only bricking class). **The STRUCTURE stands; the discriminability does not.** ⚠ changes MANUAL steering; GATE 2 applies. ✅ cal-only, no cave.

- ⭐⭐★★★★ [THE 18-22 Hz GRIND IS **RATE-COLOCATED** WITH THE 7-9 Hz OSCILLATION](accord/mechanism/accord-18to22hz-grind-is-rate-colocated-with-the-oscillation.md) — band power above a rate knot, 8,200 windows: at T=40 the two bands hold **59.3 % vs 59.5 %** ⇒ **no rate-scheduled lever can separate them**, and since D **pumps 6-9 / damps 18-22** any such lever helps one and hurts the other in the same windows. ✅ The **26-31 Hz** band IS separable (only **2.6 %** above 60 deg/s ⇒ **10-17×** selectivity, past the 4.36× needed). 🛑 18-22 gives **1.10×** against a required **2.82×** ⇒ **`Kd` stays REFUSED**, ~2.6× against. 🛑 **Corrects my own earlier framing**: rate AUC 0.630 was vs **hard curves**; vs **ordinary driving** it is **0.978** (10.86×), beating angle's 0.713 — the symptom IS the hard-curve regime (operator: *“during the peak of a hard curve”*), so that tension is structural.

- ⭐⭐★★★★★ [THE 7-9 Hz MODE IS **NONLINEARLY EXCITED** — resolves BOTH open contradictions](accord/mechanism/accord-the-7to9hz-mode-is-nonlinearly-excited-harmonics.md) — harmonic prominence at `2f0`/`3f0`, controlled against **non-oscillating windows** and bootstrapped over **DRIVES**: **1.233×, CI [1.060, 1.503]** ⇒ real. ✅ **But f0 = 7.81 Hz puts 2f0 at 15.62 and 3f0 at 23.44 — NEITHER in 18-22 or 26-31** ⇒ the harmonic hypothesis FAILS, the rate co-location is **shared exposure not shared mechanism**, and [[accord-two-symptoms-two-mechanisms-rez-spectrum]] **wins**. 🛑 **Fixing 7-9 Hz will NOT fix grind #1 — budget for two fixes.** ✅ Also reconciles the resonance record: **a linear resonance driven THROUGH a nonlinear element** gives a clean ring-down *and* harmonics ⇒ both stand. ⭐ Points at the **Coulomb relay** (a signum — the textbook harmonic generator) ⇒ **V120 now has a measured mechanism, not just reasoning.** [BELIEF, one converging line.]

- ⭐⭐★★★★★ [`KNEE` IS THE RELAY'S **SHAPE**, `K1` IS ONLY **GAIN** ⇒ fly **V116**, not V120](accord/calibration/accord-knee-is-the-relay-shape-variable-k1-is-only-gain.md) — `K1` multiplies AFTER the relay, and **a signum's harmonic ratio is scale-invariant**, so K1 cannot move it *by construction*; my first dose test used `fric_gain` and conflated the two. Re-tested on `knee`: **300 → 1.743, 600 → 1.412, 1800 → 1.213** — monotone across all three levels flown, direction as predicted (ρ − 0.291, p 0.257, CIs include 1.0 ⇒ **BELIEF, suggestive**). ✅ **V116 (knee 1800→2400, K1 612→816) holds the small-signal gain EXACTLY at V112's while the relay saturates 1.333× later** = more linear relay, **no assist change**. 🛑 **V120 halves K1 ⇒ halves modelled friction ⇒ LESS assist** ([[accord-friction-polarity-more-assist]]), against the operator's constraint, and being pure gain it cannot touch the harmonics. ⇒ **V116 supersedes V120.**

- ✅★★★★★ [**V121 BUILT** — the MAXIMAL gain-matched knee, `0xC40BC` 1800→3000 + `0xC40D2` 612→1020](accord/calibration/accord-knee-is-the-relay-shape-variable-k1-is-only-gain.md) — `ce565da7…` / `8c154edb…`, **38/38**, 4 payload bytes, cal-only, alpha2 held. 🛑 **The dose is set by a HARD CEILING**: `K1/1024` is the friction's max as a fraction of `|model|`, so **`K1 ≥ 1024` inverts the residual's sign** — V121 sits at **0.996** and knee 4000 (1.328) is **REFUSED**. 🛑 **V116 is a HALF-STEP** — it saturates at 42.4 deg/s but oscillating windows median **47.06**, so its relay is still a signum where the symptom lives; **V121's 53.1 clears it**. ✅ Feel is **bit-identical to V112 ≤ 31.8 deg/s**, then **1.571×/1.667×** more friction ⇒ **MORE assist** at high rate ⇒ the operator's constraint moved the RIGHT way. ⚠ manual feel changes >31.8 deg/s; does NOT address grind #1 (separate mechanism). **Falsifier: harmonic ratio must drop below V112's 1.213.** ⇒ **fly V121; V120 withdrawn.**

- 🛑🛑★★★★ [NEVER EXTEND A **MEASURED** LADDER BY EYE](feedback/measurement/feedback-never-extend-a-measured-ladder-by-eye.md) — I added `3000: 0.0370` to a dict printed as *“MEASURED relay saturation duty”* in a **flashable build's provenance**, by eyeballing the neighbouring rungs. 🛑 Recomputing it properly **also failed**: on the published gate my reconstruction gave **n = 572 vs 289** and missed every rung ⇒ **the ladder's exact gate is NOT recoverable from the r21 cache. OPEN.** ✅ Fixed **structurally** — the builder now asserts `KNEE_NEW not in MEASURED_DUTY` and `2400 < KNEE_NEW < 3600`, so it **fails** if anyone adds an unmeasured rung, and claims only a **bracket**. ⭐ Rule: a table labelled MEASURED holds only measured values — reproduce the existing rungs first, and if they do not reproduce, **stop**.

- 🛑★★★★ [THE KNEE HAS **NO MEASURED** DOSE-RESPONSE ON GRIND #1](accord/mechanism/accord-knee-has-no-measured-dose-response-on-grind1.md) — I was about to upgrade V121's grind-#1 claim on the operator's report (constant → *“rare… a few moments”* exactly when knee went 600→1800). **Tested first; it does not reproduce**: across 17 routes 18-22 Hz share goes the **WRONG WAY** (ρ **+0.356**) and 26-31 Hz is flat (ρ −0.158, p 0.546). ⇒ **V121 does NOT address grind #1** — upgrade withdrawn before it was made. ⚠ Not a refutation of the operator: **n=2 routes at knee 1800**, and 🛑 **band SHARE is not severity** (normalised by broadband power ⇒ the share can rise while the absolute level falls; the 6-9 Hz reference row rises with knee, ρ +0.477, opposite to the harmonic and on-road results). **Absolute band level with exposure controlled is the right statistic. OPEN.**

- ✅★★★★★ [**LEVER B IS ON THE CAR** — grind #1 needs something NEW](accord/builds/accord-lever-b-is-on-the-car-grind1-needs-something-new.md) — byte scan, 25 images + stock: **V112 (on the car) and V121 both carry Lever B** (`0x3AA96`=FB, `0xC6446`=5244) — grind #1 **0.40 [0.27,0.58]**, best in the kit, **and** creep grind #2 → **0 bursts**, mode-proof. 🛑 **Corrects my own alarm** from [[accord-v81-carries-neither-grind1-fix]], which is **specific to V81**: Lever B was restored at V88 and is continuous, the only later gap being **V101/V102/V103**, closed at V104. ✅ **Lever A absent from all 25 — correctly**, its r24 half raised 40-49 Hz **×11.7 and caused grind #2**. ⚠ Its **r26 half alone** (ONE byte) never flew in isolation but leans **inert**. 🛑 ⇒ **the remaining grind #1 is NOT a lost fix — the best fix is deployed and the symptom persists, so it needs a NEW lever.** Not the knee, not the base-assist damper.

- 🛑🛑★★★★★ [GRIND #1 IS **UNMEASURABLE** ON THE RECENT ROUTES — no creep exposure](accord/mechanism/accord-grind1-is-unmeasurable-on-the-recent-routes.md) — a grind pipeline that **passed its positive control** (recovered Lever B at **2.32× [1.62,2.94]** vs the on-car ≈2.5×) then ranked **V112 WORST**, which the operator flatly contradicts (*“best firmware ever… grind #1 rare”*). ✅ **Cause: grind #1 was characterised at CREEP** (*“grinding at 2-5 mph is gone”*) and **r21/r22/r23 have ZERO engaged 2-5 mph windows** (vs 39 for r77, 11 each for r85/r9e/ra5/r1e) ⇒ the all-speed stat measured **road-speed 18-22 Hz, not creep grind #1**; its four p<0.10 hits are one collinear old-vs-new contrast — **do not act on them.** 🛑 Also weakens the knee-vs-grind null, which pooled all speeds. ✅ **Unblocked by a creep-inclusive drive OR operator timestamps at the moment it happens** — **second gating measurement item. No build, no flash.**

- 🛑★★★★ [V121's HARMONIC RATIONALE IS **WEAKENED** — the one quantitative check fails](accord/calibration/accord-knee-is-the-relay-shape-variable-k1-is-only-gain.md) — fed the measured V112 rate through the relay at each knee: **0.951 / 1.365 / 1.248 / 1.493 / 1.832 / 1.278** for knee 600/1800/2400/3000/4000/8000 ⇒ **non-monotone, knee 3000 predicts 1.094× (slightly WORSE)**, and it **contradicts the cross-build trend**. ⚠ The sim is **INVALID as a prediction** — the measured rate already contains the running relay's effect, and **a memoryless nonlinearity in a CLOSED LOOP cannot be simulated by post-processing the loop's own output** — but **failing to reproduce the trend still lowers confidence**. ⇒ **V121 not withdrawn**; its case now rests on **gain held exactly at V112's** (bit-identical ≤31.8 deg/s), **more assist above it**, and knee's on-car track record. ⚠ **V116 is the conservative version** (K1 0.797 vs 0.996 of |model|). 🛑 **Effect on the oscillation: UNKNOWN.**

- ⭐⭐★★★★★ [THE **ONLY** ENGAGED ASYMMETRY LEFT IS THE FRICTION ROW](accord/calibration/accord-the-only-engaged-asymmetry-is-the-friction-row.md) — all 14 mode-indexed families dereferenced at `arr+mode*4`: on V112/V121 **exactly one** differs m24 vs m26, **`0xCBE74` friction**; FactorB/C/D/E, ceiling, the four r24 `gain_B` arrays and boost curve/amp/ceiling are **all byte-stock and symmetric**. ✅ **The V74-V81 engaged-only FactorC/E damper is GONE** (V90+ = Honda's ramp) ⇒ *“restore the RAMP”* **already satisfied, no build needed.** ⭐ The row escalated **×1.5 at V91, ×3.0 at V107 — and V107 changed its SHAPE** (Y[2] **8.14×** vs Y[0] 3.0×): stock's |Y| decays **5.0×** across the axis, ours only **1.84×**. **All prior analysis was of UNIFORM scaling.** 🛑 **FLAG:** the kit closed this lever partly on **“int32 wraparound at 1.6005×”** and **V107..V121 carry 3.00×** — unverified; the output is hard-clamped to ≤1024 so it *should* saturate not wrap, but that is a belief. ✅ **OPEN: pure-arithmetic check, no drive, no flash.**

- ✅★★★★ [FRICTION-ROW FLAG **RETRACTED** — no defect on the car](accord/calibration/accord-the-only-engaged-asymmetry-is-the-friction-row.md) — I flagged V107..V121's **3.00×** engaged row as past a *“1.6005× wraparound”*. **Wrong on three counts:** 🛑 the ±1024 clamp I cited is in `FUN_00034350`, which **never reads `0xCBE74`** — the row's consumer is `FUN_00036c12` → `gp-0x6b26`, clamped ±511; 🛑 I scaled an engaged **MAX** (319.1×3.0=957) when V107's builder has a **“THE TERM IS NOT SATURATING”** section — **p99 268.5 vs 511, duty 0.00121**, held-out validated on r78; ✅ the governing bound is the **int16 floor k_max = 32768/9830 = 3.3335**, and 3.00× = **90.0 % of it, chosen deliberately** (the “90.0 %” column is int16 headroom, not duty). ⇒ the 1.6005× figure in [[accord-six-levers-closed-on-arithmetic]] does **not** govern this row — reconcile those two. **The asymmetry finding stands; the defect claim does not.**

- 🛑🛑★★★★★ [THE ADDED LKAS “MASS” **IS** THE DAMPER THAT WORKS — do not cut it](accord/mechanism/accord-the-added-lkas-mass-is-the-damper-that-works.md) — I was one step from proposing a cut to the engaged friction row to buy back LKAS acceleration, on the (correct) structural fact that `gp-0x6b26 = −K·α` is apparent **inertia**. 🛑 **V94 flew that exact argument, cut it 6×, and the operator ABORTED** — *“vibrated the entire car… not safe to drive”*; motor acceleration **3-7× up above 9 Hz**. ✅ Two drives, ω-partialled + shuffled control: delivered phase **+137°/+139° vs WHEEL rate at 6-9 Hz** ⇒ **+518/+565 counts POSITIVE Re(Z) — a REAL 6-9 Hz DAMPER**; first measured `d(symptom)/dK`, **sign UP**. ⭐ **Reconciliation:** structure ≠ delivered effect in a filtered loop — both notes stand. 🛑🛑 **⇒ the operator's two goals are in MEASURED opposition on this lever**: the apparent mass he feels **is** the fix that is working, and “more damper” has only **1.11× headroom** before int16 overflow. **Any acceleration gain must come from a DIFFERENT lever.**

- ⚠★★★★ [A CANDIDATE TRIGGER — **decelerating into the turn**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — first use of the operator's **labelled** r23 event as a CASE against same-drive matched controls: speed at the **100th** pct (43.6 vs 25.4 km/h) and d(speed)/dt at the **0th** (**-1.159** vs +0.619 m/s²) — one physical fact, **braking into a corner at speed**, matching his *“peak of a hard curve”*. Corpus-wide: **12 of 17 routes** decelerate more before oscillating, median **-0.172 m/s²**, CI **[-0.249, +0.035]**, Wilcoxon **p = 0.189** ⇒ **suggestive, NOT established**. ⚠ His event is **6.7×** the corpus effect ⇒ either atypical **or a THRESHOLD**, which a mean comparison cannot see — **next test, no new data needed**. 🛑 **FOLLOW-UP: the threshold test looked decisive unstratified** (1.58-1.73×, **every** CI excluding 1.0 across all 17 routes) **but does NOT survive speed×angle stratification** — the only resolving cell is **low-angle 50-80 km/h (4.12× [1.44,19.00])**, while both **high-angle** cells (the operator's regime) span 1.0 (1.39× [0.94,2.33], 1.26× [0.71,1.92]); decelerating windows are also **slower** (40.0 vs 48.3 km/h) ⇒ partly composition. ⇒ **[BELIEF, 5 of 6 strata in direction, NOT established where it matters]. Do not build against it.**

- 🛑🛑★★★★★ [THE DAMPING ROUTE IS **CLOSED** — by the ±511 rail, not by int16](accord/mechanism/accord-the-damping-route-is-closed-by-the-rail.md) — I was about to raise `Y[1]` of the engaged friction row (**1.90× int16 headroom**, and the oscillation's median 35 km/h sits between the 20 and 90 km/h knots; `Y[1] = -24000` is even **flight-proven** on V107). 🛑 **V108's own E2 measured it harmful**: at `Y1 = -24000` the term **rails 32.32 % [29.93,35.68] at 10-25 km/h** and 21.27 % at 24-40 ⇒ **a damper that clamps a third of the time IS A RELAY**, the class that made V80 *“worst grinding ever recorded”*. ⚠ At 24-40 km/h V108 **already rails ≤10.45 %** ⇒ **no safe headroom at the symptom's own speed.** ✅ The real limit is `gp-0x6b26`'s ±511 clamp (`0xC407E`), **hard-blocked** — V73 raised it and **V74/V75 hard-faulted**. ⇒ **only EXCITATION reduction remains ⇒ V121 is strengthened BY ELIMINATION** (mechanism still BELIEF, effect still UNKNOWN).

- 🛑🛑★★★★★ [THE 7.8 Hz MODE **DOES NOT MOVE** WITH FIRMWARE GAIN — it is mechanical](accord/mechanism/accord-the-78hz-mode-does-not-move-with-firmware-gain.md) — the test that classified the ~23 Hz line (it **moved 20.3→23.0 Hz**), applied to 7-9 Hz across gains **3564/5346/7128**: **Spearman(gain, f0) = −0.015, p = 0.954**, medians non-monotone (7.764/8.008/7.617 Hz) ⇒ **a FIXED MECHANICAL RESONANCE, not a relocatable pole.** ⚠ Self-correction: my script's verdict line said *“f0 MOVES”* on a **max/min spread** dominated by the **single stock route** — spread is not the test, the **gain correlation** is. 🛑🛑 ⇒ of the three things firmware can do — **move it** (refuted here), **damp it** ([[accord-the-damping-route-is-closed-by-the-rail]]), **excite it less** — **only the third remains, and that is V121.** ⇒ **the 7.8 Hz oscillation may not be fully eliminable in firmware**; the honest claim is *reduce how hard it is driven*. **Do not promise elimination.**

- ✅⭐★★★★★ [THE 7-9 Hz ENERGY IS **MANUFACTURED**, NOT COMMANDED](accord/mechanism/accord-the-7to9hz-energy-is-manufactured-not-commanded.md) — 16 routes, oscillating windows: **command 6-9/0.5-3 = 0.00528** vs **response 0.13962** ⇒ the response carries **26.5× more relative 7-9 Hz content than the command**; coherence **0.488 vs 0.356 shuffled, CI [0.082, 0.250]**. ⇒ the command **modulates** but does not **contain** the oscillation — **the signature of a NONLINEARITY**, matching the harmonic result independently. ⭐⭐ **The hopeful consequence:** the mode cannot be moved or damped further, leaving only *excite it less* — and since the energy is **not in the command, it is NOT an inherent price of 6× torque** ⇒ **the generator can be attacked without giving up torque; the first structural reason the operator's two goals are compatible.** ✅ **V121's PREMISE is upgraded to [EVIDENCE]**; 🛑 that the *relay specifically* is the generator remains [BELIEF] and its effect UNKNOWN.

- 🛑★★★★★ [THE HARMONICS TRACK **NEITHER** FIRMWARE SATURATION](accord/mechanism/accord-the-harmonics-track-neither-firmware-saturation.md) — the relay saturates on **|RATE|**, the damper's ±511 clamp on **SPEED** (duty 15.46 % → 0.23 % over 10→65+ km/h), so they separate. **By speed the ratio is FLAT 1.110/1.159/1.162/1.117** with tight CIs over 16-17 routes ⇒ **the CLAMP is REFUTED** (a generator whose duty falls 67× cannot give a flat ratio). By |rate| there is **no rise past 31.8 deg/s** ⇒ **the RELAY is NOT SUPPORTED**, though that arm is underpowered (**not supported ≠ refuted**). ⊕ Harmonics are real and pervasive. ⇒ **a uniformly-active nonlinearity is not a saturation** — it fits **physical friction/stick-slip** better than any firmware clip, cohering with the mode being mechanical. 🛑🛑 **V121's mechanism has now failed TWO independent checks** ⇒ **good engineering properties, weak mechanism case. Fly it as a TEST, not a fix.**

- 🛑🛑★★★★★ [`K1` AND `knee` ARE **PERFECTLY CONFOUNDED**](accord/calibration/accord-k1-and-knee-are-perfectly-confounded.md) — every flown mod holds **K1/knee = 0.34** (K1=204 at knee 300 **and** 600; K1=612 at knee 1800) and **no knee value has ever been flown at two K1 values** ⇒ **the kit has never learned which cell matters.** 🛑 **V121 (knee 3000 / K1 1020) is ratio 0.34 too — a fourth point on the same line ⇒ it CANNOT separate them:** if it works we won't know why, if it fails we won't know which half. ✅ **Structural, not an oversight** — holding the small-signal gain constant **requires** K1 ∝ knee, so separation **requires a gain change**. ✅ **V113 (knee 1800 / K1 204) is the only built artifact that breaks it**, at 0.333× gain ⇒ less assist. ⇒ **Sequence: V121 first (cannot worsen normal driving); V113 second ONLY if V121 moves the pre-registered endpoint.**

- ⭐★★★★ [TABLE (b) IS THE **ANGLE HANDLE INSIDE THE OBSERVER**](accord/calibration/accord-table-b-is-the-angle-handle-inside-the-observer.md) — decompiled `FUN_0003b8f6`: table (b) (`0xC6B66`/`0xC6B80`) is **LERPed on `gp-0x6a10` = absolute steering angle** and its product is **added into the model** ⇒ it scales **`|model|`, the exact amplitude multiplying the Coulomb signum**, so it sets part of what makes the symptom **angle-gated**. Y rises **899→1084 (1.21×)**, saturating at 2.5°. ⚠ `|model|` rises **7-9×** with angle, so this table supplies only a fraction — don't oversell. ✅ **Its value is being ORTHOGONAL**: every flown mod sits on `K1/knee = 0.34`, and table (b) is on neither axis ⇒ an independent, **angle-targeted** lever (flatten to 899 ⇒ −17 % at high angle, **nothing below 2.5°**). ✅ **CLOSED:** the relay's `12` is a **hardcoded `0xc`**, not a cal ⇒ **no third handle**; the K1/knee confound is structural.

- ⭐★★★★ [A NEW **VIRGIN** CANDIDATE — the model's own bandwidth `0xC50D8`](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_0003b8f6` is the **1 kHz** observer and its **TORQUE-SENSOR** branch (`gp-0x4f60`) passes **two EMA stages at pole2 = 122**, so **at 7.8 Hz that branch passes only 27.6 %** (the command branch, pole1 = 832, passes 0.956 — I had the two poles backwards and the correction STRENGTHENS it: the attenuated branch is the **torsion-bar sensor**, exactly where the resonance lives) ⇒ **~72 % of the oscillation is classified as DISTURBANCE and the friction signum chases it — stick-slip, quantified.** (50 % needs pole2 196, 80 % needs 382, 90 % needs 560.) ✅ **Hazard CLOSED**: `0xC5000` was cleared on **three traces** (blank-check-only boot, no CRC32 polynomial, zero xrefs to `0xC5FFC`) ⇒ red herring for V40. ✅ **VIRGIN on all 115 images**, and **orthogonal to the K1/knee confound**. 🛑 **NOT a build — blocked on GATE 2**: `gp-0x6b98` is **broadband and downstream of the assist**, so this is a **feedback path** around a **Q 14-29 mode**; 🛑🛑 **GATE 2 RUN AND IT FAILS**: pole2 122→382 is gain **2.900×** with a **+63.4° phase ADVANCE** in a branch that is added to the model and subtracted downstream ⇒ the cosine term swings by up to **0.7**, so it can make the symptom **better or worse** and magnitude reasoning cannot pick. Settling it needs `arg(ACTUAL)−arg(MODEL)` at 6-9 Hz; the kit's +137°/+139° is a **different pair** and does not transfer, and **none of `gp-0x4f60`/`gp-0x6bf6`/`gp-0x6bfc` has ever been on the wire** ⇒ needs a **CAVE PROBE, the only class that has bricked this ECU.** ⇒ **BLOCKED, not deferred.**

- ✅⚠★★★★ [THE IMU IS A **VALID BUT WEAK** GRIND-#1 INSTRUMENT](accord/instruments/accord-the-imu-is-a-valid-but-weak-grind-instrument.md) — `imu_vert` logs at **100 Hz on all 17 routes** (ratio 1.00 vs `cs_rate`) ⇒ Nyquist 50 Hz, **18-22 Hz genuinely visible**. Validated on Lever B, engaged-vs-manual within drive: **OFF/ON = 1.139, CI [1.005, 1.338]** ⇒ it **discriminates**, but recovers only **1.139×** where the steering-rate instrument gives **2.32×** for the same effect ⇒ **about 10× dilution**; needs a true effect **> ~3×** to clear its floor. ✅ **Its unique value: it needs NO creep exposure**, which is exactly what makes grind #1 unmeasurable on every post-V107 route. 🛑 **Only a POSITIVE result is informative — never declare a null on it.**

- 🛑🛑★★★★★ [GRIND #1 **MOVED UP** — the kit's bands miss it, and the KNEE **IS** its lever](accord/mechanism/accord-grind1-moved-up-and-the-knee-IS-its-lever.md) — **operator correction**: grind #1 moved to a higher frequency, so **every grind measurement in this session (18-22 Hz) was aimed at a band the symptom had left.** Engaged-minus-manual peak: **STOCK 15.0 Hz**, every mod **20.3-32.8**, recent builds **20.9-23.4** ⇒ **the kit's 18-22 and 26-31 bands STRADDLE ~23 Hz and both miss it.** ✅ Re-run on **21-26 Hz**: knee **300→0.631 / 600→0.246 / 1800→0.213**, monotone, **300/1800 = 2.956 [1.164,4.079]** and the well-powered **300/600 = 2.565 [1.010,4.664]** — **both exclude 1.0**. 🛑 **Withdraws `c91a1ba5` (“no dose-response”) — a BAND ERROR, not a null**, and vindicates the operator's own 600→1800 report I had treated as unsupported. ✅ **V121 now has a MEASURED grind-#1 rationale**; its scoring card is corrected to make grind #1 an endpoint at 21-26 Hz. ⚠ knee is confounded with K1 ⇒ the AXIS is established, not the cell.

- 🛑🛑★★★★★ [GRIND #1 IS A **MOVABLE POLE** — `alpha2` is its handle](accord/mechanism/accord-grind1-is-a-movable-pole-and-alpha2-is-its-handle.md) — on the corrected **21-26 Hz** band, grind #1's **PEAK FREQUENCY tracks `alpha2` (`0xC40DC`)**: 22 → ~23.5 Hz, 14 → ~21.1 Hz, **hi/lo 1.113 CI [1.06,1.17], p 0.035**; amplitude too, **1.340 CI [1.12,2.29]**. ✅ **A frequency that moves with a cal is a CLOSED-LOOP POLE ⇒ grind #1 is RELOCATABLE in firmware**, unlike the 7.8 Hz oscillation whose `f0` is invariant ⇒ **grind #1 has the better prognosis.** ✅ **V109 already went 22→14** (right direction on both, unknowingly), and **V115 (`alpha2` 14→8) is ALREADY BUILT AND UNFLOWN** — `5f804a8a…`, 42/42. 🛑 **CONFOUND: `alpha2`=14 exists only on V111/V112 ⇒ collinear with build era** ⇒ [EVIDENCE the frequency moves; BELIEF that `alpha2` moves it]. ⚠ Check `alpha2`'s other role (the `gp-0x6b26` bandpass corner) before flying V115.

- ✅✅★★★★★ [`alpha2` **IS** THE FREQUENCY-SELECTIVE LEVER — fly **V115** for grind #1](accord/calibration/accord-alpha2-is-the-frequency-selective-lever.md) — `0xC40DC` is EMA-A's coefficient (`alpha = alpha2/64`) on a **first difference**, so the lane is a **differentiator whose response RISES with frequency** ⇒ lowering it cuts **high** bands far more than low. Measured at 1 kHz, 14→8: **3 Hz 0.993× (LKAS command UNTOUCHED) · 7.8 Hz 0.957× (damper −4.3 %) · 23.4 Hz 0.783× (grind #1 −21.7 %)** ⇒ **selectivity 5.07×**. ✅ **This is the frequency-selective lever the kit concluded does not exist**, and it satisfies the operator's constraint **by construction** — loop gain removed at 21-26 Hz with **no added mass, friction or inertia**. ✅ **GATE PASSES vs V94**, which cut this same lane 6× and caused an abort: 14→8 costs **1/20th** of that. ⊕ **V109's 22→14 was selective the same way (7.19×) and flew fault-free on V111/V112.** ⇒ **V115 is BUILT** (`5f804a8a…`, 42/42) — **fly V115 first, then V121.** 🛑 `alpha2`=14 is only on 3 routes ⇒ empirically collinear with build era.

- 🛑★★★★★ [V121 WILL PROBABLY FIX **NEITHER** — the knee axis is SATURATING](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — measured on the corrected 21-26 Hz band: knee **300→0.631 · 600→0.246 (2.57×) · 1800→0.213 (only 1.15×)**, and the relay saturation duty behind it falls **29.1 → 19.2 → 6.7 → 3.7 %** ⇒ **V121's 1800→3000 step removes only 0.25× as much saturation as 600→1800 did**, which itself bought just 1.15×. ⇒ **expected grind gain ≈ 4 %**, against **V115's −21.7 %**. 🛑 On the oscillation V121 is **UNKNOWN** (mechanism failed two independent checks). ⇒ **self-correction: V121 is downgraded as a symptom fix** — its honest value is **AUTHORITY** (1.571×/1.667× more friction above 31.8 deg/s ⇒ more assist, bit-identical below). **Sequence: V115 first, V121 second as an authority build.**

- 🛑🛑★★★★★ [THE PEAK-TURN OSCILLATION IS **PROBABLY MECHANICAL**](accord/mechanism/accord-the-oscillation-is-probably-mechanical-not-firmware.md) — the last generator hypothesis (the **`|model|`-scaled signum**) predicted **angle-gated** harmonics, since `|model|` rises **7-9×** with angle. **Tested: FLAT** — 0-5 1.102, 20-40 1.377, 40-400 1.107, **high/low 1.004 CI [0.843,1.586]**. ✅ The harmonics are **real** (1.233× vs a non-oscillating control) but track **nothing** — speed, rate or angle ⇒ **the signature is INTRINSIC to the mode.** **THREE convergent lines**: `f0` invariant to 2× gain · harmonics track neither saturation axis · harmonics track no operating variable. ⚠ Firmware still supplies the **excitation** (engagement ×2.8, angle-gated, energy manufactured downstream of the command) — but **every firmware excitation path is now closed or exhausted**. ✅ **⇒ a MECHANICAL inspection is worth more than another cal edit** — lash or a worn compliant element (I-shaft U-joints, rack bushings, tie-rod ends, motor coupling). **[BELIEF — a direction, not a diagnosis.]**

- ✅★★★★ [AUDIO IS EXTRACTABLE FOR THE CURRENT BUILD — no 50 Hz ceiling](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — every CAN/IMU channel is 100 Hz (Nyquist 50), but the rlogs carry **`rawAudioData`** PCM: **710.3 s at 16 kHz** extracted for **r22 (V112)** via `rlog-tools/decode/extract_audio_v112.py` (the old `extract_audio_grind.py` has a stale `_cache_<tag>` path from the reorg). ⭐ **THE LEAD: the strongest lines are at 51-56 Hz — ABOVE the ceiling of every other channel**, exactly where the operator's *“moved higher”* would have been invisible. 🛑 **Proves nothing yet** — whole-drive, no engaged-vs-manual contrast, so it could be engine or road; and the AM peak is **5.4-5.9 Hz in EVERY carrier band**, which looks like a common source or artifact. ⚠ Only **0.01 %** of power above 2 kHz ⇒ heavily band-limited; no claim above ~2 kHz is supported. ✅ **Next step needs no drive: align PCM to the CAN timebase and split engaged vs manual.**

- 🛑🛑★★★★★ [THE 21-26 Hz EXCESS AND THE **AUDIBLE** GRIND ARE NOT SHOWN TO BE THE SAME](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — first acoustic case-control on the current build (r22, 710 s of 16 kHz PCM). **Engaged-vs-manual is useless** (52.8 vs 11.5 km/h ⇒ a uniform **+10 dB across 20-2000 Hz** = road noise). **Engaged-ONLY, speed-matched 30-61 km/h**, 224 high-grind vs 189 low-grind windows split on 21-26 Hz steering-rate content: **every band within ±1 dB**, strongest lines only +2-3 dB. ⇒ **windows with high 21-26 Hz steering-rate content have NO distinct acoustic signature** ⇒ **the kit has been calling TWO things “grind #1”** — a measurable steering-rate excess and an audible grind — **and nothing shows they are the same.** ⚠ Don't over-read it: only **0.01 %** of audio power is above 2 kHz (band-limited), and ±1 dB is not a sensitive null. ⇒ **V122's endpoint measures the steering-rate phenomenon; whether it moves what the operator HEARS is now explicitly open.** 🛑 **FOLLOW-UP:** a “1.5 kHz signature” found at the labelled event (+11-14 dB over the p95 of 19 controls) **was RETRACTED** — across 1539 engaged windows the correlation is **−0.167 (p 4.2e-11)** and speed-matched the ratio is **0.70× [0.65,0.77]**, the **opposite** sign. **Cause: 1 window vs 19 controls scanned over ~4000 bins ⇒ the p95 is their max and many bins clear it by chance; no family-wise correction was applied.** ✅ Rule: **a single-window acoustic signature is noise unless it replicates across windows.**

- 🛑🛑★★★★★ [**V122 FLEW (r24)** — grinding better, authority NOT, and **the LKAS COMMAND RAILS in hard turns**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — 832 s, 70.6 % engaged, fault-free. 🛑🛑 **At |ang| 30-60° the command is railed at 99 % of max **51.2 % of the time, median 1.000**; 60-120° 42.7 %; 0-10° 0.0 % ⇒ **openpilot already commands 100 % in hard turns and cannot ask for more — the authority ceiling is the COMMAND, not the firmware**, so no edit downstream of it can help. ✅ Confirms him exactly: **manual p99 rate +112.2 % vs engaged +76.6 %**, engaged accel **−14.6 %**, eng/man 0.74→0.61. 🛑 The pre-registered 21-26 Hz endpoint reads **0.21898 = NOT RESOLVED** while he reports improvement ⇒ **the endpoint is not what he hears; trust the operator.** ✅ His seg-11 event (t 673.4, **7.81 Hz**, identical to V112's) has command p50 **0.277** — **far below the rail**, so the oscillation is NOT a saturation artefact. ⭐ Angle-gated 6-9 Hz vs gain: 4× **3.56** → 6× **2.15** → 8× **1.23**, monotone DOWN — opposite to the 23 Hz vibration — but **n=1 at 8× and the 6× group spans 18×**, and r24 itself is the corpus maximum at 12.05.

- ✅🛑★★★★ [THE ANGLE-GATED **RATIO** MISLEADS WHEN THE DENOMINATOR MOVES](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I flagged V122's ratio of **12.05** as the corpus maximum. **Artefact.** Absolute: hi-ang p90 **9.871** vs r23's **8.320** ⇒ inside the same-firmware spread (the two V112 drives differ **2.9×**). The ratio blew up because the **low-angle denominator fell to 0.819, the lowest in the corpus** ⇒ **`alpha2`=8 improved LOW angle and left HIGH angle unchanged**, exactly matching the operator's *“grinding better, oscillation still there”*. 🛑 **RULE: a ratio moves when EITHER end moves — always report numerator and denominator beside it.** A build that improves the denominator looks worse on the ratio while being better on the car.

- ✅⭐★★★★★ [DELIVERY IS **NOT** SATURATED — and the AUTHORITY GAP is quantified](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — at 30-60°, rate still rises with command (p50 **18.9→26.5**, p90 **66.7→112.1**) ⇒ **nothing hard-clips delivery**; the efficiency fall is **load**, not a rail ⇒ **supports V123's 8×**. 🛑 But efficiency drops **2.9×** (18.93→6.47 per 1000 cmd) ⇒ **8× buys ~1.1-1.2× rate, NOT the 1.33× clamp ratio** — do not promise it. ⭐ **THE GAP: at MAXIMUM command in a hard turn the car manages p50 26.5 deg/s vs the driver's 51.4 — LKAS at full command delivers about HALF the driver's ordinary rate.** ⊕ With the windup result (tracking error **101×** larger at the rail) the picture is coherent: **under-delivery → openpilot winds up → the loop rings at 7.81 Hz → the command carries that same peak.**

- 🛑★★★★ [“SAFE BY CONSTRUCTION” WAS AN OVER-CLAIM — reducing a feedback term is only safe if it is DESTABILISING](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — I justified V124's `0xC63D2` 6→3 as safe whatever its phase. **Wrong: if the term is dissipative, cutting it is the V94 direction**, and V94 is the case where a structurally-inertial term measured as a **damper** (+137°) and the operator aborted. ✅ **But the risk is BOUNDED**: reader #5 is clamped to 5.00 % of aggregator authority and the edit moves **0.297 %** — **17× smaller than V94's change** ⇒ **[LOW RISK, bounded], not [safe by construction]**; V124 unchanged. ⭐ Also found **`0xC642A`/`0xC642C` = 194 (VIRGIN)**, reader #3's input IIRs at **fc 30 Hz passing 97 % of 7.8 Hz** — lowering to ~29 would halve the oscillation band while leaving ≤2 Hz untouched, **better selectivity than `alpha2`** — but 🛑 **NOT proposed: reader #3's authority is unbounded, so the downside cannot be capped without its delivered sign.** Unlock = 427 repoint onto `gp-0x6af0` (2-byte displacement, proven class).

- 🛑🛑★★★★★ [**V122 carries V112's gain — it could not have improved authority**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — byte-verified: `0xC6CD0` is **5346 on BOTH V112 and V122**; V122's edits (knee 1800→3000, K1 612→1020, α2 14→8) are **all friction/shape, none authority** ⇒ the operator's *"does not feel like it has improved at all"* is the **expected** result, and **V124 is the first build since V112 to raise authority** (×1.333). ✅ The 8× chain is clean end to end: full-command forward **3341** < clamp **4096** (82 % used) < governor **4762** (**1.43× headroom**, room to ~11.4×) ⇒ nothing downstream clips it. ⊕ V123's clamp defect quantified: 3341 vs a 3072 clamp = **8.1 % clipped**, effective 7.34× — so scaling the clamps with the gain was necessary. 🛑 **Never raise `0xC6202`** (lockstep-shadowed → fault `0x17`). ✅ *"Our gain never reaches LKAS"* **REFUTED** — `FUN_0002b62c`/`0xC646C` is the **base-assist** path (→ `gp-0x6AF0`), not delivered LKAS.

- 🛑🛑★★★★★ [**The knee/K1 ladder is EXHAUSTED at V122/V124**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the gain-holding invariant `(K1/1024)·(12/knee) = 0.0039844` ends at **knee 3000 / K1 1020**, because knee 3300 needs **K1 1122 > the 1023 ceiling** where friction exceeds `|model|` and the residual inverts. 🛑 **`MEASURED_DUTY`'s 3600 rung is UNREACHABLE — never propose it.** The friction axis is **closed**; further grind-#1 work needs a different lever. 🛑🛑 And the relay is **saturated through the oscillation**: a 2°-peak 7.8 Hz motion peaks at **98 °/s** vs the relay's **53.1 °/s** knee ⇒ constant-magnitude Coulomb friction, the classic limit-cycle driver; linearising it would need knee 5542 / K1 1884, impossible. ⊕ **PICK-TWO**: `plateau = K1/1024`, `sat_rate = knee/56.545`, `slope = plateau/sat_rate` — grind wants slope, the oscillation wants a small plateau AND a high sat rate. One relay cannot serve both. ⚠ knee 3000→4200 with K1 held (friction −29 %, sat 53→74 °/s) is **characterised, NOT recommended**.

- 🛑🛑★★★★★ [**The firmware RAISES the damper scale when it detects an oscillation — V126 fixes it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00036c12`: `gp-0x671a >= cal(0xC64FD)=5` → `Y = cal(0xC640A) = -8192` FIXED, bypassing the speed LERP. **`gp-0x671a` is the hard-reversal counter CLAMPED to CEIL=5** ⇒ that branch is reachable **only when the counter SATURATES** = an oscillation is happening. Honda tapers Y with speed but the fallback is flat ⇒ **fallback/LERP = 0.93× at 5 km/h, 1.84× at 44 (his event), 4.17× at 90** ⇒ the term is driven into its **511 rail = `sign(α)·511`, a bang-bang Coulomb relay** (V80's mechanism). Explains the peak-turn oscillation, **grinding at 15-40 mph but never below 5-6 mph**, and ⭐ **resolves the INERT `0xCBE74` dose** — a saturated counter BYPASSES the mode record. ⚠ **V126 SUPERSEDED BY V127 — −3277 still RAILED at the arming threshold; artifacts DELETED.** V127 uses **−1966**: `0xC640A` −8192→−1966 (**Honda's own value at `0xC640C` for the same variable**) + a 427 probe on `gp-0x6B26` at sar 2 (rail → wire 638/1023, duty countable). 5 payload bytes, cal-only, **1 reader / 0 writers**, byte-scan **and** instruction-boundary verified; identical to V124 whenever the counter is not saturated. 56/56, CRC 50/50. 🛑 [BELIEF] that it de-rails on-car — duty is **unpredictable open-loop** (V107 missed by 32×, closed loop), hence the probe.

- ✅★★★★★ [**Oscillation-response census: `0xC640A` is the ONLY magnitude**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the saturated counter `gp-0x671a` has **8 sites / 5 consumers**: `0x36C1E` picks the fixed −8192 **(the only MAGNITUDE change)**; `0x3AA70` flips an enable flag **1→0, DISABLING** a lane; `0x3A4A6`'s LERP `0xC67B0` is **FLAT UNITY [1024,1024,1024] with X[0]=5 at the counter's own CEIL ⇒ DOUBLY INERT**; `0x35A06`/`0x35BEA` are boolean `setfnc` gates. ⇒ **V126 targets the right cell and there is no second hidden gain jump.** 🛑 **CORRECTION** to [[accord-gp671a-blast-radius-not-a-free-lever]]: the PID use is **INERT, not "the worrying one"** — lowering `T` does NOT reshape the PID; the "five things at once" is **two live + three inert/boolean**. 🛑 **CORRECTION** to my own V126 framing: Honda's response is **coherent** (disable a lane + strong fixed damping) and **the defect is the 511 RAIL under our elevated gain**, not Honda's intent ⇒ −1966 is a **floor, not a target**. 🛑🛑 **SCAN TRAP**: scanning disp `0x98E6` gave **1 hit** and nearly retracted a correct record — **every reader is `ld.bu`, which stores disp as `0x98E7`** (`hw2 = disp|1`). Scan **both** forms; a LOW count is a symptom of this trap too. Ghidra `get_xrefs_to` on the absolute RAM address returns **nothing** — gp-relative is never resolved.

- 🛑🛑★★★★★ [**V126 was sized wrong — V127 is the build**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the detector reads **`gp-0x6c2c`, the MOTOR-RATE DERIVATIVE, not driver torque** (`ld.h -0x6c2c,gp,r10` @`0x428FA` vs `cal(0xC620A)=12800` @`0x42910`) ⇒ **it fires HANDS-OFF**, and it is the **same signal `FUN_00036c12` multiplies by Y** — the positive feedback stated exactly. 🛑 The branch is only taken **after** the detector arms, so Y must be sized against the ARMING THRESHOLD: `b26@arm` = **1706 railed** (−8192) · **682 railed** (−3277, V126) · **409 linear, 80 % of clamp** (−1966). ⇒ **−3277 STILL RAILS ⇒ V126 would have left the relay in place**; artifacts **DELETED**. ✅ **V127** = −1966, the largest Honda-shipped value linear at arm (its own Y[2]); the fallback now never exceeds the schedule at any speed. **The sizing gate is a HARD ASSERTION** that fails −3277. 60/60, CRC 50/50. ⭐ **The NORMAL LERP also rails at mid speed** (44 km/h rails from `|c2c|`=7070; 24 km/h from 5691, both under 12800) ⇒ **the term rails in ordinary 15-40 mph driving too** — `0xCBE74` is the bigger lever, deliberately NOT taken here.

- 🛑🛑🛑★★★★★ [**THE GRINDING IS OURS — we tripled the ENGAGED-mode damper at V96/V106; V128 restores it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `gp-0x6b26`'s `Y` is a **per-mode** LERP via `0xCBE74`; **MANUAL=(24,25), ENGAGED=(26,27)**. Mode-26 `Y`: **STOCK [−9830,−5734,−1966]** (byte-identical to mode 24) vs **ours [−29490,−17202,−16000]** since V106 ⇒ rail threshold on `|gp-0x6c2c|` fell **3195/5477/15974 → 1065/1826/1963 = 3.0×/3.0×/8.1×**. The detector arms at **12800**, so stock was **LINEAR at the arm point at 90 km/h** while ours **rails from 1963 — essentially always** ⇒ a bang-bang Coulomb relay. ✅ Unifies four things: the symptom is **ENGAGED-ONLY and so is the dose** (manual 24/25 byte-stock on every build); **α2 works by DE-RAILING** (it shrinks `gp-0x6c2c`, the term's INPUT — which is why 22→14→8 tracked his reports); 🛑 **V91/V92's ×1.5 dose was INERT because the term was ALREADY RAILED and the clamp absorbed it** (**supersedes** the counter-bypass explanation); and V107's 32.32 % rail duty / *"symptom map = rail-duty map"*. ❌ **V128 RETRACTED AND DELETED — see the V129 entry** (it was V127 + modes 26/27 **restored to STOCK**, 12 payload bytes, 71/71, CRC 50/50; manual rows asserted byte-identical. **No confound with V127**: `0xC640A` fires only when the counter is SATURATED, the record only when it is NOT — **mutually exclusive**. 🛑 [BELIEF] that it helps: it removes 3–8× of feedback below the old rail point. If grinding WORSENS → **partial** restore sized to measured duty, never back to −29490.

- 🛑★★★★ [**The LKAS setpoint clamp sits EXACTLY on the SM2 arming threshold**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — full V128-vs-stock audit (302 payload bytes / 112 runs). `arb_setpoint_limit` `0xE4194` was **15360 on stock and V31**, **16384 since V38**, while SM2's arm `0xC6422` is **16384** ⇒ **margin 1024 (6.2 %) → 0**. Honda ships that clamp one step under its own trip, **the same idiom as the `gp-0x6b26` clamp at 511 under the 512 monitor trip** — and we removed the margin in one of the two. SM2 arms on `demand > 16384` so a clamp *at* 16384 does not itself cross, but there is **zero headroom**, and V19's record shows the SM does arm on 2× builds. ⚠ **NOT BUILT**: restoring costs **6.7 % of top-end setpoint** against the operator's stated priority and **no EME is currently reported**. **Test that settles it**: raw CAN 399 `STEER_STATUS` cuts during a hard engaged turn. 🛑 **CORRECTION**: the 15360→16384 block is **not** an unexamined change — it is V38's documented `arb_setpoint_limit` lever (LEDGER G6, 8 of 12 records raised); **only the SM2-margin observation is new**. ✅ **No second instance of the V128 relay pathology exists** — the engaged-mode `Y` record was the only raise that pushed a term past a clamp.

- 🛑🛑🛑★★★★★ [**V128 RETRACTED — restoring to stock would have undone the kit's BEST measured fix; V129 is the build**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — [[accord-v106-extinguished-the-mode-at-low-speed]]: V106's ×3 raise **EXTINGUISHED** the 21–27 Hz mode (prominence **1.51 vs stock 1.46**, argmax following the band edge; the **only** band-power result ever to clear its own split-half null, 0.347 vs [0.482,1.982], positive control 5.735). ⊕ **A Coulomb relay is a STRONG nonlinear damper** ⇒ *"railed"* and *"extinguished the mode"* are **both true**; my *"railed ⇒ restore to stock"* skipped the measurement. ✅ **The real defect is V107's HIGH-SPEED escalation**: V106 `Y[2]` = −5898 (rails from **5325**) but **V112 inherited V107's −16000** (rails from **1963**) ⇒ the car has **never run the configuration that measured the extinction**. ⚠ **V129 HELD, not recommended — see the product entry** = `Y[2]` −16000→−5898 on modes 26/27, **4 payload bytes**, giving **exactly V106's row**; `Y[0]`/`Y[1]` asserted UNTOUCHED, manual modes byte-stock, 75/75, CRC 50/50. 🛑 **LESSON**: *"the arithmetic says it is railed"* does **not** license *"restore it"* — whether a relay helps is EMPIRICAL and this kit had already answered it on-car. **Search the flight record before acting on a structural argument** ([[feedback-search-the-kit-before-naming-a-cause]]).

- 🛑🛑🛑★★★★★ [**α2 and Y MULTIPLY — the α2 ladder silently halved the damper**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — effective damping is `|H_lane(α2)| × |Y|` and **no build ever held the product constant**. At 21.73 Hz, vs V106 (the only configuration that MEASURED the mode extinguished): STOCK **0.33×**, V106 **1.00×**, V112 0.92/0.92/2.50, V122 0.74/0.74/1.99, **V124/V127 0.54/0.54/1.46 ← on the car**, V129 0.54/0.54/**0.54**. 🛑 **CORRECTION ×2**: (a) the α2 ladder 22→5 is **not only a de-rail, it is a DAMPING CUT to 0.54×** — the two effects oppose and no session separated them; (b) **V129 is the WRONG DIRECTION** — it drops high-speed damping to 0.54× while the build carries **more excitation than V104** (8× vs 6×), and V104 is the build that showed a **pinned line, prominence 6.89**. 🛑 The product **cannot** be restored at α2=5: `Y[0]` would need **54,733, past int16** ⇒ **only α2 recovers low-speed damping**. ✅ **FLY V127** (oscillation branch + probe, no `Y` knot touched); **V129 HELD** — unsized, not wrong. Direction is a MEASUREMENT: rail duty **cannot** be predicted open-loop (V107 missed 32×, closed loop). Rule: **>10 % → V129**; **≤2 % → raise `Y[2]` toward −32767 and/or α2 5→8**; **2–10 % → size to the duty**.

- ✅★★★★★ [**Both fork branches built — V129 (Y down) and V130 (Y up); V127 flies and decides**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — for `gp-0x6b26`, **damping and railing are ONE knob** (both `|H(α2)|×Y×raw`) ⇒ the **511 clamp is a hard ceiling on deliverable damping**. α2 changes the SHAPE: 22→5 costs **0.538× in-band** but cuts **broadband RMS to 0.263×** ⇒ **2.05× more in-band damping per unit of rail-driving content**, so restoring V106's damping at α2=5 costs **less** rail duty than V106 paid — an argument to RAISE Y. 🛑 But **V107 raised Y and its rail-duty map matched the symptom map**, at α2=22 where broadband was 3.8× higher. **Conflicting, and only the probe separates them.** ✅ **V130** = `Y[1]` −17202→−31923, `Y[2]` −16000→−29692 (×1.856 = |H|(22)/|H|(5)), `Y[0]` held (int16 blocks it; no low-speed symptom), 8 bytes, 83/83; **it rails EARLIER by design** (1963→1058) and flattens Honda's taper ⇒ **gated, not recommended**. **Rule: >10 % → V129; ≤2 % → V130; 2–10 % → size to the duty.** ✅ **Builder defect fixed in 11 builders**: the diff classifier tested whether a run CONTAINS a CRC trailer start instead of OVERLAPS `[t,t+4)`, so a 3-of-4-byte trailer change read as payload and failed a correct build; emitted artifacts re-run to identical SHAs.

- 🛑🛑🛑★★★★★ [**V62's grind fix has been OFF THE CAR since ~V80, and a GUARD kept it off — V131 restores it**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `0x3AB76`/`0x3AC20` are **0xaa (STOCK) on V80…V130**; V62 set both to **0xa9** (`sar 0xa→0x9`). V62 measured **18-22 Hz ×0.124 [0.036,0.387] = 8× better, 42× at |rate| 16-32 °/s**, a **30-40 Hz negative control at ~1.0**, ST==4 on **0 of 86,278** frames, operator *"Original grinding at 2-5 mph is gone!"* — **the kit's strongest measured anti-grind result, absent for ~50 builds.** 🛑🛑 **A GUARD ENSHRINED IT**: `V106B.FROZEN` lists both as *"V62's edit is ABSENT (stock). Carried"*, so every later builder **asserted the fix stayed OFF**. ⭐ **RULE: a FROZEN entry must record WHY — "absent (stock), carried" describes a LOSS and was read as a DECISION.** Same family as [[accord-v42-ratchet-fix-lost-since-v53]], but worse. ✅ The stated reason for dropping it (*"r24 half caused grind #2"*) is **not established**: **n=1**, one 0.92 s burst, V62's CI **inside** V59's, V61 **72×** V62, **p = 0.51**. ✅ **V131** = V127 + both bytes restored, 2 payload bytes, 68/68, instruction-verified (`aa42`, LE ⇒ immediate is the FIRST byte; register byte asserted untouched). **Fork-independent** — it is the RATE lane, not `gp-0x6b26`'s ⇒ composes with V129 or V130, and one drive scores both the grind symptom and the rail-duty probe.

- ✅★★★★★ [**Lost-fix audit closes clean — V131 carries EVERY measured-good edit**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — checked byte-by-byte: V42 `0x454FE`, V62 `0x3AB76`/`0x3AC20` (restored), V88 `0x3AA96`, Lever B `0xC6446`, V106 `Y[0]`/`Y[1]`, V112 knee/K1, V14 clamps — **11 of 11 present, nothing else lost**. 🛑 **FALSE POSITIVE corrected**: V33's `0xC6312` 320→65535 is **NOT** a measured fix — **V33 was never flashed**, and V37 resolved the gentle-EME via the **debounce SM** instead ⇒ stock 320 is CORRECT and raising it would be untested, not a restoration. ⭐ Of 74 frozen/tracked cells, **6 descriptions record a LOSS or a null rather than a decision** — notably `0xC6446`: *"the ONLY measured fix on the car. **Reverted 3x at rebases**"* (present at 5244) ⇒ **the V131 rule generalises: a frozen entry must state WHY; "carried"/"absent" is not a why.** ⚠ OPEN: `0x454FE` is called **"MEASURED INERT"** by the builder but *"the confirmed root cause"* by [[accord-v42-flashed-ratchet-fixed-r26-falsified]] — the byte is present either way, so nothing is at risk; recorded, not resolved.

- ✅★★★★ [**Lever A × Lever B multiply — but V62 already flew the stack; V131 stands**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `out = (r1 × arm) >> sar` with `0xC6446` read @`0x3AC08` and the `sar` @`0x3AC20`, **24 bytes apart in one expression** ⇒ arm and sar **compound**. ⊕ **r26 has its OWN arm `0xC643E`** @`0x3AB68`, so the r26 `sar` edit **cannot** compound with Lever B. 🛑 **I was WRONG that V62 flew at `0xC6446` = 512**: the V62 IMAGE has **5244**, so V62's r24 total was already **20.48×**, and **V131 reproduces V62's rate lane byte-for-byte** (all 6 cells identical) ⇒ the stack is the very configuration that measured **8–42× better, `ST==4` on 0 of 86,278 frames**. The *"2× ≈ optimum"* note is about the **`sar` step alone**, not the lane total. ⊕ `0xC6446` is non-monotone: 5244 at V62, **512 at V87**, 5244 from V88 — matching its own note *"Reverted 3× at rebases"*. ⭐ **METHOD RULE: when a risk depends on what a PRIOR BUILD contained, read that build's IMAGE, not the record of it** — the narrative said 512, the bytes said 5244, and I nearly revised a correct build on the narrative.

- 🛑★★★★ [**Build set stated exactly; V129/V130 LACK Lever A; the base-assist damper is neither closed nor usable**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **GAP 1: V129 and V130 are built on V127**, so `0x3AB76`/`0x3AC20` are **aa/aa** ⇒ after V131 flies the winning branch must be **REBUILT ON V131**, or V62's 8–42× fix is silently dropped **again**. **GAP 2:** the *"task 5 = 100 Hz ⇒ the base-assist damper cannot damp 21 Hz"* claim is **RETRACTED** (its own header says so) — the RTOS handler was identified on an **address coincidence** and flown `gp-0x6bbe` telemetry misses the 100 Hz prediction by **~7 dB** (measured −1.2 dB vs −8.4 predicted at 7.79 Hz). ⇒ **task 5's rate is OPEN**: the damper is **not ruled out**, and **not sized** — do neither. ✅ **task 1 = 1 kHz survives**, so `FUN_0003aa2c`/`FUN_0003a382` (Lever A's home) are confirmed. ⭐ **The bound on grinding**: `gp-0x6b26` damping is capped at **511** by `0xC407E`, which **cannot be raised** (V73→V74/V75 hard-faulted), and **damping = railing is one knob**, so the lane has a hard ceiling; at 8× the build sits at **0.54×** of V106's in-band product with `Y[0]` blocked by **int16**.

- 🛑🛑🛑★★★★★ [**The 511 ceiling has a FLOAT TWIN — it is raisable, and that explains V74/V75**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00036d74` monitors `gp-0x6b26/1024` against **float cal `0xC4004`**, shipped MATCHED to the int clamp `0xC407E`: **511 ↔ 0.5** (`0.5×1024 = 512 = 511+1`). 🛑 **V73 raised the int to 850 and left the float at 512** ⇒ every excursion past 512 fired the monitor ⇒ **that is why V74/V75 HARD-FAULTED** — the same int/float desync as DTC `0xF00049` in V21–V24. ⇒ *"never raise `0xC407E`"* is **HALF RIGHT: never raise it ALONE.** ✅ **V132** = V131 + the **matched pair** `511/0.5 → 1023/1.0`, **2 payload bytes**, 73/73, with `float×1024 == int+1` as a HARD GATE and the float asserted against its **encoding** `0x3F800000`. ⭐ **It is the only move in this lane that is NOT a trade**: linear damping `|H|×Y` unchanged, rail threshold **2.00× later at every speed knot** (1963→3930 at 90 km/h). ✅ Both cells **private** (3 readers each, all in their own function); 🛑 **5 of 11 raw scan hits were FALSE POSITIVES** (`0x07028` is a `dispose`; `0x0B8DE` is `mov 0xfedf5004` — a RAM address, not a displacement) — I nearly abandoned the lever on them.

- ✅★★★★★ [**A twin verifier that rediscovers both historical fault classes**](../../../../Desktop/Projects/accord-eps-torque-mod/analysis-2020accord/verify/verify_int_float_twins.py) — checks Honda's invariant **`float×1024 == int`** on every documented mirror family. It FAILS **_v73/_v74×3/_v75×2/_v76/_v77/_v77b** (int 850 / float 512) and **_v25–_v28**, and PASSES V124–V132 and stock. ⭐ **Every image it flags actually hard-faulted on the car** — V74/V75 via the b26 twin, V25–V28 via the corridor/envelope twin — and it was **not tuned to find them**. 🛑 **Run it on every built image before flashing**: a desync does not fail at build time or on the bench, only on the road. ✅ V132 is clean on all three families; the corridor and boost pairs were already lockstepped, **only the b26 ceiling had never been lifted**. 🛑 METHOD: my first pass paired int↔float **by value** across the cal region and produced **353 KB of nonsense** (cave `0xFF`=−1 pairing with an unrelated 0.0011) ⇒ **value similarity is not a structural claim**; a pair is real only when a monitor is traced that COMPARES them.

- 🛑🛑★★★★★ [**V132 would have broken its own measurement — V133 is the build**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — V132 raised the clamp **511→1023** but left the packer at **sar 2**, sized for 511 ⇒ the **wire saturates at |x| = 819, BELOW the new clamp**, so the scorer would report *"railed"* at 819 and be **blind between 819 and 1023** — destroying the one number the V129-vs-V130 fork depends on, **while looking like it worked**. Artifacts DELETED. ⭐ **RULE (distinct from V126's mis-sizing): when a build changes a quantity's RANGE, re-derive every probe that measures it IN THE SAME BUILD** — the edit and the instrument were sized against different assumptions. Now enforced by a **PROBE GATE** assertion plus one recording that sar 2 clips at 819. ✅ **V133** = V131 + clamp `511/0.5 → 1023/1.0` + **sar 2→3** (1023 → wire 639/1023, LSB 1.6), **3 payload bytes**, 75/75, CRC 50/50, passes `verify_int_float_twins.py`. Score it with **`ACCORD_RAIL_V133=1`**.

- 🛑🛑★★★★★ [**1024 is a HARD CEILING on `0xC407E` — V133's 1023 is the maximum, by one count**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00038148` admits `gp-0x6b26` into the `gp-0x6b70` sum only while **`(x + 0x400) < 0x801` ⇔ |x| ≤ 1024**; outside it the multiplier is **literally 0**, so the damper **VANISHES from that sum** while the aggregator at `0x3AC98` still sees it — an asymmetric failure nearly impossible to diagnose from a drive. **1023 ADMITTED · 1024 ADMITTED · 1025 ZEROED.** ⊕ Sibling windows: `gp-0x6b4e`/`6b4c` **±10240**, `6bd0`/`6bbe` **±2048**, `6b26`/`6b46` **±1024** (tightest). ⭐ **This is why Honda ships 511** — inside a ±1024 gate with its monitor twin at 512 just above. 🛑 A future build trying **2047** would silently zero the damper; the builder now asserts both that 1023 is admitted and that **1025 would be zeroed** (*"NEVER raise 0xC407E above 1024"*). ⇒ **the gp-0x6b26 lane is now fully bounded and fully exploited** — no headroom remains in it.

- ⭐★★★★ [**`0xC63A6` — a virgin weight that multiplies the damper PAST both ceilings, NOT built**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00038148` applies per-term cal weights `0xC63A0`–`0xC63AA` **after** the 1023 clamp and the ±1024 admission gate; all six are **unity (1024) on stock and every build**, only `0xC63A0` ever moved (V72 2048, V77 back). ⇒ `0xC63A6` is the escape from the exhausted-lane bound. 🛑 **NOT BUILT — fails GATE 2**: these weights are **LOOP GAIN** ([[accord-path2-is-a-closed-firmware-loop-and-c63a0-weights-it]] — sum → `gp-0x6b70` → PID → aggregator → `gp-0x6b98` → **re-enters via `FUN_0003b8f6` one sample later**), and the loop's IIR corner is **16.70 Hz** (`0xC63AC`=102) so grind #1's 21–26 Hz is **past the corner** where added gain destabilises. ⚠ The one datum — V83a **lowered** sibling `0xC63A0` and flew **the worst build in the modern lineage** (grind #1 **2.674× V81 [1.956,3.885]**, 10/10 cells > 1) — **does NOT attribute**: V83a moved THREE things and left mode 27 carrying V81's damper. ✅ **What opens it**: V133's probe showing LOW rail duty **and** grinding persisting ⇒ the damper is under-delivering, not relay-ing, and `0xC63A6` (×2 = **+6.02 dB**, zero cost to Path 1) is the only remaining authority — with the phase question answered on `FUN_00038148`'s **measured** transfer, not its structure.

- ✅★★★★★ [**The `gp-0x6b26` damper is STRUCTURALLY EXHAUSTED — definitive**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the aggregator (Path 1, the **zero-phase** route that actually delivers damping) adds `gp-0x6b26` through a chain of **BARE `add` instructions** (`0x3ACC8`–`0x3ACDA`) ⇒ **there is NO weight cell in Path 1 — nothing to raise.** The only weight is `0xC63A6`, in Path 2, which fails GATE 2 on phase. 🛑 And the **±1024 admission gate binds in BOTH consumers** (`0x3ACB0` aggregator **and** `0x3815C` `FUN_00038148`), so **1024 is unambiguously final** and V133's 1023 stays admitted in both. ⇒ **Every knob is now at its bound**: α2 = 5 (a trade), `Y` = the fork (probe decides), ceiling **1023/1.0 = maximum**, Path-1 weight **nonexistent**, `0xC640A` sized linear at arm. **No further damping is available from this term by ANY calibration in EITHER path** — do not re-open the lane. ⭐ It converts *"try more damper doses"* into a bounded question: **probe LINEAR + still grinding ⇒ `0xC63A6` with phase measured; probe RAILING ⇒ V129 rebuilt on V133.**

- ✅★★★★★ [**The displacement scan is FIXED — and it closes `0xC642A/C`**](../../../../Desktop/Projects/accord-eps-torque-mod/analysis-2020accord/verify/tp_cal_readers.py) — the kit's standard census (scan the disp16 in both `|1` forms) is **up to 98 % noise**: `0xC642A` **raw 52 → real 1** (`0x3A118` was `cmpf.s`), `0xC4004` 7→3 (`0x0B8DE` was `mov 0xfedf5004, r8`, a RAM **address**), `0xC407E` 4→3 (`0x07028` was `dispose`). ✅ **FIX**: V850 Format-VII encodes the base register in the **low 5 bits of hw1**, so require **`(hw1 & 0x1F) == 5`** — it reproduces **every** hand-verified census exactly. Shipped as `verify/tp_cal_readers.py`. ⚠ A filter, **not a proof**: blind to the 6-byte form and to absolute-address register loads — confirm survivors in Ghidra. 🛑 **`0xC642A/C` CLOSED as a grind lever**: one reader each, both in `FUN_0002b62c` (**base assist**), both **EMA alphas on its INPUTS** (`0xC642A` filters **driver column torque** `gp-0x4f60`) ⇒ they change **manual feel**, not broadband HF in the delivered command. ✅ This also retires **V125's original probe question**, which was about a base-assist cell all along.

- 🛑🛑★★★★★ [**The calibration search for grinding is BOUNDED — V133 is its endpoint**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — the delivered shaper/integrator region `[0x43100,0x43280)` reads only **five** cals and **not one is a filter**: `0xC41E0` 257, `0xC47F0` 0, `0xC61DC` 30720 (SM3 arm), `0xC64C8` 0 (mode switch — mode 1 **discards** the aggregator), `0xC61D4` 0 (adjacent to the slew cell V16 was rejected for; nonzero **activates an uncalibrated map**). ⇒ **V87's lever class — *less broadband HF in the delivered command* — has NO calibration implementing it**, agreeing with *"no notch exists anywhere"* (V44) and *"no biquad"*. ✅ **Full picture**: b26 lane **exhausted (provably)** · rate lane **at V62's measured-good config** · HF filtering **no cal** · `0xC642A/C` **closed** · `0xC63A6` **blocked on GATE 2** · the `Y` fork **needs one number** · base-assist damper **neither closed nor sized**. ⇒ every avenue is exhausted, closed, or gated on a measurement. 🛑 **No further build should be cut before a V133 drive** — three builds this session were superseded pre-flight (V126 mis-sized, V128 contradicted a measured result, V132 broke its own probe); the next defect of that class **will be found by the car, not by more analysis**.

- 🛑🛑🛑★★★★★ [**Task 5 is ≥ 250 Hz — the base-assist damper CAN act at 21–26 Hz**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00034350`'s sole caller `FUN_00022ca0` has **no callers** ⇒ a task entry; it and the confirmed 1 kHz task sit in one **TCB array, stride 0x60** (`0xBB9E8` vs `0xBB928`). 🛑 That is structure, **not a rate** — inferring one from field positions is the exact error the 2026-08-12 retraction documents. ✅ **BOUNDED EMPIRICALLY instead**: `gp-0x6bbe`'s EMA α = 205/1024 measured **≈ −1.2 dB** at 6–9 Hz with step τ ∈ [0,20] ms on route 79 ⇒ **100 Hz EXCLUDED (off 6.1 dB, τ = 50 ms), 200 Hz EXCLUDED (2.1 dB, 25 ms), 500 Hz CONSISTENT** ⇒ **≥ 250 Hz, Nyquist ≥ 125 Hz** ⇒ the *"structurally cannot damp 20.9 Hz"* claim is **empirically excluded**, not just retracted. ⚠ Assumes `gp-0x6bbe`'s EMA is in task 5 (inherited), and both statistics share one route. ✅ The damper is at **Honda's stock cal** (V80 modified, V84 reverted): FactorC m26 `X=[35,60,80,140] km/h`, `Y=[0,234,429,908]` ⇒ **DEAD below 35 km/h**, i.e. across 24–35 km/h, the lower half of the 15–40 mph grinding band. ⭐ **CANDIDATE**: raise `Y[1..3]`, keep `Y[0]=0` and the **monotone ramp** — V80 flattened it to 566 (a **plateau = RELAY**) and produced the **worst grinding on record**; its own rule is *"restore the RAMP, don't merely lower k"*. 🛑 **GATE NOT YET RUN**: `FUN_00034350` has a **ceiling clamp** — the product must be bounded below it first or the raise recreates a relay by saturation, **the identical `gp-0x6b26` failure on a different term**.

- 🛑🛑★★★★★ [**The base-assist damper is ALREADY a relay — lane closes, V80 explained**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — `FUN_00034350`: `gp-0x6bd0 = clamp(FactorB·C·D·E·F, ±LERP(gp-0x6ac2)[mode])`, and that ceiling is **Y=[512,1024] on ALL of modes 24/25/26/27** while the stock product reaches **~2216** ⇒ **the damper already SATURATES at high speed on STOCK.** Raising FactorC therefore **widens the relay region** rather than adding damping — **which is exactly what V80 measured** (flat FactorC 566 → worst grinding on record; its *"no-clip gate is blind to `= ceiling − 17`"* note IS this clamp). ⚠ FactorE read as `X=[140,539,927,0]` ⇒ field offsets off by one ⇒ **2216 approximate**; ordering robust since the ceiling is 512–1024 and single factors reach 908–2500. ✅ The **task-5 rate objection is dead** (≥250 Hz), so the blocker moved from *wrong physics* to **capped authority**. 🛑 **BOTH damping lanes are capped the same way**: `gp-0x6b26` clamp+±1024 gate (**at max on V133**) and `gp-0x6bd0` ceiling 512–1024 (**already saturating**) ⇒ **the firmware has no damping headroom left**, and forcing more converts a damper into a relay — measured **three times** (V74/V75, V80, V107).

- 🛑🛑🛑★★★★★ [**The damper NEVER railed — a direct measurement that was sitting in the cache**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **route 77 = V90** put `gp-0x6b26` on 427 at **sar 3**, the same tap V133 carries. 52,258 engaged frames, wire unclipped (saturates at 1637 vs the 511 clamp): **p99 109, max 296, RAIL DUTY 0.0000 % in EVERY speed bin** — p99 is 21 % of the clamp. 🛑 **This contradicts the 32.32 % at 10–25 km/h** that motivated the whole relay story: that figure was **RECONSTRUCTED**, not measured, and the direct wire reads **0.0000 %** in that exact bin. ⚠ **Two consequences against my own builds**: the *railed → relay → grinding* premise for `gp-0x6b26` is **weakened**, and **V133's 511→1023 ceiling raise may be INERT** (harmless, but possibly buying nothing). 🛑 It does **not** settle it — V90 ran **stock `Y` and α2=22** vs V133's **×3 `Y` and α2=5**, and scaling between them is the **forbidden open-loop move** (V107 missed 32× doing it). ⭐ **It points the fork**: the pre-registered rule says ≤2 % ⇒ LINEAR ⇒ **fly V130 (`Y` up)**, so V130 is now favoured and **`0xC63A6` becomes the LIKELY next lever**. ⊕ **METHOD**: the answer was already in the cache — **before designing a probe, check whether a past build already flew the signal.**

- 🛑🛑★★★★★ [**The oscillation detector's threshold is ~11× the observed p90 — `0xC640A` may be INERT**](../../../../Desktop/Projects/accord-eps-torque-mod/docs/STATE.md) — **V107–V110 flew `gp-0x6c2c` on 427**, and `r1e` is V107: 48,047 engaged frames, **p50 125, p90 1170**, and **3.15 %** clip at the wire rail 1637. The detector arms at `cal(0xC620A)` = **12800**, i.e. **7.8× above what the wire can represent and ~11× p90**. 🛑 P(arming) is therefore **NOT measurable from r1e — only BOUNDED at ≤3.15 %**, and the steep unclipped tail makes 12800 look unlikely (an inference, not a measurement). ⚠ **Consequence against my own builds**: the `0xC640A` branch may be taken **almost never**, making V126/V127's central premise — and that part of V133 — **close to inert**. The V131 pre-registration already flagged this as a confound; **I had treated it as drive planning, not as the likely steady state.** ✅ **What settles it cheaply**: a `gp-0x6c2c` probe at **sar 6** puts 12800 at wire **1000/1023**, making the threshold directly visible — a 2-byte packer edit, no cave. 🛑 **Not built**: it would cost the `gp-0x6b26` rail measurement that decides the V129/V130 fork, which is the higher-value question. ⊕ **Two open questions answered from data already on disk** (r77, r1e) — neither needed a new drive.
